//! X86 emitter traits, generated by `meta/x86_emitter_gen.py` from
//! `meta/x86_emitter.txt` (AsmJit's `x86emitter.h` declarations). Do not edit by
//! hand; regenerate instead.
//!
//! Conventions:
//! - One `{Name}Emitter<T0..Tn>` trait per (mnemonic, arity) pair. `Assembler<'_>`
//!   implements it once per declared concrete operand tuple, forwarding to
//!   `emit_n(InstId::{Id} as u32, &[...])`. Unlike the legacy `features/*` API there
//!   are no inherent forwarding methods, so both APIs can coexist.
//! - Both `[EXPLICIT]` (fixed-register operands spelled out) and `[IMPLICIT]`
//!   (fixed registers hidden) input declarations are kept. When a mnemonic is
//!   declared with more than one arity, the first-seen arity keeps the plain name
//!   and later arities are suffixed: the 1-operand explicit `cbw` keeps its name,
//!   the 0-operand implicit form becomes `cbw_0`.
//! - AsmJit fixed-register alias operands (`Gp_AX`, `DS_ZSI`, `XMM0`, ...) are
//!   collapsed to their base operand kind (`Gp`, `Mem`, `Vec`); operand tuples
//!   that become duplicates after collapsing are dropped (first one wins).
//! - Besides the abstract-kind impls (`MovEmitter<Gp, Gp>`), impls are generated
//!   for the concrete sized register wrappers (`Gpq`/`Gpd`/`Gpw`/`GpbLo`/`GpbHi`,
//!   `Xmm`/`Ymm`/`Zmm`, ...) so register constants work without dereferencing
//!   (`a.mov(RAX, RBX)`). Only width combinations present in the instruction's
//!   instdb signatures are generated. Immediate positions take `U: Into<Imm>`,
//!   so integer literals work too (`a.mov(RAX, 42)`); when a sized impl would
//!   overlap its abstract `Imm` counterpart, the generic one replaces it.
//! - The conditional families `j`, `set` and `cmov` take an explicit [`CondCode`]
//!   translated through the `JCC_FROM_COND` / `SETCC_FROM_COND` /
//!   `CMOVCC_FROM_COND` tables below, plus one named method per x86 condition
//!   suffix (`jo`, `jno`, ...) that calls a concrete `InstId::{Id}{Suffix}`.
//! - Rust keywords are raw-escaped (`r#loop`, `r#in`); names already C++-escaped
//!   in the input (`and_`, `or_`, `not_`, `xor_`, `int_`) are kept as-is.
//! - Rustdoc lists compact assembly forms derived from pinned AsmJit metadata.
//!   When `asm-docs/` is available, its Intel-derived summary and reference link
//!   are included as well.
#![allow(non_snake_case, non_camel_case_types)]
use super::{assembler::*, instdb::*, operands::*};
use crate::core::globals::CondCode;
use crate::core::operand::*;

/// Translates asmkit's ARM-ordered [`CondCode`] to a `jcc`
/// instruction id, reindexing AsmJit's x86-ordered
/// `_jcc_from_cond_table` (x86globals.h). Conditions with no x86
/// form map to `InstId::None`.
pub(crate) const JCC_FROM_COND: [u32; 16] = [
    InstId::Jmp as u32,  // AL: unconditional jump
    InstId::None as u32, // NA: no x86 equivalent
    InstId::Jz as u32,   // EQ
    InstId::Jnz as u32,  // NE
    InstId::Jnb as u32,  // CS
    InstId::Jb as u32,   // LO
    InstId::Js as u32,   // MI
    InstId::Jns as u32,  // PL
    InstId::Jo as u32,   // VS
    InstId::Jno as u32,  // VC
    InstId::Jnbe as u32, // HI
    InstId::Jbe as u32,  // LS
    InstId::Jnl as u32,  // GE
    InstId::Jl as u32,   // LT
    InstId::Jnle as u32, // GT
    InstId::Jle as u32,  // LE
];

/// Translates asmkit's ARM-ordered [`CondCode`] to a `setcc`
/// instruction id, reindexing AsmJit's x86-ordered
/// `_setcc_from_cond_table` (x86globals.h). Conditions with no x86
/// form map to `InstId::None`.
pub(crate) const SETCC_FROM_COND: [u32; 16] = [
    InstId::None as u32,   // AL: no x86 equivalent
    InstId::None as u32,   // NA: no x86 equivalent
    InstId::Setz as u32,   // EQ
    InstId::Setnz as u32,  // NE
    InstId::Setnb as u32,  // CS
    InstId::Setb as u32,   // LO
    InstId::Sets as u32,   // MI
    InstId::Setns as u32,  // PL
    InstId::Seto as u32,   // VS
    InstId::Setno as u32,  // VC
    InstId::Setnbe as u32, // HI
    InstId::Setbe as u32,  // LS
    InstId::Setnl as u32,  // GE
    InstId::Setl as u32,   // LT
    InstId::Setnle as u32, // GT
    InstId::Setle as u32,  // LE
];

/// Translates asmkit's ARM-ordered [`CondCode`] to a `cmovcc`
/// instruction id, reindexing AsmJit's x86-ordered
/// `_cmovcc_from_cond_table` (x86globals.h). Conditions with no x86
/// form map to `InstId::None`.
pub(crate) const CMOVCC_FROM_COND: [u32; 16] = [
    InstId::None as u32,    // AL: no x86 equivalent
    InstId::None as u32,    // NA: no x86 equivalent
    InstId::Cmovz as u32,   // EQ
    InstId::Cmovnz as u32,  // NE
    InstId::Cmovnb as u32,  // CS
    InstId::Cmovb as u32,   // LO
    InstId::Cmovs as u32,   // MI
    InstId::Cmovns as u32,  // PL
    InstId::Cmovo as u32,   // VS
    InstId::Cmovno as u32,  // VC
    InstId::Cmovnbe as u32, // HI
    InstId::Cmovbe as u32,  // LS
    InstId::Cmovnl as u32,  // GE
    InstId::Cmovl as u32,   // LT
    InstId::Cmovnle as u32, // GT
    InstId::Cmovle as u32,  // LE
];

/// Emits `ADC`.
///
/// Assembly forms: `adc al, imm8`, `adc axv, immv`, `adc r8/m8, r8`.
pub trait AdcEmitter<T0, T1> {
    fn adc(&mut self, dst: T0, src: T1);
}

/// Emits `ADD`.
///
/// Assembly forms: `add al, imm8`, `add axv, immv`, `add r8/m8, r8`.
pub trait AddEmitter<T0, T1> {
    fn add(&mut self, dst: T0, src: T1);
}

/// Emits `AND_`.
///
/// Assembly forms: `and al, imm8`, `and ax, imm16`, `and eax, imm32`.
pub trait And_Emitter<T0, T1> {
    fn and_(&mut self, dst: T0, src: T1);
}

/// Emits `BOUND`.
///
/// Assembly forms: `bound r16, m32`, `bound r32, m64`.
pub trait BoundEmitter<T0, T1> {
    fn bound(&mut self, lhs: T0, rhs: T1);
}

/// Emits `BSF`.
///
/// Assembly forms: `bsf rv, rv/mv`.
pub trait BsfEmitter<T0, T1> {
    fn bsf(&mut self, dst: T0, src: T1);
}

/// Emits `BSR`.
///
/// Assembly forms: `bsr rv, rv/mv`.
pub trait BsrEmitter<T0, T1> {
    fn bsr(&mut self, dst: T0, src: T1);
}

/// Emits `BSWAP`.
///
/// Assembly forms: `bswap rv`.
pub trait BswapEmitter<T0> {
    fn bswap(&mut self, dst: T0);
}

/// Emits `BT`.
///
/// Assembly forms: `bt rv/mv, rv`, `bt rv/mv, imm8`.
pub trait BtEmitter<T0, T1> {
    fn bt(&mut self, lhs: T0, rhs: T1);
}

/// Emits `BTC`.
///
/// Assembly forms: `btc rv/mv, rv`, `btc rv/mv, imm8`.
pub trait BtcEmitter<T0, T1> {
    fn btc(&mut self, lhs: T0, rhs: T1);
}

/// Emits `BTR`.
///
/// Assembly forms: `btr rv/mv, rv`, `btr rv/mv, imm8`.
pub trait BtrEmitter<T0, T1> {
    fn btr(&mut self, lhs: T0, rhs: T1);
}

/// Emits `BTS`.
///
/// Assembly forms: `bts rv/mv, rv`, `bts rv/mv, imm8`.
pub trait BtsEmitter<T0, T1> {
    fn bts(&mut self, lhs: T0, rhs: T1);
}

/// Emits `CBW`.
///
/// Assembly forms: `cbw <ax>`.
pub trait CbwEmitter<T0> {
    fn cbw(&mut self, dst: T0);
}

/// Emits `CDQ`.
///
/// Assembly forms: `cdq <edx>, <eax>`.
pub trait CdqEmitter<T0, T1> {
    fn cdq(&mut self, dst: T0, src: T1);
}

/// Emits `CDQE`.
///
/// Assembly forms: `cdqe <rax>`.
pub trait CdqeEmitter<T0> {
    fn cdqe(&mut self, dst: T0);
}

/// Emits `CQO`.
///
/// Assembly forms: `cqo <rdx>, <rax>`.
pub trait CqoEmitter<T0, T1> {
    fn cqo(&mut self, dst: T0, src: T1);
}

/// Emits `CWD`.
///
/// Assembly forms: `cwd <dx>, <ax>`.
pub trait CwdEmitter<T0, T1> {
    fn cwd(&mut self, dst: T0, src: T1);
}

/// Emits `CWDE`.
///
/// Assembly forms: `cwde <eax>`.
pub trait CwdeEmitter<T0> {
    fn cwde(&mut self, dst: T0);
}

/// Emits `CALL`.
///
/// Assembly forms: `call rel16`, `call rel32`, `call r16/m16`.
pub trait CallEmitter<T0> {
    fn call(&mut self, target: T0);
}

/// Emits `CMOV`.
///
/// Assembly forms: `cmovcc rv, rv/mv`.
pub trait CmovEmitter<T0, T1> {
    fn cmov(&mut self, cc: CondCode, dst: T0, src: T1);
    fn cmovo(&mut self, dst: T0, src: T1);
    fn cmovno(&mut self, dst: T0, src: T1);
    fn cmovb(&mut self, dst: T0, src: T1);
    fn cmovnb(&mut self, dst: T0, src: T1);
    fn cmovz(&mut self, dst: T0, src: T1);
    fn cmovnz(&mut self, dst: T0, src: T1);
    fn cmovbe(&mut self, dst: T0, src: T1);
    fn cmovnbe(&mut self, dst: T0, src: T1);
    fn cmovs(&mut self, dst: T0, src: T1);
    fn cmovns(&mut self, dst: T0, src: T1);
    fn cmovp(&mut self, dst: T0, src: T1);
    fn cmovnp(&mut self, dst: T0, src: T1);
    fn cmovl(&mut self, dst: T0, src: T1);
    fn cmovnl(&mut self, dst: T0, src: T1);
    fn cmovle(&mut self, dst: T0, src: T1);
    fn cmovnle(&mut self, dst: T0, src: T1);
}

/// Emits `CMP`.
///
/// Assembly forms: `cmp r8/m8, r8`, `cmp rv/mv, rv`, `cmp r8, r8/m8`.
pub trait CmpEmitter<T0, T1> {
    fn cmp(&mut self, lhs: T0, rhs: T1);
}

/// Emits `CMPS`.
///
/// Assembly forms: `cmps m8(ds:zsi), m8(es:zdi)`, `cmps m16(ds:zsi), m16(es:zdi)`, `cmps m32(ds:zsi), m32(es:zdi)`.
pub trait CmpsEmitter<T0, T1> {
    fn cmps(&mut self, lhs: T0, rhs: T1);
}

/// Emits `CMPXCHG`.
///
/// Assembly forms: `cmpxchg r8/m8, r8, <al>`, `cmpxchg rv/mv, rv, <axv>`.
pub trait CmpxchgEmitter<T0, T1, T2> {
    fn cmpxchg(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `CMPXCHG16B`.
///
/// Assembly forms: `cmpxchg16b m128, <rdx>, <rax>, <rcx>, <rbx>`.
pub trait Cmpxchg16bEmitter<T0, T1, T2, T3, T4> {
    fn cmpxchg16b(&mut self, dst: T0, dst2: T1, dst3: T2, src1: T3, src2: T4);
}

/// Emits `CMPXCHG8B`.
///
/// Assembly forms: `cmpxchg8b m64, <edx>, <eax>, <ecx>, <ebx>`.
pub trait Cmpxchg8bEmitter<T0, T1, T2, T3, T4> {
    fn cmpxchg8b(&mut self, dst: T0, dst2: T1, dst3: T2, src1: T3, src2: T4);
}

/// Emits `DEC`.
///
/// Assembly forms: `dec r16`, `dec r32`, `dec r8/m8`.
pub trait DecEmitter<T0> {
    fn dec(&mut self, dst: T0);
}

/// Emits `DIV`.
///
/// Assembly forms: `div <ax>, r8/m8`, `div <dxv>, <axv>, rv/mv`.
pub trait DivEmitter<T0, T1> {
    fn div(&mut self, dst: T0, src: T1);
}

/// Emits `DIV_3`.
///
/// Assembly forms: `div <ax>, r8/m8`, `div <dxv>, <axv>, rv/mv`.
pub trait Div3Emitter<T0, T1, T2> {
    fn div_3(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `IDIV`.
///
/// Assembly forms: `idiv <ax>, r8/m8`, `idiv <dxv>, <axv>, rv/mv`.
pub trait IdivEmitter<T0, T1> {
    fn idiv(&mut self, dst: T0, src: T1);
}

/// Emits `IDIV_3`.
///
/// Assembly forms: `idiv <ax>, r8/m8`, `idiv <dxv>, <axv>, rv/mv`.
pub trait Idiv3Emitter<T0, T1, T2> {
    fn idiv_3(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `IMUL`.
///
/// Assembly forms: `imul <ax>, r8/m8`, `imul <dxv>, <axv>, rv/mv`, `imul rv, rv/mv`.
pub trait ImulEmitter<T0, T1> {
    fn imul(&mut self, dst: T0, src: T1);
}

/// Emits `IMUL_3`.
///
/// Assembly forms: `imul <ax>, r8/m8`, `imul <dxv>, <axv>, rv/mv`, `imul rv, rv/mv`.
pub trait Imul3Emitter<T0, T1, T2> {
    fn imul_3(&mut self, dst: T0, dst2: T1, imm: T2);
}

/// Emits `INC`.
///
/// Assembly forms: `inc r16`, `inc r32`, `inc r8/m8`.
pub trait IncEmitter<T0> {
    fn inc(&mut self, dst: T0);
}

/// Emits `J`.
///
/// Assembly forms: `jcc rel8`, `jcc rel16`, `jcc rel32`.
pub trait JEmitter<T0> {
    fn j(&mut self, cc: CondCode, target: T0);
    fn jo(&mut self, target: T0);
    fn jno(&mut self, target: T0);
    fn jb(&mut self, target: T0);
    fn jnb(&mut self, target: T0);
    fn jz(&mut self, target: T0);
    fn jnz(&mut self, target: T0);
    fn jbe(&mut self, target: T0);
    fn jnbe(&mut self, target: T0);
    fn js(&mut self, target: T0);
    fn jns(&mut self, target: T0);
    fn jp(&mut self, target: T0);
    fn jnp(&mut self, target: T0);
    fn jl(&mut self, target: T0);
    fn jnl(&mut self, target: T0);
    fn jle(&mut self, target: T0);
    fn jnle(&mut self, target: T0);
}

/// Emits `JECXZ`.
///
/// Assembly forms: `jecxz <cx>, rel8`, `jecxz <ecx>, rel8`, `jecxz <rcx>, rel8`.
pub trait JecxzEmitter<T0, T1> {
    fn jecxz(&mut self, lhs: T0, target: T1);
}

/// Emits `JMP`.
///
/// Assembly forms: `jmp rel8`, `jmp rel16`, `jmp rel32`.
pub trait JmpEmitter<T0> {
    fn jmp(&mut self, target: T0);
}

/// Emits `LCALL`.
///
/// Assembly forms: `lcall imm16, imm16`, `lcall imm16, imm32`, `lcall m16_16`.
pub trait LcallEmitter<T0, T1> {
    fn lcall(&mut self, imm: T0, imm2: T1);
}

/// Emits `LCALL_1`.
///
/// Assembly forms: `lcall imm16, imm16`, `lcall imm16, imm32`, `lcall m16_16`.
pub trait Lcall1Emitter<T0> {
    fn lcall_1(&mut self, mem: T0);
}

/// Emits `LEA`.
///
/// Assembly forms: `lea r16, mem`, `lea r32, mem`, `lea r64, mem`.
pub trait LeaEmitter<T0, T1> {
    fn lea(&mut self, dst: T0, src: T1);
}

/// Emits `LJMP`.
///
/// Assembly forms: `ljmp imm16, imm16`, `ljmp imm16, imm32`, `ljmp m16_16`.
pub trait LjmpEmitter<T0, T1> {
    fn ljmp(&mut self, imm: T0, imm2: T1);
}

/// Emits `LJMP_1`.
///
/// Assembly forms: `ljmp imm16, imm16`, `ljmp imm16, imm32`, `ljmp m16_16`.
pub trait Ljmp1Emitter<T0> {
    fn ljmp_1(&mut self, mem: T0);
}

/// Emits `LODS`.
///
/// Assembly forms: `lods al, m8(ds:zsi)`, `lods ax, m16(ds:zsi)`, `lods eax, m32(ds:zsi)`.
pub trait LodsEmitter<T0, T1> {
    fn lods(&mut self, lhs: T0, rhs: T1);
}

/// Emits `LOOP`.
///
/// Assembly forms: `loop <cx>, rel8`, `loop <ecx>, rel8`, `loop <rcx>, rel8`.
pub trait LoopEmitter<T0, T1> {
    fn r#loop(&mut self, target: T0, target2: T1);
}

/// Emits `LOOPE`.
///
/// Assembly forms: `loope <cx>, rel8`, `loope <ecx>, rel8`, `loope <rcx>, rel8`.
pub trait LoopeEmitter<T0, T1> {
    fn loope(&mut self, target: T0, target2: T1);
}

/// Emits `LOOPNE`.
///
/// Assembly forms: `loopne <cx>, rel8`, `loopne <ecx>, rel8`, `loopne <rcx>, rel8`.
pub trait LoopneEmitter<T0, T1> {
    fn loopne(&mut self, target: T0, target2: T1);
}

/// Emits `MOV`.
///
/// Assembly forms: `mov r8/m8, r8`, `mov rv/mv, rv`, `mov r8/m8, imm8`.
pub trait MovEmitter<T0, T1> {
    fn mov(&mut self, lhs: T0, rhs: T1);
}

/// Emits `MOVABS`.
///
/// Assembly forms: `movabs r64, imm64`, `movabs al, moff8`, `movabs ax, moff16`.
pub trait MovabsEmitter<T0, T1> {
    fn movabs(&mut self, dst: T0, src: T1);
}

/// Emits `MOVNTI`.
///
/// Assembly forms: `movnti my, ry`.
pub trait MovntiEmitter<T0, T1> {
    fn movnti(&mut self, dst: T0, src: T1);
}

/// Emits `MOVS`.
///
/// Assembly forms: `movs m8(es:zdi), m8(ds:zsi)`, `movs m16(es:zdi), m16(ds:zsi)`, `movs m32(es:zdi), m32(ds:zsi)`.
pub trait MovsEmitter<T0, T1> {
    fn movs(&mut self, lhs: T0, rhs: T1);
}

/// Emits `MOVSX`.
///
/// Assembly forms: `movsx rv, r8/m8`, `movsx ry, r16/m16`.
pub trait MovsxEmitter<T0, T1> {
    fn movsx(&mut self, dst: T0, src: T1);
}

/// Emits `MOVSXD`.
///
/// Assembly forms: `movsxd r16, r16/m16`, `movsxd r32, r32/m32`, `movsxd r64, r32/m32`.
pub trait MovsxdEmitter<T0, T1> {
    fn movsxd(&mut self, dst: T0, src: T1);
}

/// Emits `MOVZX`.
///
/// Assembly forms: `movzx rv, r8/m8`, `movzx r32, r16/m16`, `movzx r64, r16/m16`.
pub trait MovzxEmitter<T0, T1> {
    fn movzx(&mut self, dst: T0, src: T1);
}

/// Emits `MUL`.
///
/// Assembly forms: `mul <ax>, r8/m8`, `mul <dxv>, <axv>, rv/mv`.
pub trait MulEmitter<T0, T1> {
    fn mul(&mut self, dst: T0, src: T1);
}

/// Emits `MUL_3`.
///
/// Assembly forms: `mul <ax>, r8/m8`, `mul <dxv>, <axv>, rv/mv`.
pub trait Mul3Emitter<T0, T1, T2> {
    fn mul_3(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `NEG`.
///
/// Assembly forms: `neg r8/m8`, `neg rv/mv`.
pub trait NegEmitter<T0> {
    fn neg(&mut self, src: T0);
}

/// Emits `NOP`.
///
/// Assembly forms: `nop`, `nop rv/mv`, `nop rv/mv, rv`.
pub trait NopEmitter {
    fn nop(&mut self);
}

/// Emits `NOP_1`.
///
/// Assembly forms: `nop`, `nop rv/mv`, `nop rv/mv, rv`.
pub trait Nop1Emitter<T0> {
    fn nop_1(&mut self, src: T0);
}

/// Emits `NOP_2`.
///
/// Assembly forms: `nop`, `nop rv/mv`, `nop rv/mv, rv`.
pub trait Nop2Emitter<T0, T1> {
    fn nop_2(&mut self, lhs: T0, rhs: T1);
}

/// Emits `NOT_`.
///
/// Assembly forms: `not r8/m8`, `not rv/mv`.
pub trait Not_Emitter<T0> {
    fn not_(&mut self, src: T0);
}

/// Emits `OR_`.
///
/// Assembly forms: `or al, imm8`, `or axv, immv`, `or r8/m8, r8`.
pub trait Or_Emitter<T0, T1> {
    fn or_(&mut self, dst: T0, src: T1);
}

/// Emits `POP`.
///
/// Assembly forms: `pop r16/m16`, `pop r32/m32`, `pop r64/m64`.
pub trait PopEmitter<T0> {
    fn pop(&mut self, dst: T0);
}

/// Emits `POPA`.
///
/// Assembly forms: `popa`.
pub trait PopaEmitter {
    fn popa(&mut self);
}

/// Emits `POPAD`.
///
/// Assembly forms: `popad`.
pub trait PopadEmitter {
    fn popad(&mut self);
}

/// Emits `POPF`.
///
/// Assembly forms: `popf`.
pub trait PopfEmitter {
    fn popf(&mut self);
}

/// Emits `POPFD`.
///
/// Assembly forms: `popfd`.
pub trait PopfdEmitter {
    fn popfd(&mut self);
}

/// Emits `POPFQ`.
///
/// Assembly forms: `popfq`.
pub trait PopfqEmitter {
    fn popfq(&mut self);
}

/// Emits `PUSH`.
///
/// Assembly forms: `push r16/m16`, `push r32/m32`, `push r64/m64`.
pub trait PushEmitter<T0> {
    fn push(&mut self, src: T0);
}

/// Emits `PUSHA`.
///
/// Assembly forms: `pusha`.
pub trait PushaEmitter {
    fn pusha(&mut self);
}

/// Emits `PUSHAD`.
///
/// Assembly forms: `pushad`.
pub trait PushadEmitter {
    fn pushad(&mut self);
}

/// Emits `PUSHF`.
///
/// Assembly forms: `pushf`.
pub trait PushfEmitter {
    fn pushf(&mut self);
}

/// Emits `PUSHFD`.
///
/// Assembly forms: `pushfd`.
pub trait PushfdEmitter {
    fn pushfd(&mut self);
}

/// Emits `PUSHFQ`.
///
/// Assembly forms: `pushfq`.
pub trait PushfqEmitter {
    fn pushfq(&mut self);
}

/// Emits `PUSHW`.
///
/// Assembly forms: `pushw imm16`.
pub trait PushwEmitter<T0> {
    fn pushw(&mut self, imm: T0);
}

/// Emits `RCL`.
///
/// Assembly forms: `rcl r8/m8, cl`, `rcl rv/mv, cl`, `rcl r8/m8, 1`.
pub trait RclEmitter<T0, T1> {
    fn rcl(&mut self, dst: T0, src: T1);
}

/// Emits `RCR`.
///
/// Assembly forms: `rcr r8/m8, cl`, `rcr rv/mv, cl`, `rcr r8/m8, 1`.
pub trait RcrEmitter<T0, T1> {
    fn rcr(&mut self, dst: T0, src: T1);
}

/// Emits `ROL`.
///
/// Assembly forms: `rol r8/m8, cl`, `rol rv/mv, cl`, `rol r8/m8, 1`.
pub trait RolEmitter<T0, T1> {
    fn rol(&mut self, dst: T0, src: T1);
}

/// Emits `ROR`.
///
/// Assembly forms: `ror r8/m8, cl`, `ror rv/mv, cl`, `ror r8/m8, 1`.
pub trait RorEmitter<T0, T1> {
    fn ror(&mut self, dst: T0, src: T1);
}

/// Emits `SBB`.
///
/// Assembly forms: `sbb al, imm8`, `sbb axv, immv`, `sbb r8/m8, r8`.
pub trait SbbEmitter<T0, T1> {
    fn sbb(&mut self, dst: T0, src: T1);
}

/// Emits `SAL`.
///
/// Syntax: `sal dst, src`.
pub trait SalEmitter<T0, T1> {
    fn sal(&mut self, dst: T0, src: T1);
}

/// Emits `SAR`.
///
/// Assembly forms: `sar r8/m8, cl`, `sar rv/mv, cl`, `sar r8/m8, 1`.
pub trait SarEmitter<T0, T1> {
    fn sar(&mut self, dst: T0, src: T1);
}

/// Emits `SCAS`.
///
/// Assembly forms: `scas al, m8(es:zdi)`, `scas ax, m16(es:zdi)`, `scas eax, m32(es:zdi)`.
pub trait ScasEmitter<T0, T1> {
    fn scas(&mut self, lhs: T0, rhs: T1);
}

/// Emits `SET`.
///
/// Assembly forms: `setcc r8/m8`, `setcc w:r8/m8`, `setcc`.
pub trait SetEmitter<T0> {
    fn set(&mut self, cc: CondCode, dst: T0);
    fn seto(&mut self, dst: T0);
    fn setno(&mut self, dst: T0);
    fn setb(&mut self, dst: T0);
    fn setnb(&mut self, dst: T0);
    fn setz(&mut self, dst: T0);
    fn setnz(&mut self, dst: T0);
    fn setbe(&mut self, dst: T0);
    fn setnbe(&mut self, dst: T0);
    fn sets(&mut self, dst: T0);
    fn setns(&mut self, dst: T0);
    fn setp(&mut self, dst: T0);
    fn setnp(&mut self, dst: T0);
    fn setl(&mut self, dst: T0);
    fn setnl(&mut self, dst: T0);
    fn setle(&mut self, dst: T0);
    fn setnle(&mut self, dst: T0);
}

/// Emits `SHL`.
///
/// Assembly forms: `shl r8/m8, cl`, `shl rv/mv, cl`, `shl r8/m8, 1`.
pub trait ShlEmitter<T0, T1> {
    fn shl(&mut self, dst: T0, src: T1);
}

/// Emits `SHR`.
///
/// Assembly forms: `shr r8/m8, cl`, `shr rv/mv, cl`, `shr r8/m8, 1`.
pub trait ShrEmitter<T0, T1> {
    fn shr(&mut self, dst: T0, src: T1);
}

/// Emits `SHLD`.
///
/// Assembly forms: `shld rv/mv, rv, cl`, `shld rv/mv, rv, imm8`.
pub trait ShldEmitter<T0, T1, T2> {
    fn shld(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `SHRD`.
///
/// Assembly forms: `shrd rv/mv, rv, cl`, `shrd rv/mv, rv, imm8`.
pub trait ShrdEmitter<T0, T1, T2> {
    fn shrd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `STOS`.
///
/// Assembly forms: `stos m8(es:zdi), al`, `stos m16(es:zdi), ax`, `stos m32(es:zdi), eax`.
pub trait StosEmitter<T0, T1> {
    fn stos(&mut self, lhs: T0, rhs: T1);
}

/// Emits `SUB`.
///
/// Assembly forms: `sub al, imm8`, `sub axv, immv`, `sub r8/m8, r8`.
pub trait SubEmitter<T0, T1> {
    fn sub(&mut self, dst: T0, src: T1);
}

/// Emits `TEST`.
///
/// Assembly forms: `test r8/m8, r8`, `test rv/mv, rv`, `test al, imm8`.
pub trait TestEmitter<T0, T1> {
    fn test(&mut self, lhs: T0, rhs: T1);
}

/// Emits `UD0`.
///
/// Assembly forms: `ud0 r32, r32/m32`.
pub trait Ud0Emitter<T0, T1> {
    fn ud0(&mut self, lhs: T0, rhs: T1);
}

/// Emits `UD1`.
///
/// Assembly forms: `ud1 r32, r32/m32`.
pub trait Ud1Emitter<T0, T1> {
    fn ud1(&mut self, lhs: T0, rhs: T1);
}

/// Emits `UD2`.
///
/// Assembly forms: `ud2`.
pub trait Ud2Emitter {
    fn ud2(&mut self);
}

/// Emits `XADD`.
///
/// Assembly forms: `xadd r8/m8, r8`, `xadd rv/mv, rv`.
pub trait XaddEmitter<T0, T1> {
    fn xadd(&mut self, dst: T0, dst2: T1);
}

/// Emits `XCHG`.
///
/// Assembly forms: `xchg ax, r16`, `xchg eax, r32`, `xchg rax, r64`.
pub trait XchgEmitter<T0, T1> {
    fn xchg(&mut self, dst: T0, dst2: T1);
}

/// Emits `XOR_`.
///
/// Assembly forms: `xor al, imm8`, `xor axv, immv`, `xor r8/m8, r8`.
pub trait Xor_Emitter<T0, T1> {
    fn xor_(&mut self, dst: T0, src: T1);
}

/// Emits `AAA`.
///
/// Assembly forms: `aaa <ax>`.
pub trait AaaEmitter<T0> {
    fn aaa(&mut self, dst: T0);
}

/// Emits `AAD`.
///
/// Assembly forms: `aad <ax>, imm8`.
pub trait AadEmitter<T0, T1> {
    fn aad(&mut self, dst: T0, imm: T1);
}

/// Emits `AAM`.
///
/// Assembly forms: `aam <ax>, imm8`.
pub trait AamEmitter<T0, T1> {
    fn aam(&mut self, dst: T0, imm: T1);
}

/// Emits `AAS`.
///
/// Assembly forms: `aas <ax>`.
pub trait AasEmitter<T0> {
    fn aas(&mut self, dst: T0);
}

/// Emits `DAA`.
///
/// Assembly forms: `daa <ax>`.
pub trait DaaEmitter<T0> {
    fn daa(&mut self, dst: T0);
}

/// Emits `DAS`.
///
/// Assembly forms: `das <ax>`.
pub trait DasEmitter<T0> {
    fn das(&mut self, dst: T0);
}

/// Emits `ENTER`.
///
/// Assembly forms: `enter imm16, imm8`.
pub trait EnterEmitter<T0, T1> {
    fn enter(&mut self, frame_size: T0, nesting_level: T1);
}

/// Emits `LEAVE`.
///
/// Assembly forms: `leave`.
pub trait LeaveEmitter {
    fn leave(&mut self);
}

/// Emits `IN`.
///
/// Assembly forms: `in al, imm8`, `in ax, imm8`, `in eax, imm8`.
pub trait InEmitter<T0, T1> {
    fn r#in(&mut self, dst: T0, imm: T1);
}

/// Emits `INS`.
///
/// Assembly forms: `ins m8(es:zdi), dx`, `ins m16(es:zdi), dx`, `ins m32(es:zdi), dx`.
pub trait InsEmitter<T0, T1> {
    fn ins(&mut self, lhs: T0, rhs: T1);
}

/// Emits `OUT`.
///
/// Assembly forms: `out imm8, al`, `out imm8, ax`, `out imm8, eax`.
pub trait OutEmitter<T0, T1> {
    fn out(&mut self, imm: T0, rhs: T1);
}

/// Emits `OUTS`.
///
/// Assembly forms: `outs dx, m8(ds:zsi)`, `outs dx, m16(ds:zsi)`, `outs dx, m32(ds:zsi)`.
pub trait OutsEmitter<T0, T1> {
    fn outs(&mut self, lhs: T0, rhs: T1);
}

/// Emits `CLC`.
///
/// Assembly forms: `clc`.
pub trait ClcEmitter {
    fn clc(&mut self);
}

/// Emits `CLD`.
///
/// Assembly forms: `cld`.
pub trait CldEmitter {
    fn cld(&mut self);
}

/// Emits `CMC`.
///
/// Assembly forms: `cmc`.
pub trait CmcEmitter {
    fn cmc(&mut self);
}

/// Emits `STC`.
///
/// Assembly forms: `stc`.
pub trait StcEmitter {
    fn stc(&mut self);
}

/// Emits `STD`.
///
/// Assembly forms: `std`.
pub trait StdEmitter {
    fn std(&mut self);
}

/// Emits `ADCX`.
///
/// Assembly forms: `adcx ry, ry/my`.
pub trait AdcxEmitter<T0, T1> {
    fn adcx(&mut self, dst: T0, src: T1);
}

/// Emits `ADOX`.
///
/// Assembly forms: `adox ry, ry/my`.
pub trait AdoxEmitter<T0, T1> {
    fn adox(&mut self, dst: T0, src: T1);
}

/// Emits `CPUID`.
///
/// Assembly forms: `cpuid <eax>, <ebx>, <ecx>, <edx>`.
pub trait CpuidEmitter<T0, T1, T2, T3> {
    fn cpuid(&mut self, dst: T0, dst2: T1, dst3: T2, dst4: T3);
}

/// Emits `LAHF`.
///
/// Assembly forms: `lahf <ah>`.
pub trait LahfEmitter<T0> {
    fn lahf(&mut self, dst: T0);
}

/// Emits `SAHF`.
///
/// Assembly forms: `sahf <ah>`.
pub trait SahfEmitter<T0> {
    fn sahf(&mut self, src: T0);
}

/// Emits `ANDN`.
///
/// Assembly forms: `andn W:ry, ry, ry/my`.
pub trait AndnEmitter<T0, T1, T2> {
    fn andn(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `BEXTR`.
///
/// Assembly forms: `bextr W:ry, ry/my, ry`.
pub trait BextrEmitter<T0, T1, T2> {
    fn bextr(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `BLSI`.
///
/// Assembly forms: `blsi W:ry, ry/my`.
pub trait BlsiEmitter<T0, T1> {
    fn blsi(&mut self, dst: T0, src: T1);
}

/// Emits `BLSMSK`.
///
/// Assembly forms: `blsmsk W:ry, ry/my`.
pub trait BlsmskEmitter<T0, T1> {
    fn blsmsk(&mut self, dst: T0, src: T1);
}

/// Emits `BLSR`.
///
/// Assembly forms: `blsr W:ry, ry/my`.
pub trait BlsrEmitter<T0, T1> {
    fn blsr(&mut self, dst: T0, src: T1);
}

/// Emits `TZCNT`.
///
/// Assembly forms: `tzcnt w:rv, rv/mv`.
pub trait TzcntEmitter<T0, T1> {
    fn tzcnt(&mut self, dst: T0, src: T1);
}

/// Emits `BZHI`.
///
/// Assembly forms: `bzhi W:ry, ry/my, ry`.
pub trait BzhiEmitter<T0, T1, T2> {
    fn bzhi(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `MULX`.
///
/// Assembly forms: `mulx W:ry, ry, ry/my, <dxy>`.
pub trait MulxEmitter<T0, T1, T2, T3> {
    fn mulx(&mut self, dst: T0, dst2: T1, src1: T2, src2: T3);
}

/// Emits `PDEP`.
///
/// Assembly forms: `pdep W:ry, ry, ry/my`.
pub trait PdepEmitter<T0, T1, T2> {
    fn pdep(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `PEXT`.
///
/// Assembly forms: `pext W:ry, ry, ry/my`.
pub trait PextEmitter<T0, T1, T2> {
    fn pext(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `RORX`.
///
/// Assembly forms: `rorx W:ry, ry/my, imm8`.
pub trait RorxEmitter<T0, T1, T2> {
    fn rorx(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `SARX`.
///
/// Assembly forms: `sarx W:ry, ry/my, ry`.
pub trait SarxEmitter<T0, T1, T2> {
    fn sarx(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `SHLX`.
///
/// Assembly forms: `shlx W:ry, ry/my, ry`.
pub trait ShlxEmitter<T0, T1, T2> {
    fn shlx(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `SHRX`.
///
/// Assembly forms: `shrx W:ry, ry/my, ry`.
pub trait ShrxEmitter<T0, T1, T2> {
    fn shrx(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `CMPBEXADD`.
///
/// Assembly forms: `cmpbexadd X:my, ry, ry`.
pub trait CmpbexaddEmitter<T0, T1, T2> {
    fn cmpbexadd(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `CMPBXADD`.
///
/// Assembly forms: `cmpbxadd X:my, ry, ry`.
pub trait CmpbxaddEmitter<T0, T1, T2> {
    fn cmpbxadd(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `CMPLEXADD`.
///
/// Assembly forms: `cmplexadd X:my, ry, ry`.
pub trait CmplexaddEmitter<T0, T1, T2> {
    fn cmplexadd(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `CMPLXADD`.
///
/// Assembly forms: `cmplxadd X:my, ry, ry`.
pub trait CmplxaddEmitter<T0, T1, T2> {
    fn cmplxadd(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `CMPNBEXADD`.
///
/// Assembly forms: `cmpnbexadd my, ry, ry`.
pub trait CmpnbexaddEmitter<T0, T1, T2> {
    fn cmpnbexadd(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `CMPNBXADD`.
///
/// Assembly forms: `cmpnbxadd X:my, ry, ry`.
pub trait CmpnbxaddEmitter<T0, T1, T2> {
    fn cmpnbxadd(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `CMPNLEXADD`.
///
/// Assembly forms: `cmpnlexadd my, ry, ry`.
pub trait CmpnlexaddEmitter<T0, T1, T2> {
    fn cmpnlexadd(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `CMPNLXADD`.
///
/// Assembly forms: `cmpnlxadd X:my, ry, ry`.
pub trait CmpnlxaddEmitter<T0, T1, T2> {
    fn cmpnlxadd(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `CMPNOXADD`.
///
/// Assembly forms: `cmpnoxadd X:my, ry, ry`.
pub trait CmpnoxaddEmitter<T0, T1, T2> {
    fn cmpnoxadd(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `CMPNPXADD`.
///
/// Assembly forms: `cmpnpxadd X:my, ry, ry`.
pub trait CmpnpxaddEmitter<T0, T1, T2> {
    fn cmpnpxadd(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `CMPNSXADD`.
///
/// Assembly forms: `cmpnsxadd X:my, ry, ry`.
pub trait CmpnsxaddEmitter<T0, T1, T2> {
    fn cmpnsxadd(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `CMPNZXADD`.
///
/// Assembly forms: `cmpnzxadd X:my, ry, ry`.
pub trait CmpnzxaddEmitter<T0, T1, T2> {
    fn cmpnzxadd(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `CMPOXADD`.
///
/// Assembly forms: `cmpoxadd X:my, ry, ry`.
pub trait CmpoxaddEmitter<T0, T1, T2> {
    fn cmpoxadd(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `CMPPXADD`.
///
/// Assembly forms: `cmppxadd X:my, ry, ry`.
pub trait CmppxaddEmitter<T0, T1, T2> {
    fn cmppxadd(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `CMPSXADD`.
///
/// Assembly forms: `cmpsxadd X:my, ry, ry`.
pub trait CmpsxaddEmitter<T0, T1, T2> {
    fn cmpsxadd(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `CMPZXADD`.
///
/// Assembly forms: `cmpzxadd X:my, ry, ry`.
pub trait CmpzxaddEmitter<T0, T1, T2> {
    fn cmpzxadd(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `CLDEMOTE`.
///
/// Assembly forms: `cldemote mem`.
pub trait CldemoteEmitter<T0> {
    fn cldemote(&mut self, mem: T0);
}

/// Emits `CLFLUSH`.
///
/// Assembly forms: `clflush mem`.
pub trait ClflushEmitter<T0> {
    fn clflush(&mut self, mem: T0);
}

/// Emits `CLFLUSHOPT`.
///
/// Assembly forms: `clflushopt mem`.
pub trait ClflushoptEmitter<T0> {
    fn clflushopt(&mut self, mem: T0);
}

/// Emits `CLWB`.
///
/// Assembly forms: `clwb mem`.
pub trait ClwbEmitter<T0> {
    fn clwb(&mut self, mem: T0);
}

/// Emits `CLZERO`.
///
/// Assembly forms: `clzero <m512(ds:zax)>`.
pub trait ClzeroEmitter<T0> {
    fn clzero(&mut self, mem: T0);
}

/// Emits `CRC32`.
///
/// Assembly forms: `crc32 r32, r8/m8`, `crc32 r32, r16/m16`, `crc32 r32, r32/m32`.
pub trait Crc32Emitter<T0, T1> {
    fn crc32(&mut self, dst: T0, src: T1);
}

/// Emits `LFENCE`.
///
/// Assembly forms: `lfence`.
pub trait LfenceEmitter {
    fn lfence(&mut self);
}

/// Emits `MFENCE`.
///
/// Assembly forms: `mfence`.
pub trait MfenceEmitter {
    fn mfence(&mut self);
}

/// Emits `SFENCE`.
///
/// Assembly forms: `sfence`.
pub trait SfenceEmitter {
    fn sfence(&mut self);
}

/// Emits `LZCNT`.
///
/// Assembly forms: `lzcnt rv, rv/mv`.
pub trait LzcntEmitter<T0, T1> {
    fn lzcnt(&mut self, dst: T0, src: T1);
}

/// Emits `MOVBE`.
///
/// Assembly forms: `movbe rv, mv`, `movbe mv, rv`.
pub trait MovbeEmitter<T0, T1> {
    fn movbe(&mut self, dst: T0, src: T1);
}

/// Emits `MOVDIRI`.
///
/// Assembly forms: `movdiri my, ry`.
pub trait MovdiriEmitter<T0, T1> {
    fn movdiri(&mut self, dst: T0, src: T1);
}

/// Emits `MOVDIR64B`.
///
/// Assembly forms: `movdir64b m512(es:r32), m512`, `movdir64b m512(es:r64), m512`.
pub trait Movdir64bEmitter<T0, T1> {
    fn movdir64b(&mut self, dst: T0, src: T1);
}

/// Emits `LDMXCSR`.
///
/// Assembly forms: `ldmxcsr m32`.
pub trait LdmxcsrEmitter<T0> {
    fn ldmxcsr(&mut self, mem: T0);
}

/// Emits `STMXCSR`.
///
/// Assembly forms: `stmxcsr m32`.
pub trait StmxcsrEmitter<T0> {
    fn stmxcsr(&mut self, dst: T0);
}

/// Emits `POPCNT`.
///
/// Assembly forms: `popcnt rv, rv/mv`.
pub trait PopcntEmitter<T0, T1> {
    fn popcnt(&mut self, dst: T0, src: T1);
}

/// Emits `PREFETCH`.
///
/// Assembly forms: `prefetch mem`.
pub trait PrefetchEmitter<T0> {
    fn prefetch(&mut self, mem: T0);
}

/// Emits `PREFETCHNTA`.
///
/// Assembly forms: `prefetchnta mem`.
pub trait PrefetchntaEmitter<T0> {
    fn prefetchnta(&mut self, mem: T0);
}

/// Emits `PREFETCHT0`.
///
/// Assembly forms: `prefetcht0 mem`.
pub trait Prefetcht0Emitter<T0> {
    fn prefetcht0(&mut self, mem: T0);
}

/// Emits `PREFETCHT1`.
///
/// Assembly forms: `prefetcht1 mem`.
pub trait Prefetcht1Emitter<T0> {
    fn prefetcht1(&mut self, mem: T0);
}

/// Emits `PREFETCHT2`.
///
/// Assembly forms: `prefetcht2 mem`.
pub trait Prefetcht2Emitter<T0> {
    fn prefetcht2(&mut self, mem: T0);
}

/// Emits `PREFETCHW`.
///
/// Assembly forms: `prefetchw mem`.
pub trait PrefetchwEmitter<T0> {
    fn prefetchw(&mut self, mem: T0);
}

/// Emits `PREFETCHWT1`.
///
/// Assembly forms: `prefetchwt1 mem`.
pub trait Prefetchwt1Emitter<T0> {
    fn prefetchwt1(&mut self, mem: T0);
}

/// Emits `PREFETCHIT0`.
///
/// Assembly forms: `prefetchit0 mem`.
pub trait Prefetchit0Emitter<T0> {
    fn prefetchit0(&mut self, mem: T0);
}

/// Emits `PREFETCHIT1`.
///
/// Assembly forms: `prefetchit1 mem`.
pub trait Prefetchit1Emitter<T0> {
    fn prefetchit1(&mut self, mem: T0);
}

/// Emits `AADD`.
///
/// Assembly forms: `aadd my, ry`.
pub trait AaddEmitter<T0, T1> {
    fn aadd(&mut self, dst: T0, src: T1);
}

/// Emits `AAND`.
///
/// Assembly forms: `aand my, ry`.
pub trait AandEmitter<T0, T1> {
    fn aand(&mut self, dst: T0, src: T1);
}

/// Emits `AOR`.
///
/// Assembly forms: `aor X:my, ry`.
pub trait AorEmitter<T0, T1> {
    fn aor(&mut self, dst: T0, src: T1);
}

/// Emits `AXOR`.
///
/// Assembly forms: `axor my, ry`.
pub trait AxorEmitter<T0, T1> {
    fn axor(&mut self, dst: T0, src: T1);
}

/// Emits `RDPID`.
///
/// Assembly forms: `rdpid r32`, `rdpid r64`.
pub trait RdpidEmitter<T0> {
    fn rdpid(&mut self, dst: T0);
}

/// Emits `RDPRU`.
///
/// Assembly forms: `rdpru <edx>, <eax>, <ecx>`.
pub trait RdpruEmitter<T0, T1, T2> {
    fn rdpru(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `RDPKRU`.
///
/// Assembly forms: `rdpkru <edx>, <eax>, <ecx>`.
pub trait RdpkruEmitter<T0, T1, T2> {
    fn rdpkru(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `RDTSC`.
///
/// Assembly forms: `rdtsc <edx>, <eax>`.
pub trait RdtscEmitter<T0, T1> {
    fn rdtsc(&mut self, dst: T0, dst2: T1);
}

/// Emits `RDTSCP`.
///
/// Assembly forms: `rdtscp <edx>, <eax>, <ecx>`.
pub trait RdtscpEmitter<T0, T1, T2> {
    fn rdtscp(&mut self, dst: T0, dst2: T1, dst3: T2);
}

/// Emits `SERIALIZE`.
///
/// Assembly forms: `serialize`.
pub trait SerializeEmitter {
    fn serialize(&mut self);
}

/// Emits `BLCFILL`.
///
/// Assembly forms: `blcfill ry, ry/my`.
pub trait BlcfillEmitter<T0, T1> {
    fn blcfill(&mut self, dst: T0, src: T1);
}

/// Emits `BLCI`.
///
/// Assembly forms: `blci ry, ry/my`.
pub trait BlciEmitter<T0, T1> {
    fn blci(&mut self, dst: T0, src: T1);
}

/// Emits `BLCIC`.
///
/// Assembly forms: `blcic ry, ry/my`.
pub trait BlcicEmitter<T0, T1> {
    fn blcic(&mut self, dst: T0, src: T1);
}

/// Emits `BLCMSK`.
///
/// Assembly forms: `blcmsk ry, ry/my`.
pub trait BlcmskEmitter<T0, T1> {
    fn blcmsk(&mut self, dst: T0, src: T1);
}

/// Emits `BLCS`.
///
/// Assembly forms: `blcs ry, ry/my`.
pub trait BlcsEmitter<T0, T1> {
    fn blcs(&mut self, dst: T0, src: T1);
}

/// Emits `BLSFILL`.
///
/// Assembly forms: `blsfill ry, ry/my`.
pub trait BlsfillEmitter<T0, T1> {
    fn blsfill(&mut self, dst: T0, src: T1);
}

/// Emits `BLSIC`.
///
/// Assembly forms: `blsic ry, ry/my`.
pub trait BlsicEmitter<T0, T1> {
    fn blsic(&mut self, dst: T0, src: T1);
}

/// Emits `T1MSKC`.
///
/// Assembly forms: `t1mskc ry, ry/my`.
pub trait T1mskcEmitter<T0, T1> {
    fn t1mskc(&mut self, dst: T0, src: T1);
}

/// Emits `TZMSK`.
///
/// Assembly forms: `tzmsk ry, ry/my`.
pub trait TzmskEmitter<T0, T1> {
    fn tzmsk(&mut self, dst: T0, src: T1);
}

/// Emits `ARPL`.
///
/// Assembly forms: `arpl r16/m16, r16`.
pub trait ArplEmitter<T0, T1> {
    fn arpl(&mut self, dst: T0, src: T1);
}

/// Emits `CLI`.
///
/// Assembly forms: `cli`.
pub trait CliEmitter {
    fn cli(&mut self);
}

/// Emits `GETSEC`.
///
/// Assembly forms: `getsec <eax>, <ebx>`.
pub trait GetsecEmitter {
    fn getsec(&mut self);
}

/// Emits `INT_`.
///
/// Assembly forms: `int imm8`.
pub trait Int_Emitter<T0> {
    fn int_(&mut self, imm: T0);
}

/// Emits `INT3`.
///
/// Assembly forms: `int3`.
pub trait Int3Emitter {
    fn int3(&mut self);
}

/// Emits `INTO`.
///
/// Assembly forms: `into`.
pub trait IntoEmitter {
    fn into(&mut self);
}

/// Emits `LAR`.
///
/// Assembly forms: `lar r16, r16/m16`, `lar r32, r32/m16`.
pub trait LarEmitter<T0, T1> {
    fn lar(&mut self, dst: T0, src: T1);
}

/// Emits `LDS`.
///
/// Assembly forms: `lds r16, m16_16`, `lds r32, m16_32`.
pub trait LdsEmitter<T0, T1> {
    fn lds(&mut self, dst: T0, src: T1);
}

/// Emits `LES`.
///
/// Assembly forms: `les r16, m16_16`, `les r32, m16_32`.
pub trait LesEmitter<T0, T1> {
    fn les(&mut self, dst: T0, src: T1);
}

/// Emits `LFS`.
///
/// Assembly forms: `lfs r16, m16_16`, `lfs r32, m16_32`, `lfs r64, m16_64`.
pub trait LfsEmitter<T0, T1> {
    fn lfs(&mut self, dst: T0, src: T1);
}

/// Emits `LGS`.
///
/// Assembly forms: `lgs r16, m16_16`, `lgs r32, m16_32`, `lgs r64, m16_64`.
pub trait LgsEmitter<T0, T1> {
    fn lgs(&mut self, dst: T0, src: T1);
}

/// Emits `LSL`.
///
/// Assembly forms: `lsl r16, r16/m16`, `lsl r32, r32/m16`, `lsl r64, r32/m16`.
pub trait LslEmitter<T0, T1> {
    fn lsl(&mut self, dst: T0, src: T1);
}

/// Emits `LSS`.
///
/// Assembly forms: `lss r16, m16_16`, `lss r32, m16_32`, `lss r64, m16_64`.
pub trait LssEmitter<T0, T1> {
    fn lss(&mut self, dst: T0, src: T1);
}

/// Emits `PAUSE`.
///
/// Assembly forms: `pause`.
pub trait PauseEmitter {
    fn pause(&mut self);
}

/// Emits `RSM`.
///
/// Assembly forms: `rsm`.
pub trait RsmEmitter {
    fn rsm(&mut self);
}

/// Emits `SGDT`.
///
/// Assembly forms: `sgdt mem`.
pub trait SgdtEmitter<T0> {
    fn sgdt(&mut self, dst: T0);
}

/// Emits `SIDT`.
///
/// Assembly forms: `sidt mem`.
pub trait SidtEmitter<T0> {
    fn sidt(&mut self, dst: T0);
}

/// Emits `SLDT`.
///
/// Assembly forms: `sldt r16/m16`, `sldt r32/m16`, `sldt r64/m16`.
pub trait SldtEmitter<T0> {
    fn sldt(&mut self, dst: T0);
}

/// Emits `SMSW`.
///
/// Assembly forms: `smsw r16/m16`, `smsw r32/m16`, `smsw r64/m16`.
pub trait SmswEmitter<T0> {
    fn smsw(&mut self, dst: T0);
}

/// Emits `STI`.
///
/// Assembly forms: `sti`.
pub trait StiEmitter {
    fn sti(&mut self);
}

/// Emits `STR`.
///
/// Assembly forms: `str r16/m16`, `str r32/m16`, `str r64/m16`.
pub trait StrEmitter<T0> {
    fn str(&mut self, dst: T0);
}

/// Emits `VERR`.
///
/// Assembly forms: `verr r16/m16`.
pub trait VerrEmitter<T0> {
    fn verr(&mut self, src: T0);
}

/// Emits `VERW`.
///
/// Assembly forms: `verw r16/m16`.
pub trait VerwEmitter<T0> {
    fn verw(&mut self, src: T0);
}

/// Emits `RDFSBASE`.
///
/// Assembly forms: `rdfsbase ry`.
pub trait RdfsbaseEmitter<T0> {
    fn rdfsbase(&mut self, dst: T0);
}

/// Emits `RDGSBASE`.
///
/// Assembly forms: `rdgsbase ry`.
pub trait RdgsbaseEmitter<T0> {
    fn rdgsbase(&mut self, dst: T0);
}

/// Emits `WRFSBASE`.
///
/// Assembly forms: `wrfsbase ry`.
pub trait WrfsbaseEmitter<T0> {
    fn wrfsbase(&mut self, src: T0);
}

/// Emits `WRGSBASE`.
///
/// Assembly forms: `wrgsbase ry`.
pub trait WrgsbaseEmitter<T0> {
    fn wrgsbase(&mut self, src: T0);
}

/// Emits `FXRSTOR`.
///
/// Assembly forms: `fxrstor R:mem`.
pub trait FxrstorEmitter<T0> {
    fn fxrstor(&mut self, mem: T0);
}

/// Emits `FXRSTOR64`.
///
/// Assembly forms: `fxrstor64 mem`.
pub trait Fxrstor64Emitter<T0> {
    fn fxrstor64(&mut self, mem: T0);
}

/// Emits `FXSAVE`.
///
/// Assembly forms: `fxsave W:mem`.
pub trait FxsaveEmitter<T0> {
    fn fxsave(&mut self, dst: T0);
}

/// Emits `FXSAVE64`.
///
/// Assembly forms: `fxsave64 W:mem`.
pub trait Fxsave64Emitter<T0> {
    fn fxsave64(&mut self, dst: T0);
}

/// Emits `XGETBV`.
///
/// Assembly forms: `xgetbv <edx>, <eax>, <ecx>`.
pub trait XgetbvEmitter<T0, T1, T2> {
    fn xgetbv(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `XRSTOR`.
///
/// Assembly forms: `xrstor mem, <edx>, <eax>`.
pub trait XrstorEmitter<T0, T1, T2> {
    fn xrstor(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `XRSTOR64`.
///
/// Assembly forms: `xrstor64 mem, <edx>, <eax>`.
pub trait Xrstor64Emitter<T0, T1, T2> {
    fn xrstor64(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `XRSTORS`.
///
/// Assembly forms: `xrstors mem, <edx>, <eax>`.
pub trait XrstorsEmitter<T0, T1, T2> {
    fn xrstors(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `XRSTORS64`.
///
/// Assembly forms: `xrstors64 mem, <edx>, <eax>`.
pub trait Xrstors64Emitter<T0, T1, T2> {
    fn xrstors64(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `XSAVE`.
///
/// Assembly forms: `xsave mem, <edx>, <eax>`.
pub trait XsaveEmitter<T0, T1, T2> {
    fn xsave(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `XSAVE64`.
///
/// Assembly forms: `xsave64 mem, <edx>, <eax>`.
pub trait Xsave64Emitter<T0, T1, T2> {
    fn xsave64(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `XSAVEC`.
///
/// Assembly forms: `xsavec mem, <edx>, <eax>`.
pub trait XsavecEmitter<T0, T1, T2> {
    fn xsavec(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `XSAVEC64`.
///
/// Assembly forms: `xsavec64 mem, <edx>, <eax>`.
pub trait Xsavec64Emitter<T0, T1, T2> {
    fn xsavec64(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `XSAVEOPT`.
///
/// Assembly forms: `xsaveopt mem, <edx>, <eax>`.
pub trait XsaveoptEmitter<T0, T1, T2> {
    fn xsaveopt(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `XSAVEOPT64`.
///
/// Assembly forms: `xsaveopt64 mem, <edx>, <eax>`.
pub trait Xsaveopt64Emitter<T0, T1, T2> {
    fn xsaveopt64(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `XSAVES`.
///
/// Assembly forms: `xsaves mem, <edx>, <eax>`.
pub trait XsavesEmitter<T0, T1, T2> {
    fn xsaves(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `XSAVES64`.
///
/// Assembly forms: `xsaves64 mem, <edx>, <eax>`.
pub trait Xsaves64Emitter<T0, T1, T2> {
    fn xsaves64(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `BNDCL`.
///
/// Assembly forms: `bndcl bnd, r32/m32`, `bndcl bnd, r64/m64`.
pub trait BndclEmitter<T0, T1> {
    fn bndcl(&mut self, lhs: T0, rhs: T1);
}

/// Emits `BNDCN`.
///
/// Assembly forms: `bndcn bnd, r32/m32`, `bndcn bnd, r64/m64`.
pub trait BndcnEmitter<T0, T1> {
    fn bndcn(&mut self, lhs: T0, rhs: T1);
}

/// Emits `BNDCU`.
///
/// Assembly forms: `bndcu bnd, r32/m32`, `bndcu bnd, r64/m64`.
pub trait BndcuEmitter<T0, T1> {
    fn bndcu(&mut self, lhs: T0, rhs: T1);
}

/// Emits `BNDLDX`.
///
/// Assembly forms: `bndldx bnd, mib`.
pub trait BndldxEmitter<T0, T1> {
    fn bndldx(&mut self, dst: T0, src: T1);
}

/// Emits `BNDMK`.
///
/// Assembly forms: `bndmk bnd, mem`.
pub trait BndmkEmitter<T0, T1> {
    fn bndmk(&mut self, dst: T0, src: T1);
}

/// Emits `BNDMOV`.
///
/// Assembly forms: `bndmov bnd, bnd/mem`, `bndmov bnd/mem, bnd`.
pub trait BndmovEmitter<T0, T1> {
    fn bndmov(&mut self, dst: T0, src: T1);
}

/// Emits `BNDSTX`.
///
/// Assembly forms: `bndstx mib, bnd`.
pub trait BndstxEmitter<T0, T1> {
    fn bndstx(&mut self, dst: T0, src: T1);
}

/// Emits `MONITORX`.
///
/// Assembly forms: `monitorx <mem(ds:zax)>, <ecx>, <edx>`.
pub trait MonitorxEmitter<T0, T1, T2> {
    fn monitorx(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `MWAITX`.
///
/// Assembly forms: `mwaitx <eax>, <ecx>, <ebx>`.
pub trait MwaitxEmitter<T0, T1, T2> {
    fn mwaitx(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `MCOMMIT`.
///
/// Assembly forms: `mcommit`.
pub trait McommitEmitter {
    fn mcommit(&mut self);
}

/// Emits `PTWRITE`.
///
/// Assembly forms: `ptwrite r32/m32`, `ptwrite r64/m64`.
pub trait PtwriteEmitter<T0> {
    fn ptwrite(&mut self, src: T0);
}

/// Emits `ENQCMD`.
///
/// Assembly forms: `enqcmd m512(es:r32), m512`, `enqcmd m512(es:r64), m512`.
pub trait EnqcmdEmitter<T0, T1> {
    fn enqcmd(&mut self, dst: T0, src: T1);
}

/// Emits `ENQCMDS`.
///
/// Assembly forms: `enqcmds m512(es:r32), m512`, `enqcmds m512(es:r64), m512`.
pub trait EnqcmdsEmitter<T0, T1> {
    fn enqcmds(&mut self, dst: T0, src: T1);
}

/// Emits `TPAUSE`.
///
/// Assembly forms: `tpause r32, <edx>, <eax>`.
pub trait TpauseEmitter<T0, T1, T2> {
    fn tpause(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `UMONITOR`.
///
/// Assembly forms: `umonitor mem(ds:r32)`, `umonitor mem(ds:r64)`.
pub trait UmonitorEmitter<T0> {
    fn umonitor(&mut self, mem: T0);
}

/// Emits `UMWAIT`.
///
/// Assembly forms: `umwait r32, <edx>, <eax>`.
pub trait UmwaitEmitter<T0, T1, T2> {
    fn umwait(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `RDRAND`.
///
/// Assembly forms: `rdrand rv`.
pub trait RdrandEmitter<T0> {
    fn rdrand(&mut self, dst: T0);
}

/// Emits `RDSEED`.
///
/// Assembly forms: `rdseed rv`.
pub trait RdseedEmitter<T0> {
    fn rdseed(&mut self, dst: T0);
}

/// Emits `LLWPCB`.
///
/// Assembly forms: `llwpcb r32`, `llwpcb r64`.
pub trait LlwpcbEmitter<T0> {
    fn llwpcb(&mut self, src: T0);
}

/// Emits `LWPINS`.
///
/// Assembly forms: `lwpins r32, r32/m32, imm32`, `lwpins r64, r32/m32, imm32`.
pub trait LwpinsEmitter<T0, T1, T2> {
    fn lwpins(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `LWPVAL`.
///
/// Assembly forms: `lwpval r32, r32/m32, imm32`, `lwpval r64, r32/m32, imm32`.
pub trait LwpvalEmitter<T0, T1, T2> {
    fn lwpval(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `SLWPCB`.
///
/// Assembly forms: `slwpcb r32`, `slwpcb r64`.
pub trait SlwpcbEmitter<T0> {
    fn slwpcb(&mut self, dst: T0);
}

/// Emits `XABORT`.
///
/// Assembly forms: `xabort imm8`.
pub trait XabortEmitter<T0> {
    fn xabort(&mut self, imm: T0);
}

/// Emits `XBEGIN`.
///
/// Assembly forms: `xbegin rel16`, `xbegin rel32`.
pub trait XbeginEmitter<T0> {
    fn xbegin(&mut self, target: T0);
}

/// Emits `XEND`.
///
/// Assembly forms: `xend`.
pub trait XendEmitter {
    fn xend(&mut self);
}

/// Emits `XTEST`.
///
/// Assembly forms: `xtest`.
pub trait XtestEmitter {
    fn xtest(&mut self);
}

/// Emits `XRESLDTRK`.
///
/// Assembly forms: `xresldtrk`.
pub trait XresldtrkEmitter {
    fn xresldtrk(&mut self);
}

/// Emits `XSUSLDTRK`.
///
/// Assembly forms: `xsusldtrk`.
pub trait XsusldtrkEmitter {
    fn xsusldtrk(&mut self);
}

/// Emits `ENDBR32`.
///
/// Assembly forms: `endbr32`.
pub trait Endbr32Emitter {
    fn endbr32(&mut self);
}

/// Emits `ENDBR64`.
///
/// Assembly forms: `endbr64`.
pub trait Endbr64Emitter {
    fn endbr64(&mut self);
}

/// Emits `CLRSSBSY`.
///
/// Assembly forms: `clrssbsy m64`.
pub trait ClrssbsyEmitter<T0> {
    fn clrssbsy(&mut self, mem: T0);
}

/// Emits `SETSSBSY`.
///
/// Assembly forms: `setssbsy`.
pub trait SetssbsyEmitter {
    fn setssbsy(&mut self);
}

/// Emits `RSTORSSP`.
///
/// Assembly forms: `rstorssp m64`.
pub trait RstorsspEmitter<T0> {
    fn rstorssp(&mut self, mem: T0);
}

/// Emits `SAVEPREVSSP`.
///
/// Assembly forms: `saveprevssp`.
pub trait SaveprevsspEmitter {
    fn saveprevssp(&mut self);
}

/// Emits `INCSSPD`.
///
/// Assembly forms: `incsspd r32[7:0]`.
pub trait IncsspdEmitter<T0> {
    fn incsspd(&mut self, src: T0);
}

/// Emits `INCSSPQ`.
///
/// Assembly forms: `incsspq r64[7:0]`.
pub trait IncsspqEmitter<T0> {
    fn incsspq(&mut self, src: T0);
}

/// Emits `RDSSPD`.
///
/// Assembly forms: `rdsspd r32`.
pub trait RdsspdEmitter<T0> {
    fn rdsspd(&mut self, dst: T0);
}

/// Emits `RDSSPQ`.
///
/// Assembly forms: `rdsspq r64`.
pub trait RdsspqEmitter<T0> {
    fn rdsspq(&mut self, dst: T0);
}

/// Emits `WRSSD`.
///
/// Assembly forms: `wrssd W:m32, r32`.
pub trait WrssdEmitter<T0, T1> {
    fn wrssd(&mut self, dst: T0, src: T1);
}

/// Emits `WRSSQ`.
///
/// Assembly forms: `wrssq W:m64, r64`.
pub trait WrssqEmitter<T0, T1> {
    fn wrssq(&mut self, dst: T0, src: T1);
}

/// Emits `WRUSSD`.
///
/// Assembly forms: `wrussd m32, r32`.
pub trait WrussdEmitter<T0, T1> {
    fn wrussd(&mut self, dst: T0, src: T1);
}

/// Emits `WRUSSQ`.
///
/// Assembly forms: `wrussq m64, r64`.
pub trait WrussqEmitter<T0, T1> {
    fn wrussq(&mut self, dst: T0, src: T1);
}

/// Emits `HRESET`.
///
/// Assembly forms: `hreset imm8, <eax>`.
pub trait HresetEmitter<T0, T1> {
    fn hreset(&mut self, imm: T0, dst: T1);
}

/// Emits `CLUI`.
///
/// Assembly forms: `clui`.
pub trait CluiEmitter {
    fn clui(&mut self);
}

/// Emits `SENDUIPI`.
///
/// Assembly forms: `senduipi r64`.
pub trait SenduipiEmitter<T0> {
    fn senduipi(&mut self, src: T0);
}

/// Emits `TESTUI`.
///
/// Assembly forms: `testui`.
pub trait TestuiEmitter {
    fn testui(&mut self);
}

/// Emits `STUI`.
///
/// Assembly forms: `stui`.
pub trait StuiEmitter {
    fn stui(&mut self);
}

/// Emits `UIRET`.
///
/// Assembly forms: `uiret`.
pub trait UiretEmitter {
    fn uiret(&mut self);
}

/// Emits `CLTS`.
///
/// Assembly forms: `clts`.
pub trait CltsEmitter {
    fn clts(&mut self);
}

/// Emits `HLT`.
///
/// Assembly forms: `hlt`.
pub trait HltEmitter {
    fn hlt(&mut self);
}

/// Emits `INVD`.
///
/// Assembly forms: `invd`.
pub trait InvdEmitter {
    fn invd(&mut self);
}

/// Emits `INVLPG`.
///
/// Assembly forms: `invlpg mem`.
pub trait InvlpgEmitter<T0> {
    fn invlpg(&mut self, mem: T0);
}

/// Emits `INVPCID`.
///
/// Assembly forms: `invpcid r32, m128`, `invpcid r64, m128`.
pub trait InvpcidEmitter<T0, T1> {
    fn invpcid(&mut self, lhs: T0, rhs: T1);
}

/// Emits `LGDT`.
///
/// Assembly forms: `lgdt mem`.
pub trait LgdtEmitter<T0> {
    fn lgdt(&mut self, mem: T0);
}

/// Emits `LIDT`.
///
/// Assembly forms: `lidt mem`.
pub trait LidtEmitter<T0> {
    fn lidt(&mut self, mem: T0);
}

/// Emits `LLDT`.
///
/// Assembly forms: `lldt r16/m16`.
pub trait LldtEmitter<T0> {
    fn lldt(&mut self, src: T0);
}

/// Emits `LMSW`.
///
/// Assembly forms: `lmsw r16/m16`.
pub trait LmswEmitter<T0> {
    fn lmsw(&mut self, src: T0);
}

/// Emits `LTR`.
///
/// Assembly forms: `ltr r16/m16`.
pub trait LtrEmitter<T0> {
    fn ltr(&mut self, src: T0);
}

/// Emits `RDMSR`.
///
/// Assembly forms: `rdmsr <edx>, <eax>, <ecx>`, `rdmsr r64, imm32`.
pub trait RdmsrEmitter<T0, T1, T2> {
    fn rdmsr(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `RDPMC`.
///
/// Assembly forms: `rdpmc <edx>, <eax>, <ecx>`.
pub trait RdpmcEmitter<T0, T1, T2> {
    fn rdpmc(&mut self, dst: T0, dst2: T1, src: T2);
}

/// Emits `SWAPGS`.
///
/// Assembly forms: `swapgs`.
pub trait SwapgsEmitter {
    fn swapgs(&mut self);
}

/// Emits `WBINVD`.
///
/// Assembly forms: `wbinvd`.
pub trait WbinvdEmitter {
    fn wbinvd(&mut self);
}

/// Emits `WBNOINVD`.
///
/// Assembly forms: `wbnoinvd`.
pub trait WbnoinvdEmitter {
    fn wbnoinvd(&mut self);
}

/// Emits `WRMSR`.
///
/// Assembly forms: `wrmsr <edx>, <eax>, <ecx>`.
pub trait WrmsrEmitter<T0, T1, T2> {
    fn wrmsr(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `XSETBV`.
///
/// Assembly forms: `xsetbv <edx>, <eax>, <ecx>`.
pub trait XsetbvEmitter<T0, T1, T2> {
    fn xsetbv(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `INVLPGB`.
///
/// Assembly forms: `invlpgb <eax>, <edx>, <ecx>`, `invlpgb <rax>, <edx>, <ecx>`.
pub trait InvlpgbEmitter<T0, T1, T2> {
    fn invlpgb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `TLBSYNC`.
///
/// Assembly forms: `tlbsync`.
pub trait TlbsyncEmitter {
    fn tlbsync(&mut self);
}

/// Emits `MONITOR`.
///
/// Assembly forms: `monitor <mem(ds:zax)>, <ecx>, <edx>`.
pub trait MonitorEmitter<T0, T1, T2> {
    fn monitor(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `MWAIT`.
///
/// Assembly forms: `mwait <eax>, <ecx>`.
pub trait MwaitEmitter<T0, T1> {
    fn mwait(&mut self, lhs: T0, rhs: T1);
}

/// Emits `CLAC`.
///
/// Assembly forms: `clac`.
pub trait ClacEmitter {
    fn clac(&mut self);
}

/// Emits `STAC`.
///
/// Assembly forms: `stac`.
pub trait StacEmitter {
    fn stac(&mut self);
}

/// Emits `SKINIT`.
///
/// Assembly forms: `skinit <eax>`.
pub trait SkinitEmitter<T0> {
    fn skinit(&mut self, dst: T0);
}

/// Emits `STGI`.
///
/// Assembly forms: `stgi`.
pub trait StgiEmitter {
    fn stgi(&mut self);
}

/// Emits `PSMASH`.
///
/// Assembly forms: `psmash`.
pub trait PsmashEmitter {
    fn psmash(&mut self);
}

/// Emits `PVALIDATE`.
///
/// Assembly forms: `pvalidate`.
pub trait PvalidateEmitter {
    fn pvalidate(&mut self);
}

/// Emits `RMPADJUST`.
///
/// Assembly forms: `rmpadjust`.
pub trait RmpadjustEmitter {
    fn rmpadjust(&mut self);
}

/// Emits `RMPUPDATE`.
///
/// Assembly forms: `rmpupdate`.
pub trait RmpupdateEmitter {
    fn rmpupdate(&mut self);
}

/// Emits `INVEPT`.
///
/// Assembly forms: `invept r32, m128`, `invept r64, m128`.
pub trait InveptEmitter<T0, T1> {
    fn invept(&mut self, lhs: T0, rhs: T1);
}

/// Emits `INVVPID`.
///
/// Assembly forms: `invvpid r32, m128`, `invvpid r64, m128`.
pub trait InvvpidEmitter<T0, T1> {
    fn invvpid(&mut self, lhs: T0, rhs: T1);
}

/// Emits `VMCALL`.
///
/// Assembly forms: `vmcall`.
pub trait VmcallEmitter {
    fn vmcall(&mut self);
}

/// Emits `VMCLEAR`.
///
/// Assembly forms: `vmclear m64`.
pub trait VmclearEmitter<T0> {
    fn vmclear(&mut self, mem: T0);
}

/// Emits `VMFUNC`.
///
/// Assembly forms: `vmfunc`.
pub trait VmfuncEmitter {
    fn vmfunc(&mut self);
}

/// Emits `VMLAUNCH`.
///
/// Assembly forms: `vmlaunch`.
pub trait VmlaunchEmitter {
    fn vmlaunch(&mut self);
}

/// Emits `VMPTRLD`.
///
/// Assembly forms: `vmptrld m64`.
pub trait VmptrldEmitter<T0> {
    fn vmptrld(&mut self, mem: T0);
}

/// Emits `VMPTRST`.
///
/// Assembly forms: `vmptrst m64`.
pub trait VmptrstEmitter<T0> {
    fn vmptrst(&mut self, dst: T0);
}

/// Emits `VMREAD`.
///
/// Assembly forms: `vmread r32/m32, r32`, `vmread r64/m64, r64`.
pub trait VmreadEmitter<T0, T1> {
    fn vmread(&mut self, dst: T0, src: T1);
}

/// Emits `VMRESUME`.
///
/// Assembly forms: `vmresume`.
pub trait VmresumeEmitter {
    fn vmresume(&mut self);
}

/// Emits `VMWRITE`.
///
/// Assembly forms: `vmwrite r32, r32/m32`, `vmwrite r64, r64/m64`.
pub trait VmwriteEmitter<T0, T1> {
    fn vmwrite(&mut self, lhs: T0, rhs: T1);
}

/// Emits `VMXOFF`.
///
/// Assembly forms: `vmxoff`.
pub trait VmxoffEmitter {
    fn vmxoff(&mut self);
}

/// Emits `VMXON`.
///
/// Assembly forms: `vmxon m64`.
pub trait VmxonEmitter<T0> {
    fn vmxon(&mut self, mem: T0);
}

/// Emits `CLGI`.
///
/// Assembly forms: `clgi`.
pub trait ClgiEmitter {
    fn clgi(&mut self);
}

/// Emits `INVLPGA`.
///
/// Assembly forms: `invlpga <eax>, <ecx>`, `invlpga <rax>, <ecx>`.
pub trait InvlpgaEmitter<T0, T1> {
    fn invlpga(&mut self, lhs: T0, rhs: T1);
}

/// Emits `VMLOAD`.
///
/// Assembly forms: `vmload <eax>`, `vmload <rax>`.
pub trait VmloadEmitter<T0> {
    fn vmload(&mut self, src: T0);
}

/// Emits `VMMCALL`.
///
/// Assembly forms: `vmmcall`.
pub trait VmmcallEmitter {
    fn vmmcall(&mut self);
}

/// Emits `VMRUN`.
///
/// Assembly forms: `vmrun <eax>`, `vmrun <rax>`.
pub trait VmrunEmitter<T0> {
    fn vmrun(&mut self, dst: T0);
}

/// Emits `VMSAVE`.
///
/// Assembly forms: `vmsave <eax>`, `vmsave <rax>`.
pub trait VmsaveEmitter<T0> {
    fn vmsave(&mut self, src: T0);
}

/// Emits `VMGEXIT`.
///
/// Assembly forms: `vmgexit`.
pub trait VmgexitEmitter {
    fn vmgexit(&mut self);
}

/// Emits `F2XM1`.
///
/// Assembly forms: `f2xm1`.
pub trait F2xm1Emitter {
    fn f2xm1(&mut self);
}

/// Emits `FABS`.
///
/// Assembly forms: `fabs`.
pub trait FabsEmitter {
    fn fabs(&mut self);
}

/// Emits `FADD`.
///
/// Assembly forms: `fadd m32fp`, `fadd m64fp`, `fadd st(0), st(i)`.
pub trait FaddEmitter<T0, T1> {
    fn fadd(&mut self, lhs: T0, rhs: T1);
}

/// Emits `FADD_1`.
///
/// Assembly forms: `fadd m32fp`, `fadd m64fp`, `fadd st(0), st(i)`.
pub trait Fadd1Emitter<T0> {
    fn fadd_1(&mut self, mem: T0);
}

/// Emits `FADDP`.
///
/// Assembly forms: `faddp`, `faddp st(i)`.
pub trait FaddpEmitter<T0> {
    fn faddp(&mut self, src: T0);
}

/// Emits `FADDP_0`.
///
/// Assembly forms: `faddp`, `faddp st(i)`.
pub trait Faddp0Emitter {
    fn faddp_0(&mut self);
}

/// Emits `FBLD`.
///
/// Assembly forms: `fbld m80dec`.
pub trait FbldEmitter<T0> {
    fn fbld(&mut self, mem: T0);
}

/// Emits `FBSTP`.
///
/// Assembly forms: `fbstp m80bcd`.
pub trait FbstpEmitter<T0> {
    fn fbstp(&mut self, dst: T0);
}

/// Emits `FCHS`.
///
/// Assembly forms: `fchs`.
pub trait FchsEmitter {
    fn fchs(&mut self);
}

/// Emits `FCLEX`.
///
/// Assembly forms: `fclex`.
pub trait FclexEmitter {
    fn fclex(&mut self);
}

/// Emits `FCMOVB`.
///
/// Assembly forms: `fcmovb st(i)`.
pub trait FcmovbEmitter<T0> {
    fn fcmovb(&mut self, src: T0);
}

/// Emits `FCMOVBE`.
///
/// Assembly forms: `fcmovbe st(i)`.
pub trait FcmovbeEmitter<T0> {
    fn fcmovbe(&mut self, src: T0);
}

/// Emits `FCMOVE`.
///
/// Assembly forms: `fcmove st(i)`.
pub trait FcmoveEmitter<T0> {
    fn fcmove(&mut self, src: T0);
}

/// Emits `FCMOVNB`.
///
/// Assembly forms: `fcmovnb st(i)`.
pub trait FcmovnbEmitter<T0> {
    fn fcmovnb(&mut self, src: T0);
}

/// Emits `FCMOVNBE`.
///
/// Assembly forms: `fcmovnbe st(i)`.
pub trait FcmovnbeEmitter<T0> {
    fn fcmovnbe(&mut self, src: T0);
}

/// Emits `FCMOVNE`.
///
/// Assembly forms: `fcmovne st(i)`.
pub trait FcmovneEmitter<T0> {
    fn fcmovne(&mut self, src: T0);
}

/// Emits `FCMOVNU`.
///
/// Assembly forms: `fcmovnu st(i)`.
pub trait FcmovnuEmitter<T0> {
    fn fcmovnu(&mut self, src: T0);
}

/// Emits `FCMOVU`.
///
/// Assembly forms: `fcmovu st(i)`.
pub trait FcmovuEmitter<T0> {
    fn fcmovu(&mut self, src: T0);
}

/// Emits `FCOM`.
///
/// Assembly forms: `fcom`, `fcom m32fp`, `fcom m64fp`.
pub trait FcomEmitter<T0> {
    fn fcom(&mut self, src: T0);
}

/// Emits `FCOM_0`.
///
/// Assembly forms: `fcom`, `fcom m32fp`, `fcom m64fp`.
pub trait Fcom0Emitter {
    fn fcom_0(&mut self);
}

/// Emits `FCOMP`.
///
/// Assembly forms: `fcomp`, `fcomp m32fp`, `fcomp m64fp`.
pub trait FcompEmitter<T0> {
    fn fcomp(&mut self, src: T0);
}

/// Emits `FCOMP_0`.
///
/// Assembly forms: `fcomp`, `fcomp m32fp`, `fcomp m64fp`.
pub trait Fcomp0Emitter {
    fn fcomp_0(&mut self);
}

/// Emits `FCOMPP`.
///
/// Assembly forms: `fcompp`.
pub trait FcomppEmitter {
    fn fcompp(&mut self);
}

/// Emits `FCOMI`.
///
/// Assembly forms: `fcomi st(i)`.
pub trait FcomiEmitter<T0> {
    fn fcomi(&mut self, src: T0);
}

/// Emits `FCOMIP`.
///
/// Assembly forms: `fcomip st(i)`.
pub trait FcomipEmitter<T0> {
    fn fcomip(&mut self, src: T0);
}

/// Emits `FCOS`.
///
/// Assembly forms: `fcos`.
pub trait FcosEmitter {
    fn fcos(&mut self);
}

/// Emits `FDECSTP`.
///
/// Assembly forms: `fdecstp`.
pub trait FdecstpEmitter {
    fn fdecstp(&mut self);
}

/// Emits `FDIV`.
///
/// Assembly forms: `fdiv m32fp`, `fdiv m64fp`, `fdiv st(0), st(i)`.
pub trait FdivEmitter<T0, T1> {
    fn fdiv(&mut self, lhs: T0, rhs: T1);
}

/// Emits `FDIV_1`.
///
/// Assembly forms: `fdiv m32fp`, `fdiv m64fp`, `fdiv st(0), st(i)`.
pub trait Fdiv1Emitter<T0> {
    fn fdiv_1(&mut self, mem: T0);
}

/// Emits `FDIVP`.
///
/// Assembly forms: `fdivp`, `fdivp st(i)`.
pub trait FdivpEmitter<T0> {
    fn fdivp(&mut self, src: T0);
}

/// Emits `FDIVP_0`.
///
/// Assembly forms: `fdivp`, `fdivp st(i)`.
pub trait Fdivp0Emitter {
    fn fdivp_0(&mut self);
}

/// Emits `FDIVR`.
///
/// Assembly forms: `fdivr m32fp`, `fdivr m64fp`, `fdivr st(0), st(i)`.
pub trait FdivrEmitter<T0, T1> {
    fn fdivr(&mut self, lhs: T0, rhs: T1);
}

/// Emits `FDIVR_1`.
///
/// Assembly forms: `fdivr m32fp`, `fdivr m64fp`, `fdivr st(0), st(i)`.
pub trait Fdivr1Emitter<T0> {
    fn fdivr_1(&mut self, mem: T0);
}

/// Emits `FDIVRP`.
///
/// Assembly forms: `fdivrp`, `fdivrp st(i)`.
pub trait FdivrpEmitter<T0> {
    fn fdivrp(&mut self, src: T0);
}

/// Emits `FDIVRP_0`.
///
/// Assembly forms: `fdivrp`, `fdivrp st(i)`.
pub trait Fdivrp0Emitter {
    fn fdivrp_0(&mut self);
}

/// Emits `FFREE`.
///
/// Assembly forms: `ffree st(i)`.
pub trait FfreeEmitter<T0> {
    fn ffree(&mut self, src: T0);
}

/// Emits `FIADD`.
///
/// Assembly forms: `fiadd m16int`, `fiadd m32int`.
pub trait FiaddEmitter<T0> {
    fn fiadd(&mut self, mem: T0);
}

/// Emits `FICOM`.
///
/// Assembly forms: `ficom m16int`, `ficom m32int`.
pub trait FicomEmitter<T0> {
    fn ficom(&mut self, mem: T0);
}

/// Emits `FICOMP`.
///
/// Assembly forms: `ficomp m16int`, `ficomp m32int`.
pub trait FicompEmitter<T0> {
    fn ficomp(&mut self, mem: T0);
}

/// Emits `FIDIV`.
///
/// Assembly forms: `fidiv m16int`, `fidiv m32int`.
pub trait FidivEmitter<T0> {
    fn fidiv(&mut self, mem: T0);
}

/// Emits `FIDIVR`.
///
/// Assembly forms: `fidivr m16int`, `fidivr m32int`.
pub trait FidivrEmitter<T0> {
    fn fidivr(&mut self, mem: T0);
}

/// Emits `FILD`.
///
/// Assembly forms: `fild m16int`, `fild m32int`, `fild m64int`.
pub trait FildEmitter<T0> {
    fn fild(&mut self, mem: T0);
}

/// Emits `FIMUL`.
///
/// Assembly forms: `fimul m16int`, `fimul m32int`.
pub trait FimulEmitter<T0> {
    fn fimul(&mut self, mem: T0);
}

/// Emits `FINCSTP`.
///
/// Assembly forms: `fincstp`.
pub trait FincstpEmitter {
    fn fincstp(&mut self);
}

/// Emits `FINIT`.
///
/// Assembly forms: `finit`.
pub trait FinitEmitter {
    fn finit(&mut self);
}

/// Emits `FISUB`.
///
/// Assembly forms: `fisub m16int`, `fisub m32int`.
pub trait FisubEmitter<T0> {
    fn fisub(&mut self, mem: T0);
}

/// Emits `FISUBR`.
///
/// Assembly forms: `fisubr m16int`, `fisubr m32int`.
pub trait FisubrEmitter<T0> {
    fn fisubr(&mut self, mem: T0);
}

/// Emits `FNINIT`.
///
/// Assembly forms: `fninit`.
pub trait FninitEmitter {
    fn fninit(&mut self);
}

/// Emits `FIST`.
///
/// Assembly forms: `fist m16int`, `fist m32int`.
pub trait FistEmitter<T0> {
    fn fist(&mut self, dst: T0);
}

/// Emits `FISTP`.
///
/// Assembly forms: `fistp m16int`, `fistp m32int`, `fistp m64int`.
pub trait FistpEmitter<T0> {
    fn fistp(&mut self, dst: T0);
}

/// Emits `FISTTP`.
///
/// Assembly forms: `fisttp m16int`, `fisttp m32int`, `fisttp m64int`.
pub trait FisttpEmitter<T0> {
    fn fisttp(&mut self, dst: T0);
}

/// Emits `FLD`.
///
/// Assembly forms: `fld m32fp`, `fld m64fp`, `fld m80fp`.
pub trait FldEmitter<T0> {
    fn fld(&mut self, mem: T0);
}

/// Emits `FLD1`.
///
/// Assembly forms: `fld1`.
pub trait Fld1Emitter {
    fn fld1(&mut self);
}

/// Emits `FLDL2T`.
///
/// Assembly forms: `fldl2t`.
pub trait Fldl2tEmitter {
    fn fldl2t(&mut self);
}

/// Emits `FLDL2E`.
///
/// Assembly forms: `fldl2e`.
pub trait Fldl2eEmitter {
    fn fldl2e(&mut self);
}

/// Emits `FLDPI`.
///
/// Assembly forms: `fldpi`.
pub trait FldpiEmitter {
    fn fldpi(&mut self);
}

/// Emits `FLDLG2`.
///
/// Assembly forms: `fldlg2`.
pub trait Fldlg2Emitter {
    fn fldlg2(&mut self);
}

/// Emits `FLDLN2`.
///
/// Assembly forms: `fldln2`.
pub trait Fldln2Emitter {
    fn fldln2(&mut self);
}

/// Emits `FLDZ`.
///
/// Assembly forms: `fldz`.
pub trait FldzEmitter {
    fn fldz(&mut self);
}

/// Emits `FLDCW`.
///
/// Assembly forms: `fldcw m16`.
pub trait FldcwEmitter<T0> {
    fn fldcw(&mut self, mem: T0);
}

/// Emits `FLDENV`.
///
/// Assembly forms: `fldenv mem`.
pub trait FldenvEmitter<T0> {
    fn fldenv(&mut self, mem: T0);
}

/// Emits `FMUL`.
///
/// Assembly forms: `fmul m32fp`, `fmul m64fp`, `fmul st(0), st(i)`.
pub trait FmulEmitter<T0, T1> {
    fn fmul(&mut self, lhs: T0, rhs: T1);
}

/// Emits `FMUL_1`.
///
/// Assembly forms: `fmul m32fp`, `fmul m64fp`, `fmul st(0), st(i)`.
pub trait Fmul1Emitter<T0> {
    fn fmul_1(&mut self, mem: T0);
}

/// Emits `FMULP`.
///
/// Assembly forms: `fmulp`, `fmulp st(i)`.
pub trait FmulpEmitter<T0> {
    fn fmulp(&mut self, src: T0);
}

/// Emits `FMULP_0`.
///
/// Assembly forms: `fmulp`, `fmulp st(i)`.
pub trait Fmulp0Emitter {
    fn fmulp_0(&mut self);
}

/// Emits `FNCLEX`.
///
/// Assembly forms: `fnclex`.
pub trait FnclexEmitter {
    fn fnclex(&mut self);
}

/// Emits `FNOP`.
///
/// Assembly forms: `fnop`.
pub trait FnopEmitter {
    fn fnop(&mut self);
}

/// Emits `FNSAVE`.
///
/// Assembly forms: `fnsave mem`.
pub trait FnsaveEmitter<T0> {
    fn fnsave(&mut self, dst: T0);
}

/// Emits `FNSTENV`.
///
/// Assembly forms: `fnstenv mem`.
pub trait FnstenvEmitter<T0> {
    fn fnstenv(&mut self, dst: T0);
}

/// Emits `FNSTCW`.
///
/// Assembly forms: `fnstcw m16`.
pub trait FnstcwEmitter<T0> {
    fn fnstcw(&mut self, dst: T0);
}

/// Emits `FPATAN`.
///
/// Assembly forms: `fpatan`.
pub trait FpatanEmitter {
    fn fpatan(&mut self);
}

/// Emits `FPREM`.
///
/// Assembly forms: `fprem`.
pub trait FpremEmitter {
    fn fprem(&mut self);
}

/// Emits `FPREM1`.
///
/// Assembly forms: `fprem1`.
pub trait Fprem1Emitter {
    fn fprem1(&mut self);
}

/// Emits `FPTAN`.
///
/// Assembly forms: `fptan`.
pub trait FptanEmitter {
    fn fptan(&mut self);
}

/// Emits `FRNDINT`.
///
/// Assembly forms: `frndint`.
pub trait FrndintEmitter {
    fn frndint(&mut self);
}

/// Emits `FRSTOR`.
///
/// Assembly forms: `frstor mem`.
pub trait FrstorEmitter<T0> {
    fn frstor(&mut self, mem: T0);
}

/// Emits `FSAVE`.
///
/// Assembly forms: `fsave mem`.
pub trait FsaveEmitter<T0> {
    fn fsave(&mut self, dst: T0);
}

/// Emits `FSCALE`.
///
/// Assembly forms: `fscale`.
pub trait FscaleEmitter {
    fn fscale(&mut self);
}

/// Emits `FSIN`.
///
/// Assembly forms: `fsin`.
pub trait FsinEmitter {
    fn fsin(&mut self);
}

/// Emits `FSINCOS`.
///
/// Assembly forms: `fsincos`.
pub trait FsincosEmitter {
    fn fsincos(&mut self);
}

/// Emits `FSQRT`.
///
/// Assembly forms: `fsqrt`.
pub trait FsqrtEmitter {
    fn fsqrt(&mut self);
}

/// Emits `FST`.
///
/// Assembly forms: `fst m32fp`, `fst m64fp`, `fst st(i)`.
pub trait FstEmitter<T0> {
    fn fst(&mut self, dst: T0);
}

/// Emits `FSTP`.
///
/// Assembly forms: `fstp m32fp`, `fstp m64fp`, `fstp m80fp`.
pub trait FstpEmitter<T0> {
    fn fstp(&mut self, dst: T0);
}

/// Emits `FSTCW`.
///
/// Assembly forms: `fstcw m16`.
pub trait FstcwEmitter<T0> {
    fn fstcw(&mut self, dst: T0);
}

/// Emits `FSTENV`.
///
/// Assembly forms: `fstenv mem`.
pub trait FstenvEmitter<T0> {
    fn fstenv(&mut self, dst: T0);
}

/// Emits `FSUB`.
///
/// Assembly forms: `fsub m32fp`, `fsub m64fp`, `fsub st(0), st(i)`.
pub trait FsubEmitter<T0, T1> {
    fn fsub(&mut self, lhs: T0, rhs: T1);
}

/// Emits `FSUB_1`.
///
/// Assembly forms: `fsub m32fp`, `fsub m64fp`, `fsub st(0), st(i)`.
pub trait Fsub1Emitter<T0> {
    fn fsub_1(&mut self, mem: T0);
}

/// Emits `FSUBP`.
///
/// Assembly forms: `fsubp`, `fsubp st(i)`.
pub trait FsubpEmitter<T0> {
    fn fsubp(&mut self, src: T0);
}

/// Emits `FSUBP_0`.
///
/// Assembly forms: `fsubp`, `fsubp st(i)`.
pub trait Fsubp0Emitter {
    fn fsubp_0(&mut self);
}

/// Emits `FSUBR`.
///
/// Assembly forms: `fsubr m32fp`, `fsubr m64fp`, `fsubr st(0), st(i)`.
pub trait FsubrEmitter<T0, T1> {
    fn fsubr(&mut self, lhs: T0, rhs: T1);
}

/// Emits `FSUBR_1`.
///
/// Assembly forms: `fsubr m32fp`, `fsubr m64fp`, `fsubr st(0), st(i)`.
pub trait Fsubr1Emitter<T0> {
    fn fsubr_1(&mut self, mem: T0);
}

/// Emits `FSUBRP`.
///
/// Assembly forms: `fsubrp`, `fsubrp st(i)`.
pub trait FsubrpEmitter<T0> {
    fn fsubrp(&mut self, src: T0);
}

/// Emits `FSUBRP_0`.
///
/// Assembly forms: `fsubrp`, `fsubrp st(i)`.
pub trait Fsubrp0Emitter {
    fn fsubrp_0(&mut self);
}

/// Emits `FTST`.
///
/// Assembly forms: `ftst`.
pub trait FtstEmitter {
    fn ftst(&mut self);
}

/// Emits `FUCOM`.
///
/// Assembly forms: `fucom`, `fucom st(i)`.
pub trait FucomEmitter<T0> {
    fn fucom(&mut self, src: T0);
}

/// Emits `FUCOM_0`.
///
/// Assembly forms: `fucom`, `fucom st(i)`.
pub trait Fucom0Emitter {
    fn fucom_0(&mut self);
}

/// Emits `FUCOMI`.
///
/// Assembly forms: `fucomi st(i)`.
pub trait FucomiEmitter<T0> {
    fn fucomi(&mut self, src: T0);
}

/// Emits `FUCOMIP`.
///
/// Assembly forms: `fucomip st(i)`.
pub trait FucomipEmitter<T0> {
    fn fucomip(&mut self, src: T0);
}

/// Emits `FUCOMP`.
///
/// Assembly forms: `fucomp`, `fucomp st(i)`.
pub trait FucompEmitter<T0> {
    fn fucomp(&mut self, src: T0);
}

/// Emits `FUCOMP_0`.
///
/// Assembly forms: `fucomp`, `fucomp st(i)`.
pub trait Fucomp0Emitter {
    fn fucomp_0(&mut self);
}

/// Emits `FUCOMPP`.
///
/// Assembly forms: `fucompp`.
pub trait FucomppEmitter {
    fn fucompp(&mut self);
}

/// Emits `FWAIT`.
///
/// Assembly forms: `fwait`.
pub trait FwaitEmitter {
    fn fwait(&mut self);
}

/// Emits `FXAM`.
///
/// Assembly forms: `fxam`.
pub trait FxamEmitter {
    fn fxam(&mut self);
}

/// Emits `FXCH`.
///
/// Assembly forms: `fxch`, `fxch st(i)`.
pub trait FxchEmitter<T0> {
    fn fxch(&mut self, src: T0);
}

/// Emits `FXTRACT`.
///
/// Assembly forms: `fxtract`.
pub trait FxtractEmitter {
    fn fxtract(&mut self);
}

/// Emits `FYL2X`.
///
/// Assembly forms: `fyl2x`.
pub trait Fyl2xEmitter {
    fn fyl2x(&mut self);
}

/// Emits `FYL2XP1`.
///
/// Assembly forms: `fyl2xp1`.
pub trait Fyl2xp1Emitter {
    fn fyl2xp1(&mut self);
}

/// Emits `FSTSW`.
///
/// Assembly forms: `fstsw ax`, `fstsw m16`.
pub trait FstswEmitter<T0> {
    fn fstsw(&mut self, dst: T0);
}

/// Emits `FNSTSW`.
///
/// Assembly forms: `fnstsw ax`, `fnstsw m16`.
pub trait FnstswEmitter<T0> {
    fn fnstsw(&mut self, dst: T0);
}

/// Emits `ADDPD`.
///
/// Assembly forms: `addpd xmm, xmm/m128`.
pub trait AddpdEmitter<T0, T1> {
    fn addpd(&mut self, dst: T0, src: T1);
}

/// Emits `ADDPS`.
///
/// Assembly forms: `addps xmm, xmm/m128`.
pub trait AddpsEmitter<T0, T1> {
    fn addps(&mut self, dst: T0, src: T1);
}

/// Emits `ADDSD`.
///
/// Assembly forms: `addsd xmm[63:0], xmm[63:0]/m64`.
pub trait AddsdEmitter<T0, T1> {
    fn addsd(&mut self, dst: T0, src: T1);
}

/// Emits `ADDSS`.
///
/// Assembly forms: `addss xmm[31:0], xmm[31:0]/m32`.
pub trait AddssEmitter<T0, T1> {
    fn addss(&mut self, dst: T0, src: T1);
}

/// Emits `ADDSUBPD`.
///
/// Assembly forms: `addsubpd xmm, xmm/m128`.
pub trait AddsubpdEmitter<T0, T1> {
    fn addsubpd(&mut self, dst: T0, src: T1);
}

/// Emits `ADDSUBPS`.
///
/// Assembly forms: `addsubps xmm, xmm/m128`.
pub trait AddsubpsEmitter<T0, T1> {
    fn addsubps(&mut self, dst: T0, src: T1);
}

/// Emits `ANDNPD`.
///
/// Assembly forms: `andnpd xmm, xmm/m128`.
pub trait AndnpdEmitter<T0, T1> {
    fn andnpd(&mut self, dst: T0, src: T1);
}

/// Emits `ANDNPS`.
///
/// Assembly forms: `andnps xmm, xmm/m128`.
pub trait AndnpsEmitter<T0, T1> {
    fn andnps(&mut self, dst: T0, src: T1);
}

/// Emits `ANDPD`.
///
/// Assembly forms: `andpd xmm, xmm/m128`.
pub trait AndpdEmitter<T0, T1> {
    fn andpd(&mut self, dst: T0, src: T1);
}

/// Emits `ANDPS`.
///
/// Assembly forms: `andps xmm, xmm/m128`.
pub trait AndpsEmitter<T0, T1> {
    fn andps(&mut self, dst: T0, src: T1);
}

/// Emits `BLENDPD`.
///
/// Assembly forms: `blendpd xmm, xmm/m128, imm8`.
pub trait BlendpdEmitter<T0, T1, T2> {
    fn blendpd(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `BLENDPS`.
///
/// Assembly forms: `blendps xmm, xmm/m128, imm8`.
pub trait BlendpsEmitter<T0, T1, T2> {
    fn blendps(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `BLENDVPD`.
///
/// Assembly forms: `blendvpd xmm, xmm/m128, <xmm0>`.
pub trait BlendvpdEmitter<T0, T1, T2> {
    fn blendvpd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `BLENDVPS`.
///
/// Assembly forms: `blendvps xmm, xmm/m128, <xmm0>`.
pub trait BlendvpsEmitter<T0, T1, T2> {
    fn blendvps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `CMPPD`.
///
/// Assembly forms: `cmppd xmm, xmm/m128, imm8`.
pub trait CmppdEmitter<T0, T1, T2> {
    fn cmppd(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `CMPPS`.
///
/// Assembly forms: `cmpps xmm, xmm/m128, imm8`.
pub trait CmppsEmitter<T0, T1, T2> {
    fn cmpps(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `CMPSD`.
///
/// Assembly forms: `cmpsd xmm[63:0], xmm[63:0]/m64, imm8`.
pub trait CmpsdEmitter<T0, T1, T2> {
    fn cmpsd(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `CMPSS`.
///
/// Assembly forms: `cmpss xmm[31:0], xmm[31:0]/m32, imm8`.
pub trait CmpssEmitter<T0, T1, T2> {
    fn cmpss(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `COMISD`.
///
/// Assembly forms: `comisd xmm[63:0], xmm[63:0]/m64`.
pub trait ComisdEmitter<T0, T1> {
    fn comisd(&mut self, lhs: T0, rhs: T1);
}

/// Emits `COMISS`.
///
/// Assembly forms: `comiss xmm[31:0], xmm[31:0]/m32`.
pub trait ComissEmitter<T0, T1> {
    fn comiss(&mut self, lhs: T0, rhs: T1);
}

/// Emits `CVTDQ2PD`.
///
/// Assembly forms: `cvtdq2pd xmm, xmm[63:0]/m64`.
pub trait Cvtdq2pdEmitter<T0, T1> {
    fn cvtdq2pd(&mut self, dst: T0, src: T1);
}

/// Emits `CVTDQ2PS`.
///
/// Assembly forms: `cvtdq2ps xmm, xmm/m128`.
pub trait Cvtdq2psEmitter<T0, T1> {
    fn cvtdq2ps(&mut self, dst: T0, src: T1);
}

/// Emits `CVTPD2DQ`.
///
/// Assembly forms: `cvtpd2dq xmm[63:0], xmm/m128`.
pub trait Cvtpd2dqEmitter<T0, T1> {
    fn cvtpd2dq(&mut self, dst: T0, src: T1);
}

/// Emits `CVTPD2PI`.
///
/// Assembly forms: `cvtpd2pi mm, xmm/m128`.
pub trait Cvtpd2piEmitter<T0, T1> {
    fn cvtpd2pi(&mut self, dst: T0, src: T1);
}

/// Emits `CVTPD2PS`.
///
/// Assembly forms: `cvtpd2ps xmm[63:0], xmm/m128`.
pub trait Cvtpd2psEmitter<T0, T1> {
    fn cvtpd2ps(&mut self, dst: T0, src: T1);
}

/// Emits `CVTPI2PD`.
///
/// Assembly forms: `cvtpi2pd xmm, mm[63:0]/m64`.
pub trait Cvtpi2pdEmitter<T0, T1> {
    fn cvtpi2pd(&mut self, dst: T0, src: T1);
}

/// Emits `CVTPI2PS`.
///
/// Assembly forms: `cvtpi2ps xmm[63:0], mm/m64`.
pub trait Cvtpi2psEmitter<T0, T1> {
    fn cvtpi2ps(&mut self, dst: T0, src: T1);
}

/// Emits `CVTPS2DQ`.
///
/// Assembly forms: `cvtps2dq xmm, xmm/m128`.
pub trait Cvtps2dqEmitter<T0, T1> {
    fn cvtps2dq(&mut self, dst: T0, src: T1);
}

/// Emits `CVTPS2PD`.
///
/// Assembly forms: `cvtps2pd xmm, xmm[63:0]/m64`.
pub trait Cvtps2pdEmitter<T0, T1> {
    fn cvtps2pd(&mut self, dst: T0, src: T1);
}

/// Emits `CVTPS2PI`.
///
/// Assembly forms: `cvtps2pi mm, xmm[63:0]/m64`.
pub trait Cvtps2piEmitter<T0, T1> {
    fn cvtps2pi(&mut self, dst: T0, src: T1);
}

/// Emits `CVTSD2SI`.
///
/// Assembly forms: `cvtsd2si ry, xmm[63:0]/m64`.
pub trait Cvtsd2siEmitter<T0, T1> {
    fn cvtsd2si(&mut self, dst: T0, src: T1);
}

/// Emits `CVTSD2SS`.
///
/// Assembly forms: `cvtsd2ss xmm[31:0], xmm[63:0]/m64`.
pub trait Cvtsd2ssEmitter<T0, T1> {
    fn cvtsd2ss(&mut self, dst: T0, src: T1);
}

/// Emits `CVTSI2SD`.
///
/// Assembly forms: `cvtsi2sd xmm[63:0], ry/my`.
pub trait Cvtsi2sdEmitter<T0, T1> {
    fn cvtsi2sd(&mut self, dst: T0, src: T1);
}

/// Emits `CVTSI2SS`.
///
/// Assembly forms: `cvtsi2ss xmm[31:0], ry/my`.
pub trait Cvtsi2ssEmitter<T0, T1> {
    fn cvtsi2ss(&mut self, dst: T0, src: T1);
}

/// Emits `CVTSS2SD`.
///
/// Assembly forms: `cvtss2sd xmm[63:0], xmm[31:0]/m32`.
pub trait Cvtss2sdEmitter<T0, T1> {
    fn cvtss2sd(&mut self, dst: T0, src: T1);
}

/// Emits `CVTSS2SI`.
///
/// Assembly forms: `cvtss2si ry, xmm[31:0]/m32`.
pub trait Cvtss2siEmitter<T0, T1> {
    fn cvtss2si(&mut self, dst: T0, src: T1);
}

/// Emits `CVTTPD2PI`.
///
/// Assembly forms: `cvttpd2pi mm, xmm/m128`.
pub trait Cvttpd2piEmitter<T0, T1> {
    fn cvttpd2pi(&mut self, dst: T0, src: T1);
}

/// Emits `CVTTPD2DQ`.
///
/// Assembly forms: `cvttpd2dq xmm[63:0], xmm/m128`.
pub trait Cvttpd2dqEmitter<T0, T1> {
    fn cvttpd2dq(&mut self, dst: T0, src: T1);
}

/// Emits `CVTTPS2DQ`.
///
/// Assembly forms: `cvttps2dq xmm, xmm/m128`.
pub trait Cvttps2dqEmitter<T0, T1> {
    fn cvttps2dq(&mut self, dst: T0, src: T1);
}

/// Emits `CVTTPS2PI`.
///
/// Assembly forms: `cvttps2pi mm, xmm[63:0]/m64`.
pub trait Cvttps2piEmitter<T0, T1> {
    fn cvttps2pi(&mut self, dst: T0, src: T1);
}

/// Emits `CVTTSD2SI`.
///
/// Assembly forms: `cvttsd2si ry, xmm[63:0]/m64`.
pub trait Cvttsd2siEmitter<T0, T1> {
    fn cvttsd2si(&mut self, dst: T0, src: T1);
}

/// Emits `CVTTSS2SI`.
///
/// Assembly forms: `cvttss2si ry, xmm[31:0]/m32`.
pub trait Cvttss2siEmitter<T0, T1> {
    fn cvttss2si(&mut self, dst: T0, src: T1);
}

/// Emits `DIVPD`.
///
/// Assembly forms: `divpd xmm, xmm/m128`.
pub trait DivpdEmitter<T0, T1> {
    fn divpd(&mut self, dst: T0, src: T1);
}

/// Emits `DIVPS`.
///
/// Assembly forms: `divps xmm, xmm/m128`.
pub trait DivpsEmitter<T0, T1> {
    fn divps(&mut self, dst: T0, src: T1);
}

/// Emits `DIVSD`.
///
/// Assembly forms: `divsd xmm[63:0], xmm[63:0]/m64`.
pub trait DivsdEmitter<T0, T1> {
    fn divsd(&mut self, dst: T0, src: T1);
}

/// Emits `DIVSS`.
///
/// Assembly forms: `divss xmm[31:0], xmm[31:0]/m32`.
pub trait DivssEmitter<T0, T1> {
    fn divss(&mut self, dst: T0, src: T1);
}

/// Emits `DPPD`.
///
/// Assembly forms: `dppd xmm, xmm/m128, imm8`.
pub trait DppdEmitter<T0, T1, T2> {
    fn dppd(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `DPPS`.
///
/// Assembly forms: `dpps xmm, xmm/m128, imm8`.
pub trait DppsEmitter<T0, T1, T2> {
    fn dpps(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `EXTRACTPS`.
///
/// Assembly forms: `extractps r32/m32, xmm, imm8`.
pub trait ExtractpsEmitter<T0, T1, T2> {
    fn extractps(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `EXTRQ`.
///
/// Assembly forms: `extrq xmm, imm8, imm8`, `extrq xmm, xmm`.
pub trait ExtrqEmitter<T0, T1> {
    fn extrq(&mut self, dst: T0, src: T1);
}

/// Emits `EXTRQ_3`.
///
/// Assembly forms: `extrq xmm, imm8, imm8`, `extrq xmm, xmm`.
pub trait Extrq3Emitter<T0, T1, T2> {
    fn extrq_3(&mut self, dst: T0, imm: T1, imm2: T2);
}

/// Emits `HADDPD`.
///
/// Assembly forms: `haddpd xmm, xmm/m128`.
pub trait HaddpdEmitter<T0, T1> {
    fn haddpd(&mut self, dst: T0, src: T1);
}

/// Emits `HADDPS`.
///
/// Assembly forms: `haddps xmm, xmm/m128`.
pub trait HaddpsEmitter<T0, T1> {
    fn haddps(&mut self, dst: T0, src: T1);
}

/// Emits `HSUBPD`.
///
/// Assembly forms: `hsubpd xmm, xmm/m128`.
pub trait HsubpdEmitter<T0, T1> {
    fn hsubpd(&mut self, dst: T0, src: T1);
}

/// Emits `HSUBPS`.
///
/// Assembly forms: `hsubps xmm, xmm/m128`.
pub trait HsubpsEmitter<T0, T1> {
    fn hsubps(&mut self, dst: T0, src: T1);
}

/// Emits `INSERTPS`.
///
/// Assembly forms: `insertps xmm, xmm[31:0]/m32, imm8`.
pub trait InsertpsEmitter<T0, T1, T2> {
    fn insertps(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `INSERTQ`.
///
/// Assembly forms: `insertq xmm, xmm`, `insertq xmm, xmm, imm8, imm8`.
pub trait InsertqEmitter<T0, T1> {
    fn insertq(&mut self, dst: T0, src: T1);
}

/// Emits `INSERTQ_4`.
///
/// Assembly forms: `insertq xmm, xmm`, `insertq xmm, xmm, imm8, imm8`.
pub trait Insertq4Emitter<T0, T1, T2, T3> {
    fn insertq_4(&mut self, dst: T0, src: T1, imm: T2, imm2: T3);
}

/// Emits `LDDQU`.
///
/// Assembly forms: `lddqu xmm, m128`.
pub trait LddquEmitter<T0, T1> {
    fn lddqu(&mut self, dst: T0, src: T1);
}

/// Emits `MASKMOVQ`.
///
/// Assembly forms: `maskmovq mm, mm, <m64(ds:zdi)>`.
pub trait MaskmovqEmitter<T0, T1, T2> {
    fn maskmovq(&mut self, dst: T0, src: T1, dst2: T2);
}

/// Emits `MASKMOVDQU`.
///
/// Assembly forms: `maskmovdqu xmm, xmm, <m128(ds:zdi)>`.
pub trait MaskmovdquEmitter<T0, T1, T2> {
    fn maskmovdqu(&mut self, dst: T0, src: T1, dst2: T2);
}

/// Emits `MAXPD`.
///
/// Assembly forms: `maxpd xmm, xmm/m128`.
pub trait MaxpdEmitter<T0, T1> {
    fn maxpd(&mut self, dst: T0, src: T1);
}

/// Emits `MAXPS`.
///
/// Assembly forms: `maxps xmm, xmm/m128`.
pub trait MaxpsEmitter<T0, T1> {
    fn maxps(&mut self, dst: T0, src: T1);
}

/// Emits `MAXSD`.
///
/// Assembly forms: `maxsd xmm[63:0], xmm[63:0]/m64`.
pub trait MaxsdEmitter<T0, T1> {
    fn maxsd(&mut self, dst: T0, src: T1);
}

/// Emits `MAXSS`.
///
/// Assembly forms: `maxss xmm[31:0], xmm[31:0]/m32`.
pub trait MaxssEmitter<T0, T1> {
    fn maxss(&mut self, dst: T0, src: T1);
}

/// Emits `MINPD`.
///
/// Assembly forms: `minpd xmm, xmm/m128`.
pub trait MinpdEmitter<T0, T1> {
    fn minpd(&mut self, dst: T0, src: T1);
}

/// Emits `MINPS`.
///
/// Assembly forms: `minps xmm, xmm/m128`.
pub trait MinpsEmitter<T0, T1> {
    fn minps(&mut self, dst: T0, src: T1);
}

/// Emits `MINSD`.
///
/// Assembly forms: `minsd xmm[63:0], xmm[63:0]/m64`.
pub trait MinsdEmitter<T0, T1> {
    fn minsd(&mut self, dst: T0, src: T1);
}

/// Emits `MINSS`.
///
/// Assembly forms: `minss xmm[31:0], xmm[31:0]/m32`.
pub trait MinssEmitter<T0, T1> {
    fn minss(&mut self, dst: T0, src: T1);
}

/// Emits `MOVAPD`.
///
/// Assembly forms: `movapd xmm, xmm/m128`, `movapd xmm/m128, xmm`.
pub trait MovapdEmitter<T0, T1> {
    fn movapd(&mut self, dst: T0, src: T1);
}

/// Emits `MOVAPS`.
///
/// Assembly forms: `movaps xmm, xmm/m128`, `movaps xmm/m128, xmm`.
pub trait MovapsEmitter<T0, T1> {
    fn movaps(&mut self, dst: T0, src: T1);
}

/// Emits `MOVD`.
///
/// Assembly forms: `movd mm[31:0], r32[31:0]/m32`, `movd r32[31:0]/m32, mm[31:0]`, `movd r32[31:0]/m32, xmm[31:0]`.
pub trait MovdEmitter<T0, T1> {
    fn movd(&mut self, dst: T0, src: T1);
}

/// Emits `MOVDDUP`.
///
/// Assembly forms: `movddup xmm, xmm[63:0]/m64`.
pub trait MovddupEmitter<T0, T1> {
    fn movddup(&mut self, dst: T0, src: T1);
}

/// Emits `MOVDQ2Q`.
///
/// Assembly forms: `movdq2q mm, xmm[63:0]`.
pub trait Movdq2qEmitter<T0, T1> {
    fn movdq2q(&mut self, dst: T0, src: T1);
}

/// Emits `MOVDQA`.
///
/// Assembly forms: `movdqa xmm, xmm/m128`, `movdqa xmm/m128, xmm`.
pub trait MovdqaEmitter<T0, T1> {
    fn movdqa(&mut self, dst: T0, src: T1);
}

/// Emits `MOVDQU`.
///
/// Assembly forms: `movdqu xmm, xmm/m128`, `movdqu xmm/m128, xmm`.
pub trait MovdquEmitter<T0, T1> {
    fn movdqu(&mut self, dst: T0, src: T1);
}

/// Emits `MOVHLPS`.
///
/// Assembly forms: `movhlps xmm[63:0], xmm[127:64]`.
pub trait MovhlpsEmitter<T0, T1> {
    fn movhlps(&mut self, dst: T0, src: T1);
}

/// Emits `MOVHPD`.
///
/// Assembly forms: `movhpd m64, xmm[127:64]`, `movhpd xmm[127:64], m64`.
pub trait MovhpdEmitter<T0, T1> {
    fn movhpd(&mut self, dst: T0, src: T1);
}

/// Emits `MOVHPS`.
///
/// Assembly forms: `movhps m64, xmm[127:64]`, `movhps xmm[127:64], m64`.
pub trait MovhpsEmitter<T0, T1> {
    fn movhps(&mut self, dst: T0, src: T1);
}

/// Emits `MOVLHPS`.
///
/// Assembly forms: `movlhps xmm[127:64], xmm[63:0]`.
pub trait MovlhpsEmitter<T0, T1> {
    fn movlhps(&mut self, dst: T0, src: T1);
}

/// Emits `MOVLPD`.
///
/// Assembly forms: `movlpd m64, xmm[63:0]`, `movlpd xmm[63:0], m64`.
pub trait MovlpdEmitter<T0, T1> {
    fn movlpd(&mut self, dst: T0, src: T1);
}

/// Emits `MOVLPS`.
///
/// Assembly forms: `movlps m64, xmm[63:0]`, `movlps xmm[63:0], m64`.
pub trait MovlpsEmitter<T0, T1> {
    fn movlps(&mut self, dst: T0, src: T1);
}

/// Emits `MOVMSKPS`.
///
/// Assembly forms: `movmskps r32[3:0], xmm`.
pub trait MovmskpsEmitter<T0, T1> {
    fn movmskps(&mut self, dst: T0, src: T1);
}

/// Emits `MOVMSKPD`.
///
/// Assembly forms: `movmskpd r32[1:0], xmm`.
pub trait MovmskpdEmitter<T0, T1> {
    fn movmskpd(&mut self, dst: T0, src: T1);
}

/// Emits `MOVNTDQ`.
///
/// Assembly forms: `movntdq m128, xmm`.
pub trait MovntdqEmitter<T0, T1> {
    fn movntdq(&mut self, dst: T0, src: T1);
}

/// Emits `MOVNTDQA`.
///
/// Assembly forms: `movntdqa xmm, m128`.
pub trait MovntdqaEmitter<T0, T1> {
    fn movntdqa(&mut self, dst: T0, src: T1);
}

/// Emits `MOVNTPD`.
///
/// Assembly forms: `movntpd m128, xmm`.
pub trait MovntpdEmitter<T0, T1> {
    fn movntpd(&mut self, dst: T0, src: T1);
}

/// Emits `MOVNTPS`.
///
/// Assembly forms: `movntps m128, xmm`.
pub trait MovntpsEmitter<T0, T1> {
    fn movntps(&mut self, dst: T0, src: T1);
}

/// Emits `MOVNTSD`.
///
/// Assembly forms: `movntsd m64, xmm[63:0]`.
pub trait MovntsdEmitter<T0, T1> {
    fn movntsd(&mut self, dst: T0, src: T1);
}

/// Emits `MOVNTSS`.
///
/// Assembly forms: `movntss m32, xmm[31:0]`.
pub trait MovntssEmitter<T0, T1> {
    fn movntss(&mut self, dst: T0, src: T1);
}

/// Emits `MOVNTQ`.
///
/// Assembly forms: `movntq m64, mm`.
pub trait MovntqEmitter<T0, T1> {
    fn movntq(&mut self, dst: T0, src: T1);
}

/// Emits `MOVQ`.
///
/// Assembly forms: `movq mm, mm/m64`, `movq mm, r64/m64`, `movq mm/m64, mm`.
pub trait MovqEmitter<T0, T1> {
    fn movq(&mut self, dst: T0, src: T1);
}

/// Emits `MOVQ2DQ`.
///
/// Assembly forms: `movq2dq xmm[63:0], mm`.
pub trait Movq2dqEmitter<T0, T1> {
    fn movq2dq(&mut self, dst: T0, src: T1);
}

/// Emits `MOVSD`.
///
/// Assembly forms: `movsd xmm[63:0], xmm[63:0]`, `movsd xmm[63:0], m64`, `movsd m64, xmm[63:0]`.
pub trait MovsdEmitter<T0, T1> {
    fn movsd(&mut self, dst: T0, src: T1);
}

/// Emits `MOVSHDUP`.
///
/// Assembly forms: `movshdup xmm, xmm/m128`.
pub trait MovshdupEmitter<T0, T1> {
    fn movshdup(&mut self, dst: T0, src: T1);
}

/// Emits `MOVSLDUP`.
///
/// Assembly forms: `movsldup xmm, xmm/m128`.
pub trait MovsldupEmitter<T0, T1> {
    fn movsldup(&mut self, dst: T0, src: T1);
}

/// Emits `MOVSS`.
///
/// Assembly forms: `movss xmm[31:0], xmm[31:0]`, `movss xmm[31:0], m32`, `movss m32, xmm[31:0]`.
pub trait MovssEmitter<T0, T1> {
    fn movss(&mut self, dst: T0, src: T1);
}

/// Emits `MOVUPD`.
///
/// Assembly forms: `movupd xmm, xmm/m128`, `movupd xmm/m128, xmm`.
pub trait MovupdEmitter<T0, T1> {
    fn movupd(&mut self, dst: T0, src: T1);
}

/// Emits `MOVUPS`.
///
/// Assembly forms: `movups xmm, xmm/m128`, `movups xmm/m128, xmm`.
pub trait MovupsEmitter<T0, T1> {
    fn movups(&mut self, dst: T0, src: T1);
}

/// Emits `MPSADBW`.
///
/// Assembly forms: `mpsadbw xmm, xmm/m128, imm8`.
pub trait MpsadbwEmitter<T0, T1, T2> {
    fn mpsadbw(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `MULPD`.
///
/// Assembly forms: `mulpd xmm, xmm/m128`.
pub trait MulpdEmitter<T0, T1> {
    fn mulpd(&mut self, dst: T0, src: T1);
}

/// Emits `MULPS`.
///
/// Assembly forms: `mulps xmm, xmm/m128`.
pub trait MulpsEmitter<T0, T1> {
    fn mulps(&mut self, dst: T0, src: T1);
}

/// Emits `MULSD`.
///
/// Assembly forms: `mulsd xmm[63:0], xmm[63:0]/m64`.
pub trait MulsdEmitter<T0, T1> {
    fn mulsd(&mut self, dst: T0, src: T1);
}

/// Emits `MULSS`.
///
/// Assembly forms: `mulss xmm[31:0], xmm[31:0]/m32`.
pub trait MulssEmitter<T0, T1> {
    fn mulss(&mut self, dst: T0, src: T1);
}

/// Emits `ORPD`.
///
/// Assembly forms: `orpd xmm, xmm/m128`.
pub trait OrpdEmitter<T0, T1> {
    fn orpd(&mut self, dst: T0, src: T1);
}

/// Emits `ORPS`.
///
/// Assembly forms: `orps xmm, xmm/m128`.
pub trait OrpsEmitter<T0, T1> {
    fn orps(&mut self, dst: T0, src: T1);
}

/// Emits `PACKSSDW`.
///
/// Assembly forms: `packssdw mm, mm/m64`, `packssdw xmm, xmm/m128`.
pub trait PackssdwEmitter<T0, T1> {
    fn packssdw(&mut self, dst: T0, src: T1);
}

/// Emits `PACKSSWB`.
///
/// Assembly forms: `packsswb mm, mm/m64`, `packsswb xmm, xmm/m128`.
pub trait PacksswbEmitter<T0, T1> {
    fn packsswb(&mut self, dst: T0, src: T1);
}

/// Emits `PACKUSDW`.
///
/// Assembly forms: `packusdw xmm, xmm/m128`.
pub trait PackusdwEmitter<T0, T1> {
    fn packusdw(&mut self, dst: T0, src: T1);
}

/// Emits `PACKUSWB`.
///
/// Assembly forms: `packuswb mm, mm/m64`, `packuswb xmm, xmm/m128`.
pub trait PackuswbEmitter<T0, T1> {
    fn packuswb(&mut self, dst: T0, src: T1);
}

/// Emits `PABSB`.
///
/// Assembly forms: `pabsb mm, mm/m64`, `pabsb xmm, xmm/m128`.
pub trait PabsbEmitter<T0, T1> {
    fn pabsb(&mut self, dst: T0, src: T1);
}

/// Emits `PABSD`.
///
/// Assembly forms: `pabsd mm, mm/m64`, `pabsd xmm, xmm/m128`.
pub trait PabsdEmitter<T0, T1> {
    fn pabsd(&mut self, dst: T0, src: T1);
}

/// Emits `PABSW`.
///
/// Assembly forms: `pabsw mm, mm/m64`, `pabsw xmm, xmm/m128`.
pub trait PabswEmitter<T0, T1> {
    fn pabsw(&mut self, dst: T0, src: T1);
}

/// Emits `PADDB`.
///
/// Assembly forms: `paddb mm, mm/m64`, `paddb xmm, xmm/m128`.
pub trait PaddbEmitter<T0, T1> {
    fn paddb(&mut self, dst: T0, src: T1);
}

/// Emits `PADDD`.
///
/// Assembly forms: `paddd mm, mm/m64`, `paddd xmm, xmm/m128`.
pub trait PadddEmitter<T0, T1> {
    fn paddd(&mut self, dst: T0, src: T1);
}

/// Emits `PADDQ`.
///
/// Assembly forms: `paddq mm, mm/m64`, `paddq xmm, xmm/m128`.
pub trait PaddqEmitter<T0, T1> {
    fn paddq(&mut self, dst: T0, src: T1);
}

/// Emits `PADDSB`.
///
/// Assembly forms: `paddsb mm, mm/m64`, `paddsb xmm, xmm/m128`.
pub trait PaddsbEmitter<T0, T1> {
    fn paddsb(&mut self, dst: T0, src: T1);
}

/// Emits `PADDSW`.
///
/// Assembly forms: `paddsw mm, mm/m64`, `paddsw xmm, xmm/m128`.
pub trait PaddswEmitter<T0, T1> {
    fn paddsw(&mut self, dst: T0, src: T1);
}

/// Emits `PADDUSB`.
///
/// Assembly forms: `paddusb mm, mm/m64`, `paddusb xmm, xmm/m128`.
pub trait PaddusbEmitter<T0, T1> {
    fn paddusb(&mut self, dst: T0, src: T1);
}

/// Emits `PADDUSW`.
///
/// Assembly forms: `paddusw mm, mm/m64`, `paddusw xmm, xmm/m128`.
pub trait PadduswEmitter<T0, T1> {
    fn paddusw(&mut self, dst: T0, src: T1);
}

/// Emits `PADDW`.
///
/// Assembly forms: `paddw mm, mm/m64`, `paddw xmm, xmm/m128`.
pub trait PaddwEmitter<T0, T1> {
    fn paddw(&mut self, dst: T0, src: T1);
}

/// Emits `PALIGNR`.
///
/// Assembly forms: `palignr mm, mm/m64, imm8`, `palignr xmm, xmm/m128, imm8`.
pub trait PalignrEmitter<T0, T1, T2> {
    fn palignr(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `PAND`.
///
/// Assembly forms: `pand mm, mm/m64`, `pand xmm, xmm/m128`.
pub trait PandEmitter<T0, T1> {
    fn pand(&mut self, dst: T0, src: T1);
}

/// Emits `PANDN`.
///
/// Assembly forms: `pandn mm, mm/m64`, `pandn xmm, xmm/m128`.
pub trait PandnEmitter<T0, T1> {
    fn pandn(&mut self, dst: T0, src: T1);
}

/// Emits `PAVGB`.
///
/// Assembly forms: `pavgb mm, mm/m64`, `pavgb xmm, xmm/m128`.
pub trait PavgbEmitter<T0, T1> {
    fn pavgb(&mut self, dst: T0, src: T1);
}

/// Emits `PAVGW`.
///
/// Assembly forms: `pavgw mm, mm/m64`, `pavgw xmm, xmm/m128`.
pub trait PavgwEmitter<T0, T1> {
    fn pavgw(&mut self, dst: T0, src: T1);
}

/// Emits `PBLENDVB`.
///
/// Assembly forms: `pblendvb xmm, xmm/m128, <xmm0>`.
pub trait PblendvbEmitter<T0, T1, T2> {
    fn pblendvb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `PBLENDW`.
///
/// Assembly forms: `pblendw xmm, xmm/m128, imm8`.
pub trait PblendwEmitter<T0, T1, T2> {
    fn pblendw(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `PCLMULQDQ`.
///
/// Assembly forms: `pclmulqdq xmm, xmm/m128, imm8`.
pub trait PclmulqdqEmitter<T0, T1, T2> {
    fn pclmulqdq(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `PCMPESTRI`.
///
/// Assembly forms: `pcmpestri xmm, xmm/m128, imm8, <ecx>, <eax>, <edx>`.
pub trait PcmpestriEmitter<T0, T1, T2, T3, T4, T5> {
    fn pcmpestri(&mut self, dst: T0, src1: T1, imm: T2, dst2: T3, src2: T4, src3: T5);
}

/// Emits `PCMPESTRM`.
///
/// Assembly forms: `pcmpestrm xmm, xmm/m128, imm8, <xmm0>, <eax>, <edx>`.
pub trait PcmpestrmEmitter<T0, T1, T2, T3, T4, T5> {
    fn pcmpestrm(&mut self, dst: T0, src1: T1, imm: T2, dst2: T3, src2: T4, src3: T5);
}

/// Emits `PCMPEQB`.
///
/// Assembly forms: `pcmpeqb mm, mm/m64`, `pcmpeqb xmm, xmm/m128`.
pub trait PcmpeqbEmitter<T0, T1> {
    fn pcmpeqb(&mut self, dst: T0, src: T1);
}

/// Emits `PCMPEQD`.
///
/// Assembly forms: `pcmpeqd mm, mm/m64`, `pcmpeqd xmm, xmm/m128`.
pub trait PcmpeqdEmitter<T0, T1> {
    fn pcmpeqd(&mut self, dst: T0, src: T1);
}

/// Emits `PCMPEQQ`.
///
/// Assembly forms: `pcmpeqq xmm, xmm/m128`.
pub trait PcmpeqqEmitter<T0, T1> {
    fn pcmpeqq(&mut self, dst: T0, src: T1);
}

/// Emits `PCMPEQW`.
///
/// Assembly forms: `pcmpeqw mm, mm/m64`, `pcmpeqw xmm, xmm/m128`.
pub trait PcmpeqwEmitter<T0, T1> {
    fn pcmpeqw(&mut self, dst: T0, src: T1);
}

/// Emits `PCMPGTB`.
///
/// Assembly forms: `pcmpgtb mm, mm/m64`, `pcmpgtb xmm, xmm/m128`.
pub trait PcmpgtbEmitter<T0, T1> {
    fn pcmpgtb(&mut self, dst: T0, src: T1);
}

/// Emits `PCMPGTD`.
///
/// Assembly forms: `pcmpgtd mm, mm/m64`, `pcmpgtd xmm, xmm/m128`.
pub trait PcmpgtdEmitter<T0, T1> {
    fn pcmpgtd(&mut self, dst: T0, src: T1);
}

/// Emits `PCMPGTQ`.
///
/// Assembly forms: `pcmpgtq xmm, xmm/m128`.
pub trait PcmpgtqEmitter<T0, T1> {
    fn pcmpgtq(&mut self, dst: T0, src: T1);
}

/// Emits `PCMPGTW`.
///
/// Assembly forms: `pcmpgtw mm, mm/m64`, `pcmpgtw xmm, xmm/m128`.
pub trait PcmpgtwEmitter<T0, T1> {
    fn pcmpgtw(&mut self, dst: T0, src: T1);
}

/// Emits `PCMPISTRI`.
///
/// Assembly forms: `pcmpistri xmm, xmm/m128, imm8, <ecx>`.
pub trait PcmpistriEmitter<T0, T1, T2, T3> {
    fn pcmpistri(&mut self, dst: T0, src: T1, imm: T2, dst2: T3);
}

/// Emits `PCMPISTRM`.
///
/// Assembly forms: `pcmpistrm xmm, xmm/m128, imm8, <xmm0>`.
pub trait PcmpistrmEmitter<T0, T1, T2, T3> {
    fn pcmpistrm(&mut self, dst: T0, src: T1, imm: T2, dst2: T3);
}

/// Emits `PEXTRB`.
///
/// Assembly forms: `pextrb r32[7:0]/m8, xmm, imm8`.
pub trait PextrbEmitter<T0, T1, T2> {
    fn pextrb(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `PEXTRD`.
///
/// Assembly forms: `pextrd r32[31:0]/m32, xmm, imm8`.
pub trait PextrdEmitter<T0, T1, T2> {
    fn pextrd(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `PEXTRQ`.
///
/// Assembly forms: `pextrq r64/m64, xmm, imm8`.
pub trait PextrqEmitter<T0, T1, T2> {
    fn pextrq(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `PEXTRW`.
///
/// Assembly forms: `pextrw r32[15:0], mm, imm8`, `pextrw r32[15:0], xmm, imm8`, `pextrw r32[15:0]/m16, xmm, imm8`.
pub trait PextrwEmitter<T0, T1, T2> {
    fn pextrw(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `PHADDD`.
///
/// Assembly forms: `phaddd mm, mm/m64`, `phaddd xmm, xmm/m128`.
pub trait PhadddEmitter<T0, T1> {
    fn phaddd(&mut self, dst: T0, src: T1);
}

/// Emits `PHADDSW`.
///
/// Assembly forms: `phaddsw mm, mm/m64`, `phaddsw xmm, xmm/m128`.
pub trait PhaddswEmitter<T0, T1> {
    fn phaddsw(&mut self, dst: T0, src: T1);
}

/// Emits `PHADDW`.
///
/// Assembly forms: `phaddw mm, mm/m64`, `phaddw xmm, xmm/m128`.
pub trait PhaddwEmitter<T0, T1> {
    fn phaddw(&mut self, dst: T0, src: T1);
}

/// Emits `PHMINPOSUW`.
///
/// Assembly forms: `phminposuw xmm[18:0], xmm/m128`.
pub trait PhminposuwEmitter<T0, T1> {
    fn phminposuw(&mut self, dst: T0, src: T1);
}

/// Emits `PHSUBD`.
///
/// Assembly forms: `phsubd mm, mm/m64`, `phsubd xmm, xmm/m128`.
pub trait PhsubdEmitter<T0, T1> {
    fn phsubd(&mut self, dst: T0, src: T1);
}

/// Emits `PHSUBSW`.
///
/// Assembly forms: `phsubsw mm, mm/m64`, `phsubsw xmm, xmm/m128`.
pub trait PhsubswEmitter<T0, T1> {
    fn phsubsw(&mut self, dst: T0, src: T1);
}

/// Emits `PHSUBW`.
///
/// Assembly forms: `phsubw mm, mm/m64`, `phsubw xmm, xmm/m128`.
pub trait PhsubwEmitter<T0, T1> {
    fn phsubw(&mut self, dst: T0, src: T1);
}

/// Emits `PINSRB`.
///
/// Assembly forms: `pinsrb xmm, r32[7:0]/m8, imm8`.
pub trait PinsrbEmitter<T0, T1, T2> {
    fn pinsrb(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `PINSRD`.
///
/// Assembly forms: `pinsrd xmm, r32[31:0]/m32, imm8`.
pub trait PinsrdEmitter<T0, T1, T2> {
    fn pinsrd(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `PINSRQ`.
///
/// Assembly forms: `pinsrq xmm, r64/m64, imm8`.
pub trait PinsrqEmitter<T0, T1, T2> {
    fn pinsrq(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `PINSRW`.
///
/// Assembly forms: `pinsrw mm, r32[15:0]/m16, imm8`, `pinsrw xmm, r32[15:0]/m16, imm8`.
pub trait PinsrwEmitter<T0, T1, T2> {
    fn pinsrw(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `PMADDUBSW`.
///
/// Assembly forms: `pmaddubsw mm, mm/m64`, `pmaddubsw xmm, xmm/m128`.
pub trait PmaddubswEmitter<T0, T1> {
    fn pmaddubsw(&mut self, dst: T0, src: T1);
}

/// Emits `PMADDWD`.
///
/// Assembly forms: `pmaddwd mm, mm/m64`, `pmaddwd xmm, xmm/m128`.
pub trait PmaddwdEmitter<T0, T1> {
    fn pmaddwd(&mut self, dst: T0, src: T1);
}

/// Emits `PMAXSB`.
///
/// Assembly forms: `pmaxsb xmm, xmm/m128`.
pub trait PmaxsbEmitter<T0, T1> {
    fn pmaxsb(&mut self, dst: T0, src: T1);
}

/// Emits `PMAXSD`.
///
/// Assembly forms: `pmaxsd xmm, xmm/m128`.
pub trait PmaxsdEmitter<T0, T1> {
    fn pmaxsd(&mut self, dst: T0, src: T1);
}

/// Emits `PMAXSW`.
///
/// Assembly forms: `pmaxsw mm, mm/m64`, `pmaxsw xmm, xmm/m128`.
pub trait PmaxswEmitter<T0, T1> {
    fn pmaxsw(&mut self, dst: T0, src: T1);
}

/// Emits `PMAXUB`.
///
/// Assembly forms: `pmaxub mm, mm/m64`, `pmaxub xmm, xmm/m128`.
pub trait PmaxubEmitter<T0, T1> {
    fn pmaxub(&mut self, dst: T0, src: T1);
}

/// Emits `PMAXUD`.
///
/// Assembly forms: `pmaxud xmm, xmm/m128`.
pub trait PmaxudEmitter<T0, T1> {
    fn pmaxud(&mut self, dst: T0, src: T1);
}

/// Emits `PMAXUW`.
///
/// Assembly forms: `pmaxuw xmm, xmm/m128`.
pub trait PmaxuwEmitter<T0, T1> {
    fn pmaxuw(&mut self, dst: T0, src: T1);
}

/// Emits `PMINSB`.
///
/// Assembly forms: `pminsb xmm, xmm/m128`.
pub trait PminsbEmitter<T0, T1> {
    fn pminsb(&mut self, dst: T0, src: T1);
}

/// Emits `PMINSD`.
///
/// Assembly forms: `pminsd xmm, xmm/m128`.
pub trait PminsdEmitter<T0, T1> {
    fn pminsd(&mut self, dst: T0, src: T1);
}

/// Emits `PMINSW`.
///
/// Assembly forms: `pminsw mm, mm/m64`, `pminsw xmm, xmm/m128`.
pub trait PminswEmitter<T0, T1> {
    fn pminsw(&mut self, dst: T0, src: T1);
}

/// Emits `PMINUB`.
///
/// Assembly forms: `pminub mm, mm/m64`, `pminub xmm, xmm/m128`.
pub trait PminubEmitter<T0, T1> {
    fn pminub(&mut self, dst: T0, src: T1);
}

/// Emits `PMINUD`.
///
/// Assembly forms: `pminud xmm, xmm/m128`.
pub trait PminudEmitter<T0, T1> {
    fn pminud(&mut self, dst: T0, src: T1);
}

/// Emits `PMINUW`.
///
/// Assembly forms: `pminuw xmm, xmm/m128`.
pub trait PminuwEmitter<T0, T1> {
    fn pminuw(&mut self, dst: T0, src: T1);
}

/// Emits `PMOVMSKB`.
///
/// Assembly forms: `pmovmskb r32[7:0], mm`, `pmovmskb r32[15:0], xmm`.
pub trait PmovmskbEmitter<T0, T1> {
    fn pmovmskb(&mut self, dst: T0, src: T1);
}

/// Emits `PMOVSXBD`.
///
/// Assembly forms: `pmovsxbd xmm, xmm[31:0]/m32`.
pub trait PmovsxbdEmitter<T0, T1> {
    fn pmovsxbd(&mut self, dst: T0, src: T1);
}

/// Emits `PMOVSXBQ`.
///
/// Assembly forms: `pmovsxbq xmm, xmm[15:0]/m16`.
pub trait PmovsxbqEmitter<T0, T1> {
    fn pmovsxbq(&mut self, dst: T0, src: T1);
}

/// Emits `PMOVSXBW`.
///
/// Assembly forms: `pmovsxbw xmm, xmm[63:0]/m64`.
pub trait PmovsxbwEmitter<T0, T1> {
    fn pmovsxbw(&mut self, dst: T0, src: T1);
}

/// Emits `PMOVSXDQ`.
///
/// Assembly forms: `pmovsxdq xmm, xmm[63:0]/m64`.
pub trait PmovsxdqEmitter<T0, T1> {
    fn pmovsxdq(&mut self, dst: T0, src: T1);
}

/// Emits `PMOVSXWD`.
///
/// Assembly forms: `pmovsxwd xmm, xmm[63:0]/m64`.
pub trait PmovsxwdEmitter<T0, T1> {
    fn pmovsxwd(&mut self, dst: T0, src: T1);
}

/// Emits `PMOVSXWQ`.
///
/// Assembly forms: `pmovsxwq xmm, xmm[31:0]/m32`.
pub trait PmovsxwqEmitter<T0, T1> {
    fn pmovsxwq(&mut self, dst: T0, src: T1);
}

/// Emits `PMOVZXBD`.
///
/// Assembly forms: `pmovzxbd xmm, xmm[31:0]/m32`.
pub trait PmovzxbdEmitter<T0, T1> {
    fn pmovzxbd(&mut self, dst: T0, src: T1);
}

/// Emits `PMOVZXBQ`.
///
/// Assembly forms: `pmovzxbq xmm, xmm[15:0]/m16`.
pub trait PmovzxbqEmitter<T0, T1> {
    fn pmovzxbq(&mut self, dst: T0, src: T1);
}

/// Emits `PMOVZXBW`.
///
/// Assembly forms: `pmovzxbw xmm, xmm[63:0]/m64`.
pub trait PmovzxbwEmitter<T0, T1> {
    fn pmovzxbw(&mut self, dst: T0, src: T1);
}

/// Emits `PMOVZXDQ`.
///
/// Assembly forms: `pmovzxdq xmm, xmm[63:0]/m64`.
pub trait PmovzxdqEmitter<T0, T1> {
    fn pmovzxdq(&mut self, dst: T0, src: T1);
}

/// Emits `PMOVZXWD`.
///
/// Assembly forms: `pmovzxwd xmm, xmm[63:0]/m64`.
pub trait PmovzxwdEmitter<T0, T1> {
    fn pmovzxwd(&mut self, dst: T0, src: T1);
}

/// Emits `PMOVZXWQ`.
///
/// Assembly forms: `pmovzxwq xmm, xmm[31:0]/m32`.
pub trait PmovzxwqEmitter<T0, T1> {
    fn pmovzxwq(&mut self, dst: T0, src: T1);
}

/// Emits `PMULDQ`.
///
/// Assembly forms: `pmuldq xmm, xmm/m128`.
pub trait PmuldqEmitter<T0, T1> {
    fn pmuldq(&mut self, dst: T0, src: T1);
}

/// Emits `PMULHRSW`.
///
/// Assembly forms: `pmulhrsw mm, mm/m64`, `pmulhrsw xmm, xmm/m128`.
pub trait PmulhrswEmitter<T0, T1> {
    fn pmulhrsw(&mut self, dst: T0, src: T1);
}

/// Emits `PMULHW`.
///
/// Assembly forms: `pmulhw mm, mm/m64`, `pmulhw xmm, xmm/m128`.
pub trait PmulhwEmitter<T0, T1> {
    fn pmulhw(&mut self, dst: T0, src: T1);
}

/// Emits `PMULHUW`.
///
/// Assembly forms: `pmulhuw mm, mm/m64`, `pmulhuw xmm, xmm/m128`.
pub trait PmulhuwEmitter<T0, T1> {
    fn pmulhuw(&mut self, dst: T0, src: T1);
}

/// Emits `PMULLD`.
///
/// Assembly forms: `pmulld xmm, xmm/m128`.
pub trait PmulldEmitter<T0, T1> {
    fn pmulld(&mut self, dst: T0, src: T1);
}

/// Emits `PMULLW`.
///
/// Assembly forms: `pmullw mm, mm/m64`, `pmullw xmm, xmm/m128`.
pub trait PmullwEmitter<T0, T1> {
    fn pmullw(&mut self, dst: T0, src: T1);
}

/// Emits `PMULUDQ`.
///
/// Assembly forms: `pmuludq mm, mm/m64`, `pmuludq xmm, xmm/m128`.
pub trait PmuludqEmitter<T0, T1> {
    fn pmuludq(&mut self, dst: T0, src: T1);
}

/// Emits `POR`.
///
/// Assembly forms: `por mm, mm/m64`, `por xmm, xmm/m128`.
pub trait PorEmitter<T0, T1> {
    fn por(&mut self, dst: T0, src: T1);
}

/// Emits `PSADBW`.
///
/// Assembly forms: `psadbw mm, mm/m64`, `psadbw xmm, xmm/m128`.
pub trait PsadbwEmitter<T0, T1> {
    fn psadbw(&mut self, dst: T0, src: T1);
}

/// Emits `PSLLD`.
///
/// Assembly forms: `pslld mm, mm/m64`, `pslld mm, imm8`, `pslld xmm, xmm/m128`.
pub trait PslldEmitter<T0, T1> {
    fn pslld(&mut self, dst: T0, src: T1);
}

/// Emits `PSLLDQ`.
///
/// Assembly forms: `pslldq xmm, imm8`.
pub trait PslldqEmitter<T0, T1> {
    fn pslldq(&mut self, dst: T0, imm: T1);
}

/// Emits `PSLLQ`.
///
/// Assembly forms: `psllq mm, mm/m64`, `psllq mm, imm8`, `psllq xmm, xmm/m128`.
pub trait PsllqEmitter<T0, T1> {
    fn psllq(&mut self, dst: T0, src: T1);
}

/// Emits `PSLLW`.
///
/// Assembly forms: `psllw mm, mm/m64`, `psllw mm, imm8`, `psllw xmm, xmm/m128`.
pub trait PsllwEmitter<T0, T1> {
    fn psllw(&mut self, dst: T0, src: T1);
}

/// Emits `PSRAD`.
///
/// Assembly forms: `psrad mm, mm/m64`, `psrad mm, imm8`, `psrad xmm, xmm/m128`.
pub trait PsradEmitter<T0, T1> {
    fn psrad(&mut self, dst: T0, src: T1);
}

/// Emits `PSRAW`.
///
/// Assembly forms: `psraw mm, mm/m64`, `psraw mm, imm8`, `psraw xmm, xmm/m128`.
pub trait PsrawEmitter<T0, T1> {
    fn psraw(&mut self, dst: T0, src: T1);
}

/// Emits `PSHUFB`.
///
/// Assembly forms: `pshufb mm, mm/m64`, `pshufb xmm, xmm/m128`.
pub trait PshufbEmitter<T0, T1> {
    fn pshufb(&mut self, dst: T0, src: T1);
}

/// Emits `PSHUFD`.
///
/// Assembly forms: `pshufd xmm, xmm/m128, imm8`.
pub trait PshufdEmitter<T0, T1, T2> {
    fn pshufd(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `PSHUFHW`.
///
/// Assembly forms: `pshufhw xmm, xmm/m128, imm8`.
pub trait PshufhwEmitter<T0, T1, T2> {
    fn pshufhw(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `PSHUFLW`.
///
/// Assembly forms: `pshuflw xmm, xmm/m128, imm8`.
pub trait PshuflwEmitter<T0, T1, T2> {
    fn pshuflw(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `PSHUFW`.
///
/// Assembly forms: `pshufw mm, mm/m64, imm8`.
pub trait PshufwEmitter<T0, T1, T2> {
    fn pshufw(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `PSIGNB`.
///
/// Assembly forms: `psignb mm, mm/m64`, `psignb xmm, xmm/m128`.
pub trait PsignbEmitter<T0, T1> {
    fn psignb(&mut self, dst: T0, src: T1);
}

/// Emits `PSIGND`.
///
/// Assembly forms: `psignd mm, mm/m64`, `psignd xmm, xmm/m128`.
pub trait PsigndEmitter<T0, T1> {
    fn psignd(&mut self, dst: T0, src: T1);
}

/// Emits `PSIGNW`.
///
/// Assembly forms: `psignw mm, mm/m64`, `psignw xmm, xmm/m128`.
pub trait PsignwEmitter<T0, T1> {
    fn psignw(&mut self, dst: T0, src: T1);
}

/// Emits `PSRLD`.
///
/// Assembly forms: `psrld mm, mm/m64`, `psrld mm, imm8`, `psrld xmm, xmm/m128`.
pub trait PsrldEmitter<T0, T1> {
    fn psrld(&mut self, dst: T0, src: T1);
}

/// Emits `PSRLDQ`.
///
/// Assembly forms: `psrldq xmm, imm8`.
pub trait PsrldqEmitter<T0, T1> {
    fn psrldq(&mut self, dst: T0, imm: T1);
}

/// Emits `PSRLQ`.
///
/// Assembly forms: `psrlq mm, mm/m64`, `psrlq mm, imm8`, `psrlq xmm, xmm/m128`.
pub trait PsrlqEmitter<T0, T1> {
    fn psrlq(&mut self, dst: T0, src: T1);
}

/// Emits `PSRLW`.
///
/// Assembly forms: `psrlw mm, mm/m64`, `psrlw mm, imm8`, `psrlw xmm, imm8`.
pub trait PsrlwEmitter<T0, T1> {
    fn psrlw(&mut self, dst: T0, src: T1);
}

/// Emits `PSUBB`.
///
/// Assembly forms: `psubb mm, mm/m64`, `psubb xmm, xmm/m128`.
pub trait PsubbEmitter<T0, T1> {
    fn psubb(&mut self, dst: T0, src: T1);
}

/// Emits `PSUBD`.
///
/// Assembly forms: `psubd mm, mm/m64`, `psubd xmm, xmm/m128`.
pub trait PsubdEmitter<T0, T1> {
    fn psubd(&mut self, dst: T0, src: T1);
}

/// Emits `PSUBQ`.
///
/// Assembly forms: `psubq mm, mm/m64`, `psubq xmm, xmm/m128`.
pub trait PsubqEmitter<T0, T1> {
    fn psubq(&mut self, dst: T0, src: T1);
}

/// Emits `PSUBSB`.
///
/// Assembly forms: `psubsb mm, mm/m64`, `psubsb xmm, xmm/m128`.
pub trait PsubsbEmitter<T0, T1> {
    fn psubsb(&mut self, dst: T0, src: T1);
}

/// Emits `PSUBSW`.
///
/// Assembly forms: `psubsw mm, mm/m64`, `psubsw xmm, xmm/m128`.
pub trait PsubswEmitter<T0, T1> {
    fn psubsw(&mut self, dst: T0, src: T1);
}

/// Emits `PSUBUSB`.
///
/// Assembly forms: `psubusb mm, mm/m64`, `psubusb xmm, xmm/m128`.
pub trait PsubusbEmitter<T0, T1> {
    fn psubusb(&mut self, dst: T0, src: T1);
}

/// Emits `PSUBUSW`.
///
/// Assembly forms: `psubusw mm, mm/m64`, `psubusw xmm, xmm/m128`.
pub trait PsubuswEmitter<T0, T1> {
    fn psubusw(&mut self, dst: T0, src: T1);
}

/// Emits `PSUBW`.
///
/// Assembly forms: `psubw mm, mm/m64`, `psubw xmm, xmm/m128`.
pub trait PsubwEmitter<T0, T1> {
    fn psubw(&mut self, dst: T0, src: T1);
}

/// Emits `PTEST`.
///
/// Assembly forms: `ptest xmm, xmm/m128`.
pub trait PtestEmitter<T0, T1> {
    fn ptest(&mut self, lhs: T0, rhs: T1);
}

/// Emits `PUNPCKHBW`.
///
/// Assembly forms: `punpckhbw mm, mm/m64`, `punpckhbw xmm, xmm/m128`.
pub trait PunpckhbwEmitter<T0, T1> {
    fn punpckhbw(&mut self, dst: T0, src: T1);
}

/// Emits `PUNPCKHDQ`.
///
/// Assembly forms: `punpckhdq mm, mm/m64`, `punpckhdq xmm, xmm/m128`.
pub trait PunpckhdqEmitter<T0, T1> {
    fn punpckhdq(&mut self, dst: T0, src: T1);
}

/// Emits `PUNPCKHQDQ`.
///
/// Assembly forms: `punpckhqdq xmm, xmm/m128`.
pub trait PunpckhqdqEmitter<T0, T1> {
    fn punpckhqdq(&mut self, dst: T0, src: T1);
}

/// Emits `PUNPCKHWD`.
///
/// Assembly forms: `punpckhwd mm, mm/m64`, `punpckhwd xmm, xmm/m128`.
pub trait PunpckhwdEmitter<T0, T1> {
    fn punpckhwd(&mut self, dst: T0, src: T1);
}

/// Emits `PUNPCKLBW`.
///
/// Assembly forms: `punpcklbw mm, mm/m32`, `punpcklbw xmm, xmm/m128`.
pub trait PunpcklbwEmitter<T0, T1> {
    fn punpcklbw(&mut self, dst: T0, src: T1);
}

/// Emits `PUNPCKLDQ`.
///
/// Assembly forms: `punpckldq mm, mm/m32`, `punpckldq xmm, xmm/m128`.
pub trait PunpckldqEmitter<T0, T1> {
    fn punpckldq(&mut self, dst: T0, src: T1);
}

/// Emits `PUNPCKLQDQ`.
///
/// Assembly forms: `punpcklqdq xmm, xmm/m128`.
pub trait PunpcklqdqEmitter<T0, T1> {
    fn punpcklqdq(&mut self, dst: T0, src: T1);
}

/// Emits `PUNPCKLWD`.
///
/// Assembly forms: `punpcklwd mm, mm/m32`, `punpcklwd xmm, xmm/m128`.
pub trait PunpcklwdEmitter<T0, T1> {
    fn punpcklwd(&mut self, dst: T0, src: T1);
}

/// Emits `PXOR`.
///
/// Assembly forms: `pxor mm, mm/m64`, `pxor xmm, xmm/m128`.
pub trait PxorEmitter<T0, T1> {
    fn pxor(&mut self, dst: T0, src: T1);
}

/// Emits `RCPPS`.
///
/// Assembly forms: `rcpps xmm, xmm/m128`.
pub trait RcppsEmitter<T0, T1> {
    fn rcpps(&mut self, dst: T0, src: T1);
}

/// Emits `RCPSS`.
///
/// Assembly forms: `rcpss xmm[31:0], xmm[31:0]/m32`.
pub trait RcpssEmitter<T0, T1> {
    fn rcpss(&mut self, dst: T0, src: T1);
}

/// Emits `ROUNDPD`.
///
/// Assembly forms: `roundpd xmm, xmm/m128, imm8`.
pub trait RoundpdEmitter<T0, T1, T2> {
    fn roundpd(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `ROUNDPS`.
///
/// Assembly forms: `roundps xmm, xmm/m128, imm8`.
pub trait RoundpsEmitter<T0, T1, T2> {
    fn roundps(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `ROUNDSD`.
///
/// Assembly forms: `roundsd xmm[63:0], xmm[63:0]/m64, imm8`.
pub trait RoundsdEmitter<T0, T1, T2> {
    fn roundsd(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `ROUNDSS`.
///
/// Assembly forms: `roundss xmm[31:0], xmm[31:0]/m32, imm8`.
pub trait RoundssEmitter<T0, T1, T2> {
    fn roundss(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `RSQRTPS`.
///
/// Assembly forms: `rsqrtps xmm, xmm/m128`.
pub trait RsqrtpsEmitter<T0, T1> {
    fn rsqrtps(&mut self, dst: T0, src: T1);
}

/// Emits `RSQRTSS`.
///
/// Assembly forms: `rsqrtss xmm[31:0], xmm[31:0]/m32`.
pub trait RsqrtssEmitter<T0, T1> {
    fn rsqrtss(&mut self, dst: T0, src: T1);
}

/// Emits `SHUFPD`.
///
/// Assembly forms: `shufpd xmm, xmm/m128, imm8`.
pub trait ShufpdEmitter<T0, T1, T2> {
    fn shufpd(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `SHUFPS`.
///
/// Assembly forms: `shufps xmm, xmm/m128, imm8`.
pub trait ShufpsEmitter<T0, T1, T2> {
    fn shufps(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `SQRTPD`.
///
/// Assembly forms: `sqrtpd xmm, xmm/m128`.
pub trait SqrtpdEmitter<T0, T1> {
    fn sqrtpd(&mut self, dst: T0, src: T1);
}

/// Emits `SQRTPS`.
///
/// Assembly forms: `sqrtps xmm, xmm/m128`.
pub trait SqrtpsEmitter<T0, T1> {
    fn sqrtps(&mut self, dst: T0, src: T1);
}

/// Emits `SQRTSD`.
///
/// Assembly forms: `sqrtsd xmm[63:0], xmm[63:0]/m64`.
pub trait SqrtsdEmitter<T0, T1> {
    fn sqrtsd(&mut self, dst: T0, src: T1);
}

/// Emits `SQRTSS`.
///
/// Assembly forms: `sqrtss xmm[31:0], xmm[31:0]/m32`.
pub trait SqrtssEmitter<T0, T1> {
    fn sqrtss(&mut self, dst: T0, src: T1);
}

/// Emits `SUBPD`.
///
/// Assembly forms: `subpd xmm, xmm/m128`.
pub trait SubpdEmitter<T0, T1> {
    fn subpd(&mut self, dst: T0, src: T1);
}

/// Emits `SUBPS`.
///
/// Assembly forms: `subps xmm, xmm/m128`.
pub trait SubpsEmitter<T0, T1> {
    fn subps(&mut self, dst: T0, src: T1);
}

/// Emits `SUBSD`.
///
/// Assembly forms: `subsd xmm[63:0], xmm[63:0]/m64`.
pub trait SubsdEmitter<T0, T1> {
    fn subsd(&mut self, dst: T0, src: T1);
}

/// Emits `SUBSS`.
///
/// Assembly forms: `subss xmm[31:0], xmm[31:0]/m32`.
pub trait SubssEmitter<T0, T1> {
    fn subss(&mut self, dst: T0, src: T1);
}

/// Emits `UCOMISD`.
///
/// Assembly forms: `ucomisd xmm[63:0], xmm[63:0]/m64`.
pub trait UcomisdEmitter<T0, T1> {
    fn ucomisd(&mut self, lhs: T0, rhs: T1);
}

/// Emits `UCOMISS`.
///
/// Assembly forms: `ucomiss xmm[31:0], xmm[31:0]/m32`.
pub trait UcomissEmitter<T0, T1> {
    fn ucomiss(&mut self, lhs: T0, rhs: T1);
}

/// Emits `UNPCKHPD`.
///
/// Assembly forms: `unpckhpd xmm, xmm/m128`.
pub trait UnpckhpdEmitter<T0, T1> {
    fn unpckhpd(&mut self, dst: T0, src: T1);
}

/// Emits `UNPCKHPS`.
///
/// Assembly forms: `unpckhps xmm, xmm/m128`.
pub trait UnpckhpsEmitter<T0, T1> {
    fn unpckhps(&mut self, dst: T0, src: T1);
}

/// Emits `UNPCKLPD`.
///
/// Assembly forms: `unpcklpd xmm, xmm/m128`.
pub trait UnpcklpdEmitter<T0, T1> {
    fn unpcklpd(&mut self, dst: T0, src: T1);
}

/// Emits `UNPCKLPS`.
///
/// Assembly forms: `unpcklps xmm, xmm/m128`.
pub trait UnpcklpsEmitter<T0, T1> {
    fn unpcklps(&mut self, dst: T0, src: T1);
}

/// Emits `XORPD`.
///
/// Assembly forms: `xorpd xmm, xmm/m128`.
pub trait XorpdEmitter<T0, T1> {
    fn xorpd(&mut self, dst: T0, src: T1);
}

/// Emits `XORPS`.
///
/// Assembly forms: `xorps xmm, xmm/m128`.
pub trait XorpsEmitter<T0, T1> {
    fn xorps(&mut self, dst: T0, src: T1);
}

/// Emits `PAVGUSB`.
///
/// Assembly forms: `pavgusb mm, mm/m64`.
pub trait PavgusbEmitter<T0, T1> {
    fn pavgusb(&mut self, dst: T0, src: T1);
}

/// Emits `PF2ID`.
///
/// Assembly forms: `pf2id mm, mm/m64`.
pub trait Pf2idEmitter<T0, T1> {
    fn pf2id(&mut self, dst: T0, src: T1);
}

/// Emits `PF2IW`.
///
/// Assembly forms: `pf2iw mm, mm/m64`.
pub trait Pf2iwEmitter<T0, T1> {
    fn pf2iw(&mut self, dst: T0, src: T1);
}

/// Emits `PFACC`.
///
/// Assembly forms: `pfacc mm, mm/m64`.
pub trait PfaccEmitter<T0, T1> {
    fn pfacc(&mut self, dst: T0, src: T1);
}

/// Emits `PFADD`.
///
/// Assembly forms: `pfadd mm, mm/m64`.
pub trait PfaddEmitter<T0, T1> {
    fn pfadd(&mut self, dst: T0, src: T1);
}

/// Emits `PFCMPEQ`.
///
/// Assembly forms: `pfcmpeq mm, mm/m64`.
pub trait PfcmpeqEmitter<T0, T1> {
    fn pfcmpeq(&mut self, dst: T0, src: T1);
}

/// Emits `PFCMPGE`.
///
/// Assembly forms: `pfcmpge mm, mm/m64`.
pub trait PfcmpgeEmitter<T0, T1> {
    fn pfcmpge(&mut self, dst: T0, src: T1);
}

/// Emits `PFCMPGT`.
///
/// Assembly forms: `pfcmpgt mm, mm/m64`.
pub trait PfcmpgtEmitter<T0, T1> {
    fn pfcmpgt(&mut self, dst: T0, src: T1);
}

/// Emits `PFMAX`.
///
/// Assembly forms: `pfmax mm, mm/m64`.
pub trait PfmaxEmitter<T0, T1> {
    fn pfmax(&mut self, dst: T0, src: T1);
}

/// Emits `PFMIN`.
///
/// Assembly forms: `pfmin mm, mm/m64`.
pub trait PfminEmitter<T0, T1> {
    fn pfmin(&mut self, dst: T0, src: T1);
}

/// Emits `PFMUL`.
///
/// Assembly forms: `pfmul mm, mm/m64`.
pub trait PfmulEmitter<T0, T1> {
    fn pfmul(&mut self, dst: T0, src: T1);
}

/// Emits `PFNACC`.
///
/// Assembly forms: `pfnacc mm, mm/m64`.
pub trait PfnaccEmitter<T0, T1> {
    fn pfnacc(&mut self, dst: T0, src: T1);
}

/// Emits `PFPNACC`.
///
/// Assembly forms: `pfpnacc mm, mm/m64`.
pub trait PfpnaccEmitter<T0, T1> {
    fn pfpnacc(&mut self, dst: T0, src: T1);
}

/// Emits `PFRCP`.
///
/// Assembly forms: `pfrcp mm, mm/m64`.
pub trait PfrcpEmitter<T0, T1> {
    fn pfrcp(&mut self, dst: T0, src: T1);
}

/// Emits `PFRCPIT1`.
///
/// Assembly forms: `pfrcpit1 mm, mm/m64`.
pub trait Pfrcpit1Emitter<T0, T1> {
    fn pfrcpit1(&mut self, dst: T0, src: T1);
}

/// Emits `PFRCPIT2`.
///
/// Assembly forms: `pfrcpit2 mm, mm/m64`.
pub trait Pfrcpit2Emitter<T0, T1> {
    fn pfrcpit2(&mut self, dst: T0, src: T1);
}

/// Emits `PFRCPV`.
///
/// Assembly forms: `pfrcpv mm, mm/m64`.
pub trait PfrcpvEmitter<T0, T1> {
    fn pfrcpv(&mut self, dst: T0, src: T1);
}

/// Emits `PFRSQIT1`.
///
/// Assembly forms: `pfrsqit1 mm, mm/m64`.
pub trait Pfrsqit1Emitter<T0, T1> {
    fn pfrsqit1(&mut self, dst: T0, src: T1);
}

/// Emits `PFRSQRT`.
///
/// Assembly forms: `pfrsqrt mm, mm/m64`.
pub trait PfrsqrtEmitter<T0, T1> {
    fn pfrsqrt(&mut self, dst: T0, src: T1);
}

/// Emits `PFRSQRTV`.
///
/// Assembly forms: `pfrsqrtv mm, mm/m64`.
pub trait PfrsqrtvEmitter<T0, T1> {
    fn pfrsqrtv(&mut self, dst: T0, src: T1);
}

/// Emits `PFSUB`.
///
/// Assembly forms: `pfsub mm, mm/m64`.
pub trait PfsubEmitter<T0, T1> {
    fn pfsub(&mut self, dst: T0, src: T1);
}

/// Emits `PFSUBR`.
///
/// Assembly forms: `pfsubr mm, mm/m64`.
pub trait PfsubrEmitter<T0, T1> {
    fn pfsubr(&mut self, dst: T0, src: T1);
}

/// Emits `PI2FD`.
///
/// Assembly forms: `pi2fd mm, mm/m64`.
pub trait Pi2fdEmitter<T0, T1> {
    fn pi2fd(&mut self, dst: T0, src: T1);
}

/// Emits `PI2FW`.
///
/// Assembly forms: `pi2fw mm, mm/m64`.
pub trait Pi2fwEmitter<T0, T1> {
    fn pi2fw(&mut self, dst: T0, src: T1);
}

/// Emits `PMULHRW`.
///
/// Assembly forms: `pmulhrw mm, mm/m64`.
pub trait PmulhrwEmitter<T0, T1> {
    fn pmulhrw(&mut self, dst: T0, src: T1);
}

/// Emits `PSWAPD`.
///
/// Assembly forms: `pswapd mm, mm/m64`.
pub trait PswapdEmitter<T0, T1> {
    fn pswapd(&mut self, dst: T0, src: T1);
}

/// Emits `EMMS`.
///
/// Assembly forms: `emms`.
pub trait EmmsEmitter {
    fn emms(&mut self);
}

/// Emits `FEMMS`.
///
/// Assembly forms: `femms`.
pub trait FemmsEmitter {
    fn femms(&mut self);
}

/// Emits `AESDEC`.
///
/// Assembly forms: `aesdec xmm, xmm/m128`.
pub trait AesdecEmitter<T0, T1> {
    fn aesdec(&mut self, dst: T0, src: T1);
}

/// Emits `AESDECLAST`.
///
/// Assembly forms: `aesdeclast xmm, xmm/m128`.
pub trait AesdeclastEmitter<T0, T1> {
    fn aesdeclast(&mut self, dst: T0, src: T1);
}

/// Emits `AESENC`.
///
/// Assembly forms: `aesenc xmm, xmm/m128`.
pub trait AesencEmitter<T0, T1> {
    fn aesenc(&mut self, dst: T0, src: T1);
}

/// Emits `AESENCLAST`.
///
/// Assembly forms: `aesenclast xmm, xmm/m128`.
pub trait AesenclastEmitter<T0, T1> {
    fn aesenclast(&mut self, dst: T0, src: T1);
}

/// Emits `AESIMC`.
///
/// Assembly forms: `aesimc xmm, xmm/m128`.
pub trait AesimcEmitter<T0, T1> {
    fn aesimc(&mut self, dst: T0, src: T1);
}

/// Emits `AESKEYGENASSIST`.
///
/// Assembly forms: `aeskeygenassist xmm, xmm/m128, imm8`.
pub trait AeskeygenassistEmitter<T0, T1, T2> {
    fn aeskeygenassist(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `SHA1MSG1`.
///
/// Assembly forms: `sha1msg1 xmm, xmm/m128`.
pub trait Sha1msg1Emitter<T0, T1> {
    fn sha1msg1(&mut self, lhs: T0, rhs: T1);
}

/// Emits `SHA1MSG2`.
///
/// Assembly forms: `sha1msg2 xmm, xmm/m128`.
pub trait Sha1msg2Emitter<T0, T1> {
    fn sha1msg2(&mut self, lhs: T0, rhs: T1);
}

/// Emits `SHA1NEXTE`.
///
/// Assembly forms: `sha1nexte xmm, xmm/m128`.
pub trait Sha1nexteEmitter<T0, T1> {
    fn sha1nexte(&mut self, lhs: T0, rhs: T1);
}

/// Emits `SHA1RNDS4`.
///
/// Assembly forms: `sha1rnds4 xmm, xmm/m128, imm8`.
pub trait Sha1rnds4Emitter<T0, T1, T2> {
    fn sha1rnds4(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `SHA256MSG1`.
///
/// Assembly forms: `sha256msg1 xmm, xmm/m128`.
pub trait Sha256msg1Emitter<T0, T1> {
    fn sha256msg1(&mut self, lhs: T0, rhs: T1);
}

/// Emits `SHA256MSG2`.
///
/// Assembly forms: `sha256msg2 xmm, xmm/m128`.
pub trait Sha256msg2Emitter<T0, T1> {
    fn sha256msg2(&mut self, lhs: T0, rhs: T1);
}

/// Emits `SHA256RNDS2`.
///
/// Assembly forms: `sha256rnds2 xmm, xmm/m128, <xmm0>`.
pub trait Sha256rnds2Emitter<T0, T1, T2> {
    fn sha256rnds2(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `GF2P8AFFINEINVQB`.
///
/// Assembly forms: `gf2p8affineinvqb xmm, xmm/m128, imm8`.
pub trait Gf2p8affineinvqbEmitter<T0, T1, T2> {
    fn gf2p8affineinvqb(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `GF2P8AFFINEQB`.
///
/// Assembly forms: `gf2p8affineqb xmm, xmm/m128, imm8`.
pub trait Gf2p8affineqbEmitter<T0, T1, T2> {
    fn gf2p8affineqb(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `GF2P8MULB`.
///
/// Assembly forms: `gf2p8mulb xmm, xmm/m128`.
pub trait Gf2p8mulbEmitter<T0, T1> {
    fn gf2p8mulb(&mut self, dst: T0, src: T1);
}

/// Emits `KADDB`.
///
/// Assembly forms: `kaddb k[7:0], k[7:0], k[7:0]`.
pub trait KaddbEmitter<T0, T1, T2> {
    fn kaddb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KADDD`.
///
/// Assembly forms: `kaddd k[31:0], k[31:0], k[31:0]`.
pub trait KadddEmitter<T0, T1, T2> {
    fn kaddd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KADDQ`.
///
/// Assembly forms: `kaddq k[63:0], k[63:0], k[63:0]`.
pub trait KaddqEmitter<T0, T1, T2> {
    fn kaddq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KADDW`.
///
/// Assembly forms: `kaddw k[15:0], k[15:0], k[15:0]`.
pub trait KaddwEmitter<T0, T1, T2> {
    fn kaddw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KANDB`.
///
/// Assembly forms: `kandb k[7:0], k[7:0], k[7:0]`.
pub trait KandbEmitter<T0, T1, T2> {
    fn kandb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KANDD`.
///
/// Assembly forms: `kandd k[31:0], k[31:0], k[31:0]`.
pub trait KanddEmitter<T0, T1, T2> {
    fn kandd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KANDNB`.
///
/// Assembly forms: `kandnb k[7:0], k[7:0], k[7:0]`.
pub trait KandnbEmitter<T0, T1, T2> {
    fn kandnb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KANDND`.
///
/// Assembly forms: `kandnd k[31:0], k[31:0], k[31:0]`.
pub trait KandndEmitter<T0, T1, T2> {
    fn kandnd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KANDNQ`.
///
/// Assembly forms: `kandnq k[63:0], k[63:0], k[63:0]`.
pub trait KandnqEmitter<T0, T1, T2> {
    fn kandnq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KANDNW`.
///
/// Assembly forms: `kandnw k[15:0], k[15:0], k[15:0]`.
pub trait KandnwEmitter<T0, T1, T2> {
    fn kandnw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KANDQ`.
///
/// Assembly forms: `kandq k[63:0], k[63:0], k[63:0]`.
pub trait KandqEmitter<T0, T1, T2> {
    fn kandq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KANDW`.
///
/// Assembly forms: `kandw k[15:0], k[15:0], k[15:0]`.
pub trait KandwEmitter<T0, T1, T2> {
    fn kandw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KMOVB`.
///
/// Assembly forms: `kmovb k[7:0], k[7:0]/m8`, `kmovb k[7:0], r32[7:0]`, `kmovb m8, k[7:0]`.
pub trait KmovbEmitter<T0, T1> {
    fn kmovb(&mut self, dst: T0, src: T1);
}

/// Emits `KMOVD`.
///
/// Assembly forms: `kmovd k[31:0], k[31:0]/m32`, `kmovd k[31:0], r32`, `kmovd m32, k[31:0]`.
pub trait KmovdEmitter<T0, T1> {
    fn kmovd(&mut self, dst: T0, src: T1);
}

/// Emits `KMOVQ`.
///
/// Assembly forms: `kmovq k[63:0], k[63:0]/m64`, `kmovq k[63:0], r64`, `kmovq m64, k[63:0]`.
pub trait KmovqEmitter<T0, T1> {
    fn kmovq(&mut self, dst: T0, src: T1);
}

/// Emits `KMOVW`.
///
/// Assembly forms: `kmovw k[15:0], k[15:0]/m16`, `kmovw k[15:0], r32[15:0]`, `kmovw m16, k[15:0]`.
pub trait KmovwEmitter<T0, T1> {
    fn kmovw(&mut self, dst: T0, src: T1);
}

/// Emits `KNOTB`.
///
/// Assembly forms: `knotb k[7:0], k[7:0]`.
pub trait KnotbEmitter<T0, T1> {
    fn knotb(&mut self, dst: T0, src: T1);
}

/// Emits `KNOTD`.
///
/// Assembly forms: `knotd k[31:0], k[31:0]`.
pub trait KnotdEmitter<T0, T1> {
    fn knotd(&mut self, dst: T0, src: T1);
}

/// Emits `KNOTQ`.
///
/// Assembly forms: `knotq k[63:0], k[63:0]`.
pub trait KnotqEmitter<T0, T1> {
    fn knotq(&mut self, dst: T0, src: T1);
}

/// Emits `KNOTW`.
///
/// Assembly forms: `knotw k[15:0], k[15:0]`.
pub trait KnotwEmitter<T0, T1> {
    fn knotw(&mut self, dst: T0, src: T1);
}

/// Emits `KORB`.
///
/// Assembly forms: `korb k[7:0], k[7:0], k[7:0]`.
pub trait KorbEmitter<T0, T1, T2> {
    fn korb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KORD`.
///
/// Assembly forms: `kord k[31:0], k[31:0], k[31:0]`.
pub trait KordEmitter<T0, T1, T2> {
    fn kord(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KORQ`.
///
/// Assembly forms: `korq k[63:0], k[63:0], k[63:0]`.
pub trait KorqEmitter<T0, T1, T2> {
    fn korq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KORTESTB`.
///
/// Assembly forms: `kortestb k[7:0], k[7:0]`.
pub trait KortestbEmitter<T0, T1> {
    fn kortestb(&mut self, lhs: T0, rhs: T1);
}

/// Emits `KORTESTD`.
///
/// Assembly forms: `kortestd k[31:0], k[31:0]`.
pub trait KortestdEmitter<T0, T1> {
    fn kortestd(&mut self, lhs: T0, rhs: T1);
}

/// Emits `KORTESTQ`.
///
/// Assembly forms: `kortestq k[63:0], k[63:0]`.
pub trait KortestqEmitter<T0, T1> {
    fn kortestq(&mut self, lhs: T0, rhs: T1);
}

/// Emits `KORTESTW`.
///
/// Assembly forms: `kortestw k[15:0], k[15:0]`.
pub trait KortestwEmitter<T0, T1> {
    fn kortestw(&mut self, lhs: T0, rhs: T1);
}

/// Emits `KORW`.
///
/// Assembly forms: `korw k[15:0], k[15:0], k[15:0]`.
pub trait KorwEmitter<T0, T1, T2> {
    fn korw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KSHIFTLB`.
///
/// Assembly forms: `kshiftlb k[7:0], k[7:0], imm8`.
pub trait KshiftlbEmitter<T0, T1, T2> {
    fn kshiftlb(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `KSHIFTLD`.
///
/// Assembly forms: `kshiftld k[31:0], k[31:0], imm8`.
pub trait KshiftldEmitter<T0, T1, T2> {
    fn kshiftld(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `KSHIFTLQ`.
///
/// Assembly forms: `kshiftlq k[63:0], k[63:0], imm8`.
pub trait KshiftlqEmitter<T0, T1, T2> {
    fn kshiftlq(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `KSHIFTLW`.
///
/// Assembly forms: `kshiftlw k[15:0], k[15:0], imm8`.
pub trait KshiftlwEmitter<T0, T1, T2> {
    fn kshiftlw(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `KSHIFTRB`.
///
/// Assembly forms: `kshiftrb k[7:0], k[7:0], imm8`.
pub trait KshiftrbEmitter<T0, T1, T2> {
    fn kshiftrb(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `KSHIFTRD`.
///
/// Assembly forms: `kshiftrd k[31:0], k[31:0], imm8`.
pub trait KshiftrdEmitter<T0, T1, T2> {
    fn kshiftrd(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `KSHIFTRQ`.
///
/// Assembly forms: `kshiftrq k[63:0], k[63:0], imm8`.
pub trait KshiftrqEmitter<T0, T1, T2> {
    fn kshiftrq(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `KSHIFTRW`.
///
/// Assembly forms: `kshiftrw k[15:0], k[15:0], imm8`.
pub trait KshiftrwEmitter<T0, T1, T2> {
    fn kshiftrw(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `KTESTB`.
///
/// Assembly forms: `ktestb k[7:0], k[7:0]`.
pub trait KtestbEmitter<T0, T1> {
    fn ktestb(&mut self, lhs: T0, rhs: T1);
}

/// Emits `KTESTD`.
///
/// Assembly forms: `ktestd k[31:0], k[31:0]`.
pub trait KtestdEmitter<T0, T1> {
    fn ktestd(&mut self, lhs: T0, rhs: T1);
}

/// Emits `KTESTQ`.
///
/// Assembly forms: `ktestq k[63:0], k[63:0]`.
pub trait KtestqEmitter<T0, T1> {
    fn ktestq(&mut self, lhs: T0, rhs: T1);
}

/// Emits `KTESTW`.
///
/// Assembly forms: `ktestw k[15:0], k[15:0]`.
pub trait KtestwEmitter<T0, T1> {
    fn ktestw(&mut self, lhs: T0, rhs: T1);
}

/// Emits `KUNPCKBW`.
///
/// Assembly forms: `kunpckbw k[15:0], k[7:0], k[7:0]`.
pub trait KunpckbwEmitter<T0, T1, T2> {
    fn kunpckbw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KUNPCKDQ`.
///
/// Assembly forms: `kunpckdq k[63:0], k[31:0], k[31:0]`.
pub trait KunpckdqEmitter<T0, T1, T2> {
    fn kunpckdq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KUNPCKWD`.
///
/// Assembly forms: `kunpckwd k[31:0], k[15:0], k[15:0]`.
pub trait KunpckwdEmitter<T0, T1, T2> {
    fn kunpckwd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KXNORB`.
///
/// Assembly forms: `kxnorb k[7:0], k[7:0], k[7:0]`.
pub trait KxnorbEmitter<T0, T1, T2> {
    fn kxnorb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KXNORD`.
///
/// Assembly forms: `kxnord k[31:0], k[31:0], k[31:0]`.
pub trait KxnordEmitter<T0, T1, T2> {
    fn kxnord(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KXNORQ`.
///
/// Assembly forms: `kxnorq k[63:0], k[63:0], k[63:0]`.
pub trait KxnorqEmitter<T0, T1, T2> {
    fn kxnorq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KXNORW`.
///
/// Assembly forms: `kxnorw k[15:0], k[15:0], k[15:0]`.
pub trait KxnorwEmitter<T0, T1, T2> {
    fn kxnorw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KXORB`.
///
/// Assembly forms: `kxorb k[7:0], k[7:0], k[7:0]`.
pub trait KxorbEmitter<T0, T1, T2> {
    fn kxorb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KXORD`.
///
/// Assembly forms: `kxord k[31:0], k[31:0], k[31:0]`.
pub trait KxordEmitter<T0, T1, T2> {
    fn kxord(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KXORQ`.
///
/// Assembly forms: `kxorq k[63:0], k[63:0], k[63:0]`.
pub trait KxorqEmitter<T0, T1, T2> {
    fn kxorq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `KXORW`.
///
/// Assembly forms: `kxorw k[15:0], k[15:0], k[15:0]`.
pub trait KxorwEmitter<T0, T1, T2> {
    fn kxorw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VADDPD`.
///
/// Assembly forms: `vaddpd xy, xy, xy/mxy`, `vaddpd xyz, xyz, xyz/mxyz/b64`.
pub trait VaddpdEmitter<T0, T1, T2> {
    fn vaddpd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VADDPS`.
///
/// Assembly forms: `vaddps xy, xy, xy/mxy`, `vaddps xyz, xyz, xyz/mxyz/b32`.
pub trait VaddpsEmitter<T0, T1, T2> {
    fn vaddps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VADDSD`.
///
/// Assembly forms: `vaddsd xmm, xmm, xmm[63:0]/m64`.
pub trait VaddsdEmitter<T0, T1, T2> {
    fn vaddsd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VADDSS`.
///
/// Assembly forms: `vaddss xmm, xmm, xmm[31:0]/m32`.
pub trait VaddssEmitter<T0, T1, T2> {
    fn vaddss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VADDSUBPD`.
///
/// Assembly forms: `vaddsubpd xy, xy, xy/mxy`.
pub trait VaddsubpdEmitter<T0, T1, T2> {
    fn vaddsubpd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VADDSUBPS`.
///
/// Assembly forms: `vaddsubps xy, xy, xy/mxy`.
pub trait VaddsubpsEmitter<T0, T1, T2> {
    fn vaddsubps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VAESDEC`.
///
/// Assembly forms: `vaesdec xmm, xmm, xmm/m128`, `vaesdec ymm, ymm, ymm/m256`, `vaesdec xyz, xyz, xyz/mxyz`.
pub trait VaesdecEmitter<T0, T1, T2> {
    fn vaesdec(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VAESDECLAST`.
///
/// Assembly forms: `vaesdeclast xmm, xmm, xmm/m128`, `vaesdeclast ymm, ymm, ymm/m256`, `vaesdeclast xyz, xyz, xyz/mxyz`.
pub trait VaesdeclastEmitter<T0, T1, T2> {
    fn vaesdeclast(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VAESENC`.
///
/// Assembly forms: `vaesenc xmm, xmm, xmm/m128`, `vaesenc ymm, ymm, ymm/m256`, `vaesenc xyz, xyz, xyz/mxyz`.
pub trait VaesencEmitter<T0, T1, T2> {
    fn vaesenc(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VAESENCLAST`.
///
/// Assembly forms: `vaesenclast xmm, xmm, xmm/m128`, `vaesenclast ymm, ymm, ymm/m256`, `vaesenclast xyz, xyz, xyz/mxyz`.
pub trait VaesenclastEmitter<T0, T1, T2> {
    fn vaesenclast(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VAESIMC`.
///
/// Assembly forms: `vaesimc xmm, xmm/m128`.
pub trait VaesimcEmitter<T0, T1> {
    fn vaesimc(&mut self, dst: T0, src: T1);
}

/// Emits `VAESKEYGENASSIST`.
///
/// Assembly forms: `vaeskeygenassist xmm, xmm/m128, imm8`.
pub trait VaeskeygenassistEmitter<T0, T1, T2> {
    fn vaeskeygenassist(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VALIGND`.
///
/// Assembly forms: `valignd xyz, xyz, xyz/mxyz/b32, imm8`.
pub trait ValigndEmitter<T0, T1, T2, T3> {
    fn valignd(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VALIGNQ`.
///
/// Assembly forms: `valignq xyz, xyz, xyz/mxyz/b64, imm8`.
pub trait ValignqEmitter<T0, T1, T2, T3> {
    fn valignq(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VANDNPD`.
///
/// Assembly forms: `vandnpd xy, xy, xy/mxy`, `vandnpd xyz, xyz, xyz/mxyz/b64`.
pub trait VandnpdEmitter<T0, T1, T2> {
    fn vandnpd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VANDNPS`.
///
/// Assembly forms: `vandnps xy, xy, xy/mxy`, `vandnps xyz, xyz, xyz/mxyz/b32`.
pub trait VandnpsEmitter<T0, T1, T2> {
    fn vandnps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VANDPD`.
///
/// Assembly forms: `vandpd xy, xy, xy/mxy`, `vandpd xyz, xyz, xyz/mxyz/b64`.
pub trait VandpdEmitter<T0, T1, T2> {
    fn vandpd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VANDPS`.
///
/// Assembly forms: `vandps xy, xy, xy/mxy`, `vandps xyz, xyz, xyz/mxyz/b32`.
pub trait VandpsEmitter<T0, T1, T2> {
    fn vandps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VBLENDMPD`.
///
/// Assembly forms: `vblendmpd xyz, xyz, xyz/mxyz/b64`.
pub trait VblendmpdEmitter<T0, T1, T2> {
    fn vblendmpd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VBLENDMPS`.
///
/// Assembly forms: `vblendmps xyz, xyz, xyz/mxyz/b32`.
pub trait VblendmpsEmitter<T0, T1, T2> {
    fn vblendmps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VBLENDPD`.
///
/// Assembly forms: `vblendpd xy, xy, xy/mxy, imm8`.
pub trait VblendpdEmitter<T0, T1, T2, T3> {
    fn vblendpd(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VBLENDPS`.
///
/// Assembly forms: `vblendps xy, xy, xy/mxy, imm8`.
pub trait VblendpsEmitter<T0, T1, T2, T3> {
    fn vblendps(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VBLENDVPD`.
///
/// Assembly forms: `vblendvpd xy, xy, xy/mxy, xy`.
pub trait VblendvpdEmitter<T0, T1, T2, T3> {
    fn vblendvpd(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VBLENDVPS`.
///
/// Assembly forms: `vblendvps xy, xy, xy/mxy, xy`.
pub trait VblendvpsEmitter<T0, T1, T2, T3> {
    fn vblendvps(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VBROADCASTF128`.
///
/// Assembly forms: `vbroadcastf128 ymm, m128`.
pub trait Vbroadcastf128Emitter<T0, T1> {
    fn vbroadcastf128(&mut self, dst: T0, src: T1);
}

/// Emits `VBROADCASTF32X2`.
///
/// Assembly forms: `vbroadcastf32x2 ymm, xmm[63:0]/m64`, `vbroadcastf32x2 zmm, xmm[63:0]/m64`.
pub trait Vbroadcastf32x2Emitter<T0, T1> {
    fn vbroadcastf32x2(&mut self, dst: T0, src: T1);
}

/// Emits `VBROADCASTF32X4`.
///
/// Assembly forms: `vbroadcastf32x4 ymm, m128`, `vbroadcastf32x4 zmm, m128`.
pub trait Vbroadcastf32x4Emitter<T0, T1> {
    fn vbroadcastf32x4(&mut self, dst: T0, src: T1);
}

/// Emits `VBROADCASTF32X8`.
///
/// Assembly forms: `vbroadcastf32x8 zmm, m256`.
pub trait Vbroadcastf32x8Emitter<T0, T1> {
    fn vbroadcastf32x8(&mut self, dst: T0, src: T1);
}

/// Emits `VBROADCASTF64X2`.
///
/// Assembly forms: `vbroadcastf64x2 ymm, m128`, `vbroadcastf64x2 zmm, m128`.
pub trait Vbroadcastf64x2Emitter<T0, T1> {
    fn vbroadcastf64x2(&mut self, dst: T0, src: T1);
}

/// Emits `VBROADCASTF64X4`.
///
/// Assembly forms: `vbroadcastf64x4 zmm, m256`.
pub trait Vbroadcastf64x4Emitter<T0, T1> {
    fn vbroadcastf64x4(&mut self, dst: T0, src: T1);
}

/// Emits `VBROADCASTI128`.
///
/// Assembly forms: `vbroadcasti128 ymm, m128`.
pub trait Vbroadcasti128Emitter<T0, T1> {
    fn vbroadcasti128(&mut self, dst: T0, src: T1);
}

/// Emits `VBROADCASTI32X2`.
///
/// Assembly forms: `vbroadcasti32x2 xmm, xmm[63:0]/m64`, `vbroadcasti32x2 ymm, xmm[63:0]/m64`, `vbroadcasti32x2 zmm, xmm[63:0]/m64`.
pub trait Vbroadcasti32x2Emitter<T0, T1> {
    fn vbroadcasti32x2(&mut self, dst: T0, src: T1);
}

/// Emits `VBROADCASTI32X4`.
///
/// Assembly forms: `vbroadcasti32x4 ymm, m128`, `vbroadcasti32x4 zmm, m128`.
pub trait Vbroadcasti32x4Emitter<T0, T1> {
    fn vbroadcasti32x4(&mut self, dst: T0, src: T1);
}

/// Emits `VBROADCASTI32X8`.
///
/// Assembly forms: `vbroadcasti32x8 zmm, m256`.
pub trait Vbroadcasti32x8Emitter<T0, T1> {
    fn vbroadcasti32x8(&mut self, dst: T0, src: T1);
}

/// Emits `VBROADCASTI64X2`.
///
/// Assembly forms: `vbroadcasti64x2 ymm, m128`, `vbroadcasti64x2 zmm, m128`.
pub trait Vbroadcasti64x2Emitter<T0, T1> {
    fn vbroadcasti64x2(&mut self, dst: T0, src: T1);
}

/// Emits `VBROADCASTI64X4`.
///
/// Assembly forms: `vbroadcasti64x4 zmm, m256`.
pub trait Vbroadcasti64x4Emitter<T0, T1> {
    fn vbroadcasti64x4(&mut self, dst: T0, src: T1);
}

/// Emits `VBROADCASTSD`.
///
/// Assembly forms: `vbroadcastsd ymm, m64`, `vbroadcastsd ymm, xmm[63:0]`, `vbroadcastsd ymm, xmm[63:0]/m64`.
pub trait VbroadcastsdEmitter<T0, T1> {
    fn vbroadcastsd(&mut self, dst: T0, src: T1);
}

/// Emits `VBROADCASTSS`.
///
/// Assembly forms: `vbroadcastss xy, m32`, `vbroadcastss xmm, xmm[31:0]`, `vbroadcastss ymm, xmm[31:0]`.
pub trait VbroadcastssEmitter<T0, T1> {
    fn vbroadcastss(&mut self, dst: T0, src: T1);
}

/// Emits `VCMPPD`.
///
/// Assembly forms: `vcmppd xy, xy, xy/mxy, imm8`, `vcmppd k, xyz, xyz/mxyz/b64, imm8`.
pub trait VcmppdEmitter<T0, T1, T2, T3> {
    fn vcmppd(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VCMPPS`.
///
/// Assembly forms: `vcmpps xy, xy, xy/mxy, imm8`, `vcmpps k, xyz, xyz/mxyz/b32, imm8`.
pub trait VcmppsEmitter<T0, T1, T2, T3> {
    fn vcmpps(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VCMPSD`.
///
/// Assembly forms: `vcmpsd xmm, xmm, xmm[63:0]/m64, imm8`, `vcmpsd k, xmm, xmm[63:0]/m64, imm8`.
pub trait VcmpsdEmitter<T0, T1, T2, T3> {
    fn vcmpsd(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VCMPSS`.
///
/// Assembly forms: `vcmpss xmm, xmm, xmm[31:0]/m32, imm8`, `vcmpss k, xmm, xmm[31:0]/m32, imm8`.
pub trait VcmpssEmitter<T0, T1, T2, T3> {
    fn vcmpss(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VCOMISD`.
///
/// Assembly forms: `vcomisd xmm[63:0], xmm[63:0]/m64`.
pub trait VcomisdEmitter<T0, T1> {
    fn vcomisd(&mut self, lhs: T0, rhs: T1);
}

/// Emits `VCOMISS`.
///
/// Assembly forms: `vcomiss xmm[31:0], xmm[31:0]/m32`.
pub trait VcomissEmitter<T0, T1> {
    fn vcomiss(&mut self, lhs: T0, rhs: T1);
}

/// Emits `VCOMPRESSPD`.
///
/// Assembly forms: `vcompresspd xyz/mxyz, xyz`.
pub trait VcompresspdEmitter<T0, T1> {
    fn vcompresspd(&mut self, dst: T0, src: T1);
}

/// Emits `VCOMPRESSPS`.
///
/// Assembly forms: `vcompressps xyz/mxyz, xyz`.
pub trait VcompresspsEmitter<T0, T1> {
    fn vcompressps(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTDQ2PD`.
///
/// Assembly forms: `vcvtdq2pd xmm, xmm[63:0]/m64`, `vcvtdq2pd ymm, xmm/m128`, `vcvtdq2pd xyz, xxy/mxxy/b32`.
pub trait Vcvtdq2pdEmitter<T0, T1> {
    fn vcvtdq2pd(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTDQ2PS`.
///
/// Assembly forms: `vcvtdq2ps xmm, xmm/m128`, `vcvtdq2ps ymm, ymm/m256`, `vcvtdq2ps xyz, xyz/mxyz/b32`.
pub trait Vcvtdq2psEmitter<T0, T1> {
    fn vcvtdq2ps(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTNE2PS2BF16`.
///
/// Assembly forms: `vcvtne2ps2bf16 xyz, xyz, xyz/mxyz/b32`.
pub trait Vcvtne2ps2bf16Emitter<T0, T1, T2> {
    fn vcvtne2ps2bf16(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VCVTNEPS2BF16`.
///
/// Assembly forms: `vcvtneps2bf16 W:xmm[63:0], xmm/m128`, `vcvtneps2bf16 W:xmm, ymm/m256`, `vcvtneps2bf16 xxy, xyz/mxyz/b32`.
pub trait Vcvtneps2bf16Emitter<T0, T1> {
    fn vcvtneps2bf16(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTPD2DQ`.
///
/// Assembly forms: `vcvtpd2dq xmm[63:0], xmm/m128`, `vcvtpd2dq xmm, ymm/m256`, `vcvtpd2dq xxy, xyz/mxyz/b64`.
pub trait Vcvtpd2dqEmitter<T0, T1> {
    fn vcvtpd2dq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTPD2PS`.
///
/// Assembly forms: `vcvtpd2ps xmm[63:0], xmm/m128`, `vcvtpd2ps xmm, ymm/m256`, `vcvtpd2ps xxy, xyz/mxyz/b64`.
pub trait Vcvtpd2psEmitter<T0, T1> {
    fn vcvtpd2ps(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTPD2QQ`.
///
/// Assembly forms: `vcvtpd2qq xyz, xyz/mxyz/b64`.
pub trait Vcvtpd2qqEmitter<T0, T1> {
    fn vcvtpd2qq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTPD2UDQ`.
///
/// Assembly forms: `vcvtpd2udq xxy, xyz/mxyz/b64`.
pub trait Vcvtpd2udqEmitter<T0, T1> {
    fn vcvtpd2udq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTPD2UQQ`.
///
/// Assembly forms: `vcvtpd2uqq xyz, xyz/mxyz/b64`.
pub trait Vcvtpd2uqqEmitter<T0, T1> {
    fn vcvtpd2uqq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTPH2PS`.
///
/// Assembly forms: `vcvtph2ps xmm, xmm[63:0]/m64`, `vcvtph2ps ymm, xmm/m128`, `vcvtph2ps xyz, xxy/mxxy`.
pub trait Vcvtph2psEmitter<T0, T1> {
    fn vcvtph2ps(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTPS2DQ`.
///
/// Assembly forms: `vcvtps2dq xmm, xmm/m128`, `vcvtps2dq ymm, ymm/m256`, `vcvtps2dq xyz, xyz/mxyz/b32`.
pub trait Vcvtps2dqEmitter<T0, T1> {
    fn vcvtps2dq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTPS2PD`.
///
/// Assembly forms: `vcvtps2pd xmm, xmm[63:0]/m64`, `vcvtps2pd ymm, xmm/m128`, `vcvtps2pd xyz, xxy/mxxy/b32`.
pub trait Vcvtps2pdEmitter<T0, T1> {
    fn vcvtps2pd(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTPS2PH`.
///
/// Assembly forms: `vcvtps2ph xmm[63:0]/m64, xmm, imm8`, `vcvtps2ph xmm/m128, ymm, imm8`, `vcvtps2ph xxy/mxxy, xyz, imm8`.
pub trait Vcvtps2phEmitter<T0, T1, T2> {
    fn vcvtps2ph(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VCVTPS2QQ`.
///
/// Assembly forms: `vcvtps2qq xyz, xxy/mxxy/b32`.
pub trait Vcvtps2qqEmitter<T0, T1> {
    fn vcvtps2qq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTPS2UDQ`.
///
/// Assembly forms: `vcvtps2udq xyz, xyz/mxyz/b32`.
pub trait Vcvtps2udqEmitter<T0, T1> {
    fn vcvtps2udq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTPS2UQQ`.
///
/// Assembly forms: `vcvtps2uqq xyz, xxy/mxxy/b32`.
pub trait Vcvtps2uqqEmitter<T0, T1> {
    fn vcvtps2uqq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTQQ2PD`.
///
/// Assembly forms: `vcvtqq2pd xyz, xyz/mxyz/b64`.
pub trait Vcvtqq2pdEmitter<T0, T1> {
    fn vcvtqq2pd(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTQQ2PS`.
///
/// Assembly forms: `vcvtqq2ps xxy, xyz/mxyz/b64`.
pub trait Vcvtqq2psEmitter<T0, T1> {
    fn vcvtqq2ps(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTSD2SI`.
///
/// Assembly forms: `vcvtsd2si ry, xmm[63:0]/m64`.
pub trait Vcvtsd2siEmitter<T0, T1> {
    fn vcvtsd2si(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTSD2SS`.
///
/// Assembly forms: `vcvtsd2ss xmm, xmm[127:32], xmm[63:0]/m64`.
pub trait Vcvtsd2ssEmitter<T0, T1, T2> {
    fn vcvtsd2ss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VCVTSD2USI`.
///
/// Assembly forms: `vcvtsd2usi ry, xmm[63:0]/m64`.
pub trait Vcvtsd2usiEmitter<T0, T1> {
    fn vcvtsd2usi(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTSI2SD`.
///
/// Assembly forms: `vcvtsi2sd xmm, xmm[127:64], ry/my`.
pub trait Vcvtsi2sdEmitter<T0, T1, T2> {
    fn vcvtsi2sd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VCVTSI2SS`.
///
/// Assembly forms: `vcvtsi2ss xmm, xmm[127:32], ry/my`.
pub trait Vcvtsi2ssEmitter<T0, T1, T2> {
    fn vcvtsi2ss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VCVTSS2SD`.
///
/// Assembly forms: `vcvtss2sd xmm, xmm[127:64], xmm[31:0]/m32`.
pub trait Vcvtss2sdEmitter<T0, T1, T2> {
    fn vcvtss2sd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VCVTSS2SI`.
///
/// Assembly forms: `vcvtss2si ry, xmm[31:0]/m32`.
pub trait Vcvtss2siEmitter<T0, T1> {
    fn vcvtss2si(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTSS2USI`.
///
/// Assembly forms: `vcvtss2usi ry, xmm[31:0]/m32`.
pub trait Vcvtss2usiEmitter<T0, T1> {
    fn vcvtss2usi(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTTPD2DQ`.
///
/// Assembly forms: `vcvttpd2dq xmm[63:0], xmm/m128`, `vcvttpd2dq xmm, ymm/m256`, `vcvttpd2dq xxy, xyz/mxyz/b64`.
pub trait Vcvttpd2dqEmitter<T0, T1> {
    fn vcvttpd2dq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTTPD2QQ`.
///
/// Assembly forms: `vcvttpd2qq xyz, xyz/mxyz/b64`.
pub trait Vcvttpd2qqEmitter<T0, T1> {
    fn vcvttpd2qq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTTPD2UDQ`.
///
/// Assembly forms: `vcvttpd2udq xxy, xyz/mxyz/b64`.
pub trait Vcvttpd2udqEmitter<T0, T1> {
    fn vcvttpd2udq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTTPD2UQQ`.
///
/// Assembly forms: `vcvttpd2uqq xyz, xyz/mxyz/b64`.
pub trait Vcvttpd2uqqEmitter<T0, T1> {
    fn vcvttpd2uqq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTTPS2DQ`.
///
/// Assembly forms: `vcvttps2dq xmm, xmm/m128`, `vcvttps2dq ymm, ymm/m256`, `vcvttps2dq xyz, xyz/mxyz/b32`.
pub trait Vcvttps2dqEmitter<T0, T1> {
    fn vcvttps2dq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTTPS2QQ`.
///
/// Assembly forms: `vcvttps2qq xyz, xxy/mxxy/b32`.
pub trait Vcvttps2qqEmitter<T0, T1> {
    fn vcvttps2qq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTTPS2UDQ`.
///
/// Assembly forms: `vcvttps2udq xyz, xyz/mxyz/b32`.
pub trait Vcvttps2udqEmitter<T0, T1> {
    fn vcvttps2udq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTTPS2UQQ`.
///
/// Assembly forms: `vcvttps2uqq xyz, xxy/mxxy/b32`.
pub trait Vcvttps2uqqEmitter<T0, T1> {
    fn vcvttps2uqq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTTSD2SI`.
///
/// Assembly forms: `vcvttsd2si ry, xmm[63:0]/m64`.
pub trait Vcvttsd2siEmitter<T0, T1> {
    fn vcvttsd2si(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTTSD2USI`.
///
/// Assembly forms: `vcvttsd2usi ry, xmm[63:0]/m64`.
pub trait Vcvttsd2usiEmitter<T0, T1> {
    fn vcvttsd2usi(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTTSS2SI`.
///
/// Assembly forms: `vcvttss2si ry, xmm[31:0]/m32`.
pub trait Vcvttss2siEmitter<T0, T1> {
    fn vcvttss2si(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTTSS2USI`.
///
/// Assembly forms: `vcvttss2usi ry, xmm[31:0]/m32`.
pub trait Vcvttss2usiEmitter<T0, T1> {
    fn vcvttss2usi(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTUDQ2PD`.
///
/// Assembly forms: `vcvtudq2pd xyz, xxy/mxxy/b32`.
pub trait Vcvtudq2pdEmitter<T0, T1> {
    fn vcvtudq2pd(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTUDQ2PS`.
///
/// Assembly forms: `vcvtudq2ps xyz, xyz/mxyz/b32`.
pub trait Vcvtudq2psEmitter<T0, T1> {
    fn vcvtudq2ps(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTUQQ2PD`.
///
/// Assembly forms: `vcvtuqq2pd xyz, xyz/mxyz/b64`.
pub trait Vcvtuqq2pdEmitter<T0, T1> {
    fn vcvtuqq2pd(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTUQQ2PS`.
///
/// Assembly forms: `vcvtuqq2ps xxy, xyz/mxyz/b64`.
pub trait Vcvtuqq2psEmitter<T0, T1> {
    fn vcvtuqq2ps(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTUSI2SD`.
///
/// Assembly forms: `vcvtusi2sd xmm, xmm[127:64], ry/my`.
pub trait Vcvtusi2sdEmitter<T0, T1, T2> {
    fn vcvtusi2sd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VCVTUSI2SS`.
///
/// Assembly forms: `vcvtusi2ss xmm, xmm[127:32], ry/my`.
pub trait Vcvtusi2ssEmitter<T0, T1, T2> {
    fn vcvtusi2ss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VDBPSADBW`.
///
/// Assembly forms: `vdbpsadbw xyz, xyz, xyz/mxyz, imm8`.
pub trait VdbpsadbwEmitter<T0, T1, T2, T3> {
    fn vdbpsadbw(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VDIVPD`.
///
/// Assembly forms: `vdivpd xmm, xmm, xmm/m128`, `vdivpd ymm, ymm, ymm/m256`, `vdivpd xyz, xyz, xyz/mxyz/b64`.
pub trait VdivpdEmitter<T0, T1, T2> {
    fn vdivpd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VDIVPS`.
///
/// Assembly forms: `vdivps xmm, xmm, xmm/m128`, `vdivps ymm, ymm, ymm/m256`, `vdivps xyz, xyz, xyz/mxyz/b32`.
pub trait VdivpsEmitter<T0, T1, T2> {
    fn vdivps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VDIVSD`.
///
/// Assembly forms: `vdivsd xmm, xmm, xmm[63:0]/m64`.
pub trait VdivsdEmitter<T0, T1, T2> {
    fn vdivsd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VDIVSS`.
///
/// Assembly forms: `vdivss xmm, xmm, xmm[31:0]/m32`.
pub trait VdivssEmitter<T0, T1, T2> {
    fn vdivss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VDPBF16PS`.
///
/// Assembly forms: `vdpbf16ps xyz, xyz, xyz/mxyz/b32`.
pub trait Vdpbf16psEmitter<T0, T1, T2> {
    fn vdpbf16ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VDPPD`.
///
/// Assembly forms: `vdppd xmm, xmm, xmm/m128, imm8`.
pub trait VdppdEmitter<T0, T1, T2, T3> {
    fn vdppd(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VDPPS`.
///
/// Assembly forms: `vdpps xy, xy, xy/mxy, imm8`.
pub trait VdppsEmitter<T0, T1, T2, T3> {
    fn vdpps(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VEXPANDPD`.
///
/// Assembly forms: `vexpandpd xyz, xyz/mxyz`.
pub trait VexpandpdEmitter<T0, T1> {
    fn vexpandpd(&mut self, dst: T0, src: T1);
}

/// Emits `VEXPANDPS`.
///
/// Assembly forms: `vexpandps xyz, xyz/mxyz`.
pub trait VexpandpsEmitter<T0, T1> {
    fn vexpandps(&mut self, dst: T0, src: T1);
}

/// Emits `VEXTRACTF128`.
///
/// Assembly forms: `vextractf128 xmm/m128, ymm, imm8`.
pub trait Vextractf128Emitter<T0, T1, T2> {
    fn vextractf128(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VEXTRACTF32X4`.
///
/// Assembly forms: `vextractf32x4 xmm/m128, ymm, imm8`, `vextractf32x4 xmm/m128, zmm, imm8`.
pub trait Vextractf32x4Emitter<T0, T1, T2> {
    fn vextractf32x4(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VEXTRACTF32X8`.
///
/// Assembly forms: `vextractf32x8 ymm/m256, zmm, imm8`.
pub trait Vextractf32x8Emitter<T0, T1, T2> {
    fn vextractf32x8(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VEXTRACTF64X2`.
///
/// Assembly forms: `vextractf64x2 xmm/m128, ymm, imm8`, `vextractf64x2 xmm/m128, zmm, imm8`.
pub trait Vextractf64x2Emitter<T0, T1, T2> {
    fn vextractf64x2(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VEXTRACTF64X4`.
///
/// Assembly forms: `vextractf64x4 ymm/m256, zmm, imm8`.
pub trait Vextractf64x4Emitter<T0, T1, T2> {
    fn vextractf64x4(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VEXTRACTI128`.
///
/// Assembly forms: `vextracti128 xmm/m128, ymm, imm8`.
pub trait Vextracti128Emitter<T0, T1, T2> {
    fn vextracti128(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VEXTRACTI32X4`.
///
/// Assembly forms: `vextracti32x4 xmm/m128, ymm, imm8`, `vextracti32x4 xmm/m128, zmm, imm8`.
pub trait Vextracti32x4Emitter<T0, T1, T2> {
    fn vextracti32x4(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VEXTRACTI32X8`.
///
/// Assembly forms: `vextracti32x8 ymm/m256, zmm, imm8`.
pub trait Vextracti32x8Emitter<T0, T1, T2> {
    fn vextracti32x8(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VEXTRACTI64X2`.
///
/// Assembly forms: `vextracti64x2 xmm/m128, ymm, imm8`, `vextracti64x2 xmm/m128, zmm, imm8`.
pub trait Vextracti64x2Emitter<T0, T1, T2> {
    fn vextracti64x2(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VEXTRACTI64X4`.
///
/// Assembly forms: `vextracti64x4 ymm/m256, zmm, imm8`.
pub trait Vextracti64x4Emitter<T0, T1, T2> {
    fn vextracti64x4(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VEXTRACTPS`.
///
/// Assembly forms: `vextractps r32[31:0]/m32, xmm, imm8`.
pub trait VextractpsEmitter<T0, T1, T2> {
    fn vextractps(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VFIXUPIMMPD`.
///
/// Assembly forms: `vfixupimmpd xyz, xyz, xyz/mxyz/b64, imm8`.
pub trait VfixupimmpdEmitter<T0, T1, T2, T3> {
    fn vfixupimmpd(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VFIXUPIMMPS`.
///
/// Assembly forms: `vfixupimmps xyz, xyz, xyz/mxyz/b32, imm8`.
pub trait VfixupimmpsEmitter<T0, T1, T2, T3> {
    fn vfixupimmps(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VFIXUPIMMSD`.
///
/// Assembly forms: `vfixupimmsd xmm, xmm[127:64], xmm[63:0]/m64, imm8`.
pub trait VfixupimmsdEmitter<T0, T1, T2, T3> {
    fn vfixupimmsd(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VFIXUPIMMSS`.
///
/// Assembly forms: `vfixupimmss xmm, xmm[127:32], xmm[31:0]/m32, imm8`.
pub trait VfixupimmssEmitter<T0, T1, T2, T3> {
    fn vfixupimmss(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VFMADD132PD`.
///
/// Assembly forms: `vfmadd132pd xy, xy, xy/mxy`, `vfmadd132pd xyz, xyz, xyz/mxyz/b64`.
pub trait Vfmadd132pdEmitter<T0, T1, T2> {
    fn vfmadd132pd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADD132PS`.
///
/// Assembly forms: `vfmadd132ps xy, xy, xy/mxy`, `vfmadd132ps xyz, xyz, xyz/mxyz/b32`.
pub trait Vfmadd132psEmitter<T0, T1, T2> {
    fn vfmadd132ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADD132SD`.
///
/// Assembly forms: `vfmadd132sd xmm[63:0], xmm[63:0], xmm[63:0]/m64`.
pub trait Vfmadd132sdEmitter<T0, T1, T2> {
    fn vfmadd132sd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADD132SS`.
///
/// Assembly forms: `vfmadd132ss xmm[31:0], xmm[31:0], xmm[31:0]/m32`.
pub trait Vfmadd132ssEmitter<T0, T1, T2> {
    fn vfmadd132ss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADD213PD`.
///
/// Assembly forms: `vfmadd213pd xy, xy, xy/mxy`, `vfmadd213pd xyz, xyz, xyz/mxyz/b64`.
pub trait Vfmadd213pdEmitter<T0, T1, T2> {
    fn vfmadd213pd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADD213PS`.
///
/// Assembly forms: `vfmadd213ps xy, xy, xy/mxy`, `vfmadd213ps xyz, xyz, xyz/mxyz/b32`.
pub trait Vfmadd213psEmitter<T0, T1, T2> {
    fn vfmadd213ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADD213SD`.
///
/// Assembly forms: `vfmadd213sd xmm[63:0], xmm[63:0], xmm[63:0]/m64`.
pub trait Vfmadd213sdEmitter<T0, T1, T2> {
    fn vfmadd213sd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADD213SS`.
///
/// Assembly forms: `vfmadd213ss xmm[31:0], xmm[31:0], xmm[31:0]/m32`.
pub trait Vfmadd213ssEmitter<T0, T1, T2> {
    fn vfmadd213ss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADD231PD`.
///
/// Assembly forms: `vfmadd231pd xy, xy, xy/mxy`, `vfmadd231pd xyz, xyz, xyz/mxyz/b64`.
pub trait Vfmadd231pdEmitter<T0, T1, T2> {
    fn vfmadd231pd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADD231PS`.
///
/// Assembly forms: `vfmadd231ps xy, xy, xy/mxy`, `vfmadd231ps xyz, xyz, xyz/mxyz/b32`.
pub trait Vfmadd231psEmitter<T0, T1, T2> {
    fn vfmadd231ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADD231SD`.
///
/// Assembly forms: `vfmadd231sd xmm[63:0], xmm[63:0], xmm[63:0]/m64`.
pub trait Vfmadd231sdEmitter<T0, T1, T2> {
    fn vfmadd231sd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADD231SS`.
///
/// Assembly forms: `vfmadd231ss xmm[31:0], xmm[31:0], xmm[31:0]/m32`.
pub trait Vfmadd231ssEmitter<T0, T1, T2> {
    fn vfmadd231ss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADDSUB132PD`.
///
/// Assembly forms: `vfmaddsub132pd xy, xy, xy/mxy`, `vfmaddsub132pd xyz, xyz, xyz/mxyz/b64`.
pub trait Vfmaddsub132pdEmitter<T0, T1, T2> {
    fn vfmaddsub132pd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADDSUB132PS`.
///
/// Assembly forms: `vfmaddsub132ps xy, xy, xy/mxy`, `vfmaddsub132ps xyz, xyz, xyz/mxyz/b32`.
pub trait Vfmaddsub132psEmitter<T0, T1, T2> {
    fn vfmaddsub132ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADDSUB213PD`.
///
/// Assembly forms: `vfmaddsub213pd xy, xy, xy/mxy`, `vfmaddsub213pd xyz, xyz, xyz/mxyz/b64`.
pub trait Vfmaddsub213pdEmitter<T0, T1, T2> {
    fn vfmaddsub213pd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADDSUB213PS`.
///
/// Assembly forms: `vfmaddsub213ps xy, xy, xy/mxy`, `vfmaddsub213ps xyz, xyz, xyz/mxyz/b32`.
pub trait Vfmaddsub213psEmitter<T0, T1, T2> {
    fn vfmaddsub213ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADDSUB231PD`.
///
/// Assembly forms: `vfmaddsub231pd xy, xy, xy/mxy`, `vfmaddsub231pd xyz, xyz, xyz/mxyz/b64`.
pub trait Vfmaddsub231pdEmitter<T0, T1, T2> {
    fn vfmaddsub231pd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADDSUB231PS`.
///
/// Assembly forms: `vfmaddsub231ps xy, xy, xy/mxy`, `vfmaddsub231ps xyz, xyz, xyz/mxyz/b32`.
pub trait Vfmaddsub231psEmitter<T0, T1, T2> {
    fn vfmaddsub231ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUB132PD`.
///
/// Assembly forms: `vfmsub132pd xy, xy, xy/mxy`, `vfmsub132pd xyz, xyz, xyz/mxyz/b64`.
pub trait Vfmsub132pdEmitter<T0, T1, T2> {
    fn vfmsub132pd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUB132PS`.
///
/// Assembly forms: `vfmsub132ps xy, xy, xy/mxy`, `vfmsub132ps xyz, xyz, xyz/mxyz/b32`.
pub trait Vfmsub132psEmitter<T0, T1, T2> {
    fn vfmsub132ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUB132SD`.
///
/// Assembly forms: `vfmsub132sd xmm[63:0], xmm[63:0], xmm[63:0]/m64`.
pub trait Vfmsub132sdEmitter<T0, T1, T2> {
    fn vfmsub132sd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUB132SS`.
///
/// Assembly forms: `vfmsub132ss xmm[31:0], xmm[31:0], xmm[31:0]/m32`.
pub trait Vfmsub132ssEmitter<T0, T1, T2> {
    fn vfmsub132ss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUB213PD`.
///
/// Assembly forms: `vfmsub213pd xy, xy, xy/mxy`, `vfmsub213pd xyz, xyz, xyz/mxyz/b64`.
pub trait Vfmsub213pdEmitter<T0, T1, T2> {
    fn vfmsub213pd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUB213PS`.
///
/// Assembly forms: `vfmsub213ps xy, xy, xy/mxy`, `vfmsub213ps xyz, xyz, xyz/mxyz/b32`.
pub trait Vfmsub213psEmitter<T0, T1, T2> {
    fn vfmsub213ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUB213SD`.
///
/// Assembly forms: `vfmsub213sd xmm[63:0], xmm[63:0], xmm[63:0]/m64`.
pub trait Vfmsub213sdEmitter<T0, T1, T2> {
    fn vfmsub213sd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUB213SS`.
///
/// Assembly forms: `vfmsub213ss xmm[31:0], xmm[31:0], xmm[31:0]/m32`.
pub trait Vfmsub213ssEmitter<T0, T1, T2> {
    fn vfmsub213ss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUB231PD`.
///
/// Assembly forms: `vfmsub231pd xy, xy, xy/mxy`, `vfmsub231pd xyz, xyz, xyz/mxyz/b64`.
pub trait Vfmsub231pdEmitter<T0, T1, T2> {
    fn vfmsub231pd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUB231PS`.
///
/// Assembly forms: `vfmsub231ps xy, xy, xy/mxy`, `vfmsub231ps xyz, xyz, xyz/mxyz/b32`.
pub trait Vfmsub231psEmitter<T0, T1, T2> {
    fn vfmsub231ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUB231SD`.
///
/// Assembly forms: `vfmsub231sd xmm[63:0], xmm[63:0], xmm[63:0]/m64`.
pub trait Vfmsub231sdEmitter<T0, T1, T2> {
    fn vfmsub231sd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUB231SS`.
///
/// Assembly forms: `vfmsub231ss xmm[31:0], xmm[31:0], xmm[31:0]/m32`.
pub trait Vfmsub231ssEmitter<T0, T1, T2> {
    fn vfmsub231ss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUBADD132PD`.
///
/// Assembly forms: `vfmsubadd132pd xy, xy, xy/mxy`, `vfmsubadd132pd xyz, xyz, xyz/mxyz/b64`.
pub trait Vfmsubadd132pdEmitter<T0, T1, T2> {
    fn vfmsubadd132pd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUBADD132PS`.
///
/// Assembly forms: `vfmsubadd132ps xy, xy, xy/mxy`, `vfmsubadd132ps xyz, xyz, xyz/mxyz/b32`.
pub trait Vfmsubadd132psEmitter<T0, T1, T2> {
    fn vfmsubadd132ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUBADD213PD`.
///
/// Assembly forms: `vfmsubadd213pd xy, xy, xy/mxy`, `vfmsubadd213pd xyz, xyz, xyz/mxyz/b64`.
pub trait Vfmsubadd213pdEmitter<T0, T1, T2> {
    fn vfmsubadd213pd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUBADD213PS`.
///
/// Assembly forms: `vfmsubadd213ps xy, xy, xy/mxy`, `vfmsubadd213ps xyz, xyz, xyz/mxyz/b32`.
pub trait Vfmsubadd213psEmitter<T0, T1, T2> {
    fn vfmsubadd213ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUBADD231PD`.
///
/// Assembly forms: `vfmsubadd231pd xy, xy, xy/mxy`, `vfmsubadd231pd xyz, xyz, xyz/mxyz/b64`.
pub trait Vfmsubadd231pdEmitter<T0, T1, T2> {
    fn vfmsubadd231pd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUBADD231PS`.
///
/// Assembly forms: `vfmsubadd231ps xy, xy, xy/mxy`, `vfmsubadd231ps xyz, xyz, xyz/mxyz/b32`.
pub trait Vfmsubadd231psEmitter<T0, T1, T2> {
    fn vfmsubadd231ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMADD132PD`.
///
/// Assembly forms: `vfnmadd132pd xy, xy, xy/mxy`, `vfnmadd132pd xyz, xyz, xyz/mxyz/b64`.
pub trait Vfnmadd132pdEmitter<T0, T1, T2> {
    fn vfnmadd132pd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMADD132PS`.
///
/// Assembly forms: `vfnmadd132ps xy, xy, xy/mxy`, `vfnmadd132ps xyz, xyz, xyz/mxyz/b32`.
pub trait Vfnmadd132psEmitter<T0, T1, T2> {
    fn vfnmadd132ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMADD132SD`.
///
/// Assembly forms: `vfnmadd132sd xmm[63:0], xmm[63:0], xmm[63:0]/m64`.
pub trait Vfnmadd132sdEmitter<T0, T1, T2> {
    fn vfnmadd132sd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMADD132SS`.
///
/// Assembly forms: `vfnmadd132ss xmm[31:0], xmm[31:0], xmm[31:0]/m32`.
pub trait Vfnmadd132ssEmitter<T0, T1, T2> {
    fn vfnmadd132ss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMADD213PD`.
///
/// Assembly forms: `vfnmadd213pd xy, xy, xy/mxy`, `vfnmadd213pd xyz, xyz, xyz/mxyz/b64`.
pub trait Vfnmadd213pdEmitter<T0, T1, T2> {
    fn vfnmadd213pd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMADD213PS`.
///
/// Assembly forms: `vfnmadd213ps xy, xy, xy/mxy`, `vfnmadd213ps xyz, xyz, xyz/mxyz/b32`.
pub trait Vfnmadd213psEmitter<T0, T1, T2> {
    fn vfnmadd213ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMADD213SD`.
///
/// Assembly forms: `vfnmadd213sd xmm[63:0], xmm[63:0], xmm[63:0]/m64`.
pub trait Vfnmadd213sdEmitter<T0, T1, T2> {
    fn vfnmadd213sd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMADD213SS`.
///
/// Assembly forms: `vfnmadd213ss xmm[31:0], xmm[31:0], xmm[31:0]/m32`.
pub trait Vfnmadd213ssEmitter<T0, T1, T2> {
    fn vfnmadd213ss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMADD231PD`.
///
/// Assembly forms: `vfnmadd231pd xy, xy, xy/mxy`, `vfnmadd231pd xyz, xyz, xyz/mxyz/b64`.
pub trait Vfnmadd231pdEmitter<T0, T1, T2> {
    fn vfnmadd231pd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMADD231PS`.
///
/// Assembly forms: `vfnmadd231ps xy, xy, xy/mxy`, `vfnmadd231ps xyz, xyz, xyz/mxyz/b32`.
pub trait Vfnmadd231psEmitter<T0, T1, T2> {
    fn vfnmadd231ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMADD231SD`.
///
/// Assembly forms: `vfnmadd231sd xmm[63:0], xmm[63:0], xmm[63:0]/m64`.
pub trait Vfnmadd231sdEmitter<T0, T1, T2> {
    fn vfnmadd231sd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMADD231SS`.
///
/// Assembly forms: `vfnmadd231ss xmm[31:0], xmm[31:0], xmm[31:0]/m32`.
pub trait Vfnmadd231ssEmitter<T0, T1, T2> {
    fn vfnmadd231ss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMSUB132PD`.
///
/// Assembly forms: `vfnmsub132pd xy, xy, xy/mxy`, `vfnmsub132pd xyz, xyz, xyz/mxyz/b64`.
pub trait Vfnmsub132pdEmitter<T0, T1, T2> {
    fn vfnmsub132pd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMSUB132PS`.
///
/// Assembly forms: `vfnmsub132ps xy, xy, xy/mxy`, `vfnmsub132ps xyz, xyz, xyz/mxyz/b32`.
pub trait Vfnmsub132psEmitter<T0, T1, T2> {
    fn vfnmsub132ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMSUB132SD`.
///
/// Assembly forms: `vfnmsub132sd xmm[63:0], xmm[63:0], xmm[63:0]/m64`.
pub trait Vfnmsub132sdEmitter<T0, T1, T2> {
    fn vfnmsub132sd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMSUB132SS`.
///
/// Assembly forms: `vfnmsub132ss xmm[31:0], xmm[31:0], xmm[31:0]/m32`.
pub trait Vfnmsub132ssEmitter<T0, T1, T2> {
    fn vfnmsub132ss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMSUB213PD`.
///
/// Assembly forms: `vfnmsub213pd xy, xy, xy/mxy`, `vfnmsub213pd xyz, xyz, xyz/mxyz/b64`.
pub trait Vfnmsub213pdEmitter<T0, T1, T2> {
    fn vfnmsub213pd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMSUB213PS`.
///
/// Assembly forms: `vfnmsub213ps xy, xy, xy/mxy`, `vfnmsub213ps xyz, xyz, xyz/mxyz/b32`.
pub trait Vfnmsub213psEmitter<T0, T1, T2> {
    fn vfnmsub213ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMSUB213SD`.
///
/// Assembly forms: `vfnmsub213sd xmm[63:0], xmm[63:0], xmm[63:0]/m64`.
pub trait Vfnmsub213sdEmitter<T0, T1, T2> {
    fn vfnmsub213sd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMSUB213SS`.
///
/// Assembly forms: `vfnmsub213ss xmm[31:0], xmm[31:0], xmm[31:0]/m32`.
pub trait Vfnmsub213ssEmitter<T0, T1, T2> {
    fn vfnmsub213ss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMSUB231PD`.
///
/// Assembly forms: `vfnmsub231pd xy, xy, xy/mxy`, `vfnmsub231pd xyz, xyz, xyz/mxyz/b64`.
pub trait Vfnmsub231pdEmitter<T0, T1, T2> {
    fn vfnmsub231pd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMSUB231PS`.
///
/// Assembly forms: `vfnmsub231ps xy, xy, xy/mxy`, `vfnmsub231ps xyz, xyz, xyz/mxyz/b32`.
pub trait Vfnmsub231psEmitter<T0, T1, T2> {
    fn vfnmsub231ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMSUB231SD`.
///
/// Assembly forms: `vfnmsub231sd xmm[63:0], xmm[63:0], xmm[63:0]/m64`.
pub trait Vfnmsub231sdEmitter<T0, T1, T2> {
    fn vfnmsub231sd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMSUB231SS`.
///
/// Assembly forms: `vfnmsub231ss xmm[31:0], xmm[31:0], xmm[31:0]/m32`.
pub trait Vfnmsub231ssEmitter<T0, T1, T2> {
    fn vfnmsub231ss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFPCLASSPD`.
///
/// Assembly forms: `vfpclasspd k, xyz/mxyz/b64, imm8`.
pub trait VfpclasspdEmitter<T0, T1, T2> {
    fn vfpclasspd(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VFPCLASSPS`.
///
/// Assembly forms: `vfpclassps k, xyz/mxyz/b32, imm8`.
pub trait VfpclasspsEmitter<T0, T1, T2> {
    fn vfpclassps(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VFPCLASSSD`.
///
/// Assembly forms: `vfpclasssd k, xmm[63:0]/m64, imm8`.
pub trait VfpclasssdEmitter<T0, T1, T2> {
    fn vfpclasssd(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VFPCLASSSS`.
///
/// Assembly forms: `vfpclassss k, xmm[31:0]/m32, imm8`.
pub trait VfpclassssEmitter<T0, T1, T2> {
    fn vfpclassss(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VGATHERDPD`.
///
/// Assembly forms: `vgatherdpd xmm, vm32x, xmm`, `vgatherdpd ymm, vm32x, ymm`, `vgatherdpd xmm, vm32x`.
pub trait VgatherdpdEmitter<T0, T1> {
    fn vgatherdpd(&mut self, dst: T0, src: T1);
}

/// Emits `VGATHERDPD_3`.
///
/// Assembly forms: `vgatherdpd xmm, vm32x, xmm`, `vgatherdpd ymm, vm32x, ymm`, `vgatherdpd xmm, vm32x`.
pub trait Vgatherdpd3Emitter<T0, T1, T2> {
    fn vgatherdpd_3(&mut self, dst: T0, src: T1, dst2: T2);
}

/// Emits `VGATHERDPS`.
///
/// Assembly forms: `vgatherdps xmm, vm32x, xmm`, `vgatherdps ymm, vm32y, ymm`, `vgatherdps xmm, vm32x`.
pub trait VgatherdpsEmitter<T0, T1> {
    fn vgatherdps(&mut self, dst: T0, src: T1);
}

/// Emits `VGATHERDPS_3`.
///
/// Assembly forms: `vgatherdps xmm, vm32x, xmm`, `vgatherdps ymm, vm32y, ymm`, `vgatherdps xmm, vm32x`.
pub trait Vgatherdps3Emitter<T0, T1, T2> {
    fn vgatherdps_3(&mut self, dst: T0, src: T1, dst2: T2);
}

/// Emits `VGATHERQPD`.
///
/// Assembly forms: `vgatherqpd xmm, vm64x, xmm`, `vgatherqpd ymm, vm64y, ymm`, `vgatherqpd xmm, vm64x`.
pub trait VgatherqpdEmitter<T0, T1> {
    fn vgatherqpd(&mut self, dst: T0, src: T1);
}

/// Emits `VGATHERQPD_3`.
///
/// Assembly forms: `vgatherqpd xmm, vm64x, xmm`, `vgatherqpd ymm, vm64y, ymm`, `vgatherqpd xmm, vm64x`.
pub trait Vgatherqpd3Emitter<T0, T1, T2> {
    fn vgatherqpd_3(&mut self, dst: T0, src: T1, dst2: T2);
}

/// Emits `VGATHERQPS`.
///
/// Assembly forms: `vgatherqps xmm, vm64x, xmm`, `vgatherqps xmm, vm64y, xmm`, `vgatherqps xmm, vm64x`.
pub trait VgatherqpsEmitter<T0, T1> {
    fn vgatherqps(&mut self, dst: T0, src: T1);
}

/// Emits `VGATHERQPS_3`.
///
/// Assembly forms: `vgatherqps xmm, vm64x, xmm`, `vgatherqps xmm, vm64y, xmm`, `vgatherqps xmm, vm64x`.
pub trait Vgatherqps3Emitter<T0, T1, T2> {
    fn vgatherqps_3(&mut self, dst: T0, src: T1, dst2: T2);
}

/// Emits `VGETEXPPD`.
///
/// Assembly forms: `vgetexppd xyz, xyz/mxyz/b64`.
pub trait VgetexppdEmitter<T0, T1> {
    fn vgetexppd(&mut self, dst: T0, src: T1);
}

/// Emits `VGETEXPPS`.
///
/// Assembly forms: `vgetexpps xyz, xyz/mxyz/b32`.
pub trait VgetexppsEmitter<T0, T1> {
    fn vgetexpps(&mut self, dst: T0, src: T1);
}

/// Emits `VGETEXPSD`.
///
/// Assembly forms: `vgetexpsd xmm, xmm[127:64], xmm[63:0]/m64`.
pub trait VgetexpsdEmitter<T0, T1, T2> {
    fn vgetexpsd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VGETEXPSS`.
///
/// Assembly forms: `vgetexpss xmm, xmm[127:32], xmm[31:0]/m32`.
pub trait VgetexpssEmitter<T0, T1, T2> {
    fn vgetexpss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VGETMANTPD`.
///
/// Assembly forms: `vgetmantpd xyz, xyz/mxyz/b64, imm8`.
pub trait VgetmantpdEmitter<T0, T1, T2> {
    fn vgetmantpd(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VGETMANTPS`.
///
/// Assembly forms: `vgetmantps xyz, xyz/mxyz/b32, imm8`.
pub trait VgetmantpsEmitter<T0, T1, T2> {
    fn vgetmantps(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VGETMANTSD`.
///
/// Assembly forms: `vgetmantsd xmm, xmm[127:64], xmm[63:0]/m64, imm8`.
pub trait VgetmantsdEmitter<T0, T1, T2, T3> {
    fn vgetmantsd(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VGETMANTSS`.
///
/// Assembly forms: `vgetmantss xmm, xmm[127:32], xmm[31:0]/m32, imm8`.
pub trait VgetmantssEmitter<T0, T1, T2, T3> {
    fn vgetmantss(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VGF2P8AFFINEINVQB`.
///
/// Assembly forms: `vgf2p8affineinvqb xy, xy, xy/mxy, imm8`, `vgf2p8affineinvqb xyz, xyz, xyz/mxyz, imm8`.
pub trait Vgf2p8affineinvqbEmitter<T0, T1, T2, T3> {
    fn vgf2p8affineinvqb(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VGF2P8AFFINEQB`.
///
/// Assembly forms: `vgf2p8affineqb W:xy, xy, xy/mxy, imm8`, `vgf2p8affineqb xyz, xyz, xyz/mxyz, imm8`.
pub trait Vgf2p8affineqbEmitter<T0, T1, T2, T3> {
    fn vgf2p8affineqb(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VGF2P8MULB`.
///
/// Assembly forms: `vgf2p8mulb W:xy, xy, xy/mxy`, `vgf2p8mulb xyz, xyz, xyz/mxyz`.
pub trait Vgf2p8mulbEmitter<T0, T1, T2> {
    fn vgf2p8mulb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VHADDPD`.
///
/// Assembly forms: `vhaddpd xy, xy, xy/mxy`.
pub trait VhaddpdEmitter<T0, T1, T2> {
    fn vhaddpd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VHADDPS`.
///
/// Assembly forms: `vhaddps xy, xy, xy/mxy`.
pub trait VhaddpsEmitter<T0, T1, T2> {
    fn vhaddps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VHSUBPD`.
///
/// Assembly forms: `vhsubpd xy, xy, xy/mxy`.
pub trait VhsubpdEmitter<T0, T1, T2> {
    fn vhsubpd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VHSUBPS`.
///
/// Assembly forms: `vhsubps xy, xy, xy/mxy`.
pub trait VhsubpsEmitter<T0, T1, T2> {
    fn vhsubps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VINSERTF128`.
///
/// Assembly forms: `vinsertf128 ymm, ymm, xmm/m128, imm8`.
pub trait Vinsertf128Emitter<T0, T1, T2, T3> {
    fn vinsertf128(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VINSERTF32X4`.
///
/// Assembly forms: `vinsertf32x4 ymm, ymm, xmm/m128, imm8`, `vinsertf32x4 zmm, zmm, xmm/m128, imm8`.
pub trait Vinsertf32x4Emitter<T0, T1, T2, T3> {
    fn vinsertf32x4(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VINSERTF32X8`.
///
/// Assembly forms: `vinsertf32x8 zmm, zmm, ymm/m256, imm8`.
pub trait Vinsertf32x8Emitter<T0, T1, T2, T3> {
    fn vinsertf32x8(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VINSERTF64X2`.
///
/// Assembly forms: `vinsertf64x2 ymm, ymm, xmm/m128, imm8`, `vinsertf64x2 zmm, zmm, xmm/m128, imm8`.
pub trait Vinsertf64x2Emitter<T0, T1, T2, T3> {
    fn vinsertf64x2(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VINSERTF64X4`.
///
/// Assembly forms: `vinsertf64x4 zmm, zmm, ymm/m256, imm8`.
pub trait Vinsertf64x4Emitter<T0, T1, T2, T3> {
    fn vinsertf64x4(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VINSERTI128`.
///
/// Assembly forms: `vinserti128 ymm, ymm, xmm/m128, imm8`.
pub trait Vinserti128Emitter<T0, T1, T2, T3> {
    fn vinserti128(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VINSERTI32X4`.
///
/// Assembly forms: `vinserti32x4 ymm, ymm, xmm/m128, imm8`, `vinserti32x4 zmm, zmm, xmm/m128, imm8`.
pub trait Vinserti32x4Emitter<T0, T1, T2, T3> {
    fn vinserti32x4(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VINSERTI32X8`.
///
/// Assembly forms: `vinserti32x8 zmm, zmm, ymm/m256, imm8`.
pub trait Vinserti32x8Emitter<T0, T1, T2, T3> {
    fn vinserti32x8(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VINSERTI64X2`.
///
/// Assembly forms: `vinserti64x2 ymm, ymm, xmm/m128, imm8`, `vinserti64x2 zmm, zmm, xmm/m128, imm8`.
pub trait Vinserti64x2Emitter<T0, T1, T2, T3> {
    fn vinserti64x2(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VINSERTI64X4`.
///
/// Assembly forms: `vinserti64x4 zmm, zmm, ymm/m256, imm8`.
pub trait Vinserti64x4Emitter<T0, T1, T2, T3> {
    fn vinserti64x4(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VINSERTPS`.
///
/// Assembly forms: `vinsertps xmm, xmm, xmm[31:0]/m32, imm8`.
pub trait VinsertpsEmitter<T0, T1, T2, T3> {
    fn vinsertps(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VLDDQU`.
///
/// Assembly forms: `vlddqu xy, mxy`.
pub trait VlddquEmitter<T0, T1> {
    fn vlddqu(&mut self, dst: T0, src: T1);
}

/// Emits `VLDMXCSR`.
///
/// Assembly forms: `vldmxcsr m32`.
pub trait VldmxcsrEmitter<T0> {
    fn vldmxcsr(&mut self, mem: T0);
}

/// Emits `VMASKMOVDQU`.
///
/// Assembly forms: `vmaskmovdqu xmm, xmm, <m128(ds:zdi)>`.
pub trait VmaskmovdquEmitter<T0, T1, T2> {
    fn vmaskmovdqu(&mut self, dst: T0, src: T1, dst2: T2);
}

/// Emits `VMASKMOVPD`.
///
/// Assembly forms: `vmaskmovpd mxy, xy, xy`, `vmaskmovpd xy, xy, mxy`.
pub trait VmaskmovpdEmitter<T0, T1, T2> {
    fn vmaskmovpd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMASKMOVPS`.
///
/// Assembly forms: `vmaskmovps mxy, xy, xy`, `vmaskmovps xy, xy, mxy`.
pub trait VmaskmovpsEmitter<T0, T1, T2> {
    fn vmaskmovps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMAXPD`.
///
/// Assembly forms: `vmaxpd xy, xy, xy/mxy`, `vmaxpd xyz, xyz, xyz/mxyz/b64`.
pub trait VmaxpdEmitter<T0, T1, T2> {
    fn vmaxpd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMAXPS`.
///
/// Assembly forms: `vmaxps xy, xy, xy/mxy`, `vmaxps xyz, xyz, xyz/mxyz/b32`.
pub trait VmaxpsEmitter<T0, T1, T2> {
    fn vmaxps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMAXSD`.
///
/// Assembly forms: `vmaxsd xmm, xmm, xmm[63:0]/m64`.
pub trait VmaxsdEmitter<T0, T1, T2> {
    fn vmaxsd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMAXSS`.
///
/// Assembly forms: `vmaxss xmm, xmm, xmm[31:0]/m32`.
pub trait VmaxssEmitter<T0, T1, T2> {
    fn vmaxss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMINPD`.
///
/// Assembly forms: `vminpd xy, xy, xy/mxy`, `vminpd xyz, xyz, xyz/mxyz/b64`.
pub trait VminpdEmitter<T0, T1, T2> {
    fn vminpd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMINPS`.
///
/// Assembly forms: `vminps xy, xy, xy/mxy`, `vminps xyz, xyz, xyz/mxyz/b32`.
pub trait VminpsEmitter<T0, T1, T2> {
    fn vminps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMINSD`.
///
/// Assembly forms: `vminsd xmm, xmm, xmm[63:0]/m64`.
pub trait VminsdEmitter<T0, T1, T2> {
    fn vminsd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMINSS`.
///
/// Assembly forms: `vminss xmm, xmm, xmm[31:0]/m32`.
pub trait VminssEmitter<T0, T1, T2> {
    fn vminss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMOVAPD`.
///
/// Assembly forms: `vmovapd xy, xy/mxy`, `vmovapd xy/mxy, xy`, `vmovapd xyz, xyz/mxyz`.
pub trait VmovapdEmitter<T0, T1> {
    fn vmovapd(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVAPS`.
///
/// Assembly forms: `vmovaps xy, xy/mxy`, `vmovaps xy/mxy, xy`, `vmovaps xyz, xyz/mxyz`.
pub trait VmovapsEmitter<T0, T1> {
    fn vmovaps(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVD`.
///
/// Assembly forms: `vmovd r32[31:0]/m32, xmm[31:0]`, `vmovd xmm[31:0], r32[31:0]/m32`, `vmovd r32/m32, xmm[31:0]`.
pub trait VmovdEmitter<T0, T1> {
    fn vmovd(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVDDUP`.
///
/// Assembly forms: `vmovddup xmm, xmm[63:0]/m64`, `vmovddup ymm, ymm/m256`, `vmovddup zmm, zmm/m512`.
pub trait VmovddupEmitter<T0, T1> {
    fn vmovddup(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVDQA`.
///
/// Assembly forms: `vmovdqa xy, xy/mxy`, `vmovdqa xy/mxy, xy`.
pub trait VmovdqaEmitter<T0, T1> {
    fn vmovdqa(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVDQA32`.
///
/// Assembly forms: `vmovdqa32 xyz, xyz/mxyz`, `vmovdqa32 xyz/mxyz, xyz`.
pub trait Vmovdqa32Emitter<T0, T1> {
    fn vmovdqa32(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVDQA64`.
///
/// Assembly forms: `vmovdqa64 xyz, xyz/mxyz`, `vmovdqa64 xyz/mxyz, xyz`.
pub trait Vmovdqa64Emitter<T0, T1> {
    fn vmovdqa64(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVDQU`.
///
/// Assembly forms: `vmovdqu xy, xy/mxy`, `vmovdqu xy/mxy, xy`.
pub trait VmovdquEmitter<T0, T1> {
    fn vmovdqu(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVDQU16`.
///
/// Assembly forms: `vmovdqu16 xyz, xyz/mxyz`, `vmovdqu16 xyz/mxyz, xyz`.
pub trait Vmovdqu16Emitter<T0, T1> {
    fn vmovdqu16(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVDQU32`.
///
/// Assembly forms: `vmovdqu32 xyz, xyz/mxyz`, `vmovdqu32 xyz/mxyz, xyz`.
pub trait Vmovdqu32Emitter<T0, T1> {
    fn vmovdqu32(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVDQU64`.
///
/// Assembly forms: `vmovdqu64 xyz, xyz/mxyz`, `vmovdqu64 xyz/mxyz, xyz`.
pub trait Vmovdqu64Emitter<T0, T1> {
    fn vmovdqu64(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVDQU8`.
///
/// Assembly forms: `vmovdqu8 xyz, xyz/mxyz`, `vmovdqu8 xyz/mxyz, xyz`.
pub trait Vmovdqu8Emitter<T0, T1> {
    fn vmovdqu8(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVHLPS`.
///
/// Assembly forms: `vmovhlps xmm, xmm[127:64], xmm[127:64]`.
pub trait VmovhlpsEmitter<T0, T1, T2> {
    fn vmovhlps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMOVHPD`.
///
/// Assembly forms: `vmovhpd m64, xmm[127:64]`, `vmovhpd xmm, xmm[63:0], m64`.
pub trait VmovhpdEmitter<T0, T1> {
    fn vmovhpd(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVHPD_3`.
///
/// Assembly forms: `vmovhpd m64, xmm[127:64]`, `vmovhpd xmm, xmm[63:0], m64`.
pub trait Vmovhpd3Emitter<T0, T1, T2> {
    fn vmovhpd_3(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMOVHPS`.
///
/// Assembly forms: `vmovhps m64, xmm[127:64]`, `vmovhps xmm, xmm[63:0], m64`.
pub trait VmovhpsEmitter<T0, T1> {
    fn vmovhps(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVHPS_3`.
///
/// Assembly forms: `vmovhps m64, xmm[127:64]`, `vmovhps xmm, xmm[63:0], m64`.
pub trait Vmovhps3Emitter<T0, T1, T2> {
    fn vmovhps_3(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMOVLHPS`.
///
/// Assembly forms: `vmovlhps xmm, xmm[63:0], xmm[63:0]`.
pub trait VmovlhpsEmitter<T0, T1, T2> {
    fn vmovlhps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMOVLPD`.
///
/// Assembly forms: `vmovlpd m64, xmm[63:0]`, `vmovlpd xmm, xmm[127:64], m64`.
pub trait VmovlpdEmitter<T0, T1> {
    fn vmovlpd(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVLPD_3`.
///
/// Assembly forms: `vmovlpd m64, xmm[63:0]`, `vmovlpd xmm, xmm[127:64], m64`.
pub trait Vmovlpd3Emitter<T0, T1, T2> {
    fn vmovlpd_3(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMOVLPS`.
///
/// Assembly forms: `vmovlps m64, xmm[63:0]`, `vmovlps xmm, xmm[127:64], m64`.
pub trait VmovlpsEmitter<T0, T1> {
    fn vmovlps(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVLPS_3`.
///
/// Assembly forms: `vmovlps m64, xmm[63:0]`, `vmovlps xmm, xmm[127:64], m64`.
pub trait Vmovlps3Emitter<T0, T1, T2> {
    fn vmovlps_3(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMOVMSKPD`.
///
/// Assembly forms: `vmovmskpd r32[1:0], xmm`, `vmovmskpd r32[3:0], ymm`.
pub trait VmovmskpdEmitter<T0, T1> {
    fn vmovmskpd(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVMSKPS`.
///
/// Assembly forms: `vmovmskps r32[3:0], xmm`, `vmovmskps r32[7:0], ymm`.
pub trait VmovmskpsEmitter<T0, T1> {
    fn vmovmskps(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVNTDQ`.
///
/// Assembly forms: `vmovntdq mxy, xy`, `vmovntdq mxyz, xyz`.
pub trait VmovntdqEmitter<T0, T1> {
    fn vmovntdq(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVNTDQA`.
///
/// Assembly forms: `vmovntdqa xmm, m128`, `vmovntdqa ymm, m256`, `vmovntdqa xyz, mxyz`.
pub trait VmovntdqaEmitter<T0, T1> {
    fn vmovntdqa(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVNTPD`.
///
/// Assembly forms: `vmovntpd mxy, xy`, `vmovntpd mxyz, xyz`.
pub trait VmovntpdEmitter<T0, T1> {
    fn vmovntpd(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVNTPS`.
///
/// Assembly forms: `vmovntps mxy, xy`, `vmovntps mxyz, xyz`.
pub trait VmovntpsEmitter<T0, T1> {
    fn vmovntps(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVQ`.
///
/// Assembly forms: `vmovq r64/m64, xmm[63:0]`, `vmovq xmm[63:0], xmm[63:0]/m64`, `vmovq xmm[63:0], r64/m64`.
pub trait VmovqEmitter<T0, T1> {
    fn vmovq(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVSD`.
///
/// Assembly forms: `vmovsd m64, xmm[63:0]`, `vmovsd xmm[63:0], m64`, `vmovsd xmm, xmm[127:64], xmm[63:0]`.
pub trait VmovsdEmitter<T0, T1> {
    fn vmovsd(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVSD_3`.
///
/// Assembly forms: `vmovsd m64, xmm[63:0]`, `vmovsd xmm[63:0], m64`, `vmovsd xmm, xmm[127:64], xmm[63:0]`.
pub trait Vmovsd3Emitter<T0, T1, T2> {
    fn vmovsd_3(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMOVSHDUP`.
///
/// Assembly forms: `vmovshdup xy, xy/mxy`, `vmovshdup xyz, xyz/mxyz`.
pub trait VmovshdupEmitter<T0, T1> {
    fn vmovshdup(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVSLDUP`.
///
/// Assembly forms: `vmovsldup xy, xy/mxy`, `vmovsldup xyz, xyz/mxyz`.
pub trait VmovsldupEmitter<T0, T1> {
    fn vmovsldup(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVSS`.
///
/// Assembly forms: `vmovss m32, xmm[31:0]`, `vmovss xmm[31:0], m32`, `vmovss xmm, xmm[127:32], xmm[31:0]`.
pub trait VmovssEmitter<T0, T1> {
    fn vmovss(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVSS_3`.
///
/// Assembly forms: `vmovss m32, xmm[31:0]`, `vmovss xmm[31:0], m32`, `vmovss xmm, xmm[127:32], xmm[31:0]`.
pub trait Vmovss3Emitter<T0, T1, T2> {
    fn vmovss_3(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMOVUPD`.
///
/// Assembly forms: `vmovupd xy, xy/mxy`, `vmovupd xy/mxy, xy`, `vmovupd xyz, xyz/mxyz`.
pub trait VmovupdEmitter<T0, T1> {
    fn vmovupd(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVUPS`.
///
/// Assembly forms: `vmovups xy, xy/mxy`, `vmovups xy/mxy, xy`, `vmovups xyz, xyz/mxyz`.
pub trait VmovupsEmitter<T0, T1> {
    fn vmovups(&mut self, dst: T0, src: T1);
}

/// Emits `VMPSADBW`.
///
/// Assembly forms: `vmpsadbw xmm, xmm, xmm/m128, imm8`, `vmpsadbw ymm, ymm, ymm/m256, imm8`, `vmpsadbw W:xyz, xyz, xyz/mxyz, imm8`.
pub trait VmpsadbwEmitter<T0, T1, T2, T3> {
    fn vmpsadbw(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VMULPD`.
///
/// Assembly forms: `vmulpd xy, xy, xy/mxy`, `vmulpd xyz, xyz, xyz/mxyz/b64`.
pub trait VmulpdEmitter<T0, T1, T2> {
    fn vmulpd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMULPS`.
///
/// Assembly forms: `vmulps xy, xy, xy/mxy`, `vmulps xyz, xyz, xyz/mxyz/b32`.
pub trait VmulpsEmitter<T0, T1, T2> {
    fn vmulps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMULSD`.
///
/// Assembly forms: `vmulsd xmm, xmm, xmm[63:0]/m64`.
pub trait VmulsdEmitter<T0, T1, T2> {
    fn vmulsd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMULSS`.
///
/// Assembly forms: `vmulss xmm, xmm, xmm[31:0]/m32`.
pub trait VmulssEmitter<T0, T1, T2> {
    fn vmulss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VORPD`.
///
/// Assembly forms: `vorpd xy, xy, xy/mxy`, `vorpd xyz, xyz, xyz/mxyz/b64`.
pub trait VorpdEmitter<T0, T1, T2> {
    fn vorpd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VORPS`.
///
/// Assembly forms: `vorps xy, xy, xy/mxy`, `vorps xyz, xyz, xyz/mxyz/b32`.
pub trait VorpsEmitter<T0, T1, T2> {
    fn vorps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VP2INTERSECTD`.
///
/// Assembly forms: `vp2intersectd k, k+1, xyz, xyz/mxyz/b32`.
pub trait Vp2intersectdEmitter<T0, T1, T2, T3> {
    fn vp2intersectd(&mut self, dst: T0, dst2: T1, src1: T2, src2: T3);
}

/// Emits `VP2INTERSECTQ`.
///
/// Assembly forms: `vp2intersectq k, k+1, xyz, xyz/mxyz/b64`.
pub trait Vp2intersectqEmitter<T0, T1, T2, T3> {
    fn vp2intersectq(&mut self, dst: T0, dst2: T1, src1: T2, src2: T3);
}

/// Emits `VPABSB`.
///
/// Assembly forms: `vpabsb xmm, xmm/m128`, `vpabsb ymm, ymm/m256`, `vpabsb xyz, xyz/mxyz`.
pub trait VpabsbEmitter<T0, T1> {
    fn vpabsb(&mut self, dst: T0, src: T1);
}

/// Emits `VPABSD`.
///
/// Assembly forms: `vpabsd xmm, xmm/m128`, `vpabsd ymm, ymm/m256`, `vpabsd xyz, xyz/mxyz/b32`.
pub trait VpabsdEmitter<T0, T1> {
    fn vpabsd(&mut self, dst: T0, src: T1);
}

/// Emits `VPABSQ`.
///
/// Assembly forms: `vpabsq xyz, xyz/mxyz/b64`.
pub trait VpabsqEmitter<T0, T1> {
    fn vpabsq(&mut self, dst: T0, src: T1);
}

/// Emits `VPABSW`.
///
/// Assembly forms: `vpabsw xmm, xmm/m128`, `vpabsw ymm, ymm/m256`, `vpabsw xyz, xyz/mxyz`.
pub trait VpabswEmitter<T0, T1> {
    fn vpabsw(&mut self, dst: T0, src: T1);
}

/// Emits `VPACKSSDW`.
///
/// Assembly forms: `vpackssdw xmm, xmm, xmm/m128`, `vpackssdw ymm, ymm, ymm/m256`, `vpackssdw xyz, xyz, xyz/mxyz/b32`.
pub trait VpackssdwEmitter<T0, T1, T2> {
    fn vpackssdw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPACKSSWB`.
///
/// Assembly forms: `vpacksswb xmm, xmm, xmm/m128`, `vpacksswb ymm, ymm, ymm/m256`, `vpacksswb xyz, xyz, xyz/mxyz`.
pub trait VpacksswbEmitter<T0, T1, T2> {
    fn vpacksswb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPACKUSDW`.
///
/// Assembly forms: `vpackusdw xmm, xmm, xmm/m128`, `vpackusdw ymm, ymm, ymm/m256`, `vpackusdw xyz, xyz, xyz/mxyz/b32`.
pub trait VpackusdwEmitter<T0, T1, T2> {
    fn vpackusdw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPACKUSWB`.
///
/// Assembly forms: `vpackuswb xmm, xmm, xmm/m128`, `vpackuswb ymm, ymm, ymm/m256`, `vpackuswb xyz, xyz, xyz/mxyz`.
pub trait VpackuswbEmitter<T0, T1, T2> {
    fn vpackuswb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPADDB`.
///
/// Assembly forms: `vpaddb xmm, xmm, xmm/m128`, `vpaddb ymm, ymm, ymm/m256`, `vpaddb xyz, xyz, xyz/mxyz`.
pub trait VpaddbEmitter<T0, T1, T2> {
    fn vpaddb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPADDD`.
///
/// Assembly forms: `vpaddd xmm, xmm, xmm/m128`, `vpaddd ymm, ymm, ymm/m256`, `vpaddd xyz, xyz, xyz/mxyz/b32`.
pub trait VpadddEmitter<T0, T1, T2> {
    fn vpaddd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPADDQ`.
///
/// Assembly forms: `vpaddq xmm, xmm, xmm/m128`, `vpaddq ymm, ymm, ymm/m256`, `vpaddq xyz, xyz, xyz/mxyz/b64`.
pub trait VpaddqEmitter<T0, T1, T2> {
    fn vpaddq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPADDSB`.
///
/// Assembly forms: `vpaddsb xmm, xmm, xmm/m128`, `vpaddsb ymm, ymm, ymm/m256`, `vpaddsb xyz, xyz, xyz/mxyz`.
pub trait VpaddsbEmitter<T0, T1, T2> {
    fn vpaddsb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPADDSW`.
///
/// Assembly forms: `vpaddsw xmm, xmm, xmm/m128`, `vpaddsw ymm, ymm, ymm/m256`, `vpaddsw xyz, xyz, xyz/mxyz`.
pub trait VpaddswEmitter<T0, T1, T2> {
    fn vpaddsw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPADDUSB`.
///
/// Assembly forms: `vpaddusb xmm, xmm, xmm/m128`, `vpaddusb ymm, ymm, ymm/m256`, `vpaddusb xyz, xyz, xyz/mxyz`.
pub trait VpaddusbEmitter<T0, T1, T2> {
    fn vpaddusb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPADDUSW`.
///
/// Assembly forms: `vpaddusw xmm, xmm, xmm/m128`, `vpaddusw ymm, ymm, ymm/m256`, `vpaddusw xyz, xyz, xyz/mxyz`.
pub trait VpadduswEmitter<T0, T1, T2> {
    fn vpaddusw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPADDW`.
///
/// Assembly forms: `vpaddw xmm, xmm, xmm/m128`, `vpaddw ymm, ymm, ymm/m256`, `vpaddw xyz, xyz, xyz/mxyz`.
pub trait VpaddwEmitter<T0, T1, T2> {
    fn vpaddw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPALIGNR`.
///
/// Assembly forms: `vpalignr xmm, xmm, xmm/m128, imm8`, `vpalignr ymm, ymm, ymm/m256, imm8`, `vpalignr xyz, xyz, xyz/mxyz, imm8`.
pub trait VpalignrEmitter<T0, T1, T2, T3> {
    fn vpalignr(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPAND`.
///
/// Assembly forms: `vpand xmm, xmm, xmm/m128`, `vpand ymm, ymm, ymm/m256`.
pub trait VpandEmitter<T0, T1, T2> {
    fn vpand(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPANDD`.
///
/// Assembly forms: `vpandd xyz, xyz, xyz/mxyz/b32`.
pub trait VpanddEmitter<T0, T1, T2> {
    fn vpandd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPANDN`.
///
/// Assembly forms: `vpandn xmm, xmm, xmm/m128`, `vpandn ymm, ymm, ymm/m256`.
pub trait VpandnEmitter<T0, T1, T2> {
    fn vpandn(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPANDND`.
///
/// Assembly forms: `vpandnd xyz, xyz, xyz/mxyz/b32`.
pub trait VpandndEmitter<T0, T1, T2> {
    fn vpandnd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPANDNQ`.
///
/// Assembly forms: `vpandnq xyz, xyz, xyz/mxyz/b64`.
pub trait VpandnqEmitter<T0, T1, T2> {
    fn vpandnq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPANDQ`.
///
/// Assembly forms: `vpandq xyz, xyz, xyz/mxyz/b64`.
pub trait VpandqEmitter<T0, T1, T2> {
    fn vpandq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPAVGB`.
///
/// Assembly forms: `vpavgb xmm, xmm, xmm/m128`, `vpavgb ymm, ymm, ymm/m256`, `vpavgb xyz, xyz, xyz/mxyz`.
pub trait VpavgbEmitter<T0, T1, T2> {
    fn vpavgb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPAVGW`.
///
/// Assembly forms: `vpavgw xmm, xmm, xmm/m128`, `vpavgw ymm, ymm, ymm/m256`, `vpavgw xyz, xyz, xyz/mxyz`.
pub trait VpavgwEmitter<T0, T1, T2> {
    fn vpavgw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPBLENDD`.
///
/// Assembly forms: `vpblendd xmm, xmm, xmm/m128, imm8`, `vpblendd ymm, ymm, ymm/m256, imm8`.
pub trait VpblenddEmitter<T0, T1, T2, T3> {
    fn vpblendd(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPBLENDMB`.
///
/// Assembly forms: `vpblendmb xyz, xyz, xyz/mxyz`.
pub trait VpblendmbEmitter<T0, T1, T2> {
    fn vpblendmb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPBLENDMD`.
///
/// Assembly forms: `vpblendmd xyz, xyz, xyz/mxyz/b32`.
pub trait VpblendmdEmitter<T0, T1, T2> {
    fn vpblendmd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPBLENDMQ`.
///
/// Assembly forms: `vpblendmq xyz, xyz, xyz/mxyz/b64`.
pub trait VpblendmqEmitter<T0, T1, T2> {
    fn vpblendmq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPBLENDMW`.
///
/// Assembly forms: `vpblendmw xyz, xyz, xyz/mxyz`.
pub trait VpblendmwEmitter<T0, T1, T2> {
    fn vpblendmw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPBLENDVB`.
///
/// Assembly forms: `vpblendvb xmm, xmm, xmm/m128, xmm`, `vpblendvb ymm, ymm, ymm/m256, ymm`.
pub trait VpblendvbEmitter<T0, T1, T2, T3> {
    fn vpblendvb(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VPBLENDW`.
///
/// Assembly forms: `vpblendw xmm, xmm, xmm/m128, imm8`, `vpblendw ymm, ymm, ymm/m256, imm8`.
pub trait VpblendwEmitter<T0, T1, T2, T3> {
    fn vpblendw(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPBROADCASTB`.
///
/// Assembly forms: `vpbroadcastb xmm, xmm[7:0]/m8`, `vpbroadcastb ymm, xmm[7:0]/m8`, `vpbroadcastb xyz, r32[7:0]`.
pub trait VpbroadcastbEmitter<T0, T1> {
    fn vpbroadcastb(&mut self, dst: T0, src: T1);
}

/// Emits `VPBROADCASTD`.
///
/// Assembly forms: `vpbroadcastd xmm, xmm[31:0]/m32`, `vpbroadcastd ymm, xmm[31:0]/m32`, `vpbroadcastd xyz, r32[31:0]`.
pub trait VpbroadcastdEmitter<T0, T1> {
    fn vpbroadcastd(&mut self, dst: T0, src: T1);
}

/// Emits `VPBROADCASTMB2Q`.
///
/// Assembly forms: `vpbroadcastmb2q xyz, k`.
pub trait Vpbroadcastmb2qEmitter<T0, T1> {
    fn vpbroadcastmb2q(&mut self, dst: T0, src: T1);
}

/// Emits `VPBROADCASTMW2D`.
///
/// Assembly forms: `vpbroadcastmw2d xyz, k`.
pub trait Vpbroadcastmw2dEmitter<T0, T1> {
    fn vpbroadcastmw2d(&mut self, dst: T0, src: T1);
}

/// Emits `VPBROADCASTQ`.
///
/// Assembly forms: `vpbroadcastq xmm, xmm[63:0]/m64`, `vpbroadcastq ymm, xmm[63:0]/m64`, `vpbroadcastq xyz, r64`.
pub trait VpbroadcastqEmitter<T0, T1> {
    fn vpbroadcastq(&mut self, dst: T0, src: T1);
}

/// Emits `VPBROADCASTW`.
///
/// Assembly forms: `vpbroadcastw xmm, xmm[15:0]/m16`, `vpbroadcastw ymm, xmm[15:0]/m16`, `vpbroadcastw xyz, r32[15:0]`.
pub trait VpbroadcastwEmitter<T0, T1> {
    fn vpbroadcastw(&mut self, dst: T0, src: T1);
}

/// Emits `VPCLMULQDQ`.
///
/// Assembly forms: `vpclmulqdq xmm, xmm, xmm/m128, imm8`, `vpclmulqdq ymm, ymm, ymm/m256, imm8`, `vpclmulqdq xyz, xyz, xyz/mxyz, imm8`.
pub trait VpclmulqdqEmitter<T0, T1, T2, T3> {
    fn vpclmulqdq(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPCMPB`.
///
/// Assembly forms: `vpcmpb k, xyz, xyz/mxyz, imm8`.
pub trait VpcmpbEmitter<T0, T1, T2, T3> {
    fn vpcmpb(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPCMPD`.
///
/// Assembly forms: `vpcmpd k, xyz, xyz/mxyz/b32, imm8`.
pub trait VpcmpdEmitter<T0, T1, T2, T3> {
    fn vpcmpd(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPCMPEQB`.
///
/// Assembly forms: `vpcmpeqb xmm, xmm, xmm/m128`, `vpcmpeqb ymm, ymm, ymm/m256`, `vpcmpeqb k, xyz, xyz/mxyz`.
pub trait VpcmpeqbEmitter<T0, T1, T2> {
    fn vpcmpeqb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPCMPEQD`.
///
/// Assembly forms: `vpcmpeqd xmm, xmm, xmm/m128`, `vpcmpeqd ymm, ymm, ymm/m256`, `vpcmpeqd k, xyz, xyz/mxyz/b32`.
pub trait VpcmpeqdEmitter<T0, T1, T2> {
    fn vpcmpeqd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPCMPEQQ`.
///
/// Assembly forms: `vpcmpeqq xmm, xmm, xmm/m128`, `vpcmpeqq ymm, ymm, ymm/m256`, `vpcmpeqq k, xyz, xyz/mxyz/b64`.
pub trait VpcmpeqqEmitter<T0, T1, T2> {
    fn vpcmpeqq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPCMPEQW`.
///
/// Assembly forms: `vpcmpeqw xmm, xmm, xmm/m128`, `vpcmpeqw ymm, ymm, ymm/m256`, `vpcmpeqw k, xyz, xyz/mxyz`.
pub trait VpcmpeqwEmitter<T0, T1, T2> {
    fn vpcmpeqw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPCMPESTRI`.
///
/// Assembly forms: `vpcmpestri xmm, xmm/m128, imm8, <ecx>, <eax>, <edx>`.
pub trait VpcmpestriEmitter<T0, T1, T2, T3, T4, T5> {
    fn vpcmpestri(&mut self, dst: T0, src1: T1, imm: T2, dst2: T3, src2: T4, src3: T5);
}

/// Emits `VPCMPESTRM`.
///
/// Assembly forms: `vpcmpestrm xmm, xmm/m128, imm8, <xmm0>, <eax>, <edx>`.
pub trait VpcmpestrmEmitter<T0, T1, T2, T3, T4, T5> {
    fn vpcmpestrm(&mut self, dst: T0, src1: T1, imm: T2, dst2: T3, src2: T4, src3: T5);
}

/// Emits `VPCMPGTB`.
///
/// Assembly forms: `vpcmpgtb xmm, xmm, xmm/m128`, `vpcmpgtb ymm, ymm, ymm/m256`, `vpcmpgtb k, xyz, xyz/mxyz`.
pub trait VpcmpgtbEmitter<T0, T1, T2> {
    fn vpcmpgtb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPCMPGTD`.
///
/// Assembly forms: `vpcmpgtd xmm, xmm, xmm/m128`, `vpcmpgtd ymm, ymm, ymm/m256`, `vpcmpgtd k, xyz, xyz/mxyz/b32`.
pub trait VpcmpgtdEmitter<T0, T1, T2> {
    fn vpcmpgtd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPCMPGTQ`.
///
/// Assembly forms: `vpcmpgtq xmm, xmm, xmm/m128`, `vpcmpgtq ymm, ymm, ymm/m256`, `vpcmpgtq k, xyz, xyz/mxyz/b64`.
pub trait VpcmpgtqEmitter<T0, T1, T2> {
    fn vpcmpgtq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPCMPGTW`.
///
/// Assembly forms: `vpcmpgtw xmm, xmm, xmm/m128`, `vpcmpgtw ymm, ymm, ymm/m256`, `vpcmpgtw k, xyz, xyz/mxyz`.
pub trait VpcmpgtwEmitter<T0, T1, T2> {
    fn vpcmpgtw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPCMPISTRI`.
///
/// Assembly forms: `vpcmpistri xmm, xmm/m128, imm8, <ecx>`.
pub trait VpcmpistriEmitter<T0, T1, T2, T3> {
    fn vpcmpistri(&mut self, dst: T0, src: T1, imm: T2, dst2: T3);
}

/// Emits `VPCMPISTRM`.
///
/// Assembly forms: `vpcmpistrm xmm, xmm/m128, imm8, <xmm0>`.
pub trait VpcmpistrmEmitter<T0, T1, T2, T3> {
    fn vpcmpistrm(&mut self, dst: T0, src: T1, imm: T2, dst2: T3);
}

/// Emits `VPCMPQ`.
///
/// Assembly forms: `vpcmpq k, xyz, xyz/mxyz/b64, imm8`.
pub trait VpcmpqEmitter<T0, T1, T2, T3> {
    fn vpcmpq(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPCMPUB`.
///
/// Assembly forms: `vpcmpub k, xyz, xyz/mxyz, imm8`.
pub trait VpcmpubEmitter<T0, T1, T2, T3> {
    fn vpcmpub(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPCMPUD`.
///
/// Assembly forms: `vpcmpud k, xyz, xyz/mxyz/b32, imm8`.
pub trait VpcmpudEmitter<T0, T1, T2, T3> {
    fn vpcmpud(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPCMPUQ`.
///
/// Assembly forms: `vpcmpuq k, xyz, xyz/mxyz/b64, imm8`.
pub trait VpcmpuqEmitter<T0, T1, T2, T3> {
    fn vpcmpuq(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPCMPUW`.
///
/// Assembly forms: `vpcmpuw k, xyz, xyz/mxyz, imm8`.
pub trait VpcmpuwEmitter<T0, T1, T2, T3> {
    fn vpcmpuw(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPCMPW`.
///
/// Assembly forms: `vpcmpw k, xyz, xyz/mxyz, imm8`.
pub trait VpcmpwEmitter<T0, T1, T2, T3> {
    fn vpcmpw(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPCOMPRESSB`.
///
/// Assembly forms: `vpcompressb xyz/mxyz, xyz`.
pub trait VpcompressbEmitter<T0, T1> {
    fn vpcompressb(&mut self, dst: T0, src: T1);
}

/// Emits `VPCOMPRESSD`.
///
/// Assembly forms: `vpcompressd xyz/mxyz, xyz`.
pub trait VpcompressdEmitter<T0, T1> {
    fn vpcompressd(&mut self, dst: T0, src: T1);
}

/// Emits `VPCOMPRESSQ`.
///
/// Assembly forms: `vpcompressq xyz/mxyz, xyz`.
pub trait VpcompressqEmitter<T0, T1> {
    fn vpcompressq(&mut self, dst: T0, src: T1);
}

/// Emits `VPCOMPRESSW`.
///
/// Assembly forms: `vpcompressw xyz/mxyz, xyz`.
pub trait VpcompresswEmitter<T0, T1> {
    fn vpcompressw(&mut self, dst: T0, src: T1);
}

/// Emits `VPCONFLICTD`.
///
/// Assembly forms: `vpconflictd xyz, xyz/mxyz/b32`.
pub trait VpconflictdEmitter<T0, T1> {
    fn vpconflictd(&mut self, dst: T0, src: T1);
}

/// Emits `VPCONFLICTQ`.
///
/// Assembly forms: `vpconflictq xyz, xyz/mxyz/b32`.
pub trait VpconflictqEmitter<T0, T1> {
    fn vpconflictq(&mut self, dst: T0, src: T1);
}

/// Emits `VPDPBUSD`.
///
/// Assembly forms: `vpdpbusd X:xy, xy, xy/mxy`, `vpdpbusd xyz, xyz, xyz/mxyz/b32`.
pub trait VpdpbusdEmitter<T0, T1, T2> {
    fn vpdpbusd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPDPBUSDS`.
///
/// Assembly forms: `vpdpbusds xy, xy, xy/mxy`, `vpdpbusds xyz, xyz, xyz/mxyz/b32`.
pub trait VpdpbusdsEmitter<T0, T1, T2> {
    fn vpdpbusds(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPDPWSSD`.
///
/// Assembly forms: `vpdpwssd X:xy, xy, xy/mxy`, `vpdpwssd xyz, xyz, xyz/mxyz/b32`.
pub trait VpdpwssdEmitter<T0, T1, T2> {
    fn vpdpwssd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPDPWSSDS`.
///
/// Assembly forms: `vpdpwssds xy, xy, xy/mxy`, `vpdpwssds xyz, xyz, xyz/mxyz/b32`.
pub trait VpdpwssdsEmitter<T0, T1, T2> {
    fn vpdpwssds(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPERM2F128`.
///
/// Assembly forms: `vperm2f128 ymm, ymm, ymm/m256, imm8`.
pub trait Vperm2f128Emitter<T0, T1, T2, T3> {
    fn vperm2f128(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPERM2I128`.
///
/// Assembly forms: `vperm2i128 ymm, ymm, ymm/m256, imm8`.
pub trait Vperm2i128Emitter<T0, T1, T2, T3> {
    fn vperm2i128(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPERMB`.
///
/// Assembly forms: `vpermb xyz, xyz, xyz/mxyz`.
pub trait VpermbEmitter<T0, T1, T2> {
    fn vpermb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPERMD`.
///
/// Assembly forms: `vpermd ymm, ymm, ymm/m256`, `vpermd ymm, ymm, ymm/m256/b32`, `vpermd zmm, zmm, zmm/m512/b32`.
pub trait VpermdEmitter<T0, T1, T2> {
    fn vpermd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPERMI2B`.
///
/// Assembly forms: `vpermi2b xyz, xyz, xyz/mxyz`.
pub trait Vpermi2bEmitter<T0, T1, T2> {
    fn vpermi2b(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPERMI2D`.
///
/// Assembly forms: `vpermi2d xyz, xyz, xyz/mxyz/b32`.
pub trait Vpermi2dEmitter<T0, T1, T2> {
    fn vpermi2d(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPERMI2PD`.
///
/// Assembly forms: `vpermi2pd xyz, xyz, xyz/mxyz/b64`.
pub trait Vpermi2pdEmitter<T0, T1, T2> {
    fn vpermi2pd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPERMI2PS`.
///
/// Assembly forms: `vpermi2ps xyz, xyz, xyz/mxyz/b32`.
pub trait Vpermi2psEmitter<T0, T1, T2> {
    fn vpermi2ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPERMI2Q`.
///
/// Assembly forms: `vpermi2q xyz, xyz, xyz/mxyz/b64`.
pub trait Vpermi2qEmitter<T0, T1, T2> {
    fn vpermi2q(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPERMI2W`.
///
/// Assembly forms: `vpermi2w xyz, xyz, xyz/mxyz`.
pub trait Vpermi2wEmitter<T0, T1, T2> {
    fn vpermi2w(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPERMILPD`.
///
/// Assembly forms: `vpermilpd xmm, xmm, xmm/m128`, `vpermilpd xmm, xmm/m128, imm8`, `vpermilpd ymm, ymm, ymm/m256`.
pub trait VpermilpdEmitter<T0, T1, T2> {
    fn vpermilpd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPERMILPS`.
///
/// Assembly forms: `vpermilps xmm, xmm, xmm/m128`, `vpermilps xmm, xmm/m128, imm8`, `vpermilps ymm, ymm, ymm/m256`.
pub trait VpermilpsEmitter<T0, T1, T2> {
    fn vpermilps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPERMPD`.
///
/// Assembly forms: `vpermpd ymm, ymm/m256, imm8`, `vpermpd ymm, ymm, ymm/m256/b64`, `vpermpd zmm, zmm, zmm/m512/b64`.
pub trait VpermpdEmitter<T0, T1, T2> {
    fn vpermpd(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPERMPS`.
///
/// Assembly forms: `vpermps ymm, ymm, ymm/m256`, `vpermps ymm, ymm, ymm/m256/b32`, `vpermps zmm, zmm, zmm/m512/b32`.
pub trait VpermpsEmitter<T0, T1, T2> {
    fn vpermps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPERMQ`.
///
/// Assembly forms: `vpermq ymm, ymm/m256, imm8`, `vpermq ymm, ymm, ymm/m256/b64`, `vpermq zmm, zmm, zmm/m512/b64`.
pub trait VpermqEmitter<T0, T1, T2> {
    fn vpermq(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPERMT2B`.
///
/// Assembly forms: `vpermt2b xyz, xyz, xyz/mxyz`.
pub trait Vpermt2bEmitter<T0, T1, T2> {
    fn vpermt2b(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPERMT2D`.
///
/// Assembly forms: `vpermt2d xyz, xyz, xyz/mxyz/b32`.
pub trait Vpermt2dEmitter<T0, T1, T2> {
    fn vpermt2d(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPERMT2PD`.
///
/// Assembly forms: `vpermt2pd xyz, xyz, xyz/mxyz/b64`.
pub trait Vpermt2pdEmitter<T0, T1, T2> {
    fn vpermt2pd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPERMT2PS`.
///
/// Assembly forms: `vpermt2ps xyz, xyz, xyz/mxyz/b32`.
pub trait Vpermt2psEmitter<T0, T1, T2> {
    fn vpermt2ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPERMT2Q`.
///
/// Assembly forms: `vpermt2q xyz, xyz, xyz/mxyz/b64`.
pub trait Vpermt2qEmitter<T0, T1, T2> {
    fn vpermt2q(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPERMT2W`.
///
/// Assembly forms: `vpermt2w xyz, xyz, xyz/mxyz`.
pub trait Vpermt2wEmitter<T0, T1, T2> {
    fn vpermt2w(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPERMW`.
///
/// Assembly forms: `vpermw xyz, xyz, xyz/mxyz`.
pub trait VpermwEmitter<T0, T1, T2> {
    fn vpermw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPEXPANDB`.
///
/// Assembly forms: `vpexpandb xyz, xyz/mxyz`.
pub trait VpexpandbEmitter<T0, T1> {
    fn vpexpandb(&mut self, dst: T0, src: T1);
}

/// Emits `VPEXPANDD`.
///
/// Assembly forms: `vpexpandd xyz, xyz/mxyz`.
pub trait VpexpanddEmitter<T0, T1> {
    fn vpexpandd(&mut self, dst: T0, src: T1);
}

/// Emits `VPEXPANDQ`.
///
/// Assembly forms: `vpexpandq xyz, xyz/mxyz`.
pub trait VpexpandqEmitter<T0, T1> {
    fn vpexpandq(&mut self, dst: T0, src: T1);
}

/// Emits `VPEXPANDW`.
///
/// Assembly forms: `vpexpandw xyz, xyz/mxyz`.
pub trait VpexpandwEmitter<T0, T1> {
    fn vpexpandw(&mut self, dst: T0, src: T1);
}

/// Emits `VPEXTRB`.
///
/// Assembly forms: `vpextrb r32[7:0]/m8, xmm, imm8`.
pub trait VpextrbEmitter<T0, T1, T2> {
    fn vpextrb(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPEXTRD`.
///
/// Assembly forms: `vpextrd r32/m32, xmm, imm8`.
pub trait VpextrdEmitter<T0, T1, T2> {
    fn vpextrd(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPEXTRQ`.
///
/// Assembly forms: `vpextrq r64/m64, xmm, imm8`.
pub trait VpextrqEmitter<T0, T1, T2> {
    fn vpextrq(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPEXTRW`.
///
/// Assembly forms: `vpextrw r32[15:0], xmm, imm8`, `vpextrw r32[15:0]/m16, xmm, imm8`.
pub trait VpextrwEmitter<T0, T1, T2> {
    fn vpextrw(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPGATHERDD`.
///
/// Assembly forms: `vpgatherdd xmm, vm32x, xmm`, `vpgatherdd ymm, vm32y, ymm`, `vpgatherdd xmm, vm32x`.
pub trait VpgatherddEmitter<T0, T1> {
    fn vpgatherdd(&mut self, dst: T0, src: T1);
}

/// Emits `VPGATHERDD_3`.
///
/// Assembly forms: `vpgatherdd xmm, vm32x, xmm`, `vpgatherdd ymm, vm32y, ymm`, `vpgatherdd xmm, vm32x`.
pub trait Vpgatherdd3Emitter<T0, T1, T2> {
    fn vpgatherdd_3(&mut self, dst: T0, src: T1, dst2: T2);
}

/// Emits `VPGATHERDQ`.
///
/// Assembly forms: `vpgatherdq xmm, vm32x, xmm`, `vpgatherdq ymm, vm32x, ymm`, `vpgatherdq xmm, vm32x`.
pub trait VpgatherdqEmitter<T0, T1> {
    fn vpgatherdq(&mut self, dst: T0, src: T1);
}

/// Emits `VPGATHERDQ_3`.
///
/// Assembly forms: `vpgatherdq xmm, vm32x, xmm`, `vpgatherdq ymm, vm32x, ymm`, `vpgatherdq xmm, vm32x`.
pub trait Vpgatherdq3Emitter<T0, T1, T2> {
    fn vpgatherdq_3(&mut self, dst: T0, src: T1, dst2: T2);
}

/// Emits `VPGATHERQD`.
///
/// Assembly forms: `vpgatherqd xmm, vm64x, xmm`, `vpgatherqd xmm, vm64y, xmm`, `vpgatherqd xmm, vm64x`.
pub trait VpgatherqdEmitter<T0, T1> {
    fn vpgatherqd(&mut self, dst: T0, src: T1);
}

/// Emits `VPGATHERQD_3`.
///
/// Assembly forms: `vpgatherqd xmm, vm64x, xmm`, `vpgatherqd xmm, vm64y, xmm`, `vpgatherqd xmm, vm64x`.
pub trait Vpgatherqd3Emitter<T0, T1, T2> {
    fn vpgatherqd_3(&mut self, dst: T0, src: T1, dst2: T2);
}

/// Emits `VPGATHERQQ`.
///
/// Assembly forms: `vpgatherqq xmm, vm64x, xmm`, `vpgatherqq ymm, vm64y, ymm`, `vpgatherqq xmm, vm64x`.
pub trait VpgatherqqEmitter<T0, T1> {
    fn vpgatherqq(&mut self, dst: T0, src: T1);
}

/// Emits `VPGATHERQQ_3`.
///
/// Assembly forms: `vpgatherqq xmm, vm64x, xmm`, `vpgatherqq ymm, vm64y, ymm`, `vpgatherqq xmm, vm64x`.
pub trait Vpgatherqq3Emitter<T0, T1, T2> {
    fn vpgatherqq_3(&mut self, dst: T0, src: T1, dst2: T2);
}

/// Emits `VPHADDD`.
///
/// Assembly forms: `vphaddd xmm, xmm, xmm/m128`, `vphaddd ymm, ymm, ymm/m256`.
pub trait VphadddEmitter<T0, T1, T2> {
    fn vphaddd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPHADDSW`.
///
/// Assembly forms: `vphaddsw xmm, xmm, xmm/m128`, `vphaddsw ymm, ymm, ymm/m256`.
pub trait VphaddswEmitter<T0, T1, T2> {
    fn vphaddsw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPHADDW`.
///
/// Assembly forms: `vphaddw xmm, xmm, xmm/m128`, `vphaddw ymm, ymm, ymm/m256`.
pub trait VphaddwEmitter<T0, T1, T2> {
    fn vphaddw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPHMINPOSUW`.
///
/// Assembly forms: `vphminposuw xmm[18:0], xmm/m128`.
pub trait VphminposuwEmitter<T0, T1> {
    fn vphminposuw(&mut self, dst: T0, src: T1);
}

/// Emits `VPHSUBD`.
///
/// Assembly forms: `vphsubd xmm, xmm, xmm/m128`, `vphsubd ymm, ymm, ymm/m256`.
pub trait VphsubdEmitter<T0, T1, T2> {
    fn vphsubd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPHSUBSW`.
///
/// Assembly forms: `vphsubsw xmm, xmm, xmm/m128`, `vphsubsw ymm, ymm, ymm/m256`.
pub trait VphsubswEmitter<T0, T1, T2> {
    fn vphsubsw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPHSUBW`.
///
/// Assembly forms: `vphsubw xmm, xmm, xmm/m128`, `vphsubw ymm, ymm, ymm/m256`.
pub trait VphsubwEmitter<T0, T1, T2> {
    fn vphsubw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPINSRB`.
///
/// Assembly forms: `vpinsrb xmm, xmm, r32[7:0]/m8, imm8`.
pub trait VpinsrbEmitter<T0, T1, T2, T3> {
    fn vpinsrb(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPINSRD`.
///
/// Assembly forms: `vpinsrd xmm, xmm, r32/m32, imm8`.
pub trait VpinsrdEmitter<T0, T1, T2, T3> {
    fn vpinsrd(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPINSRQ`.
///
/// Assembly forms: `vpinsrq xmm, xmm, r64/m64, imm8`.
pub trait VpinsrqEmitter<T0, T1, T2, T3> {
    fn vpinsrq(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPINSRW`.
///
/// Assembly forms: `vpinsrw xmm, xmm, r32[15:0]/m16, imm8`.
pub trait VpinsrwEmitter<T0, T1, T2, T3> {
    fn vpinsrw(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPLZCNTD`.
///
/// Assembly forms: `vplzcntd xyz, xyz/mxyz/b32`.
pub trait VplzcntdEmitter<T0, T1> {
    fn vplzcntd(&mut self, dst: T0, src: T1);
}

/// Emits `VPLZCNTQ`.
///
/// Assembly forms: `vplzcntq xyz, xyz/mxyz/b64`.
pub trait VplzcntqEmitter<T0, T1> {
    fn vplzcntq(&mut self, dst: T0, src: T1);
}

/// Emits `VPMADD52HUQ`.
///
/// Assembly forms: `vpmadd52huq xy, xy, xy/mxy`, `vpmadd52huq xyz, xyz, xyz/mxyz/b64`.
pub trait Vpmadd52huqEmitter<T0, T1, T2> {
    fn vpmadd52huq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMADD52LUQ`.
///
/// Assembly forms: `vpmadd52luq xy, xy, xy/mxy`, `vpmadd52luq xyz, xyz, xyz/mxyz/b64`.
pub trait Vpmadd52luqEmitter<T0, T1, T2> {
    fn vpmadd52luq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMADDUBSW`.
///
/// Assembly forms: `vpmaddubsw xmm, xmm, xmm/m128`, `vpmaddubsw ymm, ymm, ymm/m256`, `vpmaddubsw xyz, xyz, xyz/mxyz`.
pub trait VpmaddubswEmitter<T0, T1, T2> {
    fn vpmaddubsw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMADDWD`.
///
/// Assembly forms: `vpmaddwd xmm, xmm, xmm/m128`, `vpmaddwd ymm, ymm, ymm/m256`, `vpmaddwd xyz, xyz, xyz/mxyz`.
pub trait VpmaddwdEmitter<T0, T1, T2> {
    fn vpmaddwd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMASKMOVD`.
///
/// Assembly forms: `vpmaskmovd m128, xmm, xmm`, `vpmaskmovd m256, ymm, ymm`, `vpmaskmovd xmm, xmm, m128`.
pub trait VpmaskmovdEmitter<T0, T1, T2> {
    fn vpmaskmovd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMASKMOVQ`.
///
/// Assembly forms: `vpmaskmovq m128, xmm, xmm`, `vpmaskmovq m256, ymm, ymm`, `vpmaskmovq xmm, xmm, m128`.
pub trait VpmaskmovqEmitter<T0, T1, T2> {
    fn vpmaskmovq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMAXSB`.
///
/// Assembly forms: `vpmaxsb xmm, xmm, xmm/m128`, `vpmaxsb ymm, ymm, ymm/m256`, `vpmaxsb xyz, xyz, xyz/mxyz`.
pub trait VpmaxsbEmitter<T0, T1, T2> {
    fn vpmaxsb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMAXSD`.
///
/// Assembly forms: `vpmaxsd xmm, xmm, xmm/m128`, `vpmaxsd ymm, ymm, ymm/m256`, `vpmaxsd xyz, xyz, xyz/mxyz/b32`.
pub trait VpmaxsdEmitter<T0, T1, T2> {
    fn vpmaxsd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMAXSQ`.
///
/// Assembly forms: `vpmaxsq xyz, xyz, xyz/mxyz/b64`.
pub trait VpmaxsqEmitter<T0, T1, T2> {
    fn vpmaxsq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMAXSW`.
///
/// Assembly forms: `vpmaxsw xmm, xmm, xmm/m128`, `vpmaxsw ymm, ymm, ymm/m256`, `vpmaxsw xyz, xyz, xyz/mxyz`.
pub trait VpmaxswEmitter<T0, T1, T2> {
    fn vpmaxsw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMAXUB`.
///
/// Assembly forms: `vpmaxub xmm, xmm, xmm/m128`, `vpmaxub ymm, ymm, ymm/m256`, `vpmaxub xyz, xyz, xyz/mxyz`.
pub trait VpmaxubEmitter<T0, T1, T2> {
    fn vpmaxub(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMAXUD`.
///
/// Assembly forms: `vpmaxud xmm, xmm, xmm/m128`, `vpmaxud ymm, ymm, ymm/m256`, `vpmaxud xyz, xyz, xyz/mxyz/b32`.
pub trait VpmaxudEmitter<T0, T1, T2> {
    fn vpmaxud(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMAXUQ`.
///
/// Assembly forms: `vpmaxuq xyz, xyz, xyz/mxyz/b64`.
pub trait VpmaxuqEmitter<T0, T1, T2> {
    fn vpmaxuq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMAXUW`.
///
/// Assembly forms: `vpmaxuw xmm, xmm, xmm/m128`, `vpmaxuw ymm, ymm, ymm/m256`, `vpmaxuw xyz, xyz, xyz/mxyz`.
pub trait VpmaxuwEmitter<T0, T1, T2> {
    fn vpmaxuw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMINSB`.
///
/// Assembly forms: `vpminsb xmm, xmm, xmm/m128`, `vpminsb ymm, ymm, ymm/m256`, `vpminsb xyz, xyz, xyz/mxyz`.
pub trait VpminsbEmitter<T0, T1, T2> {
    fn vpminsb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMINSD`.
///
/// Assembly forms: `vpminsd xmm, xmm, xmm/m128`, `vpminsd ymm, ymm, ymm/m256`, `vpminsd xyz, xyz, xyz/mxyz/b32`.
pub trait VpminsdEmitter<T0, T1, T2> {
    fn vpminsd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMINSQ`.
///
/// Assembly forms: `vpminsq xyz, xyz, xyz/mxyz/b64`.
pub trait VpminsqEmitter<T0, T1, T2> {
    fn vpminsq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMINSW`.
///
/// Assembly forms: `vpminsw xmm, xmm, xmm/m128`, `vpminsw ymm, ymm, ymm/m256`, `vpminsw xyz, xyz, xyz/mxyz`.
pub trait VpminswEmitter<T0, T1, T2> {
    fn vpminsw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMINUB`.
///
/// Assembly forms: `vpminub xmm, xmm, xmm/m128`, `vpminub ymm, ymm, ymm/m256`, `vpminub xyz, xyz, xyz/mxyz`.
pub trait VpminubEmitter<T0, T1, T2> {
    fn vpminub(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMINUD`.
///
/// Assembly forms: `vpminud xmm, xmm, xmm/m128`, `vpminud ymm, ymm, ymm/m256`, `vpminud xyz, xyz, xyz/mxyz/b32`.
pub trait VpminudEmitter<T0, T1, T2> {
    fn vpminud(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMINUQ`.
///
/// Assembly forms: `vpminuq xyz, xyz, xyz/mxyz/b64`.
pub trait VpminuqEmitter<T0, T1, T2> {
    fn vpminuq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMINUW`.
///
/// Assembly forms: `vpminuw xmm, xmm, xmm/m128`, `vpminuw ymm, ymm, ymm/m256`, `vpminuw xyz, xyz, xyz/mxyz`.
pub trait VpminuwEmitter<T0, T1, T2> {
    fn vpminuw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMOVB2M`.
///
/// Assembly forms: `vpmovb2m k, xyz`.
pub trait Vpmovb2mEmitter<T0, T1> {
    fn vpmovb2m(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVD2M`.
///
/// Assembly forms: `vpmovd2m k, xyz`.
pub trait Vpmovd2mEmitter<T0, T1> {
    fn vpmovd2m(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVDB`.
///
/// Assembly forms: `vpmovdb xxx/mxxx, xyz`.
pub trait VpmovdbEmitter<T0, T1> {
    fn vpmovdb(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVDW`.
///
/// Assembly forms: `vpmovdw xxy/mxxy, xyz`.
pub trait VpmovdwEmitter<T0, T1> {
    fn vpmovdw(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVM2B`.
///
/// Assembly forms: `vpmovm2b xyz, k`.
pub trait Vpmovm2bEmitter<T0, T1> {
    fn vpmovm2b(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVM2D`.
///
/// Assembly forms: `vpmovm2d xyz, k`.
pub trait Vpmovm2dEmitter<T0, T1> {
    fn vpmovm2d(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVM2Q`.
///
/// Assembly forms: `vpmovm2q xyz, k`.
pub trait Vpmovm2qEmitter<T0, T1> {
    fn vpmovm2q(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVM2W`.
///
/// Assembly forms: `vpmovm2w xyz, k`.
pub trait Vpmovm2wEmitter<T0, T1> {
    fn vpmovm2w(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVMSKB`.
///
/// Assembly forms: `vpmovmskb r32[15:0], xmm`, `vpmovmskb r32[31:0], ymm`.
pub trait VpmovmskbEmitter<T0, T1> {
    fn vpmovmskb(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVQ2M`.
///
/// Assembly forms: `vpmovq2m k, xyz`.
pub trait Vpmovq2mEmitter<T0, T1> {
    fn vpmovq2m(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVQB`.
///
/// Assembly forms: `vpmovqb xmm[15:0]/m16, xmm`, `vpmovqb xmm[31:0]/m32, ymm`, `vpmovqb xmm[63:0]/m64, zmm`.
pub trait VpmovqbEmitter<T0, T1> {
    fn vpmovqb(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVQD`.
///
/// Assembly forms: `vpmovqd xxy/mxxy, xyz`.
pub trait VpmovqdEmitter<T0, T1> {
    fn vpmovqd(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVQW`.
///
/// Assembly forms: `vpmovqw xxx/mxxx, xyz`.
pub trait VpmovqwEmitter<T0, T1> {
    fn vpmovqw(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVSDB`.
///
/// Assembly forms: `vpmovsdb xxx/mxxx, xyz`.
pub trait VpmovsdbEmitter<T0, T1> {
    fn vpmovsdb(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVSDW`.
///
/// Assembly forms: `vpmovsdw xxy/mxxy, xyz`.
pub trait VpmovsdwEmitter<T0, T1> {
    fn vpmovsdw(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVSQB`.
///
/// Assembly forms: `vpmovsqb xmm[15:0]/m16, xmm`, `vpmovsqb xmm[31:0]/m32, ymm`, `vpmovsqb xmm[63:0]/m64, zmm`.
pub trait VpmovsqbEmitter<T0, T1> {
    fn vpmovsqb(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVSQD`.
///
/// Assembly forms: `vpmovsqd xxy/mxxy, xyz`.
pub trait VpmovsqdEmitter<T0, T1> {
    fn vpmovsqd(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVSQW`.
///
/// Assembly forms: `vpmovsqw xxx/mxxx, xyz`.
pub trait VpmovsqwEmitter<T0, T1> {
    fn vpmovsqw(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVSWB`.
///
/// Assembly forms: `vpmovswb xxy/mxxy, xyz`.
pub trait VpmovswbEmitter<T0, T1> {
    fn vpmovswb(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVSXBD`.
///
/// Assembly forms: `vpmovsxbd xmm, xmm[31:0]/m32`, `vpmovsxbd ymm, xmm[63:0]/m64`, `vpmovsxbd xyz, xxx/mxxx`.
pub trait VpmovsxbdEmitter<T0, T1> {
    fn vpmovsxbd(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVSXBQ`.
///
/// Assembly forms: `vpmovsxbq xmm, xmm[15:0]/m16`, `vpmovsxbq ymm, xmm[31:0]/m32`, `vpmovsxbq zmm, xmm[63:0]/m64`.
pub trait VpmovsxbqEmitter<T0, T1> {
    fn vpmovsxbq(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVSXBW`.
///
/// Assembly forms: `vpmovsxbw xmm, xmm[63:0]/m64`, `vpmovsxbw ymm, xmm/m128`, `vpmovsxbw xyz, xxy/mxxy`.
pub trait VpmovsxbwEmitter<T0, T1> {
    fn vpmovsxbw(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVSXDQ`.
///
/// Assembly forms: `vpmovsxdq xmm, xmm[63:0]/m64`, `vpmovsxdq ymm, xmm/m128`, `vpmovsxdq xyz, xxy/mxxy`.
pub trait VpmovsxdqEmitter<T0, T1> {
    fn vpmovsxdq(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVSXWD`.
///
/// Assembly forms: `vpmovsxwd xmm, xmm[63:0]/m64`, `vpmovsxwd ymm, xmm/m128`, `vpmovsxwd xyz, xxy/mxxy`.
pub trait VpmovsxwdEmitter<T0, T1> {
    fn vpmovsxwd(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVSXWQ`.
///
/// Assembly forms: `vpmovsxwq xmm, xmm[31:0]/m32`, `vpmovsxwq ymm, xmm[63:0]/m64`, `vpmovsxwq xyz, xxx/mxxx`.
pub trait VpmovsxwqEmitter<T0, T1> {
    fn vpmovsxwq(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVUSDB`.
///
/// Assembly forms: `vpmovusdb xxx/mxxx, xyz`.
pub trait VpmovusdbEmitter<T0, T1> {
    fn vpmovusdb(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVUSDW`.
///
/// Assembly forms: `vpmovusdw xxy/mxxy, xyz`.
pub trait VpmovusdwEmitter<T0, T1> {
    fn vpmovusdw(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVUSQB`.
///
/// Assembly forms: `vpmovusqb xmm[15:0]/m16, xmm`, `vpmovusqb xmm[31:0]/m32, ymm`, `vpmovusqb xmm[63:0]/m64, zmm`.
pub trait VpmovusqbEmitter<T0, T1> {
    fn vpmovusqb(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVUSQD`.
///
/// Assembly forms: `vpmovusqd xxy/mxxy, xyz`.
pub trait VpmovusqdEmitter<T0, T1> {
    fn vpmovusqd(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVUSQW`.
///
/// Assembly forms: `vpmovusqw xxx/mxxx, xyz`.
pub trait VpmovusqwEmitter<T0, T1> {
    fn vpmovusqw(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVUSWB`.
///
/// Assembly forms: `vpmovuswb xxy/mxxy, xyz`.
pub trait VpmovuswbEmitter<T0, T1> {
    fn vpmovuswb(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVW2M`.
///
/// Assembly forms: `vpmovw2m k, xyz`.
pub trait Vpmovw2mEmitter<T0, T1> {
    fn vpmovw2m(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVWB`.
///
/// Assembly forms: `vpmovwb xxy/mxxy, xyz`.
pub trait VpmovwbEmitter<T0, T1> {
    fn vpmovwb(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVZXBD`.
///
/// Assembly forms: `vpmovzxbd xmm, xmm[31:0]/m32`, `vpmovzxbd ymm, xmm[63:0]/m64`, `vpmovzxbd xyz, xxx/mxxx`.
pub trait VpmovzxbdEmitter<T0, T1> {
    fn vpmovzxbd(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVZXBQ`.
///
/// Assembly forms: `vpmovzxbq xmm, xmm[15:0]/m16`, `vpmovzxbq ymm, xmm[31:0]/m32`, `vpmovzxbq zmm, xmm[63:0]/m64`.
pub trait VpmovzxbqEmitter<T0, T1> {
    fn vpmovzxbq(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVZXBW`.
///
/// Assembly forms: `vpmovzxbw xmm, xmm[63:0]/m64`, `vpmovzxbw ymm, xmm/m128`, `vpmovzxbw xyz, xxy/mxxy`.
pub trait VpmovzxbwEmitter<T0, T1> {
    fn vpmovzxbw(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVZXDQ`.
///
/// Assembly forms: `vpmovzxdq xmm, xmm[63:0]/m64`, `vpmovzxdq ymm, xmm/m128`, `vpmovzxdq xyz, xxy/mxxy`.
pub trait VpmovzxdqEmitter<T0, T1> {
    fn vpmovzxdq(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVZXWD`.
///
/// Assembly forms: `vpmovzxwd xmm, xmm[63:0]/m64`, `vpmovzxwd ymm, xmm/m128`, `vpmovzxwd xyz, xxy/mxxy`.
pub trait VpmovzxwdEmitter<T0, T1> {
    fn vpmovzxwd(&mut self, dst: T0, src: T1);
}

/// Emits `VPMOVZXWQ`.
///
/// Assembly forms: `vpmovzxwq xmm, xmm[31:0]/m32`, `vpmovzxwq ymm, xmm[63:0]/m64`, `vpmovzxwq xyz, xxx/mxxx`.
pub trait VpmovzxwqEmitter<T0, T1> {
    fn vpmovzxwq(&mut self, dst: T0, src: T1);
}

/// Emits `VPMULDQ`.
///
/// Assembly forms: `vpmuldq xmm, xmm, xmm/m128`, `vpmuldq ymm, ymm, ymm/m256`, `vpmuldq xyz, xyz, xyz/mxyz/b64`.
pub trait VpmuldqEmitter<T0, T1, T2> {
    fn vpmuldq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMULHRSW`.
///
/// Assembly forms: `vpmulhrsw xmm, xmm, xmm/m128`, `vpmulhrsw ymm, ymm, ymm/m256`, `vpmulhrsw xyz, xyz, xyz/mxyz`.
pub trait VpmulhrswEmitter<T0, T1, T2> {
    fn vpmulhrsw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMULHUW`.
///
/// Assembly forms: `vpmulhuw xmm, xmm, xmm/m128`, `vpmulhuw ymm, ymm, ymm/m256`, `vpmulhuw xyz, xyz, xyz/mxyz`.
pub trait VpmulhuwEmitter<T0, T1, T2> {
    fn vpmulhuw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMULHW`.
///
/// Assembly forms: `vpmulhw xmm, xmm, xmm/m128`, `vpmulhw ymm, ymm, ymm/m256`, `vpmulhw xyz, xyz, xyz/mxyz`.
pub trait VpmulhwEmitter<T0, T1, T2> {
    fn vpmulhw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMULLD`.
///
/// Assembly forms: `vpmulld xmm, xmm, xmm/m128`, `vpmulld ymm, ymm, ymm/m256`, `vpmulld xyz, xyz, xyz/mxyz/b32`.
pub trait VpmulldEmitter<T0, T1, T2> {
    fn vpmulld(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMULLQ`.
///
/// Assembly forms: `vpmullq xyz, xyz, xyz/mxyz/b64`.
pub trait VpmullqEmitter<T0, T1, T2> {
    fn vpmullq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMULLW`.
///
/// Assembly forms: `vpmullw xmm, xmm, xmm/m128`, `vpmullw ymm, ymm, ymm/m256`, `vpmullw xyz, xyz, xyz/mxyz`.
pub trait VpmullwEmitter<T0, T1, T2> {
    fn vpmullw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMULTISHIFTQB`.
///
/// Assembly forms: `vpmultishiftqb xyz, xyz, xyz/mxyz/b64`.
pub trait VpmultishiftqbEmitter<T0, T1, T2> {
    fn vpmultishiftqb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPMULUDQ`.
///
/// Assembly forms: `vpmuludq xmm, xmm, xmm/m128`, `vpmuludq ymm, ymm, ymm/m256`, `vpmuludq xyz, xyz, xyz/mxyz/b64`.
pub trait VpmuludqEmitter<T0, T1, T2> {
    fn vpmuludq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPOPCNTB`.
///
/// Assembly forms: `vpopcntb xyz, xyz/mxyz`.
pub trait VpopcntbEmitter<T0, T1> {
    fn vpopcntb(&mut self, dst: T0, src: T1);
}

/// Emits `VPOPCNTD`.
///
/// Assembly forms: `vpopcntd xyz, xyz/mxyz/b32`.
pub trait VpopcntdEmitter<T0, T1> {
    fn vpopcntd(&mut self, dst: T0, src: T1);
}

/// Emits `VPOPCNTQ`.
///
/// Assembly forms: `vpopcntq xyz, xyz/mxyz/b64`.
pub trait VpopcntqEmitter<T0, T1> {
    fn vpopcntq(&mut self, dst: T0, src: T1);
}

/// Emits `VPOPCNTW`.
///
/// Assembly forms: `vpopcntw xyz, xyz/mxyz`.
pub trait VpopcntwEmitter<T0, T1> {
    fn vpopcntw(&mut self, dst: T0, src: T1);
}

/// Emits `VPOR`.
///
/// Assembly forms: `vpor xmm, xmm, xmm/m128`, `vpor ymm, ymm, ymm/m256`.
pub trait VporEmitter<T0, T1, T2> {
    fn vpor(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPORD`.
///
/// Assembly forms: `vpord xyz, xyz, xyz/mxyz/b32`.
pub trait VpordEmitter<T0, T1, T2> {
    fn vpord(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPORQ`.
///
/// Assembly forms: `vporq xyz, xyz, xyz/mxyz/b64`.
pub trait VporqEmitter<T0, T1, T2> {
    fn vporq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPROLD`.
///
/// Assembly forms: `vprold xyz, xyz/mxyz/b32, imm8`.
pub trait VproldEmitter<T0, T1, T2> {
    fn vprold(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPROLQ`.
///
/// Assembly forms: `vprolq xyz, xyz/mxyz/b64, imm8`.
pub trait VprolqEmitter<T0, T1, T2> {
    fn vprolq(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPROLVD`.
///
/// Assembly forms: `vprolvd xyz, xyz, xyz/mxyz/b32`.
pub trait VprolvdEmitter<T0, T1, T2> {
    fn vprolvd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPROLVQ`.
///
/// Assembly forms: `vprolvq xyz, xyz, xyz/mxyz/b64`.
pub trait VprolvqEmitter<T0, T1, T2> {
    fn vprolvq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPRORD`.
///
/// Assembly forms: `vprord xyz, xyz/mxyz/b32, imm8`.
pub trait VprordEmitter<T0, T1, T2> {
    fn vprord(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPRORQ`.
///
/// Assembly forms: `vprorq xyz, xyz/mxyz/b64, imm8`.
pub trait VprorqEmitter<T0, T1, T2> {
    fn vprorq(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPRORVD`.
///
/// Assembly forms: `vprorvd xyz, xyz, xyz/mxyz/b32`.
pub trait VprorvdEmitter<T0, T1, T2> {
    fn vprorvd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPRORVQ`.
///
/// Assembly forms: `vprorvq xyz, xyz, xyz/mxyz/b64`.
pub trait VprorvqEmitter<T0, T1, T2> {
    fn vprorvq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSADBW`.
///
/// Assembly forms: `vpsadbw xmm, xmm, xmm/m128`, `vpsadbw ymm, ymm, ymm/m256`, `vpsadbw xyz, xyz, xyz/mxyz`.
pub trait VpsadbwEmitter<T0, T1, T2> {
    fn vpsadbw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSCATTERDD`.
///
/// Assembly forms: `vpscatterdd vm32x, xmm`, `vpscatterdd vm32y, ymm`, `vpscatterdd vm32z, zmm`.
pub trait VpscatterddEmitter<T0, T1> {
    fn vpscatterdd(&mut self, dst: T0, src: T1);
}

/// Emits `VPSCATTERDQ`.
///
/// Assembly forms: `vpscatterdq vm32x, xmm`, `vpscatterdq vm32x, ymm`, `vpscatterdq vm32y, zmm`.
pub trait VpscatterdqEmitter<T0, T1> {
    fn vpscatterdq(&mut self, dst: T0, src: T1);
}

/// Emits `VPSCATTERQD`.
///
/// Assembly forms: `vpscatterqd vm64x, xmm`, `vpscatterqd vm64y, xmm`, `vpscatterqd vm64z, ymm`.
pub trait VpscatterqdEmitter<T0, T1> {
    fn vpscatterqd(&mut self, dst: T0, src: T1);
}

/// Emits `VPSCATTERQQ`.
///
/// Assembly forms: `vpscatterqq vm64x, xmm`, `vpscatterqq vm64y, ymm`, `vpscatterqq vm64z, zmm`.
pub trait VpscatterqqEmitter<T0, T1> {
    fn vpscatterqq(&mut self, dst: T0, src: T1);
}

/// Emits `VPSHLDD`.
///
/// Assembly forms: `vpshldd xyz, xyz, xyz/mxyz/b32, imm8`.
pub trait VpshlddEmitter<T0, T1, T2, T3> {
    fn vpshldd(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPSHLDQ`.
///
/// Assembly forms: `vpshldq xyz, xyz, xyz/mxyz/b64, imm8`.
pub trait VpshldqEmitter<T0, T1, T2, T3> {
    fn vpshldq(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPSHLDVD`.
///
/// Assembly forms: `vpshldvd xyz, xyz, xyz/mxyz/b32`.
pub trait VpshldvdEmitter<T0, T1, T2> {
    fn vpshldvd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSHLDVQ`.
///
/// Assembly forms: `vpshldvq xyz, xyz, xyz/mxyz/b64`.
pub trait VpshldvqEmitter<T0, T1, T2> {
    fn vpshldvq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSHLDVW`.
///
/// Assembly forms: `vpshldvw xyz, xyz, xyz/mxyz`.
pub trait VpshldvwEmitter<T0, T1, T2> {
    fn vpshldvw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSHLDW`.
///
/// Assembly forms: `vpshldw xyz, xyz, xyz/mxyz, imm8`.
pub trait VpshldwEmitter<T0, T1, T2, T3> {
    fn vpshldw(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPSHRDD`.
///
/// Assembly forms: `vpshrdd xyz, xyz, xyz/mxyz/b32, imm8`.
pub trait VpshrddEmitter<T0, T1, T2, T3> {
    fn vpshrdd(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPSHRDQ`.
///
/// Assembly forms: `vpshrdq xyz, xyz, xyz/mxyz/b64, imm8`.
pub trait VpshrdqEmitter<T0, T1, T2, T3> {
    fn vpshrdq(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPSHRDVD`.
///
/// Assembly forms: `vpshrdvd xyz, xyz, xyz/mxyz/b32`.
pub trait VpshrdvdEmitter<T0, T1, T2> {
    fn vpshrdvd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSHRDVQ`.
///
/// Assembly forms: `vpshrdvq xyz, xyz, xyz/mxyz/b64`.
pub trait VpshrdvqEmitter<T0, T1, T2> {
    fn vpshrdvq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSHRDVW`.
///
/// Assembly forms: `vpshrdvw xyz, xyz, xyz/mxyz`.
pub trait VpshrdvwEmitter<T0, T1, T2> {
    fn vpshrdvw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSHRDW`.
///
/// Assembly forms: `vpshrdw xyz, xyz, xyz/mxyz, imm8`.
pub trait VpshrdwEmitter<T0, T1, T2, T3> {
    fn vpshrdw(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPSHUFB`.
///
/// Assembly forms: `vpshufb xmm, xmm, xmm/m128`, `vpshufb ymm, ymm, ymm/m256`, `vpshufb xyz, xyz, xyz/mxyz`.
pub trait VpshufbEmitter<T0, T1, T2> {
    fn vpshufb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSHUFBITQMB`.
///
/// Assembly forms: `vpshufbitqmb k, xyz, xyz/mxyz`.
pub trait VpshufbitqmbEmitter<T0, T1, T2> {
    fn vpshufbitqmb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSHUFD`.
///
/// Assembly forms: `vpshufd xmm, xmm/m128, imm8`, `vpshufd ymm, ymm/m256, imm8`, `vpshufd xyz, xyz/mxyz/b32, imm8`.
pub trait VpshufdEmitter<T0, T1, T2> {
    fn vpshufd(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPSHUFHW`.
///
/// Assembly forms: `vpshufhw xmm, xmm/m128, imm8`, `vpshufhw ymm, ymm/m256, imm8`, `vpshufhw xyz, xyz/mxyz, imm8`.
pub trait VpshufhwEmitter<T0, T1, T2> {
    fn vpshufhw(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPSHUFLW`.
///
/// Assembly forms: `vpshuflw xmm, xmm/m128, imm8`, `vpshuflw ymm, ymm/m256, imm8`, `vpshuflw xyz, xyz/mxyz, imm8`.
pub trait VpshuflwEmitter<T0, T1, T2> {
    fn vpshuflw(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPSIGNB`.
///
/// Assembly forms: `vpsignb xmm, xmm, xmm/m128`, `vpsignb ymm, ymm, ymm/m256`.
pub trait VpsignbEmitter<T0, T1, T2> {
    fn vpsignb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSIGND`.
///
/// Assembly forms: `vpsignd xmm, xmm, xmm/m128`, `vpsignd ymm, ymm, ymm/m256`.
pub trait VpsigndEmitter<T0, T1, T2> {
    fn vpsignd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSIGNW`.
///
/// Assembly forms: `vpsignw xmm, xmm, xmm/m128`, `vpsignw ymm, ymm, ymm/m256`.
pub trait VpsignwEmitter<T0, T1, T2> {
    fn vpsignw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSLLD`.
///
/// Assembly forms: `vpslld xmm, xmm, xmm/m128`, `vpslld xmm, xmm, imm8`, `vpslld ymm, ymm, xmm/m128`.
pub trait VpslldEmitter<T0, T1, T2> {
    fn vpslld(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPSLLDQ`.
///
/// Assembly forms: `vpslldq xmm, xmm, imm8`, `vpslldq ymm, ymm, imm8`, `vpslldq xyz, xyz/mxyz, imm8`.
pub trait VpslldqEmitter<T0, T1, T2> {
    fn vpslldq(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPSLLQ`.
///
/// Assembly forms: `vpsllq xmm, xmm, xmm/m128`, `vpsllq xmm, xmm, imm8`, `vpsllq ymm, ymm, xmm/m128`.
pub trait VpsllqEmitter<T0, T1, T2> {
    fn vpsllq(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPSLLVD`.
///
/// Assembly forms: `vpsllvd xmm, xmm, xmm/m128`, `vpsllvd ymm, ymm, ymm/m256`, `vpsllvd xyz, xyz, xyz/mxyz/b32`.
pub trait VpsllvdEmitter<T0, T1, T2> {
    fn vpsllvd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSLLVQ`.
///
/// Assembly forms: `vpsllvq xmm, xmm, xmm/m128`, `vpsllvq ymm, ymm, ymm/m256`, `vpsllvq xyz, xyz, xyz/mxyz/b64`.
pub trait VpsllvqEmitter<T0, T1, T2> {
    fn vpsllvq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSLLVW`.
///
/// Assembly forms: `vpsllvw xyz, xyz, xyz/mxyz`.
pub trait VpsllvwEmitter<T0, T1, T2> {
    fn vpsllvw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSLLW`.
///
/// Assembly forms: `vpsllw xmm, xmm, xmm/m128`, `vpsllw xmm, xmm, imm8`, `vpsllw ymm, ymm, xmm/m128`.
pub trait VpsllwEmitter<T0, T1, T2> {
    fn vpsllw(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPSRAD`.
///
/// Assembly forms: `vpsrad xmm, xmm, xmm/m128`, `vpsrad xmm, xmm, imm8`, `vpsrad ymm, ymm, xmm/m128`.
pub trait VpsradEmitter<T0, T1, T2> {
    fn vpsrad(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPSRAQ`.
///
/// Assembly forms: `vpsraq xyz, xyz, xmm/m128`, `vpsraq xyz, xyz/mxyz/b64, imm8`.
pub trait VpsraqEmitter<T0, T1, T2> {
    fn vpsraq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSRAVD`.
///
/// Assembly forms: `vpsravd xmm, xmm, xmm/m128`, `vpsravd ymm, ymm, ymm/m256`, `vpsravd xyz, xyz, xyz/mxyz/b32`.
pub trait VpsravdEmitter<T0, T1, T2> {
    fn vpsravd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSRAVQ`.
///
/// Assembly forms: `vpsravq xyz, xyz, xyz/mxyz/b64`.
pub trait VpsravqEmitter<T0, T1, T2> {
    fn vpsravq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSRAVW`.
///
/// Assembly forms: `vpsravw xyz, xyz, xyz/mxyz`.
pub trait VpsravwEmitter<T0, T1, T2> {
    fn vpsravw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSRAW`.
///
/// Assembly forms: `vpsraw xmm, xmm, xmm/m128`, `vpsraw xmm, xmm, imm8`, `vpsraw ymm, ymm, xmm/m128`.
pub trait VpsrawEmitter<T0, T1, T2> {
    fn vpsraw(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPSRLD`.
///
/// Assembly forms: `vpsrld xmm, xmm, xmm/m128`, `vpsrld xmm, xmm, imm8`, `vpsrld ymm, ymm, xmm/m128`.
pub trait VpsrldEmitter<T0, T1, T2> {
    fn vpsrld(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPSRLDQ`.
///
/// Assembly forms: `vpsrldq xmm, xmm, imm8`, `vpsrldq ymm, ymm, imm8`, `vpsrldq xyz, xyz/mxyz, imm8`.
pub trait VpsrldqEmitter<T0, T1, T2> {
    fn vpsrldq(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPSRLQ`.
///
/// Assembly forms: `vpsrlq xmm, xmm, xmm/m128`, `vpsrlq xmm, xmm, imm8`, `vpsrlq ymm, ymm, xmm/m128`.
pub trait VpsrlqEmitter<T0, T1, T2> {
    fn vpsrlq(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPSRLVD`.
///
/// Assembly forms: `vpsrlvd xmm, xmm, xmm/m128`, `vpsrlvd ymm, ymm, ymm/m256`, `vpsrlvd xyz, xyz, xyz/mxyz/b32`.
pub trait VpsrlvdEmitter<T0, T1, T2> {
    fn vpsrlvd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSRLVQ`.
///
/// Assembly forms: `vpsrlvq xmm, xmm, xmm/m128`, `vpsrlvq ymm, ymm, ymm/m256`, `vpsrlvq xyz, xyz, xyz/mxyz/b64`.
pub trait VpsrlvqEmitter<T0, T1, T2> {
    fn vpsrlvq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSRLVW`.
///
/// Assembly forms: `vpsrlvw xyz, xyz, xyz/mxyz`.
pub trait VpsrlvwEmitter<T0, T1, T2> {
    fn vpsrlvw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSRLW`.
///
/// Assembly forms: `vpsrlw xmm, xmm, xmm/m128`, `vpsrlw xmm, xmm, imm8`, `vpsrlw ymm, ymm, xmm/m128`.
pub trait VpsrlwEmitter<T0, T1, T2> {
    fn vpsrlw(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPSUBB`.
///
/// Assembly forms: `vpsubb xmm, xmm, xmm/m128`, `vpsubb ymm, ymm, ymm/m256`, `vpsubb xyz, xyz, xyz/mxyz`.
pub trait VpsubbEmitter<T0, T1, T2> {
    fn vpsubb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSUBD`.
///
/// Assembly forms: `vpsubd xmm, xmm, xmm/m128`, `vpsubd ymm, ymm, ymm/m256`, `vpsubd xyz, xyz, xyz/mxyz/b32`.
pub trait VpsubdEmitter<T0, T1, T2> {
    fn vpsubd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSUBQ`.
///
/// Assembly forms: `vpsubq xmm, xmm, xmm/m128`, `vpsubq ymm, ymm, ymm/m256`, `vpsubq xyz, xyz, xyz/mxyz/b64`.
pub trait VpsubqEmitter<T0, T1, T2> {
    fn vpsubq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSUBSB`.
///
/// Assembly forms: `vpsubsb xmm, xmm, xmm/m128`, `vpsubsb ymm, ymm, ymm/m256`, `vpsubsb xyz, xyz, xyz/mxyz`.
pub trait VpsubsbEmitter<T0, T1, T2> {
    fn vpsubsb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSUBSW`.
///
/// Assembly forms: `vpsubsw xmm, xmm, xmm/m128`, `vpsubsw ymm, ymm, ymm/m256`, `vpsubsw xyz, xyz, xyz/mxyz`.
pub trait VpsubswEmitter<T0, T1, T2> {
    fn vpsubsw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSUBUSB`.
///
/// Assembly forms: `vpsubusb xmm, xmm, xmm/m128`, `vpsubusb ymm, ymm, ymm/m256`, `vpsubusb xyz, xyz, xyz/mxyz`.
pub trait VpsubusbEmitter<T0, T1, T2> {
    fn vpsubusb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSUBUSW`.
///
/// Assembly forms: `vpsubusw xmm, xmm, xmm/m128`, `vpsubusw ymm, ymm, ymm/m256`, `vpsubusw xyz, xyz, xyz/mxyz`.
pub trait VpsubuswEmitter<T0, T1, T2> {
    fn vpsubusw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSUBW`.
///
/// Assembly forms: `vpsubw xmm, xmm, xmm/m128`, `vpsubw ymm, ymm, ymm/m256`, `vpsubw xyz, xyz, xyz/mxyz`.
pub trait VpsubwEmitter<T0, T1, T2> {
    fn vpsubw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPTERNLOGD`.
///
/// Assembly forms: `vpternlogd xyz, xyz, xyz/mxyz/b32, imm8`.
pub trait VpternlogdEmitter<T0, T1, T2, T3> {
    fn vpternlogd(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPTERNLOGQ`.
///
/// Assembly forms: `vpternlogq xyz, xyz, xyz/mxyz/b64, imm8`.
pub trait VpternlogqEmitter<T0, T1, T2, T3> {
    fn vpternlogq(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPTEST`.
///
/// Assembly forms: `vptest xy, xy/mxy`.
pub trait VptestEmitter<T0, T1> {
    fn vptest(&mut self, lhs: T0, rhs: T1);
}

/// Emits `VPTESTMB`.
///
/// Assembly forms: `vptestmb k, xyz, xyz/mxyz`.
pub trait VptestmbEmitter<T0, T1, T2> {
    fn vptestmb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPTESTMD`.
///
/// Assembly forms: `vptestmd k, xyz, xyz/mxyz/b32`.
pub trait VptestmdEmitter<T0, T1, T2> {
    fn vptestmd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPTESTMQ`.
///
/// Assembly forms: `vptestmq k, xyz, xyz/mxyz/b64`.
pub trait VptestmqEmitter<T0, T1, T2> {
    fn vptestmq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPTESTMW`.
///
/// Assembly forms: `vptestmw k, xyz, xyz/mxyz`.
pub trait VptestmwEmitter<T0, T1, T2> {
    fn vptestmw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPTESTNMB`.
///
/// Assembly forms: `vptestnmb k, xyz, xyz/mxyz`.
pub trait VptestnmbEmitter<T0, T1, T2> {
    fn vptestnmb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPTESTNMD`.
///
/// Assembly forms: `vptestnmd k, xyz, xyz/mxyz/b32`.
pub trait VptestnmdEmitter<T0, T1, T2> {
    fn vptestnmd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPTESTNMQ`.
///
/// Assembly forms: `vptestnmq k, xyz, xyz/mxyz/b64`.
pub trait VptestnmqEmitter<T0, T1, T2> {
    fn vptestnmq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPTESTNMW`.
///
/// Assembly forms: `vptestnmw k, xyz, xyz/mxyz`.
pub trait VptestnmwEmitter<T0, T1, T2> {
    fn vptestnmw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPUNPCKHBW`.
///
/// Assembly forms: `vpunpckhbw xmm, xmm, xmm/m128`, `vpunpckhbw ymm, ymm, ymm/m256`, `vpunpckhbw xyz, xyz, xyz/mxyz`.
pub trait VpunpckhbwEmitter<T0, T1, T2> {
    fn vpunpckhbw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPUNPCKHDQ`.
///
/// Assembly forms: `vpunpckhdq xmm, xmm, xmm/m128`, `vpunpckhdq ymm, ymm, ymm/m256`, `vpunpckhdq xyz, xyz, xyz/mxyz/b32`.
pub trait VpunpckhdqEmitter<T0, T1, T2> {
    fn vpunpckhdq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPUNPCKHQDQ`.
///
/// Assembly forms: `vpunpckhqdq xmm, xmm, xmm/m128`, `vpunpckhqdq ymm, ymm, ymm/m256`, `vpunpckhqdq xyz, xyz, xyz/mxyz/b64`.
pub trait VpunpckhqdqEmitter<T0, T1, T2> {
    fn vpunpckhqdq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPUNPCKHWD`.
///
/// Assembly forms: `vpunpckhwd xmm, xmm, xmm/m128`, `vpunpckhwd ymm, ymm, ymm/m256`, `vpunpckhwd xyz, xyz, xyz/mxyz`.
pub trait VpunpckhwdEmitter<T0, T1, T2> {
    fn vpunpckhwd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPUNPCKLBW`.
///
/// Assembly forms: `vpunpcklbw xmm, xmm, xmm/m128`, `vpunpcklbw ymm, ymm, ymm/m256`, `vpunpcklbw xyz, xyz, xyz/mxyz`.
pub trait VpunpcklbwEmitter<T0, T1, T2> {
    fn vpunpcklbw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPUNPCKLDQ`.
///
/// Assembly forms: `vpunpckldq xmm, xmm, xmm/m128`, `vpunpckldq ymm, ymm, ymm/m256`, `vpunpckldq xyz, xyz, xyz/mxyz/b32`.
pub trait VpunpckldqEmitter<T0, T1, T2> {
    fn vpunpckldq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPUNPCKLQDQ`.
///
/// Assembly forms: `vpunpcklqdq xmm, xmm, xmm/m128`, `vpunpcklqdq ymm, ymm, ymm/m256`, `vpunpcklqdq xyz, xyz, xyz/mxyz/b64`.
pub trait VpunpcklqdqEmitter<T0, T1, T2> {
    fn vpunpcklqdq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPUNPCKLWD`.
///
/// Assembly forms: `vpunpcklwd xmm, xmm, xmm/m128`, `vpunpcklwd ymm, ymm, ymm/m256`, `vpunpcklwd xyz, xyz, xyz/mxyz`.
pub trait VpunpcklwdEmitter<T0, T1, T2> {
    fn vpunpcklwd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPXOR`.
///
/// Assembly forms: `vpxor xmm, xmm, xmm/m128`, `vpxor ymm, ymm, ymm/m256`.
pub trait VpxorEmitter<T0, T1, T2> {
    fn vpxor(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPXORD`.
///
/// Assembly forms: `vpxord xyz, xyz, xyz/mxyz/b32`.
pub trait VpxordEmitter<T0, T1, T2> {
    fn vpxord(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPXORQ`.
///
/// Assembly forms: `vpxorq xyz, xyz, xyz/mxyz/b64`.
pub trait VpxorqEmitter<T0, T1, T2> {
    fn vpxorq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VRANGEPD`.
///
/// Assembly forms: `vrangepd xyz, xyz, xyz/mxyz/b64, imm8`.
pub trait VrangepdEmitter<T0, T1, T2, T3> {
    fn vrangepd(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VRANGEPS`.
///
/// Assembly forms: `vrangeps xyz, xyz, xyz/mxyz/b32, imm8`.
pub trait VrangepsEmitter<T0, T1, T2, T3> {
    fn vrangeps(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VRANGESD`.
///
/// Assembly forms: `vrangesd xmm, xmm[127:64], xmm[63:0]/m64, imm8`.
pub trait VrangesdEmitter<T0, T1, T2, T3> {
    fn vrangesd(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VRANGESS`.
///
/// Assembly forms: `vrangess xmm, xmm[127:32], xmm[31:0]/m32, imm8`.
pub trait VrangessEmitter<T0, T1, T2, T3> {
    fn vrangess(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VRCP14PD`.
///
/// Assembly forms: `vrcp14pd xyz, xyz/mxyz/b64`.
pub trait Vrcp14pdEmitter<T0, T1> {
    fn vrcp14pd(&mut self, dst: T0, src: T1);
}

/// Emits `VRCP14PS`.
///
/// Assembly forms: `vrcp14ps xyz, xyz/mxyz/b32`.
pub trait Vrcp14psEmitter<T0, T1> {
    fn vrcp14ps(&mut self, dst: T0, src: T1);
}

/// Emits `VRCP14SD`.
///
/// Assembly forms: `vrcp14sd xmm, xmm[127:64], xmm[63:0]/m64`.
pub trait Vrcp14sdEmitter<T0, T1, T2> {
    fn vrcp14sd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VRCP14SS`.
///
/// Assembly forms: `vrcp14ss xmm, xmm[127:32], xmm[31:0]/m32`.
pub trait Vrcp14ssEmitter<T0, T1, T2> {
    fn vrcp14ss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VRCPPS`.
///
/// Assembly forms: `vrcpps xy, xy/mxy`.
pub trait VrcppsEmitter<T0, T1> {
    fn vrcpps(&mut self, dst: T0, src: T1);
}

/// Emits `VRCPSS`.
///
/// Assembly forms: `vrcpss xmm, xmm[127:32], xmm[31:0]/m32`.
pub trait VrcpssEmitter<T0, T1, T2> {
    fn vrcpss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VREDUCEPD`.
///
/// Assembly forms: `vreducepd xyz, xyz/mxyz/b64, imm8`.
pub trait VreducepdEmitter<T0, T1, T2> {
    fn vreducepd(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VREDUCEPS`.
///
/// Assembly forms: `vreduceps xyz, xyz/mxyz/b32, imm8`.
pub trait VreducepsEmitter<T0, T1, T2> {
    fn vreduceps(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VREDUCESD`.
///
/// Assembly forms: `vreducesd xmm, xmm[127:64], xmm[63:0]/m64, imm8`.
pub trait VreducesdEmitter<T0, T1, T2, T3> {
    fn vreducesd(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VREDUCESS`.
///
/// Assembly forms: `vreducess xmm, xmm[127:32], xmm[31:0]/m32, imm8`.
pub trait VreducessEmitter<T0, T1, T2, T3> {
    fn vreducess(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VRNDSCALEPD`.
///
/// Assembly forms: `vrndscalepd xyz, xyz/mxyz/b64, imm8`.
pub trait VrndscalepdEmitter<T0, T1, T2> {
    fn vrndscalepd(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VRNDSCALEPS`.
///
/// Assembly forms: `vrndscaleps xyz, xyz/mxyz/b32, imm8`.
pub trait VrndscalepsEmitter<T0, T1, T2> {
    fn vrndscaleps(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VRNDSCALESD`.
///
/// Assembly forms: `vrndscalesd xmm, xmm[127:64], xmm[63:0]/m64, imm8`.
pub trait VrndscalesdEmitter<T0, T1, T2, T3> {
    fn vrndscalesd(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VRNDSCALESS`.
///
/// Assembly forms: `vrndscaless xmm, xmm[127:32], xmm[31:0]/m32, imm8`.
pub trait VrndscalessEmitter<T0, T1, T2, T3> {
    fn vrndscaless(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VROUNDPD`.
///
/// Assembly forms: `vroundpd xy, xy/mxy, imm8`.
pub trait VroundpdEmitter<T0, T1, T2> {
    fn vroundpd(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VROUNDPS`.
///
/// Assembly forms: `vroundps xy, xy/mxy, imm8`.
pub trait VroundpsEmitter<T0, T1, T2> {
    fn vroundps(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VROUNDSD`.
///
/// Assembly forms: `vroundsd xmm, xmm[127:64], xmm[63:0]/m64, imm8`.
pub trait VroundsdEmitter<T0, T1, T2, T3> {
    fn vroundsd(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VROUNDSS`.
///
/// Assembly forms: `vroundss xmm, xmm[127:32], xmm[31:0]/m32, imm8`.
pub trait VroundssEmitter<T0, T1, T2, T3> {
    fn vroundss(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VRSQRT14PD`.
///
/// Assembly forms: `vrsqrt14pd xyz, xyz/mxyz/b64`.
pub trait Vrsqrt14pdEmitter<T0, T1> {
    fn vrsqrt14pd(&mut self, dst: T0, src: T1);
}

/// Emits `VRSQRT14PS`.
///
/// Assembly forms: `vrsqrt14ps xyz, xyz/mxyz/b32`.
pub trait Vrsqrt14psEmitter<T0, T1> {
    fn vrsqrt14ps(&mut self, dst: T0, src: T1);
}

/// Emits `VRSQRT14SD`.
///
/// Assembly forms: `vrsqrt14sd xmm, xmm[127:64], xmm[63:0]/m64`.
pub trait Vrsqrt14sdEmitter<T0, T1, T2> {
    fn vrsqrt14sd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VRSQRT14SS`.
///
/// Assembly forms: `vrsqrt14ss xmm, xmm[127:32], xmm[31:0]/m32`.
pub trait Vrsqrt14ssEmitter<T0, T1, T2> {
    fn vrsqrt14ss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VRSQRTPS`.
///
/// Assembly forms: `vrsqrtps xy, xy/mxy`.
pub trait VrsqrtpsEmitter<T0, T1> {
    fn vrsqrtps(&mut self, dst: T0, src: T1);
}

/// Emits `VRSQRTSS`.
///
/// Assembly forms: `vrsqrtss xmm, xmm[127:32], xmm[31:0]/m32`.
pub trait VrsqrtssEmitter<T0, T1, T2> {
    fn vrsqrtss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VSCALEFPD`.
///
/// Assembly forms: `vscalefpd xyz, xyz, xyz/mxyz/b64`.
pub trait VscalefpdEmitter<T0, T1, T2> {
    fn vscalefpd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VSCALEFPS`.
///
/// Assembly forms: `vscalefps xyz, xyz, xyz/mxyz/b32`.
pub trait VscalefpsEmitter<T0, T1, T2> {
    fn vscalefps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VSCALEFSD`.
///
/// Assembly forms: `vscalefsd xmm, xmm, xmm[63:0]/m64`.
pub trait VscalefsdEmitter<T0, T1, T2> {
    fn vscalefsd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VSCALEFSS`.
///
/// Assembly forms: `vscalefss xmm, xmm, xmm[31:0]/m32`.
pub trait VscalefssEmitter<T0, T1, T2> {
    fn vscalefss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VSCATTERDPD`.
///
/// Assembly forms: `vscatterdpd vm32x, xmm`, `vscatterdpd vm32x, ymm`, `vscatterdpd vm32y, zmm`.
pub trait VscatterdpdEmitter<T0, T1> {
    fn vscatterdpd(&mut self, dst: T0, src: T1);
}

/// Emits `VSCATTERDPS`.
///
/// Assembly forms: `vscatterdps vm32x, xmm`, `vscatterdps vm32y, ymm`, `vscatterdps vm32z, zmm`.
pub trait VscatterdpsEmitter<T0, T1> {
    fn vscatterdps(&mut self, dst: T0, src: T1);
}

/// Emits `VSCATTERQPD`.
///
/// Assembly forms: `vscatterqpd vm64x, xmm`, `vscatterqpd vm64y, ymm`, `vscatterqpd vm64z, zmm`.
pub trait VscatterqpdEmitter<T0, T1> {
    fn vscatterqpd(&mut self, dst: T0, src: T1);
}

/// Emits `VSCATTERQPS`.
///
/// Assembly forms: `vscatterqps vm64x, xmm`, `vscatterqps vm64y, xmm`, `vscatterqps vm64z, ymm`.
pub trait VscatterqpsEmitter<T0, T1> {
    fn vscatterqps(&mut self, dst: T0, src: T1);
}

/// Emits `VSHUFF32X4`.
///
/// Assembly forms: `vshuff32x4 ymm, ymm, ymm/m256/b32, imm8`, `vshuff32x4 zmm, zmm, zmm/m512/b32, imm8`.
pub trait Vshuff32x4Emitter<T0, T1, T2, T3> {
    fn vshuff32x4(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VSHUFF64X2`.
///
/// Assembly forms: `vshuff64x2 ymm, ymm, ymm/m256/b64, imm8`, `vshuff64x2 zmm, zmm, zmm/m512/b64, imm8`.
pub trait Vshuff64x2Emitter<T0, T1, T2, T3> {
    fn vshuff64x2(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VSHUFI32X4`.
///
/// Assembly forms: `vshufi32x4 ymm, ymm, ymm/m256/b32, imm8`, `vshufi32x4 zmm, zmm, zmm/m512/b32, imm8`.
pub trait Vshufi32x4Emitter<T0, T1, T2, T3> {
    fn vshufi32x4(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VSHUFI64X2`.
///
/// Assembly forms: `vshufi64x2 ymm, ymm, ymm/m256/b64, imm8`, `vshufi64x2 zmm, zmm, zmm/m512/b64, imm8`.
pub trait Vshufi64x2Emitter<T0, T1, T2, T3> {
    fn vshufi64x2(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VSHUFPD`.
///
/// Assembly forms: `vshufpd xy, xy, xy/mxy, imm8`, `vshufpd xyz, xyz, xyz/mxyz/b64, imm8`.
pub trait VshufpdEmitter<T0, T1, T2, T3> {
    fn vshufpd(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VSHUFPS`.
///
/// Assembly forms: `vshufps xy, xy, xy/mxy, imm8`, `vshufps xyz, xyz, xyz/mxyz/b32, imm8`.
pub trait VshufpsEmitter<T0, T1, T2, T3> {
    fn vshufps(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VSQRTPD`.
///
/// Assembly forms: `vsqrtpd xy, xy/mxy`, `vsqrtpd xyz, xyz/mxyz/b64`.
pub trait VsqrtpdEmitter<T0, T1> {
    fn vsqrtpd(&mut self, dst: T0, src: T1);
}

/// Emits `VSQRTPS`.
///
/// Assembly forms: `vsqrtps xy, xy/mxy`, `vsqrtps xyz, xyz/mxyz/b32`.
pub trait VsqrtpsEmitter<T0, T1> {
    fn vsqrtps(&mut self, dst: T0, src: T1);
}

/// Emits `VSQRTSD`.
///
/// Assembly forms: `vsqrtsd xmm, xmm[127:64], xmm[63:0]/m64`.
pub trait VsqrtsdEmitter<T0, T1, T2> {
    fn vsqrtsd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VSQRTSS`.
///
/// Assembly forms: `vsqrtss xmm, xmm[127:32], xmm[31:0]/m32`.
pub trait VsqrtssEmitter<T0, T1, T2> {
    fn vsqrtss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VSTMXCSR`.
///
/// Assembly forms: `vstmxcsr m32`.
pub trait VstmxcsrEmitter<T0> {
    fn vstmxcsr(&mut self, dst: T0);
}

/// Emits `VSUBPD`.
///
/// Assembly forms: `vsubpd xy, xy, xy/mxy`, `vsubpd xyz, xyz, xyz/mxyz/b64`.
pub trait VsubpdEmitter<T0, T1, T2> {
    fn vsubpd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VSUBPS`.
///
/// Assembly forms: `vsubps xy, xy, xy/mxy`, `vsubps xyz, xyz, xyz/mxyz/b32`.
pub trait VsubpsEmitter<T0, T1, T2> {
    fn vsubps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VSUBSD`.
///
/// Assembly forms: `vsubsd xmm, xmm, xmm[63:0]/m64`.
pub trait VsubsdEmitter<T0, T1, T2> {
    fn vsubsd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VSUBSS`.
///
/// Assembly forms: `vsubss xmm, xmm, xmm[31:0]/m32`.
pub trait VsubssEmitter<T0, T1, T2> {
    fn vsubss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VTESTPD`.
///
/// Assembly forms: `vtestpd xy, xy/mxy`.
pub trait VtestpdEmitter<T0, T1> {
    fn vtestpd(&mut self, lhs: T0, rhs: T1);
}

/// Emits `VTESTPS`.
///
/// Assembly forms: `vtestps xy, xy/mxy`.
pub trait VtestpsEmitter<T0, T1> {
    fn vtestps(&mut self, lhs: T0, rhs: T1);
}

/// Emits `VUCOMISD`.
///
/// Assembly forms: `vucomisd xmm[63:0], xmm[63:0]/m64`.
pub trait VucomisdEmitter<T0, T1> {
    fn vucomisd(&mut self, lhs: T0, rhs: T1);
}

/// Emits `VUCOMISS`.
///
/// Assembly forms: `vucomiss xmm[31:0], xmm[31:0]/m32`.
pub trait VucomissEmitter<T0, T1> {
    fn vucomiss(&mut self, lhs: T0, rhs: T1);
}

/// Emits `VUNPCKHPD`.
///
/// Assembly forms: `vunpckhpd xy, xy, xy/mxy`, `vunpckhpd xyz, xyz, xyz/mxyz/b64`.
pub trait VunpckhpdEmitter<T0, T1, T2> {
    fn vunpckhpd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VUNPCKHPS`.
///
/// Assembly forms: `vunpckhps xy, xy, xy/mxy`, `vunpckhps xyz, xyz, xyz/mxyz/b32`.
pub trait VunpckhpsEmitter<T0, T1, T2> {
    fn vunpckhps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VUNPCKLPD`.
///
/// Assembly forms: `vunpcklpd xy, xy, xy/mxy`, `vunpcklpd xyz, xyz, xyz/mxyz/b64`.
pub trait VunpcklpdEmitter<T0, T1, T2> {
    fn vunpcklpd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VUNPCKLPS`.
///
/// Assembly forms: `vunpcklps xy, xy, xy/mxy`, `vunpcklps xyz, xyz, xyz/mxyz/b32`.
pub trait VunpcklpsEmitter<T0, T1, T2> {
    fn vunpcklps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VXORPD`.
///
/// Assembly forms: `vxorpd xy, xy, xy/mxy`, `vxorpd xyz, xyz, xyz/mxyz/b64`.
pub trait VxorpdEmitter<T0, T1, T2> {
    fn vxorpd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VXORPS`.
///
/// Assembly forms: `vxorps xy, xy, xy/mxy`, `vxorps xyz, xyz, xyz/mxyz/b32`.
pub trait VxorpsEmitter<T0, T1, T2> {
    fn vxorps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VZEROALL`.
///
/// Assembly forms: `vzeroall`.
pub trait VzeroallEmitter {
    fn vzeroall(&mut self);
}

/// Emits `VZEROUPPER`.
///
/// Assembly forms: `vzeroupper`.
pub trait VzeroupperEmitter {
    fn vzeroupper(&mut self);
}

/// Emits `VFMADDPD`.
///
/// Assembly forms: `vfmaddpd xy, xy, xy, xy/mxy`, `vfmaddpd xy, xy, xy/mxy, xy`.
pub trait VfmaddpdEmitter<T0, T1, T2, T3> {
    fn vfmaddpd(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VFMADDPS`.
///
/// Assembly forms: `vfmaddps xy, xy, xy, xy/mxy`, `vfmaddps xy, xy, xy/mxy, xy`.
pub trait VfmaddpsEmitter<T0, T1, T2, T3> {
    fn vfmaddps(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VFMADDSD`.
///
/// Assembly forms: `vfmaddsd xmm[63:0], xmm[63:0], xmm[63:0], xmm[63:0]/m64`, `vfmaddsd xmm[63:0], xmm[63:0], xmm[63:0]/m64, xmm[63:0]`.
pub trait VfmaddsdEmitter<T0, T1, T2, T3> {
    fn vfmaddsd(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VFMADDSS`.
///
/// Assembly forms: `vfmaddss xmm[31:0], xmm[31:0], xmm[31:0], xmm[31:0]/m32`, `vfmaddss xmm[31:0], xmm[31:0], xmm[31:0]/m32, xmm[31:0]`.
pub trait VfmaddssEmitter<T0, T1, T2, T3> {
    fn vfmaddss(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VFMADDSUBPD`.
///
/// Assembly forms: `vfmaddsubpd xy, xy, xy, xy/mxy`, `vfmaddsubpd xy, xy, xy/mxy, xy`.
pub trait VfmaddsubpdEmitter<T0, T1, T2, T3> {
    fn vfmaddsubpd(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VFMADDSUBPS`.
///
/// Assembly forms: `vfmaddsubps xy, xy, xy, xy/mxy`, `vfmaddsubps xy, xy, xy/mxy, xy`.
pub trait VfmaddsubpsEmitter<T0, T1, T2, T3> {
    fn vfmaddsubps(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VFMSUBADDPD`.
///
/// Assembly forms: `vfmsubaddpd xy, xy, xy, xy/mxy`, `vfmsubaddpd xy, xy, xy/mxy, xy`.
pub trait VfmsubaddpdEmitter<T0, T1, T2, T3> {
    fn vfmsubaddpd(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VFMSUBADDPS`.
///
/// Assembly forms: `vfmsubaddps xy, xy, xy, xy/mxy`, `vfmsubaddps xy, xy, xy/mxy, xy`.
pub trait VfmsubaddpsEmitter<T0, T1, T2, T3> {
    fn vfmsubaddps(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VFMSUBPD`.
///
/// Assembly forms: `vfmsubpd xy, xy, xy, xy/mxy`, `vfmsubpd xy, xy, xy/mxy, xy`.
pub trait VfmsubpdEmitter<T0, T1, T2, T3> {
    fn vfmsubpd(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VFMSUBPS`.
///
/// Assembly forms: `vfmsubps xy, xy, xy, xy/mxy`, `vfmsubps xy, xy, xy/mxy, xy`.
pub trait VfmsubpsEmitter<T0, T1, T2, T3> {
    fn vfmsubps(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VFMSUBSD`.
///
/// Assembly forms: `vfmsubsd xmm[63:0], xmm[63:0], xmm[63:0], xmm[63:0]/m64`, `vfmsubsd xmm[63:0], xmm[63:0], xmm[63:0]/m64, xmm[63:0]`.
pub trait VfmsubsdEmitter<T0, T1, T2, T3> {
    fn vfmsubsd(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VFMSUBSS`.
///
/// Assembly forms: `vfmsubss xmm[31:0], xmm[31:0], xmm[31:0], xmm[31:0]/m32`, `vfmsubss xmm[31:0], xmm[31:0], xmm[31:0]/m32, xmm[31:0]`.
pub trait VfmsubssEmitter<T0, T1, T2, T3> {
    fn vfmsubss(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VFNMADDPD`.
///
/// Assembly forms: `vfnmaddpd xy, xy, xy, xy/mxy`, `vfnmaddpd xy, xy, xy/mxy, xy`.
pub trait VfnmaddpdEmitter<T0, T1, T2, T3> {
    fn vfnmaddpd(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VFNMADDPS`.
///
/// Assembly forms: `vfnmaddps xy, xy, xy, xy/mxy`, `vfnmaddps xy, xy, xy/mxy, xy`.
pub trait VfnmaddpsEmitter<T0, T1, T2, T3> {
    fn vfnmaddps(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VFNMADDSD`.
///
/// Assembly forms: `vfnmaddsd xmm[63:0], xmm[63:0], xmm[63:0], xmm[63:0]/m64`, `vfnmaddsd xmm[63:0], xmm[63:0], xmm[63:0]/m64, xmm[63:0]`.
pub trait VfnmaddsdEmitter<T0, T1, T2, T3> {
    fn vfnmaddsd(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VFNMADDSS`.
///
/// Assembly forms: `vfnmaddss xmm[31:0], xmm[31:0], xmm[31:0], xmm[31:0]/m32`, `vfnmaddss xmm[31:0], xmm[31:0], xmm[31:0]/m32, xmm[31:0]`.
pub trait VfnmaddssEmitter<T0, T1, T2, T3> {
    fn vfnmaddss(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VFNMSUBPD`.
///
/// Assembly forms: `vfnmsubpd xy, xy, xy, xy/mxy`, `vfnmsubpd xy, xy, xy/mxy, xy`.
pub trait VfnmsubpdEmitter<T0, T1, T2, T3> {
    fn vfnmsubpd(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VFNMSUBPS`.
///
/// Assembly forms: `vfnmsubps xy, xy, xy, xy/mxy`, `vfnmsubps xy, xy, xy/mxy, xy`.
pub trait VfnmsubpsEmitter<T0, T1, T2, T3> {
    fn vfnmsubps(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VFNMSUBSD`.
///
/// Assembly forms: `vfnmsubsd xmm[63:0], xmm[63:0], xmm[63:0], xmm[63:0]/m64`, `vfnmsubsd xmm[63:0], xmm[63:0], xmm[63:0]/m64, xmm[63:0]`.
pub trait VfnmsubsdEmitter<T0, T1, T2, T3> {
    fn vfnmsubsd(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VFNMSUBSS`.
///
/// Assembly forms: `vfnmsubss xmm[31:0], xmm[31:0], xmm[31:0], xmm[31:0]/m32`, `vfnmsubss xmm[31:0], xmm[31:0], xmm[31:0]/m32, xmm[31:0]`.
pub trait VfnmsubssEmitter<T0, T1, T2, T3> {
    fn vfnmsubss(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VFRCZPD`.
///
/// Assembly forms: `vfrczpd xmm, xmm/m128`, `vfrczpd ymm, ymm/m256`.
pub trait VfrczpdEmitter<T0, T1> {
    fn vfrczpd(&mut self, dst: T0, src: T1);
}

/// Emits `VFRCZPS`.
///
/// Assembly forms: `vfrczps xmm, xmm/m128`, `vfrczps ymm, ymm/m256`.
pub trait VfrczpsEmitter<T0, T1> {
    fn vfrczps(&mut self, dst: T0, src: T1);
}

/// Emits `VFRCZSD`.
///
/// Assembly forms: `vfrczsd xmm[63:0], xmm[63:0]/m64`.
pub trait VfrczsdEmitter<T0, T1> {
    fn vfrczsd(&mut self, dst: T0, src: T1);
}

/// Emits `VFRCZSS`.
///
/// Assembly forms: `vfrczss xmm[31:0], xmm[31:0]/m32`.
pub trait VfrczssEmitter<T0, T1> {
    fn vfrczss(&mut self, dst: T0, src: T1);
}

/// Emits `VPCMOV`.
///
/// Assembly forms: `vpcmov xmm, xmm, xmm, xmm/m128`, `vpcmov xmm, xmm, xmm/m128, xmm`, `vpcmov ymm, ymm, ymm, ymm/m256`.
pub trait VpcmovEmitter<T0, T1, T2, T3> {
    fn vpcmov(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VPCOMB`.
///
/// Assembly forms: `vpcomb xmm, xmm, xmm/m128, imm8`.
pub trait VpcombEmitter<T0, T1, T2, T3> {
    fn vpcomb(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPCOMD`.
///
/// Assembly forms: `vpcomd xmm, xmm, xmm/m128, imm8`.
pub trait VpcomdEmitter<T0, T1, T2, T3> {
    fn vpcomd(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPCOMQ`.
///
/// Assembly forms: `vpcomq xmm, xmm, xmm/m128, imm8`.
pub trait VpcomqEmitter<T0, T1, T2, T3> {
    fn vpcomq(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPCOMW`.
///
/// Assembly forms: `vpcomw xmm, xmm, xmm/m128, imm8`.
pub trait VpcomwEmitter<T0, T1, T2, T3> {
    fn vpcomw(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPCOMUB`.
///
/// Assembly forms: `vpcomub xmm, xmm, xmm/m128, imm8`.
pub trait VpcomubEmitter<T0, T1, T2, T3> {
    fn vpcomub(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPCOMUD`.
///
/// Assembly forms: `vpcomud xmm, xmm, xmm/m128, imm8`.
pub trait VpcomudEmitter<T0, T1, T2, T3> {
    fn vpcomud(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPCOMUQ`.
///
/// Assembly forms: `vpcomuq xmm, xmm, xmm/m128, imm8`.
pub trait VpcomuqEmitter<T0, T1, T2, T3> {
    fn vpcomuq(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPCOMUW`.
///
/// Assembly forms: `vpcomuw xmm, xmm, xmm/m128, imm8`.
pub trait VpcomuwEmitter<T0, T1, T2, T3> {
    fn vpcomuw(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VPERMIL2PD`.
///
/// Assembly forms: `vpermil2pd xmm, xmm, xmm/m128, xmm, imm4`, `vpermil2pd xmm, xmm, xmm, xmm/m128, imm4`, `vpermil2pd ymm, ymm, ymm/m256, ymm, imm4`.
pub trait Vpermil2pdEmitter<T0, T1, T2, T3, T4> {
    fn vpermil2pd(&mut self, dst: T0, src1: T1, src2: T2, src3: T3, imm: T4);
}

/// Emits `VPERMIL2PS`.
///
/// Assembly forms: `vpermil2ps xmm, xmm, xmm/m128, xmm, imm4`, `vpermil2ps xmm, xmm, xmm, xmm/m128, imm4`, `vpermil2ps ymm, ymm, ymm/m256, ymm, imm4`.
pub trait Vpermil2psEmitter<T0, T1, T2, T3, T4> {
    fn vpermil2ps(&mut self, dst: T0, src1: T1, src2: T2, src3: T3, imm: T4);
}

/// Emits `VPHADDBD`.
///
/// Assembly forms: `vphaddbd xmm, xmm/m128`.
pub trait VphaddbdEmitter<T0, T1> {
    fn vphaddbd(&mut self, dst: T0, src: T1);
}

/// Emits `VPHADDBQ`.
///
/// Assembly forms: `vphaddbq xmm, xmm/m128`.
pub trait VphaddbqEmitter<T0, T1> {
    fn vphaddbq(&mut self, dst: T0, src: T1);
}

/// Emits `VPHADDBW`.
///
/// Assembly forms: `vphaddbw xmm, xmm/m128`.
pub trait VphaddbwEmitter<T0, T1> {
    fn vphaddbw(&mut self, dst: T0, src: T1);
}

/// Emits `VPHADDDQ`.
///
/// Assembly forms: `vphadddq xmm, xmm/m128`.
pub trait VphadddqEmitter<T0, T1> {
    fn vphadddq(&mut self, dst: T0, src: T1);
}

/// Emits `VPHADDWD`.
///
/// Assembly forms: `vphaddwd xmm, xmm/m128`.
pub trait VphaddwdEmitter<T0, T1> {
    fn vphaddwd(&mut self, dst: T0, src: T1);
}

/// Emits `VPHADDWQ`.
///
/// Assembly forms: `vphaddwq xmm, xmm/m128`.
pub trait VphaddwqEmitter<T0, T1> {
    fn vphaddwq(&mut self, dst: T0, src: T1);
}

/// Emits `VPHADDUBD`.
///
/// Assembly forms: `vphaddubd xmm, xmm/m128`.
pub trait VphaddubdEmitter<T0, T1> {
    fn vphaddubd(&mut self, dst: T0, src: T1);
}

/// Emits `VPHADDUBQ`.
///
/// Assembly forms: `vphaddubq xmm, xmm/m128`.
pub trait VphaddubqEmitter<T0, T1> {
    fn vphaddubq(&mut self, dst: T0, src: T1);
}

/// Emits `VPHADDUBW`.
///
/// Assembly forms: `vphaddubw xmm, xmm/m128`.
pub trait VphaddubwEmitter<T0, T1> {
    fn vphaddubw(&mut self, dst: T0, src: T1);
}

/// Emits `VPHADDUDQ`.
///
/// Assembly forms: `vphaddudq xmm, xmm/m128`.
pub trait VphaddudqEmitter<T0, T1> {
    fn vphaddudq(&mut self, dst: T0, src: T1);
}

/// Emits `VPHADDUWD`.
///
/// Assembly forms: `vphadduwd xmm, xmm/m128`.
pub trait VphadduwdEmitter<T0, T1> {
    fn vphadduwd(&mut self, dst: T0, src: T1);
}

/// Emits `VPHADDUWQ`.
///
/// Assembly forms: `vphadduwq xmm, xmm/m128`.
pub trait VphadduwqEmitter<T0, T1> {
    fn vphadduwq(&mut self, dst: T0, src: T1);
}

/// Emits `VPHSUBBW`.
///
/// Assembly forms: `vphsubbw xmm, xmm/m128`.
pub trait VphsubbwEmitter<T0, T1> {
    fn vphsubbw(&mut self, dst: T0, src: T1);
}

/// Emits `VPHSUBDQ`.
///
/// Assembly forms: `vphsubdq xmm, xmm/m128`.
pub trait VphsubdqEmitter<T0, T1> {
    fn vphsubdq(&mut self, dst: T0, src: T1);
}

/// Emits `VPHSUBWD`.
///
/// Assembly forms: `vphsubwd xmm, xmm/m128`.
pub trait VphsubwdEmitter<T0, T1> {
    fn vphsubwd(&mut self, dst: T0, src: T1);
}

/// Emits `VPMACSDD`.
///
/// Assembly forms: `vpmacsdd xmm, xmm, xmm/m128, xmm`.
pub trait VpmacsddEmitter<T0, T1, T2, T3> {
    fn vpmacsdd(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VPMACSDQH`.
///
/// Assembly forms: `vpmacsdqh xmm, xmm, xmm/m128, xmm`.
pub trait VpmacsdqhEmitter<T0, T1, T2, T3> {
    fn vpmacsdqh(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VPMACSDQL`.
///
/// Assembly forms: `vpmacsdql xmm, xmm, xmm/m128, xmm`.
pub trait VpmacsdqlEmitter<T0, T1, T2, T3> {
    fn vpmacsdql(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VPMACSWD`.
///
/// Assembly forms: `vpmacswd xmm, xmm, xmm/m128, xmm`.
pub trait VpmacswdEmitter<T0, T1, T2, T3> {
    fn vpmacswd(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VPMACSWW`.
///
/// Assembly forms: `vpmacsww xmm, xmm, xmm/m128, xmm`.
pub trait VpmacswwEmitter<T0, T1, T2, T3> {
    fn vpmacsww(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VPMACSSDD`.
///
/// Assembly forms: `vpmacssdd xmm, xmm, xmm/m128, xmm`.
pub trait VpmacssddEmitter<T0, T1, T2, T3> {
    fn vpmacssdd(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VPMACSSDQH`.
///
/// Assembly forms: `vpmacssdqh xmm, xmm, xmm/m128, xmm`.
pub trait VpmacssdqhEmitter<T0, T1, T2, T3> {
    fn vpmacssdqh(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VPMACSSDQL`.
///
/// Assembly forms: `vpmacssdql xmm, xmm, xmm/m128, xmm`.
pub trait VpmacssdqlEmitter<T0, T1, T2, T3> {
    fn vpmacssdql(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VPMACSSWD`.
///
/// Assembly forms: `vpmacsswd xmm, xmm, xmm/m128, xmm`.
pub trait VpmacsswdEmitter<T0, T1, T2, T3> {
    fn vpmacsswd(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VPMACSSWW`.
///
/// Assembly forms: `vpmacssww xmm, xmm, xmm/m128, xmm`.
pub trait VpmacsswwEmitter<T0, T1, T2, T3> {
    fn vpmacssww(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VPMADCSSWD`.
///
/// Assembly forms: `vpmadcsswd xmm, xmm, xmm/m128, xmm`.
pub trait VpmadcsswdEmitter<T0, T1, T2, T3> {
    fn vpmadcsswd(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VPMADCSWD`.
///
/// Assembly forms: `vpmadcswd xmm, xmm, xmm/m128, xmm`.
pub trait VpmadcswdEmitter<T0, T1, T2, T3> {
    fn vpmadcswd(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VPPERM`.
///
/// Assembly forms: `vpperm xmm, xmm, xmm, xmm/m128`, `vpperm xmm, xmm, xmm/m128, xmm`.
pub trait VppermEmitter<T0, T1, T2, T3> {
    fn vpperm(&mut self, dst: T0, src1: T1, src2: T2, src3: T3);
}

/// Emits `VPROTB`.
///
/// Assembly forms: `vprotb xmm, xmm, xmm/m128`, `vprotb xmm, xmm/m128, imm8`, `vprotb xmm, xmm/m128, xmm`.
pub trait VprotbEmitter<T0, T1, T2> {
    fn vprotb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPROTD`.
///
/// Assembly forms: `vprotd xmm, xmm, xmm/m128`, `vprotd xmm, xmm/m128, imm8`, `vprotd xmm, xmm/m128, xmm`.
pub trait VprotdEmitter<T0, T1, T2> {
    fn vprotd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPROTQ`.
///
/// Assembly forms: `vprotq xmm, xmm, xmm/m128`, `vprotq xmm, xmm/m128, imm8`, `vprotq xmm, xmm/m128, xmm`.
pub trait VprotqEmitter<T0, T1, T2> {
    fn vprotq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPROTW`.
///
/// Assembly forms: `vprotw xmm, xmm, xmm/m128`, `vprotw xmm, xmm/m128, imm8`, `vprotw xmm, xmm/m128, xmm`.
pub trait VprotwEmitter<T0, T1, T2> {
    fn vprotw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSHAB`.
///
/// Assembly forms: `vpshab xmm, xmm, xmm/m128`, `vpshab xmm, xmm/m128, xmm`.
pub trait VpshabEmitter<T0, T1, T2> {
    fn vpshab(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSHAD`.
///
/// Assembly forms: `vpshad xmm, xmm, xmm/m128`, `vpshad xmm, xmm/m128, xmm`.
pub trait VpshadEmitter<T0, T1, T2> {
    fn vpshad(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSHAQ`.
///
/// Assembly forms: `vpshaq xmm, xmm, xmm/m128`, `vpshaq xmm, xmm/m128, xmm`.
pub trait VpshaqEmitter<T0, T1, T2> {
    fn vpshaq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSHAW`.
///
/// Assembly forms: `vpshaw xmm, xmm, xmm/m128`, `vpshaw xmm, xmm/m128, xmm`.
pub trait VpshawEmitter<T0, T1, T2> {
    fn vpshaw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSHLB`.
///
/// Assembly forms: `vpshlb xmm, xmm, xmm/m128`, `vpshlb xmm, xmm/m128, xmm`.
pub trait VpshlbEmitter<T0, T1, T2> {
    fn vpshlb(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSHLD`.
///
/// Assembly forms: `vpshld xmm, xmm, xmm/m128`, `vpshld xmm, xmm/m128, xmm`.
pub trait VpshldEmitter<T0, T1, T2> {
    fn vpshld(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSHLQ`.
///
/// Assembly forms: `vpshlq xmm, xmm, xmm/m128`, `vpshlq xmm, xmm/m128, xmm`.
pub trait VpshlqEmitter<T0, T1, T2> {
    fn vpshlq(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPSHLW`.
///
/// Assembly forms: `vpshlw xmm, xmm, xmm/m128`, `vpshlw xmm, xmm/m128, xmm`.
pub trait VpshlwEmitter<T0, T1, T2> {
    fn vpshlw(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VBCSTNEBF162PS`.
///
/// Assembly forms: `vbcstnebf162ps xy, m16`.
pub trait Vbcstnebf162psEmitter<T0, T1> {
    fn vbcstnebf162ps(&mut self, dst: T0, src: T1);
}

/// Emits `VBCSTNESH2PS`.
///
/// Assembly forms: `vbcstnesh2ps W:xy, m16`.
pub trait Vbcstnesh2psEmitter<T0, T1> {
    fn vbcstnesh2ps(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTNEEBF162PS`.
///
/// Assembly forms: `vcvtneebf162ps xy, mxy`.
pub trait Vcvtneebf162psEmitter<T0, T1> {
    fn vcvtneebf162ps(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTNEEPH2PS`.
///
/// Assembly forms: `vcvtneeph2ps W:xy, mxy`.
pub trait Vcvtneeph2psEmitter<T0, T1> {
    fn vcvtneeph2ps(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTNEOBF162PS`.
///
/// Assembly forms: `vcvtneobf162ps xy, mxy`.
pub trait Vcvtneobf162psEmitter<T0, T1> {
    fn vcvtneobf162ps(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTNEOPH2PS`.
///
/// Assembly forms: `vcvtneoph2ps W:xy, mxy`.
pub trait Vcvtneoph2psEmitter<T0, T1> {
    fn vcvtneoph2ps(&mut self, dst: T0, src: T1);
}

/// Emits `VPDPBSSD`.
///
/// Assembly forms: `vpdpbssd X:xy, xy, xy/mxy`, `vpdpbssd X:xyz, xyz, xyz/mxyz/b32`.
pub trait VpdpbssdEmitter<T0, T1, T2> {
    fn vpdpbssd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPDPBSSDS`.
///
/// Assembly forms: `vpdpbssds xy, xy, xy/mxy`, `vpdpbssds xyz, xyz, xyz/mxyz/b32`.
pub trait VpdpbssdsEmitter<T0, T1, T2> {
    fn vpdpbssds(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPDPBSUD`.
///
/// Assembly forms: `vpdpbsud X:xy, xy, xy/mxy`, `vpdpbsud X:xyz, xyz, xyz/mxyz/b32`.
pub trait VpdpbsudEmitter<T0, T1, T2> {
    fn vpdpbsud(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPDPBSUDS`.
///
/// Assembly forms: `vpdpbsuds xy, xy, xy/mxy`, `vpdpbsuds xyz, xyz, xyz/mxyz/b32`.
pub trait VpdpbsudsEmitter<T0, T1, T2> {
    fn vpdpbsuds(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPDPBUUD`.
///
/// Assembly forms: `vpdpbuud X:xy, xy, xy/mxy`, `vpdpbuud X:xyz, xyz, xyz/mxyz/b32`.
pub trait VpdpbuudEmitter<T0, T1, T2> {
    fn vpdpbuud(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPDPBUUDS`.
///
/// Assembly forms: `vpdpbuuds xy, xy, xy/mxy`, `vpdpbuuds xyz, xyz, xyz/mxyz/b32`.
pub trait VpdpbuudsEmitter<T0, T1, T2> {
    fn vpdpbuuds(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPDPWSUD`.
///
/// Assembly forms: `vpdpwsud X:xy, xy, xy/mxy`, `vpdpwsud X:xyz, xyz, xyz/mxyz/b32`.
pub trait VpdpwsudEmitter<T0, T1, T2> {
    fn vpdpwsud(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPDPWSUDS`.
///
/// Assembly forms: `vpdpwsuds xy, xy, xy/mxy`, `vpdpwsuds xyz, xyz, xyz/mxyz/b32`.
pub trait VpdpwsudsEmitter<T0, T1, T2> {
    fn vpdpwsuds(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPDPWUSD`.
///
/// Assembly forms: `vpdpwusd X:xy, xy, xy/mxy`, `vpdpwusd X:xyz, xyz, xyz/mxyz/b32`.
pub trait VpdpwusdEmitter<T0, T1, T2> {
    fn vpdpwusd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPDPWUSDS`.
///
/// Assembly forms: `vpdpwusds xy, xy, xy/mxy`, `vpdpwusds xyz, xyz, xyz/mxyz/b32`.
pub trait VpdpwusdsEmitter<T0, T1, T2> {
    fn vpdpwusds(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPDPWUUD`.
///
/// Assembly forms: `vpdpwuud X:xy, xy, xy/mxy`, `vpdpwuud X:xyz, xyz, xyz/mxyz/b32`.
pub trait VpdpwuudEmitter<T0, T1, T2> {
    fn vpdpwuud(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VPDPWUUDS`.
///
/// Assembly forms: `vpdpwuuds xy, xy, xy/mxy`, `vpdpwuuds xyz, xyz, xyz/mxyz/b32`.
pub trait VpdpwuudsEmitter<T0, T1, T2> {
    fn vpdpwuuds(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VSHA512MSG1`.
///
/// Assembly forms: `vsha512msg1 X:ymm, xmm`.
pub trait Vsha512msg1Emitter<T0, T1> {
    fn vsha512msg1(&mut self, dst: T0, src: T1);
}

/// Emits `VSHA512MSG2`.
///
/// Assembly forms: `vsha512msg2 X:ymm, ymm`.
pub trait Vsha512msg2Emitter<T0, T1> {
    fn vsha512msg2(&mut self, dst: T0, src: T1);
}

/// Emits `VSHA512RNDS2`.
///
/// Assembly forms: `vsha512rnds2 ymm, ymm, xmm`.
pub trait Vsha512rnds2Emitter<T0, T1, T2> {
    fn vsha512rnds2(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VSM3MSG1`.
///
/// Assembly forms: `vsm3msg1 X:xmm, xmm, xmm/m128`.
pub trait Vsm3msg1Emitter<T0, T1, T2> {
    fn vsm3msg1(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VSM3MSG2`.
///
/// Assembly forms: `vsm3msg2 X:xmm, xmm, xmm/m128`.
pub trait Vsm3msg2Emitter<T0, T1, T2> {
    fn vsm3msg2(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VSM3RNDS2`.
///
/// Assembly forms: `vsm3rnds2 xmm, xmm, xmm/m128, imm8`.
pub trait Vsm3rnds2Emitter<T0, T1, T2, T3> {
    fn vsm3rnds2(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VSM4KEY4`.
///
/// Assembly forms: `vsm4key4 W:xy, xy, xy/mxy`, `vsm4key4 W:xyz, xyz, xyz/mxyz`.
pub trait Vsm4key4Emitter<T0, T1, T2> {
    fn vsm4key4(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VSM4RNDS4`.
///
/// Assembly forms: `vsm4rnds4 xy, xy, xy/mxy`, `vsm4rnds4 xyz, xyz, xyz/mxyz`.
pub trait Vsm4rnds4Emitter<T0, T1, T2> {
    fn vsm4rnds4(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VADDPH`.
///
/// Assembly forms: `vaddph xyz, xyz, xyz/mxyz/b16`.
pub trait VaddphEmitter<T0, T1, T2> {
    fn vaddph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VADDSH`.
///
/// Assembly forms: `vaddsh xmm, xmm, xmm[15:0]/m16`.
pub trait VaddshEmitter<T0, T1, T2> {
    fn vaddsh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VCMPPH`.
///
/// Assembly forms: `vcmpph k, xyz, xyz/mxyz/b16, imm8`.
pub trait VcmpphEmitter<T0, T1, T2, T3> {
    fn vcmpph(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VCMPSH`.
///
/// Assembly forms: `vcmpsh k, xmm[15:0], xmm[15:0]/m16, imm8`.
pub trait VcmpshEmitter<T0, T1, T2, T3> {
    fn vcmpsh(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VCOMISH`.
///
/// Assembly forms: `vcomish xmm[15:0], xmm[15:0]/m16`.
pub trait VcomishEmitter<T0, T1> {
    fn vcomish(&mut self, lhs: T0, rhs: T1);
}

/// Emits `VCVTDQ2PH`.
///
/// Assembly forms: `vcvtdq2ph xxy, xyz/mxyz/b32`.
pub trait Vcvtdq2phEmitter<T0, T1> {
    fn vcvtdq2ph(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTPD2PH`.
///
/// Assembly forms: `vcvtpd2ph xxx, xyz/mxyz/b64`.
pub trait Vcvtpd2phEmitter<T0, T1> {
    fn vcvtpd2ph(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTPH2DQ`.
///
/// Assembly forms: `vcvtph2dq xyz, xxy/mxxy/b16`.
pub trait Vcvtph2dqEmitter<T0, T1> {
    fn vcvtph2dq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTPH2PD`.
///
/// Assembly forms: `vcvtph2pd xyz, xxx/mxxx/b16`.
pub trait Vcvtph2pdEmitter<T0, T1> {
    fn vcvtph2pd(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTPH2PSX`.
///
/// Assembly forms: `vcvtph2psx xyz, xxy/mxxy/b16`.
pub trait Vcvtph2psxEmitter<T0, T1> {
    fn vcvtph2psx(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTPH2QQ`.
///
/// Assembly forms: `vcvtph2qq xyz, xxx/mxxx/b16`.
pub trait Vcvtph2qqEmitter<T0, T1> {
    fn vcvtph2qq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTPH2UDQ`.
///
/// Assembly forms: `vcvtph2udq xyz, xxy/mxxy/b16`.
pub trait Vcvtph2udqEmitter<T0, T1> {
    fn vcvtph2udq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTPH2UQQ`.
///
/// Assembly forms: `vcvtph2uqq xyz, xxx/mxxx/b16`.
pub trait Vcvtph2uqqEmitter<T0, T1> {
    fn vcvtph2uqq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTPH2UW`.
///
/// Assembly forms: `vcvtph2uw xyz, xyz/mxyz/b16`.
pub trait Vcvtph2uwEmitter<T0, T1> {
    fn vcvtph2uw(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTPH2W`.
///
/// Assembly forms: `vcvtph2w xyz, xyz/mxyz/b16`.
pub trait Vcvtph2wEmitter<T0, T1> {
    fn vcvtph2w(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTPS2PHX`.
///
/// Assembly forms: `vcvtps2phx xxy, xyz/mxyz/b32`.
pub trait Vcvtps2phxEmitter<T0, T1> {
    fn vcvtps2phx(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTQQ2PH`.
///
/// Assembly forms: `vcvtqq2ph xmm, xyz/mxyz/b64`.
pub trait Vcvtqq2phEmitter<T0, T1> {
    fn vcvtqq2ph(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTSD2SH`.
///
/// Assembly forms: `vcvtsd2sh xmm, xmm, xmm[63:0]/m64`.
pub trait Vcvtsd2shEmitter<T0, T1, T2> {
    fn vcvtsd2sh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VCVTSH2SD`.
///
/// Assembly forms: `vcvtsh2sd xmm, xmm, xmm[15:0]/m16`.
pub trait Vcvtsh2sdEmitter<T0, T1, T2> {
    fn vcvtsh2sd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VCVTSH2SI`.
///
/// Assembly forms: `vcvtsh2si ry, xmm[15:0]/m16`.
pub trait Vcvtsh2siEmitter<T0, T1> {
    fn vcvtsh2si(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTSH2SS`.
///
/// Assembly forms: `vcvtsh2ss xmm, xmm, xmm/m16`.
pub trait Vcvtsh2ssEmitter<T0, T1, T2> {
    fn vcvtsh2ss(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VCVTSH2USI`.
///
/// Assembly forms: `vcvtsh2usi ry, xmm[15:0]/m16`.
pub trait Vcvtsh2usiEmitter<T0, T1> {
    fn vcvtsh2usi(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTSI2SH`.
///
/// Assembly forms: `vcvtsi2sh xmm, xmm, ry/my`.
pub trait Vcvtsi2shEmitter<T0, T1, T2> {
    fn vcvtsi2sh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VCVTSS2SH`.
///
/// Assembly forms: `vcvtss2sh xmm, xmm, xmm/m32`.
pub trait Vcvtss2shEmitter<T0, T1, T2> {
    fn vcvtss2sh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VCVTTPH2DQ`.
///
/// Assembly forms: `vcvttph2dq xyz, xxy/mxxy/b16`.
pub trait Vcvttph2dqEmitter<T0, T1> {
    fn vcvttph2dq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTTPH2QQ`.
///
/// Assembly forms: `vcvttph2qq xyz, xxx/mxxx/b16`.
pub trait Vcvttph2qqEmitter<T0, T1> {
    fn vcvttph2qq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTTPH2UDQ`.
///
/// Assembly forms: `vcvttph2udq xyz, xxy/mxxy/b16`.
pub trait Vcvttph2udqEmitter<T0, T1> {
    fn vcvttph2udq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTTPH2UQQ`.
///
/// Assembly forms: `vcvttph2uqq xyz, xxx/mxxx/b16`.
pub trait Vcvttph2uqqEmitter<T0, T1> {
    fn vcvttph2uqq(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTTPH2UW`.
///
/// Assembly forms: `vcvttph2uw xyz, xyz/mxyz/b16`.
pub trait Vcvttph2uwEmitter<T0, T1> {
    fn vcvttph2uw(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTTPH2W`.
///
/// Assembly forms: `vcvttph2w xyz, xyz/mxyz/b16`.
pub trait Vcvttph2wEmitter<T0, T1> {
    fn vcvttph2w(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTTSH2SI`.
///
/// Assembly forms: `vcvttsh2si ry, xmm[15:0]/m16`.
pub trait Vcvttsh2siEmitter<T0, T1> {
    fn vcvttsh2si(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTTSH2USI`.
///
/// Assembly forms: `vcvttsh2usi ry, xmm[15:0]/m16`.
pub trait Vcvttsh2usiEmitter<T0, T1> {
    fn vcvttsh2usi(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTUDQ2PH`.
///
/// Assembly forms: `vcvtudq2ph xxy, xyz/mxyz/b32`.
pub trait Vcvtudq2phEmitter<T0, T1> {
    fn vcvtudq2ph(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTUQQ2PH`.
///
/// Assembly forms: `vcvtuqq2ph xxx, xyz/mxyz/b64`.
pub trait Vcvtuqq2phEmitter<T0, T1> {
    fn vcvtuqq2ph(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTUSI2SH`.
///
/// Assembly forms: `vcvtusi2sh xmm, xmm, ry/my`.
pub trait Vcvtusi2shEmitter<T0, T1, T2> {
    fn vcvtusi2sh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VCVTUW2PH`.
///
/// Assembly forms: `vcvtuw2ph xyz, xyz/mxyz/b16`.
pub trait Vcvtuw2phEmitter<T0, T1> {
    fn vcvtuw2ph(&mut self, dst: T0, src: T1);
}

/// Emits `VCVTW2PH`.
///
/// Assembly forms: `vcvtw2ph xyz, xyz/mxyz/b16`.
pub trait Vcvtw2phEmitter<T0, T1> {
    fn vcvtw2ph(&mut self, dst: T0, src: T1);
}

/// Emits `VDIVPH`.
///
/// Assembly forms: `vdivph xyz, xyz, xyz/mxyz/b16`.
pub trait VdivphEmitter<T0, T1, T2> {
    fn vdivph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VDIVSH`.
///
/// Assembly forms: `vdivsh xmm, xmm, xmm[15:0]/m16`.
pub trait VdivshEmitter<T0, T1, T2> {
    fn vdivsh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFCMADDCPH`.
///
/// Assembly forms: `vfcmaddcph xyz, xyz, xyz/mxyz/b32`.
pub trait VfcmaddcphEmitter<T0, T1, T2> {
    fn vfcmaddcph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFCMADDCSH`.
///
/// Assembly forms: `vfcmaddcsh xmm, xmm, xmm/m32`.
pub trait VfcmaddcshEmitter<T0, T1, T2> {
    fn vfcmaddcsh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFCMULCPH`.
///
/// Assembly forms: `vfcmulcph xyz, xyz, xyz/mxyz/b32`.
pub trait VfcmulcphEmitter<T0, T1, T2> {
    fn vfcmulcph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFCMULCSH`.
///
/// Assembly forms: `vfcmulcsh xmm, xmm, xmm/m32`.
pub trait VfcmulcshEmitter<T0, T1, T2> {
    fn vfcmulcsh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADD132PH`.
///
/// Assembly forms: `vfmadd132ph xyz, xyz, xyz/mxyz/b16`.
pub trait Vfmadd132phEmitter<T0, T1, T2> {
    fn vfmadd132ph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADD132SH`.
///
/// Assembly forms: `vfmadd132sh xmm, xmm, xmm/m16`.
pub trait Vfmadd132shEmitter<T0, T1, T2> {
    fn vfmadd132sh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADD213PH`.
///
/// Assembly forms: `vfmadd213ph xyz, xyz, xyz/mxyz/b16`.
pub trait Vfmadd213phEmitter<T0, T1, T2> {
    fn vfmadd213ph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADD213SH`.
///
/// Assembly forms: `vfmadd213sh xmm, xmm, xmm/m16`.
pub trait Vfmadd213shEmitter<T0, T1, T2> {
    fn vfmadd213sh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADD231PH`.
///
/// Assembly forms: `vfmadd231ph xyz, xyz, xyz/mxyz/b16`.
pub trait Vfmadd231phEmitter<T0, T1, T2> {
    fn vfmadd231ph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADD231SH`.
///
/// Assembly forms: `vfmadd231sh xmm, xmm, xmm/m16`.
pub trait Vfmadd231shEmitter<T0, T1, T2> {
    fn vfmadd231sh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADDCPH`.
///
/// Assembly forms: `vfmaddcph xyz, xyz, xyz/mxyz/b32`.
pub trait VfmaddcphEmitter<T0, T1, T2> {
    fn vfmaddcph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADDCSH`.
///
/// Assembly forms: `vfmaddcsh xmm, xmm, xmm/m32`.
pub trait VfmaddcshEmitter<T0, T1, T2> {
    fn vfmaddcsh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADDSUB132PH`.
///
/// Assembly forms: `vfmaddsub132ph xyz, xyz, xyz/mxyz/b16`.
pub trait Vfmaddsub132phEmitter<T0, T1, T2> {
    fn vfmaddsub132ph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADDSUB213PH`.
///
/// Assembly forms: `vfmaddsub213ph xyz, xyz, xyz/mxyz/b16`.
pub trait Vfmaddsub213phEmitter<T0, T1, T2> {
    fn vfmaddsub213ph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMADDSUB231PH`.
///
/// Assembly forms: `vfmaddsub231ph xyz, xyz, xyz/mxyz/b16`.
pub trait Vfmaddsub231phEmitter<T0, T1, T2> {
    fn vfmaddsub231ph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUB132PH`.
///
/// Assembly forms: `vfmsub132ph xyz, xyz, xyz/mxyz/b16`.
pub trait Vfmsub132phEmitter<T0, T1, T2> {
    fn vfmsub132ph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUB132SH`.
///
/// Assembly forms: `vfmsub132sh xmm, xmm, xmm[15:0]/m16`.
pub trait Vfmsub132shEmitter<T0, T1, T2> {
    fn vfmsub132sh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUB213PH`.
///
/// Assembly forms: `vfmsub213ph xyz, xyz, xyz/mxyz/b16`.
pub trait Vfmsub213phEmitter<T0, T1, T2> {
    fn vfmsub213ph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUB213SH`.
///
/// Assembly forms: `vfmsub213sh xmm, xmm, xmm[15:0]/m16`.
pub trait Vfmsub213shEmitter<T0, T1, T2> {
    fn vfmsub213sh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUB231PH`.
///
/// Assembly forms: `vfmsub231ph xyz, xyz, xyz/mxyz/b16`.
pub trait Vfmsub231phEmitter<T0, T1, T2> {
    fn vfmsub231ph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUB231SH`.
///
/// Assembly forms: `vfmsub231sh xmm, xmm, xmm[15:0]/m16`.
pub trait Vfmsub231shEmitter<T0, T1, T2> {
    fn vfmsub231sh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUBADD132PH`.
///
/// Assembly forms: `vfmsubadd132ph xyz, xyz, xyz/mxyz/b16`.
pub trait Vfmsubadd132phEmitter<T0, T1, T2> {
    fn vfmsubadd132ph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUBADD213PH`.
///
/// Assembly forms: `vfmsubadd213ph xyz, xyz, xyz/mxyz/b16`.
pub trait Vfmsubadd213phEmitter<T0, T1, T2> {
    fn vfmsubadd213ph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMSUBADD231PH`.
///
/// Assembly forms: `vfmsubadd231ph xyz, xyz, xyz/mxyz/b16`.
pub trait Vfmsubadd231phEmitter<T0, T1, T2> {
    fn vfmsubadd231ph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMULCPH`.
///
/// Assembly forms: `vfmulcph xyz, xyz, xyz/mxyz/b32`.
pub trait VfmulcphEmitter<T0, T1, T2> {
    fn vfmulcph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFMULCSH`.
///
/// Assembly forms: `vfmulcsh xmm, xmm, xmm/m32`.
pub trait VfmulcshEmitter<T0, T1, T2> {
    fn vfmulcsh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMADD132PH`.
///
/// Assembly forms: `vfnmadd132ph xyz, xyz, xyz/mxyz/b16`.
pub trait Vfnmadd132phEmitter<T0, T1, T2> {
    fn vfnmadd132ph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMADD132SH`.
///
/// Assembly forms: `vfnmadd132sh xmm, xmm, xmm/m16`.
pub trait Vfnmadd132shEmitter<T0, T1, T2> {
    fn vfnmadd132sh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMADD213PH`.
///
/// Assembly forms: `vfnmadd213ph xyz, xyz, xyz/mxyz/b16`.
pub trait Vfnmadd213phEmitter<T0, T1, T2> {
    fn vfnmadd213ph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMADD213SH`.
///
/// Assembly forms: `vfnmadd213sh xmm, xmm, xmm/m16`.
pub trait Vfnmadd213shEmitter<T0, T1, T2> {
    fn vfnmadd213sh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMADD231PH`.
///
/// Assembly forms: `vfnmadd231ph xyz, xyz, xyz/mxyz/b16`.
pub trait Vfnmadd231phEmitter<T0, T1, T2> {
    fn vfnmadd231ph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMADD231SH`.
///
/// Assembly forms: `vfnmadd231sh xmm, xmm, xmm/m16`.
pub trait Vfnmadd231shEmitter<T0, T1, T2> {
    fn vfnmadd231sh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMSUB132PH`.
///
/// Assembly forms: `vfnmsub132ph xyz, xyz, xyz/mxyz/b16`.
pub trait Vfnmsub132phEmitter<T0, T1, T2> {
    fn vfnmsub132ph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMSUB132SH`.
///
/// Assembly forms: `vfnmsub132sh xmm, xmm, xmm[15:0]/m16`.
pub trait Vfnmsub132shEmitter<T0, T1, T2> {
    fn vfnmsub132sh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMSUB213PH`.
///
/// Assembly forms: `vfnmsub213ph xyz, xyz, xyz/mxyz/b16`.
pub trait Vfnmsub213phEmitter<T0, T1, T2> {
    fn vfnmsub213ph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMSUB213SH`.
///
/// Assembly forms: `vfnmsub213sh xmm, xmm, xmm[15:0]/m16`.
pub trait Vfnmsub213shEmitter<T0, T1, T2> {
    fn vfnmsub213sh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMSUB231PH`.
///
/// Assembly forms: `vfnmsub231ph xyz, xyz, xyz/mxyz/b16`.
pub trait Vfnmsub231phEmitter<T0, T1, T2> {
    fn vfnmsub231ph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFNMSUB231SH`.
///
/// Assembly forms: `vfnmsub231sh xmm, xmm, xmm[15:0]/m16`.
pub trait Vfnmsub231shEmitter<T0, T1, T2> {
    fn vfnmsub231sh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VFPCLASSPH`.
///
/// Assembly forms: `vfpclassph k, xyz/mxyz/b16, imm8`.
pub trait VfpclassphEmitter<T0, T1, T2> {
    fn vfpclassph(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VFPCLASSSH`.
///
/// Assembly forms: `vfpclasssh k, xmm[15:0]/m16, imm8`.
pub trait VfpclassshEmitter<T0, T1, T2> {
    fn vfpclasssh(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VGETEXPPH`.
///
/// Assembly forms: `vgetexpph xyz, xyz/mxyz/b16`.
pub trait VgetexpphEmitter<T0, T1> {
    fn vgetexpph(&mut self, dst: T0, src: T1);
}

/// Emits `VGETEXPSH`.
///
/// Assembly forms: `vgetexpsh xmm, xmm[127:16], xmm[15:0]/m16`.
pub trait VgetexpshEmitter<T0, T1, T2> {
    fn vgetexpsh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VGETMANTPH`.
///
/// Assembly forms: `vgetmantph xyz, xyz/mxyz/b16, imm8`.
pub trait VgetmantphEmitter<T0, T1, T2> {
    fn vgetmantph(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VGETMANTSH`.
///
/// Assembly forms: `vgetmantsh xmm, xmm[127:16], xmm[15:0]/m16, imm8`.
pub trait VgetmantshEmitter<T0, T1, T2, T3> {
    fn vgetmantsh(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VMAXPH`.
///
/// Assembly forms: `vmaxph xyz, xyz, xyz/mxyz/b16`.
pub trait VmaxphEmitter<T0, T1, T2> {
    fn vmaxph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMAXSH`.
///
/// Assembly forms: `vmaxsh xmm, xmm, xmm[15:0]/m16`.
pub trait VmaxshEmitter<T0, T1, T2> {
    fn vmaxsh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMINPH`.
///
/// Assembly forms: `vminph xyz, xyz, xyz/mxyz/b16`.
pub trait VminphEmitter<T0, T1, T2> {
    fn vminph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMINSH`.
///
/// Assembly forms: `vminsh xmm, xmm, xmm[15:0]/m16`.
pub trait VminshEmitter<T0, T1, T2> {
    fn vminsh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMOVSH`.
///
/// Assembly forms: `vmovsh m16, xmm[15:0]`, `vmovsh xmm[15:0], m16`, `vmovsh xmm, xmm[127:16], xmm[15:0]`.
pub trait VmovshEmitter<T0, T1> {
    fn vmovsh(&mut self, dst: T0, src: T1);
}

/// Emits `VMOVSH_3`.
///
/// Assembly forms: `vmovsh m16, xmm[15:0]`, `vmovsh xmm[15:0], m16`, `vmovsh xmm, xmm[127:16], xmm[15:0]`.
pub trait Vmovsh3Emitter<T0, T1, T2> {
    fn vmovsh_3(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMOVW`.
///
/// Assembly forms: `vmovw r32[15:0]/m16, xmm[15:0]`, `vmovw xmm[15:0], r32[15:0]/m16`, `vmovw xmm[15:0], xmm[15:0]/m16`.
pub trait VmovwEmitter<T0, T1> {
    fn vmovw(&mut self, dst: T0, src: T1);
}

/// Emits `VMULPH`.
///
/// Assembly forms: `vmulph xyz, xyz, xyz/mxyz/b16`.
pub trait VmulphEmitter<T0, T1, T2> {
    fn vmulph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VMULSH`.
///
/// Assembly forms: `vmulsh xmm, xmm, xmm[15:0]/m16`.
pub trait VmulshEmitter<T0, T1, T2> {
    fn vmulsh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VRCPPH`.
///
/// Assembly forms: `vrcpph xyz, xyz/mxyz/b16`.
pub trait VrcpphEmitter<T0, T1> {
    fn vrcpph(&mut self, dst: T0, src: T1);
}

/// Emits `VRCPSH`.
///
/// Assembly forms: `vrcpsh xmm, xmm[127:16], xmm[15:0]/m16`.
pub trait VrcpshEmitter<T0, T1, T2> {
    fn vrcpsh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VREDUCEPH`.
///
/// Assembly forms: `vreduceph xyz, xyz/mxyz/b16, imm8`.
pub trait VreducephEmitter<T0, T1, T2> {
    fn vreduceph(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VREDUCESH`.
///
/// Assembly forms: `vreducesh xmm, xmm[127:16], xmm[15:0]/m16, imm8`.
pub trait VreduceshEmitter<T0, T1, T2, T3> {
    fn vreducesh(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VRNDSCALEPH`.
///
/// Assembly forms: `vrndscaleph xyz, xyz/mxyz/b16, imm8`.
pub trait VrndscalephEmitter<T0, T1, T2> {
    fn vrndscaleph(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VRNDSCALESH`.
///
/// Assembly forms: `vrndscalesh xmm, xmm[127:16], xmm[15:0]/m16, imm8`.
pub trait VrndscaleshEmitter<T0, T1, T2, T3> {
    fn vrndscalesh(&mut self, dst: T0, src1: T1, src2: T2, imm: T3);
}

/// Emits `VRSQRTPH`.
///
/// Assembly forms: `vrsqrtph xyz, xyz/mxyz/b16`.
pub trait VrsqrtphEmitter<T0, T1> {
    fn vrsqrtph(&mut self, dst: T0, src: T1);
}

/// Emits `VRSQRTSH`.
///
/// Assembly forms: `vrsqrtsh xmm, xmm[127:16], xmm[15:0]/m16`.
pub trait VrsqrtshEmitter<T0, T1, T2> {
    fn vrsqrtsh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VSCALEFPH`.
///
/// Assembly forms: `vscalefph xyz, xyz, xyz/mxyz/b16`.
pub trait VscalefphEmitter<T0, T1, T2> {
    fn vscalefph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VSCALEFSH`.
///
/// Assembly forms: `vscalefsh xmm, xmm, xmm[15:0]/m16`.
pub trait VscalefshEmitter<T0, T1, T2> {
    fn vscalefsh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VSQRTPH`.
///
/// Assembly forms: `vsqrtph xyz, xyz/mxyz/b16`.
pub trait VsqrtphEmitter<T0, T1> {
    fn vsqrtph(&mut self, dst: T0, src: T1);
}

/// Emits `VSQRTSH`.
///
/// Assembly forms: `vsqrtsh xmm, xmm[127:16], xmm[15:0]/m16`.
pub trait VsqrtshEmitter<T0, T1, T2> {
    fn vsqrtsh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VSUBPH`.
///
/// Assembly forms: `vsubph xyz, xyz, xyz/mxyz/b16`.
pub trait VsubphEmitter<T0, T1, T2> {
    fn vsubph(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VSUBSH`.
///
/// Assembly forms: `vsubsh xmm, xmm, xmm[15:0]/m16`.
pub trait VsubshEmitter<T0, T1, T2> {
    fn vsubsh(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `VUCOMISH`.
///
/// Assembly forms: `vucomish xmm[15:0], xmm[15:0]/m16`.
pub trait VucomishEmitter<T0, T1> {
    fn vucomish(&mut self, lhs: T0, rhs: T1);
}

/// Emits `LDTILECFG`.
///
/// Assembly forms: `ldtilecfg m512`.
pub trait LdtilecfgEmitter<T0> {
    fn ldtilecfg(&mut self, mem: T0);
}

/// Emits `STTILECFG`.
///
/// Assembly forms: `sttilecfg m512`.
pub trait SttilecfgEmitter<T0> {
    fn sttilecfg(&mut self, dst: T0);
}

/// Emits `TILELOADD`.
///
/// Assembly forms: `tileloadd tmm, tmem`.
pub trait TileloaddEmitter<T0, T1> {
    fn tileloadd(&mut self, dst: T0, src: T1);
}

/// Emits `TILELOADDT1`.
///
/// Assembly forms: `tileloaddt1 tmm, tmem`.
pub trait Tileloaddt1Emitter<T0, T1> {
    fn tileloaddt1(&mut self, dst: T0, src: T1);
}

/// Emits `TILERELEASE`.
///
/// Assembly forms: `tilerelease`.
pub trait TilereleaseEmitter {
    fn tilerelease(&mut self);
}

/// Emits `TILESTORED`.
///
/// Assembly forms: `tilestored tmem, tmm`.
pub trait TilestoredEmitter<T0, T1> {
    fn tilestored(&mut self, dst: T0, src: T1);
}

/// Emits `TILEZERO`.
///
/// Assembly forms: `tilezero tmm`.
pub trait TilezeroEmitter<T0> {
    fn tilezero(&mut self, dst: T0);
}

/// Emits `TDPBF16PS`.
///
/// Assembly forms: `tdpbf16ps tmm, tmm, tmm`.
pub trait Tdpbf16psEmitter<T0, T1, T2> {
    fn tdpbf16ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `TCMMIMFP16PS`.
///
/// Assembly forms: `tcmmimfp16ps tmm, tmm, tmm`.
pub trait Tcmmimfp16psEmitter<T0, T1, T2> {
    fn tcmmimfp16ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `TCMMRLFP16PS`.
///
/// Assembly forms: `tcmmrlfp16ps tmm, tmm, tmm`.
pub trait Tcmmrlfp16psEmitter<T0, T1, T2> {
    fn tcmmrlfp16ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `TDPFP16PS`.
///
/// Assembly forms: `tdpfp16ps tmm, tmm, tmm`.
pub trait Tdpfp16psEmitter<T0, T1, T2> {
    fn tdpfp16ps(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `TDPBSSD`.
///
/// Assembly forms: `tdpbssd tmm, tmm, tmm`.
pub trait TdpbssdEmitter<T0, T1, T2> {
    fn tdpbssd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `TDPBSUD`.
///
/// Assembly forms: `tdpbsud tmm, tmm, tmm`.
pub trait TdpbsudEmitter<T0, T1, T2> {
    fn tdpbsud(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `TDPBUSD`.
///
/// Assembly forms: `tdpbusd tmm, tmm, tmm`.
pub trait TdpbusdEmitter<T0, T1, T2> {
    fn tdpbusd(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `TDPBUUD`.
///
/// Assembly forms: `tdpbuud tmm, tmm, tmm`.
pub trait TdpbuudEmitter<T0, T1, T2> {
    fn tdpbuud(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `CBW_0`.
///
/// Assembly forms: `cbw <ax>`.
pub trait Cbw0Emitter {
    fn cbw_0(&mut self);
}

/// Emits `CDQ_0`.
///
/// Assembly forms: `cdq <edx>, <eax>`.
pub trait Cdq0Emitter {
    fn cdq_0(&mut self);
}

/// Emits `CDQE_0`.
///
/// Assembly forms: `cdqe <rax>`.
pub trait Cdqe0Emitter {
    fn cdqe_0(&mut self);
}

/// Emits `CMPXCHG_2`.
///
/// Assembly forms: `cmpxchg r8/m8, r8, <al>`, `cmpxchg rv/mv, rv, <axv>`.
pub trait Cmpxchg2Emitter<T0, T1> {
    fn cmpxchg_2(&mut self, dst: T0, src: T1);
}

/// Emits `CMPXCHG16B_1`.
///
/// Assembly forms: `cmpxchg16b m128, <rdx>, <rax>, <rcx>, <rbx>`.
pub trait Cmpxchg16b1Emitter<T0> {
    fn cmpxchg16b_1(&mut self, mem: T0);
}

/// Emits `CMPXCHG8B_1`.
///
/// Assembly forms: `cmpxchg8b m64, <edx>, <eax>, <ecx>, <ebx>`.
pub trait Cmpxchg8b1Emitter<T0> {
    fn cmpxchg8b_1(&mut self, mem: T0);
}

/// Emits `CQO_0`.
///
/// Assembly forms: `cqo <rdx>, <rax>`.
pub trait Cqo0Emitter {
    fn cqo_0(&mut self);
}

/// Emits `CWD_0`.
///
/// Assembly forms: `cwd <dx>, <ax>`.
pub trait Cwd0Emitter {
    fn cwd_0(&mut self);
}

/// Emits `CWDE_0`.
///
/// Assembly forms: `cwde <eax>`.
pub trait Cwde0Emitter {
    fn cwde_0(&mut self);
}

/// Emits `DIV_1`.
///
/// Assembly forms: `div <ax>, r8/m8`, `div <dxv>, <axv>, rv/mv`.
pub trait Div1Emitter<T0> {
    fn div_1(&mut self, src: T0);
}

/// Emits `IDIV_1`.
///
/// Assembly forms: `idiv <ax>, r8/m8`, `idiv <dxv>, <axv>, rv/mv`.
pub trait Idiv1Emitter<T0> {
    fn idiv_1(&mut self, src: T0);
}

/// Emits `IMUL_1`.
///
/// Assembly forms: `imul <ax>, r8/m8`, `imul <dxv>, <axv>, rv/mv`, `imul rv, rv/mv`.
pub trait Imul1Emitter<T0> {
    fn imul_1(&mut self, src: T0);
}

/// Emits `IRET`.
///
/// Assembly forms: `iret`.
pub trait IretEmitter {
    fn iret(&mut self);
}

/// Emits `IRETD`.
///
/// Assembly forms: `iretd`.
pub trait IretdEmitter {
    fn iretd(&mut self);
}

/// Emits `IRETQ`.
///
/// Assembly forms: `iretq`.
pub trait IretqEmitter {
    fn iretq(&mut self);
}

/// Emits `JECXZ_1`.
///
/// Assembly forms: `jecxz <cx>, rel8`, `jecxz <ecx>, rel8`, `jecxz <rcx>, rel8`.
pub trait Jecxz1Emitter<T0> {
    fn jecxz_1(&mut self, target: T0);
}

/// Emits `LOOP_1`.
///
/// Assembly forms: `loop <cx>, rel8`, `loop <ecx>, rel8`, `loop <rcx>, rel8`.
pub trait Loop1Emitter<T0> {
    fn loop_1(&mut self, target: T0);
}

/// Emits `LOOPE_1`.
///
/// Assembly forms: `loope <cx>, rel8`, `loope <ecx>, rel8`, `loope <rcx>, rel8`.
pub trait Loope1Emitter<T0> {
    fn loope_1(&mut self, target: T0);
}

/// Emits `LOOPNE_1`.
///
/// Assembly forms: `loopne <cx>, rel8`, `loopne <ecx>, rel8`, `loopne <rcx>, rel8`.
pub trait Loopne1Emitter<T0> {
    fn loopne_1(&mut self, target: T0);
}

/// Emits `MUL_1`.
///
/// Assembly forms: `mul <ax>, r8/m8`, `mul <dxv>, <axv>, rv/mv`.
pub trait Mul1Emitter<T0> {
    fn mul_1(&mut self, src: T0);
}

/// Emits `RET`.
///
/// Assembly forms: `ret`, `ret immu16`.
pub trait RetEmitter {
    fn ret(&mut self);
}

/// Emits `RET_1`.
///
/// Assembly forms: `ret`, `ret immu16`.
pub trait Ret1Emitter<T0> {
    fn ret_1(&mut self, imm: T0);
}

/// Emits `RETF`.
///
/// Assembly forms: `retf`, `retf immu16`.
pub trait RetfEmitter {
    fn retf(&mut self);
}

/// Emits `RETF_1`.
///
/// Assembly forms: `retf`, `retf immu16`.
pub trait Retf1Emitter<T0> {
    fn retf_1(&mut self, imm: T0);
}

/// Emits `XLATB`.
///
/// Assembly forms: `xlatb`.
pub trait XlatbEmitter {
    fn xlatb(&mut self);
}

/// Emits `AAA_0`.
///
/// Assembly forms: `aaa <ax>`.
pub trait Aaa0Emitter {
    fn aaa_0(&mut self);
}

/// Emits `AAD_1`.
///
/// Assembly forms: `aad <ax>, imm8`.
pub trait Aad1Emitter<T0> {
    fn aad_1(&mut self, imm: T0);
}

/// Emits `AAM_1`.
///
/// Assembly forms: `aam <ax>, imm8`.
pub trait Aam1Emitter<T0> {
    fn aam_1(&mut self, imm: T0);
}

/// Emits `AAS_0`.
///
/// Assembly forms: `aas <ax>`.
pub trait Aas0Emitter {
    fn aas_0(&mut self);
}

/// Emits `DAA_0`.
///
/// Assembly forms: `daa <ax>`.
pub trait Daa0Emitter {
    fn daa_0(&mut self);
}

/// Emits `DAS_0`.
///
/// Assembly forms: `das <ax>`.
pub trait Das0Emitter {
    fn das_0(&mut self);
}

/// Emits `LAHF_0`.
///
/// Assembly forms: `lahf <ah>`.
pub trait Lahf0Emitter {
    fn lahf_0(&mut self);
}

/// Emits `SAHF_0`.
///
/// Assembly forms: `sahf <ah>`.
pub trait Sahf0Emitter {
    fn sahf_0(&mut self);
}

/// Emits `CPUID_0`.
///
/// Assembly forms: `cpuid <eax>, <ebx>, <ecx>, <edx>`.
pub trait Cpuid0Emitter {
    fn cpuid_0(&mut self);
}

/// Emits `CLZERO_0`.
///
/// Assembly forms: `clzero <m512(ds:zax)>`.
pub trait Clzero0Emitter {
    fn clzero_0(&mut self);
}

/// Emits `RDPRU_0`.
///
/// Assembly forms: `rdpru <edx>, <eax>, <ecx>`.
pub trait Rdpru0Emitter {
    fn rdpru_0(&mut self);
}

/// Emits `RDPKRU_0`.
///
/// Assembly forms: `rdpkru <edx>, <eax>, <ecx>`.
pub trait Rdpkru0Emitter {
    fn rdpkru_0(&mut self);
}

/// Emits `RDTSC_0`.
///
/// Assembly forms: `rdtsc <edx>, <eax>`.
pub trait Rdtsc0Emitter {
    fn rdtsc_0(&mut self);
}

/// Emits `RDTSCP_0`.
///
/// Assembly forms: `rdtscp <edx>, <eax>, <ecx>`.
pub trait Rdtscp0Emitter {
    fn rdtscp_0(&mut self);
}

/// Emits `MULX_3`.
///
/// Assembly forms: `mulx W:ry, ry, ry/my, <dxy>`.
pub trait Mulx3Emitter<T0, T1, T2> {
    fn mulx_3(&mut self, dst: T0, src1: T1, src2: T2);
}

/// Emits `XGETBV_0`.
///
/// Assembly forms: `xgetbv <edx>, <eax>, <ecx>`.
pub trait Xgetbv0Emitter {
    fn xgetbv_0(&mut self);
}

/// Emits `XRSTOR_1`.
///
/// Assembly forms: `xrstor mem, <edx>, <eax>`.
pub trait Xrstor1Emitter<T0> {
    fn xrstor_1(&mut self, mem: T0);
}

/// Emits `XRSTOR64_1`.
///
/// Assembly forms: `xrstor64 mem, <edx>, <eax>`.
pub trait Xrstor641Emitter<T0> {
    fn xrstor64_1(&mut self, mem: T0);
}

/// Emits `XRSTORS_1`.
///
/// Assembly forms: `xrstors mem, <edx>, <eax>`.
pub trait Xrstors1Emitter<T0> {
    fn xrstors_1(&mut self, mem: T0);
}

/// Emits `XRSTORS64_1`.
///
/// Assembly forms: `xrstors64 mem, <edx>, <eax>`.
pub trait Xrstors641Emitter<T0> {
    fn xrstors64_1(&mut self, mem: T0);
}

/// Emits `XSAVE_1`.
///
/// Assembly forms: `xsave mem, <edx>, <eax>`.
pub trait Xsave1Emitter<T0> {
    fn xsave_1(&mut self, mem: T0);
}

/// Emits `XSAVE64_1`.
///
/// Assembly forms: `xsave64 mem, <edx>, <eax>`.
pub trait Xsave641Emitter<T0> {
    fn xsave64_1(&mut self, mem: T0);
}

/// Emits `XSAVEC_1`.
///
/// Assembly forms: `xsavec mem, <edx>, <eax>`.
pub trait Xsavec1Emitter<T0> {
    fn xsavec_1(&mut self, mem: T0);
}

/// Emits `XSAVEC64_1`.
///
/// Assembly forms: `xsavec64 mem, <edx>, <eax>`.
pub trait Xsavec641Emitter<T0> {
    fn xsavec64_1(&mut self, mem: T0);
}

/// Emits `XSAVEOPT_1`.
///
/// Assembly forms: `xsaveopt mem, <edx>, <eax>`.
pub trait Xsaveopt1Emitter<T0> {
    fn xsaveopt_1(&mut self, mem: T0);
}

/// Emits `XSAVEOPT64_1`.
///
/// Assembly forms: `xsaveopt64 mem, <edx>, <eax>`.
pub trait Xsaveopt641Emitter<T0> {
    fn xsaveopt64_1(&mut self, mem: T0);
}

/// Emits `XSAVES_1`.
///
/// Assembly forms: `xsaves mem, <edx>, <eax>`.
pub trait Xsaves1Emitter<T0> {
    fn xsaves_1(&mut self, mem: T0);
}

/// Emits `XSAVES64_1`.
///
/// Assembly forms: `xsaves64 mem, <edx>, <eax>`.
pub trait Xsaves641Emitter<T0> {
    fn xsaves64_1(&mut self, mem: T0);
}

/// Emits `SYSCALL`.
///
/// Assembly forms: `syscall`.
pub trait SyscallEmitter {
    fn syscall(&mut self);
}

/// Emits `SYSENTER`.
///
/// Assembly forms: `sysenter`.
pub trait SysenterEmitter {
    fn sysenter(&mut self);
}

/// Emits `HRESET_1`.
///
/// Assembly forms: `hreset imm8, <eax>`.
pub trait Hreset1Emitter<T0> {
    fn hreset_1(&mut self, imm: T0);
}

/// Emits `SEAMCALL`.
///
/// Assembly forms: `seamcall`.
pub trait SeamcallEmitter {
    fn seamcall(&mut self);
}

/// Emits `SEAMOPS`.
///
/// Assembly forms: `seamops`.
pub trait SeamopsEmitter {
    fn seamops(&mut self);
}

/// Emits `SEAMRET`.
///
/// Assembly forms: `seamret`.
pub trait SeamretEmitter {
    fn seamret(&mut self);
}

/// Emits `TDCALL`.
///
/// Assembly forms: `tdcall`.
pub trait TdcallEmitter {
    fn tdcall(&mut self);
}

/// Emits `PCONFIG`.
///
/// Assembly forms: `pconfig`.
pub trait PconfigEmitter {
    fn pconfig(&mut self);
}

/// Emits `RDMSR_0`.
///
/// Assembly forms: `rdmsr <edx>, <eax>, <ecx>`, `rdmsr r64, imm32`.
pub trait Rdmsr0Emitter {
    fn rdmsr_0(&mut self);
}

/// Emits `RDPMC_0`.
///
/// Assembly forms: `rdpmc <edx>, <eax>, <ecx>`.
pub trait Rdpmc0Emitter {
    fn rdpmc_0(&mut self);
}

/// Emits `SYSEXIT`.
///
/// Assembly forms: `sysexit`.
pub trait SysexitEmitter {
    fn sysexit(&mut self);
}

/// Emits `SYSEXITQ`.
///
/// Assembly forms: `sysexitq`.
pub trait SysexitqEmitter {
    fn sysexitq(&mut self);
}

/// Emits `SYSRET`.
///
/// Assembly forms: `sysret`.
pub trait SysretEmitter {
    fn sysret(&mut self);
}

/// Emits `SYSRETQ`.
///
/// Assembly forms: `sysretq`.
pub trait SysretqEmitter {
    fn sysretq(&mut self);
}

/// Emits `WRMSR_0`.
///
/// Assembly forms: `wrmsr <edx>, <eax>, <ecx>`.
pub trait Wrmsr0Emitter {
    fn wrmsr_0(&mut self);
}

/// Emits `XSETBV_0`.
///
/// Assembly forms: `xsetbv <edx>, <eax>, <ecx>`.
pub trait Xsetbv0Emitter {
    fn xsetbv_0(&mut self);
}

/// Emits `MONITOR_0`.
///
/// Assembly forms: `monitor <mem(ds:zax)>, <ecx>, <edx>`.
pub trait Monitor0Emitter {
    fn monitor_0(&mut self);
}

/// Emits `MONITORX_0`.
///
/// Assembly forms: `monitorx <mem(ds:zax)>, <ecx>, <edx>`.
pub trait Monitorx0Emitter {
    fn monitorx_0(&mut self);
}

/// Emits `MWAIT_0`.
///
/// Assembly forms: `mwait <eax>, <ecx>`.
pub trait Mwait0Emitter {
    fn mwait_0(&mut self);
}

/// Emits `MWAITX_0`.
///
/// Assembly forms: `mwaitx <eax>, <ecx>, <ebx>`.
pub trait Mwaitx0Emitter {
    fn mwaitx_0(&mut self);
}

/// Emits `TPAUSE_1`.
///
/// Assembly forms: `tpause r32, <edx>, <eax>`.
pub trait Tpause1Emitter<T0> {
    fn tpause_1(&mut self, src: T0);
}

/// Emits `UMWAIT_1`.
///
/// Assembly forms: `umwait r32, <edx>, <eax>`.
pub trait Umwait1Emitter<T0> {
    fn umwait_1(&mut self, src: T0);
}

/// Emits `BLENDVPD_2`.
///
/// Assembly forms: `blendvpd xmm, xmm/m128, <xmm0>`.
pub trait Blendvpd2Emitter<T0, T1> {
    fn blendvpd_2(&mut self, dst: T0, src: T1);
}

/// Emits `BLENDVPS_2`.
///
/// Assembly forms: `blendvps xmm, xmm/m128, <xmm0>`.
pub trait Blendvps2Emitter<T0, T1> {
    fn blendvps_2(&mut self, dst: T0, src: T1);
}

/// Emits `PBLENDVB_2`.
///
/// Assembly forms: `pblendvb xmm, xmm/m128, <xmm0>`.
pub trait Pblendvb2Emitter<T0, T1> {
    fn pblendvb_2(&mut self, dst: T0, src: T1);
}

/// Emits `MASKMOVQ_2`.
///
/// Assembly forms: `maskmovq mm, mm, <m64(ds:zdi)>`.
pub trait Maskmovq2Emitter<T0, T1> {
    fn maskmovq_2(&mut self, dst: T0, src: T1);
}

/// Emits `MASKMOVDQU_2`.
///
/// Assembly forms: `maskmovdqu xmm, xmm, <m128(ds:zdi)>`.
pub trait Maskmovdqu2Emitter<T0, T1> {
    fn maskmovdqu_2(&mut self, dst: T0, src: T1);
}

/// Emits `PCMPESTRI_3`.
///
/// Assembly forms: `pcmpestri xmm, xmm/m128, imm8, <ecx>, <eax>, <edx>`.
pub trait Pcmpestri3Emitter<T0, T1, T2> {
    fn pcmpestri_3(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `PCMPESTRM_3`.
///
/// Assembly forms: `pcmpestrm xmm, xmm/m128, imm8, <xmm0>, <eax>, <edx>`.
pub trait Pcmpestrm3Emitter<T0, T1, T2> {
    fn pcmpestrm_3(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `PCMPISTRI_3`.
///
/// Assembly forms: `pcmpistri xmm, xmm/m128, imm8, <ecx>`.
pub trait Pcmpistri3Emitter<T0, T1, T2> {
    fn pcmpistri_3(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `PCMPISTRM_3`.
///
/// Assembly forms: `pcmpistrm xmm, xmm/m128, imm8, <xmm0>`.
pub trait Pcmpistrm3Emitter<T0, T1, T2> {
    fn pcmpistrm_3(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `SHA256RNDS2_2`.
///
/// Assembly forms: `sha256rnds2 xmm, xmm/m128, <xmm0>`.
pub trait Sha256rnds22Emitter<T0, T1> {
    fn sha256rnds2_2(&mut self, dst: T0, src: T1);
}

/// Emits `VMASKMOVDQU_2`.
///
/// Assembly forms: `vmaskmovdqu xmm, xmm, <m128(ds:zdi)>`.
pub trait Vmaskmovdqu2Emitter<T0, T1> {
    fn vmaskmovdqu_2(&mut self, dst: T0, src: T1);
}

/// Emits `VPCMPESTRI_3`.
///
/// Assembly forms: `vpcmpestri xmm, xmm/m128, imm8, <ecx>, <eax>, <edx>`.
pub trait Vpcmpestri3Emitter<T0, T1, T2> {
    fn vpcmpestri_3(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPCMPESTRM_3`.
///
/// Assembly forms: `vpcmpestrm xmm, xmm/m128, imm8, <xmm0>, <eax>, <edx>`.
pub trait Vpcmpestrm3Emitter<T0, T1, T2> {
    fn vpcmpestrm_3(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPCMPISTRI_3`.
///
/// Assembly forms: `vpcmpistri xmm, xmm/m128, imm8, <ecx>`.
pub trait Vpcmpistri3Emitter<T0, T1, T2> {
    fn vpcmpistri_3(&mut self, dst: T0, src: T1, imm: T2);
}

/// Emits `VPCMPISTRM_3`.
///
/// Assembly forms: `vpcmpistrm xmm, xmm/m128, imm8, <xmm0>`.
pub trait Vpcmpistrm3Emitter<T0, T1, T2> {
    fn vpcmpistrm_3(&mut self, dst: T0, src: T1, imm: T2);
}
impl AdcEmitter<Gp, Gp> for Assembler<'_> {
    fn adc(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Adc as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AdcEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn adc(&mut self, dst: GpbLo, src: GpbLo) {
        self.emit_n(InstId::Adc as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AdcEmitter<GpbLo, GpbHi> for Assembler<'_> {
    fn adc(&mut self, dst: GpbLo, src: GpbHi) {
        self.emit_n(InstId::Adc as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AdcEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn adc(&mut self, dst: GpbHi, src: GpbLo) {
        self.emit_n(InstId::Adc as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AdcEmitter<GpbHi, GpbHi> for Assembler<'_> {
    fn adc(&mut self, dst: GpbHi, src: GpbHi) {
        self.emit_n(InstId::Adc as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AdcEmitter<Gpw, Gpw> for Assembler<'_> {
    fn adc(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Adc as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AdcEmitter<Gpd, Gpd> for Assembler<'_> {
    fn adc(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Adc as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AdcEmitter<Gpq, Gpq> for Assembler<'_> {
    fn adc(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Adc as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AdcEmitter<Gp, Mem> for Assembler<'_> {
    fn adc(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Adc as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AdcEmitter<GpbLo, Mem> for Assembler<'_> {
    fn adc(&mut self, dst: GpbLo, src: Mem) {
        self.emit_n(InstId::Adc as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AdcEmitter<GpbHi, Mem> for Assembler<'_> {
    fn adc(&mut self, dst: GpbHi, src: Mem) {
        self.emit_n(InstId::Adc as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AdcEmitter<Gpw, Mem> for Assembler<'_> {
    fn adc(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Adc as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AdcEmitter<Gpd, Mem> for Assembler<'_> {
    fn adc(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Adc as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AdcEmitter<Gpq, Mem> for Assembler<'_> {
    fn adc(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Adc as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AdcEmitter<Gp, Imm> for Assembler<'_> {
    fn adc(&mut self, dst: Gp, imm: Imm) {
        self.emit_n(InstId::Adc as u32, &[dst.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> AdcEmitter<GpbLo, U1> for Assembler<'_> {
    fn adc(&mut self, dst: GpbLo, src: U1) {
        self.emit_n(
            InstId::Adc as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> AdcEmitter<GpbHi, U1> for Assembler<'_> {
    fn adc(&mut self, dst: GpbHi, src: U1) {
        self.emit_n(
            InstId::Adc as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> AdcEmitter<Gpw, U1> for Assembler<'_> {
    fn adc(&mut self, dst: Gpw, src: U1) {
        self.emit_n(
            InstId::Adc as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> AdcEmitter<Gpd, U1> for Assembler<'_> {
    fn adc(&mut self, dst: Gpd, src: U1) {
        self.emit_n(
            InstId::Adc as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> AdcEmitter<Gpq, U1> for Assembler<'_> {
    fn adc(&mut self, dst: Gpq, src: U1) {
        self.emit_n(
            InstId::Adc as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl AdcEmitter<Mem, Gp> for Assembler<'_> {
    fn adc(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Adc as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AdcEmitter<Mem, GpbLo> for Assembler<'_> {
    fn adc(&mut self, dst: Mem, src: GpbLo) {
        self.emit_n(InstId::Adc as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AdcEmitter<Mem, GpbHi> for Assembler<'_> {
    fn adc(&mut self, dst: Mem, src: GpbHi) {
        self.emit_n(InstId::Adc as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AdcEmitter<Mem, Gpw> for Assembler<'_> {
    fn adc(&mut self, dst: Mem, src: Gpw) {
        self.emit_n(InstId::Adc as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AdcEmitter<Mem, Gpd> for Assembler<'_> {
    fn adc(&mut self, dst: Mem, src: Gpd) {
        self.emit_n(InstId::Adc as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AdcEmitter<Mem, Gpq> for Assembler<'_> {
    fn adc(&mut self, dst: Mem, src: Gpq) {
        self.emit_n(InstId::Adc as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> AdcEmitter<Mem, U1> for Assembler<'_> {
    fn adc(&mut self, dst: Mem, src: U1) {
        self.emit_n(
            InstId::Adc as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl AddEmitter<Gp, Gp> for Assembler<'_> {
    fn add(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Add as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AddEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn add(&mut self, dst: GpbLo, src: GpbLo) {
        self.emit_n(InstId::Add as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AddEmitter<GpbLo, GpbHi> for Assembler<'_> {
    fn add(&mut self, dst: GpbLo, src: GpbHi) {
        self.emit_n(InstId::Add as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AddEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn add(&mut self, dst: GpbHi, src: GpbLo) {
        self.emit_n(InstId::Add as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AddEmitter<GpbHi, GpbHi> for Assembler<'_> {
    fn add(&mut self, dst: GpbHi, src: GpbHi) {
        self.emit_n(InstId::Add as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AddEmitter<Gpw, Gpw> for Assembler<'_> {
    fn add(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Add as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AddEmitter<Gpd, Gpd> for Assembler<'_> {
    fn add(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Add as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AddEmitter<Gpq, Gpq> for Assembler<'_> {
    fn add(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Add as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AddEmitter<Gp, Mem> for Assembler<'_> {
    fn add(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Add as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AddEmitter<GpbLo, Mem> for Assembler<'_> {
    fn add(&mut self, dst: GpbLo, src: Mem) {
        self.emit_n(InstId::Add as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AddEmitter<GpbHi, Mem> for Assembler<'_> {
    fn add(&mut self, dst: GpbHi, src: Mem) {
        self.emit_n(InstId::Add as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AddEmitter<Gpw, Mem> for Assembler<'_> {
    fn add(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Add as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AddEmitter<Gpd, Mem> for Assembler<'_> {
    fn add(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Add as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AddEmitter<Gpq, Mem> for Assembler<'_> {
    fn add(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Add as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AddEmitter<Gp, Imm> for Assembler<'_> {
    fn add(&mut self, dst: Gp, imm: Imm) {
        self.emit_n(InstId::Add as u32, &[dst.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> AddEmitter<GpbLo, U1> for Assembler<'_> {
    fn add(&mut self, dst: GpbLo, src: U1) {
        self.emit_n(
            InstId::Add as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> AddEmitter<GpbHi, U1> for Assembler<'_> {
    fn add(&mut self, dst: GpbHi, src: U1) {
        self.emit_n(
            InstId::Add as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> AddEmitter<Gpw, U1> for Assembler<'_> {
    fn add(&mut self, dst: Gpw, src: U1) {
        self.emit_n(
            InstId::Add as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> AddEmitter<Gpd, U1> for Assembler<'_> {
    fn add(&mut self, dst: Gpd, src: U1) {
        self.emit_n(
            InstId::Add as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> AddEmitter<Gpq, U1> for Assembler<'_> {
    fn add(&mut self, dst: Gpq, src: U1) {
        self.emit_n(
            InstId::Add as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl AddEmitter<Mem, Gp> for Assembler<'_> {
    fn add(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Add as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AddEmitter<Mem, GpbLo> for Assembler<'_> {
    fn add(&mut self, dst: Mem, src: GpbLo) {
        self.emit_n(InstId::Add as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AddEmitter<Mem, GpbHi> for Assembler<'_> {
    fn add(&mut self, dst: Mem, src: GpbHi) {
        self.emit_n(InstId::Add as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AddEmitter<Mem, Gpw> for Assembler<'_> {
    fn add(&mut self, dst: Mem, src: Gpw) {
        self.emit_n(InstId::Add as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AddEmitter<Mem, Gpd> for Assembler<'_> {
    fn add(&mut self, dst: Mem, src: Gpd) {
        self.emit_n(InstId::Add as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl AddEmitter<Mem, Gpq> for Assembler<'_> {
    fn add(&mut self, dst: Mem, src: Gpq) {
        self.emit_n(InstId::Add as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> AddEmitter<Mem, U1> for Assembler<'_> {
    fn add(&mut self, dst: Mem, src: U1) {
        self.emit_n(
            InstId::Add as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl And_Emitter<Gp, Gp> for Assembler<'_> {
    fn and_(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::And as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl And_Emitter<Gpq, Gpq> for Assembler<'_> {
    fn and_(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::And as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl And_Emitter<GpbLo, GpbLo> for Assembler<'_> {
    fn and_(&mut self, dst: GpbLo, src: GpbLo) {
        self.emit_n(InstId::And as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl And_Emitter<GpbLo, GpbHi> for Assembler<'_> {
    fn and_(&mut self, dst: GpbLo, src: GpbHi) {
        self.emit_n(InstId::And as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl And_Emitter<GpbHi, GpbLo> for Assembler<'_> {
    fn and_(&mut self, dst: GpbHi, src: GpbLo) {
        self.emit_n(InstId::And as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl And_Emitter<GpbHi, GpbHi> for Assembler<'_> {
    fn and_(&mut self, dst: GpbHi, src: GpbHi) {
        self.emit_n(InstId::And as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl And_Emitter<Gpw, Gpw> for Assembler<'_> {
    fn and_(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::And as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl And_Emitter<Gpd, Gpd> for Assembler<'_> {
    fn and_(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::And as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl And_Emitter<Gp, Mem> for Assembler<'_> {
    fn and_(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::And as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl And_Emitter<Gpq, Mem> for Assembler<'_> {
    fn and_(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::And as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl And_Emitter<GpbLo, Mem> for Assembler<'_> {
    fn and_(&mut self, dst: GpbLo, src: Mem) {
        self.emit_n(InstId::And as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl And_Emitter<GpbHi, Mem> for Assembler<'_> {
    fn and_(&mut self, dst: GpbHi, src: Mem) {
        self.emit_n(InstId::And as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl And_Emitter<Gpw, Mem> for Assembler<'_> {
    fn and_(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::And as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl And_Emitter<Gpd, Mem> for Assembler<'_> {
    fn and_(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::And as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl And_Emitter<Gp, Imm> for Assembler<'_> {
    fn and_(&mut self, dst: Gp, imm: Imm) {
        self.emit_n(InstId::And as u32, &[dst.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> And_Emitter<GpbLo, U1> for Assembler<'_> {
    fn and_(&mut self, dst: GpbLo, src: U1) {
        self.emit_n(
            InstId::And as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> And_Emitter<GpbHi, U1> for Assembler<'_> {
    fn and_(&mut self, dst: GpbHi, src: U1) {
        self.emit_n(
            InstId::And as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> And_Emitter<Gpw, U1> for Assembler<'_> {
    fn and_(&mut self, dst: Gpw, src: U1) {
        self.emit_n(
            InstId::And as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> And_Emitter<Gpd, U1> for Assembler<'_> {
    fn and_(&mut self, dst: Gpd, src: U1) {
        self.emit_n(
            InstId::And as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> And_Emitter<Gpq, U1> for Assembler<'_> {
    fn and_(&mut self, dst: Gpq, src: U1) {
        self.emit_n(
            InstId::And as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl And_Emitter<Mem, Gp> for Assembler<'_> {
    fn and_(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::And as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl And_Emitter<Mem, GpbLo> for Assembler<'_> {
    fn and_(&mut self, dst: Mem, src: GpbLo) {
        self.emit_n(InstId::And as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl And_Emitter<Mem, GpbHi> for Assembler<'_> {
    fn and_(&mut self, dst: Mem, src: GpbHi) {
        self.emit_n(InstId::And as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl And_Emitter<Mem, Gpw> for Assembler<'_> {
    fn and_(&mut self, dst: Mem, src: Gpw) {
        self.emit_n(InstId::And as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl And_Emitter<Mem, Gpd> for Assembler<'_> {
    fn and_(&mut self, dst: Mem, src: Gpd) {
        self.emit_n(InstId::And as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl And_Emitter<Mem, Gpq> for Assembler<'_> {
    fn and_(&mut self, dst: Mem, src: Gpq) {
        self.emit_n(InstId::And as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> And_Emitter<Mem, U1> for Assembler<'_> {
    fn and_(&mut self, dst: Mem, src: U1) {
        self.emit_n(
            InstId::And as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl BoundEmitter<Gp, Mem> for Assembler<'_> {
    fn bound(&mut self, lhs: Gp, rhs: Mem) {
        self.emit_n(InstId::Bound as u32, &[lhs.as_operand(), rhs.as_operand()]); // X86
    }
}

impl BoundEmitter<Gpw, Mem> for Assembler<'_> {
    fn bound(&mut self, lhs: Gpw, rhs: Mem) {
        self.emit_n(InstId::Bound as u32, &[lhs.as_operand(), rhs.as_operand()]); // X86
    }
}

impl BoundEmitter<Gpd, Mem> for Assembler<'_> {
    fn bound(&mut self, lhs: Gpd, rhs: Mem) {
        self.emit_n(InstId::Bound as u32, &[lhs.as_operand(), rhs.as_operand()]); // X86
    }
}

impl BsfEmitter<Gp, Gp> for Assembler<'_> {
    fn bsf(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Bsf as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl BsfEmitter<Gpw, Gpw> for Assembler<'_> {
    fn bsf(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Bsf as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl BsfEmitter<Gpd, Gpd> for Assembler<'_> {
    fn bsf(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Bsf as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl BsfEmitter<Gpq, Gpq> for Assembler<'_> {
    fn bsf(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Bsf as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl BsfEmitter<Gp, Mem> for Assembler<'_> {
    fn bsf(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Bsf as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl BsfEmitter<Gpw, Mem> for Assembler<'_> {
    fn bsf(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Bsf as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl BsfEmitter<Gpd, Mem> for Assembler<'_> {
    fn bsf(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Bsf as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl BsfEmitter<Gpq, Mem> for Assembler<'_> {
    fn bsf(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Bsf as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl BsrEmitter<Gp, Gp> for Assembler<'_> {
    fn bsr(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Bsr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl BsrEmitter<Gpw, Gpw> for Assembler<'_> {
    fn bsr(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Bsr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl BsrEmitter<Gpd, Gpd> for Assembler<'_> {
    fn bsr(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Bsr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl BsrEmitter<Gpq, Gpq> for Assembler<'_> {
    fn bsr(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Bsr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl BsrEmitter<Gp, Mem> for Assembler<'_> {
    fn bsr(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Bsr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl BsrEmitter<Gpw, Mem> for Assembler<'_> {
    fn bsr(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Bsr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl BsrEmitter<Gpd, Mem> for Assembler<'_> {
    fn bsr(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Bsr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl BsrEmitter<Gpq, Mem> for Assembler<'_> {
    fn bsr(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Bsr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl BswapEmitter<Gp> for Assembler<'_> {
    fn bswap(&mut self, dst: Gp) {
        self.emit_n(InstId::Bswap as u32, &[dst.as_operand()]); // ANY
    }
}

impl BswapEmitter<Gpw> for Assembler<'_> {
    fn bswap(&mut self, dst: Gpw) {
        self.emit_n(InstId::Bswap as u32, &[dst.as_operand()]); // ANY
    }
}

impl BswapEmitter<Gpd> for Assembler<'_> {
    fn bswap(&mut self, dst: Gpd) {
        self.emit_n(InstId::Bswap as u32, &[dst.as_operand()]); // ANY
    }
}

impl BswapEmitter<Gpq> for Assembler<'_> {
    fn bswap(&mut self, dst: Gpq) {
        self.emit_n(InstId::Bswap as u32, &[dst.as_operand()]); // ANY
    }
}

impl BtEmitter<Gp, Gp> for Assembler<'_> {
    fn bt(&mut self, lhs: Gp, rhs: Gp) {
        self.emit_n(InstId::Bt as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtEmitter<Gpw, Gpw> for Assembler<'_> {
    fn bt(&mut self, lhs: Gpw, rhs: Gpw) {
        self.emit_n(InstId::Bt as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtEmitter<Gpd, Gpd> for Assembler<'_> {
    fn bt(&mut self, lhs: Gpd, rhs: Gpd) {
        self.emit_n(InstId::Bt as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtEmitter<Gpq, Gpq> for Assembler<'_> {
    fn bt(&mut self, lhs: Gpq, rhs: Gpq) {
        self.emit_n(InstId::Bt as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtEmitter<Gp, Imm> for Assembler<'_> {
    fn bt(&mut self, lhs: Gp, imm: Imm) {
        self.emit_n(InstId::Bt as u32, &[lhs.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> BtEmitter<Gpw, U1> for Assembler<'_> {
    fn bt(&mut self, lhs: Gpw, rhs: U1) {
        self.emit_n(
            InstId::Bt as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> BtEmitter<Gpd, U1> for Assembler<'_> {
    fn bt(&mut self, lhs: Gpd, rhs: U1) {
        self.emit_n(
            InstId::Bt as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> BtEmitter<Gpq, U1> for Assembler<'_> {
    fn bt(&mut self, lhs: Gpq, rhs: U1) {
        self.emit_n(
            InstId::Bt as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl BtEmitter<Mem, Gp> for Assembler<'_> {
    fn bt(&mut self, lhs: Mem, rhs: Gp) {
        self.emit_n(InstId::Bt as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtEmitter<Mem, Gpw> for Assembler<'_> {
    fn bt(&mut self, lhs: Mem, rhs: Gpw) {
        self.emit_n(InstId::Bt as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtEmitter<Mem, Gpd> for Assembler<'_> {
    fn bt(&mut self, lhs: Mem, rhs: Gpd) {
        self.emit_n(InstId::Bt as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtEmitter<Mem, Gpq> for Assembler<'_> {
    fn bt(&mut self, lhs: Mem, rhs: Gpq) {
        self.emit_n(InstId::Bt as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> BtEmitter<Mem, U1> for Assembler<'_> {
    fn bt(&mut self, lhs: Mem, rhs: U1) {
        self.emit_n(
            InstId::Bt as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl BtcEmitter<Gp, Gp> for Assembler<'_> {
    fn btc(&mut self, lhs: Gp, rhs: Gp) {
        self.emit_n(InstId::Btc as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtcEmitter<Gpw, Gpw> for Assembler<'_> {
    fn btc(&mut self, lhs: Gpw, rhs: Gpw) {
        self.emit_n(InstId::Btc as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtcEmitter<Gpd, Gpd> for Assembler<'_> {
    fn btc(&mut self, lhs: Gpd, rhs: Gpd) {
        self.emit_n(InstId::Btc as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtcEmitter<Gpq, Gpq> for Assembler<'_> {
    fn btc(&mut self, lhs: Gpq, rhs: Gpq) {
        self.emit_n(InstId::Btc as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtcEmitter<Gp, Imm> for Assembler<'_> {
    fn btc(&mut self, lhs: Gp, imm: Imm) {
        self.emit_n(InstId::Btc as u32, &[lhs.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> BtcEmitter<Gpw, U1> for Assembler<'_> {
    fn btc(&mut self, lhs: Gpw, rhs: U1) {
        self.emit_n(
            InstId::Btc as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> BtcEmitter<Gpd, U1> for Assembler<'_> {
    fn btc(&mut self, lhs: Gpd, rhs: U1) {
        self.emit_n(
            InstId::Btc as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> BtcEmitter<Gpq, U1> for Assembler<'_> {
    fn btc(&mut self, lhs: Gpq, rhs: U1) {
        self.emit_n(
            InstId::Btc as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl BtcEmitter<Mem, Gp> for Assembler<'_> {
    fn btc(&mut self, lhs: Mem, rhs: Gp) {
        self.emit_n(InstId::Btc as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtcEmitter<Mem, Gpw> for Assembler<'_> {
    fn btc(&mut self, lhs: Mem, rhs: Gpw) {
        self.emit_n(InstId::Btc as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtcEmitter<Mem, Gpd> for Assembler<'_> {
    fn btc(&mut self, lhs: Mem, rhs: Gpd) {
        self.emit_n(InstId::Btc as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtcEmitter<Mem, Gpq> for Assembler<'_> {
    fn btc(&mut self, lhs: Mem, rhs: Gpq) {
        self.emit_n(InstId::Btc as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> BtcEmitter<Mem, U1> for Assembler<'_> {
    fn btc(&mut self, lhs: Mem, rhs: U1) {
        self.emit_n(
            InstId::Btc as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl BtrEmitter<Gp, Gp> for Assembler<'_> {
    fn btr(&mut self, lhs: Gp, rhs: Gp) {
        self.emit_n(InstId::Btr as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtrEmitter<Gpw, Gpw> for Assembler<'_> {
    fn btr(&mut self, lhs: Gpw, rhs: Gpw) {
        self.emit_n(InstId::Btr as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtrEmitter<Gpd, Gpd> for Assembler<'_> {
    fn btr(&mut self, lhs: Gpd, rhs: Gpd) {
        self.emit_n(InstId::Btr as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtrEmitter<Gpq, Gpq> for Assembler<'_> {
    fn btr(&mut self, lhs: Gpq, rhs: Gpq) {
        self.emit_n(InstId::Btr as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtrEmitter<Gp, Imm> for Assembler<'_> {
    fn btr(&mut self, lhs: Gp, imm: Imm) {
        self.emit_n(InstId::Btr as u32, &[lhs.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> BtrEmitter<Gpw, U1> for Assembler<'_> {
    fn btr(&mut self, lhs: Gpw, rhs: U1) {
        self.emit_n(
            InstId::Btr as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> BtrEmitter<Gpd, U1> for Assembler<'_> {
    fn btr(&mut self, lhs: Gpd, rhs: U1) {
        self.emit_n(
            InstId::Btr as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> BtrEmitter<Gpq, U1> for Assembler<'_> {
    fn btr(&mut self, lhs: Gpq, rhs: U1) {
        self.emit_n(
            InstId::Btr as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl BtrEmitter<Mem, Gp> for Assembler<'_> {
    fn btr(&mut self, lhs: Mem, rhs: Gp) {
        self.emit_n(InstId::Btr as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtrEmitter<Mem, Gpw> for Assembler<'_> {
    fn btr(&mut self, lhs: Mem, rhs: Gpw) {
        self.emit_n(InstId::Btr as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtrEmitter<Mem, Gpd> for Assembler<'_> {
    fn btr(&mut self, lhs: Mem, rhs: Gpd) {
        self.emit_n(InstId::Btr as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtrEmitter<Mem, Gpq> for Assembler<'_> {
    fn btr(&mut self, lhs: Mem, rhs: Gpq) {
        self.emit_n(InstId::Btr as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> BtrEmitter<Mem, U1> for Assembler<'_> {
    fn btr(&mut self, lhs: Mem, rhs: U1) {
        self.emit_n(
            InstId::Btr as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl BtsEmitter<Gp, Gp> for Assembler<'_> {
    fn bts(&mut self, lhs: Gp, rhs: Gp) {
        self.emit_n(InstId::Bts as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtsEmitter<Gpw, Gpw> for Assembler<'_> {
    fn bts(&mut self, lhs: Gpw, rhs: Gpw) {
        self.emit_n(InstId::Bts as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtsEmitter<Gpd, Gpd> for Assembler<'_> {
    fn bts(&mut self, lhs: Gpd, rhs: Gpd) {
        self.emit_n(InstId::Bts as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtsEmitter<Gpq, Gpq> for Assembler<'_> {
    fn bts(&mut self, lhs: Gpq, rhs: Gpq) {
        self.emit_n(InstId::Bts as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtsEmitter<Gp, Imm> for Assembler<'_> {
    fn bts(&mut self, lhs: Gp, imm: Imm) {
        self.emit_n(InstId::Bts as u32, &[lhs.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> BtsEmitter<Gpw, U1> for Assembler<'_> {
    fn bts(&mut self, lhs: Gpw, rhs: U1) {
        self.emit_n(
            InstId::Bts as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> BtsEmitter<Gpd, U1> for Assembler<'_> {
    fn bts(&mut self, lhs: Gpd, rhs: U1) {
        self.emit_n(
            InstId::Bts as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> BtsEmitter<Gpq, U1> for Assembler<'_> {
    fn bts(&mut self, lhs: Gpq, rhs: U1) {
        self.emit_n(
            InstId::Bts as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl BtsEmitter<Mem, Gp> for Assembler<'_> {
    fn bts(&mut self, lhs: Mem, rhs: Gp) {
        self.emit_n(InstId::Bts as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtsEmitter<Mem, Gpw> for Assembler<'_> {
    fn bts(&mut self, lhs: Mem, rhs: Gpw) {
        self.emit_n(InstId::Bts as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtsEmitter<Mem, Gpd> for Assembler<'_> {
    fn bts(&mut self, lhs: Mem, rhs: Gpd) {
        self.emit_n(InstId::Bts as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl BtsEmitter<Mem, Gpq> for Assembler<'_> {
    fn bts(&mut self, lhs: Mem, rhs: Gpq) {
        self.emit_n(InstId::Bts as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> BtsEmitter<Mem, U1> for Assembler<'_> {
    fn bts(&mut self, lhs: Mem, rhs: U1) {
        self.emit_n(
            InstId::Bts as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl CbwEmitter<Gp> for Assembler<'_> {
    fn cbw(&mut self, dst: Gp) {
        self.emit_n(InstId::Cbw as u32, &[dst.as_operand()]); // ANY [EXPLICIT] AX <- Sign Extend AL
    }
}

impl CbwEmitter<Gpw> for Assembler<'_> {
    fn cbw(&mut self, dst: Gpw) {
        self.emit_n(InstId::Cbw as u32, &[dst.as_operand()]); // ANY [EXPLICIT] AX <- Sign Extend AL
    }
}

impl CdqEmitter<Gp, Gp> for Assembler<'_> {
    fn cdq(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Cdq as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] EDX:EAX <- Sign Extend EAX
    }
}

impl CdqEmitter<Gpd, Gpd> for Assembler<'_> {
    fn cdq(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Cdq as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] EDX:EAX <- Sign Extend EAX
    }
}

impl CdqeEmitter<Gp> for Assembler<'_> {
    fn cdqe(&mut self, dst: Gp) {
        self.emit_n(InstId::Cdqe as u32, &[dst.as_operand()]); // X64 [EXPLICIT] RAX <- Sign Extend EAX
    }
}

impl CdqeEmitter<Gpq> for Assembler<'_> {
    fn cdqe(&mut self, dst: Gpq) {
        self.emit_n(InstId::Cdqe as u32, &[dst.as_operand()]); // X64 [EXPLICIT] RAX <- Sign Extend EAX
    }
}

impl CqoEmitter<Gp, Gp> for Assembler<'_> {
    fn cqo(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Cqo as u32, &[dst.as_operand(), src.as_operand()]); // X64 [EXPLICIT] RDX:RAX <- Sign Extend RAX
    }
}

impl CqoEmitter<Gpq, Gpq> for Assembler<'_> {
    fn cqo(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Cqo as u32, &[dst.as_operand(), src.as_operand()]); // X64 [EXPLICIT] RDX:RAX <- Sign Extend RAX
    }
}

impl CwdEmitter<Gp, Gp> for Assembler<'_> {
    fn cwd(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Cwd as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] DX:AX <- Sign Extend AX
    }
}

impl CwdEmitter<Gpw, Gpw> for Assembler<'_> {
    fn cwd(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Cwd as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] DX:AX <- Sign Extend AX
    }
}

impl CwdeEmitter<Gp> for Assembler<'_> {
    fn cwde(&mut self, dst: Gp) {
        self.emit_n(InstId::Cwde as u32, &[dst.as_operand()]); // ANY [EXPLICIT] EAX <- Sign Extend AX
    }
}

impl CwdeEmitter<Gpd> for Assembler<'_> {
    fn cwde(&mut self, dst: Gpd) {
        self.emit_n(InstId::Cwde as u32, &[dst.as_operand()]); // ANY [EXPLICIT] EAX <- Sign Extend AX
    }
}

impl CallEmitter<Gp> for Assembler<'_> {
    fn call(&mut self, target: Gp) {
        self.emit_n(InstId::Call as u32, &[target.as_operand()]); // ANY
    }
}

impl CallEmitter<Gpw> for Assembler<'_> {
    fn call(&mut self, target: Gpw) {
        self.emit_n(InstId::Call as u32, &[target.as_operand()]); // ANY
    }
}

impl CallEmitter<Gpd> for Assembler<'_> {
    fn call(&mut self, target: Gpd) {
        self.emit_n(InstId::Call as u32, &[target.as_operand()]); // ANY
    }
}

impl CallEmitter<Gpq> for Assembler<'_> {
    fn call(&mut self, target: Gpq) {
        self.emit_n(InstId::Call as u32, &[target.as_operand()]); // ANY
    }
}

impl CallEmitter<Mem> for Assembler<'_> {
    fn call(&mut self, target: Mem) {
        self.emit_n(InstId::Call as u32, &[target.as_operand()]); // ANY
    }
}

impl CallEmitter<Label> for Assembler<'_> {
    fn call(&mut self, target: Label) {
        self.emit_n(InstId::Call as u32, &[target.as_operand()]); // ANY
    }
}

impl<U0: Into<Imm>> CallEmitter<U0> for Assembler<'_> {
    fn call(&mut self, target: U0) {
        self.emit_n(
            InstId::Call as u32,
            &[Into::<Imm>::into(target).as_operand()],
        ); // ANY
    }
}

impl CmovEmitter<Gp, Gp> for Assembler<'_> {
    fn cmov(&mut self, cc: CondCode, dst: Gp, src: Gp) {
        self.emit_n(
            CMOVCC_FROM_COND[cc as usize],
            &[dst.as_operand(), src.as_operand()],
        ); // CMOV
    }
    fn cmovo(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Cmovo as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovno(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Cmovno as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovb(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Cmovb as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnb(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Cmovnb as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovz(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Cmovz as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnz(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Cmovnz as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovbe(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Cmovbe as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnbe(&mut self, dst: Gp, src: Gp) {
        self.emit_n(
            InstId::Cmovnbe as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
    fn cmovs(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Cmovs as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovns(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Cmovns as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovp(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Cmovp as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnp(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Cmovnp as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovl(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Cmovl as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnl(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Cmovnl as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovle(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Cmovle as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnle(&mut self, dst: Gp, src: Gp) {
        self.emit_n(
            InstId::Cmovnle as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl CmovEmitter<Gpw, Gpw> for Assembler<'_> {
    fn cmov(&mut self, cc: CondCode, dst: Gpw, src: Gpw) {
        self.emit_n(
            CMOVCC_FROM_COND[cc as usize],
            &[dst.as_operand(), src.as_operand()],
        ); // CMOV
    }
    fn cmovo(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Cmovo as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovno(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Cmovno as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovb(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Cmovb as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnb(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Cmovnb as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovz(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Cmovz as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnz(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Cmovnz as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovbe(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Cmovbe as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnbe(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(
            InstId::Cmovnbe as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
    fn cmovs(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Cmovs as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovns(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Cmovns as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovp(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Cmovp as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnp(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Cmovnp as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovl(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Cmovl as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnl(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Cmovnl as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovle(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Cmovle as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnle(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(
            InstId::Cmovnle as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl CmovEmitter<Gpd, Gpd> for Assembler<'_> {
    fn cmov(&mut self, cc: CondCode, dst: Gpd, src: Gpd) {
        self.emit_n(
            CMOVCC_FROM_COND[cc as usize],
            &[dst.as_operand(), src.as_operand()],
        ); // CMOV
    }
    fn cmovo(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Cmovo as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovno(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Cmovno as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovb(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Cmovb as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnb(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Cmovnb as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovz(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Cmovz as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnz(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Cmovnz as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovbe(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Cmovbe as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnbe(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Cmovnbe as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
    fn cmovs(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Cmovs as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovns(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Cmovns as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovp(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Cmovp as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnp(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Cmovnp as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovl(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Cmovl as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnl(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Cmovnl as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovle(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Cmovle as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnle(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Cmovnle as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl CmovEmitter<Gpq, Gpq> for Assembler<'_> {
    fn cmov(&mut self, cc: CondCode, dst: Gpq, src: Gpq) {
        self.emit_n(
            CMOVCC_FROM_COND[cc as usize],
            &[dst.as_operand(), src.as_operand()],
        ); // CMOV
    }
    fn cmovo(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Cmovo as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovno(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Cmovno as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovb(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Cmovb as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnb(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Cmovnb as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovz(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Cmovz as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnz(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Cmovnz as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovbe(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Cmovbe as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnbe(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Cmovnbe as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
    fn cmovs(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Cmovs as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovns(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Cmovns as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovp(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Cmovp as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnp(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Cmovnp as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovl(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Cmovl as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnl(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Cmovnl as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovle(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Cmovle as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnle(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Cmovnle as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl CmovEmitter<Gp, Mem> for Assembler<'_> {
    fn cmov(&mut self, cc: CondCode, dst: Gp, src: Mem) {
        self.emit_n(
            CMOVCC_FROM_COND[cc as usize],
            &[dst.as_operand(), src.as_operand()],
        ); // CMOV
    }
    fn cmovo(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Cmovo as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovno(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Cmovno as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovb(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Cmovb as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnb(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Cmovnb as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovz(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Cmovz as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnz(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Cmovnz as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovbe(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Cmovbe as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnbe(&mut self, dst: Gp, src: Mem) {
        self.emit_n(
            InstId::Cmovnbe as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
    fn cmovs(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Cmovs as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovns(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Cmovns as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovp(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Cmovp as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnp(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Cmovnp as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovl(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Cmovl as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnl(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Cmovnl as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovle(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Cmovle as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnle(&mut self, dst: Gp, src: Mem) {
        self.emit_n(
            InstId::Cmovnle as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl CmovEmitter<Gpw, Mem> for Assembler<'_> {
    fn cmov(&mut self, cc: CondCode, dst: Gpw, src: Mem) {
        self.emit_n(
            CMOVCC_FROM_COND[cc as usize],
            &[dst.as_operand(), src.as_operand()],
        ); // CMOV
    }
    fn cmovo(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Cmovo as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovno(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Cmovno as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovb(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Cmovb as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnb(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Cmovnb as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovz(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Cmovz as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnz(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Cmovnz as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovbe(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Cmovbe as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnbe(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(
            InstId::Cmovnbe as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
    fn cmovs(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Cmovs as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovns(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Cmovns as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovp(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Cmovp as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnp(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Cmovnp as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovl(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Cmovl as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnl(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Cmovnl as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovle(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Cmovle as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnle(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(
            InstId::Cmovnle as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl CmovEmitter<Gpd, Mem> for Assembler<'_> {
    fn cmov(&mut self, cc: CondCode, dst: Gpd, src: Mem) {
        self.emit_n(
            CMOVCC_FROM_COND[cc as usize],
            &[dst.as_operand(), src.as_operand()],
        ); // CMOV
    }
    fn cmovo(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Cmovo as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovno(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Cmovno as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovb(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Cmovb as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnb(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Cmovnb as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovz(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Cmovz as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnz(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Cmovnz as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovbe(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Cmovbe as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnbe(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(
            InstId::Cmovnbe as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
    fn cmovs(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Cmovs as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovns(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Cmovns as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovp(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Cmovp as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnp(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Cmovnp as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovl(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Cmovl as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnl(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Cmovnl as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovle(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Cmovle as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnle(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(
            InstId::Cmovnle as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl CmovEmitter<Gpq, Mem> for Assembler<'_> {
    fn cmov(&mut self, cc: CondCode, dst: Gpq, src: Mem) {
        self.emit_n(
            CMOVCC_FROM_COND[cc as usize],
            &[dst.as_operand(), src.as_operand()],
        ); // CMOV
    }
    fn cmovo(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Cmovo as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovno(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Cmovno as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovb(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Cmovb as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnb(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Cmovnb as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovz(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Cmovz as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnz(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Cmovnz as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovbe(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Cmovbe as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnbe(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(
            InstId::Cmovnbe as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
    fn cmovs(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Cmovs as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovns(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Cmovns as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovp(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Cmovp as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnp(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Cmovnp as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovl(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Cmovl as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnl(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Cmovnl as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovle(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Cmovle as u32, &[dst.as_operand(), src.as_operand()]);
    }
    fn cmovnle(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(
            InstId::Cmovnle as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl CmpEmitter<Gp, Gp> for Assembler<'_> {
    fn cmp(&mut self, lhs: Gp, rhs: Gp) {
        self.emit_n(InstId::Cmp as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl CmpEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn cmp(&mut self, lhs: GpbLo, rhs: GpbLo) {
        self.emit_n(InstId::Cmp as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl CmpEmitter<GpbLo, GpbHi> for Assembler<'_> {
    fn cmp(&mut self, lhs: GpbLo, rhs: GpbHi) {
        self.emit_n(InstId::Cmp as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl CmpEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn cmp(&mut self, lhs: GpbHi, rhs: GpbLo) {
        self.emit_n(InstId::Cmp as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl CmpEmitter<GpbHi, GpbHi> for Assembler<'_> {
    fn cmp(&mut self, lhs: GpbHi, rhs: GpbHi) {
        self.emit_n(InstId::Cmp as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl CmpEmitter<Gpw, Gpw> for Assembler<'_> {
    fn cmp(&mut self, lhs: Gpw, rhs: Gpw) {
        self.emit_n(InstId::Cmp as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl CmpEmitter<Gpd, Gpd> for Assembler<'_> {
    fn cmp(&mut self, lhs: Gpd, rhs: Gpd) {
        self.emit_n(InstId::Cmp as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl CmpEmitter<Gpq, Gpq> for Assembler<'_> {
    fn cmp(&mut self, lhs: Gpq, rhs: Gpq) {
        self.emit_n(InstId::Cmp as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl CmpEmitter<Gp, Mem> for Assembler<'_> {
    fn cmp(&mut self, lhs: Gp, rhs: Mem) {
        self.emit_n(InstId::Cmp as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl CmpEmitter<GpbLo, Mem> for Assembler<'_> {
    fn cmp(&mut self, lhs: GpbLo, rhs: Mem) {
        self.emit_n(InstId::Cmp as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl CmpEmitter<GpbHi, Mem> for Assembler<'_> {
    fn cmp(&mut self, lhs: GpbHi, rhs: Mem) {
        self.emit_n(InstId::Cmp as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl CmpEmitter<Gpw, Mem> for Assembler<'_> {
    fn cmp(&mut self, lhs: Gpw, rhs: Mem) {
        self.emit_n(InstId::Cmp as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl CmpEmitter<Gpd, Mem> for Assembler<'_> {
    fn cmp(&mut self, lhs: Gpd, rhs: Mem) {
        self.emit_n(InstId::Cmp as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl CmpEmitter<Gpq, Mem> for Assembler<'_> {
    fn cmp(&mut self, lhs: Gpq, rhs: Mem) {
        self.emit_n(InstId::Cmp as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl CmpEmitter<Gp, Imm> for Assembler<'_> {
    fn cmp(&mut self, lhs: Gp, imm: Imm) {
        self.emit_n(InstId::Cmp as u32, &[lhs.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> CmpEmitter<GpbLo, U1> for Assembler<'_> {
    fn cmp(&mut self, lhs: GpbLo, rhs: U1) {
        self.emit_n(
            InstId::Cmp as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> CmpEmitter<GpbHi, U1> for Assembler<'_> {
    fn cmp(&mut self, lhs: GpbHi, rhs: U1) {
        self.emit_n(
            InstId::Cmp as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> CmpEmitter<Gpw, U1> for Assembler<'_> {
    fn cmp(&mut self, lhs: Gpw, rhs: U1) {
        self.emit_n(
            InstId::Cmp as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> CmpEmitter<Gpd, U1> for Assembler<'_> {
    fn cmp(&mut self, lhs: Gpd, rhs: U1) {
        self.emit_n(
            InstId::Cmp as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> CmpEmitter<Gpq, U1> for Assembler<'_> {
    fn cmp(&mut self, lhs: Gpq, rhs: U1) {
        self.emit_n(
            InstId::Cmp as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl CmpEmitter<Mem, Gp> for Assembler<'_> {
    fn cmp(&mut self, lhs: Mem, rhs: Gp) {
        self.emit_n(InstId::Cmp as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl CmpEmitter<Mem, GpbLo> for Assembler<'_> {
    fn cmp(&mut self, lhs: Mem, rhs: GpbLo) {
        self.emit_n(InstId::Cmp as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl CmpEmitter<Mem, GpbHi> for Assembler<'_> {
    fn cmp(&mut self, lhs: Mem, rhs: GpbHi) {
        self.emit_n(InstId::Cmp as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl CmpEmitter<Mem, Gpw> for Assembler<'_> {
    fn cmp(&mut self, lhs: Mem, rhs: Gpw) {
        self.emit_n(InstId::Cmp as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl CmpEmitter<Mem, Gpd> for Assembler<'_> {
    fn cmp(&mut self, lhs: Mem, rhs: Gpd) {
        self.emit_n(InstId::Cmp as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl CmpEmitter<Mem, Gpq> for Assembler<'_> {
    fn cmp(&mut self, lhs: Mem, rhs: Gpq) {
        self.emit_n(InstId::Cmp as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> CmpEmitter<Mem, U1> for Assembler<'_> {
    fn cmp(&mut self, lhs: Mem, rhs: U1) {
        self.emit_n(
            InstId::Cmp as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl CmpsEmitter<Mem, Mem> for Assembler<'_> {
    fn cmps(&mut self, lhs: Mem, rhs: Mem) {
        self.emit_n(InstId::Cmps as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY [EXPLICIT]
    }
}

impl CmpxchgEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn cmpxchg(&mut self, dst: Gp, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<GpbLo, GpbLo, GpbLo> for Assembler<'_> {
    fn cmpxchg(&mut self, dst: GpbLo, src1: GpbLo, src2: GpbLo) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<GpbLo, GpbHi, GpbLo> for Assembler<'_> {
    fn cmpxchg(&mut self, dst: GpbLo, src1: GpbHi, src2: GpbLo) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<GpbHi, GpbLo, GpbLo> for Assembler<'_> {
    fn cmpxchg(&mut self, dst: GpbHi, src1: GpbLo, src2: GpbLo) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<GpbHi, GpbHi, GpbLo> for Assembler<'_> {
    fn cmpxchg(&mut self, dst: GpbHi, src1: GpbHi, src2: GpbLo) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<Gpw, Gpw, Gpw> for Assembler<'_> {
    fn cmpxchg(&mut self, dst: Gpw, src1: Gpw, src2: Gpw) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn cmpxchg(&mut self, dst: Gpd, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn cmpxchg(&mut self, dst: Gpq, src1: Gpq, src2: Gpq) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpxchg(&mut self, dst: Mem, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<Mem, GpbLo, GpbLo> for Assembler<'_> {
    fn cmpxchg(&mut self, dst: Mem, src1: GpbLo, src2: GpbLo) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<Mem, GpbHi, GpbLo> for Assembler<'_> {
    fn cmpxchg(&mut self, dst: Mem, src1: GpbHi, src2: GpbLo) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<Mem, Gpw, Gpw> for Assembler<'_> {
    fn cmpxchg(&mut self, dst: Mem, src1: Gpw, src2: Gpw) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpxchg(&mut self, dst: Mem, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpxchg(&mut self, dst: Mem, src1: Gpq, src2: Gpq) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl Cmpxchg16bEmitter<Mem, Gp, Gp, Gp, Gp> for Assembler<'_> {
    fn cmpxchg16b(&mut self, dst: Mem, dst2: Gp, dst3: Gp, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Cmpxchg16b as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                dst3.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // CMPXCHG16B [EXPLICIT] m == EDX:EAX ? m <- ECX:EBX
    }
}

impl Cmpxchg16bEmitter<Mem, Gpq, Gpq, Gpq, Gpq> for Assembler<'_> {
    fn cmpxchg16b(&mut self, dst: Mem, dst2: Gpq, dst3: Gpq, src1: Gpq, src2: Gpq) {
        self.emit_n(
            InstId::Cmpxchg16b as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                dst3.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // CMPXCHG16B [EXPLICIT] m == EDX:EAX ? m <- ECX:EBX
    }
}

impl Cmpxchg8bEmitter<Mem, Gp, Gp, Gp, Gp> for Assembler<'_> {
    fn cmpxchg8b(&mut self, dst: Mem, dst2: Gp, dst3: Gp, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Cmpxchg8b as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                dst3.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // CMPXCHG8B [EXPLICIT] m == RDX:RAX ? m <- RCX:RBX
    }
}

impl Cmpxchg8bEmitter<Mem, Gpd, Gpd, Gpd, Gpd> for Assembler<'_> {
    fn cmpxchg8b(&mut self, dst: Mem, dst2: Gpd, dst3: Gpd, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Cmpxchg8b as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                dst3.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // CMPXCHG8B [EXPLICIT] m == RDX:RAX ? m <- RCX:RBX
    }
}

impl DecEmitter<Gp> for Assembler<'_> {
    fn dec(&mut self, dst: Gp) {
        self.emit_n(InstId::Dec as u32, &[dst.as_operand()]); // ANY
    }
}

impl DecEmitter<Gpw> for Assembler<'_> {
    fn dec(&mut self, dst: Gpw) {
        self.emit_n(InstId::Dec as u32, &[dst.as_operand()]); // ANY
    }
}

impl DecEmitter<Gpd> for Assembler<'_> {
    fn dec(&mut self, dst: Gpd) {
        self.emit_n(InstId::Dec as u32, &[dst.as_operand()]); // ANY
    }
}

impl DecEmitter<GpbLo> for Assembler<'_> {
    fn dec(&mut self, dst: GpbLo) {
        self.emit_n(InstId::Dec as u32, &[dst.as_operand()]); // ANY
    }
}

impl DecEmitter<GpbHi> for Assembler<'_> {
    fn dec(&mut self, dst: GpbHi) {
        self.emit_n(InstId::Dec as u32, &[dst.as_operand()]); // ANY
    }
}

impl DecEmitter<Gpq> for Assembler<'_> {
    fn dec(&mut self, dst: Gpq) {
        self.emit_n(InstId::Dec as u32, &[dst.as_operand()]); // ANY
    }
}

impl DecEmitter<Mem> for Assembler<'_> {
    fn dec(&mut self, dst: Mem) {
        self.emit_n(InstId::Dec as u32, &[dst.as_operand()]); // ANY
    }
}

impl DivEmitter<Gp, Gp> for Assembler<'_> {
    fn div(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Div as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AH[Rem]: AL[Quot] <- AX / r8
    }
}

impl DivEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn div(&mut self, dst: Gpw, src: GpbLo) {
        self.emit_n(InstId::Div as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AH[Rem]: AL[Quot] <- AX / r8
    }
}

impl DivEmitter<Gpw, GpbHi> for Assembler<'_> {
    fn div(&mut self, dst: Gpw, src: GpbHi) {
        self.emit_n(InstId::Div as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AH[Rem]: AL[Quot] <- AX / r8
    }
}

impl DivEmitter<Gp, Mem> for Assembler<'_> {
    fn div(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Div as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AH[Rem]: AL[Quot] <- AX / m8
    }
}

impl DivEmitter<Gpw, Mem> for Assembler<'_> {
    fn div(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Div as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AH[Rem]: AL[Quot] <- AX / m8
    }
}

impl Div3Emitter<Gp, Gp, Gp> for Assembler<'_> {
    fn div_3(&mut self, dst: Gp, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Div as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / r16|r32|r64
    }
}

impl Div3Emitter<Gpw, Gpw, Gpw> for Assembler<'_> {
    fn div_3(&mut self, dst: Gpw, dst2: Gpw, src: Gpw) {
        self.emit_n(
            InstId::Div as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / r16|r32|r64
    }
}

impl Div3Emitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn div_3(&mut self, dst: Gpd, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Div as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / r16|r32|r64
    }
}

impl Div3Emitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn div_3(&mut self, dst: Gpq, dst2: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Div as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / r16|r32|r64
    }
}

impl Div3Emitter<Gp, Gp, Mem> for Assembler<'_> {
    fn div_3(&mut self, dst: Gp, dst2: Gp, src: Mem) {
        self.emit_n(
            InstId::Div as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / m16|m32|m64
    }
}

impl Div3Emitter<Gpw, Gpw, Mem> for Assembler<'_> {
    fn div_3(&mut self, dst: Gpw, dst2: Gpw, src: Mem) {
        self.emit_n(
            InstId::Div as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / m16|m32|m64
    }
}

impl Div3Emitter<Gpd, Gpd, Mem> for Assembler<'_> {
    fn div_3(&mut self, dst: Gpd, dst2: Gpd, src: Mem) {
        self.emit_n(
            InstId::Div as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / m16|m32|m64
    }
}

impl Div3Emitter<Gpq, Gpq, Mem> for Assembler<'_> {
    fn div_3(&mut self, dst: Gpq, dst2: Gpq, src: Mem) {
        self.emit_n(
            InstId::Div as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / m16|m32|m64
    }
}

impl IdivEmitter<Gp, Gp> for Assembler<'_> {
    fn idiv(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Idiv as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AH[Rem]: AL[Quot] <- AX / r8
    }
}

impl IdivEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn idiv(&mut self, dst: Gpw, src: GpbLo) {
        self.emit_n(InstId::Idiv as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AH[Rem]: AL[Quot] <- AX / r8
    }
}

impl IdivEmitter<Gpw, GpbHi> for Assembler<'_> {
    fn idiv(&mut self, dst: Gpw, src: GpbHi) {
        self.emit_n(InstId::Idiv as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AH[Rem]: AL[Quot] <- AX / r8
    }
}

impl IdivEmitter<Gp, Mem> for Assembler<'_> {
    fn idiv(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Idiv as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AH[Rem]: AL[Quot] <- AX / m8
    }
}

impl IdivEmitter<Gpw, Mem> for Assembler<'_> {
    fn idiv(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Idiv as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AH[Rem]: AL[Quot] <- AX / m8
    }
}

impl Idiv3Emitter<Gp, Gp, Gp> for Assembler<'_> {
    fn idiv_3(&mut self, dst: Gp, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Idiv as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / r16|r32|r64
    }
}

impl Idiv3Emitter<Gpw, Gpw, Gpw> for Assembler<'_> {
    fn idiv_3(&mut self, dst: Gpw, dst2: Gpw, src: Gpw) {
        self.emit_n(
            InstId::Idiv as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / r16|r32|r64
    }
}

impl Idiv3Emitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn idiv_3(&mut self, dst: Gpd, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Idiv as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / r16|r32|r64
    }
}

impl Idiv3Emitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn idiv_3(&mut self, dst: Gpq, dst2: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Idiv as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / r16|r32|r64
    }
}

impl Idiv3Emitter<Gp, Gp, Mem> for Assembler<'_> {
    fn idiv_3(&mut self, dst: Gp, dst2: Gp, src: Mem) {
        self.emit_n(
            InstId::Idiv as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / m16|m32|m64
    }
}

impl Idiv3Emitter<Gpw, Gpw, Mem> for Assembler<'_> {
    fn idiv_3(&mut self, dst: Gpw, dst2: Gpw, src: Mem) {
        self.emit_n(
            InstId::Idiv as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / m16|m32|m64
    }
}

impl Idiv3Emitter<Gpd, Gpd, Mem> for Assembler<'_> {
    fn idiv_3(&mut self, dst: Gpd, dst2: Gpd, src: Mem) {
        self.emit_n(
            InstId::Idiv as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / m16|m32|m64
    }
}

impl Idiv3Emitter<Gpq, Gpq, Mem> for Assembler<'_> {
    fn idiv_3(&mut self, dst: Gpq, dst2: Gpq, src: Mem) {
        self.emit_n(
            InstId::Idiv as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / m16|m32|m64
    }
}

impl ImulEmitter<Gp, Gp> for Assembler<'_> {
    fn imul(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Imul as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AX <- AL * r8 | ra <- ra * rb
    }
}

impl ImulEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn imul(&mut self, dst: Gpw, src: GpbLo) {
        self.emit_n(InstId::Imul as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AX <- AL * r8 | ra <- ra * rb
    }
}

impl ImulEmitter<Gpw, GpbHi> for Assembler<'_> {
    fn imul(&mut self, dst: Gpw, src: GpbHi) {
        self.emit_n(InstId::Imul as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AX <- AL * r8 | ra <- ra * rb
    }
}

impl ImulEmitter<Gpw, Gpw> for Assembler<'_> {
    fn imul(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Imul as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AX <- AL * r8 | ra <- ra * rb
    }
}

impl ImulEmitter<Gpd, Gpd> for Assembler<'_> {
    fn imul(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Imul as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AX <- AL * r8 | ra <- ra * rb
    }
}

impl ImulEmitter<Gpq, Gpq> for Assembler<'_> {
    fn imul(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Imul as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AX <- AL * r8 | ra <- ra * rb
    }
}

impl ImulEmitter<Gp, Mem> for Assembler<'_> {
    fn imul(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Imul as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AX <- AL * m8 | ra <- ra * m16|m32|m64
    }
}

impl ImulEmitter<Gpw, Mem> for Assembler<'_> {
    fn imul(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Imul as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AX <- AL * m8 | ra <- ra * m16|m32|m64
    }
}

impl ImulEmitter<Gpd, Mem> for Assembler<'_> {
    fn imul(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Imul as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AX <- AL * m8 | ra <- ra * m16|m32|m64
    }
}

impl ImulEmitter<Gpq, Mem> for Assembler<'_> {
    fn imul(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Imul as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AX <- AL * m8 | ra <- ra * m16|m32|m64
    }
}

impl Imul3Emitter<Gp, Gp, Imm> for Assembler<'_> {
    fn imul_3(&mut self, dst: Gp, dst2: Gp, imm: Imm) {
        self.emit_n(
            InstId::Imul as u32,
            &[dst.as_operand(), dst2.as_operand(), imm.as_operand()],
        ); // ANY
    }
}

impl<U2: Into<Imm>> Imul3Emitter<Gpw, Gpw, U2> for Assembler<'_> {
    fn imul_3(&mut self, dst: Gpw, dst2: Gpw, src: U2) {
        self.emit_n(
            InstId::Imul as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                Into::<Imm>::into(src).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> Imul3Emitter<Gpd, Gpd, U2> for Assembler<'_> {
    fn imul_3(&mut self, dst: Gpd, dst2: Gpd, src: U2) {
        self.emit_n(
            InstId::Imul as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                Into::<Imm>::into(src).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> Imul3Emitter<Gpq, Gpq, U2> for Assembler<'_> {
    fn imul_3(&mut self, dst: Gpq, dst2: Gpq, src: U2) {
        self.emit_n(
            InstId::Imul as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                Into::<Imm>::into(src).as_operand(),
            ],
        ); // ANY
    }
}

impl Imul3Emitter<Gp, Mem, Imm> for Assembler<'_> {
    fn imul_3(&mut self, dst: Gp, dst2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Imul as u32,
            &[dst.as_operand(), dst2.as_operand(), imm.as_operand()],
        ); // ANY
    }
}

impl<U2: Into<Imm>> Imul3Emitter<Gpw, Mem, U2> for Assembler<'_> {
    fn imul_3(&mut self, dst: Gpw, dst2: Mem, src: U2) {
        self.emit_n(
            InstId::Imul as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                Into::<Imm>::into(src).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> Imul3Emitter<Gpd, Mem, U2> for Assembler<'_> {
    fn imul_3(&mut self, dst: Gpd, dst2: Mem, src: U2) {
        self.emit_n(
            InstId::Imul as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                Into::<Imm>::into(src).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> Imul3Emitter<Gpq, Mem, U2> for Assembler<'_> {
    fn imul_3(&mut self, dst: Gpq, dst2: Mem, src: U2) {
        self.emit_n(
            InstId::Imul as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                Into::<Imm>::into(src).as_operand(),
            ],
        ); // ANY
    }
}

impl Imul3Emitter<Gp, Gp, Gp> for Assembler<'_> {
    fn imul_3(&mut self, dst: Gp, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Imul as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * r16|r32|r64
    }
}

impl Imul3Emitter<Gpw, Gpw, Gpw> for Assembler<'_> {
    fn imul_3(&mut self, dst: Gpw, dst2: Gpw, src: Gpw) {
        self.emit_n(
            InstId::Imul as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * r16|r32|r64
    }
}

impl Imul3Emitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn imul_3(&mut self, dst: Gpd, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Imul as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * r16|r32|r64
    }
}

impl Imul3Emitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn imul_3(&mut self, dst: Gpq, dst2: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Imul as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * r16|r32|r64
    }
}

impl Imul3Emitter<Gp, Gp, Mem> for Assembler<'_> {
    fn imul_3(&mut self, dst: Gp, dst2: Gp, src: Mem) {
        self.emit_n(
            InstId::Imul as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * m16|m32|m64
    }
}

impl Imul3Emitter<Gpw, Gpw, Mem> for Assembler<'_> {
    fn imul_3(&mut self, dst: Gpw, dst2: Gpw, src: Mem) {
        self.emit_n(
            InstId::Imul as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * m16|m32|m64
    }
}

impl Imul3Emitter<Gpd, Gpd, Mem> for Assembler<'_> {
    fn imul_3(&mut self, dst: Gpd, dst2: Gpd, src: Mem) {
        self.emit_n(
            InstId::Imul as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * m16|m32|m64
    }
}

impl Imul3Emitter<Gpq, Gpq, Mem> for Assembler<'_> {
    fn imul_3(&mut self, dst: Gpq, dst2: Gpq, src: Mem) {
        self.emit_n(
            InstId::Imul as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * m16|m32|m64
    }
}

impl IncEmitter<Gp> for Assembler<'_> {
    fn inc(&mut self, dst: Gp) {
        self.emit_n(InstId::Inc as u32, &[dst.as_operand()]); // ANY
    }
}

impl IncEmitter<Gpw> for Assembler<'_> {
    fn inc(&mut self, dst: Gpw) {
        self.emit_n(InstId::Inc as u32, &[dst.as_operand()]); // ANY
    }
}

impl IncEmitter<Gpd> for Assembler<'_> {
    fn inc(&mut self, dst: Gpd) {
        self.emit_n(InstId::Inc as u32, &[dst.as_operand()]); // ANY
    }
}

impl IncEmitter<GpbLo> for Assembler<'_> {
    fn inc(&mut self, dst: GpbLo) {
        self.emit_n(InstId::Inc as u32, &[dst.as_operand()]); // ANY
    }
}

impl IncEmitter<GpbHi> for Assembler<'_> {
    fn inc(&mut self, dst: GpbHi) {
        self.emit_n(InstId::Inc as u32, &[dst.as_operand()]); // ANY
    }
}

impl IncEmitter<Gpq> for Assembler<'_> {
    fn inc(&mut self, dst: Gpq) {
        self.emit_n(InstId::Inc as u32, &[dst.as_operand()]); // ANY
    }
}

impl IncEmitter<Mem> for Assembler<'_> {
    fn inc(&mut self, dst: Mem) {
        self.emit_n(InstId::Inc as u32, &[dst.as_operand()]); // ANY
    }
}

impl JEmitter<Label> for Assembler<'_> {
    fn j(&mut self, cc: CondCode, target: Label) {
        self.emit_n(JCC_FROM_COND[cc as usize], &[target.as_operand()]); // ANY
    }
    fn jo(&mut self, target: Label) {
        self.emit_n(InstId::Jo as u32, &[target.as_operand()]);
    }
    fn jno(&mut self, target: Label) {
        self.emit_n(InstId::Jno as u32, &[target.as_operand()]);
    }
    fn jb(&mut self, target: Label) {
        self.emit_n(InstId::Jb as u32, &[target.as_operand()]);
    }
    fn jnb(&mut self, target: Label) {
        self.emit_n(InstId::Jnb as u32, &[target.as_operand()]);
    }
    fn jz(&mut self, target: Label) {
        self.emit_n(InstId::Jz as u32, &[target.as_operand()]);
    }
    fn jnz(&mut self, target: Label) {
        self.emit_n(InstId::Jnz as u32, &[target.as_operand()]);
    }
    fn jbe(&mut self, target: Label) {
        self.emit_n(InstId::Jbe as u32, &[target.as_operand()]);
    }
    fn jnbe(&mut self, target: Label) {
        self.emit_n(InstId::Jnbe as u32, &[target.as_operand()]);
    }
    fn js(&mut self, target: Label) {
        self.emit_n(InstId::Js as u32, &[target.as_operand()]);
    }
    fn jns(&mut self, target: Label) {
        self.emit_n(InstId::Jns as u32, &[target.as_operand()]);
    }
    fn jp(&mut self, target: Label) {
        self.emit_n(InstId::Jp as u32, &[target.as_operand()]);
    }
    fn jnp(&mut self, target: Label) {
        self.emit_n(InstId::Jnp as u32, &[target.as_operand()]);
    }
    fn jl(&mut self, target: Label) {
        self.emit_n(InstId::Jl as u32, &[target.as_operand()]);
    }
    fn jnl(&mut self, target: Label) {
        self.emit_n(InstId::Jnl as u32, &[target.as_operand()]);
    }
    fn jle(&mut self, target: Label) {
        self.emit_n(InstId::Jle as u32, &[target.as_operand()]);
    }
    fn jnle(&mut self, target: Label) {
        self.emit_n(InstId::Jnle as u32, &[target.as_operand()]);
    }
}

impl<U0: Into<Imm>> JEmitter<U0> for Assembler<'_> {
    fn j(&mut self, cc: CondCode, target: U0) {
        self.emit_n(
            JCC_FROM_COND[cc as usize],
            &[Into::<Imm>::into(target).as_operand()],
        ); // ANY
    }
    fn jo(&mut self, target: U0) {
        self.emit_n(InstId::Jo as u32, &[Into::<Imm>::into(target).as_operand()]);
    }
    fn jno(&mut self, target: U0) {
        self.emit_n(
            InstId::Jno as u32,
            &[Into::<Imm>::into(target).as_operand()],
        );
    }
    fn jb(&mut self, target: U0) {
        self.emit_n(InstId::Jb as u32, &[Into::<Imm>::into(target).as_operand()]);
    }
    fn jnb(&mut self, target: U0) {
        self.emit_n(
            InstId::Jnb as u32,
            &[Into::<Imm>::into(target).as_operand()],
        );
    }
    fn jz(&mut self, target: U0) {
        self.emit_n(InstId::Jz as u32, &[Into::<Imm>::into(target).as_operand()]);
    }
    fn jnz(&mut self, target: U0) {
        self.emit_n(
            InstId::Jnz as u32,
            &[Into::<Imm>::into(target).as_operand()],
        );
    }
    fn jbe(&mut self, target: U0) {
        self.emit_n(
            InstId::Jbe as u32,
            &[Into::<Imm>::into(target).as_operand()],
        );
    }
    fn jnbe(&mut self, target: U0) {
        self.emit_n(
            InstId::Jnbe as u32,
            &[Into::<Imm>::into(target).as_operand()],
        );
    }
    fn js(&mut self, target: U0) {
        self.emit_n(InstId::Js as u32, &[Into::<Imm>::into(target).as_operand()]);
    }
    fn jns(&mut self, target: U0) {
        self.emit_n(
            InstId::Jns as u32,
            &[Into::<Imm>::into(target).as_operand()],
        );
    }
    fn jp(&mut self, target: U0) {
        self.emit_n(InstId::Jp as u32, &[Into::<Imm>::into(target).as_operand()]);
    }
    fn jnp(&mut self, target: U0) {
        self.emit_n(
            InstId::Jnp as u32,
            &[Into::<Imm>::into(target).as_operand()],
        );
    }
    fn jl(&mut self, target: U0) {
        self.emit_n(InstId::Jl as u32, &[Into::<Imm>::into(target).as_operand()]);
    }
    fn jnl(&mut self, target: U0) {
        self.emit_n(
            InstId::Jnl as u32,
            &[Into::<Imm>::into(target).as_operand()],
        );
    }
    fn jle(&mut self, target: U0) {
        self.emit_n(
            InstId::Jle as u32,
            &[Into::<Imm>::into(target).as_operand()],
        );
    }
    fn jnle(&mut self, target: U0) {
        self.emit_n(
            InstId::Jnle as u32,
            &[Into::<Imm>::into(target).as_operand()],
        );
    }
}

impl JecxzEmitter<Gp, Label> for Assembler<'_> {
    fn jecxz(&mut self, lhs: Gp, target: Label) {
        self.emit_n(
            InstId::Jecxz as u32,
            &[lhs.as_operand(), target.as_operand()],
        ); // ANY [EXPLICIT] Short jump if CX/ECX/RCX is zero.
    }
}

impl JecxzEmitter<Gpw, Label> for Assembler<'_> {
    fn jecxz(&mut self, lhs: Gpw, target: Label) {
        self.emit_n(
            InstId::Jecxz as u32,
            &[lhs.as_operand(), target.as_operand()],
        ); // ANY [EXPLICIT] Short jump if CX/ECX/RCX is zero.
    }
}

impl JecxzEmitter<Gpd, Label> for Assembler<'_> {
    fn jecxz(&mut self, lhs: Gpd, target: Label) {
        self.emit_n(
            InstId::Jecxz as u32,
            &[lhs.as_operand(), target.as_operand()],
        ); // ANY [EXPLICIT] Short jump if CX/ECX/RCX is zero.
    }
}

impl JecxzEmitter<Gpq, Label> for Assembler<'_> {
    fn jecxz(&mut self, lhs: Gpq, target: Label) {
        self.emit_n(
            InstId::Jecxz as u32,
            &[lhs.as_operand(), target.as_operand()],
        ); // ANY [EXPLICIT] Short jump if CX/ECX/RCX is zero.
    }
}

impl JecxzEmitter<Gp, Imm> for Assembler<'_> {
    fn jecxz(&mut self, lhs: Gp, imm: Imm) {
        self.emit_n(InstId::Jecxz as u32, &[lhs.as_operand(), imm.as_operand()]); // ANY [EXPLICIT] Short jump if CX/ECX/RCX is zero.
    }
}

impl<U1: Into<Imm>> JecxzEmitter<Gpw, U1> for Assembler<'_> {
    fn jecxz(&mut self, lhs: Gpw, rhs: U1) {
        self.emit_n(
            InstId::Jecxz as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY [EXPLICIT] Short jump if CX/ECX/RCX is zero.
    }
}

impl<U1: Into<Imm>> JecxzEmitter<Gpd, U1> for Assembler<'_> {
    fn jecxz(&mut self, lhs: Gpd, rhs: U1) {
        self.emit_n(
            InstId::Jecxz as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY [EXPLICIT] Short jump if CX/ECX/RCX is zero.
    }
}

impl<U1: Into<Imm>> JecxzEmitter<Gpq, U1> for Assembler<'_> {
    fn jecxz(&mut self, lhs: Gpq, rhs: U1) {
        self.emit_n(
            InstId::Jecxz as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY [EXPLICIT] Short jump if CX/ECX/RCX is zero.
    }
}

impl JmpEmitter<Gp> for Assembler<'_> {
    fn jmp(&mut self, target: Gp) {
        self.emit_n(InstId::Jmp as u32, &[target.as_operand()]); // ANY
    }
}

impl JmpEmitter<Gpd> for Assembler<'_> {
    fn jmp(&mut self, target: Gpd) {
        self.emit_n(InstId::Jmp as u32, &[target.as_operand()]); // ANY
    }
}

impl JmpEmitter<Gpq> for Assembler<'_> {
    fn jmp(&mut self, target: Gpq) {
        self.emit_n(InstId::Jmp as u32, &[target.as_operand()]); // ANY
    }
}

impl JmpEmitter<Mem> for Assembler<'_> {
    fn jmp(&mut self, target: Mem) {
        self.emit_n(InstId::Jmp as u32, &[target.as_operand()]); // ANY
    }
}

impl JmpEmitter<Label> for Assembler<'_> {
    fn jmp(&mut self, target: Label) {
        self.emit_n(InstId::Jmp as u32, &[target.as_operand()]); // ANY
    }
}

impl<U0: Into<Imm>> JmpEmitter<U0> for Assembler<'_> {
    fn jmp(&mut self, target: U0) {
        self.emit_n(
            InstId::Jmp as u32,
            &[Into::<Imm>::into(target).as_operand()],
        ); // ANY
    }
}

impl<U0: Into<Imm>, U1: Into<Imm>> LcallEmitter<U0, U1> for Assembler<'_> {
    fn lcall(&mut self, lhs: U0, rhs: U1) {
        self.emit_n(
            InstId::Lcall as u32,
            &[
                Into::<Imm>::into(lhs).as_operand(),
                Into::<Imm>::into(rhs).as_operand(),
            ],
        ); // ANY
    }
}

impl Lcall1Emitter<Mem> for Assembler<'_> {
    fn lcall_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Lcall as u32, &[mem.as_operand()]); // ANY
    }
}

impl LeaEmitter<Gp, Mem> for Assembler<'_> {
    fn lea(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Lea as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LeaEmitter<Gpw, Mem> for Assembler<'_> {
    fn lea(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Lea as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LeaEmitter<Gpd, Mem> for Assembler<'_> {
    fn lea(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Lea as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LeaEmitter<Gpq, Mem> for Assembler<'_> {
    fn lea(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Lea as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl<U0: Into<Imm>, U1: Into<Imm>> LjmpEmitter<U0, U1> for Assembler<'_> {
    fn ljmp(&mut self, lhs: U0, rhs: U1) {
        self.emit_n(
            InstId::Ljmp as u32,
            &[
                Into::<Imm>::into(lhs).as_operand(),
                Into::<Imm>::into(rhs).as_operand(),
            ],
        ); // ANY
    }
}

impl Ljmp1Emitter<Mem> for Assembler<'_> {
    fn ljmp_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Ljmp as u32, &[mem.as_operand()]); // ANY
    }
}

impl LodsEmitter<Gp, Mem> for Assembler<'_> {
    fn lods(&mut self, lhs: Gp, rhs: Mem) {
        self.emit_n(InstId::Lods as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY [EXPLICIT]
    }
}

impl LodsEmitter<GpbLo, Mem> for Assembler<'_> {
    fn lods(&mut self, lhs: GpbLo, rhs: Mem) {
        self.emit_n(InstId::Lods as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY [EXPLICIT]
    }
}

impl LodsEmitter<Gpw, Mem> for Assembler<'_> {
    fn lods(&mut self, lhs: Gpw, rhs: Mem) {
        self.emit_n(InstId::Lods as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY [EXPLICIT]
    }
}

impl LodsEmitter<Gpd, Mem> for Assembler<'_> {
    fn lods(&mut self, lhs: Gpd, rhs: Mem) {
        self.emit_n(InstId::Lods as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY [EXPLICIT]
    }
}

impl LodsEmitter<Gpq, Mem> for Assembler<'_> {
    fn lods(&mut self, lhs: Gpq, rhs: Mem) {
        self.emit_n(InstId::Lods as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY [EXPLICIT]
    }
}

impl LoopEmitter<Gp, Label> for Assembler<'_> {
    fn r#loop(&mut self, target: Gp, target2: Label) {
        self.emit_n(
            InstId::Loop as u32,
            &[target.as_operand(), target2.as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0.
    }
}

impl LoopEmitter<Gpw, Label> for Assembler<'_> {
    fn r#loop(&mut self, target: Gpw, target2: Label) {
        self.emit_n(
            InstId::Loop as u32,
            &[target.as_operand(), target2.as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0.
    }
}

impl LoopEmitter<Gpd, Label> for Assembler<'_> {
    fn r#loop(&mut self, target: Gpd, target2: Label) {
        self.emit_n(
            InstId::Loop as u32,
            &[target.as_operand(), target2.as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0.
    }
}

impl LoopEmitter<Gpq, Label> for Assembler<'_> {
    fn r#loop(&mut self, target: Gpq, target2: Label) {
        self.emit_n(
            InstId::Loop as u32,
            &[target.as_operand(), target2.as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0.
    }
}

impl LoopEmitter<Gp, Imm> for Assembler<'_> {
    fn r#loop(&mut self, target: Gp, imm: Imm) {
        self.emit_n(
            InstId::Loop as u32,
            &[target.as_operand(), imm.as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0.
    }
}

impl<U1: Into<Imm>> LoopEmitter<Gpw, U1> for Assembler<'_> {
    fn r#loop(&mut self, target: Gpw, target2: U1) {
        self.emit_n(
            InstId::Loop as u32,
            &[target.as_operand(), Into::<Imm>::into(target2).as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0.
    }
}

impl<U1: Into<Imm>> LoopEmitter<Gpd, U1> for Assembler<'_> {
    fn r#loop(&mut self, target: Gpd, target2: U1) {
        self.emit_n(
            InstId::Loop as u32,
            &[target.as_operand(), Into::<Imm>::into(target2).as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0.
    }
}

impl<U1: Into<Imm>> LoopEmitter<Gpq, U1> for Assembler<'_> {
    fn r#loop(&mut self, target: Gpq, target2: U1) {
        self.emit_n(
            InstId::Loop as u32,
            &[target.as_operand(), Into::<Imm>::into(target2).as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0.
    }
}

impl LoopeEmitter<Gp, Label> for Assembler<'_> {
    fn loope(&mut self, target: Gp, target2: Label) {
        self.emit_n(
            InstId::Loope as u32,
            &[target.as_operand(), target2.as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 1.
    }
}

impl LoopeEmitter<Gpw, Label> for Assembler<'_> {
    fn loope(&mut self, target: Gpw, target2: Label) {
        self.emit_n(
            InstId::Loope as u32,
            &[target.as_operand(), target2.as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 1.
    }
}

impl LoopeEmitter<Gpd, Label> for Assembler<'_> {
    fn loope(&mut self, target: Gpd, target2: Label) {
        self.emit_n(
            InstId::Loope as u32,
            &[target.as_operand(), target2.as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 1.
    }
}

impl LoopeEmitter<Gpq, Label> for Assembler<'_> {
    fn loope(&mut self, target: Gpq, target2: Label) {
        self.emit_n(
            InstId::Loope as u32,
            &[target.as_operand(), target2.as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 1.
    }
}

impl LoopeEmitter<Gp, Imm> for Assembler<'_> {
    fn loope(&mut self, target: Gp, imm: Imm) {
        self.emit_n(
            InstId::Loope as u32,
            &[target.as_operand(), imm.as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 1.
    }
}

impl<U1: Into<Imm>> LoopeEmitter<Gpw, U1> for Assembler<'_> {
    fn loope(&mut self, target: Gpw, target2: U1) {
        self.emit_n(
            InstId::Loope as u32,
            &[target.as_operand(), Into::<Imm>::into(target2).as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 1.
    }
}

impl<U1: Into<Imm>> LoopeEmitter<Gpd, U1> for Assembler<'_> {
    fn loope(&mut self, target: Gpd, target2: U1) {
        self.emit_n(
            InstId::Loope as u32,
            &[target.as_operand(), Into::<Imm>::into(target2).as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 1.
    }
}

impl<U1: Into<Imm>> LoopeEmitter<Gpq, U1> for Assembler<'_> {
    fn loope(&mut self, target: Gpq, target2: U1) {
        self.emit_n(
            InstId::Loope as u32,
            &[target.as_operand(), Into::<Imm>::into(target2).as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 1.
    }
}

impl LoopneEmitter<Gp, Label> for Assembler<'_> {
    fn loopne(&mut self, target: Gp, target2: Label) {
        self.emit_n(
            InstId::Loopne as u32,
            &[target.as_operand(), target2.as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 0.
    }
}

impl LoopneEmitter<Gpw, Label> for Assembler<'_> {
    fn loopne(&mut self, target: Gpw, target2: Label) {
        self.emit_n(
            InstId::Loopne as u32,
            &[target.as_operand(), target2.as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 0.
    }
}

impl LoopneEmitter<Gpd, Label> for Assembler<'_> {
    fn loopne(&mut self, target: Gpd, target2: Label) {
        self.emit_n(
            InstId::Loopne as u32,
            &[target.as_operand(), target2.as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 0.
    }
}

impl LoopneEmitter<Gpq, Label> for Assembler<'_> {
    fn loopne(&mut self, target: Gpq, target2: Label) {
        self.emit_n(
            InstId::Loopne as u32,
            &[target.as_operand(), target2.as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 0.
    }
}

impl LoopneEmitter<Gp, Imm> for Assembler<'_> {
    fn loopne(&mut self, target: Gp, imm: Imm) {
        self.emit_n(
            InstId::Loopne as u32,
            &[target.as_operand(), imm.as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 0.
    }
}

impl<U1: Into<Imm>> LoopneEmitter<Gpw, U1> for Assembler<'_> {
    fn loopne(&mut self, target: Gpw, target2: U1) {
        self.emit_n(
            InstId::Loopne as u32,
            &[target.as_operand(), Into::<Imm>::into(target2).as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 0.
    }
}

impl<U1: Into<Imm>> LoopneEmitter<Gpd, U1> for Assembler<'_> {
    fn loopne(&mut self, target: Gpd, target2: U1) {
        self.emit_n(
            InstId::Loopne as u32,
            &[target.as_operand(), Into::<Imm>::into(target2).as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 0.
    }
}

impl<U1: Into<Imm>> LoopneEmitter<Gpq, U1> for Assembler<'_> {
    fn loopne(&mut self, target: Gpq, target2: U1) {
        self.emit_n(
            InstId::Loopne as u32,
            &[target.as_operand(), Into::<Imm>::into(target2).as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 0.
    }
}

impl MovEmitter<Gp, Gp> for Assembler<'_> {
    fn mov(&mut self, lhs: Gp, rhs: Gp) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn mov(&mut self, lhs: GpbLo, rhs: GpbLo) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<GpbLo, GpbHi> for Assembler<'_> {
    fn mov(&mut self, lhs: GpbLo, rhs: GpbHi) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn mov(&mut self, lhs: GpbHi, rhs: GpbLo) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<GpbHi, GpbHi> for Assembler<'_> {
    fn mov(&mut self, lhs: GpbHi, rhs: GpbHi) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpw, Gpw> for Assembler<'_> {
    fn mov(&mut self, lhs: Gpw, rhs: Gpw) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpd, Gpd> for Assembler<'_> {
    fn mov(&mut self, lhs: Gpd, rhs: Gpd) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpq, Gpq> for Assembler<'_> {
    fn mov(&mut self, lhs: Gpq, rhs: Gpq) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Gp, Mem> for Assembler<'_> {
    fn mov(&mut self, lhs: Gp, rhs: Mem) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<GpbLo, Mem> for Assembler<'_> {
    fn mov(&mut self, lhs: GpbLo, rhs: Mem) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<GpbHi, Mem> for Assembler<'_> {
    fn mov(&mut self, lhs: GpbHi, rhs: Mem) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpw, Mem> for Assembler<'_> {
    fn mov(&mut self, lhs: Gpw, rhs: Mem) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpd, Mem> for Assembler<'_> {
    fn mov(&mut self, lhs: Gpd, rhs: Mem) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpq, Mem> for Assembler<'_> {
    fn mov(&mut self, lhs: Gpq, rhs: Mem) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Gp, Imm> for Assembler<'_> {
    fn mov(&mut self, lhs: Gp, imm: Imm) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> MovEmitter<GpbLo, U1> for Assembler<'_> {
    fn mov(&mut self, lhs: GpbLo, rhs: U1) {
        self.emit_n(
            InstId::Mov as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> MovEmitter<GpbHi, U1> for Assembler<'_> {
    fn mov(&mut self, lhs: GpbHi, rhs: U1) {
        self.emit_n(
            InstId::Mov as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> MovEmitter<Gpw, U1> for Assembler<'_> {
    fn mov(&mut self, lhs: Gpw, rhs: U1) {
        self.emit_n(
            InstId::Mov as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> MovEmitter<Gpd, U1> for Assembler<'_> {
    fn mov(&mut self, lhs: Gpd, rhs: U1) {
        self.emit_n(
            InstId::Mov as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> MovEmitter<Gpq, U1> for Assembler<'_> {
    fn mov(&mut self, lhs: Gpq, rhs: U1) {
        self.emit_n(
            InstId::Mov as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl MovEmitter<Mem, Gp> for Assembler<'_> {
    fn mov(&mut self, lhs: Mem, rhs: Gp) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Mem, GpbLo> for Assembler<'_> {
    fn mov(&mut self, lhs: Mem, rhs: GpbLo) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Mem, GpbHi> for Assembler<'_> {
    fn mov(&mut self, lhs: Mem, rhs: GpbHi) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Mem, Gpw> for Assembler<'_> {
    fn mov(&mut self, lhs: Mem, rhs: Gpw) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Mem, Gpd> for Assembler<'_> {
    fn mov(&mut self, lhs: Mem, rhs: Gpd) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Mem, Gpq> for Assembler<'_> {
    fn mov(&mut self, lhs: Mem, rhs: Gpq) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> MovEmitter<Mem, U1> for Assembler<'_> {
    fn mov(&mut self, lhs: Mem, rhs: U1) {
        self.emit_n(
            InstId::Mov as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl MovEmitter<Gp, CReg> for Assembler<'_> {
    fn mov(&mut self, lhs: Gp, rhs: CReg) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpq, CReg> for Assembler<'_> {
    fn mov(&mut self, lhs: Gpq, rhs: CReg) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpd, CReg> for Assembler<'_> {
    fn mov(&mut self, lhs: Gpd, rhs: CReg) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<CReg, Gp> for Assembler<'_> {
    fn mov(&mut self, lhs: CReg, rhs: Gp) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<CReg, Gpq> for Assembler<'_> {
    fn mov(&mut self, lhs: CReg, rhs: Gpq) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<CReg, Gpd> for Assembler<'_> {
    fn mov(&mut self, lhs: CReg, rhs: Gpd) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Gp, DReg> for Assembler<'_> {
    fn mov(&mut self, lhs: Gp, rhs: DReg) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpq, DReg> for Assembler<'_> {
    fn mov(&mut self, lhs: Gpq, rhs: DReg) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpd, DReg> for Assembler<'_> {
    fn mov(&mut self, lhs: Gpd, rhs: DReg) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<DReg, Gp> for Assembler<'_> {
    fn mov(&mut self, lhs: DReg, rhs: Gp) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<DReg, Gpq> for Assembler<'_> {
    fn mov(&mut self, lhs: DReg, rhs: Gpq) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<DReg, Gpd> for Assembler<'_> {
    fn mov(&mut self, lhs: DReg, rhs: Gpd) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Gp, SReg> for Assembler<'_> {
    fn mov(&mut self, lhs: Gp, rhs: SReg) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpw, SReg> for Assembler<'_> {
    fn mov(&mut self, lhs: Gpw, rhs: SReg) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpd, SReg> for Assembler<'_> {
    fn mov(&mut self, lhs: Gpd, rhs: SReg) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpq, SReg> for Assembler<'_> {
    fn mov(&mut self, lhs: Gpq, rhs: SReg) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<Mem, SReg> for Assembler<'_> {
    fn mov(&mut self, lhs: Mem, rhs: SReg) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<SReg, Gp> for Assembler<'_> {
    fn mov(&mut self, lhs: SReg, rhs: Gp) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<SReg, Gpw> for Assembler<'_> {
    fn mov(&mut self, lhs: SReg, rhs: Gpw) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<SReg, Gpd> for Assembler<'_> {
    fn mov(&mut self, lhs: SReg, rhs: Gpd) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<SReg, Gpq> for Assembler<'_> {
    fn mov(&mut self, lhs: SReg, rhs: Gpq) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovEmitter<SReg, Mem> for Assembler<'_> {
    fn mov(&mut self, lhs: SReg, rhs: Mem) {
        self.emit_n(InstId::Mov as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl MovabsEmitter<Gp, Mem> for Assembler<'_> {
    fn movabs(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Movabs as u32, &[dst.as_operand(), src.as_operand()]); // X64
    }
}

impl MovabsEmitter<GpbLo, Mem> for Assembler<'_> {
    fn movabs(&mut self, dst: GpbLo, src: Mem) {
        self.emit_n(InstId::Movabs as u32, &[dst.as_operand(), src.as_operand()]); // X64
    }
}

impl MovabsEmitter<Gpw, Mem> for Assembler<'_> {
    fn movabs(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Movabs as u32, &[dst.as_operand(), src.as_operand()]); // X64
    }
}

impl MovabsEmitter<Gpd, Mem> for Assembler<'_> {
    fn movabs(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Movabs as u32, &[dst.as_operand(), src.as_operand()]); // X64
    }
}

impl MovabsEmitter<Gpq, Mem> for Assembler<'_> {
    fn movabs(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Movabs as u32, &[dst.as_operand(), src.as_operand()]); // X64
    }
}

impl MovabsEmitter<Gp, Imm> for Assembler<'_> {
    fn movabs(&mut self, dst: Gp, imm: Imm) {
        self.emit_n(InstId::Movabs as u32, &[dst.as_operand(), imm.as_operand()]); // X64
    }
}

impl<U1: Into<Imm>> MovabsEmitter<Gpq, U1> for Assembler<'_> {
    fn movabs(&mut self, dst: Gpq, src: U1) {
        self.emit_n(
            InstId::Movabs as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // X64
    }
}

impl MovabsEmitter<Mem, Gp> for Assembler<'_> {
    fn movabs(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Movabs as u32, &[dst.as_operand(), src.as_operand()]); // X64
    }
}

impl MovabsEmitter<Mem, GpbLo> for Assembler<'_> {
    fn movabs(&mut self, dst: Mem, src: GpbLo) {
        self.emit_n(InstId::Movabs as u32, &[dst.as_operand(), src.as_operand()]); // X64
    }
}

impl MovabsEmitter<Mem, Gpw> for Assembler<'_> {
    fn movabs(&mut self, dst: Mem, src: Gpw) {
        self.emit_n(InstId::Movabs as u32, &[dst.as_operand(), src.as_operand()]); // X64
    }
}

impl MovabsEmitter<Mem, Gpd> for Assembler<'_> {
    fn movabs(&mut self, dst: Mem, src: Gpd) {
        self.emit_n(InstId::Movabs as u32, &[dst.as_operand(), src.as_operand()]); // X64
    }
}

impl MovabsEmitter<Mem, Gpq> for Assembler<'_> {
    fn movabs(&mut self, dst: Mem, src: Gpq) {
        self.emit_n(InstId::Movabs as u32, &[dst.as_operand(), src.as_operand()]); // X64
    }
}

impl MovntiEmitter<Mem, Gp> for Assembler<'_> {
    fn movnti(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Movnti as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovntiEmitter<Mem, Gpd> for Assembler<'_> {
    fn movnti(&mut self, dst: Mem, src: Gpd) {
        self.emit_n(InstId::Movnti as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovntiEmitter<Mem, Gpq> for Assembler<'_> {
    fn movnti(&mut self, dst: Mem, src: Gpq) {
        self.emit_n(InstId::Movnti as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovsEmitter<Mem, Mem> for Assembler<'_> {
    fn movs(&mut self, lhs: Mem, rhs: Mem) {
        self.emit_n(InstId::Movs as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY [EXPLICIT]
    }
}

impl MovsxEmitter<Gp, Gp> for Assembler<'_> {
    fn movsx(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Movsx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn movsx(&mut self, dst: Gpw, src: GpbLo) {
        self.emit_n(InstId::Movsx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpw, GpbHi> for Assembler<'_> {
    fn movsx(&mut self, dst: Gpw, src: GpbHi) {
        self.emit_n(InstId::Movsx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpd, GpbLo> for Assembler<'_> {
    fn movsx(&mut self, dst: Gpd, src: GpbLo) {
        self.emit_n(InstId::Movsx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpd, GpbHi> for Assembler<'_> {
    fn movsx(&mut self, dst: Gpd, src: GpbHi) {
        self.emit_n(InstId::Movsx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpd, Gpw> for Assembler<'_> {
    fn movsx(&mut self, dst: Gpd, src: Gpw) {
        self.emit_n(InstId::Movsx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpq, GpbLo> for Assembler<'_> {
    fn movsx(&mut self, dst: Gpq, src: GpbLo) {
        self.emit_n(InstId::Movsx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpq, GpbHi> for Assembler<'_> {
    fn movsx(&mut self, dst: Gpq, src: GpbHi) {
        self.emit_n(InstId::Movsx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpq, Gpw> for Assembler<'_> {
    fn movsx(&mut self, dst: Gpq, src: Gpw) {
        self.emit_n(InstId::Movsx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gp, Mem> for Assembler<'_> {
    fn movsx(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Movsx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpw, Mem> for Assembler<'_> {
    fn movsx(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Movsx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpd, Mem> for Assembler<'_> {
    fn movsx(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Movsx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpq, Mem> for Assembler<'_> {
    fn movsx(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Movsx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovsxdEmitter<Gp, Gp> for Assembler<'_> {
    fn movsxd(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Movsxd as u32, &[dst.as_operand(), src.as_operand()]); // X64
    }
}

impl MovsxdEmitter<Gpw, Gpw> for Assembler<'_> {
    fn movsxd(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Movsxd as u32, &[dst.as_operand(), src.as_operand()]); // X64
    }
}

impl MovsxdEmitter<Gpd, Gpd> for Assembler<'_> {
    fn movsxd(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Movsxd as u32, &[dst.as_operand(), src.as_operand()]); // X64
    }
}

impl MovsxdEmitter<Gpq, Gpd> for Assembler<'_> {
    fn movsxd(&mut self, dst: Gpq, src: Gpd) {
        self.emit_n(InstId::Movsxd as u32, &[dst.as_operand(), src.as_operand()]); // X64
    }
}

impl MovsxdEmitter<Gp, Mem> for Assembler<'_> {
    fn movsxd(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Movsxd as u32, &[dst.as_operand(), src.as_operand()]); // X64
    }
}

impl MovsxdEmitter<Gpw, Mem> for Assembler<'_> {
    fn movsxd(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Movsxd as u32, &[dst.as_operand(), src.as_operand()]); // X64
    }
}

impl MovsxdEmitter<Gpd, Mem> for Assembler<'_> {
    fn movsxd(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Movsxd as u32, &[dst.as_operand(), src.as_operand()]); // X64
    }
}

impl MovsxdEmitter<Gpq, Mem> for Assembler<'_> {
    fn movsxd(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Movsxd as u32, &[dst.as_operand(), src.as_operand()]); // X64
    }
}

impl MovzxEmitter<Gp, Gp> for Assembler<'_> {
    fn movzx(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Movzx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn movzx(&mut self, dst: Gpw, src: GpbLo) {
        self.emit_n(InstId::Movzx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpw, GpbHi> for Assembler<'_> {
    fn movzx(&mut self, dst: Gpw, src: GpbHi) {
        self.emit_n(InstId::Movzx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpd, GpbLo> for Assembler<'_> {
    fn movzx(&mut self, dst: Gpd, src: GpbLo) {
        self.emit_n(InstId::Movzx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpd, GpbHi> for Assembler<'_> {
    fn movzx(&mut self, dst: Gpd, src: GpbHi) {
        self.emit_n(InstId::Movzx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpd, Gpw> for Assembler<'_> {
    fn movzx(&mut self, dst: Gpd, src: Gpw) {
        self.emit_n(InstId::Movzx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpq, GpbLo> for Assembler<'_> {
    fn movzx(&mut self, dst: Gpq, src: GpbLo) {
        self.emit_n(InstId::Movzx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpq, GpbHi> for Assembler<'_> {
    fn movzx(&mut self, dst: Gpq, src: GpbHi) {
        self.emit_n(InstId::Movzx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpq, Gpw> for Assembler<'_> {
    fn movzx(&mut self, dst: Gpq, src: Gpw) {
        self.emit_n(InstId::Movzx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gp, Mem> for Assembler<'_> {
    fn movzx(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Movzx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpw, Mem> for Assembler<'_> {
    fn movzx(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Movzx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpd, Mem> for Assembler<'_> {
    fn movzx(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Movzx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpq, Mem> for Assembler<'_> {
    fn movzx(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Movzx as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl MulEmitter<Gp, Gp> for Assembler<'_> {
    fn mul(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Mul as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AX <- AL * r8
    }
}

impl MulEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn mul(&mut self, dst: Gpw, src: GpbLo) {
        self.emit_n(InstId::Mul as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AX <- AL * r8
    }
}

impl MulEmitter<Gpw, GpbHi> for Assembler<'_> {
    fn mul(&mut self, dst: Gpw, src: GpbHi) {
        self.emit_n(InstId::Mul as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AX <- AL * r8
    }
}

impl MulEmitter<Gp, Mem> for Assembler<'_> {
    fn mul(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Mul as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AX <- AL * m8
    }
}

impl MulEmitter<Gpw, Mem> for Assembler<'_> {
    fn mul(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Mul as u32, &[dst.as_operand(), src.as_operand()]); // ANY [EXPLICIT] AX <- AL * m8
    }
}

impl Mul3Emitter<Gp, Gp, Gp> for Assembler<'_> {
    fn mul_3(&mut self, dst: Gp, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Mul as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * r16|r32|r64
    }
}

impl Mul3Emitter<Gpw, Gpw, Gpw> for Assembler<'_> {
    fn mul_3(&mut self, dst: Gpw, dst2: Gpw, src: Gpw) {
        self.emit_n(
            InstId::Mul as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * r16|r32|r64
    }
}

impl Mul3Emitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn mul_3(&mut self, dst: Gpd, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Mul as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * r16|r32|r64
    }
}

impl Mul3Emitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn mul_3(&mut self, dst: Gpq, dst2: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Mul as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * r16|r32|r64
    }
}

impl Mul3Emitter<Gp, Gp, Mem> for Assembler<'_> {
    fn mul_3(&mut self, dst: Gp, dst2: Gp, src: Mem) {
        self.emit_n(
            InstId::Mul as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * m16|m32|m64
    }
}

impl Mul3Emitter<Gpw, Gpw, Mem> for Assembler<'_> {
    fn mul_3(&mut self, dst: Gpw, dst2: Gpw, src: Mem) {
        self.emit_n(
            InstId::Mul as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * m16|m32|m64
    }
}

impl Mul3Emitter<Gpd, Gpd, Mem> for Assembler<'_> {
    fn mul_3(&mut self, dst: Gpd, dst2: Gpd, src: Mem) {
        self.emit_n(
            InstId::Mul as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * m16|m32|m64
    }
}

impl Mul3Emitter<Gpq, Gpq, Mem> for Assembler<'_> {
    fn mul_3(&mut self, dst: Gpq, dst2: Gpq, src: Mem) {
        self.emit_n(
            InstId::Mul as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * m16|m32|m64
    }
}

impl NegEmitter<Gp> for Assembler<'_> {
    fn neg(&mut self, src: Gp) {
        self.emit_n(InstId::Neg as u32, &[src.as_operand()]); // ANY
    }
}

impl NegEmitter<GpbLo> for Assembler<'_> {
    fn neg(&mut self, src: GpbLo) {
        self.emit_n(InstId::Neg as u32, &[src.as_operand()]); // ANY
    }
}

impl NegEmitter<GpbHi> for Assembler<'_> {
    fn neg(&mut self, src: GpbHi) {
        self.emit_n(InstId::Neg as u32, &[src.as_operand()]); // ANY
    }
}

impl NegEmitter<Gpw> for Assembler<'_> {
    fn neg(&mut self, src: Gpw) {
        self.emit_n(InstId::Neg as u32, &[src.as_operand()]); // ANY
    }
}

impl NegEmitter<Gpd> for Assembler<'_> {
    fn neg(&mut self, src: Gpd) {
        self.emit_n(InstId::Neg as u32, &[src.as_operand()]); // ANY
    }
}

impl NegEmitter<Gpq> for Assembler<'_> {
    fn neg(&mut self, src: Gpq) {
        self.emit_n(InstId::Neg as u32, &[src.as_operand()]); // ANY
    }
}

impl NegEmitter<Mem> for Assembler<'_> {
    fn neg(&mut self, mem: Mem) {
        self.emit_n(InstId::Neg as u32, &[mem.as_operand()]); // ANY
    }
}

impl NopEmitter for Assembler<'_> {
    fn nop(&mut self) {
        self.emit_n(InstId::Nop as u32, &[]); // ANY
    }
}

impl Nop1Emitter<Gp> for Assembler<'_> {
    fn nop_1(&mut self, src: Gp) {
        self.emit_n(InstId::Nop as u32, &[src.as_operand()]); // ANY
    }
}

impl Nop1Emitter<Gpw> for Assembler<'_> {
    fn nop_1(&mut self, src: Gpw) {
        self.emit_n(InstId::Nop as u32, &[src.as_operand()]); // ANY
    }
}

impl Nop1Emitter<Gpd> for Assembler<'_> {
    fn nop_1(&mut self, src: Gpd) {
        self.emit_n(InstId::Nop as u32, &[src.as_operand()]); // ANY
    }
}

impl Nop1Emitter<Gpq> for Assembler<'_> {
    fn nop_1(&mut self, src: Gpq) {
        self.emit_n(InstId::Nop as u32, &[src.as_operand()]); // ANY
    }
}

impl Nop1Emitter<Mem> for Assembler<'_> {
    fn nop_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Nop as u32, &[mem.as_operand()]); // ANY
    }
}

impl Nop2Emitter<Gp, Gp> for Assembler<'_> {
    fn nop_2(&mut self, lhs: Gp, rhs: Gp) {
        self.emit_n(InstId::Nop as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl Nop2Emitter<Gpw, Gpw> for Assembler<'_> {
    fn nop_2(&mut self, lhs: Gpw, rhs: Gpw) {
        self.emit_n(InstId::Nop as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl Nop2Emitter<Gpd, Gpd> for Assembler<'_> {
    fn nop_2(&mut self, lhs: Gpd, rhs: Gpd) {
        self.emit_n(InstId::Nop as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl Nop2Emitter<Gpq, Gpq> for Assembler<'_> {
    fn nop_2(&mut self, lhs: Gpq, rhs: Gpq) {
        self.emit_n(InstId::Nop as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl Nop2Emitter<Mem, Gp> for Assembler<'_> {
    fn nop_2(&mut self, lhs: Mem, rhs: Gp) {
        self.emit_n(InstId::Nop as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl Nop2Emitter<Mem, Gpw> for Assembler<'_> {
    fn nop_2(&mut self, lhs: Mem, rhs: Gpw) {
        self.emit_n(InstId::Nop as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl Nop2Emitter<Mem, Gpd> for Assembler<'_> {
    fn nop_2(&mut self, lhs: Mem, rhs: Gpd) {
        self.emit_n(InstId::Nop as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl Nop2Emitter<Mem, Gpq> for Assembler<'_> {
    fn nop_2(&mut self, lhs: Mem, rhs: Gpq) {
        self.emit_n(InstId::Nop as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl Not_Emitter<Gp> for Assembler<'_> {
    fn not_(&mut self, src: Gp) {
        self.emit_n(InstId::Not as u32, &[src.as_operand()]); // ANY
    }
}

impl Not_Emitter<GpbLo> for Assembler<'_> {
    fn not_(&mut self, src: GpbLo) {
        self.emit_n(InstId::Not as u32, &[src.as_operand()]); // ANY
    }
}

impl Not_Emitter<GpbHi> for Assembler<'_> {
    fn not_(&mut self, src: GpbHi) {
        self.emit_n(InstId::Not as u32, &[src.as_operand()]); // ANY
    }
}

impl Not_Emitter<Gpw> for Assembler<'_> {
    fn not_(&mut self, src: Gpw) {
        self.emit_n(InstId::Not as u32, &[src.as_operand()]); // ANY
    }
}

impl Not_Emitter<Gpd> for Assembler<'_> {
    fn not_(&mut self, src: Gpd) {
        self.emit_n(InstId::Not as u32, &[src.as_operand()]); // ANY
    }
}

impl Not_Emitter<Gpq> for Assembler<'_> {
    fn not_(&mut self, src: Gpq) {
        self.emit_n(InstId::Not as u32, &[src.as_operand()]); // ANY
    }
}

impl Not_Emitter<Mem> for Assembler<'_> {
    fn not_(&mut self, mem: Mem) {
        self.emit_n(InstId::Not as u32, &[mem.as_operand()]); // ANY
    }
}

impl Or_Emitter<Gp, Gp> for Assembler<'_> {
    fn or_(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Or as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Or_Emitter<GpbLo, GpbLo> for Assembler<'_> {
    fn or_(&mut self, dst: GpbLo, src: GpbLo) {
        self.emit_n(InstId::Or as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Or_Emitter<GpbLo, GpbHi> for Assembler<'_> {
    fn or_(&mut self, dst: GpbLo, src: GpbHi) {
        self.emit_n(InstId::Or as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Or_Emitter<GpbHi, GpbLo> for Assembler<'_> {
    fn or_(&mut self, dst: GpbHi, src: GpbLo) {
        self.emit_n(InstId::Or as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Or_Emitter<GpbHi, GpbHi> for Assembler<'_> {
    fn or_(&mut self, dst: GpbHi, src: GpbHi) {
        self.emit_n(InstId::Or as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Or_Emitter<Gpw, Gpw> for Assembler<'_> {
    fn or_(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Or as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Or_Emitter<Gpd, Gpd> for Assembler<'_> {
    fn or_(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Or as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Or_Emitter<Gpq, Gpq> for Assembler<'_> {
    fn or_(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Or as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Or_Emitter<Gp, Mem> for Assembler<'_> {
    fn or_(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Or as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Or_Emitter<GpbLo, Mem> for Assembler<'_> {
    fn or_(&mut self, dst: GpbLo, src: Mem) {
        self.emit_n(InstId::Or as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Or_Emitter<GpbHi, Mem> for Assembler<'_> {
    fn or_(&mut self, dst: GpbHi, src: Mem) {
        self.emit_n(InstId::Or as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Or_Emitter<Gpw, Mem> for Assembler<'_> {
    fn or_(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Or as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Or_Emitter<Gpd, Mem> for Assembler<'_> {
    fn or_(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Or as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Or_Emitter<Gpq, Mem> for Assembler<'_> {
    fn or_(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Or as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Or_Emitter<Gp, Imm> for Assembler<'_> {
    fn or_(&mut self, dst: Gp, imm: Imm) {
        self.emit_n(InstId::Or as u32, &[dst.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> Or_Emitter<GpbLo, U1> for Assembler<'_> {
    fn or_(&mut self, dst: GpbLo, src: U1) {
        self.emit_n(
            InstId::Or as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> Or_Emitter<GpbHi, U1> for Assembler<'_> {
    fn or_(&mut self, dst: GpbHi, src: U1) {
        self.emit_n(
            InstId::Or as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> Or_Emitter<Gpw, U1> for Assembler<'_> {
    fn or_(&mut self, dst: Gpw, src: U1) {
        self.emit_n(
            InstId::Or as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> Or_Emitter<Gpd, U1> for Assembler<'_> {
    fn or_(&mut self, dst: Gpd, src: U1) {
        self.emit_n(
            InstId::Or as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> Or_Emitter<Gpq, U1> for Assembler<'_> {
    fn or_(&mut self, dst: Gpq, src: U1) {
        self.emit_n(
            InstId::Or as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl Or_Emitter<Mem, Gp> for Assembler<'_> {
    fn or_(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Or as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Or_Emitter<Mem, GpbLo> for Assembler<'_> {
    fn or_(&mut self, dst: Mem, src: GpbLo) {
        self.emit_n(InstId::Or as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Or_Emitter<Mem, GpbHi> for Assembler<'_> {
    fn or_(&mut self, dst: Mem, src: GpbHi) {
        self.emit_n(InstId::Or as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Or_Emitter<Mem, Gpw> for Assembler<'_> {
    fn or_(&mut self, dst: Mem, src: Gpw) {
        self.emit_n(InstId::Or as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Or_Emitter<Mem, Gpd> for Assembler<'_> {
    fn or_(&mut self, dst: Mem, src: Gpd) {
        self.emit_n(InstId::Or as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Or_Emitter<Mem, Gpq> for Assembler<'_> {
    fn or_(&mut self, dst: Mem, src: Gpq) {
        self.emit_n(InstId::Or as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> Or_Emitter<Mem, U1> for Assembler<'_> {
    fn or_(&mut self, dst: Mem, src: U1) {
        self.emit_n(
            InstId::Or as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl PopEmitter<Gp> for Assembler<'_> {
    fn pop(&mut self, dst: Gp) {
        self.emit_n(InstId::Pop as u32, &[dst.as_operand()]); // ANY
    }
}

impl PopEmitter<Gpw> for Assembler<'_> {
    fn pop(&mut self, dst: Gpw) {
        self.emit_n(InstId::Pop as u32, &[dst.as_operand()]); // ANY
    }
}

impl PopEmitter<Gpd> for Assembler<'_> {
    fn pop(&mut self, dst: Gpd) {
        self.emit_n(InstId::Pop as u32, &[dst.as_operand()]); // ANY
    }
}

impl PopEmitter<Gpq> for Assembler<'_> {
    fn pop(&mut self, dst: Gpq) {
        self.emit_n(InstId::Pop as u32, &[dst.as_operand()]); // ANY
    }
}

impl PopEmitter<Mem> for Assembler<'_> {
    fn pop(&mut self, dst: Mem) {
        self.emit_n(InstId::Pop as u32, &[dst.as_operand()]); // ANY
    }
}

impl PopEmitter<SReg> for Assembler<'_> {
    fn pop(&mut self, dst: SReg) {
        self.emit_n(InstId::Pop as u32, &[dst.as_operand()]); // ANY
    }
}

impl PopaEmitter for Assembler<'_> {
    fn popa(&mut self) {
        self.emit_n(InstId::Popa as u32, &[]); // X86
    }
}

impl PopadEmitter for Assembler<'_> {
    fn popad(&mut self) {
        self.emit_n(InstId::Popad as u32, &[]); // X86
    }
}

impl PopfEmitter for Assembler<'_> {
    fn popf(&mut self) {
        self.emit_n(InstId::Popf as u32, &[]); // ANY
    }
}

impl PopfdEmitter for Assembler<'_> {
    fn popfd(&mut self) {
        self.emit_n(InstId::Popfd as u32, &[]); // X86
    }
}

impl PopfqEmitter for Assembler<'_> {
    fn popfq(&mut self) {
        self.emit_n(InstId::Popfq as u32, &[]); // X64
    }
}

impl PushEmitter<Gp> for Assembler<'_> {
    fn push(&mut self, src: Gp) {
        self.emit_n(InstId::Push as u32, &[src.as_operand()]); // ANY
    }
}

impl PushEmitter<Gpw> for Assembler<'_> {
    fn push(&mut self, src: Gpw) {
        self.emit_n(InstId::Push as u32, &[src.as_operand()]); // ANY
    }
}

impl PushEmitter<Gpd> for Assembler<'_> {
    fn push(&mut self, src: Gpd) {
        self.emit_n(InstId::Push as u32, &[src.as_operand()]); // ANY
    }
}

impl PushEmitter<Gpq> for Assembler<'_> {
    fn push(&mut self, src: Gpq) {
        self.emit_n(InstId::Push as u32, &[src.as_operand()]); // ANY
    }
}

impl PushEmitter<Mem> for Assembler<'_> {
    fn push(&mut self, mem: Mem) {
        self.emit_n(InstId::Push as u32, &[mem.as_operand()]); // ANY
    }
}

impl PushEmitter<SReg> for Assembler<'_> {
    fn push(&mut self, src: SReg) {
        self.emit_n(InstId::Push as u32, &[src.as_operand()]); // ANY
    }
}

impl<U0: Into<Imm>> PushEmitter<U0> for Assembler<'_> {
    fn push(&mut self, src: U0) {
        self.emit_n(InstId::Push as u32, &[Into::<Imm>::into(src).as_operand()]); // ANY
    }
}

impl PushaEmitter for Assembler<'_> {
    fn pusha(&mut self) {
        self.emit_n(InstId::Pusha as u32, &[]); // X86
    }
}

impl PushadEmitter for Assembler<'_> {
    fn pushad(&mut self) {
        self.emit_n(InstId::Pushad as u32, &[]); // X86
    }
}

impl PushfEmitter for Assembler<'_> {
    fn pushf(&mut self) {
        self.emit_n(InstId::Pushf as u32, &[]); // ANY
    }
}

impl PushfdEmitter for Assembler<'_> {
    fn pushfd(&mut self) {
        self.emit_n(InstId::Pushfd as u32, &[]); // X86
    }
}

impl PushfqEmitter for Assembler<'_> {
    fn pushfq(&mut self) {
        self.emit_n(InstId::Pushfq as u32, &[]); // X64
    }
}

impl<U0: Into<Imm>> PushwEmitter<U0> for Assembler<'_> {
    fn pushw(&mut self, src: U0) {
        self.emit_n(InstId::Pushw as u32, &[Into::<Imm>::into(src).as_operand()]); // ANY
    }
}

impl RclEmitter<Gp, Gp> for Assembler<'_> {
    fn rcl(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Rcl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RclEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn rcl(&mut self, dst: GpbLo, src: GpbLo) {
        self.emit_n(InstId::Rcl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RclEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn rcl(&mut self, dst: GpbHi, src: GpbLo) {
        self.emit_n(InstId::Rcl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RclEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn rcl(&mut self, dst: Gpw, src: GpbLo) {
        self.emit_n(InstId::Rcl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RclEmitter<Gpd, GpbLo> for Assembler<'_> {
    fn rcl(&mut self, dst: Gpd, src: GpbLo) {
        self.emit_n(InstId::Rcl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RclEmitter<Gpq, GpbLo> for Assembler<'_> {
    fn rcl(&mut self, dst: Gpq, src: GpbLo) {
        self.emit_n(InstId::Rcl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RclEmitter<Mem, Gp> for Assembler<'_> {
    fn rcl(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Rcl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RclEmitter<Mem, GpbLo> for Assembler<'_> {
    fn rcl(&mut self, dst: Mem, src: GpbLo) {
        self.emit_n(InstId::Rcl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RclEmitter<Gp, Imm> for Assembler<'_> {
    fn rcl(&mut self, dst: Gp, imm: Imm) {
        self.emit_n(InstId::Rcl as u32, &[dst.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> RclEmitter<GpbLo, U1> for Assembler<'_> {
    fn rcl(&mut self, dst: GpbLo, src: U1) {
        self.emit_n(
            InstId::Rcl as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RclEmitter<GpbHi, U1> for Assembler<'_> {
    fn rcl(&mut self, dst: GpbHi, src: U1) {
        self.emit_n(
            InstId::Rcl as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RclEmitter<Gpw, U1> for Assembler<'_> {
    fn rcl(&mut self, dst: Gpw, src: U1) {
        self.emit_n(
            InstId::Rcl as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RclEmitter<Gpd, U1> for Assembler<'_> {
    fn rcl(&mut self, dst: Gpd, src: U1) {
        self.emit_n(
            InstId::Rcl as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RclEmitter<Gpq, U1> for Assembler<'_> {
    fn rcl(&mut self, dst: Gpq, src: U1) {
        self.emit_n(
            InstId::Rcl as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RclEmitter<Mem, U1> for Assembler<'_> {
    fn rcl(&mut self, dst: Mem, src: U1) {
        self.emit_n(
            InstId::Rcl as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl RcrEmitter<Gp, Gp> for Assembler<'_> {
    fn rcr(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Rcr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RcrEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn rcr(&mut self, dst: GpbLo, src: GpbLo) {
        self.emit_n(InstId::Rcr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RcrEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn rcr(&mut self, dst: GpbHi, src: GpbLo) {
        self.emit_n(InstId::Rcr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RcrEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn rcr(&mut self, dst: Gpw, src: GpbLo) {
        self.emit_n(InstId::Rcr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RcrEmitter<Gpd, GpbLo> for Assembler<'_> {
    fn rcr(&mut self, dst: Gpd, src: GpbLo) {
        self.emit_n(InstId::Rcr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RcrEmitter<Gpq, GpbLo> for Assembler<'_> {
    fn rcr(&mut self, dst: Gpq, src: GpbLo) {
        self.emit_n(InstId::Rcr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RcrEmitter<Mem, Gp> for Assembler<'_> {
    fn rcr(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Rcr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RcrEmitter<Mem, GpbLo> for Assembler<'_> {
    fn rcr(&mut self, dst: Mem, src: GpbLo) {
        self.emit_n(InstId::Rcr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RcrEmitter<Gp, Imm> for Assembler<'_> {
    fn rcr(&mut self, dst: Gp, imm: Imm) {
        self.emit_n(InstId::Rcr as u32, &[dst.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> RcrEmitter<GpbLo, U1> for Assembler<'_> {
    fn rcr(&mut self, dst: GpbLo, src: U1) {
        self.emit_n(
            InstId::Rcr as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RcrEmitter<GpbHi, U1> for Assembler<'_> {
    fn rcr(&mut self, dst: GpbHi, src: U1) {
        self.emit_n(
            InstId::Rcr as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RcrEmitter<Gpw, U1> for Assembler<'_> {
    fn rcr(&mut self, dst: Gpw, src: U1) {
        self.emit_n(
            InstId::Rcr as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RcrEmitter<Gpd, U1> for Assembler<'_> {
    fn rcr(&mut self, dst: Gpd, src: U1) {
        self.emit_n(
            InstId::Rcr as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RcrEmitter<Gpq, U1> for Assembler<'_> {
    fn rcr(&mut self, dst: Gpq, src: U1) {
        self.emit_n(
            InstId::Rcr as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RcrEmitter<Mem, U1> for Assembler<'_> {
    fn rcr(&mut self, dst: Mem, src: U1) {
        self.emit_n(
            InstId::Rcr as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl RolEmitter<Gp, Gp> for Assembler<'_> {
    fn rol(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Rol as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RolEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn rol(&mut self, dst: GpbLo, src: GpbLo) {
        self.emit_n(InstId::Rol as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RolEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn rol(&mut self, dst: GpbHi, src: GpbLo) {
        self.emit_n(InstId::Rol as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RolEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn rol(&mut self, dst: Gpw, src: GpbLo) {
        self.emit_n(InstId::Rol as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RolEmitter<Gpd, GpbLo> for Assembler<'_> {
    fn rol(&mut self, dst: Gpd, src: GpbLo) {
        self.emit_n(InstId::Rol as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RolEmitter<Gpq, GpbLo> for Assembler<'_> {
    fn rol(&mut self, dst: Gpq, src: GpbLo) {
        self.emit_n(InstId::Rol as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RolEmitter<Mem, Gp> for Assembler<'_> {
    fn rol(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Rol as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RolEmitter<Mem, GpbLo> for Assembler<'_> {
    fn rol(&mut self, dst: Mem, src: GpbLo) {
        self.emit_n(InstId::Rol as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RolEmitter<Gp, Imm> for Assembler<'_> {
    fn rol(&mut self, dst: Gp, imm: Imm) {
        self.emit_n(InstId::Rol as u32, &[dst.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> RolEmitter<GpbLo, U1> for Assembler<'_> {
    fn rol(&mut self, dst: GpbLo, src: U1) {
        self.emit_n(
            InstId::Rol as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RolEmitter<GpbHi, U1> for Assembler<'_> {
    fn rol(&mut self, dst: GpbHi, src: U1) {
        self.emit_n(
            InstId::Rol as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RolEmitter<Gpw, U1> for Assembler<'_> {
    fn rol(&mut self, dst: Gpw, src: U1) {
        self.emit_n(
            InstId::Rol as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RolEmitter<Gpd, U1> for Assembler<'_> {
    fn rol(&mut self, dst: Gpd, src: U1) {
        self.emit_n(
            InstId::Rol as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RolEmitter<Gpq, U1> for Assembler<'_> {
    fn rol(&mut self, dst: Gpq, src: U1) {
        self.emit_n(
            InstId::Rol as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RolEmitter<Mem, U1> for Assembler<'_> {
    fn rol(&mut self, dst: Mem, src: U1) {
        self.emit_n(
            InstId::Rol as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl RorEmitter<Gp, Gp> for Assembler<'_> {
    fn ror(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Ror as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RorEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn ror(&mut self, dst: GpbLo, src: GpbLo) {
        self.emit_n(InstId::Ror as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RorEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn ror(&mut self, dst: GpbHi, src: GpbLo) {
        self.emit_n(InstId::Ror as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RorEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn ror(&mut self, dst: Gpw, src: GpbLo) {
        self.emit_n(InstId::Ror as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RorEmitter<Gpd, GpbLo> for Assembler<'_> {
    fn ror(&mut self, dst: Gpd, src: GpbLo) {
        self.emit_n(InstId::Ror as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RorEmitter<Gpq, GpbLo> for Assembler<'_> {
    fn ror(&mut self, dst: Gpq, src: GpbLo) {
        self.emit_n(InstId::Ror as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RorEmitter<Mem, Gp> for Assembler<'_> {
    fn ror(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Ror as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RorEmitter<Mem, GpbLo> for Assembler<'_> {
    fn ror(&mut self, dst: Mem, src: GpbLo) {
        self.emit_n(InstId::Ror as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl RorEmitter<Gp, Imm> for Assembler<'_> {
    fn ror(&mut self, dst: Gp, imm: Imm) {
        self.emit_n(InstId::Ror as u32, &[dst.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> RorEmitter<GpbLo, U1> for Assembler<'_> {
    fn ror(&mut self, dst: GpbLo, src: U1) {
        self.emit_n(
            InstId::Ror as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RorEmitter<GpbHi, U1> for Assembler<'_> {
    fn ror(&mut self, dst: GpbHi, src: U1) {
        self.emit_n(
            InstId::Ror as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RorEmitter<Gpw, U1> for Assembler<'_> {
    fn ror(&mut self, dst: Gpw, src: U1) {
        self.emit_n(
            InstId::Ror as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RorEmitter<Gpd, U1> for Assembler<'_> {
    fn ror(&mut self, dst: Gpd, src: U1) {
        self.emit_n(
            InstId::Ror as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RorEmitter<Gpq, U1> for Assembler<'_> {
    fn ror(&mut self, dst: Gpq, src: U1) {
        self.emit_n(
            InstId::Ror as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RorEmitter<Mem, U1> for Assembler<'_> {
    fn ror(&mut self, dst: Mem, src: U1) {
        self.emit_n(
            InstId::Ror as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl SbbEmitter<Gp, Gp> for Assembler<'_> {
    fn sbb(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Sbb as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SbbEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn sbb(&mut self, dst: GpbLo, src: GpbLo) {
        self.emit_n(InstId::Sbb as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SbbEmitter<GpbLo, GpbHi> for Assembler<'_> {
    fn sbb(&mut self, dst: GpbLo, src: GpbHi) {
        self.emit_n(InstId::Sbb as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SbbEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn sbb(&mut self, dst: GpbHi, src: GpbLo) {
        self.emit_n(InstId::Sbb as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SbbEmitter<GpbHi, GpbHi> for Assembler<'_> {
    fn sbb(&mut self, dst: GpbHi, src: GpbHi) {
        self.emit_n(InstId::Sbb as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SbbEmitter<Gpw, Gpw> for Assembler<'_> {
    fn sbb(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Sbb as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SbbEmitter<Gpd, Gpd> for Assembler<'_> {
    fn sbb(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Sbb as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SbbEmitter<Gpq, Gpq> for Assembler<'_> {
    fn sbb(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Sbb as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SbbEmitter<Gp, Mem> for Assembler<'_> {
    fn sbb(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Sbb as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SbbEmitter<GpbLo, Mem> for Assembler<'_> {
    fn sbb(&mut self, dst: GpbLo, src: Mem) {
        self.emit_n(InstId::Sbb as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SbbEmitter<GpbHi, Mem> for Assembler<'_> {
    fn sbb(&mut self, dst: GpbHi, src: Mem) {
        self.emit_n(InstId::Sbb as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SbbEmitter<Gpw, Mem> for Assembler<'_> {
    fn sbb(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Sbb as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SbbEmitter<Gpd, Mem> for Assembler<'_> {
    fn sbb(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Sbb as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SbbEmitter<Gpq, Mem> for Assembler<'_> {
    fn sbb(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Sbb as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SbbEmitter<Gp, Imm> for Assembler<'_> {
    fn sbb(&mut self, dst: Gp, imm: Imm) {
        self.emit_n(InstId::Sbb as u32, &[dst.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> SbbEmitter<GpbLo, U1> for Assembler<'_> {
    fn sbb(&mut self, dst: GpbLo, src: U1) {
        self.emit_n(
            InstId::Sbb as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SbbEmitter<GpbHi, U1> for Assembler<'_> {
    fn sbb(&mut self, dst: GpbHi, src: U1) {
        self.emit_n(
            InstId::Sbb as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SbbEmitter<Gpw, U1> for Assembler<'_> {
    fn sbb(&mut self, dst: Gpw, src: U1) {
        self.emit_n(
            InstId::Sbb as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SbbEmitter<Gpd, U1> for Assembler<'_> {
    fn sbb(&mut self, dst: Gpd, src: U1) {
        self.emit_n(
            InstId::Sbb as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SbbEmitter<Gpq, U1> for Assembler<'_> {
    fn sbb(&mut self, dst: Gpq, src: U1) {
        self.emit_n(
            InstId::Sbb as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl SbbEmitter<Mem, Gp> for Assembler<'_> {
    fn sbb(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Sbb as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SbbEmitter<Mem, GpbLo> for Assembler<'_> {
    fn sbb(&mut self, dst: Mem, src: GpbLo) {
        self.emit_n(InstId::Sbb as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SbbEmitter<Mem, GpbHi> for Assembler<'_> {
    fn sbb(&mut self, dst: Mem, src: GpbHi) {
        self.emit_n(InstId::Sbb as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SbbEmitter<Mem, Gpw> for Assembler<'_> {
    fn sbb(&mut self, dst: Mem, src: Gpw) {
        self.emit_n(InstId::Sbb as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SbbEmitter<Mem, Gpd> for Assembler<'_> {
    fn sbb(&mut self, dst: Mem, src: Gpd) {
        self.emit_n(InstId::Sbb as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SbbEmitter<Mem, Gpq> for Assembler<'_> {
    fn sbb(&mut self, dst: Mem, src: Gpq) {
        self.emit_n(InstId::Sbb as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> SbbEmitter<Mem, U1> for Assembler<'_> {
    fn sbb(&mut self, dst: Mem, src: U1) {
        self.emit_n(
            InstId::Sbb as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl SalEmitter<Gp, Gp> for Assembler<'_> {
    fn sal(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Shl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SalEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn sal(&mut self, dst: GpbLo, src: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SalEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn sal(&mut self, dst: GpbHi, src: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SalEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn sal(&mut self, dst: Gpw, src: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SalEmitter<Gpd, GpbLo> for Assembler<'_> {
    fn sal(&mut self, dst: Gpd, src: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SalEmitter<Gpq, GpbLo> for Assembler<'_> {
    fn sal(&mut self, dst: Gpq, src: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SalEmitter<Mem, Gp> for Assembler<'_> {
    fn sal(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Shl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SalEmitter<Mem, GpbLo> for Assembler<'_> {
    fn sal(&mut self, dst: Mem, src: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SalEmitter<Gp, Imm> for Assembler<'_> {
    fn sal(&mut self, dst: Gp, imm: Imm) {
        self.emit_n(InstId::Shl as u32, &[dst.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> SalEmitter<GpbLo, U1> for Assembler<'_> {
    fn sal(&mut self, dst: GpbLo, src: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SalEmitter<GpbHi, U1> for Assembler<'_> {
    fn sal(&mut self, dst: GpbHi, src: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SalEmitter<Gpw, U1> for Assembler<'_> {
    fn sal(&mut self, dst: Gpw, src: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SalEmitter<Gpd, U1> for Assembler<'_> {
    fn sal(&mut self, dst: Gpd, src: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SalEmitter<Gpq, U1> for Assembler<'_> {
    fn sal(&mut self, dst: Gpq, src: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SalEmitter<Mem, U1> for Assembler<'_> {
    fn sal(&mut self, dst: Mem, src: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl SarEmitter<Gp, Gp> for Assembler<'_> {
    fn sar(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Sar as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SarEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn sar(&mut self, dst: GpbLo, src: GpbLo) {
        self.emit_n(InstId::Sar as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SarEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn sar(&mut self, dst: GpbHi, src: GpbLo) {
        self.emit_n(InstId::Sar as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SarEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn sar(&mut self, dst: Gpw, src: GpbLo) {
        self.emit_n(InstId::Sar as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SarEmitter<Gpd, GpbLo> for Assembler<'_> {
    fn sar(&mut self, dst: Gpd, src: GpbLo) {
        self.emit_n(InstId::Sar as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SarEmitter<Gpq, GpbLo> for Assembler<'_> {
    fn sar(&mut self, dst: Gpq, src: GpbLo) {
        self.emit_n(InstId::Sar as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SarEmitter<Mem, Gp> for Assembler<'_> {
    fn sar(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Sar as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SarEmitter<Mem, GpbLo> for Assembler<'_> {
    fn sar(&mut self, dst: Mem, src: GpbLo) {
        self.emit_n(InstId::Sar as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SarEmitter<Gp, Imm> for Assembler<'_> {
    fn sar(&mut self, dst: Gp, imm: Imm) {
        self.emit_n(InstId::Sar as u32, &[dst.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> SarEmitter<GpbLo, U1> for Assembler<'_> {
    fn sar(&mut self, dst: GpbLo, src: U1) {
        self.emit_n(
            InstId::Sar as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SarEmitter<GpbHi, U1> for Assembler<'_> {
    fn sar(&mut self, dst: GpbHi, src: U1) {
        self.emit_n(
            InstId::Sar as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SarEmitter<Gpw, U1> for Assembler<'_> {
    fn sar(&mut self, dst: Gpw, src: U1) {
        self.emit_n(
            InstId::Sar as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SarEmitter<Gpd, U1> for Assembler<'_> {
    fn sar(&mut self, dst: Gpd, src: U1) {
        self.emit_n(
            InstId::Sar as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SarEmitter<Gpq, U1> for Assembler<'_> {
    fn sar(&mut self, dst: Gpq, src: U1) {
        self.emit_n(
            InstId::Sar as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SarEmitter<Mem, U1> for Assembler<'_> {
    fn sar(&mut self, dst: Mem, src: U1) {
        self.emit_n(
            InstId::Sar as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl ScasEmitter<Gp, Mem> for Assembler<'_> {
    fn scas(&mut self, lhs: Gp, rhs: Mem) {
        self.emit_n(InstId::Scas as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY [EXPLICIT]
    }
}

impl ScasEmitter<GpbLo, Mem> for Assembler<'_> {
    fn scas(&mut self, lhs: GpbLo, rhs: Mem) {
        self.emit_n(InstId::Scas as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY [EXPLICIT]
    }
}

impl ScasEmitter<Gpw, Mem> for Assembler<'_> {
    fn scas(&mut self, lhs: Gpw, rhs: Mem) {
        self.emit_n(InstId::Scas as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY [EXPLICIT]
    }
}

impl ScasEmitter<Gpd, Mem> for Assembler<'_> {
    fn scas(&mut self, lhs: Gpd, rhs: Mem) {
        self.emit_n(InstId::Scas as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY [EXPLICIT]
    }
}

impl ScasEmitter<Gpq, Mem> for Assembler<'_> {
    fn scas(&mut self, lhs: Gpq, rhs: Mem) {
        self.emit_n(InstId::Scas as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY [EXPLICIT]
    }
}

impl SetEmitter<Gp> for Assembler<'_> {
    fn set(&mut self, cc: CondCode, dst: Gp) {
        self.emit_n(SETCC_FROM_COND[cc as usize], &[dst.as_operand()]); // ANY
    }
    fn seto(&mut self, dst: Gp) {
        self.emit_n(InstId::Seto as u32, &[dst.as_operand()]);
    }
    fn setno(&mut self, dst: Gp) {
        self.emit_n(InstId::Setno as u32, &[dst.as_operand()]);
    }
    fn setb(&mut self, dst: Gp) {
        self.emit_n(InstId::Setb as u32, &[dst.as_operand()]);
    }
    fn setnb(&mut self, dst: Gp) {
        self.emit_n(InstId::Setnb as u32, &[dst.as_operand()]);
    }
    fn setz(&mut self, dst: Gp) {
        self.emit_n(InstId::Setz as u32, &[dst.as_operand()]);
    }
    fn setnz(&mut self, dst: Gp) {
        self.emit_n(InstId::Setnz as u32, &[dst.as_operand()]);
    }
    fn setbe(&mut self, dst: Gp) {
        self.emit_n(InstId::Setbe as u32, &[dst.as_operand()]);
    }
    fn setnbe(&mut self, dst: Gp) {
        self.emit_n(InstId::Setnbe as u32, &[dst.as_operand()]);
    }
    fn sets(&mut self, dst: Gp) {
        self.emit_n(InstId::Sets as u32, &[dst.as_operand()]);
    }
    fn setns(&mut self, dst: Gp) {
        self.emit_n(InstId::Setns as u32, &[dst.as_operand()]);
    }
    fn setp(&mut self, dst: Gp) {
        self.emit_n(InstId::Setp as u32, &[dst.as_operand()]);
    }
    fn setnp(&mut self, dst: Gp) {
        self.emit_n(InstId::Setnp as u32, &[dst.as_operand()]);
    }
    fn setl(&mut self, dst: Gp) {
        self.emit_n(InstId::Setl as u32, &[dst.as_operand()]);
    }
    fn setnl(&mut self, dst: Gp) {
        self.emit_n(InstId::Setnl as u32, &[dst.as_operand()]);
    }
    fn setle(&mut self, dst: Gp) {
        self.emit_n(InstId::Setle as u32, &[dst.as_operand()]);
    }
    fn setnle(&mut self, dst: Gp) {
        self.emit_n(InstId::Setnle as u32, &[dst.as_operand()]);
    }
}

impl SetEmitter<GpbLo> for Assembler<'_> {
    fn set(&mut self, cc: CondCode, dst: GpbLo) {
        self.emit_n(SETCC_FROM_COND[cc as usize], &[dst.as_operand()]); // ANY
    }
    fn seto(&mut self, dst: GpbLo) {
        self.emit_n(InstId::Seto as u32, &[dst.as_operand()]);
    }
    fn setno(&mut self, dst: GpbLo) {
        self.emit_n(InstId::Setno as u32, &[dst.as_operand()]);
    }
    fn setb(&mut self, dst: GpbLo) {
        self.emit_n(InstId::Setb as u32, &[dst.as_operand()]);
    }
    fn setnb(&mut self, dst: GpbLo) {
        self.emit_n(InstId::Setnb as u32, &[dst.as_operand()]);
    }
    fn setz(&mut self, dst: GpbLo) {
        self.emit_n(InstId::Setz as u32, &[dst.as_operand()]);
    }
    fn setnz(&mut self, dst: GpbLo) {
        self.emit_n(InstId::Setnz as u32, &[dst.as_operand()]);
    }
    fn setbe(&mut self, dst: GpbLo) {
        self.emit_n(InstId::Setbe as u32, &[dst.as_operand()]);
    }
    fn setnbe(&mut self, dst: GpbLo) {
        self.emit_n(InstId::Setnbe as u32, &[dst.as_operand()]);
    }
    fn sets(&mut self, dst: GpbLo) {
        self.emit_n(InstId::Sets as u32, &[dst.as_operand()]);
    }
    fn setns(&mut self, dst: GpbLo) {
        self.emit_n(InstId::Setns as u32, &[dst.as_operand()]);
    }
    fn setp(&mut self, dst: GpbLo) {
        self.emit_n(InstId::Setp as u32, &[dst.as_operand()]);
    }
    fn setnp(&mut self, dst: GpbLo) {
        self.emit_n(InstId::Setnp as u32, &[dst.as_operand()]);
    }
    fn setl(&mut self, dst: GpbLo) {
        self.emit_n(InstId::Setl as u32, &[dst.as_operand()]);
    }
    fn setnl(&mut self, dst: GpbLo) {
        self.emit_n(InstId::Setnl as u32, &[dst.as_operand()]);
    }
    fn setle(&mut self, dst: GpbLo) {
        self.emit_n(InstId::Setle as u32, &[dst.as_operand()]);
    }
    fn setnle(&mut self, dst: GpbLo) {
        self.emit_n(InstId::Setnle as u32, &[dst.as_operand()]);
    }
}

impl SetEmitter<GpbHi> for Assembler<'_> {
    fn set(&mut self, cc: CondCode, dst: GpbHi) {
        self.emit_n(SETCC_FROM_COND[cc as usize], &[dst.as_operand()]); // ANY
    }
    fn seto(&mut self, dst: GpbHi) {
        self.emit_n(InstId::Seto as u32, &[dst.as_operand()]);
    }
    fn setno(&mut self, dst: GpbHi) {
        self.emit_n(InstId::Setno as u32, &[dst.as_operand()]);
    }
    fn setb(&mut self, dst: GpbHi) {
        self.emit_n(InstId::Setb as u32, &[dst.as_operand()]);
    }
    fn setnb(&mut self, dst: GpbHi) {
        self.emit_n(InstId::Setnb as u32, &[dst.as_operand()]);
    }
    fn setz(&mut self, dst: GpbHi) {
        self.emit_n(InstId::Setz as u32, &[dst.as_operand()]);
    }
    fn setnz(&mut self, dst: GpbHi) {
        self.emit_n(InstId::Setnz as u32, &[dst.as_operand()]);
    }
    fn setbe(&mut self, dst: GpbHi) {
        self.emit_n(InstId::Setbe as u32, &[dst.as_operand()]);
    }
    fn setnbe(&mut self, dst: GpbHi) {
        self.emit_n(InstId::Setnbe as u32, &[dst.as_operand()]);
    }
    fn sets(&mut self, dst: GpbHi) {
        self.emit_n(InstId::Sets as u32, &[dst.as_operand()]);
    }
    fn setns(&mut self, dst: GpbHi) {
        self.emit_n(InstId::Setns as u32, &[dst.as_operand()]);
    }
    fn setp(&mut self, dst: GpbHi) {
        self.emit_n(InstId::Setp as u32, &[dst.as_operand()]);
    }
    fn setnp(&mut self, dst: GpbHi) {
        self.emit_n(InstId::Setnp as u32, &[dst.as_operand()]);
    }
    fn setl(&mut self, dst: GpbHi) {
        self.emit_n(InstId::Setl as u32, &[dst.as_operand()]);
    }
    fn setnl(&mut self, dst: GpbHi) {
        self.emit_n(InstId::Setnl as u32, &[dst.as_operand()]);
    }
    fn setle(&mut self, dst: GpbHi) {
        self.emit_n(InstId::Setle as u32, &[dst.as_operand()]);
    }
    fn setnle(&mut self, dst: GpbHi) {
        self.emit_n(InstId::Setnle as u32, &[dst.as_operand()]);
    }
}

impl SetEmitter<Mem> for Assembler<'_> {
    fn set(&mut self, cc: CondCode, dst: Mem) {
        self.emit_n(SETCC_FROM_COND[cc as usize], &[dst.as_operand()]); // ANY
    }
    fn seto(&mut self, dst: Mem) {
        self.emit_n(InstId::Seto as u32, &[dst.as_operand()]);
    }
    fn setno(&mut self, dst: Mem) {
        self.emit_n(InstId::Setno as u32, &[dst.as_operand()]);
    }
    fn setb(&mut self, dst: Mem) {
        self.emit_n(InstId::Setb as u32, &[dst.as_operand()]);
    }
    fn setnb(&mut self, dst: Mem) {
        self.emit_n(InstId::Setnb as u32, &[dst.as_operand()]);
    }
    fn setz(&mut self, dst: Mem) {
        self.emit_n(InstId::Setz as u32, &[dst.as_operand()]);
    }
    fn setnz(&mut self, dst: Mem) {
        self.emit_n(InstId::Setnz as u32, &[dst.as_operand()]);
    }
    fn setbe(&mut self, dst: Mem) {
        self.emit_n(InstId::Setbe as u32, &[dst.as_operand()]);
    }
    fn setnbe(&mut self, dst: Mem) {
        self.emit_n(InstId::Setnbe as u32, &[dst.as_operand()]);
    }
    fn sets(&mut self, dst: Mem) {
        self.emit_n(InstId::Sets as u32, &[dst.as_operand()]);
    }
    fn setns(&mut self, dst: Mem) {
        self.emit_n(InstId::Setns as u32, &[dst.as_operand()]);
    }
    fn setp(&mut self, dst: Mem) {
        self.emit_n(InstId::Setp as u32, &[dst.as_operand()]);
    }
    fn setnp(&mut self, dst: Mem) {
        self.emit_n(InstId::Setnp as u32, &[dst.as_operand()]);
    }
    fn setl(&mut self, dst: Mem) {
        self.emit_n(InstId::Setl as u32, &[dst.as_operand()]);
    }
    fn setnl(&mut self, dst: Mem) {
        self.emit_n(InstId::Setnl as u32, &[dst.as_operand()]);
    }
    fn setle(&mut self, dst: Mem) {
        self.emit_n(InstId::Setle as u32, &[dst.as_operand()]);
    }
    fn setnle(&mut self, dst: Mem) {
        self.emit_n(InstId::Setnle as u32, &[dst.as_operand()]);
    }
}

impl ShlEmitter<Gp, Gp> for Assembler<'_> {
    fn shl(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Shl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl ShlEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn shl(&mut self, dst: GpbLo, src: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl ShlEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn shl(&mut self, dst: GpbHi, src: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl ShlEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn shl(&mut self, dst: Gpw, src: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl ShlEmitter<Gpd, GpbLo> for Assembler<'_> {
    fn shl(&mut self, dst: Gpd, src: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl ShlEmitter<Gpq, GpbLo> for Assembler<'_> {
    fn shl(&mut self, dst: Gpq, src: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl ShlEmitter<Mem, Gp> for Assembler<'_> {
    fn shl(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Shl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl ShlEmitter<Mem, GpbLo> for Assembler<'_> {
    fn shl(&mut self, dst: Mem, src: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl ShlEmitter<Gp, Imm> for Assembler<'_> {
    fn shl(&mut self, dst: Gp, imm: Imm) {
        self.emit_n(InstId::Shl as u32, &[dst.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> ShlEmitter<GpbLo, U1> for Assembler<'_> {
    fn shl(&mut self, dst: GpbLo, src: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> ShlEmitter<GpbHi, U1> for Assembler<'_> {
    fn shl(&mut self, dst: GpbHi, src: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> ShlEmitter<Gpw, U1> for Assembler<'_> {
    fn shl(&mut self, dst: Gpw, src: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> ShlEmitter<Gpd, U1> for Assembler<'_> {
    fn shl(&mut self, dst: Gpd, src: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> ShlEmitter<Gpq, U1> for Assembler<'_> {
    fn shl(&mut self, dst: Gpq, src: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> ShlEmitter<Mem, U1> for Assembler<'_> {
    fn shl(&mut self, dst: Mem, src: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl ShrEmitter<Gp, Gp> for Assembler<'_> {
    fn shr(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Shr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl ShrEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn shr(&mut self, dst: GpbLo, src: GpbLo) {
        self.emit_n(InstId::Shr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl ShrEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn shr(&mut self, dst: GpbHi, src: GpbLo) {
        self.emit_n(InstId::Shr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl ShrEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn shr(&mut self, dst: Gpw, src: GpbLo) {
        self.emit_n(InstId::Shr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl ShrEmitter<Gpd, GpbLo> for Assembler<'_> {
    fn shr(&mut self, dst: Gpd, src: GpbLo) {
        self.emit_n(InstId::Shr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl ShrEmitter<Gpq, GpbLo> for Assembler<'_> {
    fn shr(&mut self, dst: Gpq, src: GpbLo) {
        self.emit_n(InstId::Shr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl ShrEmitter<Mem, Gp> for Assembler<'_> {
    fn shr(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Shr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl ShrEmitter<Mem, GpbLo> for Assembler<'_> {
    fn shr(&mut self, dst: Mem, src: GpbLo) {
        self.emit_n(InstId::Shr as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl ShrEmitter<Gp, Imm> for Assembler<'_> {
    fn shr(&mut self, dst: Gp, imm: Imm) {
        self.emit_n(InstId::Shr as u32, &[dst.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> ShrEmitter<GpbLo, U1> for Assembler<'_> {
    fn shr(&mut self, dst: GpbLo, src: U1) {
        self.emit_n(
            InstId::Shr as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> ShrEmitter<GpbHi, U1> for Assembler<'_> {
    fn shr(&mut self, dst: GpbHi, src: U1) {
        self.emit_n(
            InstId::Shr as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> ShrEmitter<Gpw, U1> for Assembler<'_> {
    fn shr(&mut self, dst: Gpw, src: U1) {
        self.emit_n(
            InstId::Shr as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> ShrEmitter<Gpd, U1> for Assembler<'_> {
    fn shr(&mut self, dst: Gpd, src: U1) {
        self.emit_n(
            InstId::Shr as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> ShrEmitter<Gpq, U1> for Assembler<'_> {
    fn shr(&mut self, dst: Gpq, src: U1) {
        self.emit_n(
            InstId::Shr as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> ShrEmitter<Mem, U1> for Assembler<'_> {
    fn shr(&mut self, dst: Mem, src: U1) {
        self.emit_n(
            InstId::Shr as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl ShldEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn shld(&mut self, dst: Gp, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Shld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // ANY
    }
}

impl ShldEmitter<Gpw, Gpw, GpbLo> for Assembler<'_> {
    fn shld(&mut self, dst: Gpw, src1: Gpw, src2: GpbLo) {
        self.emit_n(
            InstId::Shld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // ANY
    }
}

impl ShldEmitter<Gpd, Gpd, GpbLo> for Assembler<'_> {
    fn shld(&mut self, dst: Gpd, src1: Gpd, src2: GpbLo) {
        self.emit_n(
            InstId::Shld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // ANY
    }
}

impl ShldEmitter<Gpq, Gpq, GpbLo> for Assembler<'_> {
    fn shld(&mut self, dst: Gpq, src1: Gpq, src2: GpbLo) {
        self.emit_n(
            InstId::Shld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // ANY
    }
}

impl ShldEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn shld(&mut self, dst: Mem, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Shld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // ANY
    }
}

impl ShldEmitter<Mem, Gpw, GpbLo> for Assembler<'_> {
    fn shld(&mut self, dst: Mem, src1: Gpw, src2: GpbLo) {
        self.emit_n(
            InstId::Shld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // ANY
    }
}

impl ShldEmitter<Mem, Gpd, GpbLo> for Assembler<'_> {
    fn shld(&mut self, dst: Mem, src1: Gpd, src2: GpbLo) {
        self.emit_n(
            InstId::Shld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // ANY
    }
}

impl ShldEmitter<Mem, Gpq, GpbLo> for Assembler<'_> {
    fn shld(&mut self, dst: Mem, src1: Gpq, src2: GpbLo) {
        self.emit_n(
            InstId::Shld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // ANY
    }
}

impl ShldEmitter<Gp, Gp, Imm> for Assembler<'_> {
    fn shld(&mut self, dst: Gp, src: Gp, imm: Imm) {
        self.emit_n(
            InstId::Shld as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShldEmitter<Gpw, Gpw, U2> for Assembler<'_> {
    fn shld(&mut self, dst: Gpw, src1: Gpw, src2: U2) {
        self.emit_n(
            InstId::Shld as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShldEmitter<Gpd, Gpd, U2> for Assembler<'_> {
    fn shld(&mut self, dst: Gpd, src1: Gpd, src2: U2) {
        self.emit_n(
            InstId::Shld as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShldEmitter<Gpq, Gpq, U2> for Assembler<'_> {
    fn shld(&mut self, dst: Gpq, src1: Gpq, src2: U2) {
        self.emit_n(
            InstId::Shld as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // ANY
    }
}

impl ShldEmitter<Mem, Gp, Imm> for Assembler<'_> {
    fn shld(&mut self, dst: Mem, src: Gp, imm: Imm) {
        self.emit_n(
            InstId::Shld as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShldEmitter<Mem, Gpw, U2> for Assembler<'_> {
    fn shld(&mut self, dst: Mem, src1: Gpw, src2: U2) {
        self.emit_n(
            InstId::Shld as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShldEmitter<Mem, Gpd, U2> for Assembler<'_> {
    fn shld(&mut self, dst: Mem, src1: Gpd, src2: U2) {
        self.emit_n(
            InstId::Shld as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShldEmitter<Mem, Gpq, U2> for Assembler<'_> {
    fn shld(&mut self, dst: Mem, src1: Gpq, src2: U2) {
        self.emit_n(
            InstId::Shld as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // ANY
    }
}

impl ShrdEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn shrd(&mut self, dst: Gp, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Shrd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // ANY
    }
}

impl ShrdEmitter<Gpw, Gpw, GpbLo> for Assembler<'_> {
    fn shrd(&mut self, dst: Gpw, src1: Gpw, src2: GpbLo) {
        self.emit_n(
            InstId::Shrd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // ANY
    }
}

impl ShrdEmitter<Gpd, Gpd, GpbLo> for Assembler<'_> {
    fn shrd(&mut self, dst: Gpd, src1: Gpd, src2: GpbLo) {
        self.emit_n(
            InstId::Shrd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // ANY
    }
}

impl ShrdEmitter<Gpq, Gpq, GpbLo> for Assembler<'_> {
    fn shrd(&mut self, dst: Gpq, src1: Gpq, src2: GpbLo) {
        self.emit_n(
            InstId::Shrd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // ANY
    }
}

impl ShrdEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn shrd(&mut self, dst: Mem, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Shrd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // ANY
    }
}

impl ShrdEmitter<Mem, Gpw, GpbLo> for Assembler<'_> {
    fn shrd(&mut self, dst: Mem, src1: Gpw, src2: GpbLo) {
        self.emit_n(
            InstId::Shrd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // ANY
    }
}

impl ShrdEmitter<Mem, Gpd, GpbLo> for Assembler<'_> {
    fn shrd(&mut self, dst: Mem, src1: Gpd, src2: GpbLo) {
        self.emit_n(
            InstId::Shrd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // ANY
    }
}

impl ShrdEmitter<Mem, Gpq, GpbLo> for Assembler<'_> {
    fn shrd(&mut self, dst: Mem, src1: Gpq, src2: GpbLo) {
        self.emit_n(
            InstId::Shrd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // ANY
    }
}

impl ShrdEmitter<Gp, Gp, Imm> for Assembler<'_> {
    fn shrd(&mut self, dst: Gp, src: Gp, imm: Imm) {
        self.emit_n(
            InstId::Shrd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShrdEmitter<Gpw, Gpw, U2> for Assembler<'_> {
    fn shrd(&mut self, dst: Gpw, src1: Gpw, src2: U2) {
        self.emit_n(
            InstId::Shrd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShrdEmitter<Gpd, Gpd, U2> for Assembler<'_> {
    fn shrd(&mut self, dst: Gpd, src1: Gpd, src2: U2) {
        self.emit_n(
            InstId::Shrd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShrdEmitter<Gpq, Gpq, U2> for Assembler<'_> {
    fn shrd(&mut self, dst: Gpq, src1: Gpq, src2: U2) {
        self.emit_n(
            InstId::Shrd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // ANY
    }
}

impl ShrdEmitter<Mem, Gp, Imm> for Assembler<'_> {
    fn shrd(&mut self, dst: Mem, src: Gp, imm: Imm) {
        self.emit_n(
            InstId::Shrd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShrdEmitter<Mem, Gpw, U2> for Assembler<'_> {
    fn shrd(&mut self, dst: Mem, src1: Gpw, src2: U2) {
        self.emit_n(
            InstId::Shrd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShrdEmitter<Mem, Gpd, U2> for Assembler<'_> {
    fn shrd(&mut self, dst: Mem, src1: Gpd, src2: U2) {
        self.emit_n(
            InstId::Shrd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShrdEmitter<Mem, Gpq, U2> for Assembler<'_> {
    fn shrd(&mut self, dst: Mem, src1: Gpq, src2: U2) {
        self.emit_n(
            InstId::Shrd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // ANY
    }
}

impl StosEmitter<Mem, Gp> for Assembler<'_> {
    fn stos(&mut self, lhs: Mem, rhs: Gp) {
        self.emit_n(InstId::Stos as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY [EXPLICIT]
    }
}

impl StosEmitter<Mem, GpbLo> for Assembler<'_> {
    fn stos(&mut self, lhs: Mem, rhs: GpbLo) {
        self.emit_n(InstId::Stos as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY [EXPLICIT]
    }
}

impl StosEmitter<Mem, Gpw> for Assembler<'_> {
    fn stos(&mut self, lhs: Mem, rhs: Gpw) {
        self.emit_n(InstId::Stos as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY [EXPLICIT]
    }
}

impl StosEmitter<Mem, Gpd> for Assembler<'_> {
    fn stos(&mut self, lhs: Mem, rhs: Gpd) {
        self.emit_n(InstId::Stos as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY [EXPLICIT]
    }
}

impl StosEmitter<Mem, Gpq> for Assembler<'_> {
    fn stos(&mut self, lhs: Mem, rhs: Gpq) {
        self.emit_n(InstId::Stos as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY [EXPLICIT]
    }
}

impl SubEmitter<Gp, Gp> for Assembler<'_> {
    fn sub(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Sub as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SubEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn sub(&mut self, dst: GpbLo, src: GpbLo) {
        self.emit_n(InstId::Sub as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SubEmitter<GpbLo, GpbHi> for Assembler<'_> {
    fn sub(&mut self, dst: GpbLo, src: GpbHi) {
        self.emit_n(InstId::Sub as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SubEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn sub(&mut self, dst: GpbHi, src: GpbLo) {
        self.emit_n(InstId::Sub as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SubEmitter<GpbHi, GpbHi> for Assembler<'_> {
    fn sub(&mut self, dst: GpbHi, src: GpbHi) {
        self.emit_n(InstId::Sub as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SubEmitter<Gpw, Gpw> for Assembler<'_> {
    fn sub(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Sub as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SubEmitter<Gpd, Gpd> for Assembler<'_> {
    fn sub(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Sub as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SubEmitter<Gpq, Gpq> for Assembler<'_> {
    fn sub(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Sub as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SubEmitter<Gp, Mem> for Assembler<'_> {
    fn sub(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Sub as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SubEmitter<GpbLo, Mem> for Assembler<'_> {
    fn sub(&mut self, dst: GpbLo, src: Mem) {
        self.emit_n(InstId::Sub as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SubEmitter<GpbHi, Mem> for Assembler<'_> {
    fn sub(&mut self, dst: GpbHi, src: Mem) {
        self.emit_n(InstId::Sub as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SubEmitter<Gpw, Mem> for Assembler<'_> {
    fn sub(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Sub as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SubEmitter<Gpd, Mem> for Assembler<'_> {
    fn sub(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Sub as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SubEmitter<Gpq, Mem> for Assembler<'_> {
    fn sub(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Sub as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SubEmitter<Gp, Imm> for Assembler<'_> {
    fn sub(&mut self, dst: Gp, imm: Imm) {
        self.emit_n(InstId::Sub as u32, &[dst.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> SubEmitter<GpbLo, U1> for Assembler<'_> {
    fn sub(&mut self, dst: GpbLo, src: U1) {
        self.emit_n(
            InstId::Sub as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SubEmitter<GpbHi, U1> for Assembler<'_> {
    fn sub(&mut self, dst: GpbHi, src: U1) {
        self.emit_n(
            InstId::Sub as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SubEmitter<Gpw, U1> for Assembler<'_> {
    fn sub(&mut self, dst: Gpw, src: U1) {
        self.emit_n(
            InstId::Sub as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SubEmitter<Gpd, U1> for Assembler<'_> {
    fn sub(&mut self, dst: Gpd, src: U1) {
        self.emit_n(
            InstId::Sub as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SubEmitter<Gpq, U1> for Assembler<'_> {
    fn sub(&mut self, dst: Gpq, src: U1) {
        self.emit_n(
            InstId::Sub as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl SubEmitter<Mem, Gp> for Assembler<'_> {
    fn sub(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Sub as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SubEmitter<Mem, GpbLo> for Assembler<'_> {
    fn sub(&mut self, dst: Mem, src: GpbLo) {
        self.emit_n(InstId::Sub as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SubEmitter<Mem, GpbHi> for Assembler<'_> {
    fn sub(&mut self, dst: Mem, src: GpbHi) {
        self.emit_n(InstId::Sub as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SubEmitter<Mem, Gpw> for Assembler<'_> {
    fn sub(&mut self, dst: Mem, src: Gpw) {
        self.emit_n(InstId::Sub as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SubEmitter<Mem, Gpd> for Assembler<'_> {
    fn sub(&mut self, dst: Mem, src: Gpd) {
        self.emit_n(InstId::Sub as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl SubEmitter<Mem, Gpq> for Assembler<'_> {
    fn sub(&mut self, dst: Mem, src: Gpq) {
        self.emit_n(InstId::Sub as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> SubEmitter<Mem, U1> for Assembler<'_> {
    fn sub(&mut self, dst: Mem, src: U1) {
        self.emit_n(
            InstId::Sub as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl TestEmitter<Gp, Gp> for Assembler<'_> {
    fn test(&mut self, lhs: Gp, rhs: Gp) {
        self.emit_n(InstId::Test as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl TestEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn test(&mut self, lhs: GpbLo, rhs: GpbLo) {
        self.emit_n(InstId::Test as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl TestEmitter<GpbLo, GpbHi> for Assembler<'_> {
    fn test(&mut self, lhs: GpbLo, rhs: GpbHi) {
        self.emit_n(InstId::Test as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl TestEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn test(&mut self, lhs: GpbHi, rhs: GpbLo) {
        self.emit_n(InstId::Test as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl TestEmitter<GpbHi, GpbHi> for Assembler<'_> {
    fn test(&mut self, lhs: GpbHi, rhs: GpbHi) {
        self.emit_n(InstId::Test as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl TestEmitter<Gpw, Gpw> for Assembler<'_> {
    fn test(&mut self, lhs: Gpw, rhs: Gpw) {
        self.emit_n(InstId::Test as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl TestEmitter<Gpd, Gpd> for Assembler<'_> {
    fn test(&mut self, lhs: Gpd, rhs: Gpd) {
        self.emit_n(InstId::Test as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl TestEmitter<Gpq, Gpq> for Assembler<'_> {
    fn test(&mut self, lhs: Gpq, rhs: Gpq) {
        self.emit_n(InstId::Test as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl TestEmitter<Gp, Imm> for Assembler<'_> {
    fn test(&mut self, lhs: Gp, imm: Imm) {
        self.emit_n(InstId::Test as u32, &[lhs.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> TestEmitter<GpbLo, U1> for Assembler<'_> {
    fn test(&mut self, lhs: GpbLo, rhs: U1) {
        self.emit_n(
            InstId::Test as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> TestEmitter<GpbHi, U1> for Assembler<'_> {
    fn test(&mut self, lhs: GpbHi, rhs: U1) {
        self.emit_n(
            InstId::Test as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> TestEmitter<Gpw, U1> for Assembler<'_> {
    fn test(&mut self, lhs: Gpw, rhs: U1) {
        self.emit_n(
            InstId::Test as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> TestEmitter<Gpd, U1> for Assembler<'_> {
    fn test(&mut self, lhs: Gpd, rhs: U1) {
        self.emit_n(
            InstId::Test as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> TestEmitter<Gpq, U1> for Assembler<'_> {
    fn test(&mut self, lhs: Gpq, rhs: U1) {
        self.emit_n(
            InstId::Test as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl TestEmitter<Mem, Gp> for Assembler<'_> {
    fn test(&mut self, lhs: Mem, rhs: Gp) {
        self.emit_n(InstId::Test as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl TestEmitter<Mem, GpbLo> for Assembler<'_> {
    fn test(&mut self, lhs: Mem, rhs: GpbLo) {
        self.emit_n(InstId::Test as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl TestEmitter<Mem, GpbHi> for Assembler<'_> {
    fn test(&mut self, lhs: Mem, rhs: GpbHi) {
        self.emit_n(InstId::Test as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl TestEmitter<Mem, Gpw> for Assembler<'_> {
    fn test(&mut self, lhs: Mem, rhs: Gpw) {
        self.emit_n(InstId::Test as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl TestEmitter<Mem, Gpd> for Assembler<'_> {
    fn test(&mut self, lhs: Mem, rhs: Gpd) {
        self.emit_n(InstId::Test as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl TestEmitter<Mem, Gpq> for Assembler<'_> {
    fn test(&mut self, lhs: Mem, rhs: Gpq) {
        self.emit_n(InstId::Test as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> TestEmitter<Mem, U1> for Assembler<'_> {
    fn test(&mut self, lhs: Mem, rhs: U1) {
        self.emit_n(
            InstId::Test as u32,
            &[lhs.as_operand(), Into::<Imm>::into(rhs).as_operand()],
        ); // ANY
    }
}

impl Ud0Emitter<Gp, Gp> for Assembler<'_> {
    fn ud0(&mut self, lhs: Gp, rhs: Gp) {
        self.emit_n(InstId::Ud0 as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl Ud0Emitter<Gpd, Gpd> for Assembler<'_> {
    fn ud0(&mut self, lhs: Gpd, rhs: Gpd) {
        self.emit_n(InstId::Ud0 as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl Ud0Emitter<Gp, Mem> for Assembler<'_> {
    fn ud0(&mut self, lhs: Gp, rhs: Mem) {
        self.emit_n(InstId::Ud0 as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl Ud0Emitter<Gpd, Mem> for Assembler<'_> {
    fn ud0(&mut self, lhs: Gpd, rhs: Mem) {
        self.emit_n(InstId::Ud0 as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl Ud1Emitter<Gp, Gp> for Assembler<'_> {
    fn ud1(&mut self, lhs: Gp, rhs: Gp) {
        self.emit_n(InstId::Ud1 as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl Ud1Emitter<Gpd, Gpd> for Assembler<'_> {
    fn ud1(&mut self, lhs: Gpd, rhs: Gpd) {
        self.emit_n(InstId::Ud1 as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl Ud1Emitter<Gp, Mem> for Assembler<'_> {
    fn ud1(&mut self, lhs: Gp, rhs: Mem) {
        self.emit_n(InstId::Ud1 as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl Ud1Emitter<Gpd, Mem> for Assembler<'_> {
    fn ud1(&mut self, lhs: Gpd, rhs: Mem) {
        self.emit_n(InstId::Ud1 as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl Ud2Emitter for Assembler<'_> {
    fn ud2(&mut self) {
        self.emit_n(InstId::Ud2 as u32, &[]); // ANY
    }
}

impl XaddEmitter<Gp, Gp> for Assembler<'_> {
    fn xadd(&mut self, dst: Gp, dst2: Gp) {
        self.emit_n(InstId::Xadd as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XaddEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn xadd(&mut self, dst: GpbLo, dst2: GpbLo) {
        self.emit_n(InstId::Xadd as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XaddEmitter<GpbLo, GpbHi> for Assembler<'_> {
    fn xadd(&mut self, dst: GpbLo, dst2: GpbHi) {
        self.emit_n(InstId::Xadd as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XaddEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn xadd(&mut self, dst: GpbHi, dst2: GpbLo) {
        self.emit_n(InstId::Xadd as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XaddEmitter<GpbHi, GpbHi> for Assembler<'_> {
    fn xadd(&mut self, dst: GpbHi, dst2: GpbHi) {
        self.emit_n(InstId::Xadd as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XaddEmitter<Gpw, Gpw> for Assembler<'_> {
    fn xadd(&mut self, dst: Gpw, dst2: Gpw) {
        self.emit_n(InstId::Xadd as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XaddEmitter<Gpd, Gpd> for Assembler<'_> {
    fn xadd(&mut self, dst: Gpd, dst2: Gpd) {
        self.emit_n(InstId::Xadd as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XaddEmitter<Gpq, Gpq> for Assembler<'_> {
    fn xadd(&mut self, dst: Gpq, dst2: Gpq) {
        self.emit_n(InstId::Xadd as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XaddEmitter<Mem, Gp> for Assembler<'_> {
    fn xadd(&mut self, dst: Mem, dst2: Gp) {
        self.emit_n(InstId::Xadd as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XaddEmitter<Mem, GpbLo> for Assembler<'_> {
    fn xadd(&mut self, dst: Mem, dst2: GpbLo) {
        self.emit_n(InstId::Xadd as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XaddEmitter<Mem, GpbHi> for Assembler<'_> {
    fn xadd(&mut self, dst: Mem, dst2: GpbHi) {
        self.emit_n(InstId::Xadd as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XaddEmitter<Mem, Gpw> for Assembler<'_> {
    fn xadd(&mut self, dst: Mem, dst2: Gpw) {
        self.emit_n(InstId::Xadd as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XaddEmitter<Mem, Gpd> for Assembler<'_> {
    fn xadd(&mut self, dst: Mem, dst2: Gpd) {
        self.emit_n(InstId::Xadd as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XaddEmitter<Mem, Gpq> for Assembler<'_> {
    fn xadd(&mut self, dst: Mem, dst2: Gpq) {
        self.emit_n(InstId::Xadd as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XchgEmitter<Gp, Gp> for Assembler<'_> {
    fn xchg(&mut self, dst: Gp, dst2: Gp) {
        self.emit_n(InstId::Xchg as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XchgEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn xchg(&mut self, dst: GpbLo, dst2: GpbLo) {
        self.emit_n(InstId::Xchg as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XchgEmitter<GpbLo, GpbHi> for Assembler<'_> {
    fn xchg(&mut self, dst: GpbLo, dst2: GpbHi) {
        self.emit_n(InstId::Xchg as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XchgEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn xchg(&mut self, dst: GpbHi, dst2: GpbLo) {
        self.emit_n(InstId::Xchg as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XchgEmitter<GpbHi, GpbHi> for Assembler<'_> {
    fn xchg(&mut self, dst: GpbHi, dst2: GpbHi) {
        self.emit_n(InstId::Xchg as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XchgEmitter<Gpw, Gpw> for Assembler<'_> {
    fn xchg(&mut self, dst: Gpw, dst2: Gpw) {
        self.emit_n(InstId::Xchg as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XchgEmitter<Gpd, Gpd> for Assembler<'_> {
    fn xchg(&mut self, dst: Gpd, dst2: Gpd) {
        self.emit_n(InstId::Xchg as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XchgEmitter<Gpq, Gpq> for Assembler<'_> {
    fn xchg(&mut self, dst: Gpq, dst2: Gpq) {
        self.emit_n(InstId::Xchg as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XchgEmitter<Mem, Gp> for Assembler<'_> {
    fn xchg(&mut self, dst: Mem, dst2: Gp) {
        self.emit_n(InstId::Xchg as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XchgEmitter<Mem, GpbLo> for Assembler<'_> {
    fn xchg(&mut self, dst: Mem, dst2: GpbLo) {
        self.emit_n(InstId::Xchg as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XchgEmitter<Mem, GpbHi> for Assembler<'_> {
    fn xchg(&mut self, dst: Mem, dst2: GpbHi) {
        self.emit_n(InstId::Xchg as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XchgEmitter<Mem, Gpw> for Assembler<'_> {
    fn xchg(&mut self, dst: Mem, dst2: Gpw) {
        self.emit_n(InstId::Xchg as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XchgEmitter<Mem, Gpd> for Assembler<'_> {
    fn xchg(&mut self, dst: Mem, dst2: Gpd) {
        self.emit_n(InstId::Xchg as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XchgEmitter<Mem, Gpq> for Assembler<'_> {
    fn xchg(&mut self, dst: Mem, dst2: Gpq) {
        self.emit_n(InstId::Xchg as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XchgEmitter<Gp, Mem> for Assembler<'_> {
    fn xchg(&mut self, dst: Gp, dst2: Mem) {
        self.emit_n(InstId::Xchg as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XchgEmitter<GpbLo, Mem> for Assembler<'_> {
    fn xchg(&mut self, dst: GpbLo, dst2: Mem) {
        self.emit_n(InstId::Xchg as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XchgEmitter<GpbHi, Mem> for Assembler<'_> {
    fn xchg(&mut self, dst: GpbHi, dst2: Mem) {
        self.emit_n(InstId::Xchg as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XchgEmitter<Gpw, Mem> for Assembler<'_> {
    fn xchg(&mut self, dst: Gpw, dst2: Mem) {
        self.emit_n(InstId::Xchg as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XchgEmitter<Gpd, Mem> for Assembler<'_> {
    fn xchg(&mut self, dst: Gpd, dst2: Mem) {
        self.emit_n(InstId::Xchg as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl XchgEmitter<Gpq, Mem> for Assembler<'_> {
    fn xchg(&mut self, dst: Gpq, dst2: Mem) {
        self.emit_n(InstId::Xchg as u32, &[dst.as_operand(), dst2.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Gp, Gp> for Assembler<'_> {
    fn xor_(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Xor as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Xor_Emitter<GpbLo, GpbLo> for Assembler<'_> {
    fn xor_(&mut self, dst: GpbLo, src: GpbLo) {
        self.emit_n(InstId::Xor as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Xor_Emitter<GpbLo, GpbHi> for Assembler<'_> {
    fn xor_(&mut self, dst: GpbLo, src: GpbHi) {
        self.emit_n(InstId::Xor as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Xor_Emitter<GpbHi, GpbLo> for Assembler<'_> {
    fn xor_(&mut self, dst: GpbHi, src: GpbLo) {
        self.emit_n(InstId::Xor as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Xor_Emitter<GpbHi, GpbHi> for Assembler<'_> {
    fn xor_(&mut self, dst: GpbHi, src: GpbHi) {
        self.emit_n(InstId::Xor as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Gpw, Gpw> for Assembler<'_> {
    fn xor_(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Xor as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Gpd, Gpd> for Assembler<'_> {
    fn xor_(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Xor as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Gpq, Gpq> for Assembler<'_> {
    fn xor_(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Xor as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Gp, Mem> for Assembler<'_> {
    fn xor_(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Xor as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Xor_Emitter<GpbLo, Mem> for Assembler<'_> {
    fn xor_(&mut self, dst: GpbLo, src: Mem) {
        self.emit_n(InstId::Xor as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Xor_Emitter<GpbHi, Mem> for Assembler<'_> {
    fn xor_(&mut self, dst: GpbHi, src: Mem) {
        self.emit_n(InstId::Xor as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Gpw, Mem> for Assembler<'_> {
    fn xor_(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Xor as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Gpd, Mem> for Assembler<'_> {
    fn xor_(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Xor as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Gpq, Mem> for Assembler<'_> {
    fn xor_(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Xor as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Gp, Imm> for Assembler<'_> {
    fn xor_(&mut self, dst: Gp, imm: Imm) {
        self.emit_n(InstId::Xor as u32, &[dst.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> Xor_Emitter<GpbLo, U1> for Assembler<'_> {
    fn xor_(&mut self, dst: GpbLo, src: U1) {
        self.emit_n(
            InstId::Xor as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> Xor_Emitter<GpbHi, U1> for Assembler<'_> {
    fn xor_(&mut self, dst: GpbHi, src: U1) {
        self.emit_n(
            InstId::Xor as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> Xor_Emitter<Gpw, U1> for Assembler<'_> {
    fn xor_(&mut self, dst: Gpw, src: U1) {
        self.emit_n(
            InstId::Xor as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> Xor_Emitter<Gpd, U1> for Assembler<'_> {
    fn xor_(&mut self, dst: Gpd, src: U1) {
        self.emit_n(
            InstId::Xor as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> Xor_Emitter<Gpq, U1> for Assembler<'_> {
    fn xor_(&mut self, dst: Gpq, src: U1) {
        self.emit_n(
            InstId::Xor as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl Xor_Emitter<Mem, Gp> for Assembler<'_> {
    fn xor_(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Xor as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Mem, GpbLo> for Assembler<'_> {
    fn xor_(&mut self, dst: Mem, src: GpbLo) {
        self.emit_n(InstId::Xor as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Mem, GpbHi> for Assembler<'_> {
    fn xor_(&mut self, dst: Mem, src: GpbHi) {
        self.emit_n(InstId::Xor as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Mem, Gpw> for Assembler<'_> {
    fn xor_(&mut self, dst: Mem, src: Gpw) {
        self.emit_n(InstId::Xor as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Mem, Gpd> for Assembler<'_> {
    fn xor_(&mut self, dst: Mem, src: Gpd) {
        self.emit_n(InstId::Xor as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Mem, Gpq> for Assembler<'_> {
    fn xor_(&mut self, dst: Mem, src: Gpq) {
        self.emit_n(InstId::Xor as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> Xor_Emitter<Mem, U1> for Assembler<'_> {
    fn xor_(&mut self, dst: Mem, src: U1) {
        self.emit_n(
            InstId::Xor as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl AaaEmitter<Gp> for Assembler<'_> {
    fn aaa(&mut self, dst: Gp) {
        self.emit_n(InstId::Aaa as u32, &[dst.as_operand()]); // X86 [EXPLICIT]
    }
}

impl AaaEmitter<Gpw> for Assembler<'_> {
    fn aaa(&mut self, dst: Gpw) {
        self.emit_n(InstId::Aaa as u32, &[dst.as_operand()]); // X86 [EXPLICIT]
    }
}

impl AadEmitter<Gp, Imm> for Assembler<'_> {
    fn aad(&mut self, dst: Gp, imm: Imm) {
        self.emit_n(InstId::Aad as u32, &[dst.as_operand(), imm.as_operand()]); // X86 [EXPLICIT]
    }
}

impl<U1: Into<Imm>> AadEmitter<Gpw, U1> for Assembler<'_> {
    fn aad(&mut self, dst: Gpw, src: U1) {
        self.emit_n(
            InstId::Aad as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // X86 [EXPLICIT]
    }
}

impl AamEmitter<Gp, Imm> for Assembler<'_> {
    fn aam(&mut self, dst: Gp, imm: Imm) {
        self.emit_n(InstId::Aam as u32, &[dst.as_operand(), imm.as_operand()]); // X86 [EXPLICIT]
    }
}

impl<U1: Into<Imm>> AamEmitter<Gpw, U1> for Assembler<'_> {
    fn aam(&mut self, dst: Gpw, src: U1) {
        self.emit_n(
            InstId::Aam as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // X86 [EXPLICIT]
    }
}

impl AasEmitter<Gp> for Assembler<'_> {
    fn aas(&mut self, dst: Gp) {
        self.emit_n(InstId::Aas as u32, &[dst.as_operand()]); // X86 [EXPLICIT]
    }
}

impl AasEmitter<Gpw> for Assembler<'_> {
    fn aas(&mut self, dst: Gpw) {
        self.emit_n(InstId::Aas as u32, &[dst.as_operand()]); // X86 [EXPLICIT]
    }
}

impl DaaEmitter<Gp> for Assembler<'_> {
    fn daa(&mut self, dst: Gp) {
        self.emit_n(InstId::Daa as u32, &[dst.as_operand()]); // X86 [EXPLICIT]
    }
}

impl DaaEmitter<Gpw> for Assembler<'_> {
    fn daa(&mut self, dst: Gpw) {
        self.emit_n(InstId::Daa as u32, &[dst.as_operand()]); // X86 [EXPLICIT]
    }
}

impl DasEmitter<Gp> for Assembler<'_> {
    fn das(&mut self, dst: Gp) {
        self.emit_n(InstId::Das as u32, &[dst.as_operand()]); // X86 [EXPLICIT]
    }
}

impl DasEmitter<Gpw> for Assembler<'_> {
    fn das(&mut self, dst: Gpw) {
        self.emit_n(InstId::Das as u32, &[dst.as_operand()]); // X86 [EXPLICIT]
    }
}

impl<U0: Into<Imm>, U1: Into<Imm>> EnterEmitter<U0, U1> for Assembler<'_> {
    fn enter(&mut self, frame_size: U0, nesting_level: U1) {
        self.emit_n(
            InstId::Enter as u32,
            &[
                Into::<Imm>::into(frame_size).as_operand(),
                Into::<Imm>::into(nesting_level).as_operand(),
            ],
        ); // ANY
    }
}

impl LeaveEmitter for Assembler<'_> {
    fn leave(&mut self) {
        self.emit_n(InstId::Leave as u32, &[]); // ANY
    }
}

impl InEmitter<Gp, Imm> for Assembler<'_> {
    fn r#in(&mut self, dst: Gp, imm: Imm) {
        self.emit_n(InstId::In as u32, &[dst.as_operand(), imm.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> InEmitter<GpbLo, U1> for Assembler<'_> {
    fn r#in(&mut self, dst: GpbLo, src: U1) {
        self.emit_n(
            InstId::In as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> InEmitter<Gpw, U1> for Assembler<'_> {
    fn r#in(&mut self, dst: Gpw, src: U1) {
        self.emit_n(
            InstId::In as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> InEmitter<Gpd, U1> for Assembler<'_> {
    fn r#in(&mut self, dst: Gpd, src: U1) {
        self.emit_n(
            InstId::In as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // ANY
    }
}

impl InEmitter<Gp, Gp> for Assembler<'_> {
    fn r#in(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::In as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl InEmitter<GpbLo, Gpw> for Assembler<'_> {
    fn r#in(&mut self, dst: GpbLo, src: Gpw) {
        self.emit_n(InstId::In as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl InEmitter<Gpw, Gpw> for Assembler<'_> {
    fn r#in(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::In as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl InEmitter<Gpd, Gpw> for Assembler<'_> {
    fn r#in(&mut self, dst: Gpd, src: Gpw) {
        self.emit_n(InstId::In as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl InsEmitter<Mem, Gp> for Assembler<'_> {
    fn ins(&mut self, lhs: Mem, rhs: Gp) {
        self.emit_n(InstId::Ins as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl InsEmitter<Mem, Gpw> for Assembler<'_> {
    fn ins(&mut self, lhs: Mem, rhs: Gpw) {
        self.emit_n(InstId::Ins as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl OutEmitter<Imm, Gp> for Assembler<'_> {
    fn out(&mut self, imm: Imm, rhs: Gp) {
        self.emit_n(InstId::Out as u32, &[imm.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl<U0: Into<Imm>> OutEmitter<U0, GpbLo> for Assembler<'_> {
    fn out(&mut self, lhs: U0, rhs: GpbLo) {
        self.emit_n(
            InstId::Out as u32,
            &[Into::<Imm>::into(lhs).as_operand(), rhs.as_operand()],
        ); // ANY
    }
}

impl<U0: Into<Imm>> OutEmitter<U0, Gpw> for Assembler<'_> {
    fn out(&mut self, lhs: U0, rhs: Gpw) {
        self.emit_n(
            InstId::Out as u32,
            &[Into::<Imm>::into(lhs).as_operand(), rhs.as_operand()],
        ); // ANY
    }
}

impl<U0: Into<Imm>> OutEmitter<U0, Gpd> for Assembler<'_> {
    fn out(&mut self, lhs: U0, rhs: Gpd) {
        self.emit_n(
            InstId::Out as u32,
            &[Into::<Imm>::into(lhs).as_operand(), rhs.as_operand()],
        ); // ANY
    }
}

impl OutEmitter<Gp, Gp> for Assembler<'_> {
    fn out(&mut self, lhs: Gp, rhs: Gp) {
        self.emit_n(InstId::Out as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl OutEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn out(&mut self, lhs: Gpw, rhs: GpbLo) {
        self.emit_n(InstId::Out as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl OutEmitter<Gpw, Gpw> for Assembler<'_> {
    fn out(&mut self, lhs: Gpw, rhs: Gpw) {
        self.emit_n(InstId::Out as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl OutEmitter<Gpw, Gpd> for Assembler<'_> {
    fn out(&mut self, lhs: Gpw, rhs: Gpd) {
        self.emit_n(InstId::Out as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl OutsEmitter<Gp, Mem> for Assembler<'_> {
    fn outs(&mut self, lhs: Gp, rhs: Mem) {
        self.emit_n(InstId::Outs as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl OutsEmitter<Gpw, Mem> for Assembler<'_> {
    fn outs(&mut self, lhs: Gpw, rhs: Mem) {
        self.emit_n(InstId::Outs as u32, &[lhs.as_operand(), rhs.as_operand()]); // ANY
    }
}

impl ClcEmitter for Assembler<'_> {
    fn clc(&mut self) {
        self.emit_n(InstId::Clc as u32, &[]); // ANY
    }
}

impl CldEmitter for Assembler<'_> {
    fn cld(&mut self) {
        self.emit_n(InstId::Cld as u32, &[]); // ANY
    }
}

impl CmcEmitter for Assembler<'_> {
    fn cmc(&mut self) {
        self.emit_n(InstId::Cmc as u32, &[]); // ANY
    }
}

impl StcEmitter for Assembler<'_> {
    fn stc(&mut self) {
        self.emit_n(InstId::Stc as u32, &[]); // ANY
    }
}

impl StdEmitter for Assembler<'_> {
    fn std(&mut self) {
        self.emit_n(InstId::Std as u32, &[]); // ANY
    }
}

impl AdcxEmitter<Gp, Gp> for Assembler<'_> {
    fn adcx(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Adcx as u32, &[dst.as_operand(), src.as_operand()]); // ADX
    }
}

impl AdcxEmitter<Gpd, Gpd> for Assembler<'_> {
    fn adcx(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Adcx as u32, &[dst.as_operand(), src.as_operand()]); // ADX
    }
}

impl AdcxEmitter<Gpq, Gpq> for Assembler<'_> {
    fn adcx(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Adcx as u32, &[dst.as_operand(), src.as_operand()]); // ADX
    }
}

impl AdcxEmitter<Gp, Mem> for Assembler<'_> {
    fn adcx(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Adcx as u32, &[dst.as_operand(), src.as_operand()]); // ADX
    }
}

impl AdcxEmitter<Gpd, Mem> for Assembler<'_> {
    fn adcx(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Adcx as u32, &[dst.as_operand(), src.as_operand()]); // ADX
    }
}

impl AdcxEmitter<Gpq, Mem> for Assembler<'_> {
    fn adcx(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Adcx as u32, &[dst.as_operand(), src.as_operand()]); // ADX
    }
}

impl AdoxEmitter<Gp, Gp> for Assembler<'_> {
    fn adox(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Adox as u32, &[dst.as_operand(), src.as_operand()]); // ADX
    }
}

impl AdoxEmitter<Gpd, Gpd> for Assembler<'_> {
    fn adox(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Adox as u32, &[dst.as_operand(), src.as_operand()]); // ADX
    }
}

impl AdoxEmitter<Gpq, Gpq> for Assembler<'_> {
    fn adox(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Adox as u32, &[dst.as_operand(), src.as_operand()]); // ADX
    }
}

impl AdoxEmitter<Gp, Mem> for Assembler<'_> {
    fn adox(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Adox as u32, &[dst.as_operand(), src.as_operand()]); // ADX
    }
}

impl AdoxEmitter<Gpd, Mem> for Assembler<'_> {
    fn adox(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Adox as u32, &[dst.as_operand(), src.as_operand()]); // ADX
    }
}

impl AdoxEmitter<Gpq, Mem> for Assembler<'_> {
    fn adox(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Adox as u32, &[dst.as_operand(), src.as_operand()]); // ADX
    }
}

impl CpuidEmitter<Gp, Gp, Gp, Gp> for Assembler<'_> {
    fn cpuid(&mut self, dst: Gp, dst2: Gp, dst3: Gp, dst4: Gp) {
        self.emit_n(
            InstId::Cpuid as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                dst3.as_operand(),
                dst4.as_operand(),
            ],
        ); // I486 [EXPLICIT] EAX:EBX:ECX:EDX <- CPUID[EAX:ECX]
    }
}

impl CpuidEmitter<Gpd, Gpd, Gpd, Gpd> for Assembler<'_> {
    fn cpuid(&mut self, dst: Gpd, dst2: Gpd, dst3: Gpd, dst4: Gpd) {
        self.emit_n(
            InstId::Cpuid as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                dst3.as_operand(),
                dst4.as_operand(),
            ],
        ); // I486 [EXPLICIT] EAX:EBX:ECX:EDX <- CPUID[EAX:ECX]
    }
}

impl LahfEmitter<Gp> for Assembler<'_> {
    fn lahf(&mut self, dst: Gp) {
        self.emit_n(InstId::Lahf as u32, &[dst.as_operand()]); // LAHFSAHF [EXPLICIT] AH <- EFL
    }
}

impl LahfEmitter<GpbHi> for Assembler<'_> {
    fn lahf(&mut self, dst: GpbHi) {
        self.emit_n(InstId::Lahf as u32, &[dst.as_operand()]); // LAHFSAHF [EXPLICIT] AH <- EFL
    }
}

impl SahfEmitter<Gp> for Assembler<'_> {
    fn sahf(&mut self, src: Gp) {
        self.emit_n(InstId::Sahf as u32, &[src.as_operand()]); // LAHFSAHF [EXPLICIT] EFL <- AH
    }
}

impl SahfEmitter<GpbHi> for Assembler<'_> {
    fn sahf(&mut self, src: GpbHi) {
        self.emit_n(InstId::Sahf as u32, &[src.as_operand()]); // LAHFSAHF [EXPLICIT] EFL <- AH
    }
}

impl AndnEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn andn(&mut self, dst: Gp, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Andn as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI
    }
}

impl AndnEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn andn(&mut self, dst: Gpd, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Andn as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI
    }
}

impl AndnEmitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn andn(&mut self, dst: Gpq, src1: Gpq, src2: Gpq) {
        self.emit_n(
            InstId::Andn as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI
    }
}

impl AndnEmitter<Gp, Gp, Mem> for Assembler<'_> {
    fn andn(&mut self, dst: Gp, src1: Gp, src2: Mem) {
        self.emit_n(
            InstId::Andn as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI
    }
}

impl AndnEmitter<Gpd, Gpd, Mem> for Assembler<'_> {
    fn andn(&mut self, dst: Gpd, src1: Gpd, src2: Mem) {
        self.emit_n(
            InstId::Andn as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI
    }
}

impl AndnEmitter<Gpq, Gpq, Mem> for Assembler<'_> {
    fn andn(&mut self, dst: Gpq, src1: Gpq, src2: Mem) {
        self.emit_n(
            InstId::Andn as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI
    }
}

impl BextrEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn bextr(&mut self, dst: Gp, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Bextr as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI
    }
}

impl BextrEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn bextr(&mut self, dst: Gpd, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Bextr as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI
    }
}

impl BextrEmitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn bextr(&mut self, dst: Gpq, src1: Gpq, src2: Gpq) {
        self.emit_n(
            InstId::Bextr as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI
    }
}

impl BextrEmitter<Gp, Mem, Gp> for Assembler<'_> {
    fn bextr(&mut self, dst: Gp, src1: Mem, src2: Gp) {
        self.emit_n(
            InstId::Bextr as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI
    }
}

impl BextrEmitter<Gpd, Mem, Gpd> for Assembler<'_> {
    fn bextr(&mut self, dst: Gpd, src1: Mem, src2: Gpd) {
        self.emit_n(
            InstId::Bextr as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI
    }
}

impl BextrEmitter<Gpq, Mem, Gpq> for Assembler<'_> {
    fn bextr(&mut self, dst: Gpq, src1: Mem, src2: Gpq) {
        self.emit_n(
            InstId::Bextr as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI
    }
}

impl BlsiEmitter<Gp, Gp> for Assembler<'_> {
    fn blsi(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Blsi as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl BlsiEmitter<Gpd, Gpd> for Assembler<'_> {
    fn blsi(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Blsi as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl BlsiEmitter<Gpq, Gpq> for Assembler<'_> {
    fn blsi(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Blsi as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl BlsiEmitter<Gp, Mem> for Assembler<'_> {
    fn blsi(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Blsi as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl BlsiEmitter<Gpd, Mem> for Assembler<'_> {
    fn blsi(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Blsi as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl BlsiEmitter<Gpq, Mem> for Assembler<'_> {
    fn blsi(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Blsi as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl BlsmskEmitter<Gp, Gp> for Assembler<'_> {
    fn blsmsk(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Blsmsk as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl BlsmskEmitter<Gpd, Gpd> for Assembler<'_> {
    fn blsmsk(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Blsmsk as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl BlsmskEmitter<Gpq, Gpq> for Assembler<'_> {
    fn blsmsk(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Blsmsk as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl BlsmskEmitter<Gp, Mem> for Assembler<'_> {
    fn blsmsk(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Blsmsk as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl BlsmskEmitter<Gpd, Mem> for Assembler<'_> {
    fn blsmsk(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Blsmsk as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl BlsmskEmitter<Gpq, Mem> for Assembler<'_> {
    fn blsmsk(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Blsmsk as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl BlsrEmitter<Gp, Gp> for Assembler<'_> {
    fn blsr(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Blsr as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl BlsrEmitter<Gpd, Gpd> for Assembler<'_> {
    fn blsr(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Blsr as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl BlsrEmitter<Gpq, Gpq> for Assembler<'_> {
    fn blsr(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Blsr as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl BlsrEmitter<Gp, Mem> for Assembler<'_> {
    fn blsr(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Blsr as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl BlsrEmitter<Gpd, Mem> for Assembler<'_> {
    fn blsr(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Blsr as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl BlsrEmitter<Gpq, Mem> for Assembler<'_> {
    fn blsr(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Blsr as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl TzcntEmitter<Gp, Gp> for Assembler<'_> {
    fn tzcnt(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Tzcnt as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl TzcntEmitter<Gpw, Gpw> for Assembler<'_> {
    fn tzcnt(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Tzcnt as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl TzcntEmitter<Gpd, Gpd> for Assembler<'_> {
    fn tzcnt(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Tzcnt as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl TzcntEmitter<Gpq, Gpq> for Assembler<'_> {
    fn tzcnt(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Tzcnt as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl TzcntEmitter<Gp, Mem> for Assembler<'_> {
    fn tzcnt(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Tzcnt as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl TzcntEmitter<Gpw, Mem> for Assembler<'_> {
    fn tzcnt(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Tzcnt as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl TzcntEmitter<Gpd, Mem> for Assembler<'_> {
    fn tzcnt(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Tzcnt as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl TzcntEmitter<Gpq, Mem> for Assembler<'_> {
    fn tzcnt(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Tzcnt as u32, &[dst.as_operand(), src.as_operand()]); // BMI
    }
}

impl BzhiEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn bzhi(&mut self, dst: Gp, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Bzhi as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl BzhiEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn bzhi(&mut self, dst: Gpd, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Bzhi as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl BzhiEmitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn bzhi(&mut self, dst: Gpq, src1: Gpq, src2: Gpq) {
        self.emit_n(
            InstId::Bzhi as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl BzhiEmitter<Gp, Mem, Gp> for Assembler<'_> {
    fn bzhi(&mut self, dst: Gp, src1: Mem, src2: Gp) {
        self.emit_n(
            InstId::Bzhi as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl BzhiEmitter<Gpd, Mem, Gpd> for Assembler<'_> {
    fn bzhi(&mut self, dst: Gpd, src1: Mem, src2: Gpd) {
        self.emit_n(
            InstId::Bzhi as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl BzhiEmitter<Gpq, Mem, Gpq> for Assembler<'_> {
    fn bzhi(&mut self, dst: Gpq, src1: Mem, src2: Gpq) {
        self.emit_n(
            InstId::Bzhi as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl MulxEmitter<Gp, Gp, Gp, Gp> for Assembler<'_> {
    fn mulx(&mut self, dst: Gp, dst2: Gp, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Mulx as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // BMI2 [EXPLICIT]
    }
}

impl MulxEmitter<Gpd, Gpd, Gpd, Gpd> for Assembler<'_> {
    fn mulx(&mut self, dst: Gpd, dst2: Gpd, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Mulx as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // BMI2 [EXPLICIT]
    }
}

impl MulxEmitter<Gpq, Gpq, Gpq, Gpq> for Assembler<'_> {
    fn mulx(&mut self, dst: Gpq, dst2: Gpq, src1: Gpq, src2: Gpq) {
        self.emit_n(
            InstId::Mulx as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // BMI2 [EXPLICIT]
    }
}

impl MulxEmitter<Gp, Gp, Mem, Gp> for Assembler<'_> {
    fn mulx(&mut self, dst: Gp, dst2: Gp, src1: Mem, src2: Gp) {
        self.emit_n(
            InstId::Mulx as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // BMI2 [EXPLICIT]
    }
}

impl MulxEmitter<Gpd, Gpd, Mem, Gpd> for Assembler<'_> {
    fn mulx(&mut self, dst: Gpd, dst2: Gpd, src1: Mem, src2: Gpd) {
        self.emit_n(
            InstId::Mulx as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // BMI2 [EXPLICIT]
    }
}

impl MulxEmitter<Gpq, Gpq, Mem, Gpq> for Assembler<'_> {
    fn mulx(&mut self, dst: Gpq, dst2: Gpq, src1: Mem, src2: Gpq) {
        self.emit_n(
            InstId::Mulx as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // BMI2 [EXPLICIT]
    }
}

impl PdepEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn pdep(&mut self, dst: Gp, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Pdep as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl PdepEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn pdep(&mut self, dst: Gpd, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Pdep as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl PdepEmitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn pdep(&mut self, dst: Gpq, src1: Gpq, src2: Gpq) {
        self.emit_n(
            InstId::Pdep as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl PdepEmitter<Gp, Gp, Mem> for Assembler<'_> {
    fn pdep(&mut self, dst: Gp, src1: Gp, src2: Mem) {
        self.emit_n(
            InstId::Pdep as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl PdepEmitter<Gpd, Gpd, Mem> for Assembler<'_> {
    fn pdep(&mut self, dst: Gpd, src1: Gpd, src2: Mem) {
        self.emit_n(
            InstId::Pdep as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl PdepEmitter<Gpq, Gpq, Mem> for Assembler<'_> {
    fn pdep(&mut self, dst: Gpq, src1: Gpq, src2: Mem) {
        self.emit_n(
            InstId::Pdep as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl PextEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn pext(&mut self, dst: Gp, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Pext as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl PextEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn pext(&mut self, dst: Gpd, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Pext as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl PextEmitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn pext(&mut self, dst: Gpq, src1: Gpq, src2: Gpq) {
        self.emit_n(
            InstId::Pext as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl PextEmitter<Gp, Gp, Mem> for Assembler<'_> {
    fn pext(&mut self, dst: Gp, src1: Gp, src2: Mem) {
        self.emit_n(
            InstId::Pext as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl PextEmitter<Gpd, Gpd, Mem> for Assembler<'_> {
    fn pext(&mut self, dst: Gpd, src1: Gpd, src2: Mem) {
        self.emit_n(
            InstId::Pext as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl PextEmitter<Gpq, Gpq, Mem> for Assembler<'_> {
    fn pext(&mut self, dst: Gpq, src1: Gpq, src2: Mem) {
        self.emit_n(
            InstId::Pext as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl RorxEmitter<Gp, Gp, Imm> for Assembler<'_> {
    fn rorx(&mut self, dst: Gp, src: Gp, imm: Imm) {
        self.emit_n(
            InstId::Rorx as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // BMI2
    }
}

impl<U2: Into<Imm>> RorxEmitter<Gpd, Gpd, U2> for Assembler<'_> {
    fn rorx(&mut self, dst: Gpd, src1: Gpd, src2: U2) {
        self.emit_n(
            InstId::Rorx as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // BMI2
    }
}

impl<U2: Into<Imm>> RorxEmitter<Gpq, Gpq, U2> for Assembler<'_> {
    fn rorx(&mut self, dst: Gpq, src1: Gpq, src2: U2) {
        self.emit_n(
            InstId::Rorx as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // BMI2
    }
}

impl RorxEmitter<Gp, Mem, Imm> for Assembler<'_> {
    fn rorx(&mut self, dst: Gp, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Rorx as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // BMI2
    }
}

impl<U2: Into<Imm>> RorxEmitter<Gpd, Mem, U2> for Assembler<'_> {
    fn rorx(&mut self, dst: Gpd, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Rorx as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // BMI2
    }
}

impl<U2: Into<Imm>> RorxEmitter<Gpq, Mem, U2> for Assembler<'_> {
    fn rorx(&mut self, dst: Gpq, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Rorx as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // BMI2
    }
}

impl SarxEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn sarx(&mut self, dst: Gp, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Sarx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl SarxEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn sarx(&mut self, dst: Gpd, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Sarx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl SarxEmitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn sarx(&mut self, dst: Gpq, src1: Gpq, src2: Gpq) {
        self.emit_n(
            InstId::Sarx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl SarxEmitter<Gp, Mem, Gp> for Assembler<'_> {
    fn sarx(&mut self, dst: Gp, src1: Mem, src2: Gp) {
        self.emit_n(
            InstId::Sarx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl SarxEmitter<Gpd, Mem, Gpd> for Assembler<'_> {
    fn sarx(&mut self, dst: Gpd, src1: Mem, src2: Gpd) {
        self.emit_n(
            InstId::Sarx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl SarxEmitter<Gpq, Mem, Gpq> for Assembler<'_> {
    fn sarx(&mut self, dst: Gpq, src1: Mem, src2: Gpq) {
        self.emit_n(
            InstId::Sarx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl ShlxEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn shlx(&mut self, dst: Gp, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Shlx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl ShlxEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn shlx(&mut self, dst: Gpd, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Shlx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl ShlxEmitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn shlx(&mut self, dst: Gpq, src1: Gpq, src2: Gpq) {
        self.emit_n(
            InstId::Shlx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl ShlxEmitter<Gp, Mem, Gp> for Assembler<'_> {
    fn shlx(&mut self, dst: Gp, src1: Mem, src2: Gp) {
        self.emit_n(
            InstId::Shlx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl ShlxEmitter<Gpd, Mem, Gpd> for Assembler<'_> {
    fn shlx(&mut self, dst: Gpd, src1: Mem, src2: Gpd) {
        self.emit_n(
            InstId::Shlx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl ShlxEmitter<Gpq, Mem, Gpq> for Assembler<'_> {
    fn shlx(&mut self, dst: Gpq, src1: Mem, src2: Gpq) {
        self.emit_n(
            InstId::Shlx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl ShrxEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn shrx(&mut self, dst: Gp, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Shrx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl ShrxEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn shrx(&mut self, dst: Gpd, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Shrx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl ShrxEmitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn shrx(&mut self, dst: Gpq, src1: Gpq, src2: Gpq) {
        self.emit_n(
            InstId::Shrx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl ShrxEmitter<Gp, Mem, Gp> for Assembler<'_> {
    fn shrx(&mut self, dst: Gp, src1: Mem, src2: Gp) {
        self.emit_n(
            InstId::Shrx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl ShrxEmitter<Gpd, Mem, Gpd> for Assembler<'_> {
    fn shrx(&mut self, dst: Gpd, src1: Mem, src2: Gpd) {
        self.emit_n(
            InstId::Shrx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl ShrxEmitter<Gpq, Mem, Gpq> for Assembler<'_> {
    fn shrx(&mut self, dst: Gpq, src1: Mem, src2: Gpq) {
        self.emit_n(
            InstId::Shrx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2
    }
}

impl CmpbexaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpbexadd(&mut self, dst: Mem, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Cmpbexadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpbexaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpbexadd(&mut self, dst: Mem, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Cmpbexadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpbexaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpbexadd(&mut self, dst: Mem, dst2: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Cmpbexadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpbxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpbxadd(&mut self, dst: Mem, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Cmpbxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpbxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpbxadd(&mut self, dst: Mem, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Cmpbxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpbxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpbxadd(&mut self, dst: Mem, dst2: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Cmpbxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmplexaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmplexadd(&mut self, dst: Mem, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Cmplexadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmplexaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmplexadd(&mut self, dst: Mem, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Cmplexadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmplexaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmplexadd(&mut self, dst: Mem, dst2: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Cmplexadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmplxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmplxadd(&mut self, dst: Mem, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Cmplxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmplxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmplxadd(&mut self, dst: Mem, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Cmplxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmplxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmplxadd(&mut self, dst: Mem, dst2: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Cmplxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnbexaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpnbexadd(&mut self, dst: Mem, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Cmpnbexadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnbexaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpnbexadd(&mut self, dst: Mem, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Cmpnbexadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnbexaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpnbexadd(&mut self, dst: Mem, dst2: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Cmpnbexadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnbxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpnbxadd(&mut self, dst: Mem, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Cmpnbxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnbxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpnbxadd(&mut self, dst: Mem, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Cmpnbxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnbxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpnbxadd(&mut self, dst: Mem, dst2: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Cmpnbxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnlexaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpnlexadd(&mut self, dst: Mem, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Cmpnlexadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnlexaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpnlexadd(&mut self, dst: Mem, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Cmpnlexadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnlexaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpnlexadd(&mut self, dst: Mem, dst2: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Cmpnlexadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnlxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpnlxadd(&mut self, dst: Mem, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Cmpnlxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnlxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpnlxadd(&mut self, dst: Mem, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Cmpnlxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnlxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpnlxadd(&mut self, dst: Mem, dst2: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Cmpnlxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnoxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpnoxadd(&mut self, dst: Mem, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Cmpnoxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnoxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpnoxadd(&mut self, dst: Mem, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Cmpnoxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnoxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpnoxadd(&mut self, dst: Mem, dst2: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Cmpnoxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnpxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpnpxadd(&mut self, dst: Mem, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Cmpnpxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnpxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpnpxadd(&mut self, dst: Mem, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Cmpnpxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnpxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpnpxadd(&mut self, dst: Mem, dst2: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Cmpnpxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnsxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpnsxadd(&mut self, dst: Mem, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Cmpnsxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnsxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpnsxadd(&mut self, dst: Mem, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Cmpnsxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnsxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpnsxadd(&mut self, dst: Mem, dst2: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Cmpnsxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnzxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpnzxadd(&mut self, dst: Mem, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Cmpnzxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnzxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpnzxadd(&mut self, dst: Mem, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Cmpnzxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpnzxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpnzxadd(&mut self, dst: Mem, dst2: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Cmpnzxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpoxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpoxadd(&mut self, dst: Mem, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Cmpoxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpoxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpoxadd(&mut self, dst: Mem, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Cmpoxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpoxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpoxadd(&mut self, dst: Mem, dst2: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Cmpoxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmppxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmppxadd(&mut self, dst: Mem, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Cmppxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmppxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmppxadd(&mut self, dst: Mem, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Cmppxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmppxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmppxadd(&mut self, dst: Mem, dst2: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Cmppxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpsxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpsxadd(&mut self, dst: Mem, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Cmpsxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpsxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpsxadd(&mut self, dst: Mem, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Cmpsxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpsxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpsxadd(&mut self, dst: Mem, dst2: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Cmpsxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpzxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpzxadd(&mut self, dst: Mem, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Cmpzxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpzxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpzxadd(&mut self, dst: Mem, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Cmpzxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CmpzxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpzxadd(&mut self, dst: Mem, dst2: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Cmpzxadd as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        );
    }
}

impl CldemoteEmitter<Mem> for Assembler<'_> {
    fn cldemote(&mut self, mem: Mem) {
        self.emit_n(InstId::Cldemote as u32, &[mem.as_operand()]); // CLDEMOTE
    }
}

impl ClflushEmitter<Mem> for Assembler<'_> {
    fn clflush(&mut self, mem: Mem) {
        self.emit_n(InstId::Clflush as u32, &[mem.as_operand()]); // CLFLUSH
    }
}

impl ClflushoptEmitter<Mem> for Assembler<'_> {
    fn clflushopt(&mut self, mem: Mem) {
        self.emit_n(InstId::Clflushopt as u32, &[mem.as_operand()]); // CLFLUSH_OPT
    }
}

impl ClwbEmitter<Mem> for Assembler<'_> {
    fn clwb(&mut self, mem: Mem) {
        self.emit_n(InstId::Clwb as u32, &[mem.as_operand()]); // CLWB
    }
}

impl ClzeroEmitter<Mem> for Assembler<'_> {
    fn clzero(&mut self, mem: Mem) {
        self.emit_n(InstId::Clzero as u32, &[mem.as_operand()]); // CLZERO [EXPLICIT]
    }
}

impl Crc32Emitter<Gp, Gp> for Assembler<'_> {
    fn crc32(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Crc32 as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_2
    }
}

impl Crc32Emitter<Gpd, GpbLo> for Assembler<'_> {
    fn crc32(&mut self, dst: Gpd, src: GpbLo) {
        self.emit_n(InstId::Crc32 as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_2
    }
}

impl Crc32Emitter<Gpd, GpbHi> for Assembler<'_> {
    fn crc32(&mut self, dst: Gpd, src: GpbHi) {
        self.emit_n(InstId::Crc32 as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_2
    }
}

impl Crc32Emitter<Gpd, Gpw> for Assembler<'_> {
    fn crc32(&mut self, dst: Gpd, src: Gpw) {
        self.emit_n(InstId::Crc32 as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_2
    }
}

impl Crc32Emitter<Gpd, Gpd> for Assembler<'_> {
    fn crc32(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Crc32 as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_2
    }
}

impl Crc32Emitter<Gpq, GpbLo> for Assembler<'_> {
    fn crc32(&mut self, dst: Gpq, src: GpbLo) {
        self.emit_n(InstId::Crc32 as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_2
    }
}

impl Crc32Emitter<Gpq, GpbHi> for Assembler<'_> {
    fn crc32(&mut self, dst: Gpq, src: GpbHi) {
        self.emit_n(InstId::Crc32 as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_2
    }
}

impl Crc32Emitter<Gpq, Gpq> for Assembler<'_> {
    fn crc32(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Crc32 as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_2
    }
}

impl Crc32Emitter<Gp, Mem> for Assembler<'_> {
    fn crc32(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Crc32 as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_2
    }
}

impl Crc32Emitter<Gpd, Mem> for Assembler<'_> {
    fn crc32(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Crc32 as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_2
    }
}

impl Crc32Emitter<Gpq, Mem> for Assembler<'_> {
    fn crc32(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Crc32 as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_2
    }
}

impl LfenceEmitter for Assembler<'_> {
    fn lfence(&mut self) {
        self.emit_n(InstId::Lfence as u32, &[]); // SSE2
    }
}

impl MfenceEmitter for Assembler<'_> {
    fn mfence(&mut self) {
        self.emit_n(InstId::Mfence as u32, &[]); // SSE2
    }
}

impl SfenceEmitter for Assembler<'_> {
    fn sfence(&mut self) {
        self.emit_n(InstId::Sfence as u32, &[]); // SSE
    }
}

impl LzcntEmitter<Gp, Gp> for Assembler<'_> {
    fn lzcnt(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Lzcnt as u32, &[dst.as_operand(), src.as_operand()]); // LZCNT
    }
}

impl LzcntEmitter<Gpw, Gpw> for Assembler<'_> {
    fn lzcnt(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Lzcnt as u32, &[dst.as_operand(), src.as_operand()]); // LZCNT
    }
}

impl LzcntEmitter<Gpd, Gpd> for Assembler<'_> {
    fn lzcnt(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Lzcnt as u32, &[dst.as_operand(), src.as_operand()]); // LZCNT
    }
}

impl LzcntEmitter<Gpq, Gpq> for Assembler<'_> {
    fn lzcnt(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Lzcnt as u32, &[dst.as_operand(), src.as_operand()]); // LZCNT
    }
}

impl LzcntEmitter<Gp, Mem> for Assembler<'_> {
    fn lzcnt(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Lzcnt as u32, &[dst.as_operand(), src.as_operand()]); // LZCNT
    }
}

impl LzcntEmitter<Gpw, Mem> for Assembler<'_> {
    fn lzcnt(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Lzcnt as u32, &[dst.as_operand(), src.as_operand()]); // LZCNT
    }
}

impl LzcntEmitter<Gpd, Mem> for Assembler<'_> {
    fn lzcnt(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Lzcnt as u32, &[dst.as_operand(), src.as_operand()]); // LZCNT
    }
}

impl LzcntEmitter<Gpq, Mem> for Assembler<'_> {
    fn lzcnt(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Lzcnt as u32, &[dst.as_operand(), src.as_operand()]); // LZCNT
    }
}

impl MovbeEmitter<Gp, Mem> for Assembler<'_> {
    fn movbe(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Movbe as u32, &[dst.as_operand(), src.as_operand()]); // MOVBE
    }
}

impl MovbeEmitter<Gpw, Mem> for Assembler<'_> {
    fn movbe(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Movbe as u32, &[dst.as_operand(), src.as_operand()]); // MOVBE
    }
}

impl MovbeEmitter<Gpd, Mem> for Assembler<'_> {
    fn movbe(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Movbe as u32, &[dst.as_operand(), src.as_operand()]); // MOVBE
    }
}

impl MovbeEmitter<Gpq, Mem> for Assembler<'_> {
    fn movbe(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Movbe as u32, &[dst.as_operand(), src.as_operand()]); // MOVBE
    }
}

impl MovbeEmitter<Mem, Gp> for Assembler<'_> {
    fn movbe(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Movbe as u32, &[dst.as_operand(), src.as_operand()]); // MOVBE
    }
}

impl MovbeEmitter<Mem, Gpw> for Assembler<'_> {
    fn movbe(&mut self, dst: Mem, src: Gpw) {
        self.emit_n(InstId::Movbe as u32, &[dst.as_operand(), src.as_operand()]); // MOVBE
    }
}

impl MovbeEmitter<Mem, Gpd> for Assembler<'_> {
    fn movbe(&mut self, dst: Mem, src: Gpd) {
        self.emit_n(InstId::Movbe as u32, &[dst.as_operand(), src.as_operand()]); // MOVBE
    }
}

impl MovbeEmitter<Mem, Gpq> for Assembler<'_> {
    fn movbe(&mut self, dst: Mem, src: Gpq) {
        self.emit_n(InstId::Movbe as u32, &[dst.as_operand(), src.as_operand()]); // MOVBE
    }
}

impl MovdiriEmitter<Mem, Gp> for Assembler<'_> {
    fn movdiri(&mut self, dst: Mem, src: Gp) {
        self.emit_n(
            InstId::Movdiri as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MOVDIRI
    }
}

impl MovdiriEmitter<Mem, Gpd> for Assembler<'_> {
    fn movdiri(&mut self, dst: Mem, src: Gpd) {
        self.emit_n(
            InstId::Movdiri as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MOVDIRI
    }
}

impl MovdiriEmitter<Mem, Gpq> for Assembler<'_> {
    fn movdiri(&mut self, dst: Mem, src: Gpq) {
        self.emit_n(
            InstId::Movdiri as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MOVDIRI
    }
}

impl Movdir64bEmitter<Mem, Mem> for Assembler<'_> {
    fn movdir64b(&mut self, dst: Mem, src: Mem) {
        self.emit_n(
            InstId::Movdir64b as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MOVDIR64B
    }
}

impl LdmxcsrEmitter<Mem> for Assembler<'_> {
    fn ldmxcsr(&mut self, mem: Mem) {
        self.emit_n(InstId::Ldmxcsr as u32, &[mem.as_operand()]); // SSE
    }
}

impl StmxcsrEmitter<Mem> for Assembler<'_> {
    fn stmxcsr(&mut self, dst: Mem) {
        self.emit_n(InstId::Stmxcsr as u32, &[dst.as_operand()]); // SSE
    }
}

impl PopcntEmitter<Gp, Gp> for Assembler<'_> {
    fn popcnt(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Popcnt as u32, &[dst.as_operand(), src.as_operand()]); // POPCNT
    }
}

impl PopcntEmitter<Gpw, Gpw> for Assembler<'_> {
    fn popcnt(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Popcnt as u32, &[dst.as_operand(), src.as_operand()]); // POPCNT
    }
}

impl PopcntEmitter<Gpd, Gpd> for Assembler<'_> {
    fn popcnt(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Popcnt as u32, &[dst.as_operand(), src.as_operand()]); // POPCNT
    }
}

impl PopcntEmitter<Gpq, Gpq> for Assembler<'_> {
    fn popcnt(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Popcnt as u32, &[dst.as_operand(), src.as_operand()]); // POPCNT
    }
}

impl PopcntEmitter<Gp, Mem> for Assembler<'_> {
    fn popcnt(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Popcnt as u32, &[dst.as_operand(), src.as_operand()]); // POPCNT
    }
}

impl PopcntEmitter<Gpw, Mem> for Assembler<'_> {
    fn popcnt(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Popcnt as u32, &[dst.as_operand(), src.as_operand()]); // POPCNT
    }
}

impl PopcntEmitter<Gpd, Mem> for Assembler<'_> {
    fn popcnt(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Popcnt as u32, &[dst.as_operand(), src.as_operand()]); // POPCNT
    }
}

impl PopcntEmitter<Gpq, Mem> for Assembler<'_> {
    fn popcnt(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Popcnt as u32, &[dst.as_operand(), src.as_operand()]); // POPCNT
    }
}

impl PrefetchEmitter<Mem> for Assembler<'_> {
    fn prefetch(&mut self, mem: Mem) {
        self.emit_n(InstId::Prefetch as u32, &[mem.as_operand()]); // 3DNOW
    }
}

impl PrefetchntaEmitter<Mem> for Assembler<'_> {
    fn prefetchnta(&mut self, mem: Mem) {
        self.emit_n(InstId::Prefetchnta as u32, &[mem.as_operand()]); // SSE
    }
}

impl Prefetcht0Emitter<Mem> for Assembler<'_> {
    fn prefetcht0(&mut self, mem: Mem) {
        self.emit_n(InstId::Prefetcht0 as u32, &[mem.as_operand()]); // SSE
    }
}

impl Prefetcht1Emitter<Mem> for Assembler<'_> {
    fn prefetcht1(&mut self, mem: Mem) {
        self.emit_n(InstId::Prefetcht1 as u32, &[mem.as_operand()]); // SSE
    }
}

impl Prefetcht2Emitter<Mem> for Assembler<'_> {
    fn prefetcht2(&mut self, mem: Mem) {
        self.emit_n(InstId::Prefetcht2 as u32, &[mem.as_operand()]); // SSE
    }
}

impl PrefetchwEmitter<Mem> for Assembler<'_> {
    fn prefetchw(&mut self, mem: Mem) {
        self.emit_n(InstId::Prefetchw as u32, &[mem.as_operand()]); // PREFETCHW
    }
}

impl Prefetchwt1Emitter<Mem> for Assembler<'_> {
    fn prefetchwt1(&mut self, mem: Mem) {
        self.emit_n(InstId::Prefetchwt1 as u32, &[mem.as_operand()]); // PREFETCHW1
    }
}

impl Prefetchit0Emitter<Mem> for Assembler<'_> {
    fn prefetchit0(&mut self, mem: Mem) {
        self.emit_n(InstId::Prefetchit0 as u32, &[mem.as_operand()]);
    }
}

impl Prefetchit1Emitter<Mem> for Assembler<'_> {
    fn prefetchit1(&mut self, mem: Mem) {
        self.emit_n(InstId::Prefetchit1 as u32, &[mem.as_operand()]);
    }
}

impl AaddEmitter<Mem, Gp> for Assembler<'_> {
    fn aadd(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Aadd as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl AaddEmitter<Mem, Gpd> for Assembler<'_> {
    fn aadd(&mut self, dst: Mem, src: Gpd) {
        self.emit_n(InstId::Aadd as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl AaddEmitter<Mem, Gpq> for Assembler<'_> {
    fn aadd(&mut self, dst: Mem, src: Gpq) {
        self.emit_n(InstId::Aadd as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl AandEmitter<Mem, Gp> for Assembler<'_> {
    fn aand(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Aand as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl AandEmitter<Mem, Gpd> for Assembler<'_> {
    fn aand(&mut self, dst: Mem, src: Gpd) {
        self.emit_n(InstId::Aand as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl AandEmitter<Mem, Gpq> for Assembler<'_> {
    fn aand(&mut self, dst: Mem, src: Gpq) {
        self.emit_n(InstId::Aand as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl AorEmitter<Mem, Gp> for Assembler<'_> {
    fn aor(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Aor as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl AorEmitter<Mem, Gpd> for Assembler<'_> {
    fn aor(&mut self, dst: Mem, src: Gpd) {
        self.emit_n(InstId::Aor as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl AorEmitter<Mem, Gpq> for Assembler<'_> {
    fn aor(&mut self, dst: Mem, src: Gpq) {
        self.emit_n(InstId::Aor as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl AxorEmitter<Mem, Gp> for Assembler<'_> {
    fn axor(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Axor as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl AxorEmitter<Mem, Gpd> for Assembler<'_> {
    fn axor(&mut self, dst: Mem, src: Gpd) {
        self.emit_n(InstId::Axor as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl AxorEmitter<Mem, Gpq> for Assembler<'_> {
    fn axor(&mut self, dst: Mem, src: Gpq) {
        self.emit_n(InstId::Axor as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl RdpidEmitter<Gp> for Assembler<'_> {
    fn rdpid(&mut self, dst: Gp) {
        self.emit_n(InstId::Rdpid as u32, &[dst.as_operand()]); // RDPID
    }
}

impl RdpidEmitter<Gpd> for Assembler<'_> {
    fn rdpid(&mut self, dst: Gpd) {
        self.emit_n(InstId::Rdpid as u32, &[dst.as_operand()]); // RDPID
    }
}

impl RdpidEmitter<Gpq> for Assembler<'_> {
    fn rdpid(&mut self, dst: Gpq) {
        self.emit_n(InstId::Rdpid as u32, &[dst.as_operand()]); // RDPID
    }
}

impl RdpruEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn rdpru(&mut self, dst: Gp, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Rdpru as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // RDPRU [EXPLICIT] EDX:EAX <- PRU[ECX]
    }
}

impl RdpruEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn rdpru(&mut self, dst: Gpd, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Rdpru as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // RDPRU [EXPLICIT] EDX:EAX <- PRU[ECX]
    }
}

impl RdpkruEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn rdpkru(&mut self, dst: Gp, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Rdpkru as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // RDPKRU [EXPLICIT] EDX:EAX <- PKRU[ECX]
    }
}

impl RdpkruEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn rdpkru(&mut self, dst: Gpd, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Rdpkru as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // RDPKRU [EXPLICIT] EDX:EAX <- PKRU[ECX]
    }
}

impl RdtscEmitter<Gp, Gp> for Assembler<'_> {
    fn rdtsc(&mut self, dst: Gp, dst2: Gp) {
        self.emit_n(InstId::Rdtsc as u32, &[dst.as_operand(), dst2.as_operand()]); // RDTSC [EXPLICIT] EDX:EAX <- Counter
    }
}

impl RdtscEmitter<Gpd, Gpd> for Assembler<'_> {
    fn rdtsc(&mut self, dst: Gpd, dst2: Gpd) {
        self.emit_n(InstId::Rdtsc as u32, &[dst.as_operand(), dst2.as_operand()]); // RDTSC [EXPLICIT] EDX:EAX <- Counter
    }
}

impl RdtscpEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn rdtscp(&mut self, dst: Gp, dst2: Gp, dst3: Gp) {
        self.emit_n(
            InstId::Rdtscp as u32,
            &[dst.as_operand(), dst2.as_operand(), dst3.as_operand()],
        ); // RDTSCP [EXPLICIT] EDX:EAX:EXC <- Counter
    }
}

impl RdtscpEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn rdtscp(&mut self, dst: Gpd, dst2: Gpd, dst3: Gpd) {
        self.emit_n(
            InstId::Rdtscp as u32,
            &[dst.as_operand(), dst2.as_operand(), dst3.as_operand()],
        ); // RDTSCP [EXPLICIT] EDX:EAX:EXC <- Counter
    }
}

impl SerializeEmitter for Assembler<'_> {
    fn serialize(&mut self) {
        self.emit_n(InstId::Serialize as u32, &[]); // SERIALIZE
    }
}

impl BlcfillEmitter<Gp, Gp> for Assembler<'_> {
    fn blcfill(&mut self, dst: Gp, src: Gp) {
        self.emit_n(
            InstId::Blcfill as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // TBM
    }
}

impl BlcfillEmitter<Gpd, Gpd> for Assembler<'_> {
    fn blcfill(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Blcfill as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // TBM
    }
}

impl BlcfillEmitter<Gpq, Gpq> for Assembler<'_> {
    fn blcfill(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Blcfill as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // TBM
    }
}

impl BlcfillEmitter<Gp, Mem> for Assembler<'_> {
    fn blcfill(&mut self, dst: Gp, src: Mem) {
        self.emit_n(
            InstId::Blcfill as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // TBM
    }
}

impl BlcfillEmitter<Gpd, Mem> for Assembler<'_> {
    fn blcfill(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(
            InstId::Blcfill as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // TBM
    }
}

impl BlcfillEmitter<Gpq, Mem> for Assembler<'_> {
    fn blcfill(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(
            InstId::Blcfill as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // TBM
    }
}

impl BlciEmitter<Gp, Gp> for Assembler<'_> {
    fn blci(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Blci as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlciEmitter<Gpd, Gpd> for Assembler<'_> {
    fn blci(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Blci as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlciEmitter<Gpq, Gpq> for Assembler<'_> {
    fn blci(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Blci as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlciEmitter<Gp, Mem> for Assembler<'_> {
    fn blci(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Blci as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlciEmitter<Gpd, Mem> for Assembler<'_> {
    fn blci(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Blci as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlciEmitter<Gpq, Mem> for Assembler<'_> {
    fn blci(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Blci as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlcicEmitter<Gp, Gp> for Assembler<'_> {
    fn blcic(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Blcic as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlcicEmitter<Gpd, Gpd> for Assembler<'_> {
    fn blcic(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Blcic as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlcicEmitter<Gpq, Gpq> for Assembler<'_> {
    fn blcic(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Blcic as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlcicEmitter<Gp, Mem> for Assembler<'_> {
    fn blcic(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Blcic as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlcicEmitter<Gpd, Mem> for Assembler<'_> {
    fn blcic(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Blcic as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlcicEmitter<Gpq, Mem> for Assembler<'_> {
    fn blcic(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Blcic as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlcmskEmitter<Gp, Gp> for Assembler<'_> {
    fn blcmsk(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Blcmsk as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlcmskEmitter<Gpd, Gpd> for Assembler<'_> {
    fn blcmsk(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Blcmsk as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlcmskEmitter<Gpq, Gpq> for Assembler<'_> {
    fn blcmsk(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Blcmsk as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlcmskEmitter<Gp, Mem> for Assembler<'_> {
    fn blcmsk(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Blcmsk as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlcmskEmitter<Gpd, Mem> for Assembler<'_> {
    fn blcmsk(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Blcmsk as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlcmskEmitter<Gpq, Mem> for Assembler<'_> {
    fn blcmsk(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Blcmsk as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlcsEmitter<Gp, Gp> for Assembler<'_> {
    fn blcs(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Blcs as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlcsEmitter<Gpd, Gpd> for Assembler<'_> {
    fn blcs(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Blcs as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlcsEmitter<Gpq, Gpq> for Assembler<'_> {
    fn blcs(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Blcs as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlcsEmitter<Gp, Mem> for Assembler<'_> {
    fn blcs(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Blcs as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlcsEmitter<Gpd, Mem> for Assembler<'_> {
    fn blcs(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Blcs as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlcsEmitter<Gpq, Mem> for Assembler<'_> {
    fn blcs(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Blcs as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlsfillEmitter<Gp, Gp> for Assembler<'_> {
    fn blsfill(&mut self, dst: Gp, src: Gp) {
        self.emit_n(
            InstId::Blsfill as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // TBM
    }
}

impl BlsfillEmitter<Gpd, Gpd> for Assembler<'_> {
    fn blsfill(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Blsfill as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // TBM
    }
}

impl BlsfillEmitter<Gpq, Gpq> for Assembler<'_> {
    fn blsfill(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Blsfill as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // TBM
    }
}

impl BlsfillEmitter<Gp, Mem> for Assembler<'_> {
    fn blsfill(&mut self, dst: Gp, src: Mem) {
        self.emit_n(
            InstId::Blsfill as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // TBM
    }
}

impl BlsfillEmitter<Gpd, Mem> for Assembler<'_> {
    fn blsfill(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(
            InstId::Blsfill as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // TBM
    }
}

impl BlsfillEmitter<Gpq, Mem> for Assembler<'_> {
    fn blsfill(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(
            InstId::Blsfill as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // TBM
    }
}

impl BlsicEmitter<Gp, Gp> for Assembler<'_> {
    fn blsic(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Blsic as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlsicEmitter<Gpd, Gpd> for Assembler<'_> {
    fn blsic(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Blsic as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlsicEmitter<Gpq, Gpq> for Assembler<'_> {
    fn blsic(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Blsic as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlsicEmitter<Gp, Mem> for Assembler<'_> {
    fn blsic(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Blsic as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlsicEmitter<Gpd, Mem> for Assembler<'_> {
    fn blsic(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Blsic as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl BlsicEmitter<Gpq, Mem> for Assembler<'_> {
    fn blsic(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Blsic as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl T1mskcEmitter<Gp, Gp> for Assembler<'_> {
    fn t1mskc(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::T1mskc as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl T1mskcEmitter<Gpd, Gpd> for Assembler<'_> {
    fn t1mskc(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::T1mskc as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl T1mskcEmitter<Gpq, Gpq> for Assembler<'_> {
    fn t1mskc(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::T1mskc as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl T1mskcEmitter<Gp, Mem> for Assembler<'_> {
    fn t1mskc(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::T1mskc as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl T1mskcEmitter<Gpd, Mem> for Assembler<'_> {
    fn t1mskc(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::T1mskc as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl T1mskcEmitter<Gpq, Mem> for Assembler<'_> {
    fn t1mskc(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::T1mskc as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl TzmskEmitter<Gp, Gp> for Assembler<'_> {
    fn tzmsk(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Tzmsk as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl TzmskEmitter<Gpd, Gpd> for Assembler<'_> {
    fn tzmsk(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Tzmsk as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl TzmskEmitter<Gpq, Gpq> for Assembler<'_> {
    fn tzmsk(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Tzmsk as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl TzmskEmitter<Gp, Mem> for Assembler<'_> {
    fn tzmsk(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Tzmsk as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl TzmskEmitter<Gpd, Mem> for Assembler<'_> {
    fn tzmsk(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Tzmsk as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl TzmskEmitter<Gpq, Mem> for Assembler<'_> {
    fn tzmsk(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Tzmsk as u32, &[dst.as_operand(), src.as_operand()]); // TBM
    }
}

impl ArplEmitter<Gp, Gp> for Assembler<'_> {
    fn arpl(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Arpl as u32, &[dst.as_operand(), src.as_operand()]); // X86
    }
}

impl ArplEmitter<Gpw, Gpw> for Assembler<'_> {
    fn arpl(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Arpl as u32, &[dst.as_operand(), src.as_operand()]); // X86
    }
}

impl ArplEmitter<Mem, Gp> for Assembler<'_> {
    fn arpl(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Arpl as u32, &[dst.as_operand(), src.as_operand()]); // X86
    }
}

impl ArplEmitter<Mem, Gpw> for Assembler<'_> {
    fn arpl(&mut self, dst: Mem, src: Gpw) {
        self.emit_n(InstId::Arpl as u32, &[dst.as_operand(), src.as_operand()]); // X86
    }
}

impl CliEmitter for Assembler<'_> {
    fn cli(&mut self) {
        self.emit_n(InstId::Cli as u32, &[]); // ANY
    }
}

impl GetsecEmitter for Assembler<'_> {
    fn getsec(&mut self) {
        self.emit_n(InstId::Getsec as u32, &[]); // SMX
    }
}

impl<U0: Into<Imm>> Int_Emitter<U0> for Assembler<'_> {
    fn int_(&mut self, src: U0) {
        self.emit_n(InstId::Int as u32, &[Into::<Imm>::into(src).as_operand()]); // ANY
    }
}

impl Int3Emitter for Assembler<'_> {
    fn int3(&mut self) {
        self.emit_n(InstId::Int3 as u32, &[]); // ANY
    }
}

impl IntoEmitter for Assembler<'_> {
    fn into(&mut self) {
        self.emit_n(InstId::Into as u32, &[]); // ANY
    }
}

impl LarEmitter<Gp, Gp> for Assembler<'_> {
    fn lar(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Lar as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LarEmitter<Gpw, Gpw> for Assembler<'_> {
    fn lar(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Lar as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LarEmitter<Gpd, Gpd> for Assembler<'_> {
    fn lar(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Lar as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LarEmitter<Gp, Mem> for Assembler<'_> {
    fn lar(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Lar as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LarEmitter<Gpw, Mem> for Assembler<'_> {
    fn lar(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Lar as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LarEmitter<Gpd, Mem> for Assembler<'_> {
    fn lar(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Lar as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LdsEmitter<Gp, Mem> for Assembler<'_> {
    fn lds(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Lds as u32, &[dst.as_operand(), src.as_operand()]); // X86
    }
}

impl LdsEmitter<Gpw, Mem> for Assembler<'_> {
    fn lds(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Lds as u32, &[dst.as_operand(), src.as_operand()]); // X86
    }
}

impl LdsEmitter<Gpd, Mem> for Assembler<'_> {
    fn lds(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Lds as u32, &[dst.as_operand(), src.as_operand()]); // X86
    }
}

impl LesEmitter<Gp, Mem> for Assembler<'_> {
    fn les(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Les as u32, &[dst.as_operand(), src.as_operand()]); // X86
    }
}

impl LesEmitter<Gpw, Mem> for Assembler<'_> {
    fn les(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Les as u32, &[dst.as_operand(), src.as_operand()]); // X86
    }
}

impl LesEmitter<Gpd, Mem> for Assembler<'_> {
    fn les(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Les as u32, &[dst.as_operand(), src.as_operand()]); // X86
    }
}

impl LfsEmitter<Gp, Mem> for Assembler<'_> {
    fn lfs(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Lfs as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LfsEmitter<Gpw, Mem> for Assembler<'_> {
    fn lfs(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Lfs as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LfsEmitter<Gpd, Mem> for Assembler<'_> {
    fn lfs(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Lfs as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LfsEmitter<Gpq, Mem> for Assembler<'_> {
    fn lfs(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Lfs as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LgsEmitter<Gp, Mem> for Assembler<'_> {
    fn lgs(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Lgs as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LgsEmitter<Gpw, Mem> for Assembler<'_> {
    fn lgs(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Lgs as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LgsEmitter<Gpd, Mem> for Assembler<'_> {
    fn lgs(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Lgs as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LgsEmitter<Gpq, Mem> for Assembler<'_> {
    fn lgs(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Lgs as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LslEmitter<Gp, Gp> for Assembler<'_> {
    fn lsl(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Lsl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LslEmitter<Gpw, Gpw> for Assembler<'_> {
    fn lsl(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(InstId::Lsl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LslEmitter<Gpd, Gpd> for Assembler<'_> {
    fn lsl(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Lsl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LslEmitter<Gpq, Gpd> for Assembler<'_> {
    fn lsl(&mut self, dst: Gpq, src: Gpd) {
        self.emit_n(InstId::Lsl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LslEmitter<Gp, Mem> for Assembler<'_> {
    fn lsl(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Lsl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LslEmitter<Gpw, Mem> for Assembler<'_> {
    fn lsl(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Lsl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LslEmitter<Gpd, Mem> for Assembler<'_> {
    fn lsl(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Lsl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LslEmitter<Gpq, Mem> for Assembler<'_> {
    fn lsl(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Lsl as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LssEmitter<Gp, Mem> for Assembler<'_> {
    fn lss(&mut self, dst: Gp, src: Mem) {
        self.emit_n(InstId::Lss as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LssEmitter<Gpw, Mem> for Assembler<'_> {
    fn lss(&mut self, dst: Gpw, src: Mem) {
        self.emit_n(InstId::Lss as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LssEmitter<Gpd, Mem> for Assembler<'_> {
    fn lss(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(InstId::Lss as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl LssEmitter<Gpq, Mem> for Assembler<'_> {
    fn lss(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(InstId::Lss as u32, &[dst.as_operand(), src.as_operand()]); // ANY
    }
}

impl PauseEmitter for Assembler<'_> {
    fn pause(&mut self) {
        self.emit_n(InstId::Pause as u32, &[]); // SSE2
    }
}

impl RsmEmitter for Assembler<'_> {
    fn rsm(&mut self) {
        self.emit_n(InstId::Rsm as u32, &[]); // X86
    }
}

impl SgdtEmitter<Mem> for Assembler<'_> {
    fn sgdt(&mut self, dst: Mem) {
        self.emit_n(InstId::Sgdt as u32, &[dst.as_operand()]); // ANY
    }
}

impl SidtEmitter<Mem> for Assembler<'_> {
    fn sidt(&mut self, dst: Mem) {
        self.emit_n(InstId::Sidt as u32, &[dst.as_operand()]); // ANY
    }
}

impl SldtEmitter<Gp> for Assembler<'_> {
    fn sldt(&mut self, dst: Gp) {
        self.emit_n(InstId::Sldt as u32, &[dst.as_operand()]); // ANY
    }
}

impl SldtEmitter<Gpw> for Assembler<'_> {
    fn sldt(&mut self, dst: Gpw) {
        self.emit_n(InstId::Sldt as u32, &[dst.as_operand()]); // ANY
    }
}

impl SldtEmitter<Gpd> for Assembler<'_> {
    fn sldt(&mut self, dst: Gpd) {
        self.emit_n(InstId::Sldt as u32, &[dst.as_operand()]); // ANY
    }
}

impl SldtEmitter<Gpq> for Assembler<'_> {
    fn sldt(&mut self, dst: Gpq) {
        self.emit_n(InstId::Sldt as u32, &[dst.as_operand()]); // ANY
    }
}

impl SldtEmitter<Mem> for Assembler<'_> {
    fn sldt(&mut self, dst: Mem) {
        self.emit_n(InstId::Sldt as u32, &[dst.as_operand()]); // ANY
    }
}

impl SmswEmitter<Gp> for Assembler<'_> {
    fn smsw(&mut self, dst: Gp) {
        self.emit_n(InstId::Smsw as u32, &[dst.as_operand()]); // ANY
    }
}

impl SmswEmitter<Gpw> for Assembler<'_> {
    fn smsw(&mut self, dst: Gpw) {
        self.emit_n(InstId::Smsw as u32, &[dst.as_operand()]); // ANY
    }
}

impl SmswEmitter<Gpd> for Assembler<'_> {
    fn smsw(&mut self, dst: Gpd) {
        self.emit_n(InstId::Smsw as u32, &[dst.as_operand()]); // ANY
    }
}

impl SmswEmitter<Gpq> for Assembler<'_> {
    fn smsw(&mut self, dst: Gpq) {
        self.emit_n(InstId::Smsw as u32, &[dst.as_operand()]); // ANY
    }
}

impl SmswEmitter<Mem> for Assembler<'_> {
    fn smsw(&mut self, dst: Mem) {
        self.emit_n(InstId::Smsw as u32, &[dst.as_operand()]); // ANY
    }
}

impl StiEmitter for Assembler<'_> {
    fn sti(&mut self) {
        self.emit_n(InstId::Sti as u32, &[]); // ANY
    }
}

impl StrEmitter<Gp> for Assembler<'_> {
    fn str(&mut self, dst: Gp) {
        self.emit_n(InstId::Str as u32, &[dst.as_operand()]); // ANY
    }
}

impl StrEmitter<Gpw> for Assembler<'_> {
    fn str(&mut self, dst: Gpw) {
        self.emit_n(InstId::Str as u32, &[dst.as_operand()]); // ANY
    }
}

impl StrEmitter<Gpd> for Assembler<'_> {
    fn str(&mut self, dst: Gpd) {
        self.emit_n(InstId::Str as u32, &[dst.as_operand()]); // ANY
    }
}

impl StrEmitter<Gpq> for Assembler<'_> {
    fn str(&mut self, dst: Gpq) {
        self.emit_n(InstId::Str as u32, &[dst.as_operand()]); // ANY
    }
}

impl StrEmitter<Mem> for Assembler<'_> {
    fn str(&mut self, dst: Mem) {
        self.emit_n(InstId::Str as u32, &[dst.as_operand()]); // ANY
    }
}

impl VerrEmitter<Gp> for Assembler<'_> {
    fn verr(&mut self, src: Gp) {
        self.emit_n(InstId::Verr as u32, &[src.as_operand()]); // ANY
    }
}

impl VerrEmitter<Gpw> for Assembler<'_> {
    fn verr(&mut self, src: Gpw) {
        self.emit_n(InstId::Verr as u32, &[src.as_operand()]); // ANY
    }
}

impl VerrEmitter<Mem> for Assembler<'_> {
    fn verr(&mut self, mem: Mem) {
        self.emit_n(InstId::Verr as u32, &[mem.as_operand()]); // ANY
    }
}

impl VerwEmitter<Gp> for Assembler<'_> {
    fn verw(&mut self, src: Gp) {
        self.emit_n(InstId::Verw as u32, &[src.as_operand()]); // ANY
    }
}

impl VerwEmitter<Gpw> for Assembler<'_> {
    fn verw(&mut self, src: Gpw) {
        self.emit_n(InstId::Verw as u32, &[src.as_operand()]); // ANY
    }
}

impl VerwEmitter<Mem> for Assembler<'_> {
    fn verw(&mut self, mem: Mem) {
        self.emit_n(InstId::Verw as u32, &[mem.as_operand()]); // ANY
    }
}

impl RdfsbaseEmitter<Gp> for Assembler<'_> {
    fn rdfsbase(&mut self, dst: Gp) {
        self.emit_n(InstId::Rdfsbase as u32, &[dst.as_operand()]); // FSGSBASE
    }
}

impl RdfsbaseEmitter<Gpd> for Assembler<'_> {
    fn rdfsbase(&mut self, dst: Gpd) {
        self.emit_n(InstId::Rdfsbase as u32, &[dst.as_operand()]); // FSGSBASE
    }
}

impl RdfsbaseEmitter<Gpq> for Assembler<'_> {
    fn rdfsbase(&mut self, dst: Gpq) {
        self.emit_n(InstId::Rdfsbase as u32, &[dst.as_operand()]); // FSGSBASE
    }
}

impl RdgsbaseEmitter<Gp> for Assembler<'_> {
    fn rdgsbase(&mut self, dst: Gp) {
        self.emit_n(InstId::Rdgsbase as u32, &[dst.as_operand()]); // FSGSBASE
    }
}

impl RdgsbaseEmitter<Gpd> for Assembler<'_> {
    fn rdgsbase(&mut self, dst: Gpd) {
        self.emit_n(InstId::Rdgsbase as u32, &[dst.as_operand()]); // FSGSBASE
    }
}

impl RdgsbaseEmitter<Gpq> for Assembler<'_> {
    fn rdgsbase(&mut self, dst: Gpq) {
        self.emit_n(InstId::Rdgsbase as u32, &[dst.as_operand()]); // FSGSBASE
    }
}

impl WrfsbaseEmitter<Gp> for Assembler<'_> {
    fn wrfsbase(&mut self, src: Gp) {
        self.emit_n(InstId::Wrfsbase as u32, &[src.as_operand()]); // FSGSBASE
    }
}

impl WrfsbaseEmitter<Gpd> for Assembler<'_> {
    fn wrfsbase(&mut self, src: Gpd) {
        self.emit_n(InstId::Wrfsbase as u32, &[src.as_operand()]); // FSGSBASE
    }
}

impl WrfsbaseEmitter<Gpq> for Assembler<'_> {
    fn wrfsbase(&mut self, src: Gpq) {
        self.emit_n(InstId::Wrfsbase as u32, &[src.as_operand()]); // FSGSBASE
    }
}

impl WrgsbaseEmitter<Gp> for Assembler<'_> {
    fn wrgsbase(&mut self, src: Gp) {
        self.emit_n(InstId::Wrgsbase as u32, &[src.as_operand()]); // FSGSBASE
    }
}

impl WrgsbaseEmitter<Gpd> for Assembler<'_> {
    fn wrgsbase(&mut self, src: Gpd) {
        self.emit_n(InstId::Wrgsbase as u32, &[src.as_operand()]); // FSGSBASE
    }
}

impl WrgsbaseEmitter<Gpq> for Assembler<'_> {
    fn wrgsbase(&mut self, src: Gpq) {
        self.emit_n(InstId::Wrgsbase as u32, &[src.as_operand()]); // FSGSBASE
    }
}

impl FxrstorEmitter<Mem> for Assembler<'_> {
    fn fxrstor(&mut self, mem: Mem) {
        self.emit_n(InstId::Fxrstor as u32, &[mem.as_operand()]); // FXSR
    }
}

impl Fxrstor64Emitter<Mem> for Assembler<'_> {
    fn fxrstor64(&mut self, mem: Mem) {
        self.emit_n(InstId::Fxrstor64 as u32, &[mem.as_operand()]); // FXSR
    }
}

impl FxsaveEmitter<Mem> for Assembler<'_> {
    fn fxsave(&mut self, dst: Mem) {
        self.emit_n(InstId::Fxsave as u32, &[dst.as_operand()]); // FXSR
    }
}

impl Fxsave64Emitter<Mem> for Assembler<'_> {
    fn fxsave64(&mut self, dst: Mem) {
        self.emit_n(InstId::Fxsave64 as u32, &[dst.as_operand()]); // FXSR
    }
}

impl XgetbvEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn xgetbv(&mut self, dst: Gp, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Xgetbv as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // XSAVE [EXPLICIT] EDX:EAX <- XCR[ECX]
    }
}

impl XgetbvEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn xgetbv(&mut self, dst: Gpd, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Xgetbv as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // XSAVE [EXPLICIT] EDX:EAX <- XCR[ECX]
    }
}

impl XrstorEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xrstor(&mut self, dst: Mem, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Xrstor as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl XrstorEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xrstor(&mut self, dst: Mem, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Xrstor as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl Xrstor64Emitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xrstor64(&mut self, dst: Mem, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Xrstor64 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl Xrstor64Emitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xrstor64(&mut self, dst: Mem, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Xrstor64 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl XrstorsEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xrstors(&mut self, dst: Mem, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Xrstors as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl XrstorsEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xrstors(&mut self, dst: Mem, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Xrstors as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl Xrstors64Emitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xrstors64(&mut self, dst: Mem, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Xrstors64 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl Xrstors64Emitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xrstors64(&mut self, dst: Mem, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Xrstors64 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl XsaveEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xsave(&mut self, dst: Mem, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Xsave as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl XsaveEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xsave(&mut self, dst: Mem, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Xsave as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl Xsave64Emitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xsave64(&mut self, dst: Mem, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Xsave64 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl Xsave64Emitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xsave64(&mut self, dst: Mem, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Xsave64 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl XsavecEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xsavec(&mut self, dst: Mem, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Xsavec as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl XsavecEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xsavec(&mut self, dst: Mem, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Xsavec as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl Xsavec64Emitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xsavec64(&mut self, dst: Mem, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Xsavec64 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl Xsavec64Emitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xsavec64(&mut self, dst: Mem, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Xsavec64 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl XsaveoptEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xsaveopt(&mut self, dst: Mem, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Xsaveopt as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl XsaveoptEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xsaveopt(&mut self, dst: Mem, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Xsaveopt as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl Xsaveopt64Emitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xsaveopt64(&mut self, dst: Mem, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Xsaveopt64 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl Xsaveopt64Emitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xsaveopt64(&mut self, dst: Mem, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Xsaveopt64 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl XsavesEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xsaves(&mut self, dst: Mem, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Xsaves as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl XsavesEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xsaves(&mut self, dst: Mem, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Xsaves as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl Xsaves64Emitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xsaves64(&mut self, dst: Mem, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Xsaves64 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl Xsaves64Emitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xsaves64(&mut self, dst: Mem, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Xsaves64 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl BndclEmitter<Bnd, Gp> for Assembler<'_> {
    fn bndcl(&mut self, lhs: Bnd, rhs: Gp) {
        self.emit_n(InstId::Bndcl as u32, &[lhs.as_operand(), rhs.as_operand()]); // MPX
    }
}

impl BndclEmitter<Bnd, Gpd> for Assembler<'_> {
    fn bndcl(&mut self, lhs: Bnd, rhs: Gpd) {
        self.emit_n(InstId::Bndcl as u32, &[lhs.as_operand(), rhs.as_operand()]); // MPX
    }
}

impl BndclEmitter<Bnd, Gpq> for Assembler<'_> {
    fn bndcl(&mut self, lhs: Bnd, rhs: Gpq) {
        self.emit_n(InstId::Bndcl as u32, &[lhs.as_operand(), rhs.as_operand()]); // MPX
    }
}

impl BndclEmitter<Bnd, Mem> for Assembler<'_> {
    fn bndcl(&mut self, lhs: Bnd, rhs: Mem) {
        self.emit_n(InstId::Bndcl as u32, &[lhs.as_operand(), rhs.as_operand()]); // MPX
    }
}

impl BndcnEmitter<Bnd, Gp> for Assembler<'_> {
    fn bndcn(&mut self, lhs: Bnd, rhs: Gp) {
        self.emit_n(InstId::Bndcn as u32, &[lhs.as_operand(), rhs.as_operand()]); // MPX
    }
}

impl BndcnEmitter<Bnd, Gpd> for Assembler<'_> {
    fn bndcn(&mut self, lhs: Bnd, rhs: Gpd) {
        self.emit_n(InstId::Bndcn as u32, &[lhs.as_operand(), rhs.as_operand()]); // MPX
    }
}

impl BndcnEmitter<Bnd, Gpq> for Assembler<'_> {
    fn bndcn(&mut self, lhs: Bnd, rhs: Gpq) {
        self.emit_n(InstId::Bndcn as u32, &[lhs.as_operand(), rhs.as_operand()]); // MPX
    }
}

impl BndcnEmitter<Bnd, Mem> for Assembler<'_> {
    fn bndcn(&mut self, lhs: Bnd, rhs: Mem) {
        self.emit_n(InstId::Bndcn as u32, &[lhs.as_operand(), rhs.as_operand()]); // MPX
    }
}

impl BndcuEmitter<Bnd, Gp> for Assembler<'_> {
    fn bndcu(&mut self, lhs: Bnd, rhs: Gp) {
        self.emit_n(InstId::Bndcu as u32, &[lhs.as_operand(), rhs.as_operand()]); // MPX
    }
}

impl BndcuEmitter<Bnd, Gpd> for Assembler<'_> {
    fn bndcu(&mut self, lhs: Bnd, rhs: Gpd) {
        self.emit_n(InstId::Bndcu as u32, &[lhs.as_operand(), rhs.as_operand()]); // MPX
    }
}

impl BndcuEmitter<Bnd, Gpq> for Assembler<'_> {
    fn bndcu(&mut self, lhs: Bnd, rhs: Gpq) {
        self.emit_n(InstId::Bndcu as u32, &[lhs.as_operand(), rhs.as_operand()]); // MPX
    }
}

impl BndcuEmitter<Bnd, Mem> for Assembler<'_> {
    fn bndcu(&mut self, lhs: Bnd, rhs: Mem) {
        self.emit_n(InstId::Bndcu as u32, &[lhs.as_operand(), rhs.as_operand()]); // MPX
    }
}

impl BndldxEmitter<Bnd, Mem> for Assembler<'_> {
    fn bndldx(&mut self, dst: Bnd, src: Mem) {
        self.emit_n(InstId::Bndldx as u32, &[dst.as_operand(), src.as_operand()]); // MPX
    }
}

impl BndmkEmitter<Bnd, Mem> for Assembler<'_> {
    fn bndmk(&mut self, dst: Bnd, src: Mem) {
        self.emit_n(InstId::Bndmk as u32, &[dst.as_operand(), src.as_operand()]); // MPX
    }
}

impl BndmovEmitter<Bnd, Bnd> for Assembler<'_> {
    fn bndmov(&mut self, dst: Bnd, src: Bnd) {
        self.emit_n(InstId::Bndmov as u32, &[dst.as_operand(), src.as_operand()]); // MPX
    }
}

impl BndmovEmitter<Bnd, Mem> for Assembler<'_> {
    fn bndmov(&mut self, dst: Bnd, src: Mem) {
        self.emit_n(InstId::Bndmov as u32, &[dst.as_operand(), src.as_operand()]); // MPX
    }
}

impl BndmovEmitter<Mem, Bnd> for Assembler<'_> {
    fn bndmov(&mut self, dst: Mem, src: Bnd) {
        self.emit_n(InstId::Bndmov as u32, &[dst.as_operand(), src.as_operand()]); // MPX
    }
}

impl BndstxEmitter<Mem, Bnd> for Assembler<'_> {
    fn bndstx(&mut self, dst: Mem, src: Bnd) {
        self.emit_n(InstId::Bndstx as u32, &[dst.as_operand(), src.as_operand()]); // MPX
    }
}

impl MonitorxEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn monitorx(&mut self, dst: Mem, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Monitorx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // MONITORX
    }
}

impl MonitorxEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn monitorx(&mut self, dst: Mem, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Monitorx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // MONITORX
    }
}

impl MwaitxEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn mwaitx(&mut self, dst: Gp, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Mwaitx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // MONITORX
    }
}

impl MwaitxEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn mwaitx(&mut self, dst: Gpd, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Mwaitx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // MONITORX
    }
}

impl McommitEmitter for Assembler<'_> {
    fn mcommit(&mut self) {
        self.emit_n(InstId::Mcommit as u32, &[]); // MCOMMIT
    }
}

impl PtwriteEmitter<Gp> for Assembler<'_> {
    fn ptwrite(&mut self, src: Gp) {
        self.emit_n(InstId::Ptwrite as u32, &[src.as_operand()]); // PTWRITE
    }
}

impl PtwriteEmitter<Gpd> for Assembler<'_> {
    fn ptwrite(&mut self, src: Gpd) {
        self.emit_n(InstId::Ptwrite as u32, &[src.as_operand()]); // PTWRITE
    }
}

impl PtwriteEmitter<Gpq> for Assembler<'_> {
    fn ptwrite(&mut self, src: Gpq) {
        self.emit_n(InstId::Ptwrite as u32, &[src.as_operand()]); // PTWRITE
    }
}

impl PtwriteEmitter<Mem> for Assembler<'_> {
    fn ptwrite(&mut self, mem: Mem) {
        self.emit_n(InstId::Ptwrite as u32, &[mem.as_operand()]); // PTWRITE
    }
}

impl EnqcmdEmitter<Mem, Mem> for Assembler<'_> {
    fn enqcmd(&mut self, dst: Mem, src: Mem) {
        self.emit_n(InstId::Enqcmd as u32, &[dst.as_operand(), src.as_operand()]); // ENQCMD
    }
}

impl EnqcmdsEmitter<Mem, Mem> for Assembler<'_> {
    fn enqcmds(&mut self, dst: Mem, src: Mem) {
        self.emit_n(
            InstId::Enqcmds as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // ENQCMD
    }
}

impl TpauseEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn tpause(&mut self, dst: Gp, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Tpause as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // WAITPKG
    }
}

impl TpauseEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn tpause(&mut self, dst: Gpd, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Tpause as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // WAITPKG
    }
}

impl UmonitorEmitter<Mem> for Assembler<'_> {
    fn umonitor(&mut self, mem: Mem) {
        self.emit_n(InstId::Umonitor as u32, &[mem.as_operand()]); // WAITPKG
    }
}

impl UmwaitEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn umwait(&mut self, dst: Gp, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Umwait as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // WAITPKG
    }
}

impl UmwaitEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn umwait(&mut self, dst: Gpd, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Umwait as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // WAITPKG
    }
}

impl RdrandEmitter<Gp> for Assembler<'_> {
    fn rdrand(&mut self, dst: Gp) {
        self.emit_n(InstId::Rdrand as u32, &[dst.as_operand()]); // RDRAND
    }
}

impl RdrandEmitter<Gpw> for Assembler<'_> {
    fn rdrand(&mut self, dst: Gpw) {
        self.emit_n(InstId::Rdrand as u32, &[dst.as_operand()]); // RDRAND
    }
}

impl RdrandEmitter<Gpd> for Assembler<'_> {
    fn rdrand(&mut self, dst: Gpd) {
        self.emit_n(InstId::Rdrand as u32, &[dst.as_operand()]); // RDRAND
    }
}

impl RdrandEmitter<Gpq> for Assembler<'_> {
    fn rdrand(&mut self, dst: Gpq) {
        self.emit_n(InstId::Rdrand as u32, &[dst.as_operand()]); // RDRAND
    }
}

impl RdseedEmitter<Gp> for Assembler<'_> {
    fn rdseed(&mut self, dst: Gp) {
        self.emit_n(InstId::Rdseed as u32, &[dst.as_operand()]); // RDSEED
    }
}

impl RdseedEmitter<Gpw> for Assembler<'_> {
    fn rdseed(&mut self, dst: Gpw) {
        self.emit_n(InstId::Rdseed as u32, &[dst.as_operand()]); // RDSEED
    }
}

impl RdseedEmitter<Gpd> for Assembler<'_> {
    fn rdseed(&mut self, dst: Gpd) {
        self.emit_n(InstId::Rdseed as u32, &[dst.as_operand()]); // RDSEED
    }
}

impl RdseedEmitter<Gpq> for Assembler<'_> {
    fn rdseed(&mut self, dst: Gpq) {
        self.emit_n(InstId::Rdseed as u32, &[dst.as_operand()]); // RDSEED
    }
}

impl LlwpcbEmitter<Gp> for Assembler<'_> {
    fn llwpcb(&mut self, src: Gp) {
        self.emit_n(InstId::Llwpcb as u32, &[src.as_operand()]); // LWP
    }
}

impl LlwpcbEmitter<Gpd> for Assembler<'_> {
    fn llwpcb(&mut self, src: Gpd) {
        self.emit_n(InstId::Llwpcb as u32, &[src.as_operand()]); // LWP
    }
}

impl LlwpcbEmitter<Gpq> for Assembler<'_> {
    fn llwpcb(&mut self, src: Gpq) {
        self.emit_n(InstId::Llwpcb as u32, &[src.as_operand()]); // LWP
    }
}

impl LwpinsEmitter<Gp, Gp, Imm> for Assembler<'_> {
    fn lwpins(&mut self, dst: Gp, src: Gp, imm: Imm) {
        self.emit_n(
            InstId::Lwpins as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // LWP
    }
}

impl<U2: Into<Imm>> LwpinsEmitter<Gpd, Gpd, U2> for Assembler<'_> {
    fn lwpins(&mut self, dst: Gpd, src1: Gpd, src2: U2) {
        self.emit_n(
            InstId::Lwpins as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // LWP
    }
}

impl<U2: Into<Imm>> LwpinsEmitter<Gpq, Gpd, U2> for Assembler<'_> {
    fn lwpins(&mut self, dst: Gpq, src1: Gpd, src2: U2) {
        self.emit_n(
            InstId::Lwpins as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // LWP
    }
}

impl LwpinsEmitter<Gp, Mem, Imm> for Assembler<'_> {
    fn lwpins(&mut self, dst: Gp, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Lwpins as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // LWP
    }
}

impl<U2: Into<Imm>> LwpinsEmitter<Gpd, Mem, U2> for Assembler<'_> {
    fn lwpins(&mut self, dst: Gpd, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Lwpins as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // LWP
    }
}

impl<U2: Into<Imm>> LwpinsEmitter<Gpq, Mem, U2> for Assembler<'_> {
    fn lwpins(&mut self, dst: Gpq, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Lwpins as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // LWP
    }
}

impl LwpvalEmitter<Gp, Gp, Imm> for Assembler<'_> {
    fn lwpval(&mut self, dst: Gp, src: Gp, imm: Imm) {
        self.emit_n(
            InstId::Lwpval as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // LWP
    }
}

impl<U2: Into<Imm>> LwpvalEmitter<Gpd, Gpd, U2> for Assembler<'_> {
    fn lwpval(&mut self, dst: Gpd, src1: Gpd, src2: U2) {
        self.emit_n(
            InstId::Lwpval as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // LWP
    }
}

impl<U2: Into<Imm>> LwpvalEmitter<Gpq, Gpd, U2> for Assembler<'_> {
    fn lwpval(&mut self, dst: Gpq, src1: Gpd, src2: U2) {
        self.emit_n(
            InstId::Lwpval as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // LWP
    }
}

impl LwpvalEmitter<Gp, Mem, Imm> for Assembler<'_> {
    fn lwpval(&mut self, dst: Gp, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Lwpval as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // LWP
    }
}

impl<U2: Into<Imm>> LwpvalEmitter<Gpd, Mem, U2> for Assembler<'_> {
    fn lwpval(&mut self, dst: Gpd, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Lwpval as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // LWP
    }
}

impl<U2: Into<Imm>> LwpvalEmitter<Gpq, Mem, U2> for Assembler<'_> {
    fn lwpval(&mut self, dst: Gpq, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Lwpval as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // LWP
    }
}

impl SlwpcbEmitter<Gp> for Assembler<'_> {
    fn slwpcb(&mut self, dst: Gp) {
        self.emit_n(InstId::Slwpcb as u32, &[dst.as_operand()]); // LWP
    }
}

impl SlwpcbEmitter<Gpd> for Assembler<'_> {
    fn slwpcb(&mut self, dst: Gpd) {
        self.emit_n(InstId::Slwpcb as u32, &[dst.as_operand()]); // LWP
    }
}

impl SlwpcbEmitter<Gpq> for Assembler<'_> {
    fn slwpcb(&mut self, dst: Gpq) {
        self.emit_n(InstId::Slwpcb as u32, &[dst.as_operand()]); // LWP
    }
}

impl<U0: Into<Imm>> XabortEmitter<U0> for Assembler<'_> {
    fn xabort(&mut self, src: U0) {
        self.emit_n(
            InstId::Xabort as u32,
            &[Into::<Imm>::into(src).as_operand()],
        ); // RTM
    }
}

impl XbeginEmitter<Label> for Assembler<'_> {
    fn xbegin(&mut self, target: Label) {
        self.emit_n(InstId::Xbegin as u32, &[target.as_operand()]); // RTM
    }
}

impl<U0: Into<Imm>> XbeginEmitter<U0> for Assembler<'_> {
    fn xbegin(&mut self, src: U0) {
        self.emit_n(
            InstId::Xbegin as u32,
            &[Into::<Imm>::into(src).as_operand()],
        ); // RTM
    }
}

impl XendEmitter for Assembler<'_> {
    fn xend(&mut self) {
        self.emit_n(InstId::Xend as u32, &[]); // RTM
    }
}

impl XtestEmitter for Assembler<'_> {
    fn xtest(&mut self) {
        self.emit_n(InstId::Xtest as u32, &[]); // TSX
    }
}

impl XresldtrkEmitter for Assembler<'_> {
    fn xresldtrk(&mut self) {
        self.emit_n(InstId::Xresldtrk as u32, &[]); // TSXLDTRK
    }
}

impl XsusldtrkEmitter for Assembler<'_> {
    fn xsusldtrk(&mut self) {
        self.emit_n(InstId::Xsusldtrk as u32, &[]); // TSXLDTRK
    }
}

impl Endbr32Emitter for Assembler<'_> {
    fn endbr32(&mut self) {
        self.emit_n(InstId::Endbr32 as u32, &[]); // CET_IBT
    }
}

impl Endbr64Emitter for Assembler<'_> {
    fn endbr64(&mut self) {
        self.emit_n(InstId::Endbr64 as u32, &[]); // CET_IBT
    }
}

impl ClrssbsyEmitter<Mem> for Assembler<'_> {
    fn clrssbsy(&mut self, mem: Mem) {
        self.emit_n(InstId::Clrssbsy as u32, &[mem.as_operand()]); // CET_SS
    }
}

impl SetssbsyEmitter for Assembler<'_> {
    fn setssbsy(&mut self) {
        self.emit_n(InstId::Setssbsy as u32, &[]); // CET_SS
    }
}

impl RstorsspEmitter<Mem> for Assembler<'_> {
    fn rstorssp(&mut self, mem: Mem) {
        self.emit_n(InstId::Rstorssp as u32, &[mem.as_operand()]); // CET_SS
    }
}

impl SaveprevsspEmitter for Assembler<'_> {
    fn saveprevssp(&mut self) {
        self.emit_n(InstId::Saveprevssp as u32, &[]); // CET_SS
    }
}

impl IncsspdEmitter<Gp> for Assembler<'_> {
    fn incsspd(&mut self, src: Gp) {
        self.emit_n(InstId::Incsspd as u32, &[src.as_operand()]); // CET_SS
    }
}

impl IncsspdEmitter<Gpd> for Assembler<'_> {
    fn incsspd(&mut self, src: Gpd) {
        self.emit_n(InstId::Incsspd as u32, &[src.as_operand()]); // CET_SS
    }
}

impl IncsspqEmitter<Gp> for Assembler<'_> {
    fn incsspq(&mut self, src: Gp) {
        self.emit_n(InstId::Incsspq as u32, &[src.as_operand()]); // CET_SS
    }
}

impl IncsspqEmitter<Gpq> for Assembler<'_> {
    fn incsspq(&mut self, src: Gpq) {
        self.emit_n(InstId::Incsspq as u32, &[src.as_operand()]); // CET_SS
    }
}

impl RdsspdEmitter<Gp> for Assembler<'_> {
    fn rdsspd(&mut self, dst: Gp) {
        self.emit_n(InstId::Rdsspd as u32, &[dst.as_operand()]); // CET_SS
    }
}

impl RdsspdEmitter<Gpd> for Assembler<'_> {
    fn rdsspd(&mut self, dst: Gpd) {
        self.emit_n(InstId::Rdsspd as u32, &[dst.as_operand()]); // CET_SS
    }
}

impl RdsspqEmitter<Gp> for Assembler<'_> {
    fn rdsspq(&mut self, dst: Gp) {
        self.emit_n(InstId::Rdsspq as u32, &[dst.as_operand()]); // CET_SS
    }
}

impl RdsspqEmitter<Gpq> for Assembler<'_> {
    fn rdsspq(&mut self, dst: Gpq) {
        self.emit_n(InstId::Rdsspq as u32, &[dst.as_operand()]); // CET_SS
    }
}

impl WrssdEmitter<Gp, Gp> for Assembler<'_> {
    fn wrssd(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Wrssd as u32, &[dst.as_operand(), src.as_operand()]); // CET_SS
    }
}

impl WrssdEmitter<Mem, Gp> for Assembler<'_> {
    fn wrssd(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Wrssd as u32, &[dst.as_operand(), src.as_operand()]); // CET_SS
    }
}

impl WrssdEmitter<Mem, Gpd> for Assembler<'_> {
    fn wrssd(&mut self, dst: Mem, src: Gpd) {
        self.emit_n(InstId::Wrssd as u32, &[dst.as_operand(), src.as_operand()]); // CET_SS
    }
}

impl WrssqEmitter<Gp, Gp> for Assembler<'_> {
    fn wrssq(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Wrssq as u32, &[dst.as_operand(), src.as_operand()]); // CET_SS
    }
}

impl WrssqEmitter<Mem, Gp> for Assembler<'_> {
    fn wrssq(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Wrssq as u32, &[dst.as_operand(), src.as_operand()]); // CET_SS
    }
}

impl WrssqEmitter<Mem, Gpq> for Assembler<'_> {
    fn wrssq(&mut self, dst: Mem, src: Gpq) {
        self.emit_n(InstId::Wrssq as u32, &[dst.as_operand(), src.as_operand()]); // CET_SS
    }
}

impl WrussdEmitter<Gp, Gp> for Assembler<'_> {
    fn wrussd(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Wrussd as u32, &[dst.as_operand(), src.as_operand()]); // CET_SS
    }
}

impl WrussdEmitter<Mem, Gp> for Assembler<'_> {
    fn wrussd(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Wrussd as u32, &[dst.as_operand(), src.as_operand()]); // CET_SS
    }
}

impl WrussdEmitter<Mem, Gpd> for Assembler<'_> {
    fn wrussd(&mut self, dst: Mem, src: Gpd) {
        self.emit_n(InstId::Wrussd as u32, &[dst.as_operand(), src.as_operand()]); // CET_SS
    }
}

impl WrussqEmitter<Gp, Gp> for Assembler<'_> {
    fn wrussq(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Wrussq as u32, &[dst.as_operand(), src.as_operand()]); // CET_SS
    }
}

impl WrussqEmitter<Mem, Gp> for Assembler<'_> {
    fn wrussq(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Wrussq as u32, &[dst.as_operand(), src.as_operand()]); // CET_SS
    }
}

impl WrussqEmitter<Mem, Gpq> for Assembler<'_> {
    fn wrussq(&mut self, dst: Mem, src: Gpq) {
        self.emit_n(InstId::Wrussq as u32, &[dst.as_operand(), src.as_operand()]); // CET_SS
    }
}

impl HresetEmitter<Imm, Gp> for Assembler<'_> {
    fn hreset(&mut self, imm: Imm, dst: Gp) {
        self.emit_n(InstId::Hreset as u32, &[imm.as_operand(), dst.as_operand()]); // HRESET
    }
}

impl<U0: Into<Imm>> HresetEmitter<U0, Gpd> for Assembler<'_> {
    fn hreset(&mut self, dst: U0, dst2: Gpd) {
        self.emit_n(
            InstId::Hreset as u32,
            &[Into::<Imm>::into(dst).as_operand(), dst2.as_operand()],
        ); // HRESET
    }
}

impl CluiEmitter for Assembler<'_> {
    fn clui(&mut self) {
        self.emit_n(InstId::Clui as u32, &[]); // UINTR
    }
}

impl SenduipiEmitter<Gp> for Assembler<'_> {
    fn senduipi(&mut self, src: Gp) {
        self.emit_n(InstId::Senduipi as u32, &[src.as_operand()]); // UINTR
    }
}

impl SenduipiEmitter<Gpq> for Assembler<'_> {
    fn senduipi(&mut self, src: Gpq) {
        self.emit_n(InstId::Senduipi as u32, &[src.as_operand()]); // UINTR
    }
}

impl TestuiEmitter for Assembler<'_> {
    fn testui(&mut self) {
        self.emit_n(InstId::Testui as u32, &[]); // UINTR
    }
}

impl StuiEmitter for Assembler<'_> {
    fn stui(&mut self) {
        self.emit_n(InstId::Stui as u32, &[]); // UINTR
    }
}

impl UiretEmitter for Assembler<'_> {
    fn uiret(&mut self) {
        self.emit_n(InstId::Uiret as u32, &[]); // UINTR
    }
}

impl CltsEmitter for Assembler<'_> {
    fn clts(&mut self) {
        self.emit_n(InstId::Clts as u32, &[]); // ANY
    }
}

impl HltEmitter for Assembler<'_> {
    fn hlt(&mut self) {
        self.emit_n(InstId::Hlt as u32, &[]); // ANY
    }
}

impl InvdEmitter for Assembler<'_> {
    fn invd(&mut self) {
        self.emit_n(InstId::Invd as u32, &[]); // ANY
    }
}

impl InvlpgEmitter<Mem> for Assembler<'_> {
    fn invlpg(&mut self, mem: Mem) {
        self.emit_n(InstId::Invlpg as u32, &[mem.as_operand()]); // ANY
    }
}

impl InvpcidEmitter<Gp, Mem> for Assembler<'_> {
    fn invpcid(&mut self, lhs: Gp, rhs: Mem) {
        self.emit_n(
            InstId::Invpcid as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // ANY
    }
}

impl InvpcidEmitter<Gpd, Mem> for Assembler<'_> {
    fn invpcid(&mut self, lhs: Gpd, rhs: Mem) {
        self.emit_n(
            InstId::Invpcid as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // ANY
    }
}

impl InvpcidEmitter<Gpq, Mem> for Assembler<'_> {
    fn invpcid(&mut self, lhs: Gpq, rhs: Mem) {
        self.emit_n(
            InstId::Invpcid as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // ANY
    }
}

impl LgdtEmitter<Mem> for Assembler<'_> {
    fn lgdt(&mut self, mem: Mem) {
        self.emit_n(InstId::Lgdt as u32, &[mem.as_operand()]); // ANY
    }
}

impl LidtEmitter<Mem> for Assembler<'_> {
    fn lidt(&mut self, mem: Mem) {
        self.emit_n(InstId::Lidt as u32, &[mem.as_operand()]); // ANY
    }
}

impl LldtEmitter<Gp> for Assembler<'_> {
    fn lldt(&mut self, src: Gp) {
        self.emit_n(InstId::Lldt as u32, &[src.as_operand()]); // ANY
    }
}

impl LldtEmitter<Gpw> for Assembler<'_> {
    fn lldt(&mut self, src: Gpw) {
        self.emit_n(InstId::Lldt as u32, &[src.as_operand()]); // ANY
    }
}

impl LldtEmitter<Mem> for Assembler<'_> {
    fn lldt(&mut self, mem: Mem) {
        self.emit_n(InstId::Lldt as u32, &[mem.as_operand()]); // ANY
    }
}

impl LmswEmitter<Gp> for Assembler<'_> {
    fn lmsw(&mut self, src: Gp) {
        self.emit_n(InstId::Lmsw as u32, &[src.as_operand()]); // ANY
    }
}

impl LmswEmitter<Gpw> for Assembler<'_> {
    fn lmsw(&mut self, src: Gpw) {
        self.emit_n(InstId::Lmsw as u32, &[src.as_operand()]); // ANY
    }
}

impl LmswEmitter<Mem> for Assembler<'_> {
    fn lmsw(&mut self, mem: Mem) {
        self.emit_n(InstId::Lmsw as u32, &[mem.as_operand()]); // ANY
    }
}

impl LtrEmitter<Gp> for Assembler<'_> {
    fn ltr(&mut self, src: Gp) {
        self.emit_n(InstId::Ltr as u32, &[src.as_operand()]); // ANY
    }
}

impl LtrEmitter<Gpw> for Assembler<'_> {
    fn ltr(&mut self, src: Gpw) {
        self.emit_n(InstId::Ltr as u32, &[src.as_operand()]); // ANY
    }
}

impl LtrEmitter<Mem> for Assembler<'_> {
    fn ltr(&mut self, mem: Mem) {
        self.emit_n(InstId::Ltr as u32, &[mem.as_operand()]); // ANY
    }
}

impl RdmsrEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn rdmsr(&mut self, dst: Gp, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Rdmsr as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // MSR [EXPLICIT] RDX:EAX <- MSR[ECX]
    }
}

impl RdmsrEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn rdmsr(&mut self, dst: Gpd, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Rdmsr as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // MSR [EXPLICIT] RDX:EAX <- MSR[ECX]
    }
}

impl RdpmcEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn rdpmc(&mut self, dst: Gp, dst2: Gp, src: Gp) {
        self.emit_n(
            InstId::Rdpmc as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] RDX:EAX <- PMC[ECX]
    }
}

impl RdpmcEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn rdpmc(&mut self, dst: Gpd, dst2: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Rdpmc as u32,
            &[dst.as_operand(), dst2.as_operand(), src.as_operand()],
        ); // ANY [EXPLICIT] RDX:EAX <- PMC[ECX]
    }
}

impl SwapgsEmitter for Assembler<'_> {
    fn swapgs(&mut self) {
        self.emit_n(InstId::Swapgs as u32, &[]); // X64
    }
}

impl WbinvdEmitter for Assembler<'_> {
    fn wbinvd(&mut self) {
        self.emit_n(InstId::Wbinvd as u32, &[]); // ANY
    }
}

impl WbnoinvdEmitter for Assembler<'_> {
    fn wbnoinvd(&mut self) {
        self.emit_n(InstId::Wbnoinvd as u32, &[]); // WBNOINVD
    }
}

impl WrmsrEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn wrmsr(&mut self, dst: Gp, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Wrmsr as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // MSR [EXPLICIT] RDX:EAX -> MSR[ECX]
    }
}

impl WrmsrEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn wrmsr(&mut self, dst: Gpd, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Wrmsr as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // MSR [EXPLICIT] RDX:EAX -> MSR[ECX]
    }
}

impl XsetbvEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn xsetbv(&mut self, dst: Gp, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Xsetbv as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE [EXPLICIT] XCR[ECX] <- EDX:EAX
    }
}

impl XsetbvEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn xsetbv(&mut self, dst: Gpd, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Xsetbv as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XSAVE [EXPLICIT] XCR[ECX] <- EDX:EAX
    }
}

impl InvlpgbEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn invlpgb(&mut self, dst: Gp, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Invlpgb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl InvlpgbEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn invlpgb(&mut self, dst: Gpd, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Invlpgb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl InvlpgbEmitter<Gpq, Gpd, Gpd> for Assembler<'_> {
    fn invlpgb(&mut self, dst: Gpq, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Invlpgb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl TlbsyncEmitter for Assembler<'_> {
    fn tlbsync(&mut self) {
        self.emit_n(InstId::Tlbsync as u32, &[]);
    }
}

impl MonitorEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn monitor(&mut self, dst: Mem, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Monitor as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // MONITOR
    }
}

impl MonitorEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn monitor(&mut self, dst: Mem, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Monitor as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // MONITOR
    }
}

impl MwaitEmitter<Gp, Gp> for Assembler<'_> {
    fn mwait(&mut self, lhs: Gp, rhs: Gp) {
        self.emit_n(InstId::Mwait as u32, &[lhs.as_operand(), rhs.as_operand()]); // MONITOR
    }
}

impl MwaitEmitter<Gpd, Gpd> for Assembler<'_> {
    fn mwait(&mut self, lhs: Gpd, rhs: Gpd) {
        self.emit_n(InstId::Mwait as u32, &[lhs.as_operand(), rhs.as_operand()]); // MONITOR
    }
}

impl ClacEmitter for Assembler<'_> {
    fn clac(&mut self) {
        self.emit_n(InstId::Clac as u32, &[]); // SMAP
    }
}

impl StacEmitter for Assembler<'_> {
    fn stac(&mut self) {
        self.emit_n(InstId::Stac as u32, &[]); // SMAP
    }
}

impl SkinitEmitter<Gp> for Assembler<'_> {
    fn skinit(&mut self, dst: Gp) {
        self.emit_n(InstId::Skinit as u32, &[dst.as_operand()]); // SKINIT [EXPLICIT] <eax>
    }
}

impl SkinitEmitter<Gpd> for Assembler<'_> {
    fn skinit(&mut self, dst: Gpd) {
        self.emit_n(InstId::Skinit as u32, &[dst.as_operand()]); // SKINIT [EXPLICIT] <eax>
    }
}

impl StgiEmitter for Assembler<'_> {
    fn stgi(&mut self) {
        self.emit_n(InstId::Stgi as u32, &[]); // SKINIT
    }
}

impl PsmashEmitter for Assembler<'_> {
    fn psmash(&mut self) {
        self.emit_n(InstId::Psmash as u32, &[]); // SNP
    }
}

impl PvalidateEmitter for Assembler<'_> {
    fn pvalidate(&mut self) {
        self.emit_n(InstId::Pvalidate as u32, &[]); // SNP
    }
}

impl RmpadjustEmitter for Assembler<'_> {
    fn rmpadjust(&mut self) {
        self.emit_n(InstId::Rmpadjust as u32, &[]); // SNP
    }
}

impl RmpupdateEmitter for Assembler<'_> {
    fn rmpupdate(&mut self) {
        self.emit_n(InstId::Rmpupdate as u32, &[]); // SNP
    }
}

impl InveptEmitter<Gp, Mem> for Assembler<'_> {
    fn invept(&mut self, lhs: Gp, rhs: Mem) {
        self.emit_n(InstId::Invept as u32, &[lhs.as_operand(), rhs.as_operand()]); // VMX
    }
}

impl InveptEmitter<Gpd, Mem> for Assembler<'_> {
    fn invept(&mut self, lhs: Gpd, rhs: Mem) {
        self.emit_n(InstId::Invept as u32, &[lhs.as_operand(), rhs.as_operand()]); // VMX
    }
}

impl InveptEmitter<Gpq, Mem> for Assembler<'_> {
    fn invept(&mut self, lhs: Gpq, rhs: Mem) {
        self.emit_n(InstId::Invept as u32, &[lhs.as_operand(), rhs.as_operand()]); // VMX
    }
}

impl InvvpidEmitter<Gp, Mem> for Assembler<'_> {
    fn invvpid(&mut self, lhs: Gp, rhs: Mem) {
        self.emit_n(
            InstId::Invvpid as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // VMX
    }
}

impl InvvpidEmitter<Gpd, Mem> for Assembler<'_> {
    fn invvpid(&mut self, lhs: Gpd, rhs: Mem) {
        self.emit_n(
            InstId::Invvpid as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // VMX
    }
}

impl InvvpidEmitter<Gpq, Mem> for Assembler<'_> {
    fn invvpid(&mut self, lhs: Gpq, rhs: Mem) {
        self.emit_n(
            InstId::Invvpid as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // VMX
    }
}

impl VmcallEmitter for Assembler<'_> {
    fn vmcall(&mut self) {
        self.emit_n(InstId::Vmcall as u32, &[]); // VMX
    }
}

impl VmclearEmitter<Mem> for Assembler<'_> {
    fn vmclear(&mut self, mem: Mem) {
        self.emit_n(InstId::Vmclear as u32, &[mem.as_operand()]); // VMX
    }
}

impl VmfuncEmitter for Assembler<'_> {
    fn vmfunc(&mut self) {
        self.emit_n(InstId::Vmfunc as u32, &[]); // VMX
    }
}

impl VmlaunchEmitter for Assembler<'_> {
    fn vmlaunch(&mut self) {
        self.emit_n(InstId::Vmlaunch as u32, &[]); // VMX
    }
}

impl VmptrldEmitter<Mem> for Assembler<'_> {
    fn vmptrld(&mut self, mem: Mem) {
        self.emit_n(InstId::Vmptrld as u32, &[mem.as_operand()]); // VMX
    }
}

impl VmptrstEmitter<Mem> for Assembler<'_> {
    fn vmptrst(&mut self, dst: Mem) {
        self.emit_n(InstId::Vmptrst as u32, &[dst.as_operand()]); // VMX
    }
}

impl VmreadEmitter<Gp, Gp> for Assembler<'_> {
    fn vmread(&mut self, dst: Gp, src: Gp) {
        self.emit_n(InstId::Vmread as u32, &[dst.as_operand(), src.as_operand()]); // VMX
    }
}

impl VmreadEmitter<Gpd, Gpd> for Assembler<'_> {
    fn vmread(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(InstId::Vmread as u32, &[dst.as_operand(), src.as_operand()]); // VMX
    }
}

impl VmreadEmitter<Gpq, Gpq> for Assembler<'_> {
    fn vmread(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(InstId::Vmread as u32, &[dst.as_operand(), src.as_operand()]); // VMX
    }
}

impl VmreadEmitter<Mem, Gp> for Assembler<'_> {
    fn vmread(&mut self, dst: Mem, src: Gp) {
        self.emit_n(InstId::Vmread as u32, &[dst.as_operand(), src.as_operand()]); // VMX
    }
}

impl VmreadEmitter<Mem, Gpd> for Assembler<'_> {
    fn vmread(&mut self, dst: Mem, src: Gpd) {
        self.emit_n(InstId::Vmread as u32, &[dst.as_operand(), src.as_operand()]); // VMX
    }
}

impl VmreadEmitter<Mem, Gpq> for Assembler<'_> {
    fn vmread(&mut self, dst: Mem, src: Gpq) {
        self.emit_n(InstId::Vmread as u32, &[dst.as_operand(), src.as_operand()]); // VMX
    }
}

impl VmresumeEmitter for Assembler<'_> {
    fn vmresume(&mut self) {
        self.emit_n(InstId::Vmresume as u32, &[]); // VMX
    }
}

impl VmwriteEmitter<Gp, Mem> for Assembler<'_> {
    fn vmwrite(&mut self, lhs: Gp, rhs: Mem) {
        self.emit_n(
            InstId::Vmwrite as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // VMX
    }
}

impl VmwriteEmitter<Gpd, Mem> for Assembler<'_> {
    fn vmwrite(&mut self, lhs: Gpd, rhs: Mem) {
        self.emit_n(
            InstId::Vmwrite as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // VMX
    }
}

impl VmwriteEmitter<Gpq, Mem> for Assembler<'_> {
    fn vmwrite(&mut self, lhs: Gpq, rhs: Mem) {
        self.emit_n(
            InstId::Vmwrite as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // VMX
    }
}

impl VmwriteEmitter<Gp, Gp> for Assembler<'_> {
    fn vmwrite(&mut self, lhs: Gp, rhs: Gp) {
        self.emit_n(
            InstId::Vmwrite as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // VMX
    }
}

impl VmwriteEmitter<Gpd, Gpd> for Assembler<'_> {
    fn vmwrite(&mut self, lhs: Gpd, rhs: Gpd) {
        self.emit_n(
            InstId::Vmwrite as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // VMX
    }
}

impl VmwriteEmitter<Gpq, Gpq> for Assembler<'_> {
    fn vmwrite(&mut self, lhs: Gpq, rhs: Gpq) {
        self.emit_n(
            InstId::Vmwrite as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // VMX
    }
}

impl VmxoffEmitter for Assembler<'_> {
    fn vmxoff(&mut self) {
        self.emit_n(InstId::Vmxoff as u32, &[]); // VMX
    }
}

impl VmxonEmitter<Mem> for Assembler<'_> {
    fn vmxon(&mut self, mem: Mem) {
        self.emit_n(InstId::Vmxon as u32, &[mem.as_operand()]); // VMX
    }
}

impl ClgiEmitter for Assembler<'_> {
    fn clgi(&mut self) {
        self.emit_n(InstId::Clgi as u32, &[]); // SVM
    }
}

impl InvlpgaEmitter<Gp, Gp> for Assembler<'_> {
    fn invlpga(&mut self, lhs: Gp, rhs: Gp) {
        self.emit_n(
            InstId::Invlpga as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SVM [EXPLICIT] <eax|rax, ecx>
    }
}

impl InvlpgaEmitter<Gpd, Gpd> for Assembler<'_> {
    fn invlpga(&mut self, lhs: Gpd, rhs: Gpd) {
        self.emit_n(
            InstId::Invlpga as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SVM [EXPLICIT] <eax|rax, ecx>
    }
}

impl InvlpgaEmitter<Gpq, Gpd> for Assembler<'_> {
    fn invlpga(&mut self, lhs: Gpq, rhs: Gpd) {
        self.emit_n(
            InstId::Invlpga as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SVM [EXPLICIT] <eax|rax, ecx>
    }
}

impl VmloadEmitter<Gp> for Assembler<'_> {
    fn vmload(&mut self, src: Gp) {
        self.emit_n(InstId::Vmload as u32, &[src.as_operand()]); // SVM [EXPLICIT] <zax>
    }
}

impl VmloadEmitter<Gpd> for Assembler<'_> {
    fn vmload(&mut self, src: Gpd) {
        self.emit_n(InstId::Vmload as u32, &[src.as_operand()]); // SVM [EXPLICIT] <zax>
    }
}

impl VmloadEmitter<Gpq> for Assembler<'_> {
    fn vmload(&mut self, src: Gpq) {
        self.emit_n(InstId::Vmload as u32, &[src.as_operand()]); // SVM [EXPLICIT] <zax>
    }
}

impl VmmcallEmitter for Assembler<'_> {
    fn vmmcall(&mut self) {
        self.emit_n(InstId::Vmmcall as u32, &[]); // SVM
    }
}

impl VmrunEmitter<Gp> for Assembler<'_> {
    fn vmrun(&mut self, dst: Gp) {
        self.emit_n(InstId::Vmrun as u32, &[dst.as_operand()]); // SVM [EXPLICIT] <zax>
    }
}

impl VmrunEmitter<Gpd> for Assembler<'_> {
    fn vmrun(&mut self, dst: Gpd) {
        self.emit_n(InstId::Vmrun as u32, &[dst.as_operand()]); // SVM [EXPLICIT] <zax>
    }
}

impl VmrunEmitter<Gpq> for Assembler<'_> {
    fn vmrun(&mut self, dst: Gpq) {
        self.emit_n(InstId::Vmrun as u32, &[dst.as_operand()]); // SVM [EXPLICIT] <zax>
    }
}

impl VmsaveEmitter<Gp> for Assembler<'_> {
    fn vmsave(&mut self, src: Gp) {
        self.emit_n(InstId::Vmsave as u32, &[src.as_operand()]); // SVM [EXPLICIT] <zax>
    }
}

impl VmsaveEmitter<Gpd> for Assembler<'_> {
    fn vmsave(&mut self, src: Gpd) {
        self.emit_n(InstId::Vmsave as u32, &[src.as_operand()]); // SVM [EXPLICIT] <zax>
    }
}

impl VmsaveEmitter<Gpq> for Assembler<'_> {
    fn vmsave(&mut self, src: Gpq) {
        self.emit_n(InstId::Vmsave as u32, &[src.as_operand()]); // SVM [EXPLICIT] <zax>
    }
}

impl VmgexitEmitter for Assembler<'_> {
    fn vmgexit(&mut self) {
        self.emit_n(InstId::Vmgexit as u32, &[]);
    }
}

impl F2xm1Emitter for Assembler<'_> {
    fn f2xm1(&mut self) {
        self.emit_n(InstId::F2xm1 as u32, &[]); // FPU
    }
}

impl FabsEmitter for Assembler<'_> {
    fn fabs(&mut self) {
        self.emit_n(InstId::Fabs as u32, &[]); // FPU
    }
}

impl FaddEmitter<St, St> for Assembler<'_> {
    fn fadd(&mut self, lhs: St, rhs: St) {
        self.emit_n(InstId::Fadd as u32, &[lhs.as_operand(), rhs.as_operand()]); // FPU
    }
}

impl Fadd1Emitter<Mem> for Assembler<'_> {
    fn fadd_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Fadd as u32, &[mem.as_operand()]); // FPU
    }
}

impl FaddpEmitter<St> for Assembler<'_> {
    fn faddp(&mut self, src: St) {
        self.emit_n(InstId::Faddp as u32, &[src.as_operand()]); // FPU
    }
}

impl Faddp0Emitter for Assembler<'_> {
    fn faddp_0(&mut self) {
        self.emit_n(InstId::Faddp as u32, &[]); // FPU
    }
}

impl FbldEmitter<Mem> for Assembler<'_> {
    fn fbld(&mut self, mem: Mem) {
        self.emit_n(InstId::Fbld as u32, &[mem.as_operand()]); // FPU
    }
}

impl FbstpEmitter<Mem> for Assembler<'_> {
    fn fbstp(&mut self, dst: Mem) {
        self.emit_n(InstId::Fbstp as u32, &[dst.as_operand()]); // FPU
    }
}

impl FchsEmitter for Assembler<'_> {
    fn fchs(&mut self) {
        self.emit_n(InstId::Fchs as u32, &[]); // FPU
    }
}

impl FclexEmitter for Assembler<'_> {
    fn fclex(&mut self) {
        self.emit_n(InstId::Fclex as u32, &[]); // FPU
    }
}

impl FcmovbEmitter<St> for Assembler<'_> {
    fn fcmovb(&mut self, src: St) {
        self.emit_n(InstId::Fcmovb as u32, &[src.as_operand()]); // FPU
    }
}

impl FcmovbeEmitter<St> for Assembler<'_> {
    fn fcmovbe(&mut self, src: St) {
        self.emit_n(InstId::Fcmovbe as u32, &[src.as_operand()]); // FPU
    }
}

impl FcmoveEmitter<St> for Assembler<'_> {
    fn fcmove(&mut self, src: St) {
        self.emit_n(InstId::Fcmove as u32, &[src.as_operand()]); // FPU
    }
}

impl FcmovnbEmitter<St> for Assembler<'_> {
    fn fcmovnb(&mut self, src: St) {
        self.emit_n(InstId::Fcmovnb as u32, &[src.as_operand()]); // FPU
    }
}

impl FcmovnbeEmitter<St> for Assembler<'_> {
    fn fcmovnbe(&mut self, src: St) {
        self.emit_n(InstId::Fcmovnbe as u32, &[src.as_operand()]); // FPU
    }
}

impl FcmovneEmitter<St> for Assembler<'_> {
    fn fcmovne(&mut self, src: St) {
        self.emit_n(InstId::Fcmovne as u32, &[src.as_operand()]); // FPU
    }
}

impl FcmovnuEmitter<St> for Assembler<'_> {
    fn fcmovnu(&mut self, src: St) {
        self.emit_n(InstId::Fcmovnu as u32, &[src.as_operand()]); // FPU
    }
}

impl FcmovuEmitter<St> for Assembler<'_> {
    fn fcmovu(&mut self, src: St) {
        self.emit_n(InstId::Fcmovu as u32, &[src.as_operand()]); // FPU
    }
}

impl FcomEmitter<St> for Assembler<'_> {
    fn fcom(&mut self, src: St) {
        self.emit_n(InstId::Fcom as u32, &[src.as_operand()]); // FPU
    }
}

impl FcomEmitter<Mem> for Assembler<'_> {
    fn fcom(&mut self, mem: Mem) {
        self.emit_n(InstId::Fcom as u32, &[mem.as_operand()]); // FPU
    }
}

impl Fcom0Emitter for Assembler<'_> {
    fn fcom_0(&mut self) {
        self.emit_n(InstId::Fcom as u32, &[]); // FPU
    }
}

impl FcompEmitter<St> for Assembler<'_> {
    fn fcomp(&mut self, src: St) {
        self.emit_n(InstId::Fcomp as u32, &[src.as_operand()]); // FPU
    }
}

impl FcompEmitter<Mem> for Assembler<'_> {
    fn fcomp(&mut self, mem: Mem) {
        self.emit_n(InstId::Fcomp as u32, &[mem.as_operand()]); // FPU
    }
}

impl Fcomp0Emitter for Assembler<'_> {
    fn fcomp_0(&mut self) {
        self.emit_n(InstId::Fcomp as u32, &[]); // FPU
    }
}

impl FcomppEmitter for Assembler<'_> {
    fn fcompp(&mut self) {
        self.emit_n(InstId::Fcompp as u32, &[]); // FPU
    }
}

impl FcomiEmitter<St> for Assembler<'_> {
    fn fcomi(&mut self, src: St) {
        self.emit_n(InstId::Fcomi as u32, &[src.as_operand()]); // FPU
    }
}

impl FcomipEmitter<St> for Assembler<'_> {
    fn fcomip(&mut self, src: St) {
        self.emit_n(InstId::Fcomip as u32, &[src.as_operand()]); // FPU
    }
}

impl FcosEmitter for Assembler<'_> {
    fn fcos(&mut self) {
        self.emit_n(InstId::Fcos as u32, &[]); // FPU
    }
}

impl FdecstpEmitter for Assembler<'_> {
    fn fdecstp(&mut self) {
        self.emit_n(InstId::Fdecstp as u32, &[]); // FPU
    }
}

impl FdivEmitter<St, St> for Assembler<'_> {
    fn fdiv(&mut self, lhs: St, rhs: St) {
        self.emit_n(InstId::Fdiv as u32, &[lhs.as_operand(), rhs.as_operand()]); // FPU
    }
}

impl Fdiv1Emitter<Mem> for Assembler<'_> {
    fn fdiv_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Fdiv as u32, &[mem.as_operand()]); // FPU
    }
}

impl FdivpEmitter<St> for Assembler<'_> {
    fn fdivp(&mut self, src: St) {
        self.emit_n(InstId::Fdivp as u32, &[src.as_operand()]); // FPU
    }
}

impl Fdivp0Emitter for Assembler<'_> {
    fn fdivp_0(&mut self) {
        self.emit_n(InstId::Fdivp as u32, &[]); // FPU
    }
}

impl FdivrEmitter<St, St> for Assembler<'_> {
    fn fdivr(&mut self, lhs: St, rhs: St) {
        self.emit_n(InstId::Fdivr as u32, &[lhs.as_operand(), rhs.as_operand()]); // FPU
    }
}

impl Fdivr1Emitter<Mem> for Assembler<'_> {
    fn fdivr_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Fdivr as u32, &[mem.as_operand()]); // FPU
    }
}

impl FdivrpEmitter<St> for Assembler<'_> {
    fn fdivrp(&mut self, src: St) {
        self.emit_n(InstId::Fdivrp as u32, &[src.as_operand()]); // FPU
    }
}

impl Fdivrp0Emitter for Assembler<'_> {
    fn fdivrp_0(&mut self) {
        self.emit_n(InstId::Fdivrp as u32, &[]); // FPU
    }
}

impl FfreeEmitter<St> for Assembler<'_> {
    fn ffree(&mut self, src: St) {
        self.emit_n(InstId::Ffree as u32, &[src.as_operand()]); // FPU
    }
}

impl FiaddEmitter<Mem> for Assembler<'_> {
    fn fiadd(&mut self, mem: Mem) {
        self.emit_n(InstId::Fiadd as u32, &[mem.as_operand()]); // FPU
    }
}

impl FicomEmitter<Mem> for Assembler<'_> {
    fn ficom(&mut self, mem: Mem) {
        self.emit_n(InstId::Ficom as u32, &[mem.as_operand()]); // FPU
    }
}

impl FicompEmitter<Mem> for Assembler<'_> {
    fn ficomp(&mut self, mem: Mem) {
        self.emit_n(InstId::Ficomp as u32, &[mem.as_operand()]); // FPU
    }
}

impl FidivEmitter<Mem> for Assembler<'_> {
    fn fidiv(&mut self, mem: Mem) {
        self.emit_n(InstId::Fidiv as u32, &[mem.as_operand()]); // FPU
    }
}

impl FidivrEmitter<Mem> for Assembler<'_> {
    fn fidivr(&mut self, mem: Mem) {
        self.emit_n(InstId::Fidivr as u32, &[mem.as_operand()]); // FPU
    }
}

impl FildEmitter<Mem> for Assembler<'_> {
    fn fild(&mut self, mem: Mem) {
        self.emit_n(InstId::Fild as u32, &[mem.as_operand()]); // FPU
    }
}

impl FimulEmitter<Mem> for Assembler<'_> {
    fn fimul(&mut self, mem: Mem) {
        self.emit_n(InstId::Fimul as u32, &[mem.as_operand()]); // FPU
    }
}

impl FincstpEmitter for Assembler<'_> {
    fn fincstp(&mut self) {
        self.emit_n(InstId::Fincstp as u32, &[]); // FPU
    }
}

impl FinitEmitter for Assembler<'_> {
    fn finit(&mut self) {
        self.emit_n(InstId::Finit as u32, &[]); // FPU
    }
}

impl FisubEmitter<Mem> for Assembler<'_> {
    fn fisub(&mut self, mem: Mem) {
        self.emit_n(InstId::Fisub as u32, &[mem.as_operand()]); // FPU
    }
}

impl FisubrEmitter<Mem> for Assembler<'_> {
    fn fisubr(&mut self, mem: Mem) {
        self.emit_n(InstId::Fisubr as u32, &[mem.as_operand()]); // FPU
    }
}

impl FninitEmitter for Assembler<'_> {
    fn fninit(&mut self) {
        self.emit_n(InstId::Fninit as u32, &[]); // FPU
    }
}

impl FistEmitter<Mem> for Assembler<'_> {
    fn fist(&mut self, dst: Mem) {
        self.emit_n(InstId::Fist as u32, &[dst.as_operand()]); // FPU
    }
}

impl FistpEmitter<Mem> for Assembler<'_> {
    fn fistp(&mut self, dst: Mem) {
        self.emit_n(InstId::Fistp as u32, &[dst.as_operand()]); // FPU
    }
}

impl FisttpEmitter<Mem> for Assembler<'_> {
    fn fisttp(&mut self, dst: Mem) {
        self.emit_n(InstId::Fisttp as u32, &[dst.as_operand()]); // FPU+SSE3
    }
}

impl FldEmitter<Mem> for Assembler<'_> {
    fn fld(&mut self, mem: Mem) {
        self.emit_n(InstId::Fld as u32, &[mem.as_operand()]); // FPU
    }
}

impl FldEmitter<St> for Assembler<'_> {
    fn fld(&mut self, src: St) {
        self.emit_n(InstId::Fld as u32, &[src.as_operand()]); // FPU
    }
}

impl Fld1Emitter for Assembler<'_> {
    fn fld1(&mut self) {
        self.emit_n(InstId::Fld1 as u32, &[]); // FPU
    }
}

impl Fldl2tEmitter for Assembler<'_> {
    fn fldl2t(&mut self) {
        self.emit_n(InstId::Fldl2t as u32, &[]); // FPU
    }
}

impl Fldl2eEmitter for Assembler<'_> {
    fn fldl2e(&mut self) {
        self.emit_n(InstId::Fldl2e as u32, &[]); // FPU
    }
}

impl FldpiEmitter for Assembler<'_> {
    fn fldpi(&mut self) {
        self.emit_n(InstId::Fldpi as u32, &[]); // FPU
    }
}

impl Fldlg2Emitter for Assembler<'_> {
    fn fldlg2(&mut self) {
        self.emit_n(InstId::Fldlg2 as u32, &[]); // FPU
    }
}

impl Fldln2Emitter for Assembler<'_> {
    fn fldln2(&mut self) {
        self.emit_n(InstId::Fldln2 as u32, &[]); // FPU
    }
}

impl FldzEmitter for Assembler<'_> {
    fn fldz(&mut self) {
        self.emit_n(InstId::Fldz as u32, &[]); // FPU
    }
}

impl FldcwEmitter<Mem> for Assembler<'_> {
    fn fldcw(&mut self, mem: Mem) {
        self.emit_n(InstId::Fldcw as u32, &[mem.as_operand()]); // FPU
    }
}

impl FldenvEmitter<Mem> for Assembler<'_> {
    fn fldenv(&mut self, mem: Mem) {
        self.emit_n(InstId::Fldenv as u32, &[mem.as_operand()]); // FPU
    }
}

impl FmulEmitter<St, St> for Assembler<'_> {
    fn fmul(&mut self, lhs: St, rhs: St) {
        self.emit_n(InstId::Fmul as u32, &[lhs.as_operand(), rhs.as_operand()]); // FPU
    }
}

impl Fmul1Emitter<Mem> for Assembler<'_> {
    fn fmul_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Fmul as u32, &[mem.as_operand()]); // FPU
    }
}

impl FmulpEmitter<St> for Assembler<'_> {
    fn fmulp(&mut self, src: St) {
        self.emit_n(InstId::Fmulp as u32, &[src.as_operand()]); // FPU
    }
}

impl Fmulp0Emitter for Assembler<'_> {
    fn fmulp_0(&mut self) {
        self.emit_n(InstId::Fmulp as u32, &[]); // FPU
    }
}

impl FnclexEmitter for Assembler<'_> {
    fn fnclex(&mut self) {
        self.emit_n(InstId::Fnclex as u32, &[]); // FPU
    }
}

impl FnopEmitter for Assembler<'_> {
    fn fnop(&mut self) {
        self.emit_n(InstId::Fnop as u32, &[]); // FPU
    }
}

impl FnsaveEmitter<Mem> for Assembler<'_> {
    fn fnsave(&mut self, dst: Mem) {
        self.emit_n(InstId::Fnsave as u32, &[dst.as_operand()]); // FPU
    }
}

impl FnstenvEmitter<Mem> for Assembler<'_> {
    fn fnstenv(&mut self, dst: Mem) {
        self.emit_n(InstId::Fnstenv as u32, &[dst.as_operand()]); // FPU
    }
}

impl FnstcwEmitter<Mem> for Assembler<'_> {
    fn fnstcw(&mut self, dst: Mem) {
        self.emit_n(InstId::Fnstcw as u32, &[dst.as_operand()]); // FPU
    }
}

impl FpatanEmitter for Assembler<'_> {
    fn fpatan(&mut self) {
        self.emit_n(InstId::Fpatan as u32, &[]); // FPU
    }
}

impl FpremEmitter for Assembler<'_> {
    fn fprem(&mut self) {
        self.emit_n(InstId::Fprem as u32, &[]); // FPU
    }
}

impl Fprem1Emitter for Assembler<'_> {
    fn fprem1(&mut self) {
        self.emit_n(InstId::Fprem1 as u32, &[]); // FPU
    }
}

impl FptanEmitter for Assembler<'_> {
    fn fptan(&mut self) {
        self.emit_n(InstId::Fptan as u32, &[]); // FPU
    }
}

impl FrndintEmitter for Assembler<'_> {
    fn frndint(&mut self) {
        self.emit_n(InstId::Frndint as u32, &[]); // FPU
    }
}

impl FrstorEmitter<Mem> for Assembler<'_> {
    fn frstor(&mut self, mem: Mem) {
        self.emit_n(InstId::Frstor as u32, &[mem.as_operand()]); // FPU
    }
}

impl FsaveEmitter<Mem> for Assembler<'_> {
    fn fsave(&mut self, dst: Mem) {
        self.emit_n(InstId::Fsave as u32, &[dst.as_operand()]); // FPU
    }
}

impl FscaleEmitter for Assembler<'_> {
    fn fscale(&mut self) {
        self.emit_n(InstId::Fscale as u32, &[]); // FPU
    }
}

impl FsinEmitter for Assembler<'_> {
    fn fsin(&mut self) {
        self.emit_n(InstId::Fsin as u32, &[]); // FPU
    }
}

impl FsincosEmitter for Assembler<'_> {
    fn fsincos(&mut self) {
        self.emit_n(InstId::Fsincos as u32, &[]); // FPU
    }
}

impl FsqrtEmitter for Assembler<'_> {
    fn fsqrt(&mut self) {
        self.emit_n(InstId::Fsqrt as u32, &[]); // FPU
    }
}

impl FstEmitter<Mem> for Assembler<'_> {
    fn fst(&mut self, dst: Mem) {
        self.emit_n(InstId::Fst as u32, &[dst.as_operand()]); // FPU
    }
}

impl FstEmitter<St> for Assembler<'_> {
    fn fst(&mut self, dst: St) {
        self.emit_n(InstId::Fst as u32, &[dst.as_operand()]); // FPU
    }
}

impl FstpEmitter<Mem> for Assembler<'_> {
    fn fstp(&mut self, dst: Mem) {
        self.emit_n(InstId::Fstp as u32, &[dst.as_operand()]); // FPU
    }
}

impl FstpEmitter<St> for Assembler<'_> {
    fn fstp(&mut self, dst: St) {
        self.emit_n(InstId::Fstp as u32, &[dst.as_operand()]); // FPU
    }
}

impl FstcwEmitter<Mem> for Assembler<'_> {
    fn fstcw(&mut self, dst: Mem) {
        self.emit_n(InstId::Fstcw as u32, &[dst.as_operand()]); // FPU
    }
}

impl FstenvEmitter<Mem> for Assembler<'_> {
    fn fstenv(&mut self, dst: Mem) {
        self.emit_n(InstId::Fstenv as u32, &[dst.as_operand()]); // FPU
    }
}

impl FsubEmitter<St, St> for Assembler<'_> {
    fn fsub(&mut self, lhs: St, rhs: St) {
        self.emit_n(InstId::Fsub as u32, &[lhs.as_operand(), rhs.as_operand()]); // FPU
    }
}

impl Fsub1Emitter<Mem> for Assembler<'_> {
    fn fsub_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Fsub as u32, &[mem.as_operand()]); // FPU
    }
}

impl FsubpEmitter<St> for Assembler<'_> {
    fn fsubp(&mut self, src: St) {
        self.emit_n(InstId::Fsubp as u32, &[src.as_operand()]); // FPU
    }
}

impl Fsubp0Emitter for Assembler<'_> {
    fn fsubp_0(&mut self) {
        self.emit_n(InstId::Fsubp as u32, &[]); // FPU
    }
}

impl FsubrEmitter<St, St> for Assembler<'_> {
    fn fsubr(&mut self, lhs: St, rhs: St) {
        self.emit_n(InstId::Fsubr as u32, &[lhs.as_operand(), rhs.as_operand()]); // FPU
    }
}

impl Fsubr1Emitter<Mem> for Assembler<'_> {
    fn fsubr_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Fsubr as u32, &[mem.as_operand()]); // FPU
    }
}

impl FsubrpEmitter<St> for Assembler<'_> {
    fn fsubrp(&mut self, src: St) {
        self.emit_n(InstId::Fsubrp as u32, &[src.as_operand()]); // FPU
    }
}

impl Fsubrp0Emitter for Assembler<'_> {
    fn fsubrp_0(&mut self) {
        self.emit_n(InstId::Fsubrp as u32, &[]); // FPU
    }
}

impl FtstEmitter for Assembler<'_> {
    fn ftst(&mut self) {
        self.emit_n(InstId::Ftst as u32, &[]); // FPU
    }
}

impl FucomEmitter<St> for Assembler<'_> {
    fn fucom(&mut self, src: St) {
        self.emit_n(InstId::Fucom as u32, &[src.as_operand()]); // FPU
    }
}

impl Fucom0Emitter for Assembler<'_> {
    fn fucom_0(&mut self) {
        self.emit_n(InstId::Fucom as u32, &[]); // FPU
    }
}

impl FucomiEmitter<St> for Assembler<'_> {
    fn fucomi(&mut self, src: St) {
        self.emit_n(InstId::Fucomi as u32, &[src.as_operand()]); // FPU
    }
}

impl FucomipEmitter<St> for Assembler<'_> {
    fn fucomip(&mut self, src: St) {
        self.emit_n(InstId::Fucomip as u32, &[src.as_operand()]); // FPU
    }
}

impl FucompEmitter<St> for Assembler<'_> {
    fn fucomp(&mut self, src: St) {
        self.emit_n(InstId::Fucomp as u32, &[src.as_operand()]); // FPU
    }
}

impl Fucomp0Emitter for Assembler<'_> {
    fn fucomp_0(&mut self) {
        self.emit_n(InstId::Fucomp as u32, &[]); // FPU
    }
}

impl FucomppEmitter for Assembler<'_> {
    fn fucompp(&mut self) {
        self.emit_n(InstId::Fucompp as u32, &[]); // FPU
    }
}

impl FwaitEmitter for Assembler<'_> {
    fn fwait(&mut self) {
        self.emit_n(InstId::Fwait as u32, &[]); // FPU
    }
}

impl FxamEmitter for Assembler<'_> {
    fn fxam(&mut self) {
        self.emit_n(InstId::Fxam as u32, &[]); // FPU
    }
}

impl FxchEmitter<St> for Assembler<'_> {
    fn fxch(&mut self, src: St) {
        self.emit_n(InstId::Fxch as u32, &[src.as_operand()]); // FPU
    }
}

impl FxtractEmitter for Assembler<'_> {
    fn fxtract(&mut self) {
        self.emit_n(InstId::Fxtract as u32, &[]); // FPU
    }
}

impl Fyl2xEmitter for Assembler<'_> {
    fn fyl2x(&mut self) {
        self.emit_n(InstId::Fyl2x as u32, &[]); // FPU
    }
}

impl Fyl2xp1Emitter for Assembler<'_> {
    fn fyl2xp1(&mut self) {
        self.emit_n(InstId::Fyl2xp1 as u32, &[]); // FPU
    }
}

impl FstswEmitter<Gp> for Assembler<'_> {
    fn fstsw(&mut self, dst: Gp) {
        self.emit_n(InstId::Fstsw as u32, &[dst.as_operand()]); // FPU
    }
}

impl FstswEmitter<Gpw> for Assembler<'_> {
    fn fstsw(&mut self, dst: Gpw) {
        self.emit_n(InstId::Fstsw as u32, &[dst.as_operand()]); // FPU
    }
}

impl FstswEmitter<Mem> for Assembler<'_> {
    fn fstsw(&mut self, dst: Mem) {
        self.emit_n(InstId::Fstsw as u32, &[dst.as_operand()]); // FPU
    }
}

impl FnstswEmitter<Gp> for Assembler<'_> {
    fn fnstsw(&mut self, dst: Gp) {
        self.emit_n(InstId::Fnstsw as u32, &[dst.as_operand()]); // FPU
    }
}

impl FnstswEmitter<Gpw> for Assembler<'_> {
    fn fnstsw(&mut self, dst: Gpw) {
        self.emit_n(InstId::Fnstsw as u32, &[dst.as_operand()]); // FPU
    }
}

impl FnstswEmitter<Mem> for Assembler<'_> {
    fn fnstsw(&mut self, dst: Mem) {
        self.emit_n(InstId::Fnstsw as u32, &[dst.as_operand()]); // FPU
    }
}

impl AddpdEmitter<Vec, Vec> for Assembler<'_> {
    fn addpd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Addpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl AddpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn addpd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Addpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl AddpdEmitter<Vec, Mem> for Assembler<'_> {
    fn addpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Addpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl AddpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn addpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Addpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl AddpsEmitter<Vec, Vec> for Assembler<'_> {
    fn addps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Addps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl AddpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn addps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Addps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl AddpsEmitter<Vec, Mem> for Assembler<'_> {
    fn addps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Addps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl AddpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn addps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Addps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl AddsdEmitter<Vec, Vec> for Assembler<'_> {
    fn addsd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Addsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl AddsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn addsd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Addsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl AddsdEmitter<Vec, Mem> for Assembler<'_> {
    fn addsd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Addsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl AddsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn addsd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Addsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl AddssEmitter<Vec, Vec> for Assembler<'_> {
    fn addss(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Addss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl AddssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn addss(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Addss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl AddssEmitter<Vec, Mem> for Assembler<'_> {
    fn addss(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Addss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl AddssEmitter<Xmm, Mem> for Assembler<'_> {
    fn addss(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Addss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl AddsubpdEmitter<Vec, Vec> for Assembler<'_> {
    fn addsubpd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Addsubpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE3
    }
}

impl AddsubpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn addsubpd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Addsubpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE3
    }
}

impl AddsubpdEmitter<Vec, Mem> for Assembler<'_> {
    fn addsubpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Addsubpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE3
    }
}

impl AddsubpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn addsubpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Addsubpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE3
    }
}

impl AddsubpsEmitter<Vec, Vec> for Assembler<'_> {
    fn addsubps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Addsubps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE3
    }
}

impl AddsubpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn addsubps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Addsubps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE3
    }
}

impl AddsubpsEmitter<Vec, Mem> for Assembler<'_> {
    fn addsubps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Addsubps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE3
    }
}

impl AddsubpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn addsubps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Addsubps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE3
    }
}

impl AndnpdEmitter<Vec, Vec> for Assembler<'_> {
    fn andnpd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Andnpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl AndnpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn andnpd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Andnpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl AndnpdEmitter<Vec, Mem> for Assembler<'_> {
    fn andnpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Andnpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl AndnpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn andnpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Andnpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl AndnpsEmitter<Vec, Vec> for Assembler<'_> {
    fn andnps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Andnps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl AndnpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn andnps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Andnps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl AndnpsEmitter<Vec, Mem> for Assembler<'_> {
    fn andnps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Andnps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl AndnpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn andnps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Andnps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl AndpdEmitter<Vec, Vec> for Assembler<'_> {
    fn andpd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Andpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl AndpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn andpd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Andpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl AndpdEmitter<Vec, Mem> for Assembler<'_> {
    fn andpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Andpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl AndpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn andpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Andpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl AndpsEmitter<Vec, Vec> for Assembler<'_> {
    fn andps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Andps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl AndpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn andps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Andps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl AndpsEmitter<Vec, Mem> for Assembler<'_> {
    fn andps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Andps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl AndpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn andps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Andps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl BlendpdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn blendpd(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Blendpd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> BlendpdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn blendpd(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Blendpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl BlendpdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn blendpd(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Blendpd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> BlendpdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn blendpd(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Blendpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl BlendpsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn blendps(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Blendps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> BlendpsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn blendps(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Blendps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl BlendpsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn blendps(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Blendps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> BlendpsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn blendps(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Blendps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl BlendvpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn blendvpd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Blendvpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl BlendvpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn blendvpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Blendvpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl BlendvpdEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn blendvpd(&mut self, dst: Vec, src1: Mem, src2: Vec) {
        self.emit_n(
            InstId::Blendvpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl BlendvpdEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn blendvpd(&mut self, dst: Xmm, src1: Mem, src2: Xmm) {
        self.emit_n(
            InstId::Blendvpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl BlendvpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn blendvps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Blendvps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl BlendvpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn blendvps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Blendvps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl BlendvpsEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn blendvps(&mut self, dst: Vec, src1: Mem, src2: Vec) {
        self.emit_n(
            InstId::Blendvps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl BlendvpsEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn blendvps(&mut self, dst: Xmm, src1: Mem, src2: Xmm) {
        self.emit_n(
            InstId::Blendvps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl CmppdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn cmppd(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Cmppd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> CmppdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn cmppd(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Cmppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE2
    }
}

impl CmppdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn cmppd(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Cmppd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> CmppdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn cmppd(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Cmppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE2
    }
}

impl CmppsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn cmpps(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Cmpps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE
    }
}

impl<U2: Into<Imm>> CmppsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn cmpps(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Cmpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE
    }
}

impl CmppsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn cmpps(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Cmpps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE
    }
}

impl<U2: Into<Imm>> CmppsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn cmpps(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Cmpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE
    }
}

impl CmpsdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn cmpsd(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Cmpsd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> CmpsdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn cmpsd(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Cmpsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE2
    }
}

impl CmpsdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn cmpsd(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Cmpsd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> CmpsdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn cmpsd(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Cmpsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE2
    }
}

impl CmpssEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn cmpss(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Cmpss as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE
    }
}

impl<U2: Into<Imm>> CmpssEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn cmpss(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Cmpss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE
    }
}

impl CmpssEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn cmpss(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Cmpss as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE
    }
}

impl<U2: Into<Imm>> CmpssEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn cmpss(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Cmpss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE
    }
}

impl ComisdEmitter<Vec, Vec> for Assembler<'_> {
    fn comisd(&mut self, lhs: Vec, rhs: Vec) {
        self.emit_n(InstId::Comisd as u32, &[lhs.as_operand(), rhs.as_operand()]); // SSE2
    }
}

impl ComisdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn comisd(&mut self, lhs: Xmm, rhs: Xmm) {
        self.emit_n(InstId::Comisd as u32, &[lhs.as_operand(), rhs.as_operand()]); // SSE2
    }
}

impl ComisdEmitter<Vec, Mem> for Assembler<'_> {
    fn comisd(&mut self, lhs: Vec, rhs: Mem) {
        self.emit_n(InstId::Comisd as u32, &[lhs.as_operand(), rhs.as_operand()]); // SSE2
    }
}

impl ComisdEmitter<Xmm, Mem> for Assembler<'_> {
    fn comisd(&mut self, lhs: Xmm, rhs: Mem) {
        self.emit_n(InstId::Comisd as u32, &[lhs.as_operand(), rhs.as_operand()]); // SSE2
    }
}

impl ComissEmitter<Vec, Vec> for Assembler<'_> {
    fn comiss(&mut self, lhs: Vec, rhs: Vec) {
        self.emit_n(InstId::Comiss as u32, &[lhs.as_operand(), rhs.as_operand()]); // SSE
    }
}

impl ComissEmitter<Xmm, Xmm> for Assembler<'_> {
    fn comiss(&mut self, lhs: Xmm, rhs: Xmm) {
        self.emit_n(InstId::Comiss as u32, &[lhs.as_operand(), rhs.as_operand()]); // SSE
    }
}

impl ComissEmitter<Vec, Mem> for Assembler<'_> {
    fn comiss(&mut self, lhs: Vec, rhs: Mem) {
        self.emit_n(InstId::Comiss as u32, &[lhs.as_operand(), rhs.as_operand()]); // SSE
    }
}

impl ComissEmitter<Xmm, Mem> for Assembler<'_> {
    fn comiss(&mut self, lhs: Xmm, rhs: Mem) {
        self.emit_n(InstId::Comiss as u32, &[lhs.as_operand(), rhs.as_operand()]); // SSE
    }
}

impl Cvtdq2pdEmitter<Vec, Vec> for Assembler<'_> {
    fn cvtdq2pd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Cvtdq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtdq2pdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn cvtdq2pd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Cvtdq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtdq2pdEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtdq2pd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Cvtdq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtdq2pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtdq2pd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Cvtdq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtdq2psEmitter<Vec, Vec> for Assembler<'_> {
    fn cvtdq2ps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Cvtdq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtdq2psEmitter<Xmm, Xmm> for Assembler<'_> {
    fn cvtdq2ps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Cvtdq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtdq2psEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtdq2ps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Cvtdq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtdq2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtdq2ps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Cvtdq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2dqEmitter<Vec, Vec> for Assembler<'_> {
    fn cvtpd2dq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Cvtpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2dqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn cvtpd2dq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Cvtpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2dqEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtpd2dq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Cvtpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2dqEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtpd2dq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Cvtpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2piEmitter<Mm, Vec> for Assembler<'_> {
    fn cvtpd2pi(&mut self, dst: Mm, src: Vec) {
        self.emit_n(
            InstId::Cvtpd2pi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2piEmitter<Mm, Xmm> for Assembler<'_> {
    fn cvtpd2pi(&mut self, dst: Mm, src: Xmm) {
        self.emit_n(
            InstId::Cvtpd2pi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2piEmitter<Mm, Mem> for Assembler<'_> {
    fn cvtpd2pi(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Cvtpd2pi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2psEmitter<Vec, Vec> for Assembler<'_> {
    fn cvtpd2ps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Cvtpd2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2psEmitter<Xmm, Xmm> for Assembler<'_> {
    fn cvtpd2ps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Cvtpd2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2psEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtpd2ps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Cvtpd2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtpd2ps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Cvtpd2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtpi2pdEmitter<Vec, Mm> for Assembler<'_> {
    fn cvtpi2pd(&mut self, dst: Vec, src: Mm) {
        self.emit_n(
            InstId::Cvtpi2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtpi2pdEmitter<Xmm, Mm> for Assembler<'_> {
    fn cvtpi2pd(&mut self, dst: Xmm, src: Mm) {
        self.emit_n(
            InstId::Cvtpi2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtpi2pdEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtpi2pd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Cvtpi2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtpi2pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtpi2pd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Cvtpi2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtpi2psEmitter<Vec, Mm> for Assembler<'_> {
    fn cvtpi2ps(&mut self, dst: Vec, src: Mm) {
        self.emit_n(
            InstId::Cvtpi2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvtpi2psEmitter<Xmm, Mm> for Assembler<'_> {
    fn cvtpi2ps(&mut self, dst: Xmm, src: Mm) {
        self.emit_n(
            InstId::Cvtpi2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvtpi2psEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtpi2ps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Cvtpi2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvtpi2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtpi2ps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Cvtpi2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvtps2dqEmitter<Vec, Vec> for Assembler<'_> {
    fn cvtps2dq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Cvtps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtps2dqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn cvtps2dq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Cvtps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtps2dqEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtps2dq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Cvtps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtps2dqEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtps2dq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Cvtps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtps2pdEmitter<Vec, Vec> for Assembler<'_> {
    fn cvtps2pd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Cvtps2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtps2pdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn cvtps2pd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Cvtps2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtps2pdEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtps2pd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Cvtps2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtps2pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtps2pd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Cvtps2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtps2piEmitter<Mm, Vec> for Assembler<'_> {
    fn cvtps2pi(&mut self, dst: Mm, src: Vec) {
        self.emit_n(
            InstId::Cvtps2pi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvtps2piEmitter<Mm, Xmm> for Assembler<'_> {
    fn cvtps2pi(&mut self, dst: Mm, src: Xmm) {
        self.emit_n(
            InstId::Cvtps2pi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvtps2piEmitter<Mm, Mem> for Assembler<'_> {
    fn cvtps2pi(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Cvtps2pi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvtsd2siEmitter<Gp, Vec> for Assembler<'_> {
    fn cvtsd2si(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Cvtsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtsd2siEmitter<Gpd, Xmm> for Assembler<'_> {
    fn cvtsd2si(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Cvtsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtsd2siEmitter<Gpq, Xmm> for Assembler<'_> {
    fn cvtsd2si(&mut self, dst: Gpq, src: Xmm) {
        self.emit_n(
            InstId::Cvtsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtsd2siEmitter<Gp, Mem> for Assembler<'_> {
    fn cvtsd2si(&mut self, dst: Gp, src: Mem) {
        self.emit_n(
            InstId::Cvtsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtsd2siEmitter<Gpd, Mem> for Assembler<'_> {
    fn cvtsd2si(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(
            InstId::Cvtsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtsd2siEmitter<Gpq, Mem> for Assembler<'_> {
    fn cvtsd2si(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(
            InstId::Cvtsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtsd2ssEmitter<Vec, Vec> for Assembler<'_> {
    fn cvtsd2ss(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Cvtsd2ss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtsd2ssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn cvtsd2ss(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Cvtsd2ss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtsd2ssEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtsd2ss(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Cvtsd2ss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtsd2ssEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtsd2ss(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Cvtsd2ss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtsi2sdEmitter<Vec, Gp> for Assembler<'_> {
    fn cvtsi2sd(&mut self, dst: Vec, src: Gp) {
        self.emit_n(
            InstId::Cvtsi2sd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtsi2sdEmitter<Xmm, Gpd> for Assembler<'_> {
    fn cvtsi2sd(&mut self, dst: Xmm, src: Gpd) {
        self.emit_n(
            InstId::Cvtsi2sd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtsi2sdEmitter<Xmm, Gpq> for Assembler<'_> {
    fn cvtsi2sd(&mut self, dst: Xmm, src: Gpq) {
        self.emit_n(
            InstId::Cvtsi2sd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtsi2sdEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtsi2sd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Cvtsi2sd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtsi2sdEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtsi2sd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Cvtsi2sd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtsi2ssEmitter<Vec, Gp> for Assembler<'_> {
    fn cvtsi2ss(&mut self, dst: Vec, src: Gp) {
        self.emit_n(
            InstId::Cvtsi2ss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvtsi2ssEmitter<Xmm, Gpd> for Assembler<'_> {
    fn cvtsi2ss(&mut self, dst: Xmm, src: Gpd) {
        self.emit_n(
            InstId::Cvtsi2ss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvtsi2ssEmitter<Xmm, Gpq> for Assembler<'_> {
    fn cvtsi2ss(&mut self, dst: Xmm, src: Gpq) {
        self.emit_n(
            InstId::Cvtsi2ss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvtsi2ssEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtsi2ss(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Cvtsi2ss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvtsi2ssEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtsi2ss(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Cvtsi2ss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvtss2sdEmitter<Vec, Vec> for Assembler<'_> {
    fn cvtss2sd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Cvtss2sd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtss2sdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn cvtss2sd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Cvtss2sd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtss2sdEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtss2sd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Cvtss2sd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtss2sdEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtss2sd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Cvtss2sd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvtss2siEmitter<Gp, Vec> for Assembler<'_> {
    fn cvtss2si(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Cvtss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvtss2siEmitter<Gpd, Xmm> for Assembler<'_> {
    fn cvtss2si(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Cvtss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvtss2siEmitter<Gpq, Xmm> for Assembler<'_> {
    fn cvtss2si(&mut self, dst: Gpq, src: Xmm) {
        self.emit_n(
            InstId::Cvtss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvtss2siEmitter<Gp, Mem> for Assembler<'_> {
    fn cvtss2si(&mut self, dst: Gp, src: Mem) {
        self.emit_n(
            InstId::Cvtss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvtss2siEmitter<Gpd, Mem> for Assembler<'_> {
    fn cvtss2si(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(
            InstId::Cvtss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvtss2siEmitter<Gpq, Mem> for Assembler<'_> {
    fn cvtss2si(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(
            InstId::Cvtss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvttpd2piEmitter<Mm, Vec> for Assembler<'_> {
    fn cvttpd2pi(&mut self, dst: Mm, src: Vec) {
        self.emit_n(
            InstId::Cvttpd2pi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvttpd2piEmitter<Mm, Xmm> for Assembler<'_> {
    fn cvttpd2pi(&mut self, dst: Mm, src: Xmm) {
        self.emit_n(
            InstId::Cvttpd2pi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvttpd2piEmitter<Mm, Mem> for Assembler<'_> {
    fn cvttpd2pi(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Cvttpd2pi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvttpd2dqEmitter<Vec, Vec> for Assembler<'_> {
    fn cvttpd2dq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Cvttpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvttpd2dqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn cvttpd2dq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Cvttpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvttpd2dqEmitter<Vec, Mem> for Assembler<'_> {
    fn cvttpd2dq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Cvttpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvttpd2dqEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvttpd2dq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Cvttpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvttps2dqEmitter<Vec, Vec> for Assembler<'_> {
    fn cvttps2dq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Cvttps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvttps2dqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn cvttps2dq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Cvttps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvttps2dqEmitter<Vec, Mem> for Assembler<'_> {
    fn cvttps2dq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Cvttps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvttps2dqEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvttps2dq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Cvttps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvttps2piEmitter<Mm, Vec> for Assembler<'_> {
    fn cvttps2pi(&mut self, dst: Mm, src: Vec) {
        self.emit_n(
            InstId::Cvttps2pi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvttps2piEmitter<Mm, Xmm> for Assembler<'_> {
    fn cvttps2pi(&mut self, dst: Mm, src: Xmm) {
        self.emit_n(
            InstId::Cvttps2pi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvttps2piEmitter<Mm, Mem> for Assembler<'_> {
    fn cvttps2pi(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Cvttps2pi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvttsd2siEmitter<Gp, Vec> for Assembler<'_> {
    fn cvttsd2si(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Cvttsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvttsd2siEmitter<Gpd, Xmm> for Assembler<'_> {
    fn cvttsd2si(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Cvttsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvttsd2siEmitter<Gpq, Xmm> for Assembler<'_> {
    fn cvttsd2si(&mut self, dst: Gpq, src: Xmm) {
        self.emit_n(
            InstId::Cvttsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvttsd2siEmitter<Gp, Mem> for Assembler<'_> {
    fn cvttsd2si(&mut self, dst: Gp, src: Mem) {
        self.emit_n(
            InstId::Cvttsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvttsd2siEmitter<Gpd, Mem> for Assembler<'_> {
    fn cvttsd2si(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(
            InstId::Cvttsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvttsd2siEmitter<Gpq, Mem> for Assembler<'_> {
    fn cvttsd2si(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(
            InstId::Cvttsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Cvttss2siEmitter<Gp, Vec> for Assembler<'_> {
    fn cvttss2si(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Cvttss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvttss2siEmitter<Gpd, Xmm> for Assembler<'_> {
    fn cvttss2si(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Cvttss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvttss2siEmitter<Gpq, Xmm> for Assembler<'_> {
    fn cvttss2si(&mut self, dst: Gpq, src: Xmm) {
        self.emit_n(
            InstId::Cvttss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvttss2siEmitter<Gp, Mem> for Assembler<'_> {
    fn cvttss2si(&mut self, dst: Gp, src: Mem) {
        self.emit_n(
            InstId::Cvttss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvttss2siEmitter<Gpd, Mem> for Assembler<'_> {
    fn cvttss2si(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(
            InstId::Cvttss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl Cvttss2siEmitter<Gpq, Mem> for Assembler<'_> {
    fn cvttss2si(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(
            InstId::Cvttss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl DivpdEmitter<Vec, Vec> for Assembler<'_> {
    fn divpd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Divpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl DivpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn divpd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Divpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl DivpdEmitter<Vec, Mem> for Assembler<'_> {
    fn divpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Divpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl DivpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn divpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Divpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl DivpsEmitter<Vec, Vec> for Assembler<'_> {
    fn divps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Divps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl DivpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn divps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Divps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl DivpsEmitter<Vec, Mem> for Assembler<'_> {
    fn divps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Divps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl DivpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn divps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Divps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl DivsdEmitter<Vec, Vec> for Assembler<'_> {
    fn divsd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Divsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl DivsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn divsd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Divsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl DivsdEmitter<Vec, Mem> for Assembler<'_> {
    fn divsd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Divsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl DivsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn divsd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Divsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl DivssEmitter<Vec, Vec> for Assembler<'_> {
    fn divss(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Divss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl DivssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn divss(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Divss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl DivssEmitter<Vec, Mem> for Assembler<'_> {
    fn divss(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Divss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl DivssEmitter<Xmm, Mem> for Assembler<'_> {
    fn divss(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Divss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl DppdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn dppd(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Dppd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> DppdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn dppd(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Dppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl DppdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn dppd(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Dppd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> DppdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn dppd(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Dppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl DppsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn dpps(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Dpps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> DppsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn dpps(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Dpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl DppsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn dpps(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Dpps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> DppsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn dpps(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Dpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl ExtractpsEmitter<Gp, Vec, Imm> for Assembler<'_> {
    fn extractps(&mut self, dst: Gp, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Extractps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> ExtractpsEmitter<Gpd, Xmm, U2> for Assembler<'_> {
    fn extractps(&mut self, dst: Gpd, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Extractps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl ExtractpsEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn extractps(&mut self, dst: Mem, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Extractps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> ExtractpsEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn extractps(&mut self, dst: Mem, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Extractps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl ExtrqEmitter<Vec, Vec> for Assembler<'_> {
    fn extrq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Extrq as u32, &[dst.as_operand(), src.as_operand()]); // SSE4A
    }
}

impl ExtrqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn extrq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Extrq as u32, &[dst.as_operand(), src.as_operand()]); // SSE4A
    }
}

impl Extrq3Emitter<Vec, Imm, Imm> for Assembler<'_> {
    fn extrq_3(&mut self, dst: Vec, imm: Imm, imm2: Imm) {
        self.emit_n(
            InstId::Extrq as u32,
            &[dst.as_operand(), imm.as_operand(), imm2.as_operand()],
        ); // SSE4A
    }
}

impl<U1: Into<Imm>, U2: Into<Imm>> Extrq3Emitter<Xmm, U1, U2> for Assembler<'_> {
    fn extrq_3(&mut self, dst: Xmm, src1: U1, src2: U2) {
        self.emit_n(
            InstId::Extrq as u32,
            &[
                dst.as_operand(),
                Into::<Imm>::into(src1).as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4A
    }
}

impl HaddpdEmitter<Vec, Vec> for Assembler<'_> {
    fn haddpd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Haddpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE3
    }
}

impl HaddpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn haddpd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Haddpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE3
    }
}

impl HaddpdEmitter<Vec, Mem> for Assembler<'_> {
    fn haddpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Haddpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE3
    }
}

impl HaddpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn haddpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Haddpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE3
    }
}

impl HaddpsEmitter<Vec, Vec> for Assembler<'_> {
    fn haddps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Haddps as u32, &[dst.as_operand(), src.as_operand()]); // SSE3
    }
}

impl HaddpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn haddps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Haddps as u32, &[dst.as_operand(), src.as_operand()]); // SSE3
    }
}

impl HaddpsEmitter<Vec, Mem> for Assembler<'_> {
    fn haddps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Haddps as u32, &[dst.as_operand(), src.as_operand()]); // SSE3
    }
}

impl HaddpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn haddps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Haddps as u32, &[dst.as_operand(), src.as_operand()]); // SSE3
    }
}

impl HsubpdEmitter<Vec, Vec> for Assembler<'_> {
    fn hsubpd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Hsubpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE3
    }
}

impl HsubpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn hsubpd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Hsubpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE3
    }
}

impl HsubpdEmitter<Vec, Mem> for Assembler<'_> {
    fn hsubpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Hsubpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE3
    }
}

impl HsubpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn hsubpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Hsubpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE3
    }
}

impl HsubpsEmitter<Vec, Vec> for Assembler<'_> {
    fn hsubps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Hsubps as u32, &[dst.as_operand(), src.as_operand()]); // SSE3
    }
}

impl HsubpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn hsubps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Hsubps as u32, &[dst.as_operand(), src.as_operand()]); // SSE3
    }
}

impl HsubpsEmitter<Vec, Mem> for Assembler<'_> {
    fn hsubps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Hsubps as u32, &[dst.as_operand(), src.as_operand()]); // SSE3
    }
}

impl HsubpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn hsubps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Hsubps as u32, &[dst.as_operand(), src.as_operand()]); // SSE3
    }
}

impl InsertpsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn insertps(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Insertps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> InsertpsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn insertps(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Insertps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl InsertpsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn insertps(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Insertps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> InsertpsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn insertps(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Insertps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl InsertqEmitter<Vec, Vec> for Assembler<'_> {
    fn insertq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Insertq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4A
    }
}

impl InsertqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn insertq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Insertq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4A
    }
}

impl Insertq4Emitter<Vec, Vec, Imm, Imm> for Assembler<'_> {
    fn insertq_4(&mut self, dst: Vec, src: Vec, imm: Imm, imm2: Imm) {
        self.emit_n(
            InstId::Insertq as u32,
            &[
                dst.as_operand(),
                src.as_operand(),
                imm.as_operand(),
                imm2.as_operand(),
            ],
        ); // SSE4A
    }
}

impl<U2: Into<Imm>, U3: Into<Imm>> Insertq4Emitter<Xmm, Xmm, U2, U3> for Assembler<'_> {
    fn insertq_4(&mut self, dst: Xmm, src1: Xmm, src2: U2, src3: U3) {
        self.emit_n(
            InstId::Insertq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // SSE4A
    }
}

impl LddquEmitter<Vec, Mem> for Assembler<'_> {
    fn lddqu(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Lddqu as u32, &[dst.as_operand(), src.as_operand()]); // SSE3
    }
}

impl LddquEmitter<Xmm, Mem> for Assembler<'_> {
    fn lddqu(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Lddqu as u32, &[dst.as_operand(), src.as_operand()]); // SSE3
    }
}

impl MaskmovqEmitter<Mm, Mm, Mem> for Assembler<'_> {
    fn maskmovq(&mut self, dst: Mm, src: Mm, dst2: Mem) {
        self.emit_n(
            InstId::Maskmovq as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // SSE [EXPLICIT]
    }
}

impl MaskmovdquEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn maskmovdqu(&mut self, dst: Vec, src: Vec, dst2: Mem) {
        self.emit_n(
            InstId::Maskmovdqu as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // SSE2 [EXPLICIT]
    }
}

impl MaskmovdquEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn maskmovdqu(&mut self, dst: Xmm, src: Xmm, dst2: Mem) {
        self.emit_n(
            InstId::Maskmovdqu as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // SSE2 [EXPLICIT]
    }
}

impl MaxpdEmitter<Vec, Vec> for Assembler<'_> {
    fn maxpd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Maxpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MaxpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn maxpd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Maxpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MaxpdEmitter<Vec, Mem> for Assembler<'_> {
    fn maxpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Maxpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MaxpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn maxpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Maxpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MaxpsEmitter<Vec, Vec> for Assembler<'_> {
    fn maxps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Maxps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MaxpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn maxps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Maxps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MaxpsEmitter<Vec, Mem> for Assembler<'_> {
    fn maxps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Maxps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MaxpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn maxps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Maxps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MaxsdEmitter<Vec, Vec> for Assembler<'_> {
    fn maxsd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Maxsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MaxsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn maxsd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Maxsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MaxsdEmitter<Vec, Mem> for Assembler<'_> {
    fn maxsd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Maxsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MaxsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn maxsd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Maxsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MaxssEmitter<Vec, Vec> for Assembler<'_> {
    fn maxss(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Maxss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MaxssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn maxss(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Maxss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MaxssEmitter<Vec, Mem> for Assembler<'_> {
    fn maxss(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Maxss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MaxssEmitter<Xmm, Mem> for Assembler<'_> {
    fn maxss(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Maxss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MinpdEmitter<Vec, Vec> for Assembler<'_> {
    fn minpd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Minpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MinpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn minpd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Minpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MinpdEmitter<Vec, Mem> for Assembler<'_> {
    fn minpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Minpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MinpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn minpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Minpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MinpsEmitter<Vec, Vec> for Assembler<'_> {
    fn minps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Minps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MinpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn minps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Minps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MinpsEmitter<Vec, Mem> for Assembler<'_> {
    fn minps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Minps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MinpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn minps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Minps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MinsdEmitter<Vec, Vec> for Assembler<'_> {
    fn minsd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Minsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MinsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn minsd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Minsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MinsdEmitter<Vec, Mem> for Assembler<'_> {
    fn minsd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Minsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MinsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn minsd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Minsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MinssEmitter<Vec, Vec> for Assembler<'_> {
    fn minss(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Minss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MinssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn minss(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Minss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MinssEmitter<Vec, Mem> for Assembler<'_> {
    fn minss(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Minss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MinssEmitter<Xmm, Mem> for Assembler<'_> {
    fn minss(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Minss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovapdEmitter<Vec, Vec> for Assembler<'_> {
    fn movapd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Movapd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovapdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movapd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Movapd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovapdEmitter<Vec, Mem> for Assembler<'_> {
    fn movapd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Movapd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovapdEmitter<Xmm, Mem> for Assembler<'_> {
    fn movapd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Movapd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovapdEmitter<Mem, Vec> for Assembler<'_> {
    fn movapd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(InstId::Movapd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovapdEmitter<Mem, Xmm> for Assembler<'_> {
    fn movapd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(InstId::Movapd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovapsEmitter<Vec, Vec> for Assembler<'_> {
    fn movaps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Movaps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovapsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movaps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Movaps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovapsEmitter<Vec, Mem> for Assembler<'_> {
    fn movaps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Movaps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovapsEmitter<Xmm, Mem> for Assembler<'_> {
    fn movaps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Movaps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovapsEmitter<Mem, Vec> for Assembler<'_> {
    fn movaps(&mut self, dst: Mem, src: Vec) {
        self.emit_n(InstId::Movaps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovapsEmitter<Mem, Xmm> for Assembler<'_> {
    fn movaps(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(InstId::Movaps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovdEmitter<Mem, Mm> for Assembler<'_> {
    fn movd(&mut self, dst: Mem, src: Mm) {
        self.emit_n(InstId::Movd as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl MovdEmitter<Mem, Vec> for Assembler<'_> {
    fn movd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(InstId::Movd as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovdEmitter<Mem, Xmm> for Assembler<'_> {
    fn movd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(InstId::Movd as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovdEmitter<Gp, Mm> for Assembler<'_> {
    fn movd(&mut self, dst: Gp, src: Mm) {
        self.emit_n(InstId::Movd as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl MovdEmitter<Gpd, Mm> for Assembler<'_> {
    fn movd(&mut self, dst: Gpd, src: Mm) {
        self.emit_n(InstId::Movd as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl MovdEmitter<Gp, Vec> for Assembler<'_> {
    fn movd(&mut self, dst: Gp, src: Vec) {
        self.emit_n(InstId::Movd as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovdEmitter<Gpd, Xmm> for Assembler<'_> {
    fn movd(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(InstId::Movd as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovdEmitter<Mm, Mem> for Assembler<'_> {
    fn movd(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Movd as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl MovdEmitter<Vec, Mem> for Assembler<'_> {
    fn movd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Movd as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovdEmitter<Xmm, Mem> for Assembler<'_> {
    fn movd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Movd as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovdEmitter<Mm, Gp> for Assembler<'_> {
    fn movd(&mut self, dst: Mm, src: Gp) {
        self.emit_n(InstId::Movd as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl MovdEmitter<Mm, Gpd> for Assembler<'_> {
    fn movd(&mut self, dst: Mm, src: Gpd) {
        self.emit_n(InstId::Movd as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl MovdEmitter<Vec, Gp> for Assembler<'_> {
    fn movd(&mut self, dst: Vec, src: Gp) {
        self.emit_n(InstId::Movd as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovdEmitter<Xmm, Gpd> for Assembler<'_> {
    fn movd(&mut self, dst: Xmm, src: Gpd) {
        self.emit_n(InstId::Movd as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovddupEmitter<Vec, Vec> for Assembler<'_> {
    fn movddup(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Movddup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE3
    }
}

impl MovddupEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movddup(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Movddup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE3
    }
}

impl MovddupEmitter<Vec, Mem> for Assembler<'_> {
    fn movddup(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Movddup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE3
    }
}

impl MovddupEmitter<Xmm, Mem> for Assembler<'_> {
    fn movddup(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Movddup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE3
    }
}

impl Movdq2qEmitter<Mm, Vec> for Assembler<'_> {
    fn movdq2q(&mut self, dst: Mm, src: Vec) {
        self.emit_n(
            InstId::Movdq2q as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Movdq2qEmitter<Mm, Xmm> for Assembler<'_> {
    fn movdq2q(&mut self, dst: Mm, src: Xmm) {
        self.emit_n(
            InstId::Movdq2q as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl MovdqaEmitter<Vec, Vec> for Assembler<'_> {
    fn movdqa(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Movdqa as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovdqaEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movdqa(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Movdqa as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovdqaEmitter<Vec, Mem> for Assembler<'_> {
    fn movdqa(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Movdqa as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovdqaEmitter<Xmm, Mem> for Assembler<'_> {
    fn movdqa(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Movdqa as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovdqaEmitter<Mem, Vec> for Assembler<'_> {
    fn movdqa(&mut self, dst: Mem, src: Vec) {
        self.emit_n(InstId::Movdqa as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovdqaEmitter<Mem, Xmm> for Assembler<'_> {
    fn movdqa(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(InstId::Movdqa as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovdquEmitter<Vec, Vec> for Assembler<'_> {
    fn movdqu(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Movdqu as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovdquEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movdqu(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Movdqu as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovdquEmitter<Vec, Mem> for Assembler<'_> {
    fn movdqu(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Movdqu as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovdquEmitter<Xmm, Mem> for Assembler<'_> {
    fn movdqu(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Movdqu as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovdquEmitter<Mem, Vec> for Assembler<'_> {
    fn movdqu(&mut self, dst: Mem, src: Vec) {
        self.emit_n(InstId::Movdqu as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovdquEmitter<Mem, Xmm> for Assembler<'_> {
    fn movdqu(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(InstId::Movdqu as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovhlpsEmitter<Vec, Vec> for Assembler<'_> {
    fn movhlps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Movhlps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl MovhlpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movhlps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Movhlps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl MovhpdEmitter<Vec, Mem> for Assembler<'_> {
    fn movhpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Movhpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovhpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn movhpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Movhpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovhpdEmitter<Mem, Vec> for Assembler<'_> {
    fn movhpd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(InstId::Movhpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovhpdEmitter<Mem, Xmm> for Assembler<'_> {
    fn movhpd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(InstId::Movhpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovhpsEmitter<Vec, Mem> for Assembler<'_> {
    fn movhps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Movhps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovhpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn movhps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Movhps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovhpsEmitter<Mem, Vec> for Assembler<'_> {
    fn movhps(&mut self, dst: Mem, src: Vec) {
        self.emit_n(InstId::Movhps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovhpsEmitter<Mem, Xmm> for Assembler<'_> {
    fn movhps(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(InstId::Movhps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovlhpsEmitter<Vec, Vec> for Assembler<'_> {
    fn movlhps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Movlhps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl MovlhpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movlhps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Movlhps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl MovlpdEmitter<Vec, Mem> for Assembler<'_> {
    fn movlpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Movlpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovlpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn movlpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Movlpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovlpdEmitter<Mem, Vec> for Assembler<'_> {
    fn movlpd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(InstId::Movlpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovlpdEmitter<Mem, Xmm> for Assembler<'_> {
    fn movlpd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(InstId::Movlpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovlpsEmitter<Vec, Mem> for Assembler<'_> {
    fn movlps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Movlps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovlpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn movlps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Movlps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovlpsEmitter<Mem, Vec> for Assembler<'_> {
    fn movlps(&mut self, dst: Mem, src: Vec) {
        self.emit_n(InstId::Movlps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovlpsEmitter<Mem, Xmm> for Assembler<'_> {
    fn movlps(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(InstId::Movlps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovmskpsEmitter<Gp, Vec> for Assembler<'_> {
    fn movmskps(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Movmskps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl MovmskpsEmitter<Gpd, Xmm> for Assembler<'_> {
    fn movmskps(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Movmskps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl MovmskpdEmitter<Gp, Vec> for Assembler<'_> {
    fn movmskpd(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Movmskpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl MovmskpdEmitter<Gpd, Xmm> for Assembler<'_> {
    fn movmskpd(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Movmskpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl MovntdqEmitter<Mem, Vec> for Assembler<'_> {
    fn movntdq(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Movntdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl MovntdqEmitter<Mem, Xmm> for Assembler<'_> {
    fn movntdq(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Movntdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl MovntdqaEmitter<Vec, Mem> for Assembler<'_> {
    fn movntdqa(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Movntdqa as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl MovntdqaEmitter<Xmm, Mem> for Assembler<'_> {
    fn movntdqa(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Movntdqa as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl MovntpdEmitter<Mem, Vec> for Assembler<'_> {
    fn movntpd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Movntpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl MovntpdEmitter<Mem, Xmm> for Assembler<'_> {
    fn movntpd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Movntpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl MovntpsEmitter<Mem, Vec> for Assembler<'_> {
    fn movntps(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Movntps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl MovntpsEmitter<Mem, Xmm> for Assembler<'_> {
    fn movntps(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Movntps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl MovntsdEmitter<Mem, Vec> for Assembler<'_> {
    fn movntsd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Movntsd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4A
    }
}

impl MovntsdEmitter<Mem, Xmm> for Assembler<'_> {
    fn movntsd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Movntsd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4A
    }
}

impl MovntssEmitter<Mem, Vec> for Assembler<'_> {
    fn movntss(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Movntss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4A
    }
}

impl MovntssEmitter<Mem, Xmm> for Assembler<'_> {
    fn movntss(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Movntss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4A
    }
}

impl MovntqEmitter<Mem, Mm> for Assembler<'_> {
    fn movntq(&mut self, dst: Mem, src: Mm) {
        self.emit_n(InstId::Movntq as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovqEmitter<Mm, Mm> for Assembler<'_> {
    fn movq(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Movq as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl MovqEmitter<Vec, Vec> for Assembler<'_> {
    fn movq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Movq as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Movq as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovqEmitter<Mem, Mm> for Assembler<'_> {
    fn movq(&mut self, dst: Mem, src: Mm) {
        self.emit_n(InstId::Movq as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl MovqEmitter<Mem, Vec> for Assembler<'_> {
    fn movq(&mut self, dst: Mem, src: Vec) {
        self.emit_n(InstId::Movq as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovqEmitter<Mem, Xmm> for Assembler<'_> {
    fn movq(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(InstId::Movq as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovqEmitter<Mm, Mem> for Assembler<'_> {
    fn movq(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Movq as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl MovqEmitter<Vec, Mem> for Assembler<'_> {
    fn movq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Movq as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovqEmitter<Xmm, Mem> for Assembler<'_> {
    fn movq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Movq as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovqEmitter<Gp, Mm> for Assembler<'_> {
    fn movq(&mut self, dst: Gp, src: Mm) {
        self.emit_n(InstId::Movq as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl MovqEmitter<Gpq, Mm> for Assembler<'_> {
    fn movq(&mut self, dst: Gpq, src: Mm) {
        self.emit_n(InstId::Movq as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl MovqEmitter<Gp, Vec> for Assembler<'_> {
    fn movq(&mut self, dst: Gp, src: Vec) {
        self.emit_n(InstId::Movq as u32, &[dst.as_operand(), src.as_operand()]); // SSE+X64.
    }
}

impl MovqEmitter<Gpq, Xmm> for Assembler<'_> {
    fn movq(&mut self, dst: Gpq, src: Xmm) {
        self.emit_n(InstId::Movq as u32, &[dst.as_operand(), src.as_operand()]); // SSE+X64.
    }
}

impl MovqEmitter<Mm, Gp> for Assembler<'_> {
    fn movq(&mut self, dst: Mm, src: Gp) {
        self.emit_n(InstId::Movq as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl MovqEmitter<Mm, Gpq> for Assembler<'_> {
    fn movq(&mut self, dst: Mm, src: Gpq) {
        self.emit_n(InstId::Movq as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl MovqEmitter<Vec, Gp> for Assembler<'_> {
    fn movq(&mut self, dst: Vec, src: Gp) {
        self.emit_n(InstId::Movq as u32, &[dst.as_operand(), src.as_operand()]); // SSE+X64.
    }
}

impl MovqEmitter<Xmm, Gpq> for Assembler<'_> {
    fn movq(&mut self, dst: Xmm, src: Gpq) {
        self.emit_n(InstId::Movq as u32, &[dst.as_operand(), src.as_operand()]); // SSE+X64.
    }
}

impl Movq2dqEmitter<Vec, Mm> for Assembler<'_> {
    fn movq2dq(&mut self, dst: Vec, src: Mm) {
        self.emit_n(
            InstId::Movq2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl Movq2dqEmitter<Xmm, Mm> for Assembler<'_> {
    fn movq2dq(&mut self, dst: Xmm, src: Mm) {
        self.emit_n(
            InstId::Movq2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl MovsdEmitter<Vec, Vec> for Assembler<'_> {
    fn movsd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Movsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movsd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Movsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovsdEmitter<Vec, Mem> for Assembler<'_> {
    fn movsd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Movsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn movsd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Movsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovsdEmitter<Mem, Vec> for Assembler<'_> {
    fn movsd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(InstId::Movsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovsdEmitter<Mem, Xmm> for Assembler<'_> {
    fn movsd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(InstId::Movsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovshdupEmitter<Vec, Vec> for Assembler<'_> {
    fn movshdup(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Movshdup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE3
    }
}

impl MovshdupEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movshdup(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Movshdup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE3
    }
}

impl MovshdupEmitter<Vec, Mem> for Assembler<'_> {
    fn movshdup(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Movshdup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE3
    }
}

impl MovshdupEmitter<Xmm, Mem> for Assembler<'_> {
    fn movshdup(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Movshdup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE3
    }
}

impl MovsldupEmitter<Vec, Vec> for Assembler<'_> {
    fn movsldup(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Movsldup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE3
    }
}

impl MovsldupEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movsldup(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Movsldup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE3
    }
}

impl MovsldupEmitter<Vec, Mem> for Assembler<'_> {
    fn movsldup(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Movsldup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE3
    }
}

impl MovsldupEmitter<Xmm, Mem> for Assembler<'_> {
    fn movsldup(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Movsldup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE3
    }
}

impl MovssEmitter<Vec, Vec> for Assembler<'_> {
    fn movss(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Movss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movss(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Movss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovssEmitter<Vec, Mem> for Assembler<'_> {
    fn movss(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Movss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovssEmitter<Xmm, Mem> for Assembler<'_> {
    fn movss(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Movss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovssEmitter<Mem, Vec> for Assembler<'_> {
    fn movss(&mut self, dst: Mem, src: Vec) {
        self.emit_n(InstId::Movss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovssEmitter<Mem, Xmm> for Assembler<'_> {
    fn movss(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(InstId::Movss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovupdEmitter<Vec, Vec> for Assembler<'_> {
    fn movupd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Movupd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovupdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movupd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Movupd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovupdEmitter<Vec, Mem> for Assembler<'_> {
    fn movupd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Movupd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovupdEmitter<Xmm, Mem> for Assembler<'_> {
    fn movupd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Movupd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovupdEmitter<Mem, Vec> for Assembler<'_> {
    fn movupd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(InstId::Movupd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovupdEmitter<Mem, Xmm> for Assembler<'_> {
    fn movupd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(InstId::Movupd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MovupsEmitter<Vec, Vec> for Assembler<'_> {
    fn movups(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Movups as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovupsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movups(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Movups as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovupsEmitter<Vec, Mem> for Assembler<'_> {
    fn movups(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Movups as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovupsEmitter<Xmm, Mem> for Assembler<'_> {
    fn movups(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Movups as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovupsEmitter<Mem, Vec> for Assembler<'_> {
    fn movups(&mut self, dst: Mem, src: Vec) {
        self.emit_n(InstId::Movups as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MovupsEmitter<Mem, Xmm> for Assembler<'_> {
    fn movups(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(InstId::Movups as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MpsadbwEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn mpsadbw(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Mpsadbw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> MpsadbwEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn mpsadbw(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Mpsadbw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl MpsadbwEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn mpsadbw(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Mpsadbw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> MpsadbwEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn mpsadbw(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Mpsadbw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl MulpdEmitter<Vec, Vec> for Assembler<'_> {
    fn mulpd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Mulpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MulpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn mulpd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Mulpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MulpdEmitter<Vec, Mem> for Assembler<'_> {
    fn mulpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Mulpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MulpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn mulpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Mulpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MulpsEmitter<Vec, Vec> for Assembler<'_> {
    fn mulps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Mulps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MulpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn mulps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Mulps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MulpsEmitter<Vec, Mem> for Assembler<'_> {
    fn mulps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Mulps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MulpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn mulps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Mulps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MulsdEmitter<Vec, Vec> for Assembler<'_> {
    fn mulsd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Mulsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MulsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn mulsd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Mulsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MulsdEmitter<Vec, Mem> for Assembler<'_> {
    fn mulsd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Mulsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MulsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn mulsd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Mulsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl MulssEmitter<Vec, Vec> for Assembler<'_> {
    fn mulss(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Mulss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MulssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn mulss(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Mulss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MulssEmitter<Vec, Mem> for Assembler<'_> {
    fn mulss(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Mulss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl MulssEmitter<Xmm, Mem> for Assembler<'_> {
    fn mulss(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Mulss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl OrpdEmitter<Vec, Vec> for Assembler<'_> {
    fn orpd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Orpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl OrpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn orpd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Orpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl OrpdEmitter<Vec, Mem> for Assembler<'_> {
    fn orpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Orpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl OrpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn orpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Orpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl OrpsEmitter<Vec, Vec> for Assembler<'_> {
    fn orps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Orps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl OrpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn orps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Orps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl OrpsEmitter<Vec, Mem> for Assembler<'_> {
    fn orps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Orps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl OrpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn orps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Orps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl PackssdwEmitter<Mm, Mm> for Assembler<'_> {
    fn packssdw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Packssdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PackssdwEmitter<Mm, Mem> for Assembler<'_> {
    fn packssdw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Packssdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PackssdwEmitter<Vec, Vec> for Assembler<'_> {
    fn packssdw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Packssdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PackssdwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn packssdw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Packssdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PackssdwEmitter<Vec, Mem> for Assembler<'_> {
    fn packssdw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Packssdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PackssdwEmitter<Xmm, Mem> for Assembler<'_> {
    fn packssdw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Packssdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PacksswbEmitter<Mm, Mm> for Assembler<'_> {
    fn packsswb(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Packsswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PacksswbEmitter<Mm, Mem> for Assembler<'_> {
    fn packsswb(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Packsswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PacksswbEmitter<Vec, Vec> for Assembler<'_> {
    fn packsswb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Packsswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PacksswbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn packsswb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Packsswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PacksswbEmitter<Vec, Mem> for Assembler<'_> {
    fn packsswb(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Packsswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PacksswbEmitter<Xmm, Mem> for Assembler<'_> {
    fn packsswb(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Packsswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PackusdwEmitter<Vec, Vec> for Assembler<'_> {
    fn packusdw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Packusdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PackusdwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn packusdw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Packusdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PackusdwEmitter<Vec, Mem> for Assembler<'_> {
    fn packusdw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Packusdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PackusdwEmitter<Xmm, Mem> for Assembler<'_> {
    fn packusdw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Packusdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PackuswbEmitter<Mm, Mm> for Assembler<'_> {
    fn packuswb(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Packuswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PackuswbEmitter<Mm, Mem> for Assembler<'_> {
    fn packuswb(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Packuswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PackuswbEmitter<Vec, Vec> for Assembler<'_> {
    fn packuswb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Packuswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PackuswbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn packuswb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Packuswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PackuswbEmitter<Vec, Mem> for Assembler<'_> {
    fn packuswb(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Packuswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PackuswbEmitter<Xmm, Mem> for Assembler<'_> {
    fn packuswb(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Packuswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PabsbEmitter<Mm, Mm> for Assembler<'_> {
    fn pabsb(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pabsb as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PabsbEmitter<Mm, Mem> for Assembler<'_> {
    fn pabsb(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pabsb as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PabsbEmitter<Vec, Vec> for Assembler<'_> {
    fn pabsb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pabsb as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PabsbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pabsb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pabsb as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PabsbEmitter<Vec, Mem> for Assembler<'_> {
    fn pabsb(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pabsb as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PabsbEmitter<Xmm, Mem> for Assembler<'_> {
    fn pabsb(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pabsb as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PabsdEmitter<Mm, Mm> for Assembler<'_> {
    fn pabsd(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pabsd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PabsdEmitter<Mm, Mem> for Assembler<'_> {
    fn pabsd(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pabsd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PabsdEmitter<Vec, Vec> for Assembler<'_> {
    fn pabsd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pabsd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PabsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pabsd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pabsd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PabsdEmitter<Vec, Mem> for Assembler<'_> {
    fn pabsd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pabsd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PabsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn pabsd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pabsd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PabswEmitter<Mm, Mm> for Assembler<'_> {
    fn pabsw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pabsw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PabswEmitter<Mm, Mem> for Assembler<'_> {
    fn pabsw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pabsw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PabswEmitter<Vec, Vec> for Assembler<'_> {
    fn pabsw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pabsw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PabswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pabsw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pabsw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PabswEmitter<Vec, Mem> for Assembler<'_> {
    fn pabsw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pabsw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PabswEmitter<Xmm, Mem> for Assembler<'_> {
    fn pabsw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pabsw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PaddbEmitter<Mm, Mm> for Assembler<'_> {
    fn paddb(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Paddb as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PaddbEmitter<Mm, Mem> for Assembler<'_> {
    fn paddb(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Paddb as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PaddbEmitter<Vec, Vec> for Assembler<'_> {
    fn paddb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Paddb as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PaddbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn paddb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Paddb as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PaddbEmitter<Vec, Mem> for Assembler<'_> {
    fn paddb(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Paddb as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PaddbEmitter<Xmm, Mem> for Assembler<'_> {
    fn paddb(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Paddb as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PadddEmitter<Mm, Mm> for Assembler<'_> {
    fn paddd(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Paddd as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PadddEmitter<Mm, Mem> for Assembler<'_> {
    fn paddd(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Paddd as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PadddEmitter<Vec, Vec> for Assembler<'_> {
    fn paddd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Paddd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PadddEmitter<Xmm, Xmm> for Assembler<'_> {
    fn paddd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Paddd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PadddEmitter<Vec, Mem> for Assembler<'_> {
    fn paddd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Paddd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PadddEmitter<Xmm, Mem> for Assembler<'_> {
    fn paddd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Paddd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PaddqEmitter<Mm, Mm> for Assembler<'_> {
    fn paddq(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Paddq as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PaddqEmitter<Mm, Mem> for Assembler<'_> {
    fn paddq(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Paddq as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PaddqEmitter<Vec, Vec> for Assembler<'_> {
    fn paddq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Paddq as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PaddqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn paddq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Paddq as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PaddqEmitter<Vec, Mem> for Assembler<'_> {
    fn paddq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Paddq as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PaddqEmitter<Xmm, Mem> for Assembler<'_> {
    fn paddq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Paddq as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PaddsbEmitter<Mm, Mm> for Assembler<'_> {
    fn paddsb(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Paddsb as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PaddsbEmitter<Mm, Mem> for Assembler<'_> {
    fn paddsb(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Paddsb as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PaddsbEmitter<Vec, Vec> for Assembler<'_> {
    fn paddsb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Paddsb as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PaddsbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn paddsb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Paddsb as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PaddsbEmitter<Vec, Mem> for Assembler<'_> {
    fn paddsb(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Paddsb as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PaddsbEmitter<Xmm, Mem> for Assembler<'_> {
    fn paddsb(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Paddsb as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PaddswEmitter<Mm, Mm> for Assembler<'_> {
    fn paddsw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Paddsw as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PaddswEmitter<Mm, Mem> for Assembler<'_> {
    fn paddsw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Paddsw as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PaddswEmitter<Vec, Vec> for Assembler<'_> {
    fn paddsw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Paddsw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PaddswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn paddsw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Paddsw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PaddswEmitter<Vec, Mem> for Assembler<'_> {
    fn paddsw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Paddsw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PaddswEmitter<Xmm, Mem> for Assembler<'_> {
    fn paddsw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Paddsw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PaddusbEmitter<Mm, Mm> for Assembler<'_> {
    fn paddusb(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Paddusb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PaddusbEmitter<Mm, Mem> for Assembler<'_> {
    fn paddusb(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Paddusb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PaddusbEmitter<Vec, Vec> for Assembler<'_> {
    fn paddusb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Paddusb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PaddusbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn paddusb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Paddusb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PaddusbEmitter<Vec, Mem> for Assembler<'_> {
    fn paddusb(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Paddusb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PaddusbEmitter<Xmm, Mem> for Assembler<'_> {
    fn paddusb(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Paddusb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PadduswEmitter<Mm, Mm> for Assembler<'_> {
    fn paddusw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Paddusw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PadduswEmitter<Mm, Mem> for Assembler<'_> {
    fn paddusw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Paddusw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PadduswEmitter<Vec, Vec> for Assembler<'_> {
    fn paddusw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Paddusw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PadduswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn paddusw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Paddusw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PadduswEmitter<Vec, Mem> for Assembler<'_> {
    fn paddusw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Paddusw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PadduswEmitter<Xmm, Mem> for Assembler<'_> {
    fn paddusw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Paddusw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PaddwEmitter<Mm, Mm> for Assembler<'_> {
    fn paddw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Paddw as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PaddwEmitter<Mm, Mem> for Assembler<'_> {
    fn paddw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Paddw as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PaddwEmitter<Vec, Vec> for Assembler<'_> {
    fn paddw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Paddw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PaddwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn paddw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Paddw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PaddwEmitter<Vec, Mem> for Assembler<'_> {
    fn paddw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Paddw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PaddwEmitter<Xmm, Mem> for Assembler<'_> {
    fn paddw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Paddw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl<U2: Into<Imm>> PalignrEmitter<Mm, Mm, U2> for Assembler<'_> {
    fn palignr(&mut self, dst: Mm, src1: Mm, src2: U2) {
        self.emit_n(
            InstId::Palignr as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSSE3
    }
}

impl<U2: Into<Imm>> PalignrEmitter<Mm, Mem, U2> for Assembler<'_> {
    fn palignr(&mut self, dst: Mm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Palignr as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSSE3
    }
}

impl PalignrEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn palignr(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Palignr as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSSE3
    }
}

impl<U2: Into<Imm>> PalignrEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn palignr(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Palignr as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSSE3
    }
}

impl PalignrEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn palignr(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Palignr as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSSE3
    }
}

impl<U2: Into<Imm>> PalignrEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn palignr(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Palignr as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSSE3
    }
}

impl PandEmitter<Mm, Mm> for Assembler<'_> {
    fn pand(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pand as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PandEmitter<Mm, Mem> for Assembler<'_> {
    fn pand(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pand as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PandEmitter<Vec, Vec> for Assembler<'_> {
    fn pand(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pand as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PandEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pand(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pand as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PandEmitter<Vec, Mem> for Assembler<'_> {
    fn pand(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pand as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PandEmitter<Xmm, Mem> for Assembler<'_> {
    fn pand(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pand as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PandnEmitter<Mm, Mm> for Assembler<'_> {
    fn pandn(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pandn as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PandnEmitter<Mm, Mem> for Assembler<'_> {
    fn pandn(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pandn as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PandnEmitter<Vec, Vec> for Assembler<'_> {
    fn pandn(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pandn as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PandnEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pandn(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pandn as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PandnEmitter<Vec, Mem> for Assembler<'_> {
    fn pandn(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pandn as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PandnEmitter<Xmm, Mem> for Assembler<'_> {
    fn pandn(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pandn as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PavgbEmitter<Mm, Mm> for Assembler<'_> {
    fn pavgb(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pavgb as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl PavgbEmitter<Mm, Mem> for Assembler<'_> {
    fn pavgb(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pavgb as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl PavgbEmitter<Vec, Vec> for Assembler<'_> {
    fn pavgb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pavgb as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PavgbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pavgb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pavgb as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PavgbEmitter<Vec, Mem> for Assembler<'_> {
    fn pavgb(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pavgb as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PavgbEmitter<Xmm, Mem> for Assembler<'_> {
    fn pavgb(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pavgb as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PavgwEmitter<Mm, Mm> for Assembler<'_> {
    fn pavgw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pavgw as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl PavgwEmitter<Mm, Mem> for Assembler<'_> {
    fn pavgw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pavgw as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl PavgwEmitter<Vec, Vec> for Assembler<'_> {
    fn pavgw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pavgw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PavgwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pavgw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pavgw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PavgwEmitter<Vec, Mem> for Assembler<'_> {
    fn pavgw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pavgw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PavgwEmitter<Xmm, Mem> for Assembler<'_> {
    fn pavgw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pavgw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PblendvbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn pblendvb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Pblendvb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl PblendvbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn pblendvb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Pblendvb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl PblendvbEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn pblendvb(&mut self, dst: Vec, src1: Mem, src2: Vec) {
        self.emit_n(
            InstId::Pblendvb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl PblendvbEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn pblendvb(&mut self, dst: Xmm, src1: Mem, src2: Xmm) {
        self.emit_n(
            InstId::Pblendvb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl PblendwEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn pblendw(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Pblendw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PblendwEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn pblendw(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Pblendw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PblendwEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pblendw(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Pblendw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PblendwEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn pblendw(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Pblendw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PclmulqdqEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn pclmulqdq(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Pclmulqdq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // PCLMULQDQ.
    }
}

impl<U2: Into<Imm>> PclmulqdqEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn pclmulqdq(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Pclmulqdq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // PCLMULQDQ.
    }
}

impl PclmulqdqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pclmulqdq(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Pclmulqdq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // PCLMULQDQ.
    }
}

impl<U2: Into<Imm>> PclmulqdqEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn pclmulqdq(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Pclmulqdq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // PCLMULQDQ.
    }
}

impl PcmpestriEmitter<Vec, Vec, Imm, Gp, Gp, Gp> for Assembler<'_> {
    fn pcmpestri(&mut self, dst: Vec, src1: Vec, imm: Imm, dst2: Gp, src2: Gp, src3: Gp) {
        self.emit_n(
            InstId::Pcmpestri as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                imm.as_operand(),
                dst2.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl<U2: Into<Imm>> PcmpestriEmitter<Xmm, Xmm, U2, Gpd, Gpd, Gpd> for Assembler<'_> {
    fn pcmpestri(&mut self, dst: Xmm, src1: Xmm, src2: U2, dst2: Gpd, src3: Gpd, src4: Gpd) {
        self.emit_n(
            InstId::Pcmpestri as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
                dst2.as_operand(),
                src3.as_operand(),
                src4.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl PcmpestriEmitter<Vec, Mem, Imm, Gp, Gp, Gp> for Assembler<'_> {
    fn pcmpestri(&mut self, dst: Vec, src1: Mem, imm: Imm, dst2: Gp, src2: Gp, src3: Gp) {
        self.emit_n(
            InstId::Pcmpestri as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                imm.as_operand(),
                dst2.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl<U2: Into<Imm>> PcmpestriEmitter<Xmm, Mem, U2, Gpd, Gpd, Gpd> for Assembler<'_> {
    fn pcmpestri(&mut self, dst: Xmm, src1: Mem, src2: U2, dst2: Gpd, src3: Gpd, src4: Gpd) {
        self.emit_n(
            InstId::Pcmpestri as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
                dst2.as_operand(),
                src3.as_operand(),
                src4.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl PcmpestrmEmitter<Vec, Vec, Imm, Vec, Gp, Gp> for Assembler<'_> {
    fn pcmpestrm(&mut self, dst: Vec, src1: Vec, imm: Imm, dst2: Vec, src2: Gp, src3: Gp) {
        self.emit_n(
            InstId::Pcmpestrm as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                imm.as_operand(),
                dst2.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl<U2: Into<Imm>> PcmpestrmEmitter<Xmm, Xmm, U2, Xmm, Gpd, Gpd> for Assembler<'_> {
    fn pcmpestrm(&mut self, dst: Xmm, src1: Xmm, src2: U2, dst2: Xmm, src3: Gpd, src4: Gpd) {
        self.emit_n(
            InstId::Pcmpestrm as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
                dst2.as_operand(),
                src3.as_operand(),
                src4.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl PcmpestrmEmitter<Vec, Mem, Imm, Vec, Gp, Gp> for Assembler<'_> {
    fn pcmpestrm(&mut self, dst: Vec, src1: Mem, imm: Imm, dst2: Vec, src2: Gp, src3: Gp) {
        self.emit_n(
            InstId::Pcmpestrm as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                imm.as_operand(),
                dst2.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl<U2: Into<Imm>> PcmpestrmEmitter<Xmm, Mem, U2, Xmm, Gpd, Gpd> for Assembler<'_> {
    fn pcmpestrm(&mut self, dst: Xmm, src1: Mem, src2: U2, dst2: Xmm, src3: Gpd, src4: Gpd) {
        self.emit_n(
            InstId::Pcmpestrm as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
                dst2.as_operand(),
                src3.as_operand(),
                src4.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl PcmpeqbEmitter<Mm, Mm> for Assembler<'_> {
    fn pcmpeqb(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pcmpeqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PcmpeqbEmitter<Mm, Mem> for Assembler<'_> {
    fn pcmpeqb(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pcmpeqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PcmpeqbEmitter<Vec, Vec> for Assembler<'_> {
    fn pcmpeqb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pcmpeqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pcmpeqb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pcmpeqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqbEmitter<Vec, Mem> for Assembler<'_> {
    fn pcmpeqb(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pcmpeqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqbEmitter<Xmm, Mem> for Assembler<'_> {
    fn pcmpeqb(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pcmpeqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqdEmitter<Mm, Mm> for Assembler<'_> {
    fn pcmpeqd(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pcmpeqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PcmpeqdEmitter<Mm, Mem> for Assembler<'_> {
    fn pcmpeqd(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pcmpeqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PcmpeqdEmitter<Vec, Vec> for Assembler<'_> {
    fn pcmpeqd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pcmpeqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pcmpeqd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pcmpeqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqdEmitter<Vec, Mem> for Assembler<'_> {
    fn pcmpeqd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pcmpeqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqdEmitter<Xmm, Mem> for Assembler<'_> {
    fn pcmpeqd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pcmpeqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqqEmitter<Vec, Vec> for Assembler<'_> {
    fn pcmpeqq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pcmpeqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PcmpeqqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pcmpeqq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pcmpeqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PcmpeqqEmitter<Vec, Mem> for Assembler<'_> {
    fn pcmpeqq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pcmpeqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PcmpeqqEmitter<Xmm, Mem> for Assembler<'_> {
    fn pcmpeqq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pcmpeqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PcmpeqwEmitter<Mm, Mm> for Assembler<'_> {
    fn pcmpeqw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pcmpeqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PcmpeqwEmitter<Mm, Mem> for Assembler<'_> {
    fn pcmpeqw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pcmpeqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PcmpeqwEmitter<Vec, Vec> for Assembler<'_> {
    fn pcmpeqw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pcmpeqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pcmpeqw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pcmpeqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqwEmitter<Vec, Mem> for Assembler<'_> {
    fn pcmpeqw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pcmpeqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqwEmitter<Xmm, Mem> for Assembler<'_> {
    fn pcmpeqw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pcmpeqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtbEmitter<Mm, Mm> for Assembler<'_> {
    fn pcmpgtb(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pcmpgtb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PcmpgtbEmitter<Mm, Mem> for Assembler<'_> {
    fn pcmpgtb(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pcmpgtb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PcmpgtbEmitter<Vec, Vec> for Assembler<'_> {
    fn pcmpgtb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pcmpgtb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pcmpgtb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pcmpgtb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtbEmitter<Vec, Mem> for Assembler<'_> {
    fn pcmpgtb(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pcmpgtb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtbEmitter<Xmm, Mem> for Assembler<'_> {
    fn pcmpgtb(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pcmpgtb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtdEmitter<Mm, Mm> for Assembler<'_> {
    fn pcmpgtd(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pcmpgtd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PcmpgtdEmitter<Mm, Mem> for Assembler<'_> {
    fn pcmpgtd(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pcmpgtd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PcmpgtdEmitter<Vec, Vec> for Assembler<'_> {
    fn pcmpgtd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pcmpgtd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pcmpgtd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pcmpgtd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtdEmitter<Vec, Mem> for Assembler<'_> {
    fn pcmpgtd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pcmpgtd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtdEmitter<Xmm, Mem> for Assembler<'_> {
    fn pcmpgtd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pcmpgtd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtqEmitter<Vec, Vec> for Assembler<'_> {
    fn pcmpgtq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pcmpgtq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_2.
    }
}

impl PcmpgtqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pcmpgtq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pcmpgtq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_2.
    }
}

impl PcmpgtqEmitter<Vec, Mem> for Assembler<'_> {
    fn pcmpgtq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pcmpgtq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_2.
    }
}

impl PcmpgtqEmitter<Xmm, Mem> for Assembler<'_> {
    fn pcmpgtq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pcmpgtq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_2.
    }
}

impl PcmpgtwEmitter<Mm, Mm> for Assembler<'_> {
    fn pcmpgtw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pcmpgtw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PcmpgtwEmitter<Mm, Mem> for Assembler<'_> {
    fn pcmpgtw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pcmpgtw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PcmpgtwEmitter<Vec, Vec> for Assembler<'_> {
    fn pcmpgtw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pcmpgtw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pcmpgtw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pcmpgtw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtwEmitter<Vec, Mem> for Assembler<'_> {
    fn pcmpgtw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pcmpgtw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtwEmitter<Xmm, Mem> for Assembler<'_> {
    fn pcmpgtw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pcmpgtw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PcmpistriEmitter<Vec, Vec, Imm, Gp> for Assembler<'_> {
    fn pcmpistri(&mut self, dst: Vec, src: Vec, imm: Imm, dst2: Gp) {
        self.emit_n(
            InstId::Pcmpistri as u32,
            &[
                dst.as_operand(),
                src.as_operand(),
                imm.as_operand(),
                dst2.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl<U2: Into<Imm>> PcmpistriEmitter<Xmm, Xmm, U2, Gpd> for Assembler<'_> {
    fn pcmpistri(&mut self, dst: Xmm, src1: Xmm, src2: U2, dst2: Gpd) {
        self.emit_n(
            InstId::Pcmpistri as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
                dst2.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl PcmpistriEmitter<Vec, Mem, Imm, Gp> for Assembler<'_> {
    fn pcmpistri(&mut self, dst: Vec, src: Mem, imm: Imm, dst2: Gp) {
        self.emit_n(
            InstId::Pcmpistri as u32,
            &[
                dst.as_operand(),
                src.as_operand(),
                imm.as_operand(),
                dst2.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl<U2: Into<Imm>> PcmpistriEmitter<Xmm, Mem, U2, Gpd> for Assembler<'_> {
    fn pcmpistri(&mut self, dst: Xmm, src1: Mem, src2: U2, dst2: Gpd) {
        self.emit_n(
            InstId::Pcmpistri as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
                dst2.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl PcmpistrmEmitter<Vec, Vec, Imm, Vec> for Assembler<'_> {
    fn pcmpistrm(&mut self, dst: Vec, src: Vec, imm: Imm, dst2: Vec) {
        self.emit_n(
            InstId::Pcmpistrm as u32,
            &[
                dst.as_operand(),
                src.as_operand(),
                imm.as_operand(),
                dst2.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl<U2: Into<Imm>> PcmpistrmEmitter<Xmm, Xmm, U2, Xmm> for Assembler<'_> {
    fn pcmpistrm(&mut self, dst: Xmm, src1: Xmm, src2: U2, dst2: Xmm) {
        self.emit_n(
            InstId::Pcmpistrm as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
                dst2.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl PcmpistrmEmitter<Vec, Mem, Imm, Vec> for Assembler<'_> {
    fn pcmpistrm(&mut self, dst: Vec, src: Mem, imm: Imm, dst2: Vec) {
        self.emit_n(
            InstId::Pcmpistrm as u32,
            &[
                dst.as_operand(),
                src.as_operand(),
                imm.as_operand(),
                dst2.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl<U2: Into<Imm>> PcmpistrmEmitter<Xmm, Mem, U2, Xmm> for Assembler<'_> {
    fn pcmpistrm(&mut self, dst: Xmm, src1: Mem, src2: U2, dst2: Xmm) {
        self.emit_n(
            InstId::Pcmpistrm as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
                dst2.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl PextrbEmitter<Gp, Vec, Imm> for Assembler<'_> {
    fn pextrb(&mut self, dst: Gp, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Pextrb as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PextrbEmitter<Gpd, Xmm, U2> for Assembler<'_> {
    fn pextrb(&mut self, dst: Gpd, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Pextrb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PextrbEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn pextrb(&mut self, dst: Mem, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Pextrb as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PextrbEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn pextrb(&mut self, dst: Mem, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Pextrb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PextrdEmitter<Gp, Vec, Imm> for Assembler<'_> {
    fn pextrd(&mut self, dst: Gp, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Pextrd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PextrdEmitter<Gpd, Xmm, U2> for Assembler<'_> {
    fn pextrd(&mut self, dst: Gpd, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Pextrd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PextrdEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn pextrd(&mut self, dst: Mem, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Pextrd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PextrdEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn pextrd(&mut self, dst: Mem, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Pextrd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PextrqEmitter<Gp, Vec, Imm> for Assembler<'_> {
    fn pextrq(&mut self, dst: Gp, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Pextrq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PextrqEmitter<Gpq, Xmm, U2> for Assembler<'_> {
    fn pextrq(&mut self, dst: Gpq, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Pextrq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PextrqEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn pextrq(&mut self, dst: Mem, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Pextrq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PextrqEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn pextrq(&mut self, dst: Mem, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Pextrq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PextrwEmitter<Gp, Mm, Imm> for Assembler<'_> {
    fn pextrw(&mut self, dst: Gp, src: Mm, imm: Imm) {
        self.emit_n(
            InstId::Pextrw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE
    }
}

impl<U2: Into<Imm>> PextrwEmitter<Gpd, Mm, U2> for Assembler<'_> {
    fn pextrw(&mut self, dst: Gpd, src1: Mm, src2: U2) {
        self.emit_n(
            InstId::Pextrw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE
    }
}

impl PextrwEmitter<Gp, Vec, Imm> for Assembler<'_> {
    fn pextrw(&mut self, dst: Gp, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Pextrw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> PextrwEmitter<Gpd, Xmm, U2> for Assembler<'_> {
    fn pextrw(&mut self, dst: Gpd, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Pextrw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE2
    }
}

impl PextrwEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn pextrw(&mut self, dst: Mem, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Pextrw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PextrwEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn pextrw(&mut self, dst: Mem, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Pextrw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PhadddEmitter<Mm, Mm> for Assembler<'_> {
    fn phaddd(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Phaddd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhadddEmitter<Mm, Mem> for Assembler<'_> {
    fn phaddd(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Phaddd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhadddEmitter<Vec, Vec> for Assembler<'_> {
    fn phaddd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Phaddd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhadddEmitter<Xmm, Xmm> for Assembler<'_> {
    fn phaddd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Phaddd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhadddEmitter<Vec, Mem> for Assembler<'_> {
    fn phaddd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Phaddd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhadddEmitter<Xmm, Mem> for Assembler<'_> {
    fn phaddd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Phaddd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhaddswEmitter<Mm, Mm> for Assembler<'_> {
    fn phaddsw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Phaddsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PhaddswEmitter<Mm, Mem> for Assembler<'_> {
    fn phaddsw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Phaddsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PhaddswEmitter<Vec, Vec> for Assembler<'_> {
    fn phaddsw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Phaddsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PhaddswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn phaddsw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Phaddsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PhaddswEmitter<Vec, Mem> for Assembler<'_> {
    fn phaddsw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Phaddsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PhaddswEmitter<Xmm, Mem> for Assembler<'_> {
    fn phaddsw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Phaddsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PhaddwEmitter<Mm, Mm> for Assembler<'_> {
    fn phaddw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Phaddw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhaddwEmitter<Mm, Mem> for Assembler<'_> {
    fn phaddw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Phaddw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhaddwEmitter<Vec, Vec> for Assembler<'_> {
    fn phaddw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Phaddw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhaddwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn phaddw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Phaddw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhaddwEmitter<Vec, Mem> for Assembler<'_> {
    fn phaddw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Phaddw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhaddwEmitter<Xmm, Mem> for Assembler<'_> {
    fn phaddw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Phaddw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhminposuwEmitter<Vec, Vec> for Assembler<'_> {
    fn phminposuw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Phminposuw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PhminposuwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn phminposuw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Phminposuw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PhminposuwEmitter<Vec, Mem> for Assembler<'_> {
    fn phminposuw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Phminposuw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PhminposuwEmitter<Xmm, Mem> for Assembler<'_> {
    fn phminposuw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Phminposuw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PhsubdEmitter<Mm, Mm> for Assembler<'_> {
    fn phsubd(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Phsubd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhsubdEmitter<Mm, Mem> for Assembler<'_> {
    fn phsubd(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Phsubd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhsubdEmitter<Vec, Vec> for Assembler<'_> {
    fn phsubd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Phsubd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhsubdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn phsubd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Phsubd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhsubdEmitter<Vec, Mem> for Assembler<'_> {
    fn phsubd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Phsubd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhsubdEmitter<Xmm, Mem> for Assembler<'_> {
    fn phsubd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Phsubd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhsubswEmitter<Mm, Mm> for Assembler<'_> {
    fn phsubsw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Phsubsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PhsubswEmitter<Mm, Mem> for Assembler<'_> {
    fn phsubsw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Phsubsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PhsubswEmitter<Vec, Vec> for Assembler<'_> {
    fn phsubsw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Phsubsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PhsubswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn phsubsw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Phsubsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PhsubswEmitter<Vec, Mem> for Assembler<'_> {
    fn phsubsw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Phsubsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PhsubswEmitter<Xmm, Mem> for Assembler<'_> {
    fn phsubsw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Phsubsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PhsubwEmitter<Mm, Mm> for Assembler<'_> {
    fn phsubw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Phsubw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhsubwEmitter<Mm, Mem> for Assembler<'_> {
    fn phsubw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Phsubw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhsubwEmitter<Vec, Vec> for Assembler<'_> {
    fn phsubw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Phsubw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhsubwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn phsubw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Phsubw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhsubwEmitter<Vec, Mem> for Assembler<'_> {
    fn phsubw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Phsubw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PhsubwEmitter<Xmm, Mem> for Assembler<'_> {
    fn phsubw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Phsubw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PinsrbEmitter<Vec, Gp, Imm> for Assembler<'_> {
    fn pinsrb(&mut self, dst: Vec, src: Gp, imm: Imm) {
        self.emit_n(
            InstId::Pinsrb as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PinsrbEmitter<Xmm, Gpd, U2> for Assembler<'_> {
    fn pinsrb(&mut self, dst: Xmm, src1: Gpd, src2: U2) {
        self.emit_n(
            InstId::Pinsrb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PinsrbEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pinsrb(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Pinsrb as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PinsrbEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn pinsrb(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Pinsrb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PinsrdEmitter<Vec, Gp, Imm> for Assembler<'_> {
    fn pinsrd(&mut self, dst: Vec, src: Gp, imm: Imm) {
        self.emit_n(
            InstId::Pinsrd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PinsrdEmitter<Xmm, Gpd, U2> for Assembler<'_> {
    fn pinsrd(&mut self, dst: Xmm, src1: Gpd, src2: U2) {
        self.emit_n(
            InstId::Pinsrd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PinsrdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pinsrd(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Pinsrd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PinsrdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn pinsrd(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Pinsrd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PinsrqEmitter<Vec, Gp, Imm> for Assembler<'_> {
    fn pinsrq(&mut self, dst: Vec, src: Gp, imm: Imm) {
        self.emit_n(
            InstId::Pinsrq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PinsrqEmitter<Xmm, Gpq, U2> for Assembler<'_> {
    fn pinsrq(&mut self, dst: Xmm, src1: Gpq, src2: U2) {
        self.emit_n(
            InstId::Pinsrq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PinsrqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pinsrq(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Pinsrq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PinsrqEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn pinsrq(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Pinsrq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PinsrwEmitter<Mm, Gp, Imm> for Assembler<'_> {
    fn pinsrw(&mut self, dst: Mm, src: Gp, imm: Imm) {
        self.emit_n(
            InstId::Pinsrw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE
    }
}

impl<U2: Into<Imm>> PinsrwEmitter<Mm, Gpd, U2> for Assembler<'_> {
    fn pinsrw(&mut self, dst: Mm, src1: Gpd, src2: U2) {
        self.emit_n(
            InstId::Pinsrw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE
    }
}

impl<U2: Into<Imm>> PinsrwEmitter<Mm, Mem, U2> for Assembler<'_> {
    fn pinsrw(&mut self, dst: Mm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Pinsrw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE
    }
}

impl PinsrwEmitter<Vec, Gp, Imm> for Assembler<'_> {
    fn pinsrw(&mut self, dst: Vec, src: Gp, imm: Imm) {
        self.emit_n(
            InstId::Pinsrw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> PinsrwEmitter<Xmm, Gpd, U2> for Assembler<'_> {
    fn pinsrw(&mut self, dst: Xmm, src1: Gpd, src2: U2) {
        self.emit_n(
            InstId::Pinsrw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE2
    }
}

impl PinsrwEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pinsrw(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Pinsrw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> PinsrwEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn pinsrw(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Pinsrw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE2
    }
}

impl PmaddubswEmitter<Mm, Mm> for Assembler<'_> {
    fn pmaddubsw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pmaddubsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PmaddubswEmitter<Mm, Mem> for Assembler<'_> {
    fn pmaddubsw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pmaddubsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PmaddubswEmitter<Vec, Vec> for Assembler<'_> {
    fn pmaddubsw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pmaddubsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PmaddubswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmaddubsw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pmaddubsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PmaddubswEmitter<Vec, Mem> for Assembler<'_> {
    fn pmaddubsw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pmaddubsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PmaddubswEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmaddubsw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pmaddubsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PmaddwdEmitter<Mm, Mm> for Assembler<'_> {
    fn pmaddwd(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pmaddwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PmaddwdEmitter<Mm, Mem> for Assembler<'_> {
    fn pmaddwd(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pmaddwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PmaddwdEmitter<Vec, Vec> for Assembler<'_> {
    fn pmaddwd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pmaddwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PmaddwdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmaddwd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pmaddwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PmaddwdEmitter<Vec, Mem> for Assembler<'_> {
    fn pmaddwd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pmaddwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PmaddwdEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmaddwd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pmaddwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PmaxsbEmitter<Vec, Vec> for Assembler<'_> {
    fn pmaxsb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pmaxsb as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmaxsbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmaxsb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pmaxsb as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmaxsbEmitter<Vec, Mem> for Assembler<'_> {
    fn pmaxsb(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pmaxsb as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmaxsbEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmaxsb(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pmaxsb as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmaxsdEmitter<Vec, Vec> for Assembler<'_> {
    fn pmaxsd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pmaxsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmaxsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmaxsd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pmaxsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmaxsdEmitter<Vec, Mem> for Assembler<'_> {
    fn pmaxsd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pmaxsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmaxsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmaxsd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pmaxsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmaxswEmitter<Mm, Mm> for Assembler<'_> {
    fn pmaxsw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pmaxsw as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl PmaxswEmitter<Mm, Mem> for Assembler<'_> {
    fn pmaxsw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pmaxsw as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl PmaxswEmitter<Vec, Vec> for Assembler<'_> {
    fn pmaxsw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pmaxsw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PmaxswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmaxsw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pmaxsw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PmaxswEmitter<Vec, Mem> for Assembler<'_> {
    fn pmaxsw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pmaxsw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PmaxswEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmaxsw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pmaxsw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PmaxubEmitter<Mm, Mm> for Assembler<'_> {
    fn pmaxub(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pmaxub as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl PmaxubEmitter<Mm, Mem> for Assembler<'_> {
    fn pmaxub(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pmaxub as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl PmaxubEmitter<Vec, Vec> for Assembler<'_> {
    fn pmaxub(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pmaxub as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PmaxubEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmaxub(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pmaxub as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PmaxubEmitter<Vec, Mem> for Assembler<'_> {
    fn pmaxub(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pmaxub as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PmaxubEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmaxub(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pmaxub as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PmaxudEmitter<Vec, Vec> for Assembler<'_> {
    fn pmaxud(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pmaxud as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmaxudEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmaxud(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pmaxud as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmaxudEmitter<Vec, Mem> for Assembler<'_> {
    fn pmaxud(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pmaxud as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmaxudEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmaxud(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pmaxud as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmaxuwEmitter<Vec, Vec> for Assembler<'_> {
    fn pmaxuw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pmaxuw as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmaxuwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmaxuw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pmaxuw as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmaxuwEmitter<Vec, Mem> for Assembler<'_> {
    fn pmaxuw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pmaxuw as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmaxuwEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmaxuw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pmaxuw as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PminsbEmitter<Vec, Vec> for Assembler<'_> {
    fn pminsb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pminsb as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PminsbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pminsb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pminsb as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PminsbEmitter<Vec, Mem> for Assembler<'_> {
    fn pminsb(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pminsb as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PminsbEmitter<Xmm, Mem> for Assembler<'_> {
    fn pminsb(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pminsb as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PminsdEmitter<Vec, Vec> for Assembler<'_> {
    fn pminsd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pminsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PminsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pminsd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pminsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PminsdEmitter<Vec, Mem> for Assembler<'_> {
    fn pminsd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pminsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PminsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn pminsd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pminsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PminswEmitter<Mm, Mm> for Assembler<'_> {
    fn pminsw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pminsw as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl PminswEmitter<Mm, Mem> for Assembler<'_> {
    fn pminsw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pminsw as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl PminswEmitter<Vec, Vec> for Assembler<'_> {
    fn pminsw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pminsw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PminswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pminsw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pminsw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PminswEmitter<Vec, Mem> for Assembler<'_> {
    fn pminsw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pminsw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PminswEmitter<Xmm, Mem> for Assembler<'_> {
    fn pminsw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pminsw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PminubEmitter<Mm, Mm> for Assembler<'_> {
    fn pminub(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pminub as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl PminubEmitter<Mm, Mem> for Assembler<'_> {
    fn pminub(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pminub as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl PminubEmitter<Vec, Vec> for Assembler<'_> {
    fn pminub(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pminub as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PminubEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pminub(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pminub as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PminubEmitter<Vec, Mem> for Assembler<'_> {
    fn pminub(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pminub as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PminubEmitter<Xmm, Mem> for Assembler<'_> {
    fn pminub(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pminub as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PminudEmitter<Vec, Vec> for Assembler<'_> {
    fn pminud(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pminud as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PminudEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pminud(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pminud as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PminudEmitter<Vec, Mem> for Assembler<'_> {
    fn pminud(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pminud as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PminudEmitter<Xmm, Mem> for Assembler<'_> {
    fn pminud(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pminud as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PminuwEmitter<Vec, Vec> for Assembler<'_> {
    fn pminuw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pminuw as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PminuwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pminuw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pminuw as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PminuwEmitter<Vec, Mem> for Assembler<'_> {
    fn pminuw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pminuw as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PminuwEmitter<Xmm, Mem> for Assembler<'_> {
    fn pminuw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pminuw as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmovmskbEmitter<Gp, Mm> for Assembler<'_> {
    fn pmovmskb(&mut self, dst: Gp, src: Mm) {
        self.emit_n(
            InstId::Pmovmskb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl PmovmskbEmitter<Gpd, Mm> for Assembler<'_> {
    fn pmovmskb(&mut self, dst: Gpd, src: Mm) {
        self.emit_n(
            InstId::Pmovmskb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl PmovmskbEmitter<Gp, Vec> for Assembler<'_> {
    fn pmovmskb(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Pmovmskb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PmovmskbEmitter<Gpd, Xmm> for Assembler<'_> {
    fn pmovmskb(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Pmovmskb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PmovsxbdEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovsxbd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pmovsxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovsxbd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pmovsxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbdEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovsxbd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pmovsxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbdEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovsxbd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pmovsxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbqEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovsxbq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pmovsxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovsxbq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pmovsxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbqEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovsxbq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pmovsxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbqEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovsxbq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pmovsxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbwEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovsxbw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pmovsxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovsxbw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pmovsxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbwEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovsxbw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pmovsxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbwEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovsxbw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pmovsxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxdqEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovsxdq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pmovsxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxdqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovsxdq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pmovsxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxdqEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovsxdq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pmovsxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxdqEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovsxdq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pmovsxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxwdEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovsxwd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pmovsxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxwdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovsxwd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pmovsxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxwdEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovsxwd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pmovsxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxwdEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovsxwd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pmovsxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxwqEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovsxwq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pmovsxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxwqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovsxwq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pmovsxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxwqEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovsxwq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pmovsxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxwqEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovsxwq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pmovsxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbdEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovzxbd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pmovzxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovzxbd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pmovzxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbdEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovzxbd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pmovzxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbdEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovzxbd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pmovzxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbqEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovzxbq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pmovzxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovzxbq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pmovzxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbqEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovzxbq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pmovzxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbqEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovzxbq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pmovzxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbwEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovzxbw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pmovzxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovzxbw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pmovzxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbwEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovzxbw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pmovzxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbwEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovzxbw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pmovzxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxdqEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovzxdq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pmovzxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxdqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovzxdq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pmovzxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxdqEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovzxdq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pmovzxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxdqEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovzxdq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pmovzxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxwdEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovzxwd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pmovzxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxwdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovzxwd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pmovzxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxwdEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovzxwd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pmovzxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxwdEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovzxwd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pmovzxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxwqEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovzxwq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pmovzxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxwqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovzxwq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pmovzxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxwqEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovzxwq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pmovzxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxwqEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovzxwq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pmovzxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1
    }
}

impl PmuldqEmitter<Vec, Vec> for Assembler<'_> {
    fn pmuldq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pmuldq as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmuldqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmuldq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pmuldq as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmuldqEmitter<Vec, Mem> for Assembler<'_> {
    fn pmuldq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pmuldq as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmuldqEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmuldq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pmuldq as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmulhrswEmitter<Mm, Mm> for Assembler<'_> {
    fn pmulhrsw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pmulhrsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PmulhrswEmitter<Mm, Mem> for Assembler<'_> {
    fn pmulhrsw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pmulhrsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PmulhrswEmitter<Vec, Vec> for Assembler<'_> {
    fn pmulhrsw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pmulhrsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PmulhrswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmulhrsw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pmulhrsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PmulhrswEmitter<Vec, Mem> for Assembler<'_> {
    fn pmulhrsw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pmulhrsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PmulhrswEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmulhrsw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pmulhrsw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSSE3
    }
}

impl PmulhwEmitter<Mm, Mm> for Assembler<'_> {
    fn pmulhw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pmulhw as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PmulhwEmitter<Mm, Mem> for Assembler<'_> {
    fn pmulhw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pmulhw as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PmulhwEmitter<Vec, Vec> for Assembler<'_> {
    fn pmulhw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pmulhw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PmulhwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmulhw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pmulhw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PmulhwEmitter<Vec, Mem> for Assembler<'_> {
    fn pmulhw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pmulhw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PmulhwEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmulhw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pmulhw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PmulhuwEmitter<Mm, Mm> for Assembler<'_> {
    fn pmulhuw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pmulhuw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl PmulhuwEmitter<Mm, Mem> for Assembler<'_> {
    fn pmulhuw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pmulhuw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl PmulhuwEmitter<Vec, Vec> for Assembler<'_> {
    fn pmulhuw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pmulhuw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PmulhuwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmulhuw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pmulhuw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PmulhuwEmitter<Vec, Mem> for Assembler<'_> {
    fn pmulhuw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pmulhuw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PmulhuwEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmulhuw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pmulhuw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PmulldEmitter<Vec, Vec> for Assembler<'_> {
    fn pmulld(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pmulld as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmulldEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmulld(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pmulld as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmulldEmitter<Vec, Mem> for Assembler<'_> {
    fn pmulld(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pmulld as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmulldEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmulld(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pmulld as u32, &[dst.as_operand(), src.as_operand()]); // SSE4_1
    }
}

impl PmullwEmitter<Mm, Mm> for Assembler<'_> {
    fn pmullw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pmullw as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PmullwEmitter<Mm, Mem> for Assembler<'_> {
    fn pmullw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pmullw as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PmullwEmitter<Vec, Vec> for Assembler<'_> {
    fn pmullw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pmullw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PmullwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmullw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pmullw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PmullwEmitter<Vec, Mem> for Assembler<'_> {
    fn pmullw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pmullw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PmullwEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmullw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pmullw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PmuludqEmitter<Mm, Mm> for Assembler<'_> {
    fn pmuludq(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pmuludq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PmuludqEmitter<Mm, Mem> for Assembler<'_> {
    fn pmuludq(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pmuludq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PmuludqEmitter<Vec, Vec> for Assembler<'_> {
    fn pmuludq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pmuludq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PmuludqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmuludq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pmuludq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PmuludqEmitter<Vec, Mem> for Assembler<'_> {
    fn pmuludq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pmuludq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PmuludqEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmuludq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Pmuludq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PorEmitter<Mm, Mm> for Assembler<'_> {
    fn por(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Por as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PorEmitter<Mm, Mem> for Assembler<'_> {
    fn por(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Por as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PorEmitter<Vec, Vec> for Assembler<'_> {
    fn por(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Por as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PorEmitter<Xmm, Xmm> for Assembler<'_> {
    fn por(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Por as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PorEmitter<Vec, Mem> for Assembler<'_> {
    fn por(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Por as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PorEmitter<Xmm, Mem> for Assembler<'_> {
    fn por(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Por as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsadbwEmitter<Mm, Mm> for Assembler<'_> {
    fn psadbw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Psadbw as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl PsadbwEmitter<Mm, Mem> for Assembler<'_> {
    fn psadbw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Psadbw as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl PsadbwEmitter<Vec, Vec> for Assembler<'_> {
    fn psadbw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Psadbw as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl PsadbwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psadbw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Psadbw as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl PsadbwEmitter<Vec, Mem> for Assembler<'_> {
    fn psadbw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Psadbw as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl PsadbwEmitter<Xmm, Mem> for Assembler<'_> {
    fn psadbw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Psadbw as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl PslldEmitter<Mm, Mm> for Assembler<'_> {
    fn pslld(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pslld as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PslldEmitter<Mm, Mem> for Assembler<'_> {
    fn pslld(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pslld as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl<U1: Into<Imm>> PslldEmitter<Mm, U1> for Assembler<'_> {
    fn pslld(&mut self, dst: Mm, src: U1) {
        self.emit_n(
            InstId::Pslld as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // MMX
    }
}

impl PslldEmitter<Vec, Vec> for Assembler<'_> {
    fn pslld(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pslld as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PslldEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pslld(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pslld as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PslldEmitter<Vec, Mem> for Assembler<'_> {
    fn pslld(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pslld as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PslldEmitter<Xmm, Mem> for Assembler<'_> {
    fn pslld(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pslld as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PslldEmitter<Vec, Imm> for Assembler<'_> {
    fn pslld(&mut self, dst: Vec, imm: Imm) {
        self.emit_n(InstId::Pslld as u32, &[dst.as_operand(), imm.as_operand()]); // SSE2
    }
}

impl<U1: Into<Imm>> PslldEmitter<Xmm, U1> for Assembler<'_> {
    fn pslld(&mut self, dst: Xmm, src: U1) {
        self.emit_n(
            InstId::Pslld as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // SSE2
    }
}

impl PslldqEmitter<Vec, Imm> for Assembler<'_> {
    fn pslldq(&mut self, dst: Vec, imm: Imm) {
        self.emit_n(InstId::Pslldq as u32, &[dst.as_operand(), imm.as_operand()]); // SSE2
    }
}

impl<U1: Into<Imm>> PslldqEmitter<Xmm, U1> for Assembler<'_> {
    fn pslldq(&mut self, dst: Xmm, src: U1) {
        self.emit_n(
            InstId::Pslldq as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // SSE2
    }
}

impl PsllqEmitter<Mm, Mm> for Assembler<'_> {
    fn psllq(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Psllq as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PsllqEmitter<Mm, Mem> for Assembler<'_> {
    fn psllq(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Psllq as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl<U1: Into<Imm>> PsllqEmitter<Mm, U1> for Assembler<'_> {
    fn psllq(&mut self, dst: Mm, src: U1) {
        self.emit_n(
            InstId::Psllq as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // MMX
    }
}

impl PsllqEmitter<Vec, Vec> for Assembler<'_> {
    fn psllq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Psllq as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsllqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psllq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Psllq as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsllqEmitter<Vec, Mem> for Assembler<'_> {
    fn psllq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Psllq as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsllqEmitter<Xmm, Mem> for Assembler<'_> {
    fn psllq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Psllq as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsllqEmitter<Vec, Imm> for Assembler<'_> {
    fn psllq(&mut self, dst: Vec, imm: Imm) {
        self.emit_n(InstId::Psllq as u32, &[dst.as_operand(), imm.as_operand()]); // SSE2
    }
}

impl<U1: Into<Imm>> PsllqEmitter<Xmm, U1> for Assembler<'_> {
    fn psllq(&mut self, dst: Xmm, src: U1) {
        self.emit_n(
            InstId::Psllq as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // SSE2
    }
}

impl PsllwEmitter<Mm, Mm> for Assembler<'_> {
    fn psllw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Psllw as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PsllwEmitter<Mm, Mem> for Assembler<'_> {
    fn psllw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Psllw as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl<U1: Into<Imm>> PsllwEmitter<Mm, U1> for Assembler<'_> {
    fn psllw(&mut self, dst: Mm, src: U1) {
        self.emit_n(
            InstId::Psllw as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // MMX
    }
}

impl PsllwEmitter<Vec, Vec> for Assembler<'_> {
    fn psllw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Psllw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsllwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psllw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Psllw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsllwEmitter<Vec, Mem> for Assembler<'_> {
    fn psllw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Psllw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsllwEmitter<Xmm, Mem> for Assembler<'_> {
    fn psllw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Psllw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsllwEmitter<Vec, Imm> for Assembler<'_> {
    fn psllw(&mut self, dst: Vec, imm: Imm) {
        self.emit_n(InstId::Psllw as u32, &[dst.as_operand(), imm.as_operand()]); // SSE2
    }
}

impl<U1: Into<Imm>> PsllwEmitter<Xmm, U1> for Assembler<'_> {
    fn psllw(&mut self, dst: Xmm, src: U1) {
        self.emit_n(
            InstId::Psllw as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // SSE2
    }
}

impl PsradEmitter<Mm, Mm> for Assembler<'_> {
    fn psrad(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Psrad as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PsradEmitter<Mm, Mem> for Assembler<'_> {
    fn psrad(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Psrad as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl<U1: Into<Imm>> PsradEmitter<Mm, U1> for Assembler<'_> {
    fn psrad(&mut self, dst: Mm, src: U1) {
        self.emit_n(
            InstId::Psrad as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // MMX
    }
}

impl PsradEmitter<Vec, Vec> for Assembler<'_> {
    fn psrad(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Psrad as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsradEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psrad(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Psrad as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsradEmitter<Vec, Mem> for Assembler<'_> {
    fn psrad(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Psrad as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsradEmitter<Xmm, Mem> for Assembler<'_> {
    fn psrad(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Psrad as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsradEmitter<Vec, Imm> for Assembler<'_> {
    fn psrad(&mut self, dst: Vec, imm: Imm) {
        self.emit_n(InstId::Psrad as u32, &[dst.as_operand(), imm.as_operand()]); // SSE2
    }
}

impl<U1: Into<Imm>> PsradEmitter<Xmm, U1> for Assembler<'_> {
    fn psrad(&mut self, dst: Xmm, src: U1) {
        self.emit_n(
            InstId::Psrad as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // SSE2
    }
}

impl PsrawEmitter<Mm, Mm> for Assembler<'_> {
    fn psraw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Psraw as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PsrawEmitter<Mm, Mem> for Assembler<'_> {
    fn psraw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Psraw as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl<U1: Into<Imm>> PsrawEmitter<Mm, U1> for Assembler<'_> {
    fn psraw(&mut self, dst: Mm, src: U1) {
        self.emit_n(
            InstId::Psraw as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // MMX
    }
}

impl PsrawEmitter<Vec, Vec> for Assembler<'_> {
    fn psraw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Psraw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsrawEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psraw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Psraw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsrawEmitter<Vec, Mem> for Assembler<'_> {
    fn psraw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Psraw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsrawEmitter<Xmm, Mem> for Assembler<'_> {
    fn psraw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Psraw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsrawEmitter<Vec, Imm> for Assembler<'_> {
    fn psraw(&mut self, dst: Vec, imm: Imm) {
        self.emit_n(InstId::Psraw as u32, &[dst.as_operand(), imm.as_operand()]); // SSE2
    }
}

impl<U1: Into<Imm>> PsrawEmitter<Xmm, U1> for Assembler<'_> {
    fn psraw(&mut self, dst: Xmm, src: U1) {
        self.emit_n(
            InstId::Psraw as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // SSE2
    }
}

impl PshufbEmitter<Mm, Mm> for Assembler<'_> {
    fn pshufb(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pshufb as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PshufbEmitter<Mm, Mem> for Assembler<'_> {
    fn pshufb(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pshufb as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PshufbEmitter<Vec, Vec> for Assembler<'_> {
    fn pshufb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pshufb as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PshufbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pshufb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pshufb as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PshufbEmitter<Vec, Mem> for Assembler<'_> {
    fn pshufb(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pshufb as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PshufbEmitter<Xmm, Mem> for Assembler<'_> {
    fn pshufb(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pshufb as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PshufdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn pshufd(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Pshufd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> PshufdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn pshufd(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Pshufd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE2
    }
}

impl PshufdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pshufd(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Pshufd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> PshufdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn pshufd(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Pshufd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE2
    }
}

impl PshufhwEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn pshufhw(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Pshufhw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> PshufhwEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn pshufhw(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Pshufhw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE2
    }
}

impl PshufhwEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pshufhw(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Pshufhw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> PshufhwEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn pshufhw(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Pshufhw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE2
    }
}

impl PshuflwEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn pshuflw(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Pshuflw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> PshuflwEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn pshuflw(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Pshuflw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE2
    }
}

impl PshuflwEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pshuflw(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Pshuflw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> PshuflwEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn pshuflw(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Pshuflw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> PshufwEmitter<Mm, Mm, U2> for Assembler<'_> {
    fn pshufw(&mut self, dst: Mm, src1: Mm, src2: U2) {
        self.emit_n(
            InstId::Pshufw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE
    }
}

impl<U2: Into<Imm>> PshufwEmitter<Mm, Mem, U2> for Assembler<'_> {
    fn pshufw(&mut self, dst: Mm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Pshufw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE
    }
}

impl PsignbEmitter<Mm, Mm> for Assembler<'_> {
    fn psignb(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Psignb as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PsignbEmitter<Mm, Mem> for Assembler<'_> {
    fn psignb(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Psignb as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PsignbEmitter<Vec, Vec> for Assembler<'_> {
    fn psignb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Psignb as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PsignbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psignb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Psignb as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PsignbEmitter<Vec, Mem> for Assembler<'_> {
    fn psignb(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Psignb as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PsignbEmitter<Xmm, Mem> for Assembler<'_> {
    fn psignb(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Psignb as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PsigndEmitter<Mm, Mm> for Assembler<'_> {
    fn psignd(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Psignd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PsigndEmitter<Mm, Mem> for Assembler<'_> {
    fn psignd(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Psignd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PsigndEmitter<Vec, Vec> for Assembler<'_> {
    fn psignd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Psignd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PsigndEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psignd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Psignd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PsigndEmitter<Vec, Mem> for Assembler<'_> {
    fn psignd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Psignd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PsigndEmitter<Xmm, Mem> for Assembler<'_> {
    fn psignd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Psignd as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PsignwEmitter<Mm, Mm> for Assembler<'_> {
    fn psignw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Psignw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PsignwEmitter<Mm, Mem> for Assembler<'_> {
    fn psignw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Psignw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PsignwEmitter<Vec, Vec> for Assembler<'_> {
    fn psignw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Psignw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PsignwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psignw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Psignw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PsignwEmitter<Vec, Mem> for Assembler<'_> {
    fn psignw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Psignw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PsignwEmitter<Xmm, Mem> for Assembler<'_> {
    fn psignw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Psignw as u32, &[dst.as_operand(), src.as_operand()]); // SSSE3
    }
}

impl PsrldEmitter<Mm, Mm> for Assembler<'_> {
    fn psrld(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Psrld as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PsrldEmitter<Mm, Mem> for Assembler<'_> {
    fn psrld(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Psrld as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl<U1: Into<Imm>> PsrldEmitter<Mm, U1> for Assembler<'_> {
    fn psrld(&mut self, dst: Mm, src: U1) {
        self.emit_n(
            InstId::Psrld as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // MMX
    }
}

impl PsrldEmitter<Vec, Vec> for Assembler<'_> {
    fn psrld(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Psrld as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsrldEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psrld(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Psrld as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsrldEmitter<Vec, Mem> for Assembler<'_> {
    fn psrld(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Psrld as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsrldEmitter<Xmm, Mem> for Assembler<'_> {
    fn psrld(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Psrld as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsrldEmitter<Vec, Imm> for Assembler<'_> {
    fn psrld(&mut self, dst: Vec, imm: Imm) {
        self.emit_n(InstId::Psrld as u32, &[dst.as_operand(), imm.as_operand()]); // SSE2
    }
}

impl<U1: Into<Imm>> PsrldEmitter<Xmm, U1> for Assembler<'_> {
    fn psrld(&mut self, dst: Xmm, src: U1) {
        self.emit_n(
            InstId::Psrld as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // SSE2
    }
}

impl PsrldqEmitter<Vec, Imm> for Assembler<'_> {
    fn psrldq(&mut self, dst: Vec, imm: Imm) {
        self.emit_n(InstId::Psrldq as u32, &[dst.as_operand(), imm.as_operand()]); // SSE2
    }
}

impl<U1: Into<Imm>> PsrldqEmitter<Xmm, U1> for Assembler<'_> {
    fn psrldq(&mut self, dst: Xmm, src: U1) {
        self.emit_n(
            InstId::Psrldq as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // SSE2
    }
}

impl PsrlqEmitter<Mm, Mm> for Assembler<'_> {
    fn psrlq(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Psrlq as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PsrlqEmitter<Mm, Mem> for Assembler<'_> {
    fn psrlq(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Psrlq as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl<U1: Into<Imm>> PsrlqEmitter<Mm, U1> for Assembler<'_> {
    fn psrlq(&mut self, dst: Mm, src: U1) {
        self.emit_n(
            InstId::Psrlq as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // MMX
    }
}

impl PsrlqEmitter<Vec, Vec> for Assembler<'_> {
    fn psrlq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Psrlq as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsrlqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psrlq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Psrlq as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsrlqEmitter<Vec, Mem> for Assembler<'_> {
    fn psrlq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Psrlq as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsrlqEmitter<Xmm, Mem> for Assembler<'_> {
    fn psrlq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Psrlq as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsrlqEmitter<Vec, Imm> for Assembler<'_> {
    fn psrlq(&mut self, dst: Vec, imm: Imm) {
        self.emit_n(InstId::Psrlq as u32, &[dst.as_operand(), imm.as_operand()]); // SSE2
    }
}

impl<U1: Into<Imm>> PsrlqEmitter<Xmm, U1> for Assembler<'_> {
    fn psrlq(&mut self, dst: Xmm, src: U1) {
        self.emit_n(
            InstId::Psrlq as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // SSE2
    }
}

impl PsrlwEmitter<Mm, Mm> for Assembler<'_> {
    fn psrlw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Psrlw as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PsrlwEmitter<Mm, Mem> for Assembler<'_> {
    fn psrlw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Psrlw as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl<U1: Into<Imm>> PsrlwEmitter<Mm, U1> for Assembler<'_> {
    fn psrlw(&mut self, dst: Mm, src: U1) {
        self.emit_n(
            InstId::Psrlw as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // MMX
    }
}

impl PsrlwEmitter<Vec, Vec> for Assembler<'_> {
    fn psrlw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Psrlw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsrlwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psrlw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Psrlw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsrlwEmitter<Vec, Mem> for Assembler<'_> {
    fn psrlw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Psrlw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsrlwEmitter<Xmm, Mem> for Assembler<'_> {
    fn psrlw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Psrlw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsrlwEmitter<Vec, Imm> for Assembler<'_> {
    fn psrlw(&mut self, dst: Vec, imm: Imm) {
        self.emit_n(InstId::Psrlw as u32, &[dst.as_operand(), imm.as_operand()]); // SSE2
    }
}

impl<U1: Into<Imm>> PsrlwEmitter<Xmm, U1> for Assembler<'_> {
    fn psrlw(&mut self, dst: Xmm, src: U1) {
        self.emit_n(
            InstId::Psrlw as u32,
            &[dst.as_operand(), Into::<Imm>::into(src).as_operand()],
        ); // SSE2
    }
}

impl PsubbEmitter<Mm, Mm> for Assembler<'_> {
    fn psubb(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Psubb as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PsubbEmitter<Mm, Mem> for Assembler<'_> {
    fn psubb(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Psubb as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PsubbEmitter<Vec, Vec> for Assembler<'_> {
    fn psubb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Psubb as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psubb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Psubb as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubbEmitter<Vec, Mem> for Assembler<'_> {
    fn psubb(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Psubb as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubbEmitter<Xmm, Mem> for Assembler<'_> {
    fn psubb(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Psubb as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubdEmitter<Mm, Mm> for Assembler<'_> {
    fn psubd(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Psubd as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PsubdEmitter<Mm, Mem> for Assembler<'_> {
    fn psubd(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Psubd as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PsubdEmitter<Vec, Vec> for Assembler<'_> {
    fn psubd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Psubd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psubd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Psubd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubdEmitter<Vec, Mem> for Assembler<'_> {
    fn psubd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Psubd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubdEmitter<Xmm, Mem> for Assembler<'_> {
    fn psubd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Psubd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubqEmitter<Mm, Mm> for Assembler<'_> {
    fn psubq(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Psubq as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubqEmitter<Mm, Mem> for Assembler<'_> {
    fn psubq(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Psubq as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubqEmitter<Vec, Vec> for Assembler<'_> {
    fn psubq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Psubq as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psubq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Psubq as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubqEmitter<Vec, Mem> for Assembler<'_> {
    fn psubq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Psubq as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubqEmitter<Xmm, Mem> for Assembler<'_> {
    fn psubq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Psubq as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubsbEmitter<Mm, Mm> for Assembler<'_> {
    fn psubsb(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Psubsb as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PsubsbEmitter<Mm, Mem> for Assembler<'_> {
    fn psubsb(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Psubsb as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PsubsbEmitter<Vec, Vec> for Assembler<'_> {
    fn psubsb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Psubsb as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubsbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psubsb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Psubsb as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubsbEmitter<Vec, Mem> for Assembler<'_> {
    fn psubsb(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Psubsb as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubsbEmitter<Xmm, Mem> for Assembler<'_> {
    fn psubsb(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Psubsb as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubswEmitter<Mm, Mm> for Assembler<'_> {
    fn psubsw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Psubsw as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PsubswEmitter<Mm, Mem> for Assembler<'_> {
    fn psubsw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Psubsw as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PsubswEmitter<Vec, Vec> for Assembler<'_> {
    fn psubsw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Psubsw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psubsw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Psubsw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubswEmitter<Vec, Mem> for Assembler<'_> {
    fn psubsw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Psubsw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubswEmitter<Xmm, Mem> for Assembler<'_> {
    fn psubsw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Psubsw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubusbEmitter<Mm, Mm> for Assembler<'_> {
    fn psubusb(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Psubusb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PsubusbEmitter<Mm, Mem> for Assembler<'_> {
    fn psubusb(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Psubusb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PsubusbEmitter<Vec, Vec> for Assembler<'_> {
    fn psubusb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Psubusb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PsubusbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psubusb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Psubusb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PsubusbEmitter<Vec, Mem> for Assembler<'_> {
    fn psubusb(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Psubusb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PsubusbEmitter<Xmm, Mem> for Assembler<'_> {
    fn psubusb(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Psubusb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PsubuswEmitter<Mm, Mm> for Assembler<'_> {
    fn psubusw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Psubusw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PsubuswEmitter<Mm, Mem> for Assembler<'_> {
    fn psubusw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Psubusw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PsubuswEmitter<Vec, Vec> for Assembler<'_> {
    fn psubusw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Psubusw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PsubuswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psubusw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Psubusw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PsubuswEmitter<Vec, Mem> for Assembler<'_> {
    fn psubusw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Psubusw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PsubuswEmitter<Xmm, Mem> for Assembler<'_> {
    fn psubusw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Psubusw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PsubwEmitter<Mm, Mm> for Assembler<'_> {
    fn psubw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Psubw as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PsubwEmitter<Mm, Mem> for Assembler<'_> {
    fn psubw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Psubw as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PsubwEmitter<Vec, Vec> for Assembler<'_> {
    fn psubw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Psubw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psubw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Psubw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubwEmitter<Vec, Mem> for Assembler<'_> {
    fn psubw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Psubw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PsubwEmitter<Xmm, Mem> for Assembler<'_> {
    fn psubw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Psubw as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PtestEmitter<Vec, Vec> for Assembler<'_> {
    fn ptest(&mut self, lhs: Vec, rhs: Vec) {
        self.emit_n(InstId::Ptest as u32, &[lhs.as_operand(), rhs.as_operand()]); // SSE4_1
    }
}

impl PtestEmitter<Xmm, Xmm> for Assembler<'_> {
    fn ptest(&mut self, lhs: Xmm, rhs: Xmm) {
        self.emit_n(InstId::Ptest as u32, &[lhs.as_operand(), rhs.as_operand()]); // SSE4_1
    }
}

impl PtestEmitter<Vec, Mem> for Assembler<'_> {
    fn ptest(&mut self, lhs: Vec, rhs: Mem) {
        self.emit_n(InstId::Ptest as u32, &[lhs.as_operand(), rhs.as_operand()]); // SSE4_1
    }
}

impl PtestEmitter<Xmm, Mem> for Assembler<'_> {
    fn ptest(&mut self, lhs: Xmm, rhs: Mem) {
        self.emit_n(InstId::Ptest as u32, &[lhs.as_operand(), rhs.as_operand()]); // SSE4_1
    }
}

impl PunpckhbwEmitter<Mm, Mm> for Assembler<'_> {
    fn punpckhbw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Punpckhbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PunpckhbwEmitter<Mm, Mem> for Assembler<'_> {
    fn punpckhbw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Punpckhbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PunpckhbwEmitter<Vec, Vec> for Assembler<'_> {
    fn punpckhbw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Punpckhbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpckhbwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn punpckhbw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Punpckhbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpckhbwEmitter<Vec, Mem> for Assembler<'_> {
    fn punpckhbw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Punpckhbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpckhbwEmitter<Xmm, Mem> for Assembler<'_> {
    fn punpckhbw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Punpckhbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpckhdqEmitter<Mm, Mm> for Assembler<'_> {
    fn punpckhdq(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Punpckhdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PunpckhdqEmitter<Mm, Mem> for Assembler<'_> {
    fn punpckhdq(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Punpckhdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PunpckhdqEmitter<Vec, Vec> for Assembler<'_> {
    fn punpckhdq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Punpckhdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpckhdqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn punpckhdq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Punpckhdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpckhdqEmitter<Vec, Mem> for Assembler<'_> {
    fn punpckhdq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Punpckhdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpckhdqEmitter<Xmm, Mem> for Assembler<'_> {
    fn punpckhdq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Punpckhdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpckhqdqEmitter<Vec, Vec> for Assembler<'_> {
    fn punpckhqdq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Punpckhqdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpckhqdqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn punpckhqdq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Punpckhqdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpckhqdqEmitter<Vec, Mem> for Assembler<'_> {
    fn punpckhqdq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Punpckhqdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpckhqdqEmitter<Xmm, Mem> for Assembler<'_> {
    fn punpckhqdq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Punpckhqdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpckhwdEmitter<Mm, Mm> for Assembler<'_> {
    fn punpckhwd(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Punpckhwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PunpckhwdEmitter<Mm, Mem> for Assembler<'_> {
    fn punpckhwd(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Punpckhwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PunpckhwdEmitter<Vec, Vec> for Assembler<'_> {
    fn punpckhwd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Punpckhwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpckhwdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn punpckhwd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Punpckhwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpckhwdEmitter<Vec, Mem> for Assembler<'_> {
    fn punpckhwd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Punpckhwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpckhwdEmitter<Xmm, Mem> for Assembler<'_> {
    fn punpckhwd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Punpckhwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpcklbwEmitter<Mm, Mm> for Assembler<'_> {
    fn punpcklbw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Punpcklbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PunpcklbwEmitter<Mm, Mem> for Assembler<'_> {
    fn punpcklbw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Punpcklbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PunpcklbwEmitter<Vec, Vec> for Assembler<'_> {
    fn punpcklbw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Punpcklbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpcklbwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn punpcklbw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Punpcklbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpcklbwEmitter<Vec, Mem> for Assembler<'_> {
    fn punpcklbw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Punpcklbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpcklbwEmitter<Xmm, Mem> for Assembler<'_> {
    fn punpcklbw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Punpcklbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpckldqEmitter<Mm, Mm> for Assembler<'_> {
    fn punpckldq(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Punpckldq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PunpckldqEmitter<Mm, Mem> for Assembler<'_> {
    fn punpckldq(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Punpckldq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PunpckldqEmitter<Vec, Vec> for Assembler<'_> {
    fn punpckldq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Punpckldq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpckldqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn punpckldq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Punpckldq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpckldqEmitter<Vec, Mem> for Assembler<'_> {
    fn punpckldq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Punpckldq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpckldqEmitter<Xmm, Mem> for Assembler<'_> {
    fn punpckldq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Punpckldq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpcklqdqEmitter<Vec, Vec> for Assembler<'_> {
    fn punpcklqdq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Punpcklqdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpcklqdqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn punpcklqdq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Punpcklqdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpcklqdqEmitter<Vec, Mem> for Assembler<'_> {
    fn punpcklqdq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Punpcklqdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpcklqdqEmitter<Xmm, Mem> for Assembler<'_> {
    fn punpcklqdq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Punpcklqdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpcklwdEmitter<Mm, Mm> for Assembler<'_> {
    fn punpcklwd(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Punpcklwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PunpcklwdEmitter<Mm, Mem> for Assembler<'_> {
    fn punpcklwd(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Punpcklwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // MMX
    }
}

impl PunpcklwdEmitter<Vec, Vec> for Assembler<'_> {
    fn punpcklwd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Punpcklwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpcklwdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn punpcklwd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Punpcklwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpcklwdEmitter<Vec, Mem> for Assembler<'_> {
    fn punpcklwd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Punpcklwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PunpcklwdEmitter<Xmm, Mem> for Assembler<'_> {
    fn punpcklwd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Punpcklwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl PxorEmitter<Mm, Mm> for Assembler<'_> {
    fn pxor(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pxor as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PxorEmitter<Mm, Mem> for Assembler<'_> {
    fn pxor(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pxor as u32, &[dst.as_operand(), src.as_operand()]); // MMX
    }
}

impl PxorEmitter<Vec, Vec> for Assembler<'_> {
    fn pxor(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Pxor as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PxorEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pxor(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Pxor as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PxorEmitter<Vec, Mem> for Assembler<'_> {
    fn pxor(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Pxor as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl PxorEmitter<Xmm, Mem> for Assembler<'_> {
    fn pxor(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Pxor as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl RcppsEmitter<Vec, Vec> for Assembler<'_> {
    fn rcpps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Rcpps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl RcppsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn rcpps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Rcpps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl RcppsEmitter<Vec, Mem> for Assembler<'_> {
    fn rcpps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Rcpps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl RcppsEmitter<Xmm, Mem> for Assembler<'_> {
    fn rcpps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Rcpps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl RcpssEmitter<Vec, Vec> for Assembler<'_> {
    fn rcpss(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Rcpss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl RcpssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn rcpss(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Rcpss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl RcpssEmitter<Vec, Mem> for Assembler<'_> {
    fn rcpss(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Rcpss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl RcpssEmitter<Xmm, Mem> for Assembler<'_> {
    fn rcpss(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Rcpss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl RoundpdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn roundpd(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Roundpd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> RoundpdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn roundpd(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Roundpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl RoundpdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn roundpd(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Roundpd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> RoundpdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn roundpd(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Roundpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl RoundpsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn roundps(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Roundps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> RoundpsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn roundps(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Roundps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl RoundpsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn roundps(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Roundps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> RoundpsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn roundps(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Roundps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl RoundsdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn roundsd(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Roundsd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> RoundsdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn roundsd(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Roundsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl RoundsdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn roundsd(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Roundsd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> RoundsdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn roundsd(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Roundsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl RoundssEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn roundss(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Roundss as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> RoundssEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn roundss(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Roundss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl RoundssEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn roundss(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Roundss as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> RoundssEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn roundss(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Roundss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl RsqrtpsEmitter<Vec, Vec> for Assembler<'_> {
    fn rsqrtps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Rsqrtps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl RsqrtpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn rsqrtps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Rsqrtps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl RsqrtpsEmitter<Vec, Mem> for Assembler<'_> {
    fn rsqrtps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Rsqrtps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl RsqrtpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn rsqrtps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Rsqrtps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl RsqrtssEmitter<Vec, Vec> for Assembler<'_> {
    fn rsqrtss(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Rsqrtss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl RsqrtssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn rsqrtss(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Rsqrtss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl RsqrtssEmitter<Vec, Mem> for Assembler<'_> {
    fn rsqrtss(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Rsqrtss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl RsqrtssEmitter<Xmm, Mem> for Assembler<'_> {
    fn rsqrtss(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Rsqrtss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl ShufpdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn shufpd(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Shufpd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> ShufpdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn shufpd(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Shufpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE2
    }
}

impl ShufpdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn shufpd(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Shufpd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> ShufpdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn shufpd(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Shufpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE2
    }
}

impl ShufpsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn shufps(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Shufps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE
    }
}

impl<U2: Into<Imm>> ShufpsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn shufps(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Shufps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE
    }
}

impl ShufpsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn shufps(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Shufps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE
    }
}

impl<U2: Into<Imm>> ShufpsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn shufps(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Shufps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SSE
    }
}

impl SqrtpdEmitter<Vec, Vec> for Assembler<'_> {
    fn sqrtpd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Sqrtpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl SqrtpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn sqrtpd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Sqrtpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl SqrtpdEmitter<Vec, Mem> for Assembler<'_> {
    fn sqrtpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Sqrtpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl SqrtpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn sqrtpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Sqrtpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl SqrtpsEmitter<Vec, Vec> for Assembler<'_> {
    fn sqrtps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Sqrtps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl SqrtpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn sqrtps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Sqrtps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl SqrtpsEmitter<Vec, Mem> for Assembler<'_> {
    fn sqrtps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Sqrtps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl SqrtpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn sqrtps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Sqrtps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl SqrtsdEmitter<Vec, Vec> for Assembler<'_> {
    fn sqrtsd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Sqrtsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl SqrtsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn sqrtsd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Sqrtsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl SqrtsdEmitter<Vec, Mem> for Assembler<'_> {
    fn sqrtsd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Sqrtsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl SqrtsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn sqrtsd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Sqrtsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl SqrtssEmitter<Vec, Vec> for Assembler<'_> {
    fn sqrtss(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Sqrtss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl SqrtssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn sqrtss(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Sqrtss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl SqrtssEmitter<Vec, Mem> for Assembler<'_> {
    fn sqrtss(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Sqrtss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl SqrtssEmitter<Xmm, Mem> for Assembler<'_> {
    fn sqrtss(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Sqrtss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl SubpdEmitter<Vec, Vec> for Assembler<'_> {
    fn subpd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Subpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl SubpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn subpd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Subpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl SubpdEmitter<Vec, Mem> for Assembler<'_> {
    fn subpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Subpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl SubpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn subpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Subpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl SubpsEmitter<Vec, Vec> for Assembler<'_> {
    fn subps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Subps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl SubpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn subps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Subps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl SubpsEmitter<Vec, Mem> for Assembler<'_> {
    fn subps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Subps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl SubpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn subps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Subps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl SubsdEmitter<Vec, Vec> for Assembler<'_> {
    fn subsd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Subsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl SubsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn subsd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Subsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl SubsdEmitter<Vec, Mem> for Assembler<'_> {
    fn subsd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Subsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl SubsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn subsd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Subsd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl SubssEmitter<Vec, Vec> for Assembler<'_> {
    fn subss(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Subss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl SubssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn subss(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Subss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl SubssEmitter<Vec, Mem> for Assembler<'_> {
    fn subss(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Subss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl SubssEmitter<Xmm, Mem> for Assembler<'_> {
    fn subss(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Subss as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl UcomisdEmitter<Vec, Vec> for Assembler<'_> {
    fn ucomisd(&mut self, lhs: Vec, rhs: Vec) {
        self.emit_n(
            InstId::Ucomisd as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SSE2
    }
}

impl UcomisdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn ucomisd(&mut self, lhs: Xmm, rhs: Xmm) {
        self.emit_n(
            InstId::Ucomisd as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SSE2
    }
}

impl UcomisdEmitter<Vec, Mem> for Assembler<'_> {
    fn ucomisd(&mut self, lhs: Vec, rhs: Mem) {
        self.emit_n(
            InstId::Ucomisd as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SSE2
    }
}

impl UcomisdEmitter<Xmm, Mem> for Assembler<'_> {
    fn ucomisd(&mut self, lhs: Xmm, rhs: Mem) {
        self.emit_n(
            InstId::Ucomisd as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SSE2
    }
}

impl UcomissEmitter<Vec, Vec> for Assembler<'_> {
    fn ucomiss(&mut self, lhs: Vec, rhs: Vec) {
        self.emit_n(
            InstId::Ucomiss as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SSE
    }
}

impl UcomissEmitter<Xmm, Xmm> for Assembler<'_> {
    fn ucomiss(&mut self, lhs: Xmm, rhs: Xmm) {
        self.emit_n(
            InstId::Ucomiss as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SSE
    }
}

impl UcomissEmitter<Vec, Mem> for Assembler<'_> {
    fn ucomiss(&mut self, lhs: Vec, rhs: Mem) {
        self.emit_n(
            InstId::Ucomiss as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SSE
    }
}

impl UcomissEmitter<Xmm, Mem> for Assembler<'_> {
    fn ucomiss(&mut self, lhs: Xmm, rhs: Mem) {
        self.emit_n(
            InstId::Ucomiss as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SSE
    }
}

impl UnpckhpdEmitter<Vec, Vec> for Assembler<'_> {
    fn unpckhpd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Unpckhpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl UnpckhpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn unpckhpd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Unpckhpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl UnpckhpdEmitter<Vec, Mem> for Assembler<'_> {
    fn unpckhpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Unpckhpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl UnpckhpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn unpckhpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Unpckhpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl UnpckhpsEmitter<Vec, Vec> for Assembler<'_> {
    fn unpckhps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Unpckhps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl UnpckhpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn unpckhps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Unpckhps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl UnpckhpsEmitter<Vec, Mem> for Assembler<'_> {
    fn unpckhps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Unpckhps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl UnpckhpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn unpckhps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Unpckhps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl UnpcklpdEmitter<Vec, Vec> for Assembler<'_> {
    fn unpcklpd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Unpcklpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl UnpcklpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn unpcklpd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Unpcklpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl UnpcklpdEmitter<Vec, Mem> for Assembler<'_> {
    fn unpcklpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Unpcklpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl UnpcklpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn unpcklpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Unpcklpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2
    }
}

impl UnpcklpsEmitter<Vec, Vec> for Assembler<'_> {
    fn unpcklps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Unpcklps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl UnpcklpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn unpcklps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Unpcklps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl UnpcklpsEmitter<Vec, Mem> for Assembler<'_> {
    fn unpcklps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Unpcklps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl UnpcklpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn unpcklps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Unpcklps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE
    }
}

impl XorpdEmitter<Vec, Vec> for Assembler<'_> {
    fn xorpd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Xorpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl XorpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn xorpd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Xorpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl XorpdEmitter<Vec, Mem> for Assembler<'_> {
    fn xorpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Xorpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl XorpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn xorpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Xorpd as u32, &[dst.as_operand(), src.as_operand()]); // SSE2
    }
}

impl XorpsEmitter<Vec, Vec> for Assembler<'_> {
    fn xorps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Xorps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl XorpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn xorps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Xorps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl XorpsEmitter<Vec, Mem> for Assembler<'_> {
    fn xorps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Xorps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl XorpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn xorps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Xorps as u32, &[dst.as_operand(), src.as_operand()]); // SSE
    }
}

impl PavgusbEmitter<Mm, Mm> for Assembler<'_> {
    fn pavgusb(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pavgusb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // 3DNOW
    }
}

impl PavgusbEmitter<Mm, Mem> for Assembler<'_> {
    fn pavgusb(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pavgusb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // 3DNOW
    }
}

impl Pf2idEmitter<Mm, Mm> for Assembler<'_> {
    fn pf2id(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pf2id as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl Pf2idEmitter<Mm, Mem> for Assembler<'_> {
    fn pf2id(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pf2id as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl Pf2iwEmitter<Mm, Mm> for Assembler<'_> {
    fn pf2iw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pf2iw as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl Pf2iwEmitter<Mm, Mem> for Assembler<'_> {
    fn pf2iw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pf2iw as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl PfaccEmitter<Mm, Mm> for Assembler<'_> {
    fn pfacc(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pfacc as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl PfaccEmitter<Mm, Mem> for Assembler<'_> {
    fn pfacc(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pfacc as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl PfaddEmitter<Mm, Mm> for Assembler<'_> {
    fn pfadd(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pfadd as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl PfaddEmitter<Mm, Mem> for Assembler<'_> {
    fn pfadd(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pfadd as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl PfcmpeqEmitter<Mm, Mm> for Assembler<'_> {
    fn pfcmpeq(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pfcmpeq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // 3DNOW
    }
}

impl PfcmpeqEmitter<Mm, Mem> for Assembler<'_> {
    fn pfcmpeq(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pfcmpeq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // 3DNOW
    }
}

impl PfcmpgeEmitter<Mm, Mm> for Assembler<'_> {
    fn pfcmpge(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pfcmpge as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // 3DNOW
    }
}

impl PfcmpgeEmitter<Mm, Mem> for Assembler<'_> {
    fn pfcmpge(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pfcmpge as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // 3DNOW
    }
}

impl PfcmpgtEmitter<Mm, Mm> for Assembler<'_> {
    fn pfcmpgt(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pfcmpgt as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // 3DNOW
    }
}

impl PfcmpgtEmitter<Mm, Mem> for Assembler<'_> {
    fn pfcmpgt(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pfcmpgt as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // 3DNOW
    }
}

impl PfmaxEmitter<Mm, Mm> for Assembler<'_> {
    fn pfmax(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pfmax as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl PfmaxEmitter<Mm, Mem> for Assembler<'_> {
    fn pfmax(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pfmax as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl PfminEmitter<Mm, Mm> for Assembler<'_> {
    fn pfmin(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pfmin as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl PfminEmitter<Mm, Mem> for Assembler<'_> {
    fn pfmin(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pfmin as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl PfmulEmitter<Mm, Mm> for Assembler<'_> {
    fn pfmul(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pfmul as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl PfmulEmitter<Mm, Mem> for Assembler<'_> {
    fn pfmul(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pfmul as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl PfnaccEmitter<Mm, Mm> for Assembler<'_> {
    fn pfnacc(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pfnacc as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl PfnaccEmitter<Mm, Mem> for Assembler<'_> {
    fn pfnacc(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pfnacc as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl PfpnaccEmitter<Mm, Mm> for Assembler<'_> {
    fn pfpnacc(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pfpnacc as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // 3DNOW
    }
}

impl PfpnaccEmitter<Mm, Mem> for Assembler<'_> {
    fn pfpnacc(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pfpnacc as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // 3DNOW
    }
}

impl PfrcpEmitter<Mm, Mm> for Assembler<'_> {
    fn pfrcp(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pfrcp as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl PfrcpEmitter<Mm, Mem> for Assembler<'_> {
    fn pfrcp(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pfrcp as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl Pfrcpit1Emitter<Mm, Mm> for Assembler<'_> {
    fn pfrcpit1(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pfrcpit1 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // 3DNOW
    }
}

impl Pfrcpit1Emitter<Mm, Mem> for Assembler<'_> {
    fn pfrcpit1(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pfrcpit1 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // 3DNOW
    }
}

impl Pfrcpit2Emitter<Mm, Mm> for Assembler<'_> {
    fn pfrcpit2(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pfrcpit2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // 3DNOW
    }
}

impl Pfrcpit2Emitter<Mm, Mem> for Assembler<'_> {
    fn pfrcpit2(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pfrcpit2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // 3DNOW
    }
}

impl PfrcpvEmitter<Mm, Mm> for Assembler<'_> {
    fn pfrcpv(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pfrcpv as u32, &[dst.as_operand(), src.as_operand()]); // GEODE
    }
}

impl PfrcpvEmitter<Mm, Mem> for Assembler<'_> {
    fn pfrcpv(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pfrcpv as u32, &[dst.as_operand(), src.as_operand()]); // GEODE
    }
}

impl Pfrsqit1Emitter<Mm, Mm> for Assembler<'_> {
    fn pfrsqit1(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pfrsqit1 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // 3DNOW
    }
}

impl Pfrsqit1Emitter<Mm, Mem> for Assembler<'_> {
    fn pfrsqit1(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pfrsqit1 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // 3DNOW
    }
}

impl PfrsqrtEmitter<Mm, Mm> for Assembler<'_> {
    fn pfrsqrt(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pfrsqrt as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // 3DNOW
    }
}

impl PfrsqrtEmitter<Mm, Mem> for Assembler<'_> {
    fn pfrsqrt(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pfrsqrt as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // 3DNOW
    }
}

impl PfrsqrtvEmitter<Mm, Mm> for Assembler<'_> {
    fn pfrsqrtv(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pfrsqrtv as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // GEODE
    }
}

impl PfrsqrtvEmitter<Mm, Mem> for Assembler<'_> {
    fn pfrsqrtv(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pfrsqrtv as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // GEODE
    }
}

impl PfsubEmitter<Mm, Mm> for Assembler<'_> {
    fn pfsub(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pfsub as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl PfsubEmitter<Mm, Mem> for Assembler<'_> {
    fn pfsub(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pfsub as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl PfsubrEmitter<Mm, Mm> for Assembler<'_> {
    fn pfsubr(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pfsubr as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl PfsubrEmitter<Mm, Mem> for Assembler<'_> {
    fn pfsubr(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pfsubr as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl Pi2fdEmitter<Mm, Mm> for Assembler<'_> {
    fn pi2fd(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pi2fd as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl Pi2fdEmitter<Mm, Mem> for Assembler<'_> {
    fn pi2fd(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pi2fd as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl Pi2fwEmitter<Mm, Mm> for Assembler<'_> {
    fn pi2fw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pi2fw as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl Pi2fwEmitter<Mm, Mem> for Assembler<'_> {
    fn pi2fw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pi2fw as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl PmulhrwEmitter<Mm, Mm> for Assembler<'_> {
    fn pmulhrw(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Pmulhrw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // 3DNOW
    }
}

impl PmulhrwEmitter<Mm, Mem> for Assembler<'_> {
    fn pmulhrw(&mut self, dst: Mm, src: Mem) {
        self.emit_n(
            InstId::Pmulhrw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // 3DNOW
    }
}

impl PswapdEmitter<Mm, Mm> for Assembler<'_> {
    fn pswapd(&mut self, dst: Mm, src: Mm) {
        self.emit_n(InstId::Pswapd as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl PswapdEmitter<Mm, Mem> for Assembler<'_> {
    fn pswapd(&mut self, dst: Mm, src: Mem) {
        self.emit_n(InstId::Pswapd as u32, &[dst.as_operand(), src.as_operand()]); // 3DNOW
    }
}

impl EmmsEmitter for Assembler<'_> {
    fn emms(&mut self) {
        self.emit_n(InstId::Emms as u32, &[]); // MMX
    }
}

impl FemmsEmitter for Assembler<'_> {
    fn femms(&mut self) {
        self.emit_n(InstId::Femms as u32, &[]); // 3DNOW
    }
}

impl AesdecEmitter<Vec, Vec> for Assembler<'_> {
    fn aesdec(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Aesdec as u32, &[dst.as_operand(), src.as_operand()]); // AESNI
    }
}

impl AesdecEmitter<Xmm, Xmm> for Assembler<'_> {
    fn aesdec(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Aesdec as u32, &[dst.as_operand(), src.as_operand()]); // AESNI
    }
}

impl AesdecEmitter<Vec, Mem> for Assembler<'_> {
    fn aesdec(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Aesdec as u32, &[dst.as_operand(), src.as_operand()]); // AESNI
    }
}

impl AesdecEmitter<Xmm, Mem> for Assembler<'_> {
    fn aesdec(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Aesdec as u32, &[dst.as_operand(), src.as_operand()]); // AESNI
    }
}

impl AesdeclastEmitter<Vec, Vec> for Assembler<'_> {
    fn aesdeclast(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Aesdeclast as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AESNI
    }
}

impl AesdeclastEmitter<Xmm, Xmm> for Assembler<'_> {
    fn aesdeclast(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Aesdeclast as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AESNI
    }
}

impl AesdeclastEmitter<Vec, Mem> for Assembler<'_> {
    fn aesdeclast(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Aesdeclast as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AESNI
    }
}

impl AesdeclastEmitter<Xmm, Mem> for Assembler<'_> {
    fn aesdeclast(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Aesdeclast as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AESNI
    }
}

impl AesencEmitter<Vec, Vec> for Assembler<'_> {
    fn aesenc(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Aesenc as u32, &[dst.as_operand(), src.as_operand()]); // AESNI
    }
}

impl AesencEmitter<Xmm, Xmm> for Assembler<'_> {
    fn aesenc(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Aesenc as u32, &[dst.as_operand(), src.as_operand()]); // AESNI
    }
}

impl AesencEmitter<Vec, Mem> for Assembler<'_> {
    fn aesenc(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Aesenc as u32, &[dst.as_operand(), src.as_operand()]); // AESNI
    }
}

impl AesencEmitter<Xmm, Mem> for Assembler<'_> {
    fn aesenc(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Aesenc as u32, &[dst.as_operand(), src.as_operand()]); // AESNI
    }
}

impl AesenclastEmitter<Vec, Vec> for Assembler<'_> {
    fn aesenclast(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Aesenclast as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AESNI
    }
}

impl AesenclastEmitter<Xmm, Xmm> for Assembler<'_> {
    fn aesenclast(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Aesenclast as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AESNI
    }
}

impl AesenclastEmitter<Vec, Mem> for Assembler<'_> {
    fn aesenclast(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Aesenclast as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AESNI
    }
}

impl AesenclastEmitter<Xmm, Mem> for Assembler<'_> {
    fn aesenclast(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Aesenclast as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AESNI
    }
}

impl AesimcEmitter<Vec, Vec> for Assembler<'_> {
    fn aesimc(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Aesimc as u32, &[dst.as_operand(), src.as_operand()]); // AESNI
    }
}

impl AesimcEmitter<Xmm, Xmm> for Assembler<'_> {
    fn aesimc(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Aesimc as u32, &[dst.as_operand(), src.as_operand()]); // AESNI
    }
}

impl AesimcEmitter<Vec, Mem> for Assembler<'_> {
    fn aesimc(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Aesimc as u32, &[dst.as_operand(), src.as_operand()]); // AESNI
    }
}

impl AesimcEmitter<Xmm, Mem> for Assembler<'_> {
    fn aesimc(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Aesimc as u32, &[dst.as_operand(), src.as_operand()]); // AESNI
    }
}

impl AeskeygenassistEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn aeskeygenassist(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Aeskeygenassist as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AESNI
    }
}

impl<U2: Into<Imm>> AeskeygenassistEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn aeskeygenassist(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Aeskeygenassist as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AESNI
    }
}

impl AeskeygenassistEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn aeskeygenassist(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Aeskeygenassist as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AESNI
    }
}

impl<U2: Into<Imm>> AeskeygenassistEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn aeskeygenassist(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Aeskeygenassist as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AESNI
    }
}

impl Sha1msg1Emitter<Vec, Vec> for Assembler<'_> {
    fn sha1msg1(&mut self, lhs: Vec, rhs: Vec) {
        self.emit_n(
            InstId::Sha1msg1 as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SHA
    }
}

impl Sha1msg1Emitter<Xmm, Xmm> for Assembler<'_> {
    fn sha1msg1(&mut self, lhs: Xmm, rhs: Xmm) {
        self.emit_n(
            InstId::Sha1msg1 as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SHA
    }
}

impl Sha1msg1Emitter<Vec, Mem> for Assembler<'_> {
    fn sha1msg1(&mut self, lhs: Vec, rhs: Mem) {
        self.emit_n(
            InstId::Sha1msg1 as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SHA
    }
}

impl Sha1msg1Emitter<Xmm, Mem> for Assembler<'_> {
    fn sha1msg1(&mut self, lhs: Xmm, rhs: Mem) {
        self.emit_n(
            InstId::Sha1msg1 as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SHA
    }
}

impl Sha1msg2Emitter<Vec, Vec> for Assembler<'_> {
    fn sha1msg2(&mut self, lhs: Vec, rhs: Vec) {
        self.emit_n(
            InstId::Sha1msg2 as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SHA
    }
}

impl Sha1msg2Emitter<Xmm, Xmm> for Assembler<'_> {
    fn sha1msg2(&mut self, lhs: Xmm, rhs: Xmm) {
        self.emit_n(
            InstId::Sha1msg2 as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SHA
    }
}

impl Sha1msg2Emitter<Vec, Mem> for Assembler<'_> {
    fn sha1msg2(&mut self, lhs: Vec, rhs: Mem) {
        self.emit_n(
            InstId::Sha1msg2 as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SHA
    }
}

impl Sha1msg2Emitter<Xmm, Mem> for Assembler<'_> {
    fn sha1msg2(&mut self, lhs: Xmm, rhs: Mem) {
        self.emit_n(
            InstId::Sha1msg2 as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SHA
    }
}

impl Sha1nexteEmitter<Vec, Vec> for Assembler<'_> {
    fn sha1nexte(&mut self, lhs: Vec, rhs: Vec) {
        self.emit_n(
            InstId::Sha1nexte as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SHA
    }
}

impl Sha1nexteEmitter<Xmm, Xmm> for Assembler<'_> {
    fn sha1nexte(&mut self, lhs: Xmm, rhs: Xmm) {
        self.emit_n(
            InstId::Sha1nexte as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SHA
    }
}

impl Sha1nexteEmitter<Vec, Mem> for Assembler<'_> {
    fn sha1nexte(&mut self, lhs: Vec, rhs: Mem) {
        self.emit_n(
            InstId::Sha1nexte as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SHA
    }
}

impl Sha1nexteEmitter<Xmm, Mem> for Assembler<'_> {
    fn sha1nexte(&mut self, lhs: Xmm, rhs: Mem) {
        self.emit_n(
            InstId::Sha1nexte as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SHA
    }
}

impl Sha1rnds4Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn sha1rnds4(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Sha1rnds4 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SHA
    }
}

impl<U2: Into<Imm>> Sha1rnds4Emitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn sha1rnds4(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Sha1rnds4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SHA
    }
}

impl Sha1rnds4Emitter<Vec, Mem, Imm> for Assembler<'_> {
    fn sha1rnds4(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Sha1rnds4 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SHA
    }
}

impl<U2: Into<Imm>> Sha1rnds4Emitter<Xmm, Mem, U2> for Assembler<'_> {
    fn sha1rnds4(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Sha1rnds4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // SHA
    }
}

impl Sha256msg1Emitter<Vec, Vec> for Assembler<'_> {
    fn sha256msg1(&mut self, lhs: Vec, rhs: Vec) {
        self.emit_n(
            InstId::Sha256msg1 as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SHA
    }
}

impl Sha256msg1Emitter<Xmm, Xmm> for Assembler<'_> {
    fn sha256msg1(&mut self, lhs: Xmm, rhs: Xmm) {
        self.emit_n(
            InstId::Sha256msg1 as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SHA
    }
}

impl Sha256msg1Emitter<Vec, Mem> for Assembler<'_> {
    fn sha256msg1(&mut self, lhs: Vec, rhs: Mem) {
        self.emit_n(
            InstId::Sha256msg1 as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SHA
    }
}

impl Sha256msg1Emitter<Xmm, Mem> for Assembler<'_> {
    fn sha256msg1(&mut self, lhs: Xmm, rhs: Mem) {
        self.emit_n(
            InstId::Sha256msg1 as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SHA
    }
}

impl Sha256msg2Emitter<Vec, Vec> for Assembler<'_> {
    fn sha256msg2(&mut self, lhs: Vec, rhs: Vec) {
        self.emit_n(
            InstId::Sha256msg2 as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SHA
    }
}

impl Sha256msg2Emitter<Xmm, Xmm> for Assembler<'_> {
    fn sha256msg2(&mut self, lhs: Xmm, rhs: Xmm) {
        self.emit_n(
            InstId::Sha256msg2 as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SHA
    }
}

impl Sha256msg2Emitter<Vec, Mem> for Assembler<'_> {
    fn sha256msg2(&mut self, lhs: Vec, rhs: Mem) {
        self.emit_n(
            InstId::Sha256msg2 as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SHA
    }
}

impl Sha256msg2Emitter<Xmm, Mem> for Assembler<'_> {
    fn sha256msg2(&mut self, lhs: Xmm, rhs: Mem) {
        self.emit_n(
            InstId::Sha256msg2 as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // SHA
    }
}

impl Sha256rnds2Emitter<Vec, Vec, Vec> for Assembler<'_> {
    fn sha256rnds2(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Sha256rnds2 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // SHA [EXPLICIT]
    }
}

impl Sha256rnds2Emitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn sha256rnds2(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Sha256rnds2 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // SHA [EXPLICIT]
    }
}

impl Sha256rnds2Emitter<Vec, Mem, Vec> for Assembler<'_> {
    fn sha256rnds2(&mut self, dst: Vec, src1: Mem, src2: Vec) {
        self.emit_n(
            InstId::Sha256rnds2 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // SHA [EXPLICIT]
    }
}

impl Sha256rnds2Emitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn sha256rnds2(&mut self, dst: Xmm, src1: Mem, src2: Xmm) {
        self.emit_n(
            InstId::Sha256rnds2 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // SHA [EXPLICIT]
    }
}

impl Gf2p8affineinvqbEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn gf2p8affineinvqb(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Gf2p8affineinvqb as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // GFNI
    }
}

impl<U2: Into<Imm>> Gf2p8affineinvqbEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn gf2p8affineinvqb(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Gf2p8affineinvqb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // GFNI
    }
}

impl Gf2p8affineinvqbEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn gf2p8affineinvqb(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Gf2p8affineinvqb as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // GFNI
    }
}

impl<U2: Into<Imm>> Gf2p8affineinvqbEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn gf2p8affineinvqb(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Gf2p8affineinvqb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // GFNI
    }
}

impl Gf2p8affineqbEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn gf2p8affineqb(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Gf2p8affineqb as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // GFNI
    }
}

impl<U2: Into<Imm>> Gf2p8affineqbEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn gf2p8affineqb(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Gf2p8affineqb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // GFNI
    }
}

impl Gf2p8affineqbEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn gf2p8affineqb(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Gf2p8affineqb as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // GFNI
    }
}

impl<U2: Into<Imm>> Gf2p8affineqbEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn gf2p8affineqb(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Gf2p8affineqb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // GFNI
    }
}

impl Gf2p8mulbEmitter<Vec, Vec> for Assembler<'_> {
    fn gf2p8mulb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Gf2p8mulb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // GFNI
    }
}

impl Gf2p8mulbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn gf2p8mulb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Gf2p8mulb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // GFNI
    }
}

impl Gf2p8mulbEmitter<Vec, Mem> for Assembler<'_> {
    fn gf2p8mulb(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Gf2p8mulb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // GFNI
    }
}

impl Gf2p8mulbEmitter<Xmm, Mem> for Assembler<'_> {
    fn gf2p8mulb(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Gf2p8mulb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // GFNI
    }
}

impl KaddbEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kaddb(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kaddb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_DQ
    }
}

impl KadddEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kaddd(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kaddd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW
    }
}

impl KaddqEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kaddq(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kaddq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW
    }
}

impl KaddwEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kaddw(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kaddw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_DQ
    }
}

impl KandbEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kandb(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kandb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_DQ
    }
}

impl KanddEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kandd(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kandd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW
    }
}

impl KandnbEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kandnb(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kandnb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_DQ
    }
}

impl KandndEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kandnd(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kandnd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW
    }
}

impl KandnqEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kandnq(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kandnq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW
    }
}

impl KandnwEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kandnw(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kandnw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F
    }
}

impl KandqEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kandq(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kandq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW
    }
}

impl KandwEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kandw(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kandw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F
    }
}

impl KmovbEmitter<KReg, KReg> for Assembler<'_> {
    fn kmovb(&mut self, dst: KReg, src: KReg) {
        self.emit_n(InstId::Kmovb as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_DQ
    }
}

impl KmovbEmitter<KReg, Mem> for Assembler<'_> {
    fn kmovb(&mut self, dst: KReg, src: Mem) {
        self.emit_n(InstId::Kmovb as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_DQ
    }
}

impl KmovbEmitter<KReg, Gp> for Assembler<'_> {
    fn kmovb(&mut self, dst: KReg, src: Gp) {
        self.emit_n(InstId::Kmovb as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_DQ
    }
}

impl KmovbEmitter<KReg, Gpd> for Assembler<'_> {
    fn kmovb(&mut self, dst: KReg, src: Gpd) {
        self.emit_n(InstId::Kmovb as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_DQ
    }
}

impl KmovbEmitter<Mem, KReg> for Assembler<'_> {
    fn kmovb(&mut self, dst: Mem, src: KReg) {
        self.emit_n(InstId::Kmovb as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_DQ
    }
}

impl KmovbEmitter<Gp, KReg> for Assembler<'_> {
    fn kmovb(&mut self, dst: Gp, src: KReg) {
        self.emit_n(InstId::Kmovb as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_DQ
    }
}

impl KmovbEmitter<Gpd, KReg> for Assembler<'_> {
    fn kmovb(&mut self, dst: Gpd, src: KReg) {
        self.emit_n(InstId::Kmovb as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_DQ
    }
}

impl KmovdEmitter<KReg, KReg> for Assembler<'_> {
    fn kmovd(&mut self, dst: KReg, src: KReg) {
        self.emit_n(InstId::Kmovd as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_BW
    }
}

impl KmovdEmitter<KReg, Mem> for Assembler<'_> {
    fn kmovd(&mut self, dst: KReg, src: Mem) {
        self.emit_n(InstId::Kmovd as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_BW
    }
}

impl KmovdEmitter<KReg, Gp> for Assembler<'_> {
    fn kmovd(&mut self, dst: KReg, src: Gp) {
        self.emit_n(InstId::Kmovd as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_BW
    }
}

impl KmovdEmitter<KReg, Gpd> for Assembler<'_> {
    fn kmovd(&mut self, dst: KReg, src: Gpd) {
        self.emit_n(InstId::Kmovd as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_BW
    }
}

impl KmovdEmitter<Mem, KReg> for Assembler<'_> {
    fn kmovd(&mut self, dst: Mem, src: KReg) {
        self.emit_n(InstId::Kmovd as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_BW
    }
}

impl KmovdEmitter<Gp, KReg> for Assembler<'_> {
    fn kmovd(&mut self, dst: Gp, src: KReg) {
        self.emit_n(InstId::Kmovd as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_BW
    }
}

impl KmovdEmitter<Gpd, KReg> for Assembler<'_> {
    fn kmovd(&mut self, dst: Gpd, src: KReg) {
        self.emit_n(InstId::Kmovd as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_BW
    }
}

impl KmovqEmitter<KReg, KReg> for Assembler<'_> {
    fn kmovq(&mut self, dst: KReg, src: KReg) {
        self.emit_n(InstId::Kmovq as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_BW
    }
}

impl KmovqEmitter<KReg, Mem> for Assembler<'_> {
    fn kmovq(&mut self, dst: KReg, src: Mem) {
        self.emit_n(InstId::Kmovq as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_BW
    }
}

impl KmovqEmitter<KReg, Gp> for Assembler<'_> {
    fn kmovq(&mut self, dst: KReg, src: Gp) {
        self.emit_n(InstId::Kmovq as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_BW
    }
}

impl KmovqEmitter<KReg, Gpq> for Assembler<'_> {
    fn kmovq(&mut self, dst: KReg, src: Gpq) {
        self.emit_n(InstId::Kmovq as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_BW
    }
}

impl KmovqEmitter<Mem, KReg> for Assembler<'_> {
    fn kmovq(&mut self, dst: Mem, src: KReg) {
        self.emit_n(InstId::Kmovq as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_BW
    }
}

impl KmovqEmitter<Gp, KReg> for Assembler<'_> {
    fn kmovq(&mut self, dst: Gp, src: KReg) {
        self.emit_n(InstId::Kmovq as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_BW
    }
}

impl KmovqEmitter<Gpq, KReg> for Assembler<'_> {
    fn kmovq(&mut self, dst: Gpq, src: KReg) {
        self.emit_n(InstId::Kmovq as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_BW
    }
}

impl KmovwEmitter<KReg, KReg> for Assembler<'_> {
    fn kmovw(&mut self, dst: KReg, src: KReg) {
        self.emit_n(InstId::Kmovw as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_F
    }
}

impl KmovwEmitter<KReg, Mem> for Assembler<'_> {
    fn kmovw(&mut self, dst: KReg, src: Mem) {
        self.emit_n(InstId::Kmovw as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_F
    }
}

impl KmovwEmitter<KReg, Gp> for Assembler<'_> {
    fn kmovw(&mut self, dst: KReg, src: Gp) {
        self.emit_n(InstId::Kmovw as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_F
    }
}

impl KmovwEmitter<KReg, Gpd> for Assembler<'_> {
    fn kmovw(&mut self, dst: KReg, src: Gpd) {
        self.emit_n(InstId::Kmovw as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_F
    }
}

impl KmovwEmitter<Mem, KReg> for Assembler<'_> {
    fn kmovw(&mut self, dst: Mem, src: KReg) {
        self.emit_n(InstId::Kmovw as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_F
    }
}

impl KmovwEmitter<Gp, KReg> for Assembler<'_> {
    fn kmovw(&mut self, dst: Gp, src: KReg) {
        self.emit_n(InstId::Kmovw as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_F
    }
}

impl KmovwEmitter<Gpd, KReg> for Assembler<'_> {
    fn kmovw(&mut self, dst: Gpd, src: KReg) {
        self.emit_n(InstId::Kmovw as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_F
    }
}

impl KnotbEmitter<KReg, KReg> for Assembler<'_> {
    fn knotb(&mut self, dst: KReg, src: KReg) {
        self.emit_n(InstId::Knotb as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_DQ
    }
}

impl KnotdEmitter<KReg, KReg> for Assembler<'_> {
    fn knotd(&mut self, dst: KReg, src: KReg) {
        self.emit_n(InstId::Knotd as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_BW
    }
}

impl KnotqEmitter<KReg, KReg> for Assembler<'_> {
    fn knotq(&mut self, dst: KReg, src: KReg) {
        self.emit_n(InstId::Knotq as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_BW
    }
}

impl KnotwEmitter<KReg, KReg> for Assembler<'_> {
    fn knotw(&mut self, dst: KReg, src: KReg) {
        self.emit_n(InstId::Knotw as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_F
    }
}

impl KorbEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn korb(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Korb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_DQ
    }
}

impl KordEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kord(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kord as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW
    }
}

impl KorqEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn korq(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Korq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW
    }
}

impl KortestbEmitter<KReg, KReg> for Assembler<'_> {
    fn kortestb(&mut self, lhs: KReg, rhs: KReg) {
        self.emit_n(
            InstId::Kortestb as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX512_DQ
    }
}

impl KortestdEmitter<KReg, KReg> for Assembler<'_> {
    fn kortestd(&mut self, lhs: KReg, rhs: KReg) {
        self.emit_n(
            InstId::Kortestd as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX512_BW
    }
}

impl KortestqEmitter<KReg, KReg> for Assembler<'_> {
    fn kortestq(&mut self, lhs: KReg, rhs: KReg) {
        self.emit_n(
            InstId::Kortestq as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX512_BW
    }
}

impl KortestwEmitter<KReg, KReg> for Assembler<'_> {
    fn kortestw(&mut self, lhs: KReg, rhs: KReg) {
        self.emit_n(
            InstId::Kortestw as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX512_F
    }
}

impl KorwEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn korw(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Korw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F
    }
}

impl<U2: Into<Imm>> KshiftlbEmitter<KReg, KReg, U2> for Assembler<'_> {
    fn kshiftlb(&mut self, dst: KReg, src1: KReg, src2: U2) {
        self.emit_n(
            InstId::Kshiftlb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ
    }
}

impl<U2: Into<Imm>> KshiftldEmitter<KReg, KReg, U2> for Assembler<'_> {
    fn kshiftld(&mut self, dst: KReg, src1: KReg, src2: U2) {
        self.emit_n(
            InstId::Kshiftld as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_BW
    }
}

impl<U2: Into<Imm>> KshiftlqEmitter<KReg, KReg, U2> for Assembler<'_> {
    fn kshiftlq(&mut self, dst: KReg, src1: KReg, src2: U2) {
        self.emit_n(
            InstId::Kshiftlq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_BW
    }
}

impl<U2: Into<Imm>> KshiftlwEmitter<KReg, KReg, U2> for Assembler<'_> {
    fn kshiftlw(&mut self, dst: KReg, src1: KReg, src2: U2) {
        self.emit_n(
            InstId::Kshiftlw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F
    }
}

impl<U2: Into<Imm>> KshiftrbEmitter<KReg, KReg, U2> for Assembler<'_> {
    fn kshiftrb(&mut self, dst: KReg, src1: KReg, src2: U2) {
        self.emit_n(
            InstId::Kshiftrb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ
    }
}

impl<U2: Into<Imm>> KshiftrdEmitter<KReg, KReg, U2> for Assembler<'_> {
    fn kshiftrd(&mut self, dst: KReg, src1: KReg, src2: U2) {
        self.emit_n(
            InstId::Kshiftrd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_BW
    }
}

impl<U2: Into<Imm>> KshiftrqEmitter<KReg, KReg, U2> for Assembler<'_> {
    fn kshiftrq(&mut self, dst: KReg, src1: KReg, src2: U2) {
        self.emit_n(
            InstId::Kshiftrq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_BW
    }
}

impl<U2: Into<Imm>> KshiftrwEmitter<KReg, KReg, U2> for Assembler<'_> {
    fn kshiftrw(&mut self, dst: KReg, src1: KReg, src2: U2) {
        self.emit_n(
            InstId::Kshiftrw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F
    }
}

impl KtestbEmitter<KReg, KReg> for Assembler<'_> {
    fn ktestb(&mut self, lhs: KReg, rhs: KReg) {
        self.emit_n(InstId::Ktestb as u32, &[lhs.as_operand(), rhs.as_operand()]); // AVX512_DQ
    }
}

impl KtestdEmitter<KReg, KReg> for Assembler<'_> {
    fn ktestd(&mut self, lhs: KReg, rhs: KReg) {
        self.emit_n(InstId::Ktestd as u32, &[lhs.as_operand(), rhs.as_operand()]); // AVX512_BW
    }
}

impl KtestqEmitter<KReg, KReg> for Assembler<'_> {
    fn ktestq(&mut self, lhs: KReg, rhs: KReg) {
        self.emit_n(InstId::Ktestq as u32, &[lhs.as_operand(), rhs.as_operand()]); // AVX512_BW
    }
}

impl KtestwEmitter<KReg, KReg> for Assembler<'_> {
    fn ktestw(&mut self, lhs: KReg, rhs: KReg) {
        self.emit_n(InstId::Ktestw as u32, &[lhs.as_operand(), rhs.as_operand()]); // AVX512_DQ
    }
}

impl KunpckbwEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kunpckbw(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kunpckbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F
    }
}

impl KunpckdqEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kunpckdq(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kunpckdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW
    }
}

impl KunpckwdEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kunpckwd(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kunpckwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW
    }
}

impl KxnorbEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kxnorb(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kxnorb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_DQ
    }
}

impl KxnordEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kxnord(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kxnord as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW
    }
}

impl KxnorqEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kxnorq(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kxnorq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW
    }
}

impl KxnorwEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kxnorw(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kxnorw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F
    }
}

impl KxorbEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kxorb(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kxorb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_DQ
    }
}

impl KxordEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kxord(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kxord as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW
    }
}

impl KxorqEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kxorq(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kxorq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW
    }
}

impl KxorwEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kxorw(&mut self, dst: KReg, src1: KReg, src2: KReg) {
        self.emit_n(
            InstId::Kxorw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F
    }
}

impl VaddpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaddpd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vaddpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VaddpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaddpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vaddpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VaddpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vaddpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vaddpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VaddpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vaddpd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vaddpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VaddpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaddpd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vaddpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VaddpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaddpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vaddpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VaddpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vaddpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vaddpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VaddpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vaddpd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vaddpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VaddpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaddps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vaddps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VaddpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaddps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vaddps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VaddpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vaddps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vaddps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VaddpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vaddps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vaddps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VaddpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaddps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vaddps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VaddpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaddps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vaddps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VaddpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vaddps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vaddps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VaddpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vaddps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vaddps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VaddsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaddsd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vaddsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VaddsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaddsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vaddsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VaddsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaddsd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vaddsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VaddsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaddsd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vaddsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VaddssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaddss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vaddss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VaddssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaddss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vaddss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VaddssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaddss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vaddss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VaddssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaddss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vaddss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VaddsubpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaddsubpd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vaddsubpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaddsubpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vaddsubpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vaddsubpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vaddsubpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaddsubpd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vaddsubpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaddsubpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vaddsubpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vaddsubpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vaddsubpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaddsubps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vaddsubps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaddsubps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vaddsubps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vaddsubps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vaddsubps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaddsubps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vaddsubps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaddsubps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vaddsubps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vaddsubps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vaddsubps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VaesdecEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaesdec(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vaesdec as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdecEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaesdec(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vaesdec as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdecEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vaesdec(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vaesdec as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdecEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vaesdec(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vaesdec as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdecEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaesdec(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vaesdec as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdecEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaesdec(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vaesdec as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdecEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vaesdec(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vaesdec as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdecEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vaesdec(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vaesdec as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdeclastEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaesdeclast(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vaesdeclast as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdeclastEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaesdeclast(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vaesdeclast as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdeclastEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vaesdeclast(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vaesdeclast as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdeclastEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vaesdeclast(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vaesdeclast as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdeclastEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaesdeclast(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vaesdeclast as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdeclastEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaesdeclast(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vaesdeclast as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdeclastEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vaesdeclast(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vaesdeclast as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdeclastEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vaesdeclast(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vaesdeclast as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesencEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaesenc(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vaesenc as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesencEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaesenc(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vaesenc as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesencEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vaesenc(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vaesenc as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesencEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vaesenc(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vaesenc as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesencEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaesenc(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vaesenc as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesencEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaesenc(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vaesenc as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesencEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vaesenc(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vaesenc as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesencEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vaesenc(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vaesenc as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesenclastEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaesenclast(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vaesenclast as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesenclastEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaesenclast(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vaesenclast as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesenclastEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vaesenclast(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vaesenclast as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesenclastEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vaesenclast(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vaesenclast as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesenclastEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaesenclast(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vaesenclast as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesenclastEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaesenclast(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vaesenclast as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesenclastEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vaesenclast(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vaesenclast as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesenclastEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vaesenclast(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vaesenclast as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesimcEmitter<Vec, Vec> for Assembler<'_> {
    fn vaesimc(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vaesimc as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+AESNI
    }
}

impl VaesimcEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vaesimc(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vaesimc as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+AESNI
    }
}

impl VaesimcEmitter<Vec, Mem> for Assembler<'_> {
    fn vaesimc(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vaesimc as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+AESNI
    }
}

impl VaesimcEmitter<Xmm, Mem> for Assembler<'_> {
    fn vaesimc(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vaesimc as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+AESNI
    }
}

impl VaeskeygenassistEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vaeskeygenassist(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vaeskeygenassist as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX+AESNI
    }
}

impl<U2: Into<Imm>> VaeskeygenassistEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vaeskeygenassist(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vaeskeygenassist as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+AESNI
    }
}

impl VaeskeygenassistEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vaeskeygenassist(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vaeskeygenassist as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX+AESNI
    }
}

impl<U2: Into<Imm>> VaeskeygenassistEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vaeskeygenassist(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vaeskeygenassist as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+AESNI
    }
}

impl ValigndEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn valignd(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Valignd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> ValigndEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn valignd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Valignd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> ValigndEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn valignd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Valignd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> ValigndEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn valignd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Valignd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl ValigndEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn valignd(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Valignd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> ValigndEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn valignd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Valignd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> ValigndEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn valignd(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Valignd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> ValigndEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn valignd(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Valignd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl ValignqEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn valignq(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Valignq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> ValignqEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn valignq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Valignq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> ValignqEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn valignq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Valignq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> ValignqEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn valignq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Valignq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl ValignqEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn valignq(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Valignq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> ValignqEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn valignq(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Valignq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> ValignqEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn valignq(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Valignq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> ValignqEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn valignq(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Valignq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VandnpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vandnpd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vandnpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandnpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vandnpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vandnpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandnpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vandnpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vandnpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandnpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vandnpd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vandnpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandnpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vandnpd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vandnpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandnpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vandnpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vandnpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandnpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vandnpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vandnpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandnpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vandnpd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vandnpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandnpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vandnps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vandnps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandnpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vandnps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vandnps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandnpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vandnps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vandnps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandnpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vandnps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vandnps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandnpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vandnps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vandnps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandnpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vandnps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vandnps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandnpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vandnps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vandnps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandnpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vandnps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vandnps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vandpd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vandpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vandpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vandpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vandpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vandpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vandpd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vandpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vandpd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vandpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vandpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vandpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vandpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vandpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vandpd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vandpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vandps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vandps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vandps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vandps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vandps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vandps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vandps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vandps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vandps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vandps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vandps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vandps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vandps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vandps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vandps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vandps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VblendmpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vblendmpd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vblendmpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VblendmpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vblendmpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vblendmpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VblendmpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vblendmpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vblendmpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VblendmpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vblendmpd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vblendmpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VblendmpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vblendmpd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vblendmpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VblendmpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vblendmpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vblendmpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VblendmpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vblendmpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vblendmpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VblendmpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vblendmpd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vblendmpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VblendmpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vblendmps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vblendmps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VblendmpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vblendmps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vblendmps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VblendmpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vblendmps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vblendmps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VblendmpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vblendmps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vblendmps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VblendmpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vblendmps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vblendmps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VblendmpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vblendmps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vblendmps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VblendmpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vblendmps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vblendmps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VblendmpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vblendmps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vblendmps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VblendpdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vblendpd(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vblendpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VblendpdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vblendpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vblendpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VblendpdEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vblendpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vblendpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl VblendpdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vblendpd(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vblendpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VblendpdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vblendpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vblendpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VblendpdEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vblendpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vblendpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl VblendpsEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vblendps(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vblendps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VblendpsEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vblendps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vblendps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VblendpsEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vblendps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vblendps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl VblendpsEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vblendps(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vblendps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VblendpsEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vblendps(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vblendps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VblendpsEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vblendps(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vblendps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vblendvpd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vblendvpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vblendvpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vblendvpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpdEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vblendvpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Ymm) {
        self.emit_n(
            InstId::Vblendvpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vblendvpd(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vblendvpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vblendvpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vblendvpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpdEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vblendvpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: Ymm) {
        self.emit_n(
            InstId::Vblendvpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpsEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vblendvps(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vblendvps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpsEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vblendvps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vblendvps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpsEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vblendvps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Ymm) {
        self.emit_n(
            InstId::Vblendvps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpsEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vblendvps(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vblendvps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpsEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vblendvps(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vblendvps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpsEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vblendvps(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: Ymm) {
        self.emit_n(
            InstId::Vblendvps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX
    }
}

impl Vbroadcastf128Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcastf128(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastf128 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl Vbroadcastf128Emitter<Ymm, Mem> for Assembler<'_> {
    fn vbroadcastf128(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastf128 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl Vbroadcastf32x2Emitter<Vec, Vec> for Assembler<'_> {
    fn vbroadcastf32x2(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vbroadcastf32x2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf32x2Emitter<Ymm, Xmm> for Assembler<'_> {
    fn vbroadcastf32x2(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vbroadcastf32x2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf32x2Emitter<Zmm, Xmm> for Assembler<'_> {
    fn vbroadcastf32x2(&mut self, dst: Zmm, src: Xmm) {
        self.emit_n(
            InstId::Vbroadcastf32x2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf32x2Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcastf32x2(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastf32x2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf32x2Emitter<Ymm, Mem> for Assembler<'_> {
    fn vbroadcastf32x2(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastf32x2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf32x2Emitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcastf32x2(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastf32x2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf32x4Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcastf32x4(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastf32x4 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vbroadcastf32x4Emitter<Ymm, Mem> for Assembler<'_> {
    fn vbroadcastf32x4(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastf32x4 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vbroadcastf32x4Emitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcastf32x4(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastf32x4 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vbroadcastf32x8Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcastf32x8(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastf32x8 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf32x8Emitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcastf32x8(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastf32x8 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf64x2Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcastf64x2(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastf64x2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf64x2Emitter<Ymm, Mem> for Assembler<'_> {
    fn vbroadcastf64x2(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastf64x2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf64x2Emitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcastf64x2(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastf64x2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf64x4Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcastf64x4(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastf64x4 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vbroadcastf64x4Emitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcastf64x4(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastf64x4 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vbroadcasti128Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcasti128(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vbroadcasti128 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2
    }
}

impl Vbroadcasti128Emitter<Ymm, Mem> for Assembler<'_> {
    fn vbroadcasti128(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcasti128 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2
    }
}

impl Vbroadcasti32x2Emitter<Vec, Vec> for Assembler<'_> {
    fn vbroadcasti32x2(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vbroadcasti32x2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti32x2Emitter<Xmm, Xmm> for Assembler<'_> {
    fn vbroadcasti32x2(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vbroadcasti32x2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti32x2Emitter<Ymm, Xmm> for Assembler<'_> {
    fn vbroadcasti32x2(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vbroadcasti32x2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti32x2Emitter<Zmm, Xmm> for Assembler<'_> {
    fn vbroadcasti32x2(&mut self, dst: Zmm, src: Xmm) {
        self.emit_n(
            InstId::Vbroadcasti32x2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti32x2Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcasti32x2(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vbroadcasti32x2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti32x2Emitter<Xmm, Mem> for Assembler<'_> {
    fn vbroadcasti32x2(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcasti32x2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti32x2Emitter<Ymm, Mem> for Assembler<'_> {
    fn vbroadcasti32x2(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcasti32x2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti32x2Emitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcasti32x2(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcasti32x2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti32x4Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcasti32x4(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vbroadcasti32x4 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vbroadcasti32x4Emitter<Ymm, Mem> for Assembler<'_> {
    fn vbroadcasti32x4(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcasti32x4 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vbroadcasti32x4Emitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcasti32x4(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcasti32x4 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vbroadcasti32x8Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcasti32x8(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vbroadcasti32x8 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti32x8Emitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcasti32x8(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcasti32x8 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti64x2Emitter<Vec, Vec> for Assembler<'_> {
    fn vbroadcasti64x2(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vbroadcasti64x2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti64x2Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcasti64x2(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vbroadcasti64x2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti64x2Emitter<Ymm, Mem> for Assembler<'_> {
    fn vbroadcasti64x2(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcasti64x2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti64x2Emitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcasti64x2(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcasti64x2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti64x4Emitter<Vec, Vec> for Assembler<'_> {
    fn vbroadcasti64x4(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vbroadcasti64x4 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vbroadcasti64x4Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcasti64x4(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vbroadcasti64x4 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vbroadcasti64x4Emitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcasti64x4(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcasti64x4 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VbroadcastsdEmitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcastsd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastsd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VbroadcastsdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vbroadcastsd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastsd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VbroadcastsdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcastsd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastsd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VbroadcastsdEmitter<Vec, Vec> for Assembler<'_> {
    fn vbroadcastsd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vbroadcastsd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VbroadcastsdEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vbroadcastsd(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vbroadcastsd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VbroadcastsdEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vbroadcastsd(&mut self, dst: Zmm, src: Xmm) {
        self.emit_n(
            InstId::Vbroadcastsd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VbroadcastssEmitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcastss(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VbroadcastssEmitter<Xmm, Mem> for Assembler<'_> {
    fn vbroadcastss(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VbroadcastssEmitter<Ymm, Mem> for Assembler<'_> {
    fn vbroadcastss(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VbroadcastssEmitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcastss(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vbroadcastss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VbroadcastssEmitter<Vec, Vec> for Assembler<'_> {
    fn vbroadcastss(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vbroadcastss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VbroadcastssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vbroadcastss(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vbroadcastss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VbroadcastssEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vbroadcastss(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vbroadcastss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VbroadcastssEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vbroadcastss(&mut self, dst: Zmm, src: Xmm) {
        self.emit_n(
            InstId::Vbroadcastss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VcmppdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vcmppd(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmppdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vcmppd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmppdEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vcmppd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl VcmppdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vcmppd(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmppdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vcmppd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmppdEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vcmppd(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl VcmppdEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vcmppd(&mut self, dst: KReg, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VcmppdEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vcmppd(&mut self, dst: KReg, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VcmppdEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vcmppd(&mut self, dst: KReg, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VcmppdEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vcmppd(&mut self, dst: KReg, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VcmppdEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vcmppd(&mut self, dst: KReg, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VcmppdEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vcmppd(&mut self, dst: KReg, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VcmppdEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vcmppd(&mut self, dst: KReg, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VcmppdEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vcmppd(&mut self, dst: KReg, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VcmppsEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vcmpps(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmppsEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vcmpps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmppsEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vcmpps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl VcmppsEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vcmpps(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmppsEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vcmpps(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmppsEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vcmpps(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl VcmppsEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vcmpps(&mut self, dst: KReg, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VcmppsEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vcmpps(&mut self, dst: KReg, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VcmppsEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vcmpps(&mut self, dst: KReg, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VcmppsEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vcmpps(&mut self, dst: KReg, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VcmppsEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vcmpps(&mut self, dst: KReg, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VcmppsEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vcmpps(&mut self, dst: KReg, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VcmppsEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vcmpps(&mut self, dst: KReg, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VcmppsEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vcmpps(&mut self, dst: KReg, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VcmpsdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vcmpsd(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vcmpsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmpsdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vcmpsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vcmpsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl VcmpsdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vcmpsd(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vcmpsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmpsdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vcmpsd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vcmpsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl VcmpsdEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vcmpsd(&mut self, dst: KReg, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vcmpsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VcmpsdEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vcmpsd(&mut self, dst: KReg, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vcmpsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VcmpsdEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vcmpsd(&mut self, dst: KReg, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vcmpsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VcmpsdEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vcmpsd(&mut self, dst: KReg, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vcmpsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VcmpssEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vcmpss(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vcmpss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmpssEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vcmpss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vcmpss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl VcmpssEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vcmpss(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vcmpss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmpssEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vcmpss(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vcmpss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl VcmpssEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vcmpss(&mut self, dst: KReg, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vcmpss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VcmpssEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vcmpss(&mut self, dst: KReg, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vcmpss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VcmpssEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vcmpss(&mut self, dst: KReg, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vcmpss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VcmpssEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vcmpss(&mut self, dst: KReg, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vcmpss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VcomisdEmitter<Vec, Vec> for Assembler<'_> {
    fn vcomisd(&mut self, lhs: Vec, rhs: Vec) {
        self.emit_n(
            InstId::Vcomisd as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VcomisdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcomisd(&mut self, lhs: Xmm, rhs: Xmm) {
        self.emit_n(
            InstId::Vcomisd as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VcomisdEmitter<Vec, Mem> for Assembler<'_> {
    fn vcomisd(&mut self, lhs: Vec, rhs: Mem) {
        self.emit_n(
            InstId::Vcomisd as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VcomisdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcomisd(&mut self, lhs: Xmm, rhs: Mem) {
        self.emit_n(
            InstId::Vcomisd as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VcomissEmitter<Vec, Vec> for Assembler<'_> {
    fn vcomiss(&mut self, lhs: Vec, rhs: Vec) {
        self.emit_n(
            InstId::Vcomiss as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VcomissEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcomiss(&mut self, lhs: Xmm, rhs: Xmm) {
        self.emit_n(
            InstId::Vcomiss as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VcomissEmitter<Vec, Mem> for Assembler<'_> {
    fn vcomiss(&mut self, lhs: Vec, rhs: Mem) {
        self.emit_n(
            InstId::Vcomiss as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VcomissEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcomiss(&mut self, lhs: Xmm, rhs: Mem) {
        self.emit_n(
            InstId::Vcomiss as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VcompresspdEmitter<Vec, Vec> for Assembler<'_> {
    fn vcompresspd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcompresspd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcompresspd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcompresspd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcompresspd(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vcompresspd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcompresspd(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vcompresspd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspdEmitter<Mem, Vec> for Assembler<'_> {
    fn vcompresspd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vcompresspd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vcompresspd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vcompresspd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vcompresspd(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vcompresspd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspdEmitter<Mem, Zmm> for Assembler<'_> {
    fn vcompresspd(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vcompresspd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspsEmitter<Vec, Vec> for Assembler<'_> {
    fn vcompressps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcompressps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcompressps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcompressps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspsEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcompressps(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vcompressps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspsEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcompressps(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vcompressps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspsEmitter<Mem, Vec> for Assembler<'_> {
    fn vcompressps(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vcompressps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspsEmitter<Mem, Xmm> for Assembler<'_> {
    fn vcompressps(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vcompressps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspsEmitter<Mem, Ymm> for Assembler<'_> {
    fn vcompressps(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vcompressps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspsEmitter<Mem, Zmm> for Assembler<'_> {
    fn vcompressps(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vcompressps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vcvtdq2pdEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtdq2pd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtdq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2pdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtdq2pd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtdq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2pdEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtdq2pd(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtdq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2pdEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvtdq2pd(&mut self, dst: Zmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtdq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2pdEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtdq2pd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtdq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtdq2pd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtdq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2pdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtdq2pd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtdq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2pdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtdq2pd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtdq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2psEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtdq2ps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtdq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2psEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtdq2ps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtdq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2psEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtdq2ps(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtdq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2psEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtdq2ps(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtdq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2psEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtdq2ps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtdq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtdq2ps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtdq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtdq2ps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtdq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2psEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtdq2ps(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtdq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtne2ps2bf16Emitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vcvtne2ps2bf16(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vcvtne2ps2bf16 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtne2ps2bf16Emitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vcvtne2ps2bf16(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vcvtne2ps2bf16 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtne2ps2bf16Emitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vcvtne2ps2bf16(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vcvtne2ps2bf16 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtne2ps2bf16Emitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vcvtne2ps2bf16(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vcvtne2ps2bf16 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtne2ps2bf16Emitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtne2ps2bf16(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vcvtne2ps2bf16 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtne2ps2bf16Emitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtne2ps2bf16(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vcvtne2ps2bf16 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtne2ps2bf16Emitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vcvtne2ps2bf16(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vcvtne2ps2bf16 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtne2ps2bf16Emitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vcvtne2ps2bf16(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vcvtne2ps2bf16 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtneps2bf16Emitter<Vec, Vec> for Assembler<'_> {
    fn vcvtneps2bf16(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtneps2bf16 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtneps2bf16Emitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtneps2bf16(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtneps2bf16 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtneps2bf16Emitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtneps2bf16(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtneps2bf16 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtneps2bf16Emitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvtneps2bf16(&mut self, dst: Ymm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtneps2bf16 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtneps2bf16Emitter<Vec, Mem> for Assembler<'_> {
    fn vcvtneps2bf16(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtneps2bf16 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtneps2bf16Emitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtneps2bf16(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtneps2bf16 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtneps2bf16Emitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtneps2bf16(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtneps2bf16 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtpd2dqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtpd2dq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2dqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtpd2dq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2dqEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtpd2dq(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2dqEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvtpd2dq(&mut self, dst: Ymm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2dqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtpd2dq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2dqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtpd2dq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2dqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtpd2dq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2psEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtpd2ps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtpd2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2psEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtpd2ps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtpd2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2psEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtpd2ps(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtpd2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2psEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvtpd2ps(&mut self, dst: Ymm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtpd2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2psEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtpd2ps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtpd2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtpd2ps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtpd2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtpd2ps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtpd2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2qqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtpd2qq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtpd2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2qqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtpd2qq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtpd2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2qqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtpd2qq(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtpd2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2qqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtpd2qq(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtpd2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2qqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtpd2qq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtpd2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2qqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtpd2qq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtpd2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2qqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtpd2qq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtpd2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2qqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtpd2qq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtpd2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2udqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtpd2udq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtpd2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvtpd2udqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtpd2udq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtpd2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvtpd2udqEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtpd2udq(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtpd2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvtpd2udqEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvtpd2udq(&mut self, dst: Ymm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtpd2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvtpd2udqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtpd2udq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtpd2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvtpd2udqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtpd2udq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtpd2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvtpd2udqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtpd2udq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtpd2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvtpd2uqqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtpd2uqq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtpd2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2uqqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtpd2uqq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtpd2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2uqqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtpd2uqq(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtpd2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2uqqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtpd2uqq(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtpd2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2uqqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtpd2uqq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtpd2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2uqqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtpd2uqq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtpd2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2uqqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtpd2uqq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtpd2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2uqqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtpd2uqq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtpd2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtph2psEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtph2ps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtph2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // F16C AVX512_F{kz}
    }
}

impl Vcvtph2psEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtph2ps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtph2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // F16C AVX512_F{kz}
    }
}

impl Vcvtph2psEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtph2ps(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtph2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // F16C AVX512_F{kz}
    }
}

impl Vcvtph2psEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvtph2ps(&mut self, dst: Zmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtph2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // F16C AVX512_F{kz}
    }
}

impl Vcvtph2psEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtph2ps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // F16C AVX512_F{kz}
    }
}

impl Vcvtph2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtph2ps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // F16C AVX512_F{kz}
    }
}

impl Vcvtph2psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtph2ps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // F16C AVX512_F{kz}
    }
}

impl Vcvtph2psEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtph2ps(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // F16C AVX512_F{kz}
    }
}

impl Vcvtps2dqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtps2dq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2dqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtps2dq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2dqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtps2dq(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2dqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtps2dq(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2dqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtps2dq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2dqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtps2dq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2dqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtps2dq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2dqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtps2dq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2pdEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtps2pd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtps2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2pdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtps2pd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtps2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2pdEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtps2pd(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtps2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2pdEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvtps2pd(&mut self, dst: Zmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtps2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2pdEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtps2pd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtps2pd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2pdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtps2pd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2pdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtps2pd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2phEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vcvtps2ph(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vcvtps2ph as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // F16C AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vcvtps2phEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vcvtps2ph(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vcvtps2ph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // F16C AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vcvtps2phEmitter<Xmm, Ymm, U2> for Assembler<'_> {
    fn vcvtps2ph(&mut self, dst: Xmm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vcvtps2ph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // F16C AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vcvtps2phEmitter<Ymm, Zmm, U2> for Assembler<'_> {
    fn vcvtps2ph(&mut self, dst: Ymm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vcvtps2ph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // F16C AVX512_F{kz}
    }
}

impl Vcvtps2phEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vcvtps2ph(&mut self, dst: Mem, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vcvtps2ph as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // F16C AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vcvtps2phEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn vcvtps2ph(&mut self, dst: Mem, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vcvtps2ph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // F16C AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vcvtps2phEmitter<Mem, Ymm, U2> for Assembler<'_> {
    fn vcvtps2ph(&mut self, dst: Mem, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vcvtps2ph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // F16C AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vcvtps2phEmitter<Mem, Zmm, U2> for Assembler<'_> {
    fn vcvtps2ph(&mut self, dst: Mem, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vcvtps2ph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // F16C AVX512_F{kz}
    }
}

impl Vcvtps2qqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtps2qq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtps2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2qqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtps2qq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtps2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2qqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtps2qq(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtps2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2qqEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvtps2qq(&mut self, dst: Zmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtps2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2qqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtps2qq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2qqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtps2qq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2qqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtps2qq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2qqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtps2qq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2udqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtps2udq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtps2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtps2udqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtps2udq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtps2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtps2udqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtps2udq(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtps2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtps2udqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtps2udq(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtps2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtps2udqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtps2udq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtps2udqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtps2udq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtps2udqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtps2udq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtps2udqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtps2udq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtps2uqqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtps2uqq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtps2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2uqqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtps2uqq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtps2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2uqqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtps2uqq(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtps2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2uqqEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvtps2uqq(&mut self, dst: Zmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtps2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2uqqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtps2uqq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2uqqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtps2uqq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2uqqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtps2uqq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2uqqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtps2uqq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtqq2pdEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtqq2pd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtqq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2pdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtqq2pd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtqq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2pdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtqq2pd(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtqq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2pdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtqq2pd(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtqq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2pdEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtqq2pd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtqq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtqq2pd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtqq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2pdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtqq2pd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtqq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2pdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtqq2pd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtqq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2psEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtqq2ps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtqq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2psEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtqq2ps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtqq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2psEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtqq2ps(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtqq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2psEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvtqq2ps(&mut self, dst: Ymm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtqq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2psEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtqq2ps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtqq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtqq2ps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtqq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtqq2ps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtqq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtsd2siEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvtsd2si(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Vcvtsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsd2siEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvtsd2si(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Vcvtsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsd2siEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvtsd2si(&mut self, dst: Gpq, src: Xmm) {
        self.emit_n(
            InstId::Vcvtsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsd2siEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvtsd2si(&mut self, dst: Gp, src: Mem) {
        self.emit_n(
            InstId::Vcvtsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsd2siEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvtsd2si(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(
            InstId::Vcvtsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsd2siEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvtsd2si(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(
            InstId::Vcvtsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsd2ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vcvtsd2ss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vcvtsd2ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl Vcvtsd2ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vcvtsd2ss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vcvtsd2ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl Vcvtsd2ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtsd2ss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vcvtsd2ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl Vcvtsd2ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtsd2ss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vcvtsd2ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl Vcvtsd2usiEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvtsd2usi(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Vcvtsd2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtsd2usiEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvtsd2usi(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Vcvtsd2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtsd2usiEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvtsd2usi(&mut self, dst: Gpq, src: Xmm) {
        self.emit_n(
            InstId::Vcvtsd2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtsd2usiEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvtsd2usi(&mut self, dst: Gp, src: Mem) {
        self.emit_n(
            InstId::Vcvtsd2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtsd2usiEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvtsd2usi(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(
            InstId::Vcvtsd2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtsd2usiEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvtsd2usi(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(
            InstId::Vcvtsd2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtsi2sdEmitter<Vec, Vec, Gp> for Assembler<'_> {
    fn vcvtsi2sd(&mut self, dst: Vec, src1: Vec, src2: Gp) {
        self.emit_n(
            InstId::Vcvtsi2sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsi2sdEmitter<Xmm, Xmm, Gpd> for Assembler<'_> {
    fn vcvtsi2sd(&mut self, dst: Xmm, src1: Xmm, src2: Gpd) {
        self.emit_n(
            InstId::Vcvtsi2sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsi2sdEmitter<Xmm, Xmm, Gpq> for Assembler<'_> {
    fn vcvtsi2sd(&mut self, dst: Xmm, src1: Xmm, src2: Gpq) {
        self.emit_n(
            InstId::Vcvtsi2sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsi2sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtsi2sd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vcvtsi2sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsi2sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtsi2sd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vcvtsi2sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsi2ssEmitter<Vec, Vec, Gp> for Assembler<'_> {
    fn vcvtsi2ss(&mut self, dst: Vec, src1: Vec, src2: Gp) {
        self.emit_n(
            InstId::Vcvtsi2ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsi2ssEmitter<Xmm, Xmm, Gpd> for Assembler<'_> {
    fn vcvtsi2ss(&mut self, dst: Xmm, src1: Xmm, src2: Gpd) {
        self.emit_n(
            InstId::Vcvtsi2ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsi2ssEmitter<Xmm, Xmm, Gpq> for Assembler<'_> {
    fn vcvtsi2ss(&mut self, dst: Xmm, src1: Xmm, src2: Gpq) {
        self.emit_n(
            InstId::Vcvtsi2ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsi2ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtsi2ss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vcvtsi2ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsi2ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtsi2ss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vcvtsi2ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtss2sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vcvtss2sd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vcvtss2sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl Vcvtss2sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vcvtss2sd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vcvtss2sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl Vcvtss2sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtss2sd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vcvtss2sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl Vcvtss2sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtss2sd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vcvtss2sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl Vcvtss2siEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvtss2si(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Vcvtss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtss2siEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvtss2si(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Vcvtss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtss2siEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvtss2si(&mut self, dst: Gpq, src: Xmm) {
        self.emit_n(
            InstId::Vcvtss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtss2siEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvtss2si(&mut self, dst: Gp, src: Mem) {
        self.emit_n(
            InstId::Vcvtss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtss2siEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvtss2si(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(
            InstId::Vcvtss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtss2siEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvtss2si(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(
            InstId::Vcvtss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtss2usiEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvtss2usi(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Vcvtss2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtss2usiEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvtss2usi(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Vcvtss2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtss2usiEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvtss2usi(&mut self, dst: Gpq, src: Xmm) {
        self.emit_n(
            InstId::Vcvtss2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtss2usiEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvtss2usi(&mut self, dst: Gp, src: Mem) {
        self.emit_n(
            InstId::Vcvtss2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtss2usiEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvtss2usi(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(
            InstId::Vcvtss2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtss2usiEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvtss2usi(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(
            InstId::Vcvtss2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvttpd2dqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttpd2dq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvttpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvttpd2dqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttpd2dq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvttpd2dqEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvttpd2dq(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvttpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvttpd2dqEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvttpd2dq(&mut self, dst: Ymm, src: Zmm) {
        self.emit_n(
            InstId::Vcvttpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvttpd2dqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttpd2dq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvttpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvttpd2dqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttpd2dq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvttpd2dqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttpd2dq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvttpd2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvttpd2qqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttpd2qq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvttpd2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2qqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttpd2qq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttpd2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2qqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvttpd2qq(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vcvttpd2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2qqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvttpd2qq(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vcvttpd2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2qqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttpd2qq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvttpd2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2qqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttpd2qq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttpd2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2qqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttpd2qq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvttpd2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2qqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttpd2qq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttpd2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2udqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttpd2udq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvttpd2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2udqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttpd2udq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttpd2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2udqEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvttpd2udq(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvttpd2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2udqEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvttpd2udq(&mut self, dst: Ymm, src: Zmm) {
        self.emit_n(
            InstId::Vcvttpd2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2udqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttpd2udq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvttpd2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2udqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttpd2udq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttpd2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2udqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttpd2udq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvttpd2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2uqqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttpd2uqq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvttpd2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvttpd2uqqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttpd2uqq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttpd2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvttpd2uqqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvttpd2uqq(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vcvttpd2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvttpd2uqqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvttpd2uqq(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vcvttpd2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvttpd2uqqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttpd2uqq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvttpd2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvttpd2uqqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttpd2uqq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttpd2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvttpd2uqqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttpd2uqq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvttpd2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvttpd2uqqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttpd2uqq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttpd2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvttps2dqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttps2dq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvttps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvttps2dqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttps2dq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvttps2dqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvttps2dq(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vcvttps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvttps2dqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvttps2dq(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vcvttps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvttps2dqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttps2dq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvttps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvttps2dqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttps2dq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvttps2dqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttps2dq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvttps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvttps2dqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttps2dq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttps2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvttps2qqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttps2qq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvttps2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2qqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttps2qq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttps2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2qqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvttps2qq(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttps2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2qqEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvttps2qq(&mut self, dst: Zmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvttps2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2qqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttps2qq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvttps2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2qqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttps2qq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttps2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2qqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttps2qq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvttps2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2qqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttps2qq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttps2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2udqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttps2udq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvttps2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvttps2udqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttps2udq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttps2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvttps2udqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvttps2udq(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vcvttps2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvttps2udqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvttps2udq(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vcvttps2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvttps2udqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttps2udq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvttps2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvttps2udqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttps2udq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttps2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvttps2udqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttps2udq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvttps2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvttps2udqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttps2udq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttps2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvttps2uqqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttps2uqq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvttps2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2uqqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttps2uqq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttps2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2uqqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvttps2uqq(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttps2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2uqqEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvttps2uqq(&mut self, dst: Zmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvttps2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2uqqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttps2uqq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvttps2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2uqqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttps2uqq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttps2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2uqqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttps2uqq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvttps2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2uqqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttps2uqq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttps2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttsd2siEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvttsd2si(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Vcvttsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttsd2siEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvttsd2si(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Vcvttsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttsd2siEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvttsd2si(&mut self, dst: Gpq, src: Xmm) {
        self.emit_n(
            InstId::Vcvttsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttsd2siEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvttsd2si(&mut self, dst: Gp, src: Mem) {
        self.emit_n(
            InstId::Vcvttsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttsd2siEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvttsd2si(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(
            InstId::Vcvttsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttsd2siEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvttsd2si(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(
            InstId::Vcvttsd2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttsd2usiEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvttsd2usi(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Vcvttsd2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttsd2usiEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvttsd2usi(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Vcvttsd2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttsd2usiEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvttsd2usi(&mut self, dst: Gpq, src: Xmm) {
        self.emit_n(
            InstId::Vcvttsd2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttsd2usiEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvttsd2usi(&mut self, dst: Gp, src: Mem) {
        self.emit_n(
            InstId::Vcvttsd2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttsd2usiEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvttsd2usi(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(
            InstId::Vcvttsd2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttsd2usiEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvttsd2usi(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(
            InstId::Vcvttsd2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttss2siEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvttss2si(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Vcvttss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttss2siEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvttss2si(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Vcvttss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttss2siEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvttss2si(&mut self, dst: Gpq, src: Xmm) {
        self.emit_n(
            InstId::Vcvttss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttss2siEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvttss2si(&mut self, dst: Gp, src: Mem) {
        self.emit_n(
            InstId::Vcvttss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttss2siEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvttss2si(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(
            InstId::Vcvttss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttss2siEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvttss2si(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(
            InstId::Vcvttss2si as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttss2usiEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvttss2usi(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Vcvttss2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttss2usiEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvttss2usi(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Vcvttss2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttss2usiEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvttss2usi(&mut self, dst: Gpq, src: Xmm) {
        self.emit_n(
            InstId::Vcvttss2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttss2usiEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvttss2usi(&mut self, dst: Gp, src: Mem) {
        self.emit_n(
            InstId::Vcvttss2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttss2usiEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvttss2usi(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(
            InstId::Vcvttss2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttss2usiEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvttss2usi(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(
            InstId::Vcvttss2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvtudq2pdEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtudq2pd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtudq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2pdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtudq2pd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtudq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2pdEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtudq2pd(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtudq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2pdEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvtudq2pd(&mut self, dst: Zmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtudq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2pdEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtudq2pd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtudq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtudq2pd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtudq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2pdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtudq2pd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtudq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2pdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtudq2pd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtudq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2psEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtudq2ps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtudq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2psEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtudq2ps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtudq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2psEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtudq2ps(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtudq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2psEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtudq2ps(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtudq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2psEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtudq2ps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtudq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtudq2ps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtudq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtudq2ps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtudq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2psEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtudq2ps(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtudq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtuqq2pdEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtuqq2pd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtuqq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2pdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtuqq2pd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtuqq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2pdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtuqq2pd(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtuqq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2pdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtuqq2pd(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtuqq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2pdEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtuqq2pd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtuqq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtuqq2pd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtuqq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2pdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtuqq2pd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtuqq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2pdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtuqq2pd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtuqq2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2psEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtuqq2ps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtuqq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2psEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtuqq2ps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtuqq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2psEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtuqq2ps(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtuqq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2psEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvtuqq2ps(&mut self, dst: Ymm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtuqq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2psEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtuqq2ps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtuqq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtuqq2ps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtuqq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtuqq2ps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtuqq2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtusi2sdEmitter<Vec, Vec, Gp> for Assembler<'_> {
    fn vcvtusi2sd(&mut self, dst: Vec, src1: Vec, src2: Gp) {
        self.emit_n(
            InstId::Vcvtusi2sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtusi2sdEmitter<Xmm, Xmm, Gpd> for Assembler<'_> {
    fn vcvtusi2sd(&mut self, dst: Xmm, src1: Xmm, src2: Gpd) {
        self.emit_n(
            InstId::Vcvtusi2sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtusi2sdEmitter<Xmm, Xmm, Gpq> for Assembler<'_> {
    fn vcvtusi2sd(&mut self, dst: Xmm, src1: Xmm, src2: Gpq) {
        self.emit_n(
            InstId::Vcvtusi2sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtusi2sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtusi2sd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vcvtusi2sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtusi2sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtusi2sd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vcvtusi2sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtusi2ssEmitter<Vec, Vec, Gp> for Assembler<'_> {
    fn vcvtusi2ss(&mut self, dst: Vec, src1: Vec, src2: Gp) {
        self.emit_n(
            InstId::Vcvtusi2ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtusi2ssEmitter<Xmm, Xmm, Gpd> for Assembler<'_> {
    fn vcvtusi2ss(&mut self, dst: Xmm, src1: Xmm, src2: Gpd) {
        self.emit_n(
            InstId::Vcvtusi2ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtusi2ssEmitter<Xmm, Xmm, Gpq> for Assembler<'_> {
    fn vcvtusi2ss(&mut self, dst: Xmm, src1: Xmm, src2: Gpq) {
        self.emit_n(
            InstId::Vcvtusi2ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtusi2ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtusi2ss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vcvtusi2ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtusi2ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtusi2ss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vcvtusi2ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{er}
    }
}

impl VdbpsadbwEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vdbpsadbw(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vdbpsadbw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VdbpsadbwEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vdbpsadbw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vdbpsadbw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VdbpsadbwEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vdbpsadbw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vdbpsadbw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VdbpsadbwEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vdbpsadbw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vdbpsadbw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl VdbpsadbwEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vdbpsadbw(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vdbpsadbw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VdbpsadbwEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vdbpsadbw(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vdbpsadbw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VdbpsadbwEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vdbpsadbw(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vdbpsadbw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VdbpsadbwEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vdbpsadbw(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vdbpsadbw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl VdivpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vdivpd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vdivpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VdivpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vdivpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vdivpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VdivpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vdivpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vdivpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VdivpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vdivpd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vdivpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VdivpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vdivpd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vdivpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VdivpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vdivpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vdivpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VdivpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vdivpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vdivpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VdivpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vdivpd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vdivpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VdivpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vdivps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vdivps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VdivpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vdivps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vdivps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VdivpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vdivps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vdivps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VdivpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vdivps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vdivps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VdivpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vdivps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vdivps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VdivpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vdivps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vdivps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VdivpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vdivps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vdivps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VdivpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vdivps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vdivps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VdivsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vdivsd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vdivsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VdivsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vdivsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vdivsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VdivsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vdivsd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vdivsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VdivsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vdivsd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vdivsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VdivssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vdivss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vdivss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VdivssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vdivss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vdivss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VdivssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vdivss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vdivss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VdivssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vdivss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vdivss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl Vdpbf16psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vdpbf16ps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vdpbf16ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vdpbf16psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vdpbf16ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vdpbf16ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vdpbf16psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vdpbf16ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vdpbf16ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vdpbf16psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vdpbf16ps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vdpbf16ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vdpbf16psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vdpbf16ps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vdpbf16ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vdpbf16psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vdpbf16ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vdpbf16ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vdpbf16psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vdpbf16ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vdpbf16ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vdpbf16psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vdpbf16ps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vdpbf16ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl VdppdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vdppd(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vdppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VdppdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vdppd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vdppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl VdppdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vdppd(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vdppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VdppdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vdppd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vdppd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl VdppsEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vdpps(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vdpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VdppsEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vdpps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vdpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VdppsEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vdpps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vdpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl VdppsEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vdpps(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vdpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VdppsEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vdpps(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vdpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VdppsEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vdpps(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vdpps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl VexpandpdEmitter<Vec, Vec> for Assembler<'_> {
    fn vexpandpd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vexpandpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vexpandpd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vexpandpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vexpandpd(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vexpandpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vexpandpd(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vexpandpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpdEmitter<Vec, Mem> for Assembler<'_> {
    fn vexpandpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vexpandpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vexpandpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vexpandpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vexpandpd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vexpandpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vexpandpd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vexpandpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpsEmitter<Vec, Vec> for Assembler<'_> {
    fn vexpandps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vexpandps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vexpandps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vexpandps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpsEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vexpandps(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vexpandps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpsEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vexpandps(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vexpandps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpsEmitter<Vec, Mem> for Assembler<'_> {
    fn vexpandps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vexpandps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vexpandps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vexpandps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vexpandps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vexpandps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpsEmitter<Zmm, Mem> for Assembler<'_> {
    fn vexpandps(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vexpandps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vextractf128Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vextractf128(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextractf128 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX
    }
}

impl<U2: Into<Imm>> Vextractf128Emitter<Xmm, Ymm, U2> for Assembler<'_> {
    fn vextractf128(&mut self, dst: Xmm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vextractf128 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX
    }
}

impl Vextractf128Emitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextractf128(&mut self, dst: Mem, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextractf128 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX
    }
}

impl<U2: Into<Imm>> Vextractf128Emitter<Mem, Ymm, U2> for Assembler<'_> {
    fn vextractf128(&mut self, dst: Mem, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vextractf128 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX
    }
}

impl Vextractf32x4Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vextractf32x4(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextractf32x4 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextractf32x4Emitter<Xmm, Ymm, U2> for Assembler<'_> {
    fn vextractf32x4(&mut self, dst: Xmm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vextractf32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextractf32x4Emitter<Xmm, Zmm, U2> for Assembler<'_> {
    fn vextractf32x4(&mut self, dst: Xmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vextractf32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vextractf32x4Emitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextractf32x4(&mut self, dst: Mem, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextractf32x4 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextractf32x4Emitter<Mem, Ymm, U2> for Assembler<'_> {
    fn vextractf32x4(&mut self, dst: Mem, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vextractf32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextractf32x4Emitter<Mem, Zmm, U2> for Assembler<'_> {
    fn vextractf32x4(&mut self, dst: Mem, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vextractf32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vextractf32x8Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vextractf32x8(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextractf32x8 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextractf32x8Emitter<Ymm, Zmm, U2> for Assembler<'_> {
    fn vextractf32x8(&mut self, dst: Ymm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vextractf32x8 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vextractf32x8Emitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextractf32x8(&mut self, dst: Mem, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextractf32x8 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextractf32x8Emitter<Mem, Zmm, U2> for Assembler<'_> {
    fn vextractf32x8(&mut self, dst: Mem, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vextractf32x8 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vextractf64x2Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vextractf64x2(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextractf64x2 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextractf64x2Emitter<Xmm, Ymm, U2> for Assembler<'_> {
    fn vextractf64x2(&mut self, dst: Xmm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vextractf64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextractf64x2Emitter<Xmm, Zmm, U2> for Assembler<'_> {
    fn vextractf64x2(&mut self, dst: Xmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vextractf64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vextractf64x2Emitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextractf64x2(&mut self, dst: Mem, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextractf64x2 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextractf64x2Emitter<Mem, Ymm, U2> for Assembler<'_> {
    fn vextractf64x2(&mut self, dst: Mem, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vextractf64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextractf64x2Emitter<Mem, Zmm, U2> for Assembler<'_> {
    fn vextractf64x2(&mut self, dst: Mem, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vextractf64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vextractf64x4Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vextractf64x4(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextractf64x4 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextractf64x4Emitter<Ymm, Zmm, U2> for Assembler<'_> {
    fn vextractf64x4(&mut self, dst: Ymm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vextractf64x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vextractf64x4Emitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextractf64x4(&mut self, dst: Mem, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextractf64x4 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextractf64x4Emitter<Mem, Zmm, U2> for Assembler<'_> {
    fn vextractf64x4(&mut self, dst: Mem, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vextractf64x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vextracti128Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vextracti128(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextracti128 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX2
    }
}

impl<U2: Into<Imm>> Vextracti128Emitter<Xmm, Ymm, U2> for Assembler<'_> {
    fn vextracti128(&mut self, dst: Xmm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vextracti128 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX2
    }
}

impl Vextracti128Emitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextracti128(&mut self, dst: Mem, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextracti128 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX2
    }
}

impl<U2: Into<Imm>> Vextracti128Emitter<Mem, Ymm, U2> for Assembler<'_> {
    fn vextracti128(&mut self, dst: Mem, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vextracti128 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX2
    }
}

impl Vextracti32x4Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vextracti32x4(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextracti32x4 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextracti32x4Emitter<Xmm, Ymm, U2> for Assembler<'_> {
    fn vextracti32x4(&mut self, dst: Xmm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vextracti32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextracti32x4Emitter<Xmm, Zmm, U2> for Assembler<'_> {
    fn vextracti32x4(&mut self, dst: Xmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vextracti32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vextracti32x4Emitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextracti32x4(&mut self, dst: Mem, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextracti32x4 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextracti32x4Emitter<Mem, Ymm, U2> for Assembler<'_> {
    fn vextracti32x4(&mut self, dst: Mem, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vextracti32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextracti32x4Emitter<Mem, Zmm, U2> for Assembler<'_> {
    fn vextracti32x4(&mut self, dst: Mem, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vextracti32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vextracti32x8Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vextracti32x8(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextracti32x8 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextracti32x8Emitter<Ymm, Zmm, U2> for Assembler<'_> {
    fn vextracti32x8(&mut self, dst: Ymm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vextracti32x8 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vextracti32x8Emitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextracti32x8(&mut self, dst: Mem, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextracti32x8 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextracti32x8Emitter<Mem, Zmm, U2> for Assembler<'_> {
    fn vextracti32x8(&mut self, dst: Mem, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vextracti32x8 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vextracti64x2Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vextracti64x2(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextracti64x2 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextracti64x2Emitter<Xmm, Ymm, U2> for Assembler<'_> {
    fn vextracti64x2(&mut self, dst: Xmm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vextracti64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextracti64x2Emitter<Xmm, Zmm, U2> for Assembler<'_> {
    fn vextracti64x2(&mut self, dst: Xmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vextracti64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vextracti64x2Emitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextracti64x2(&mut self, dst: Mem, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextracti64x2 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextracti64x2Emitter<Mem, Ymm, U2> for Assembler<'_> {
    fn vextracti64x2(&mut self, dst: Mem, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vextracti64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextracti64x2Emitter<Mem, Zmm, U2> for Assembler<'_> {
    fn vextracti64x2(&mut self, dst: Mem, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vextracti64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vextracti64x4Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vextracti64x4(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextracti64x4 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextracti64x4Emitter<Ymm, Zmm, U2> for Assembler<'_> {
    fn vextracti64x4(&mut self, dst: Ymm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vextracti64x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vextracti64x4Emitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextracti64x4(&mut self, dst: Mem, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextracti64x4 as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextracti64x4Emitter<Mem, Zmm, U2> for Assembler<'_> {
    fn vextracti64x4(&mut self, dst: Mem, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vextracti64x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl VextractpsEmitter<Gp, Vec, Imm> for Assembler<'_> {
    fn vextractps(&mut self, dst: Gp, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextractps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX AVX512_F
    }
}

impl<U2: Into<Imm>> VextractpsEmitter<Gpd, Xmm, U2> for Assembler<'_> {
    fn vextractps(&mut self, dst: Gpd, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vextractps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_F
    }
}

impl VextractpsEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextractps(&mut self, dst: Mem, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vextractps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX AVX512_F
    }
}

impl<U2: Into<Imm>> VextractpsEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn vextractps(&mut self, dst: Mem, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vextractps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_F
    }
}

impl VfixupimmpdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vfixupimmpd(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vfixupimmpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VfixupimmpdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vfixupimmpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vfixupimmpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VfixupimmpdEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vfixupimmpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vfixupimmpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VfixupimmpdEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vfixupimmpd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vfixupimmpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VfixupimmpdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vfixupimmpd(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vfixupimmpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VfixupimmpdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vfixupimmpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vfixupimmpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VfixupimmpdEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vfixupimmpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vfixupimmpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VfixupimmpdEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vfixupimmpd(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vfixupimmpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VfixupimmpsEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vfixupimmps(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vfixupimmps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VfixupimmpsEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vfixupimmps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vfixupimmps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VfixupimmpsEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vfixupimmps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vfixupimmps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VfixupimmpsEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vfixupimmps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vfixupimmps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VfixupimmpsEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vfixupimmps(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vfixupimmps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VfixupimmpsEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vfixupimmps(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vfixupimmps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VfixupimmpsEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vfixupimmps(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vfixupimmps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VfixupimmpsEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vfixupimmps(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vfixupimmps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VfixupimmsdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vfixupimmsd(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vfixupimmsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VfixupimmsdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vfixupimmsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vfixupimmsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VfixupimmsdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vfixupimmsd(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vfixupimmsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VfixupimmsdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vfixupimmsd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vfixupimmsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VfixupimmssEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vfixupimmss(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vfixupimmss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VfixupimmssEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vfixupimmss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vfixupimmss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VfixupimmssEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vfixupimmss(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vfixupimmss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VfixupimmssEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vfixupimmss(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vfixupimmss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl Vfmadd132pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd132pd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd132pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd132pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd132pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmadd132pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd132pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmadd132pd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd132pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd132pd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd132pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd132pd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd132pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmadd132pd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd132pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmadd132pd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd132psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd132ps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd132psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd132ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd132psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmadd132ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd132psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmadd132ps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd132psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd132ps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd132psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd132ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd132psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmadd132ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd132psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmadd132ps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd132sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd132sd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmadd132sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd132sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd132sd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmadd132sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd132sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd132sd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd132sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd132sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd132sd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd132sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd132ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd132ss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmadd132ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd132ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd132ss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmadd132ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd132ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd132ss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd132ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd132ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd132ss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd132ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd213pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd213pd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd213pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd213pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd213pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmadd213pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd213pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmadd213pd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd213pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd213pd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd213pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd213pd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd213pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmadd213pd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd213pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmadd213pd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd213psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd213ps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd213psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd213ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd213psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmadd213ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd213psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmadd213ps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd213psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd213ps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd213psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd213ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd213psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmadd213ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd213psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmadd213ps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd213sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd213sd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmadd213sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd213sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd213sd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmadd213sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd213sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd213sd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd213sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd213sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd213sd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd213sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd213ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd213ss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmadd213ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd213ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd213ss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmadd213ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd213ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd213ss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd213ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd213ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd213ss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd213ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd231pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd231pd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd231pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd231pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd231pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmadd231pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd231pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmadd231pd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd231pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd231pd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd231pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd231pd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd231pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmadd231pd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd231pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmadd231pd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd231psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd231ps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd231psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd231ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd231psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmadd231ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd231psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmadd231ps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd231psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd231ps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd231psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd231ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd231psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmadd231ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd231psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmadd231ps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd231sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd231sd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmadd231sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd231sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd231sd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmadd231sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd231sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd231sd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd231sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd231sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd231sd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd231sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd231ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd231ss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmadd231ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd231ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd231ss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmadd231ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd231ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd231ss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd231ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd231ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd231ss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd231ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmaddsub132pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsub132pd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmaddsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub132pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsub132pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmaddsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub132pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsub132pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmaddsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub132pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmaddsub132pd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmaddsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub132pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsub132pd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub132pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsub132pd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub132pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsub132pd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub132pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmaddsub132pd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub132psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsub132ps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmaddsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub132psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsub132ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmaddsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub132psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsub132ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmaddsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub132psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmaddsub132ps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmaddsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub132psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsub132ps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub132psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsub132ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub132psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsub132ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub132psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmaddsub132ps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub213pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsub213pd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmaddsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub213pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsub213pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmaddsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub213pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsub213pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmaddsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub213pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmaddsub213pd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmaddsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub213pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsub213pd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub213pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsub213pd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub213pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsub213pd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub213pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmaddsub213pd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub213psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsub213ps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmaddsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub213psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsub213ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmaddsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub213psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsub213ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmaddsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub213psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmaddsub213ps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmaddsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub213psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsub213ps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub213psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsub213ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub213psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsub213ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub213psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmaddsub213ps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub231pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsub231pd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmaddsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub231pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsub231pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmaddsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub231pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsub231pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmaddsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub231pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmaddsub231pd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmaddsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub231pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsub231pd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub231pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsub231pd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub231pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsub231pd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub231pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmaddsub231pd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub231psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsub231ps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmaddsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub231psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsub231ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmaddsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub231psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsub231ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmaddsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub231psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmaddsub231ps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmaddsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub231psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsub231ps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub231psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsub231ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub231psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsub231ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub231psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmaddsub231ps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub132pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub132pd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub132pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub132pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub132pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsub132pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub132pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsub132pd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub132pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub132pd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub132pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub132pd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub132pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsub132pd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub132pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsub132pd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub132psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub132ps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub132psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub132ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub132psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsub132ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub132psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsub132ps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub132psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub132ps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub132psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub132ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub132psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsub132ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub132psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsub132ps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub132sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub132sd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsub132sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub132sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub132sd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsub132sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub132sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub132sd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub132sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub132sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub132sd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub132sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub132ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub132ss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsub132ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub132ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub132ss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsub132ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub132ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub132ss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub132ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub132ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub132ss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub132ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub213pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub213pd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub213pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub213pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub213pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsub213pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub213pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsub213pd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub213pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub213pd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub213pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub213pd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub213pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsub213pd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub213pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsub213pd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub213psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub213ps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub213psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub213ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub213psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsub213ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub213psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsub213ps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub213psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub213ps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub213psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub213ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub213psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsub213ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub213psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsub213ps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub213sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub213sd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsub213sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub213sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub213sd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsub213sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub213sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub213sd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub213sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub213sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub213sd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub213sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub213ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub213ss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsub213ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub213ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub213ss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsub213ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub213ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub213ss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub213ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub213ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub213ss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub213ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub231pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub231pd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub231pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub231pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub231pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsub231pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub231pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsub231pd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub231pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub231pd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub231pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub231pd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub231pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsub231pd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub231pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsub231pd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub231psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub231ps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub231psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub231ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub231psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsub231ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub231psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsub231ps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub231psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub231ps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub231psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub231ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub231psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsub231ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub231psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsub231ps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub231sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub231sd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsub231sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub231sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub231sd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsub231sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub231sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub231sd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub231sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub231sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub231sd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub231sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub231ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub231ss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsub231ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub231ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub231ss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsub231ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub231ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub231ss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub231ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub231ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub231ss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub231ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsubadd132pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubadd132pd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsubadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd132pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubadd132pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsubadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd132pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubadd132pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmsubadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd132pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsubadd132pd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmsubadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd132pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubadd132pd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd132pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubadd132pd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd132pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubadd132pd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd132pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsubadd132pd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd132psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubadd132ps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsubadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd132psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubadd132ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsubadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd132psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubadd132ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmsubadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd132psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsubadd132ps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmsubadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd132psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubadd132ps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd132psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubadd132ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd132psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubadd132ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd132psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsubadd132ps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd213pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubadd213pd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsubadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd213pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubadd213pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsubadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd213pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubadd213pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmsubadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd213pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsubadd213pd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmsubadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd213pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubadd213pd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd213pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubadd213pd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd213pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubadd213pd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd213pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsubadd213pd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd213psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubadd213ps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsubadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd213psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubadd213ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsubadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd213psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubadd213ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmsubadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd213psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsubadd213ps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmsubadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd213psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubadd213ps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd213psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubadd213ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd213psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubadd213ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd213psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsubadd213ps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd231pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubadd231pd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsubadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd231pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubadd231pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsubadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd231pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubadd231pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmsubadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd231pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsubadd231pd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmsubadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd231pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubadd231pd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd231pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubadd231pd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd231pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubadd231pd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd231pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsubadd231pd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd231psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubadd231ps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsubadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd231psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubadd231ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsubadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd231psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubadd231ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmsubadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd231psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsubadd231ps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmsubadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd231psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubadd231ps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd231psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubadd231ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd231psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubadd231ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd231psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsubadd231ps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd132pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd132pd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd132pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd132pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd132pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmadd132pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfnmadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd132pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmadd132pd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfnmadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd132pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd132pd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd132pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd132pd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd132pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmadd132pd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd132pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmadd132pd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd132psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd132ps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd132psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd132ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd132psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmadd132ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfnmadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd132psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmadd132ps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfnmadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd132psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd132ps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd132psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd132ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd132psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmadd132ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd132psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmadd132ps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd132sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd132sd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmadd132sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd132sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd132sd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd132sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd132sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd132sd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd132sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd132sd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd132ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd132ss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmadd132ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd132ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd132ss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd132ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd132ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd132ss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd132ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd132ss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd213pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd213pd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd213pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd213pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd213pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmadd213pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfnmadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd213pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmadd213pd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfnmadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd213pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd213pd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd213pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd213pd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd213pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmadd213pd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd213pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmadd213pd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd213psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd213ps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd213psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd213ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd213psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmadd213ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfnmadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd213psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmadd213ps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfnmadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd213psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd213ps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd213psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd213ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd213psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmadd213ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd213psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmadd213ps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd213sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd213sd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmadd213sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd213sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd213sd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd213sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd213sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd213sd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd213sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd213sd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd213ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd213ss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmadd213ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd213ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd213ss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd213ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd213ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd213ss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd213ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd213ss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd231pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd231pd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd231pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd231pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd231pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmadd231pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfnmadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd231pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmadd231pd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfnmadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd231pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd231pd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd231pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd231pd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd231pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmadd231pd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd231pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmadd231pd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd231psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd231ps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd231psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd231ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd231psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmadd231ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfnmadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd231psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmadd231ps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfnmadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd231psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd231ps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd231psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd231ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd231psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmadd231ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd231psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmadd231ps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd231sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd231sd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmadd231sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd231sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd231sd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd231sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd231sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd231sd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd231sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd231sd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd231ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd231ss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmadd231ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd231ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd231ss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd231ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd231ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd231ss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd231ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd231ss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub132pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub132pd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub132pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub132pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub132pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsub132pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfnmsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub132pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmsub132pd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfnmsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub132pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub132pd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub132pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub132pd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub132pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsub132pd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub132pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmsub132pd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub132psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub132ps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub132psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub132ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub132psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsub132ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfnmsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub132psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmsub132ps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfnmsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub132psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub132ps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub132psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub132ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub132psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsub132ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub132psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmsub132ps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub132sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub132sd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmsub132sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub132sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub132sd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub132sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub132sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub132sd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub132sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub132sd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub132ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub132ss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmsub132ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub132ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub132ss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub132ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub132ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub132ss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub132ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub132ss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub213pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub213pd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub213pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub213pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub213pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsub213pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfnmsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub213pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmsub213pd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfnmsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub213pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub213pd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub213pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub213pd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub213pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsub213pd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub213pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmsub213pd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub213psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub213ps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub213psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub213ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub213psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsub213ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfnmsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub213psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmsub213ps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfnmsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub213psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub213ps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub213psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub213ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub213psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsub213ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub213psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmsub213ps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub213sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub213sd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmsub213sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub213sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub213sd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub213sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub213sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub213sd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub213sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub213sd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub213ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub213ss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmsub213ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub213ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub213ss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub213ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub213ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub213ss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub213ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub213ss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub231pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub231pd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub231pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub231pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub231pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsub231pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfnmsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub231pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmsub231pd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfnmsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub231pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub231pd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub231pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub231pd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub231pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsub231pd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub231pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmsub231pd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub231psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub231ps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub231psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub231ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub231psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsub231ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfnmsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub231psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmsub231ps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfnmsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub231psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub231ps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub231psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub231ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub231psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsub231ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub231psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmsub231ps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub231sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub231sd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmsub231sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub231sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub231sd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub231sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub231sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub231sd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub231sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub231sd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub231ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub231ss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmsub231ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub231ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub231ss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub231ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub231ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub231ss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub231ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub231ss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl VfpclasspdEmitter<KReg, Vec, Imm> for Assembler<'_> {
    fn vfpclasspd(&mut self, dst: KReg, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vfpclasspd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_DQ{k|b64}
    }
}

impl<U2: Into<Imm>> VfpclasspdEmitter<KReg, Xmm, U2> for Assembler<'_> {
    fn vfpclasspd(&mut self, dst: KReg, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vfpclasspd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{k|b64}
    }
}

impl<U2: Into<Imm>> VfpclasspdEmitter<KReg, Ymm, U2> for Assembler<'_> {
    fn vfpclasspd(&mut self, dst: KReg, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vfpclasspd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{k|b64}
    }
}

impl<U2: Into<Imm>> VfpclasspdEmitter<KReg, Zmm, U2> for Assembler<'_> {
    fn vfpclasspd(&mut self, dst: KReg, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vfpclasspd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{k|b64}
    }
}

impl<U2: Into<Imm>> VfpclasspdEmitter<KReg, Mem, U2> for Assembler<'_> {
    fn vfpclasspd(&mut self, dst: KReg, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vfpclasspd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{k|b64}
    }
}

impl VfpclasspsEmitter<KReg, Vec, Imm> for Assembler<'_> {
    fn vfpclassps(&mut self, dst: KReg, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vfpclassps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_DQ{k|b32}
    }
}

impl<U2: Into<Imm>> VfpclasspsEmitter<KReg, Xmm, U2> for Assembler<'_> {
    fn vfpclassps(&mut self, dst: KReg, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vfpclassps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{k|b32}
    }
}

impl<U2: Into<Imm>> VfpclasspsEmitter<KReg, Ymm, U2> for Assembler<'_> {
    fn vfpclassps(&mut self, dst: KReg, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vfpclassps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{k|b32}
    }
}

impl<U2: Into<Imm>> VfpclasspsEmitter<KReg, Zmm, U2> for Assembler<'_> {
    fn vfpclassps(&mut self, dst: KReg, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vfpclassps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{k|b32}
    }
}

impl<U2: Into<Imm>> VfpclasspsEmitter<KReg, Mem, U2> for Assembler<'_> {
    fn vfpclassps(&mut self, dst: KReg, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vfpclassps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{k|b32}
    }
}

impl VfpclasssdEmitter<KReg, Vec, Imm> for Assembler<'_> {
    fn vfpclasssd(&mut self, dst: KReg, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vfpclasssd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_DQ{k}
    }
}

impl<U2: Into<Imm>> VfpclasssdEmitter<KReg, Xmm, U2> for Assembler<'_> {
    fn vfpclasssd(&mut self, dst: KReg, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vfpclasssd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{k}
    }
}

impl<U2: Into<Imm>> VfpclasssdEmitter<KReg, Mem, U2> for Assembler<'_> {
    fn vfpclasssd(&mut self, dst: KReg, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vfpclasssd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{k}
    }
}

impl VfpclassssEmitter<KReg, Vec, Imm> for Assembler<'_> {
    fn vfpclassss(&mut self, dst: KReg, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vfpclassss as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_DQ{k}
    }
}

impl<U2: Into<Imm>> VfpclassssEmitter<KReg, Xmm, U2> for Assembler<'_> {
    fn vfpclassss(&mut self, dst: KReg, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vfpclassss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{k}
    }
}

impl<U2: Into<Imm>> VfpclassssEmitter<KReg, Mem, U2> for Assembler<'_> {
    fn vfpclassss(&mut self, dst: KReg, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vfpclassss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{k}
    }
}

impl VgatherdpdEmitter<Vec, Mem> for Assembler<'_> {
    fn vgatherdpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vgatherdpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherdpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vgatherdpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vgatherdpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherdpdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vgatherdpd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vgatherdpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherdpdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vgatherdpd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vgatherdpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl Vgatherdpd3Emitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vgatherdpd_3(&mut self, dst: Vec, src: Mem, dst2: Vec) {
        self.emit_n(
            InstId::Vgatherdpd as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl Vgatherdpd3Emitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vgatherdpd_3(&mut self, dst: Xmm, src: Mem, dst2: Xmm) {
        self.emit_n(
            InstId::Vgatherdpd as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl Vgatherdpd3Emitter<Ymm, Mem, Ymm> for Assembler<'_> {
    fn vgatherdpd_3(&mut self, dst: Ymm, src: Mem, dst2: Ymm) {
        self.emit_n(
            InstId::Vgatherdpd as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl VgatherdpsEmitter<Vec, Mem> for Assembler<'_> {
    fn vgatherdps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vgatherdps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherdpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vgatherdps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vgatherdps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherdpsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vgatherdps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vgatherdps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherdpsEmitter<Zmm, Mem> for Assembler<'_> {
    fn vgatherdps(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vgatherdps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl Vgatherdps3Emitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vgatherdps_3(&mut self, dst: Vec, src: Mem, dst2: Vec) {
        self.emit_n(
            InstId::Vgatherdps as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl Vgatherdps3Emitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vgatherdps_3(&mut self, dst: Xmm, src: Mem, dst2: Xmm) {
        self.emit_n(
            InstId::Vgatherdps as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl Vgatherdps3Emitter<Ymm, Mem, Ymm> for Assembler<'_> {
    fn vgatherdps_3(&mut self, dst: Ymm, src: Mem, dst2: Ymm) {
        self.emit_n(
            InstId::Vgatherdps as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl VgatherqpdEmitter<Vec, Mem> for Assembler<'_> {
    fn vgatherqpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vgatherqpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherqpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vgatherqpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vgatherqpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherqpdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vgatherqpd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vgatherqpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherqpdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vgatherqpd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vgatherqpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl Vgatherqpd3Emitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vgatherqpd_3(&mut self, dst: Vec, src: Mem, dst2: Vec) {
        self.emit_n(
            InstId::Vgatherqpd as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl Vgatherqpd3Emitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vgatherqpd_3(&mut self, dst: Xmm, src: Mem, dst2: Xmm) {
        self.emit_n(
            InstId::Vgatherqpd as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl Vgatherqpd3Emitter<Ymm, Mem, Ymm> for Assembler<'_> {
    fn vgatherqpd_3(&mut self, dst: Ymm, src: Mem, dst2: Ymm) {
        self.emit_n(
            InstId::Vgatherqpd as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl VgatherqpsEmitter<Vec, Mem> for Assembler<'_> {
    fn vgatherqps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vgatherqps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherqpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vgatherqps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vgatherqps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherqpsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vgatherqps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vgatherqps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl Vgatherqps3Emitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vgatherqps_3(&mut self, dst: Vec, src: Mem, dst2: Vec) {
        self.emit_n(
            InstId::Vgatherqps as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl Vgatherqps3Emitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vgatherqps_3(&mut self, dst: Xmm, src: Mem, dst2: Xmm) {
        self.emit_n(
            InstId::Vgatherqps as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl VgetexppdEmitter<Vec, Vec> for Assembler<'_> {
    fn vgetexppd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vgetexppd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VgetexppdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vgetexppd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vgetexppd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VgetexppdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vgetexppd(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vgetexppd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VgetexppdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vgetexppd(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vgetexppd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VgetexppdEmitter<Vec, Mem> for Assembler<'_> {
    fn vgetexppd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vgetexppd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VgetexppdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vgetexppd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vgetexppd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VgetexppdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vgetexppd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vgetexppd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VgetexppdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vgetexppd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vgetexppd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VgetexppsEmitter<Vec, Vec> for Assembler<'_> {
    fn vgetexpps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vgetexpps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VgetexppsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vgetexpps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vgetexpps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VgetexppsEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vgetexpps(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vgetexpps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VgetexppsEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vgetexpps(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vgetexpps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VgetexppsEmitter<Vec, Mem> for Assembler<'_> {
    fn vgetexpps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vgetexpps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VgetexppsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vgetexpps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vgetexpps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VgetexppsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vgetexpps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vgetexpps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VgetexppsEmitter<Zmm, Mem> for Assembler<'_> {
    fn vgetexpps(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vgetexpps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VgetexpsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vgetexpsd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vgetexpsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetexpsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vgetexpsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vgetexpsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetexpsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vgetexpsd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vgetexpsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetexpsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vgetexpsd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vgetexpsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetexpssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vgetexpss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vgetexpss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetexpssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vgetexpss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vgetexpss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetexpssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vgetexpss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vgetexpss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetexpssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vgetexpss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vgetexpss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetmantpdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vgetmantpd(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vgetmantpd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VgetmantpdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vgetmantpd(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vgetmantpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VgetmantpdEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vgetmantpd(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vgetmantpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VgetmantpdEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vgetmantpd(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vgetmantpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VgetmantpdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vgetmantpd(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vgetmantpd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VgetmantpdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vgetmantpd(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vgetmantpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VgetmantpdEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vgetmantpd(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vgetmantpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VgetmantpdEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vgetmantpd(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vgetmantpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VgetmantpsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vgetmantps(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vgetmantps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VgetmantpsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vgetmantps(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vgetmantps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VgetmantpsEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vgetmantps(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vgetmantps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VgetmantpsEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vgetmantps(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vgetmantps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VgetmantpsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vgetmantps(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vgetmantps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VgetmantpsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vgetmantps(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vgetmantps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VgetmantpsEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vgetmantps(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vgetmantps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VgetmantpsEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vgetmantps(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vgetmantps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VgetmantsdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vgetmantsd(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vgetmantsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VgetmantsdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vgetmantsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vgetmantsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetmantsdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vgetmantsd(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vgetmantsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VgetmantsdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vgetmantsd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vgetmantsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetmantssEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vgetmantss(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vgetmantss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VgetmantssEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vgetmantss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vgetmantss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetmantssEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vgetmantss(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vgetmantss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VgetmantssEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vgetmantss(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vgetmantss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl Vgf2p8affineinvqbEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vgf2p8affineinvqb(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vgf2p8affineinvqb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineinvqbEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vgf2p8affineinvqb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineinvqb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineinvqbEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vgf2p8affineinvqb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineinvqb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineinvqbEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vgf2p8affineinvqb(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineinvqb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8affineinvqbEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vgf2p8affineinvqb(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vgf2p8affineinvqb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineinvqbEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vgf2p8affineinvqb(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineinvqb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineinvqbEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vgf2p8affineinvqb(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineinvqb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineinvqbEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vgf2p8affineinvqb(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineinvqb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8affineqbEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vgf2p8affineqb(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vgf2p8affineqb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineqbEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vgf2p8affineqb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineqb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineqbEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vgf2p8affineqb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineqb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineqbEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vgf2p8affineqb(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineqb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8affineqbEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vgf2p8affineqb(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vgf2p8affineqb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineqbEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vgf2p8affineqb(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineqb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineqbEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vgf2p8affineqb(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineqb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineqbEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vgf2p8affineqb(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineqb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8mulbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vgf2p8mulb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vgf2p8mulb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8mulbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vgf2p8mulb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vgf2p8mulb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8mulbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vgf2p8mulb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vgf2p8mulb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8mulbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vgf2p8mulb(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vgf2p8mulb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8mulbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vgf2p8mulb(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vgf2p8mulb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8mulbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vgf2p8mulb(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vgf2p8mulb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8mulbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vgf2p8mulb(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vgf2p8mulb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8mulbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vgf2p8mulb(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vgf2p8mulb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl VhaddpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vhaddpd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vhaddpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhaddpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vhaddpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vhaddpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhaddpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vhaddpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vhaddpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhaddpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vhaddpd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vhaddpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhaddpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vhaddpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vhaddpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhaddpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vhaddpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vhaddpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhaddpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vhaddps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vhaddps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhaddpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vhaddps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vhaddps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhaddpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vhaddps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vhaddps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhaddpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vhaddps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vhaddps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhaddpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vhaddps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vhaddps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhaddpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vhaddps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vhaddps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhsubpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vhsubpd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vhsubpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhsubpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vhsubpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vhsubpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhsubpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vhsubpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vhsubpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhsubpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vhsubpd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vhsubpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhsubpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vhsubpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vhsubpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhsubpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vhsubpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vhsubpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhsubpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vhsubps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vhsubps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhsubpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vhsubps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vhsubps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhsubpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vhsubps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vhsubps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhsubpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vhsubps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vhsubps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhsubpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vhsubps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vhsubps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VhsubpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vhsubps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vhsubps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl Vinsertf128Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinsertf128(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vinsertf128 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> Vinsertf128Emitter<Ymm, Ymm, Xmm, U3> for Assembler<'_> {
    fn vinsertf128(&mut self, dst: Ymm, src1: Ymm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vinsertf128 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl Vinsertf128Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinsertf128(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vinsertf128 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> Vinsertf128Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vinsertf128(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vinsertf128 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl Vinsertf32x4Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinsertf32x4(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vinsertf32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf32x4Emitter<Ymm, Ymm, Xmm, U3> for Assembler<'_> {
    fn vinsertf32x4(&mut self, dst: Ymm, src1: Ymm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vinsertf32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf32x4Emitter<Zmm, Zmm, Xmm, U3> for Assembler<'_> {
    fn vinsertf32x4(&mut self, dst: Zmm, src1: Zmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vinsertf32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vinsertf32x4Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinsertf32x4(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vinsertf32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf32x4Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vinsertf32x4(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vinsertf32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf32x4Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vinsertf32x4(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vinsertf32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vinsertf32x8Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinsertf32x8(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vinsertf32x8 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf32x8Emitter<Zmm, Zmm, Ymm, U3> for Assembler<'_> {
    fn vinsertf32x8(&mut self, dst: Zmm, src1: Zmm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vinsertf32x8 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vinsertf32x8Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinsertf32x8(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vinsertf32x8 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf32x8Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vinsertf32x8(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vinsertf32x8 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vinsertf64x2Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinsertf64x2(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vinsertf64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf64x2Emitter<Ymm, Ymm, Xmm, U3> for Assembler<'_> {
    fn vinsertf64x2(&mut self, dst: Ymm, src1: Ymm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vinsertf64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf64x2Emitter<Zmm, Zmm, Xmm, U3> for Assembler<'_> {
    fn vinsertf64x2(&mut self, dst: Zmm, src1: Zmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vinsertf64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vinsertf64x2Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinsertf64x2(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vinsertf64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf64x2Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vinsertf64x2(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vinsertf64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf64x2Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vinsertf64x2(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vinsertf64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vinsertf64x4Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinsertf64x4(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vinsertf64x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf64x4Emitter<Zmm, Zmm, Ymm, U3> for Assembler<'_> {
    fn vinsertf64x4(&mut self, dst: Zmm, src1: Zmm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vinsertf64x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vinsertf64x4Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinsertf64x4(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vinsertf64x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf64x4Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vinsertf64x4(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vinsertf64x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vinserti128Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinserti128(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vinserti128 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX2
    }
}

impl<U3: Into<Imm>> Vinserti128Emitter<Ymm, Ymm, Xmm, U3> for Assembler<'_> {
    fn vinserti128(&mut self, dst: Ymm, src1: Ymm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vinserti128 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX2
    }
}

impl Vinserti128Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinserti128(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vinserti128 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX2
    }
}

impl<U3: Into<Imm>> Vinserti128Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vinserti128(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vinserti128 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX2
    }
}

impl Vinserti32x4Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinserti32x4(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vinserti32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinserti32x4Emitter<Ymm, Ymm, Xmm, U3> for Assembler<'_> {
    fn vinserti32x4(&mut self, dst: Ymm, src1: Ymm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vinserti32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinserti32x4Emitter<Zmm, Zmm, Xmm, U3> for Assembler<'_> {
    fn vinserti32x4(&mut self, dst: Zmm, src1: Zmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vinserti32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vinserti32x4Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinserti32x4(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vinserti32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinserti32x4Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vinserti32x4(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vinserti32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinserti32x4Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vinserti32x4(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vinserti32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vinserti32x8Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinserti32x8(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vinserti32x8 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinserti32x8Emitter<Zmm, Zmm, Ymm, U3> for Assembler<'_> {
    fn vinserti32x8(&mut self, dst: Zmm, src1: Zmm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vinserti32x8 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vinserti32x8Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinserti32x8(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vinserti32x8 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinserti32x8Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vinserti32x8(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vinserti32x8 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vinserti64x2Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinserti64x2(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vinserti64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinserti64x2Emitter<Ymm, Ymm, Xmm, U3> for Assembler<'_> {
    fn vinserti64x2(&mut self, dst: Ymm, src1: Ymm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vinserti64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinserti64x2Emitter<Zmm, Zmm, Xmm, U3> for Assembler<'_> {
    fn vinserti64x2(&mut self, dst: Zmm, src1: Zmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vinserti64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vinserti64x2Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinserti64x2(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vinserti64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinserti64x2Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vinserti64x2(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vinserti64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinserti64x2Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vinserti64x2(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vinserti64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vinserti64x4Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinserti64x4(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vinserti64x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinserti64x4Emitter<Zmm, Zmm, Ymm, U3> for Assembler<'_> {
    fn vinserti64x4(&mut self, dst: Zmm, src1: Zmm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vinserti64x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vinserti64x4Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinserti64x4(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vinserti64x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinserti64x4Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vinserti64x4(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vinserti64x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl VinsertpsEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinsertps(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vinsertps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX AVX512_F
    }
}

impl<U3: Into<Imm>> VinsertpsEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vinsertps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vinsertps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_F
    }
}

impl VinsertpsEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinsertps(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vinsertps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX AVX512_F
    }
}

impl<U3: Into<Imm>> VinsertpsEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vinsertps(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vinsertps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_F
    }
}

impl VlddquEmitter<Vec, Mem> for Assembler<'_> {
    fn vlddqu(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Vlddqu as u32, &[dst.as_operand(), src.as_operand()]); // AVX
    }
}

impl VlddquEmitter<Xmm, Mem> for Assembler<'_> {
    fn vlddqu(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Vlddqu as u32, &[dst.as_operand(), src.as_operand()]); // AVX
    }
}

impl VlddquEmitter<Ymm, Mem> for Assembler<'_> {
    fn vlddqu(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(InstId::Vlddqu as u32, &[dst.as_operand(), src.as_operand()]); // AVX
    }
}

impl VldmxcsrEmitter<Mem> for Assembler<'_> {
    fn vldmxcsr(&mut self, mem: Mem) {
        self.emit_n(InstId::Vldmxcsr as u32, &[mem.as_operand()]); // AVX
    }
}

impl VmaskmovdquEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmaskmovdqu(&mut self, dst: Vec, src: Vec, dst2: Mem) {
        self.emit_n(
            InstId::Vmaskmovdqu as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX [EXPLICIT]
    }
}

impl VmaskmovdquEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmaskmovdqu(&mut self, dst: Xmm, src: Xmm, dst2: Mem) {
        self.emit_n(
            InstId::Vmaskmovdqu as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX [EXPLICIT]
    }
}

impl VmaskmovpdEmitter<Mem, Vec, Vec> for Assembler<'_> {
    fn vmaskmovpd(&mut self, dst: Mem, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vmaskmovpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpdEmitter<Mem, Xmm, Xmm> for Assembler<'_> {
    fn vmaskmovpd(&mut self, dst: Mem, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vmaskmovpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpdEmitter<Mem, Ymm, Ymm> for Assembler<'_> {
    fn vmaskmovpd(&mut self, dst: Mem, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vmaskmovpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmaskmovpd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vmaskmovpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmaskmovpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vmaskmovpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vmaskmovpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vmaskmovpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpsEmitter<Mem, Vec, Vec> for Assembler<'_> {
    fn vmaskmovps(&mut self, dst: Mem, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vmaskmovps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpsEmitter<Mem, Xmm, Xmm> for Assembler<'_> {
    fn vmaskmovps(&mut self, dst: Mem, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vmaskmovps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpsEmitter<Mem, Ymm, Ymm> for Assembler<'_> {
    fn vmaskmovps(&mut self, dst: Mem, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vmaskmovps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmaskmovps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vmaskmovps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmaskmovps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vmaskmovps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vmaskmovps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vmaskmovps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VmaxpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmaxpd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vmaxpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmaxpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmaxpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vmaxpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmaxpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vmaxpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vmaxpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmaxpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vmaxpd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vmaxpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmaxpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmaxpd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vmaxpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmaxpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmaxpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vmaxpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmaxpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vmaxpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vmaxpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmaxpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vmaxpd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vmaxpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmaxpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmaxps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vmaxps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmaxpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmaxps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vmaxps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmaxpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vmaxps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vmaxps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmaxpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vmaxps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vmaxps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmaxpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmaxps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vmaxps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmaxpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmaxps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vmaxps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmaxpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vmaxps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vmaxps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmaxpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vmaxps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vmaxps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmaxsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmaxsd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vmaxsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VmaxsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmaxsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vmaxsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VmaxsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmaxsd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vmaxsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VmaxsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmaxsd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vmaxsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VmaxssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmaxss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vmaxss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VmaxssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmaxss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vmaxss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VmaxssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmaxss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vmaxss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VmaxssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmaxss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vmaxss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VminpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vminpd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vminpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VminpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vminpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vminpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VminpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vminpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vminpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VminpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vminpd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vminpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VminpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vminpd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vminpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VminpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vminpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vminpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VminpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vminpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vminpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VminpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vminpd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vminpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VminpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vminps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vminps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VminpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vminps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vminps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VminpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vminps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vminps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VminpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vminps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vminps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VminpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vminps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vminps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VminpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vminps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vminps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VminpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vminps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vminps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VminpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vminps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vminps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VminsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vminsd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vminsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VminsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vminsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vminsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VminsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vminsd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vminsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VminsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vminsd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vminsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VminssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vminss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vminss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VminssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vminss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vminss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VminssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vminss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vminss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VminssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vminss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vminss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VmovapdEmitter<Vec, Vec> for Assembler<'_> {
    fn vmovapd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovapd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovapd(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovapd(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovapd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovapd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vmovapd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vmovapd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovapd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovapd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vmovapd(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Mem, Zmm> for Assembler<'_> {
    fn vmovapd(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Vec, Vec> for Assembler<'_> {
    fn vmovaps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovaps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovaps(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovaps(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovaps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovaps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vmovaps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Zmm, Mem> for Assembler<'_> {
    fn vmovaps(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovaps(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovaps(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Mem, Ymm> for Assembler<'_> {
    fn vmovaps(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Mem, Zmm> for Assembler<'_> {
    fn vmovaps(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovdEmitter<Gp, Vec> for Assembler<'_> {
    fn vmovd(&mut self, dst: Gp, src: Vec) {
        self.emit_n(InstId::Vmovd as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovdEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vmovd(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(InstId::Vmovd as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovdEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(InstId::Vmovd as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(InstId::Vmovd as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovdEmitter<Vec, Gp> for Assembler<'_> {
    fn vmovd(&mut self, dst: Vec, src: Gp) {
        self.emit_n(InstId::Vmovd as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovdEmitter<Xmm, Gpd> for Assembler<'_> {
    fn vmovd(&mut self, dst: Xmm, src: Gpd) {
        self.emit_n(InstId::Vmovd as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovdEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Vmovd as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Vmovd as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovddupEmitter<Vec, Vec> for Assembler<'_> {
    fn vmovddup(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vmovddup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovddupEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovddup(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vmovddup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovddupEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovddup(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vmovddup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovddupEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovddup(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vmovddup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovddupEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovddup(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vmovddup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovddupEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovddup(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vmovddup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovddupEmitter<Ymm, Mem> for Assembler<'_> {
    fn vmovddup(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vmovddup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovddupEmitter<Zmm, Mem> for Assembler<'_> {
    fn vmovddup(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vmovddup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovdqaEmitter<Vec, Vec> for Assembler<'_> {
    fn vmovdqa(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vmovdqa as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovdqaEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovdqa(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vmovdqa as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovdqaEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovdqa(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vmovdqa as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovdqaEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovdqa(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vmovdqa as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovdqaEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovdqa(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqa as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovdqaEmitter<Ymm, Mem> for Assembler<'_> {
    fn vmovdqa(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqa as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovdqaEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovdqa(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vmovdqa as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovdqaEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovdqa(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vmovdqa as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovdqaEmitter<Mem, Ymm> for Assembler<'_> {
    fn vmovdqa(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vmovdqa as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl Vmovdqa32Emitter<Vec, Vec> for Assembler<'_> {
    fn vmovdqa32(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovdqa32(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovdqa32(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovdqa32(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Vec, Mem> for Assembler<'_> {
    fn vmovdqa32(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Xmm, Mem> for Assembler<'_> {
    fn vmovdqa32(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Ymm, Mem> for Assembler<'_> {
    fn vmovdqa32(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Zmm, Mem> for Assembler<'_> {
    fn vmovdqa32(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Mem, Vec> for Assembler<'_> {
    fn vmovdqa32(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Mem, Xmm> for Assembler<'_> {
    fn vmovdqa32(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Mem, Ymm> for Assembler<'_> {
    fn vmovdqa32(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Mem, Zmm> for Assembler<'_> {
    fn vmovdqa32(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Vec, Vec> for Assembler<'_> {
    fn vmovdqa64(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovdqa64(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovdqa64(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovdqa64(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Vec, Mem> for Assembler<'_> {
    fn vmovdqa64(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Xmm, Mem> for Assembler<'_> {
    fn vmovdqa64(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Ymm, Mem> for Assembler<'_> {
    fn vmovdqa64(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Zmm, Mem> for Assembler<'_> {
    fn vmovdqa64(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Mem, Vec> for Assembler<'_> {
    fn vmovdqa64(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Mem, Xmm> for Assembler<'_> {
    fn vmovdqa64(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Mem, Ymm> for Assembler<'_> {
    fn vmovdqa64(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Mem, Zmm> for Assembler<'_> {
    fn vmovdqa64(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VmovdquEmitter<Vec, Vec> for Assembler<'_> {
    fn vmovdqu(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vmovdqu as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovdquEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovdqu(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vmovdqu as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovdquEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovdqu(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vmovdqu as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovdquEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovdqu(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vmovdqu as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovdquEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovdqu(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqu as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovdquEmitter<Ymm, Mem> for Assembler<'_> {
    fn vmovdqu(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqu as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovdquEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovdqu(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vmovdqu as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovdquEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovdqu(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vmovdqu as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovdquEmitter<Mem, Ymm> for Assembler<'_> {
    fn vmovdqu(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vmovdqu as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl Vmovdqu16Emitter<Vec, Vec> for Assembler<'_> {
    fn vmovdqu16(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovdqu16(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovdqu16(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovdqu16(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Vec, Mem> for Assembler<'_> {
    fn vmovdqu16(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Xmm, Mem> for Assembler<'_> {
    fn vmovdqu16(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Ymm, Mem> for Assembler<'_> {
    fn vmovdqu16(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Zmm, Mem> for Assembler<'_> {
    fn vmovdqu16(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Mem, Vec> for Assembler<'_> {
    fn vmovdqu16(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Mem, Xmm> for Assembler<'_> {
    fn vmovdqu16(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Mem, Ymm> for Assembler<'_> {
    fn vmovdqu16(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Mem, Zmm> for Assembler<'_> {
    fn vmovdqu16(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu32Emitter<Vec, Vec> for Assembler<'_> {
    fn vmovdqu32(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovdqu32(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovdqu32(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovdqu32(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Vec, Mem> for Assembler<'_> {
    fn vmovdqu32(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Xmm, Mem> for Assembler<'_> {
    fn vmovdqu32(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Ymm, Mem> for Assembler<'_> {
    fn vmovdqu32(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Zmm, Mem> for Assembler<'_> {
    fn vmovdqu32(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Mem, Vec> for Assembler<'_> {
    fn vmovdqu32(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Mem, Xmm> for Assembler<'_> {
    fn vmovdqu32(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Mem, Ymm> for Assembler<'_> {
    fn vmovdqu32(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Mem, Zmm> for Assembler<'_> {
    fn vmovdqu32(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Vec, Vec> for Assembler<'_> {
    fn vmovdqu64(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovdqu64(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovdqu64(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovdqu64(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Vec, Mem> for Assembler<'_> {
    fn vmovdqu64(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Xmm, Mem> for Assembler<'_> {
    fn vmovdqu64(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Ymm, Mem> for Assembler<'_> {
    fn vmovdqu64(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Zmm, Mem> for Assembler<'_> {
    fn vmovdqu64(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Mem, Vec> for Assembler<'_> {
    fn vmovdqu64(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Mem, Xmm> for Assembler<'_> {
    fn vmovdqu64(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Mem, Ymm> for Assembler<'_> {
    fn vmovdqu64(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Mem, Zmm> for Assembler<'_> {
    fn vmovdqu64(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu8Emitter<Vec, Vec> for Assembler<'_> {
    fn vmovdqu8(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovdqu8(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovdqu8(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovdqu8(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Vec, Mem> for Assembler<'_> {
    fn vmovdqu8(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Xmm, Mem> for Assembler<'_> {
    fn vmovdqu8(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Ymm, Mem> for Assembler<'_> {
    fn vmovdqu8(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Zmm, Mem> for Assembler<'_> {
    fn vmovdqu8(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Mem, Vec> for Assembler<'_> {
    fn vmovdqu8(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Mem, Xmm> for Assembler<'_> {
    fn vmovdqu8(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Mem, Ymm> for Assembler<'_> {
    fn vmovdqu8(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Mem, Zmm> for Assembler<'_> {
    fn vmovdqu8(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VmovhlpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmovhlps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vmovhlps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovhlpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmovhlps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vmovhlps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovhpdEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovhpd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vmovhpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovhpdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovhpd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vmovhpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F
    }
}

impl Vmovhpd3Emitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmovhpd_3(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vmovhpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl Vmovhpd3Emitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmovhpd_3(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vmovhpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovhpsEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovhps(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vmovhps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovhpsEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovhps(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vmovhps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F
    }
}

impl Vmovhps3Emitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmovhps_3(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vmovhps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl Vmovhps3Emitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmovhps_3(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vmovhps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovlhpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmovlhps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vmovlhps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovlhpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmovlhps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vmovlhps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovlpdEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovlpd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vmovlpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovlpdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovlpd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vmovlpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F
    }
}

impl Vmovlpd3Emitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmovlpd_3(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vmovlpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl Vmovlpd3Emitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmovlpd_3(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vmovlpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovlpsEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovlps(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vmovlps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovlpsEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovlps(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vmovlps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F
    }
}

impl Vmovlps3Emitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmovlps_3(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vmovlps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl Vmovlps3Emitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmovlps_3(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vmovlps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovmskpdEmitter<Gp, Vec> for Assembler<'_> {
    fn vmovmskpd(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Vmovmskpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovmskpdEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vmovmskpd(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Vmovmskpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovmskpdEmitter<Gpd, Ymm> for Assembler<'_> {
    fn vmovmskpd(&mut self, dst: Gpd, src: Ymm) {
        self.emit_n(
            InstId::Vmovmskpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovmskpsEmitter<Gp, Vec> for Assembler<'_> {
    fn vmovmskps(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Vmovmskps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovmskpsEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vmovmskps(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Vmovmskps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovmskpsEmitter<Gpd, Ymm> for Assembler<'_> {
    fn vmovmskps(&mut self, dst: Gpd, src: Ymm) {
        self.emit_n(
            InstId::Vmovmskps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VmovntdqEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovntdq(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vmovntdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F
    }
}

impl VmovntdqEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovntdq(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vmovntdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F
    }
}

impl VmovntdqEmitter<Mem, Ymm> for Assembler<'_> {
    fn vmovntdq(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vmovntdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F
    }
}

impl VmovntdqEmitter<Mem, Zmm> for Assembler<'_> {
    fn vmovntdq(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vmovntdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F
    }
}

impl VmovntdqaEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovntdqa(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vmovntdqa as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F
    }
}

impl VmovntdqaEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovntdqa(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vmovntdqa as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F
    }
}

impl VmovntdqaEmitter<Ymm, Mem> for Assembler<'_> {
    fn vmovntdqa(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vmovntdqa as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F
    }
}

impl VmovntdqaEmitter<Zmm, Mem> for Assembler<'_> {
    fn vmovntdqa(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vmovntdqa as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F
    }
}

impl VmovntpdEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovntpd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vmovntpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovntpdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovntpd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vmovntpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovntpdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vmovntpd(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vmovntpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovntpdEmitter<Mem, Zmm> for Assembler<'_> {
    fn vmovntpd(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vmovntpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovntpsEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovntps(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vmovntps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovntpsEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovntps(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vmovntps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovntpsEmitter<Mem, Ymm> for Assembler<'_> {
    fn vmovntps(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vmovntps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovntpsEmitter<Mem, Zmm> for Assembler<'_> {
    fn vmovntps(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vmovntps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovqEmitter<Gp, Vec> for Assembler<'_> {
    fn vmovq(&mut self, dst: Gp, src: Vec) {
        self.emit_n(InstId::Vmovq as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovqEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vmovq(&mut self, dst: Gpq, src: Xmm) {
        self.emit_n(InstId::Vmovq as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovqEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovq(&mut self, dst: Mem, src: Vec) {
        self.emit_n(InstId::Vmovq as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovqEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovq(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(InstId::Vmovq as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovqEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Vmovq as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Vmovq as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovqEmitter<Vec, Gp> for Assembler<'_> {
    fn vmovq(&mut self, dst: Vec, src: Gp) {
        self.emit_n(InstId::Vmovq as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovqEmitter<Xmm, Gpq> for Assembler<'_> {
    fn vmovq(&mut self, dst: Xmm, src: Gpq) {
        self.emit_n(InstId::Vmovq as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovqEmitter<Vec, Vec> for Assembler<'_> {
    fn vmovq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Vmovq as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Vmovq as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovsdEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovsd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(InstId::Vmovsd as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovsdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovsd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(InstId::Vmovsd as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovsdEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovsd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Vmovsd as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F{kz}
    }
}

impl VmovsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovsd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Vmovsd as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F{kz}
    }
}

impl Vmovsd3Emitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmovsd_3(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vmovsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl Vmovsd3Emitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmovsd_3(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vmovsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovshdupEmitter<Vec, Vec> for Assembler<'_> {
    fn vmovshdup(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vmovshdup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovshdupEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovshdup(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vmovshdup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovshdupEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovshdup(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vmovshdup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovshdupEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovshdup(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vmovshdup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovshdupEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovshdup(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vmovshdup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovshdupEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovshdup(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vmovshdup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovshdupEmitter<Ymm, Mem> for Assembler<'_> {
    fn vmovshdup(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vmovshdup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovshdupEmitter<Zmm, Mem> for Assembler<'_> {
    fn vmovshdup(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vmovshdup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovsldupEmitter<Vec, Vec> for Assembler<'_> {
    fn vmovsldup(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vmovsldup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovsldupEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovsldup(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vmovsldup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovsldupEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovsldup(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vmovsldup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovsldupEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovsldup(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vmovsldup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovsldupEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovsldup(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vmovsldup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovsldupEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovsldup(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vmovsldup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovsldupEmitter<Ymm, Mem> for Assembler<'_> {
    fn vmovsldup(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vmovsldup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovsldupEmitter<Zmm, Mem> for Assembler<'_> {
    fn vmovsldup(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vmovsldup as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovssEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovss(&mut self, dst: Mem, src: Vec) {
        self.emit_n(InstId::Vmovss as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovssEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovss(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(InstId::Vmovss as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F
    }
}

impl VmovssEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovss(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Vmovss as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F{kz}
    }
}

impl VmovssEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovss(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Vmovss as u32, &[dst.as_operand(), src.as_operand()]); // AVX AVX512_F{kz}
    }
}

impl Vmovss3Emitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmovss_3(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vmovss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl Vmovss3Emitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmovss_3(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vmovss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Vec, Vec> for Assembler<'_> {
    fn vmovupd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovupd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovupd(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovupd(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovupd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovupd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vmovupd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vmovupd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovupd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovupd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vmovupd(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Mem, Zmm> for Assembler<'_> {
    fn vmovupd(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Vec, Vec> for Assembler<'_> {
    fn vmovups(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovups(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovups(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovups(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovups(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovups(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vmovups(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Zmm, Mem> for Assembler<'_> {
    fn vmovups(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovups(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovups(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Mem, Ymm> for Assembler<'_> {
    fn vmovups(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Mem, Zmm> for Assembler<'_> {
    fn vmovups(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmpsadbwEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vmpsadbw(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vmpsadbw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX+
    }
}

impl<U3: Into<Imm>> VmpsadbwEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vmpsadbw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vmpsadbw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX+
    }
}

impl<U3: Into<Imm>> VmpsadbwEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vmpsadbw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vmpsadbw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX+
    }
}

impl VmpsadbwEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vmpsadbw(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vmpsadbw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX+
    }
}

impl<U3: Into<Imm>> VmpsadbwEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vmpsadbw(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vmpsadbw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX+
    }
}

impl<U3: Into<Imm>> VmpsadbwEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vmpsadbw(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vmpsadbw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX+
    }
}

impl VmulpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmulpd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vmulpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmulpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmulpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vmulpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmulpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vmulpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vmulpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmulpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vmulpd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vmulpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmulpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmulpd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vmulpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmulpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmulpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vmulpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmulpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vmulpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vmulpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmulpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vmulpd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vmulpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmulpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmulps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vmulps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmulpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmulps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vmulps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmulpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vmulps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vmulps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmulpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vmulps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vmulps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmulpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmulps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vmulps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmulpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmulps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vmulps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmulpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vmulps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vmulps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmulpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vmulps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vmulps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmulsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmulsd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vmulsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VmulsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmulsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vmulsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VmulsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmulsd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vmulsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VmulsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmulsd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vmulsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VmulssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmulss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vmulss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VmulssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmulss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vmulss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VmulssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmulss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vmulss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VmulssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmulss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vmulss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VorpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vorpd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vorpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VorpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vorpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vorpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VorpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vorpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vorpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VorpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vorpd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vorpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VorpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vorpd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vorpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VorpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vorpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vorpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VorpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vorpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vorpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VorpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vorpd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vorpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VorpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vorps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vorps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VorpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vorps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vorps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VorpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vorps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vorps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VorpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vorps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vorps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VorpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vorps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vorps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VorpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vorps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vorps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VorpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vorps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vorps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VorpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vorps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vorps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vp2intersectdEmitter<KReg, KReg, Vec, Vec> for Assembler<'_> {
    fn vp2intersectd(&mut self, dst: KReg, dst2: KReg, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vp2intersectd as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectdEmitter<KReg, KReg, Xmm, Xmm> for Assembler<'_> {
    fn vp2intersectd(&mut self, dst: KReg, dst2: KReg, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vp2intersectd as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectdEmitter<KReg, KReg, Ymm, Ymm> for Assembler<'_> {
    fn vp2intersectd(&mut self, dst: KReg, dst2: KReg, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vp2intersectd as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectdEmitter<KReg, KReg, Zmm, Zmm> for Assembler<'_> {
    fn vp2intersectd(&mut self, dst: KReg, dst2: KReg, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vp2intersectd as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectdEmitter<KReg, KReg, Vec, Mem> for Assembler<'_> {
    fn vp2intersectd(&mut self, dst: KReg, dst2: KReg, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vp2intersectd as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectdEmitter<KReg, KReg, Xmm, Mem> for Assembler<'_> {
    fn vp2intersectd(&mut self, dst: KReg, dst2: KReg, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vp2intersectd as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectdEmitter<KReg, KReg, Ymm, Mem> for Assembler<'_> {
    fn vp2intersectd(&mut self, dst: KReg, dst2: KReg, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vp2intersectd as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectdEmitter<KReg, KReg, Zmm, Mem> for Assembler<'_> {
    fn vp2intersectd(&mut self, dst: KReg, dst2: KReg, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vp2intersectd as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectqEmitter<KReg, KReg, Vec, Vec> for Assembler<'_> {
    fn vp2intersectq(&mut self, dst: KReg, dst2: KReg, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vp2intersectq as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectqEmitter<KReg, KReg, Xmm, Xmm> for Assembler<'_> {
    fn vp2intersectq(&mut self, dst: KReg, dst2: KReg, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vp2intersectq as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectqEmitter<KReg, KReg, Ymm, Ymm> for Assembler<'_> {
    fn vp2intersectq(&mut self, dst: KReg, dst2: KReg, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vp2intersectq as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectqEmitter<KReg, KReg, Zmm, Zmm> for Assembler<'_> {
    fn vp2intersectq(&mut self, dst: KReg, dst2: KReg, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vp2intersectq as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectqEmitter<KReg, KReg, Vec, Mem> for Assembler<'_> {
    fn vp2intersectq(&mut self, dst: KReg, dst2: KReg, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vp2intersectq as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectqEmitter<KReg, KReg, Xmm, Mem> for Assembler<'_> {
    fn vp2intersectq(&mut self, dst: KReg, dst2: KReg, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vp2intersectq as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectqEmitter<KReg, KReg, Ymm, Mem> for Assembler<'_> {
    fn vp2intersectq(&mut self, dst: KReg, dst2: KReg, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vp2intersectq as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectqEmitter<KReg, KReg, Zmm, Mem> for Assembler<'_> {
    fn vp2intersectq(&mut self, dst: KReg, dst2: KReg, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vp2intersectq as u32,
            &[
                dst.as_operand(),
                dst2.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl VpabsbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpabsb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Vpabsb as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabsbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpabsb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Vpabsb as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabsbEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpabsb(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(InstId::Vpabsb as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabsbEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpabsb(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(InstId::Vpabsb as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabsbEmitter<Vec, Mem> for Assembler<'_> {
    fn vpabsb(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Vpabsb as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabsbEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpabsb(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Vpabsb as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabsbEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpabsb(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(InstId::Vpabsb as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabsbEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpabsb(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(InstId::Vpabsb as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabsdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpabsd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Vpabsd as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_F{kz}
    }
}

impl VpabsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpabsd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Vpabsd as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_F{kz}
    }
}

impl VpabsdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpabsd(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(InstId::Vpabsd as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_F{kz}
    }
}

impl VpabsdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpabsd(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(InstId::Vpabsd as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_F{kz}
    }
}

impl VpabsdEmitter<Vec, Mem> for Assembler<'_> {
    fn vpabsd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Vpabsd as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_F{kz}
    }
}

impl VpabsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpabsd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Vpabsd as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_F{kz}
    }
}

impl VpabsdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpabsd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(InstId::Vpabsd as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_F{kz}
    }
}

impl VpabsdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpabsd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(InstId::Vpabsd as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_F{kz}
    }
}

impl VpabsqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpabsq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Vpabsq as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_F{kz}
    }
}

impl VpabsqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpabsq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Vpabsq as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_F{kz}
    }
}

impl VpabsqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpabsq(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(InstId::Vpabsq as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_F{kz}
    }
}

impl VpabsqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpabsq(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(InstId::Vpabsq as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_F{kz}
    }
}

impl VpabsqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpabsq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Vpabsq as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_F{kz}
    }
}

impl VpabsqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpabsq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Vpabsq as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_F{kz}
    }
}

impl VpabsqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpabsq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(InstId::Vpabsq as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_F{kz}
    }
}

impl VpabsqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpabsq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(InstId::Vpabsq as u32, &[dst.as_operand(), src.as_operand()]); // AVX512_F{kz}
    }
}

impl VpabswEmitter<Vec, Vec> for Assembler<'_> {
    fn vpabsw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Vpabsw as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpabsw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Vpabsw as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabswEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpabsw(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(InstId::Vpabsw as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabswEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpabsw(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(InstId::Vpabsw as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabswEmitter<Vec, Mem> for Assembler<'_> {
    fn vpabsw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Vpabsw as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabswEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpabsw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Vpabsw as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabswEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpabsw(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(InstId::Vpabsw as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabswEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpabsw(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(InstId::Vpabsw as u32, &[dst.as_operand(), src.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpackssdwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpackssdw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpackssdw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackssdwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpackssdw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpackssdw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackssdwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpackssdw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpackssdw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackssdwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpackssdw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpackssdw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackssdwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpackssdw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpackssdw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackssdwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpackssdw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpackssdw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackssdwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpackssdw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpackssdw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackssdwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpackssdw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpackssdw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpacksswbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpacksswb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpacksswb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpacksswbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpacksswb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpacksswb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpacksswbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpacksswb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpacksswb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpacksswbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpacksswb(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpacksswb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpacksswbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpacksswb(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpacksswb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpacksswbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpacksswb(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpacksswb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpacksswbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpacksswb(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpacksswb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpacksswbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpacksswb(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpacksswb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpackusdwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpackusdw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpackusdw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackusdwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpackusdw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpackusdw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackusdwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpackusdw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpackusdw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackusdwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpackusdw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpackusdw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackusdwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpackusdw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpackusdw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackusdwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpackusdw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpackusdw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackusdwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpackusdw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpackusdw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackusdwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpackusdw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpackusdw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackuswbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpackuswb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpackuswb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpackuswbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpackuswb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpackuswb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpackuswbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpackuswb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpackuswb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpackuswbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpackuswb(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpackuswb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpackuswbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpackuswb(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpackuswb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpackuswbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpackuswb(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpackuswb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpackuswbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpackuswb(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpackuswb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpackuswbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpackuswb(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpackuswb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpaddb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpaddb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpaddb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpaddb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpaddb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpaddb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpaddb(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpaddb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpaddb(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpaddb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpaddb(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpaddb(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpaddb(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpadddEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpaddd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpaddd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpadddEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpaddd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpaddd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpadddEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpaddd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpaddd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpadddEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpaddd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpaddd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpadddEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpaddd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpaddd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpadddEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpaddd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpadddEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpaddd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpadddEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpaddd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpaddqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpaddq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpaddq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpaddqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpaddq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpaddq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpaddqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpaddq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpaddq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpaddqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpaddq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpaddq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpaddqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpaddq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpaddq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpaddqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpaddq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpaddqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpaddq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpaddqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpaddq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpaddsbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpaddsb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpaddsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddsbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpaddsb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpaddsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddsbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpaddsb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpaddsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddsbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpaddsb(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpaddsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddsbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpaddsb(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpaddsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddsbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpaddsb(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddsbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpaddsb(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddsbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpaddsb(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddswEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpaddsw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpaddsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddswEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpaddsw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpaddsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddswEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpaddsw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpaddsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddswEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpaddsw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpaddsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddswEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpaddsw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpaddsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddswEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpaddsw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddswEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpaddsw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddswEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpaddsw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddusbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpaddusb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpaddusb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddusbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpaddusb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpaddusb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddusbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpaddusb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpaddusb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddusbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpaddusb(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpaddusb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddusbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpaddusb(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpaddusb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddusbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpaddusb(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddusb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddusbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpaddusb(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddusb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddusbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpaddusb(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddusb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpadduswEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpaddusw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpaddusw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpadduswEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpaddusw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpaddusw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpadduswEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpaddusw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpaddusw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpadduswEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpaddusw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpaddusw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpadduswEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpaddusw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpaddusw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpadduswEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpaddusw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddusw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpadduswEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpaddusw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddusw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpadduswEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpaddusw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddusw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpaddw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpaddw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpaddw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpaddw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpaddw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpaddw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpaddw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpaddw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpaddw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpaddw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpaddw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpaddw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpaddw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpaddw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpalignrEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpalignr(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpalignr as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VpalignrEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpalignr(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpalignr as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VpalignrEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpalignr(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vpalignr as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VpalignrEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpalignr(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vpalignr as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpalignrEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpalignr(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpalignr as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VpalignrEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpalignr(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpalignr as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VpalignrEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpalignr(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpalignr as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VpalignrEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vpalignr(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpalignr as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpandEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpand(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpand as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpandEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpand(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpand as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpandEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpand(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpand as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpandEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpand(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpand as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpandEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpand(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpand as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpandEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpand(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpand as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpanddEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpandd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpandd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpanddEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpandd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpandd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpanddEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpandd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpandd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpanddEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpandd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpandd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpanddEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpandd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpandd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpanddEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpandd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpandd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpanddEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpandd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpandd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpanddEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpandd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpandd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpandnEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpandn(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpandn as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpandnEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpandn(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpandn as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpandnEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpandn(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpandn as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpandnEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpandn(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpandn as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpandnEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpandn(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpandn as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpandnEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpandn(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpandn as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpandndEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpandnd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpandnd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpandndEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpandnd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpandnd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpandndEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpandnd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpandnd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpandndEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpandnd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpandnd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpandndEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpandnd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpandnd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpandndEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpandnd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpandnd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpandndEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpandnd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpandnd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpandndEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpandnd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpandnd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpandnqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpandnq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpandnq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandnqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpandnq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpandnq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandnqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpandnq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpandnq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandnqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpandnq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpandnq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandnqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpandnq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpandnq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandnqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpandnq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpandnq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandnqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpandnq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpandnq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandnqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpandnq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpandnq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpandq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpandq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpandq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpandq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpandq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpandq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpandq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpandq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpandq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpandq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpandq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpandq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpandq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpandq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpandq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpandq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpavgbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpavgb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpavgb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpavgb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpavgb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpavgb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpavgb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpavgb(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpavgb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpavgb(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpavgb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpavgb(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpavgb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpavgb(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpavgb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpavgb(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpavgb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpavgw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpavgw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpavgw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpavgw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpavgw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpavgw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpavgw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpavgw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpavgw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpavgw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpavgw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpavgw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpavgw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpavgw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpavgw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpavgw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpblenddEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpblendd(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpblendd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX2
    }
}

impl<U3: Into<Imm>> VpblenddEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpblendd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpblendd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX2
    }
}

impl<U3: Into<Imm>> VpblenddEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpblendd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vpblendd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX2
    }
}

impl VpblenddEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpblendd(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpblendd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX2
    }
}

impl<U3: Into<Imm>> VpblenddEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpblendd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpblendd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX2
    }
}

impl<U3: Into<Imm>> VpblenddEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpblendd(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpblendd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX2
    }
}

impl VpblendmbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpblendmb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpblendmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpblendmb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpblendmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpblendmb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpblendmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpblendmb(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpblendmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpblendmb(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpblendmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpblendmb(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpblendmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpblendmb(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpblendmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpblendmb(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpblendmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpblendmd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpblendmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpblendmdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpblendmd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpblendmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpblendmdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpblendmd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpblendmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpblendmdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpblendmd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpblendmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpblendmdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpblendmd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpblendmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpblendmdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpblendmd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpblendmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpblendmdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpblendmd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpblendmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpblendmdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpblendmd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpblendmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpblendmqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpblendmq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpblendmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpblendmqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpblendmq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpblendmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpblendmqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpblendmq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpblendmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpblendmqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpblendmq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpblendmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpblendmqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpblendmq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpblendmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpblendmqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpblendmq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpblendmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpblendmqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpblendmq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpblendmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpblendmqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpblendmq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpblendmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpblendmwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpblendmw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpblendmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpblendmw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpblendmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpblendmw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpblendmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpblendmw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpblendmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpblendmw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpblendmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpblendmw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpblendmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpblendmw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpblendmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpblendmw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpblendmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendvbEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpblendvb(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vpblendvb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX+
    }
}

impl VpblendvbEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpblendvb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vpblendvb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX+
    }
}

impl VpblendvbEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpblendvb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Ymm) {
        self.emit_n(
            InstId::Vpblendvb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX+
    }
}

impl VpblendvbEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpblendvb(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vpblendvb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX+
    }
}

impl VpblendvbEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpblendvb(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vpblendvb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX+
    }
}

impl VpblendvbEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vpblendvb(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: Ymm) {
        self.emit_n(
            InstId::Vpblendvb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX+
    }
}

impl VpblendwEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpblendw(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpblendw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX+
    }
}

impl<U3: Into<Imm>> VpblendwEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpblendw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpblendw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX+
    }
}

impl<U3: Into<Imm>> VpblendwEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpblendw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vpblendw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX+
    }
}

impl VpblendwEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpblendw(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpblendw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX+
    }
}

impl<U3: Into<Imm>> VpblendwEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpblendw(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpblendw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX+
    }
}

impl<U3: Into<Imm>> VpblendwEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpblendw(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpblendw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX+
    }
}

impl VpbroadcastbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpbroadcastb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpbroadcastb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpbroadcastb(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vpbroadcastb(&mut self, dst: Zmm, src: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Vec, Mem> for Assembler<'_> {
    fn vpbroadcastb(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpbroadcastb(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpbroadcastb(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpbroadcastb(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Vec, Gp> for Assembler<'_> {
    fn vpbroadcastb(&mut self, dst: Vec, src: Gp) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Xmm, Gpd> for Assembler<'_> {
    fn vpbroadcastb(&mut self, dst: Xmm, src: Gpd) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Ymm, Gpd> for Assembler<'_> {
    fn vpbroadcastb(&mut self, dst: Ymm, src: Gpd) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Zmm, Gpd> for Assembler<'_> {
    fn vpbroadcastb(&mut self, dst: Zmm, src: Gpd) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpbroadcastdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpbroadcastd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpbroadcastd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpbroadcastd(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vpbroadcastd(&mut self, dst: Zmm, src: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Vec, Mem> for Assembler<'_> {
    fn vpbroadcastd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpbroadcastd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpbroadcastd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpbroadcastd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Vec, Gp> for Assembler<'_> {
    fn vpbroadcastd(&mut self, dst: Vec, src: Gp) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Xmm, Gpd> for Assembler<'_> {
    fn vpbroadcastd(&mut self, dst: Xmm, src: Gpd) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Ymm, Gpd> for Assembler<'_> {
    fn vpbroadcastd(&mut self, dst: Ymm, src: Gpd) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Zmm, Gpd> for Assembler<'_> {
    fn vpbroadcastd(&mut self, dst: Zmm, src: Gpd) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vpbroadcastmb2qEmitter<Vec, KReg> for Assembler<'_> {
    fn vpbroadcastmb2q(&mut self, dst: Vec, src: KReg) {
        self.emit_n(
            InstId::Vpbroadcastmb2q as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD
    }
}

impl Vpbroadcastmb2qEmitter<Xmm, KReg> for Assembler<'_> {
    fn vpbroadcastmb2q(&mut self, dst: Xmm, src: KReg) {
        self.emit_n(
            InstId::Vpbroadcastmb2q as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD
    }
}

impl Vpbroadcastmb2qEmitter<Ymm, KReg> for Assembler<'_> {
    fn vpbroadcastmb2q(&mut self, dst: Ymm, src: KReg) {
        self.emit_n(
            InstId::Vpbroadcastmb2q as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD
    }
}

impl Vpbroadcastmb2qEmitter<Zmm, KReg> for Assembler<'_> {
    fn vpbroadcastmb2q(&mut self, dst: Zmm, src: KReg) {
        self.emit_n(
            InstId::Vpbroadcastmb2q as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD
    }
}

impl Vpbroadcastmw2dEmitter<Vec, KReg> for Assembler<'_> {
    fn vpbroadcastmw2d(&mut self, dst: Vec, src: KReg) {
        self.emit_n(
            InstId::Vpbroadcastmw2d as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD
    }
}

impl Vpbroadcastmw2dEmitter<Xmm, KReg> for Assembler<'_> {
    fn vpbroadcastmw2d(&mut self, dst: Xmm, src: KReg) {
        self.emit_n(
            InstId::Vpbroadcastmw2d as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD
    }
}

impl Vpbroadcastmw2dEmitter<Ymm, KReg> for Assembler<'_> {
    fn vpbroadcastmw2d(&mut self, dst: Ymm, src: KReg) {
        self.emit_n(
            InstId::Vpbroadcastmw2d as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD
    }
}

impl Vpbroadcastmw2dEmitter<Zmm, KReg> for Assembler<'_> {
    fn vpbroadcastmw2d(&mut self, dst: Zmm, src: KReg) {
        self.emit_n(
            InstId::Vpbroadcastmw2d as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD
    }
}

impl VpbroadcastqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpbroadcastq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpbroadcastq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpbroadcastq(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vpbroadcastq(&mut self, dst: Zmm, src: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpbroadcastq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpbroadcastq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpbroadcastq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpbroadcastq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Vec, Gp> for Assembler<'_> {
    fn vpbroadcastq(&mut self, dst: Vec, src: Gp) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Xmm, Gpq> for Assembler<'_> {
    fn vpbroadcastq(&mut self, dst: Xmm, src: Gpq) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Ymm, Gpq> for Assembler<'_> {
    fn vpbroadcastq(&mut self, dst: Ymm, src: Gpq) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Zmm, Gpq> for Assembler<'_> {
    fn vpbroadcastq(&mut self, dst: Zmm, src: Gpq) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpbroadcastwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpbroadcastw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpbroadcastw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpbroadcastw(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vpbroadcastw(&mut self, dst: Zmm, src: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Vec, Mem> for Assembler<'_> {
    fn vpbroadcastw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpbroadcastw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpbroadcastw(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpbroadcastw(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Vec, Gp> for Assembler<'_> {
    fn vpbroadcastw(&mut self, dst: Vec, src: Gp) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Xmm, Gpd> for Assembler<'_> {
    fn vpbroadcastw(&mut self, dst: Xmm, src: Gpd) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Ymm, Gpd> for Assembler<'_> {
    fn vpbroadcastw(&mut self, dst: Ymm, src: Gpd) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Zmm, Gpd> for Assembler<'_> {
    fn vpbroadcastw(&mut self, dst: Zmm, src: Gpd) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpclmulqdqEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpclmulqdq(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpclmulqdq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX VPCLMULQDQ AVX512_F
    }
}

impl<U3: Into<Imm>> VpclmulqdqEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpclmulqdq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpclmulqdq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX VPCLMULQDQ AVX512_F
    }
}

impl<U3: Into<Imm>> VpclmulqdqEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpclmulqdq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vpclmulqdq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX VPCLMULQDQ AVX512_F
    }
}

impl<U3: Into<Imm>> VpclmulqdqEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpclmulqdq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vpclmulqdq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX VPCLMULQDQ AVX512_F
    }
}

impl VpclmulqdqEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpclmulqdq(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpclmulqdq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX VPCLMULQDQ AVX512_F
    }
}

impl<U3: Into<Imm>> VpclmulqdqEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpclmulqdq(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpclmulqdq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX VPCLMULQDQ AVX512_F
    }
}

impl<U3: Into<Imm>> VpclmulqdqEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpclmulqdq(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpclmulqdq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX VPCLMULQDQ AVX512_F
    }
}

impl<U3: Into<Imm>> VpclmulqdqEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vpclmulqdq(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpclmulqdq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX VPCLMULQDQ AVX512_F
    }
}

impl VpcmpbEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpb(&mut self, dst: KReg, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpbEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcmpb(&mut self, dst: KReg, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpbEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpcmpb(&mut self, dst: KReg, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpbEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpcmpb(&mut self, dst: KReg, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl VpcmpbEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpb(&mut self, dst: KReg, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpbEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcmpb(&mut self, dst: KReg, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpbEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vpcmpb(&mut self, dst: KReg, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpbEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vpcmpb(&mut self, dst: KReg, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl VpcmpdEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpd(&mut self, dst: KReg, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpdEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcmpd(&mut self, dst: KReg, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpdEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpcmpd(&mut self, dst: KReg, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpdEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpcmpd(&mut self, dst: KReg, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpdEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpd(&mut self, dst: KReg, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpdEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcmpd(&mut self, dst: KReg, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpdEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vpcmpd(&mut self, dst: KReg, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpdEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vpcmpd(&mut self, dst: KReg, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpeqbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpcmpeqb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpeqb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpeqb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpcmpeqb(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpcmpeqb(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpcmpeqb(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqbEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vpcmpeqb(&mut self, dst: KReg, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqbEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpeqb(&mut self, dst: KReg, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqbEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpeqb(&mut self, dst: KReg, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqbEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vpcmpeqb(&mut self, dst: KReg, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqbEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vpcmpeqb(&mut self, dst: KReg, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqbEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vpcmpeqb(&mut self, dst: KReg, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqbEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vpcmpeqb(&mut self, dst: KReg, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqbEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vpcmpeqb(&mut self, dst: KReg, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpcmpeqd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpeqd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpeqd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpcmpeqd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpcmpeqd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpcmpeqd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqdEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vpcmpeqd(&mut self, dst: KReg, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpeqdEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpeqd(&mut self, dst: KReg, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpeqdEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpeqd(&mut self, dst: KReg, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpeqdEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vpcmpeqd(&mut self, dst: KReg, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpeqdEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vpcmpeqd(&mut self, dst: KReg, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpeqdEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vpcmpeqd(&mut self, dst: KReg, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpeqdEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vpcmpeqd(&mut self, dst: KReg, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpeqdEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vpcmpeqd(&mut self, dst: KReg, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpeqqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpcmpeqq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpeqq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpeqq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpcmpeqq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpcmpeqq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpcmpeqq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqqEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vpcmpeqq(&mut self, dst: KReg, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpeqqEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpeqq(&mut self, dst: KReg, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpeqqEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpeqq(&mut self, dst: KReg, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpeqqEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vpcmpeqq(&mut self, dst: KReg, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpeqqEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vpcmpeqq(&mut self, dst: KReg, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpeqqEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vpcmpeqq(&mut self, dst: KReg, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpeqqEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vpcmpeqq(&mut self, dst: KReg, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpeqqEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vpcmpeqq(&mut self, dst: KReg, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpeqwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpcmpeqw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpeqw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpeqw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpcmpeqw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpcmpeqw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpcmpeqw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqwEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vpcmpeqw(&mut self, dst: KReg, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqwEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpeqw(&mut self, dst: KReg, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqwEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpeqw(&mut self, dst: KReg, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqwEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vpcmpeqw(&mut self, dst: KReg, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqwEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vpcmpeqw(&mut self, dst: KReg, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqwEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vpcmpeqw(&mut self, dst: KReg, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqwEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vpcmpeqw(&mut self, dst: KReg, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqwEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vpcmpeqw(&mut self, dst: KReg, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpestriEmitter<Vec, Vec, Imm, Gp, Gp, Gp> for Assembler<'_> {
    fn vpcmpestri(&mut self, dst: Vec, src1: Vec, imm: Imm, dst2: Gp, src2: Gp, src3: Gp) {
        self.emit_n(
            InstId::Vpcmpestri as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                imm.as_operand(),
                dst2.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl<U2: Into<Imm>> VpcmpestriEmitter<Xmm, Xmm, U2, Gpd, Gpd, Gpd> for Assembler<'_> {
    fn vpcmpestri(&mut self, dst: Xmm, src1: Xmm, src2: U2, dst2: Gpd, src3: Gpd, src4: Gpd) {
        self.emit_n(
            InstId::Vpcmpestri as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
                dst2.as_operand(),
                src3.as_operand(),
                src4.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl VpcmpestriEmitter<Vec, Mem, Imm, Gp, Gp, Gp> for Assembler<'_> {
    fn vpcmpestri(&mut self, dst: Vec, src1: Mem, imm: Imm, dst2: Gp, src2: Gp, src3: Gp) {
        self.emit_n(
            InstId::Vpcmpestri as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                imm.as_operand(),
                dst2.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl<U2: Into<Imm>> VpcmpestriEmitter<Xmm, Mem, U2, Gpd, Gpd, Gpd> for Assembler<'_> {
    fn vpcmpestri(&mut self, dst: Xmm, src1: Mem, src2: U2, dst2: Gpd, src3: Gpd, src4: Gpd) {
        self.emit_n(
            InstId::Vpcmpestri as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
                dst2.as_operand(),
                src3.as_operand(),
                src4.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl VpcmpestrmEmitter<Vec, Vec, Imm, Vec, Gp, Gp> for Assembler<'_> {
    fn vpcmpestrm(&mut self, dst: Vec, src1: Vec, imm: Imm, dst2: Vec, src2: Gp, src3: Gp) {
        self.emit_n(
            InstId::Vpcmpestrm as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                imm.as_operand(),
                dst2.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl<U2: Into<Imm>> VpcmpestrmEmitter<Xmm, Xmm, U2, Xmm, Gpd, Gpd> for Assembler<'_> {
    fn vpcmpestrm(&mut self, dst: Xmm, src1: Xmm, src2: U2, dst2: Xmm, src3: Gpd, src4: Gpd) {
        self.emit_n(
            InstId::Vpcmpestrm as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
                dst2.as_operand(),
                src3.as_operand(),
                src4.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl VpcmpestrmEmitter<Vec, Mem, Imm, Vec, Gp, Gp> for Assembler<'_> {
    fn vpcmpestrm(&mut self, dst: Vec, src1: Mem, imm: Imm, dst2: Vec, src2: Gp, src3: Gp) {
        self.emit_n(
            InstId::Vpcmpestrm as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                imm.as_operand(),
                dst2.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl<U2: Into<Imm>> VpcmpestrmEmitter<Xmm, Mem, U2, Xmm, Gpd, Gpd> for Assembler<'_> {
    fn vpcmpestrm(&mut self, dst: Xmm, src1: Mem, src2: U2, dst2: Xmm, src3: Gpd, src4: Gpd) {
        self.emit_n(
            InstId::Vpcmpestrm as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
                dst2.as_operand(),
                src3.as_operand(),
                src4.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl VpcmpgtbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpcmpgtb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpgtb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpgtb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpcmpgtb(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpcmpgtb(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpcmpgtb(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtbEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vpcmpgtb(&mut self, dst: KReg, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtbEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpgtb(&mut self, dst: KReg, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtbEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpgtb(&mut self, dst: KReg, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtbEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vpcmpgtb(&mut self, dst: KReg, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtbEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vpcmpgtb(&mut self, dst: KReg, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtbEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vpcmpgtb(&mut self, dst: KReg, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtbEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vpcmpgtb(&mut self, dst: KReg, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtbEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vpcmpgtb(&mut self, dst: KReg, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpcmpgtd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpgtd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpgtd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpcmpgtd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpcmpgtd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpcmpgtd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtdEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vpcmpgtd(&mut self, dst: KReg, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpgtdEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpgtd(&mut self, dst: KReg, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpgtdEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpgtd(&mut self, dst: KReg, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpgtdEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vpcmpgtd(&mut self, dst: KReg, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpgtdEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vpcmpgtd(&mut self, dst: KReg, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpgtdEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vpcmpgtd(&mut self, dst: KReg, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpgtdEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vpcmpgtd(&mut self, dst: KReg, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpgtdEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vpcmpgtd(&mut self, dst: KReg, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpgtqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpcmpgtq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpgtq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpgtq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpcmpgtq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpcmpgtq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpcmpgtq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtqEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vpcmpgtq(&mut self, dst: KReg, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpgtqEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpgtq(&mut self, dst: KReg, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpgtqEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpgtq(&mut self, dst: KReg, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpgtqEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vpcmpgtq(&mut self, dst: KReg, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpgtqEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vpcmpgtq(&mut self, dst: KReg, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpgtqEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vpcmpgtq(&mut self, dst: KReg, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpgtqEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vpcmpgtq(&mut self, dst: KReg, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpgtqEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vpcmpgtq(&mut self, dst: KReg, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpgtwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpcmpgtw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpgtw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpgtw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpcmpgtw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpcmpgtw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpcmpgtw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtwEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vpcmpgtw(&mut self, dst: KReg, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtwEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpgtw(&mut self, dst: KReg, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtwEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpgtw(&mut self, dst: KReg, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtwEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vpcmpgtw(&mut self, dst: KReg, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtwEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vpcmpgtw(&mut self, dst: KReg, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtwEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vpcmpgtw(&mut self, dst: KReg, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtwEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vpcmpgtw(&mut self, dst: KReg, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtwEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vpcmpgtw(&mut self, dst: KReg, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpistriEmitter<Vec, Vec, Imm, Gp> for Assembler<'_> {
    fn vpcmpistri(&mut self, dst: Vec, src: Vec, imm: Imm, dst2: Gp) {
        self.emit_n(
            InstId::Vpcmpistri as u32,
            &[
                dst.as_operand(),
                src.as_operand(),
                imm.as_operand(),
                dst2.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl<U2: Into<Imm>> VpcmpistriEmitter<Xmm, Xmm, U2, Gpd> for Assembler<'_> {
    fn vpcmpistri(&mut self, dst: Xmm, src1: Xmm, src2: U2, dst2: Gpd) {
        self.emit_n(
            InstId::Vpcmpistri as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
                dst2.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl VpcmpistriEmitter<Vec, Mem, Imm, Gp> for Assembler<'_> {
    fn vpcmpistri(&mut self, dst: Vec, src: Mem, imm: Imm, dst2: Gp) {
        self.emit_n(
            InstId::Vpcmpistri as u32,
            &[
                dst.as_operand(),
                src.as_operand(),
                imm.as_operand(),
                dst2.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl<U2: Into<Imm>> VpcmpistriEmitter<Xmm, Mem, U2, Gpd> for Assembler<'_> {
    fn vpcmpistri(&mut self, dst: Xmm, src1: Mem, src2: U2, dst2: Gpd) {
        self.emit_n(
            InstId::Vpcmpistri as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
                dst2.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl VpcmpistrmEmitter<Vec, Vec, Imm, Vec> for Assembler<'_> {
    fn vpcmpistrm(&mut self, dst: Vec, src: Vec, imm: Imm, dst2: Vec) {
        self.emit_n(
            InstId::Vpcmpistrm as u32,
            &[
                dst.as_operand(),
                src.as_operand(),
                imm.as_operand(),
                dst2.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl<U2: Into<Imm>> VpcmpistrmEmitter<Xmm, Xmm, U2, Xmm> for Assembler<'_> {
    fn vpcmpistrm(&mut self, dst: Xmm, src1: Xmm, src2: U2, dst2: Xmm) {
        self.emit_n(
            InstId::Vpcmpistrm as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
                dst2.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl VpcmpistrmEmitter<Vec, Mem, Imm, Vec> for Assembler<'_> {
    fn vpcmpistrm(&mut self, dst: Vec, src: Mem, imm: Imm, dst2: Vec) {
        self.emit_n(
            InstId::Vpcmpistrm as u32,
            &[
                dst.as_operand(),
                src.as_operand(),
                imm.as_operand(),
                dst2.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl<U2: Into<Imm>> VpcmpistrmEmitter<Xmm, Mem, U2, Xmm> for Assembler<'_> {
    fn vpcmpistrm(&mut self, dst: Xmm, src1: Mem, src2: U2, dst2: Xmm) {
        self.emit_n(
            InstId::Vpcmpistrm as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
                dst2.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl VpcmpqEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpq(&mut self, dst: KReg, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpqEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcmpq(&mut self, dst: KReg, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpqEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpcmpq(&mut self, dst: KReg, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpqEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpcmpq(&mut self, dst: KReg, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpqEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpq(&mut self, dst: KReg, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpqEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcmpq(&mut self, dst: KReg, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpqEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vpcmpq(&mut self, dst: KReg, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpqEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vpcmpq(&mut self, dst: KReg, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpubEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpub(&mut self, dst: KReg, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpub as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpubEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcmpub(&mut self, dst: KReg, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpub as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpubEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpcmpub(&mut self, dst: KReg, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpub as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpubEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpcmpub(&mut self, dst: KReg, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpub as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl VpcmpubEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpub(&mut self, dst: KReg, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpub as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpubEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcmpub(&mut self, dst: KReg, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpub as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpubEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vpcmpub(&mut self, dst: KReg, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpub as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpubEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vpcmpub(&mut self, dst: KReg, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpub as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl VpcmpudEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpud(&mut self, dst: KReg, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpud as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpudEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcmpud(&mut self, dst: KReg, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpud as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpudEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpcmpud(&mut self, dst: KReg, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpud as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpudEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpcmpud(&mut self, dst: KReg, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpud as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpudEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpud(&mut self, dst: KReg, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpud as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpudEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcmpud(&mut self, dst: KReg, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpud as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpudEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vpcmpud(&mut self, dst: KReg, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpud as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpudEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vpcmpud(&mut self, dst: KReg, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpud as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpuqEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpuq(&mut self, dst: KReg, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpuq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuqEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcmpuq(&mut self, dst: KReg, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpuq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuqEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpcmpuq(&mut self, dst: KReg, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpuq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuqEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpcmpuq(&mut self, dst: KReg, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpuq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpuqEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpuq(&mut self, dst: KReg, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpuq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuqEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcmpuq(&mut self, dst: KReg, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpuq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuqEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vpcmpuq(&mut self, dst: KReg, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpuq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuqEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vpcmpuq(&mut self, dst: KReg, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpuq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpuwEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpuw(&mut self, dst: KReg, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpuw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuwEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcmpuw(&mut self, dst: KReg, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpuw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuwEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpcmpuw(&mut self, dst: KReg, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpuw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuwEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpcmpuw(&mut self, dst: KReg, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpuw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl VpcmpuwEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpuw(&mut self, dst: KReg, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpuw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuwEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcmpuw(&mut self, dst: KReg, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpuw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuwEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vpcmpuw(&mut self, dst: KReg, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpuw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuwEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vpcmpuw(&mut self, dst: KReg, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpuw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl VpcmpwEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpw(&mut self, dst: KReg, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpwEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcmpw(&mut self, dst: KReg, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpwEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpcmpw(&mut self, dst: KReg, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpwEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpcmpw(&mut self, dst: KReg, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vpcmpw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl VpcmpwEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpw(&mut self, dst: KReg, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpwEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcmpw(&mut self, dst: KReg, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpwEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vpcmpw(&mut self, dst: KReg, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpwEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vpcmpw(&mut self, dst: KReg, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcmpw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl VpcompressbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpcompressb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpcompressb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompressbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpcompressb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpcompressb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompressbEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpcompressb(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vpcompressb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompressbEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpcompressb(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vpcompressb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompressbEmitter<Mem, Vec> for Assembler<'_> {
    fn vpcompressb(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpcompressb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompressbEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpcompressb(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpcompressb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompressbEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpcompressb(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpcompressb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompressbEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpcompressb(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpcompressb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompressdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpcompressd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpcompressd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpcompressd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpcompressd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpcompressd(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vpcompressd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpcompressd(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vpcompressd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressdEmitter<Mem, Vec> for Assembler<'_> {
    fn vpcompressd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpcompressd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpcompressd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpcompressd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpcompressd(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpcompressd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressdEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpcompressd(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpcompressd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpcompressq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpcompressq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpcompressq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpcompressq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpcompressq(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vpcompressq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpcompressq(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vpcompressq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressqEmitter<Mem, Vec> for Assembler<'_> {
    fn vpcompressq(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpcompressq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressqEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpcompressq(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpcompressq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressqEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpcompressq(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpcompressq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressqEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpcompressq(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpcompressq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompresswEmitter<Vec, Vec> for Assembler<'_> {
    fn vpcompressw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpcompressw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompresswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpcompressw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpcompressw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompresswEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpcompressw(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vpcompressw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompresswEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpcompressw(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vpcompressw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompresswEmitter<Mem, Vec> for Assembler<'_> {
    fn vpcompressw(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpcompressw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompresswEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpcompressw(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpcompressw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompresswEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpcompressw(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpcompressw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompresswEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpcompressw(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpcompressw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpconflictdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpconflictd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpconflictd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpconflictd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpconflictd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpconflictd(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vpconflictd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpconflictd(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vpconflictd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictdEmitter<Vec, Mem> for Assembler<'_> {
    fn vpconflictd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpconflictd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpconflictd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpconflictd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpconflictd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpconflictd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpconflictd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpconflictd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpconflictq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpconflictq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpconflictq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpconflictq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpconflictq(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vpconflictq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpconflictq(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vpconflictq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpconflictq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpconflictq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpconflictq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpconflictq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpconflictq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpconflictq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpconflictq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpconflictq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpdpbusdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpbusd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpdpbusd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpbusd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpdpbusd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpbusd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpdpbusd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpdpbusd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpdpbusd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpbusd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbusd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpbusd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbusd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpbusd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbusd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpdpbusd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbusd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpbusds(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpdpbusds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpbusds(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpdpbusds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpbusds(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpdpbusds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpdpbusds(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpdpbusds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpbusds(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbusds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpbusds(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbusds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpbusds(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbusds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpdpbusds(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbusds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpwssd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpdpwssd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpwssd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpdpwssd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpwssd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpdpwssd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpdpwssd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpdpwssd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpwssd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwssd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpwssd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwssd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpwssd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwssd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpdpwssd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwssd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpwssds(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpdpwssds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpwssds(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpdpwssds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpwssds(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpdpwssds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpdpwssds(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpdpwssds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpwssds(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwssds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpwssds(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwssds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpwssds(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwssds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpdpwssds(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwssds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl Vperm2f128Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vperm2f128(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vperm2f128 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> Vperm2f128Emitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vperm2f128(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vperm2f128 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl Vperm2f128Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vperm2f128(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vperm2f128 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> Vperm2f128Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vperm2f128(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vperm2f128 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl Vperm2i128Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vperm2i128(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vperm2i128 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX2
    }
}

impl<U3: Into<Imm>> Vperm2i128Emitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vperm2i128(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vperm2i128 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX2
    }
}

impl Vperm2i128Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vperm2i128(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vperm2i128 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX2
    }
}

impl<U3: Into<Imm>> Vperm2i128Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vperm2i128(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vperm2i128 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX2
    }
}

impl VpermbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpermb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl VpermbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpermb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl VpermbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpermb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl VpermbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermb(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpermb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl VpermbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermb(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpermb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl VpermbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermb(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl VpermbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermb(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpermb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl VpermbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermb(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl VpermdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpermd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpermd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpermd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpermd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpermd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl Vpermi2bEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermi2b(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpermi2b as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermi2bEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermi2b(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpermi2b as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermi2bEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermi2b(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpermi2b as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermi2bEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermi2b(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpermi2b as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermi2bEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermi2b(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2b as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermi2bEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermi2b(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2b as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermi2bEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermi2b(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2b as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermi2bEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermi2b(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2b as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermi2dEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermi2d(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpermi2d as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2dEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermi2d(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpermi2d as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2dEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermi2d(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpermi2d as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2dEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermi2d(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpermi2d as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2dEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermi2d(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2d as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2dEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermi2d(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2d as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2dEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermi2d(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2d as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2dEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermi2d(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2d as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermi2pd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpermi2pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermi2pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpermi2pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermi2pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpermi2pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermi2pd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpermi2pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermi2pd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermi2pd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermi2pd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermi2pd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermi2ps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpermi2ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermi2ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpermi2ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermi2ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpermi2ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermi2ps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpermi2ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermi2ps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermi2ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermi2ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermi2ps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2qEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermi2q(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpermi2q as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2qEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermi2q(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpermi2q as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2qEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermi2q(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpermi2q as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2qEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermi2q(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpermi2q as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2qEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermi2q(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2q as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2qEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermi2q(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2q as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2qEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermi2q(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2q as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2qEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermi2q(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2q as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2wEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermi2w(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpermi2w as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermi2wEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermi2w(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpermi2w as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermi2wEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermi2w(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpermi2w as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermi2wEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermi2w(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpermi2w as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermi2wEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermi2w(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2w as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermi2wEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermi2w(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2w as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermi2wEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermi2w(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2w as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermi2wEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermi2w(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermi2w as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpermilpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermilpd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermilpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermilpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermilpd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermilpd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermilpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermilpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermilpd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpermilpd(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpermilpd(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpdEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpermilpd(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpdEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpermilpd(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpermilpd(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpermilpd(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpdEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpermilpd(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpdEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpermilpd(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermilps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermilps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermilps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermilps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermilps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermilps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermilps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermilps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpermilps(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpermilps(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpsEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpermilps(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpsEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpermilps(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpermilps(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpermilps(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpsEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpermilps(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpsEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpermilps(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermpdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpermpd(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX2
    }
}

impl<U2: Into<Imm>> VpermpdEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpermpd(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX2
    }
}

impl<U2: Into<Imm>> VpermpdEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpermpd(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX2
    }
}

impl VpermpdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpermpd(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX2
    }
}

impl<U2: Into<Imm>> VpermpdEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpermpd(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX2
    }
}

impl<U2: Into<Imm>> VpermpdEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpermpd(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX2
    }
}

impl VpermpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermpd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermpd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermpd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermpd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpermps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpermps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpermps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpermps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpermps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermqEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpermq(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermqEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpermq(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermqEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpermq(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpermqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpermq(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermqEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpermq(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermqEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpermq(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpermqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2bEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermt2b(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpermt2b as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermt2bEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermt2b(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpermt2b as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermt2bEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermt2b(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpermt2b as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermt2bEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermt2b(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpermt2b as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermt2bEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermt2b(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2b as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermt2bEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermt2b(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2b as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermt2bEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermt2b(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2b as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermt2bEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermt2b(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2b as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermt2dEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermt2d(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpermt2d as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2dEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermt2d(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpermt2d as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2dEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermt2d(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpermt2d as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2dEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermt2d(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpermt2d as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2dEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermt2d(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2d as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2dEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermt2d(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2d as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2dEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermt2d(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2d as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2dEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermt2d(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2d as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermt2pd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpermt2pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermt2pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpermt2pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermt2pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpermt2pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermt2pd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpermt2pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermt2pd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermt2pd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermt2pd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermt2pd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2pd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermt2ps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpermt2ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermt2ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpermt2ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermt2ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpermt2ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermt2ps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpermt2ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermt2ps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermt2ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermt2ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermt2ps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2qEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermt2q(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpermt2q as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2qEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermt2q(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpermt2q as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2qEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermt2q(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpermt2q as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2qEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermt2q(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpermt2q as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2qEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermt2q(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2q as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2qEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermt2q(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2q as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2qEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermt2q(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2q as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2qEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermt2q(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2q as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2wEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermt2w(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpermt2w as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermt2wEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermt2w(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpermt2w as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermt2wEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermt2w(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpermt2w as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermt2wEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermt2w(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpermt2w as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermt2wEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermt2w(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2w as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermt2wEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermt2w(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2w as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermt2wEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermt2w(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2w as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermt2wEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermt2w(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermt2w as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpermwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpermw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpermwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpermw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpermwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpermw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpermwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpermw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpermwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpermw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpermwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpermwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpermw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpermwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpermw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpexpandbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpexpandb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpexpandb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpexpandb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpexpandb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandbEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpexpandb(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vpexpandb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandbEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpexpandb(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vpexpandb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandbEmitter<Vec, Mem> for Assembler<'_> {
    fn vpexpandb(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpexpandb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandbEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpexpandb(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpexpandb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandbEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpexpandb(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpexpandb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandbEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpexpandb(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpexpandb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpanddEmitter<Vec, Vec> for Assembler<'_> {
    fn vpexpandd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpexpandd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpanddEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpexpandd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpexpandd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpanddEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpexpandd(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vpexpandd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpanddEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpexpandd(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vpexpandd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpanddEmitter<Vec, Mem> for Assembler<'_> {
    fn vpexpandd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpexpandd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpanddEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpexpandd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpexpandd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpanddEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpexpandd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpexpandd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpanddEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpexpandd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpexpandd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpandqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpexpandq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpexpandq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpandqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpexpandq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpexpandq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpandqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpexpandq(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vpexpandq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpandqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpexpandq(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vpexpandq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpandqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpexpandq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpexpandq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpandqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpexpandq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpexpandq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpandqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpexpandq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpexpandq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpandqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpexpandq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpexpandq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpandwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpexpandw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpexpandw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpexpandw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpexpandw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandwEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpexpandw(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vpexpandw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandwEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpexpandw(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vpexpandw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandwEmitter<Vec, Mem> for Assembler<'_> {
    fn vpexpandw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpexpandw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpexpandw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpexpandw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandwEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpexpandw(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpexpandw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandwEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpexpandw(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpexpandw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpextrbEmitter<Gp, Vec, Imm> for Assembler<'_> {
    fn vpextrb(&mut self, dst: Gp, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpextrb as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX AVX512_BW
    }
}

impl<U2: Into<Imm>> VpextrbEmitter<Gpd, Xmm, U2> for Assembler<'_> {
    fn vpextrb(&mut self, dst: Gpd, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpextrb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_BW
    }
}

impl VpextrbEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vpextrb(&mut self, dst: Mem, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpextrb as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX AVX512_BW
    }
}

impl<U2: Into<Imm>> VpextrbEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn vpextrb(&mut self, dst: Mem, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpextrb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_BW
    }
}

impl VpextrdEmitter<Gp, Vec, Imm> for Assembler<'_> {
    fn vpextrd(&mut self, dst: Gp, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpextrd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX AVX512_DQ
    }
}

impl<U2: Into<Imm>> VpextrdEmitter<Gpd, Xmm, U2> for Assembler<'_> {
    fn vpextrd(&mut self, dst: Gpd, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpextrd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_DQ
    }
}

impl VpextrdEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vpextrd(&mut self, dst: Mem, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpextrd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX AVX512_DQ
    }
}

impl<U2: Into<Imm>> VpextrdEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn vpextrd(&mut self, dst: Mem, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpextrd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_DQ
    }
}

impl VpextrqEmitter<Gp, Vec, Imm> for Assembler<'_> {
    fn vpextrq(&mut self, dst: Gp, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpextrq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX AVX512_DQ
    }
}

impl<U2: Into<Imm>> VpextrqEmitter<Gpq, Xmm, U2> for Assembler<'_> {
    fn vpextrq(&mut self, dst: Gpq, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpextrq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_DQ
    }
}

impl VpextrqEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vpextrq(&mut self, dst: Mem, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpextrq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX AVX512_DQ
    }
}

impl<U2: Into<Imm>> VpextrqEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn vpextrq(&mut self, dst: Mem, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpextrq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_DQ
    }
}

impl VpextrwEmitter<Gp, Vec, Imm> for Assembler<'_> {
    fn vpextrw(&mut self, dst: Gp, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpextrw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX AVX512_BW
    }
}

impl<U2: Into<Imm>> VpextrwEmitter<Gpd, Xmm, U2> for Assembler<'_> {
    fn vpextrw(&mut self, dst: Gpd, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpextrw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_BW
    }
}

impl VpextrwEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vpextrw(&mut self, dst: Mem, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpextrw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX AVX512_BW
    }
}

impl<U2: Into<Imm>> VpextrwEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn vpextrw(&mut self, dst: Mem, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpextrw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_BW
    }
}

impl VpgatherddEmitter<Vec, Mem> for Assembler<'_> {
    fn vpgatherdd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpgatherdd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherddEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpgatherdd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpgatherdd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherddEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpgatherdd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpgatherdd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherddEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpgatherdd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpgatherdd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl Vpgatherdd3Emitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpgatherdd_3(&mut self, dst: Vec, src: Mem, dst2: Vec) {
        self.emit_n(
            InstId::Vpgatherdd as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl Vpgatherdd3Emitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpgatherdd_3(&mut self, dst: Xmm, src: Mem, dst2: Xmm) {
        self.emit_n(
            InstId::Vpgatherdd as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl Vpgatherdd3Emitter<Ymm, Mem, Ymm> for Assembler<'_> {
    fn vpgatherdd_3(&mut self, dst: Ymm, src: Mem, dst2: Ymm) {
        self.emit_n(
            InstId::Vpgatherdd as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl VpgatherdqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpgatherdq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpgatherdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherdqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpgatherdq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpgatherdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherdqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpgatherdq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpgatherdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherdqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpgatherdq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpgatherdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl Vpgatherdq3Emitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpgatherdq_3(&mut self, dst: Vec, src: Mem, dst2: Vec) {
        self.emit_n(
            InstId::Vpgatherdq as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl Vpgatherdq3Emitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpgatherdq_3(&mut self, dst: Xmm, src: Mem, dst2: Xmm) {
        self.emit_n(
            InstId::Vpgatherdq as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl Vpgatherdq3Emitter<Ymm, Mem, Ymm> for Assembler<'_> {
    fn vpgatherdq_3(&mut self, dst: Ymm, src: Mem, dst2: Ymm) {
        self.emit_n(
            InstId::Vpgatherdq as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl VpgatherqdEmitter<Vec, Mem> for Assembler<'_> {
    fn vpgatherqd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpgatherqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherqdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpgatherqd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpgatherqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherqdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpgatherqd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpgatherqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl Vpgatherqd3Emitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpgatherqd_3(&mut self, dst: Vec, src: Mem, dst2: Vec) {
        self.emit_n(
            InstId::Vpgatherqd as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl Vpgatherqd3Emitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpgatherqd_3(&mut self, dst: Xmm, src: Mem, dst2: Xmm) {
        self.emit_n(
            InstId::Vpgatherqd as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl VpgatherqqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpgatherqq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpgatherqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherqqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpgatherqq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpgatherqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherqqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpgatherqq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpgatherqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherqqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpgatherqq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpgatherqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl Vpgatherqq3Emitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpgatherqq_3(&mut self, dst: Vec, src: Mem, dst2: Vec) {
        self.emit_n(
            InstId::Vpgatherqq as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl Vpgatherqq3Emitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpgatherqq_3(&mut self, dst: Xmm, src: Mem, dst2: Xmm) {
        self.emit_n(
            InstId::Vpgatherqq as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl Vpgatherqq3Emitter<Ymm, Mem, Ymm> for Assembler<'_> {
    fn vpgatherqq_3(&mut self, dst: Ymm, src: Mem, dst2: Ymm) {
        self.emit_n(
            InstId::Vpgatherqq as u32,
            &[dst.as_operand(), src.as_operand(), dst2.as_operand()],
        ); // AVX2
    }
}

impl VphadddEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vphaddd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vphaddd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphadddEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vphaddd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vphaddd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphadddEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vphaddd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vphaddd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphadddEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vphaddd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vphaddd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphadddEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vphaddd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vphaddd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphadddEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vphaddd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vphaddd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphaddswEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vphaddsw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vphaddsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphaddswEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vphaddsw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vphaddsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphaddswEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vphaddsw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vphaddsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphaddswEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vphaddsw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vphaddsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphaddswEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vphaddsw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vphaddsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphaddswEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vphaddsw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vphaddsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphaddwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vphaddw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vphaddw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphaddwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vphaddw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vphaddw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphaddwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vphaddw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vphaddw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphaddwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vphaddw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vphaddw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphaddwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vphaddw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vphaddw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphaddwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vphaddw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vphaddw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphminposuwEmitter<Vec, Vec> for Assembler<'_> {
    fn vphminposuw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vphminposuw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VphminposuwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphminposuw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vphminposuw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VphminposuwEmitter<Vec, Mem> for Assembler<'_> {
    fn vphminposuw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vphminposuw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VphminposuwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphminposuw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vphminposuw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VphsubdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vphsubd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vphsubd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphsubdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vphsubd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vphsubd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphsubdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vphsubd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vphsubd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphsubdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vphsubd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vphsubd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphsubdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vphsubd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vphsubd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphsubdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vphsubd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vphsubd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphsubswEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vphsubsw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vphsubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphsubswEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vphsubsw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vphsubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphsubswEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vphsubsw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vphsubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphsubswEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vphsubsw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vphsubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphsubswEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vphsubsw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vphsubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphsubswEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vphsubsw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vphsubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphsubwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vphsubw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vphsubw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphsubwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vphsubw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vphsubw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphsubwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vphsubw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vphsubw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphsubwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vphsubw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vphsubw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphsubwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vphsubw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vphsubw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VphsubwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vphsubw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vphsubw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpinsrbEmitter<Vec, Vec, Gp, Imm> for Assembler<'_> {
    fn vpinsrb(&mut self, dst: Vec, src1: Vec, src2: Gp, imm: Imm) {
        self.emit_n(
            InstId::Vpinsrb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VpinsrbEmitter<Xmm, Xmm, Gpd, U3> for Assembler<'_> {
    fn vpinsrb(&mut self, dst: Xmm, src1: Xmm, src2: Gpd, src3: U3) {
        self.emit_n(
            InstId::Vpinsrb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpinsrbEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpinsrb(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpinsrb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VpinsrbEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpinsrb(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpinsrb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpinsrdEmitter<Vec, Vec, Gp, Imm> for Assembler<'_> {
    fn vpinsrd(&mut self, dst: Vec, src1: Vec, src2: Gp, imm: Imm) {
        self.emit_n(
            InstId::Vpinsrd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> VpinsrdEmitter<Xmm, Xmm, Gpd, U3> for Assembler<'_> {
    fn vpinsrd(&mut self, dst: Xmm, src1: Xmm, src2: Gpd, src3: U3) {
        self.emit_n(
            InstId::Vpinsrd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_DQ{kz}
    }
}

impl VpinsrdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpinsrd(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpinsrd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> VpinsrdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpinsrd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpinsrd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_DQ{kz}
    }
}

impl VpinsrqEmitter<Vec, Vec, Gp, Imm> for Assembler<'_> {
    fn vpinsrq(&mut self, dst: Vec, src1: Vec, src2: Gp, imm: Imm) {
        self.emit_n(
            InstId::Vpinsrq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> VpinsrqEmitter<Xmm, Xmm, Gpq, U3> for Assembler<'_> {
    fn vpinsrq(&mut self, dst: Xmm, src1: Xmm, src2: Gpq, src3: U3) {
        self.emit_n(
            InstId::Vpinsrq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_DQ{kz}
    }
}

impl VpinsrqEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpinsrq(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpinsrq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> VpinsrqEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpinsrq(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpinsrq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_DQ{kz}
    }
}

impl VpinsrwEmitter<Vec, Vec, Gp, Imm> for Assembler<'_> {
    fn vpinsrw(&mut self, dst: Vec, src1: Vec, src2: Gp, imm: Imm) {
        self.emit_n(
            InstId::Vpinsrw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VpinsrwEmitter<Xmm, Xmm, Gpd, U3> for Assembler<'_> {
    fn vpinsrw(&mut self, dst: Xmm, src1: Xmm, src2: Gpd, src3: U3) {
        self.emit_n(
            InstId::Vpinsrw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpinsrwEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpinsrw(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpinsrw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VpinsrwEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpinsrw(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpinsrw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_BW{kz}
    }
}

impl VplzcntdEmitter<Vec, Vec> for Assembler<'_> {
    fn vplzcntd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vplzcntd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VplzcntdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vplzcntd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vplzcntd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VplzcntdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vplzcntd(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vplzcntd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VplzcntdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vplzcntd(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vplzcntd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VplzcntdEmitter<Vec, Mem> for Assembler<'_> {
    fn vplzcntd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vplzcntd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VplzcntdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vplzcntd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vplzcntd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VplzcntdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vplzcntd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vplzcntd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VplzcntdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vplzcntd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vplzcntd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VplzcntqEmitter<Vec, Vec> for Assembler<'_> {
    fn vplzcntq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vplzcntq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b64}
    }
}

impl VplzcntqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vplzcntq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vplzcntq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b64}
    }
}

impl VplzcntqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vplzcntq(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vplzcntq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b64}
    }
}

impl VplzcntqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vplzcntq(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vplzcntq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b64}
    }
}

impl VplzcntqEmitter<Vec, Mem> for Assembler<'_> {
    fn vplzcntq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vplzcntq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b64}
    }
}

impl VplzcntqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vplzcntq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vplzcntq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b64}
    }
}

impl VplzcntqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vplzcntq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vplzcntq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b64}
    }
}

impl VplzcntqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vplzcntq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vplzcntq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_CD{kz|b64}
    }
}

impl Vpmadd52huqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmadd52huq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmadd52huq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52huqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmadd52huq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmadd52huq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52huqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmadd52huq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmadd52huq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52huqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmadd52huq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpmadd52huq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52huqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmadd52huq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmadd52huq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52huqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmadd52huq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmadd52huq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52huqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmadd52huq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmadd52huq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52huqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmadd52huq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmadd52huq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52luqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmadd52luq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmadd52luq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52luqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmadd52luq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmadd52luq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52luqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmadd52luq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmadd52luq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52luqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmadd52luq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpmadd52luq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52luqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmadd52luq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmadd52luq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52luqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmadd52luq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmadd52luq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52luqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmadd52luq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmadd52luq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52luqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmadd52luq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmadd52luq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl VpmaddubswEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmaddubsw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmaddubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddubswEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmaddubsw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmaddubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddubswEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmaddubsw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmaddubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddubswEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmaddubsw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpmaddubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddubswEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaddubsw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmaddubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddubswEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaddubsw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaddubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddubswEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaddubsw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaddubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddubswEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmaddubsw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaddubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddwdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmaddwd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmaddwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddwdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmaddwd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmaddwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddwdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmaddwd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmaddwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddwdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmaddwd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpmaddwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddwdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaddwd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmaddwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddwdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaddwd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaddwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddwdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaddwd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaddwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddwdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmaddwd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaddwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaskmovdEmitter<Mem, Vec, Vec> for Assembler<'_> {
    fn vpmaskmovd(&mut self, dst: Mem, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmaskmovd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovdEmitter<Mem, Xmm, Xmm> for Assembler<'_> {
    fn vpmaskmovd(&mut self, dst: Mem, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmaskmovd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovdEmitter<Mem, Ymm, Ymm> for Assembler<'_> {
    fn vpmaskmovd(&mut self, dst: Mem, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmaskmovd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaskmovd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmaskmovd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaskmovd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaskmovd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaskmovd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaskmovd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovqEmitter<Mem, Vec, Vec> for Assembler<'_> {
    fn vpmaskmovq(&mut self, dst: Mem, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmaskmovq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovqEmitter<Mem, Xmm, Xmm> for Assembler<'_> {
    fn vpmaskmovq(&mut self, dst: Mem, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmaskmovq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovqEmitter<Mem, Ymm, Ymm> for Assembler<'_> {
    fn vpmaskmovq(&mut self, dst: Mem, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmaskmovq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaskmovq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmaskmovq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaskmovq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaskmovq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaskmovq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaskmovq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2
    }
}

impl VpmaxsbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmaxsb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmaxsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxsbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmaxsb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmaxsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxsbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmaxsb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmaxsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxsbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmaxsb(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpmaxsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxsbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaxsb(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxsbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaxsb(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxsbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaxsb(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxsbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmaxsb(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmaxsd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmaxsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmaxsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmaxsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxsdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmaxsd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmaxsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxsdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmaxsd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpmaxsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaxsd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaxsd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxsdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaxsd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxsdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmaxsd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxsqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmaxsq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmaxsq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxsqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmaxsq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmaxsq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxsqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmaxsq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmaxsq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxsqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmaxsq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpmaxsq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxsqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaxsq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxsq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxsqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaxsq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxsq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxsqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaxsq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxsq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxsqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmaxsq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxsq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxswEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmaxsw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmaxsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxswEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmaxsw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmaxsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxswEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmaxsw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmaxsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxswEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmaxsw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpmaxsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxswEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaxsw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxswEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaxsw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxswEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaxsw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxswEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmaxsw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxubEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmaxub(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmaxub as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxubEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmaxub(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmaxub as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxubEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmaxub(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmaxub as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxubEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmaxub(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpmaxub as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxubEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaxub(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxub as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxubEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaxub(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxub as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxubEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaxub(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxub as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxubEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmaxub(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxub as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxudEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmaxud(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmaxud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxudEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmaxud(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmaxud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxudEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmaxud(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmaxud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxudEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmaxud(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpmaxud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxudEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaxud(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxudEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaxud(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxudEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaxud(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxudEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmaxud(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxuqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmaxuq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmaxuq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxuqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmaxuq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmaxuq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxuqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmaxuq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmaxuq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxuqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmaxuq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpmaxuq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxuqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaxuq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxuq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxuqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaxuq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxuq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxuqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaxuq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxuq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxuqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmaxuq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxuq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxuwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmaxuw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmaxuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxuwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmaxuw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmaxuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxuwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmaxuw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmaxuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxuwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmaxuw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpmaxuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxuwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaxuw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxuwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaxuw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxuwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaxuw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxuwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmaxuw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmaxuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminsbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpminsb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpminsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminsbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpminsb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpminsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminsbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpminsb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpminsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminsbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpminsb(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpminsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminsbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpminsb(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpminsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminsbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpminsb(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpminsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminsbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpminsb(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpminsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminsbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpminsb(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpminsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpminsd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpminsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpminsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpminsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminsdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpminsd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpminsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminsdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpminsd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpminsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpminsd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpminsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpminsd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpminsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminsdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpminsd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpminsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminsdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpminsd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpminsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminsqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpminsq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpminsq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminsqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpminsq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpminsq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminsqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpminsq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpminsq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminsqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpminsq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpminsq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminsqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpminsq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpminsq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminsqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpminsq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpminsq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminsqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpminsq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpminsq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminsqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpminsq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpminsq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminswEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpminsw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpminsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminswEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpminsw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpminsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminswEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpminsw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpminsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminswEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpminsw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpminsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminswEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpminsw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpminsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminswEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpminsw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpminsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminswEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpminsw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpminsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminswEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpminsw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpminsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminubEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpminub(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpminub as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminubEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpminub(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpminub as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminubEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpminub(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpminub as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminubEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpminub(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpminub as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminubEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpminub(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpminub as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminubEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpminub(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpminub as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminubEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpminub(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpminub as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminubEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpminub(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpminub as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminudEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpminud(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpminud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminudEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpminud(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpminud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminudEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpminud(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpminud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminudEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpminud(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpminud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminudEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpminud(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpminud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminudEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpminud(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpminud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminudEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpminud(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpminud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminudEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpminud(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpminud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminuqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpminuq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpminuq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminuqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpminuq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpminuq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminuqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpminuq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpminuq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminuqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpminuq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpminuq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminuqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpminuq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpminuq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminuqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpminuq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpminuq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminuqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpminuq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpminuq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminuqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpminuq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpminuq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminuwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpminuw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpminuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminuwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpminuw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpminuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminuwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpminuw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpminuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminuwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpminuw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpminuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminuwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpminuw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpminuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminuwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpminuw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpminuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminuwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpminuw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpminuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminuwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpminuw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpminuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl Vpmovb2mEmitter<KReg, Vec> for Assembler<'_> {
    fn vpmovb2m(&mut self, dst: KReg, src: Vec) {
        self.emit_n(
            InstId::Vpmovb2m as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovb2mEmitter<KReg, Xmm> for Assembler<'_> {
    fn vpmovb2m(&mut self, dst: KReg, src: Xmm) {
        self.emit_n(
            InstId::Vpmovb2m as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovb2mEmitter<KReg, Ymm> for Assembler<'_> {
    fn vpmovb2m(&mut self, dst: KReg, src: Ymm) {
        self.emit_n(
            InstId::Vpmovb2m as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovb2mEmitter<KReg, Zmm> for Assembler<'_> {
    fn vpmovb2m(&mut self, dst: KReg, src: Zmm) {
        self.emit_n(
            InstId::Vpmovb2m as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovd2mEmitter<KReg, Vec> for Assembler<'_> {
    fn vpmovd2m(&mut self, dst: KReg, src: Vec) {
        self.emit_n(
            InstId::Vpmovd2m as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovd2mEmitter<KReg, Xmm> for Assembler<'_> {
    fn vpmovd2m(&mut self, dst: KReg, src: Xmm) {
        self.emit_n(
            InstId::Vpmovd2m as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovd2mEmitter<KReg, Ymm> for Assembler<'_> {
    fn vpmovd2m(&mut self, dst: KReg, src: Ymm) {
        self.emit_n(
            InstId::Vpmovd2m as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovd2mEmitter<KReg, Zmm> for Assembler<'_> {
    fn vpmovd2m(&mut self, dst: KReg, src: Zmm) {
        self.emit_n(
            InstId::Vpmovd2m as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ
    }
}

impl VpmovdbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovdb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovdb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdbEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovdb(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdbEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vpmovdb(&mut self, dst: Xmm, src: Zmm) {
        self.emit_n(
            InstId::Vpmovdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdbEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovdb(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpmovdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdbEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovdb(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpmovdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdbEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovdb(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpmovdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdbEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovdb(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpmovdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovdw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovdw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdwEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovdw(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdwEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vpmovdw(&mut self, dst: Ymm, src: Zmm) {
        self.emit_n(
            InstId::Vpmovdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdwEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovdw(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpmovdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdwEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovdw(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpmovdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdwEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovdw(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpmovdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdwEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovdw(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpmovdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vpmovm2bEmitter<Vec, KReg> for Assembler<'_> {
    fn vpmovm2b(&mut self, dst: Vec, src: KReg) {
        self.emit_n(
            InstId::Vpmovm2b as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovm2bEmitter<Xmm, KReg> for Assembler<'_> {
    fn vpmovm2b(&mut self, dst: Xmm, src: KReg) {
        self.emit_n(
            InstId::Vpmovm2b as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovm2bEmitter<Ymm, KReg> for Assembler<'_> {
    fn vpmovm2b(&mut self, dst: Ymm, src: KReg) {
        self.emit_n(
            InstId::Vpmovm2b as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovm2bEmitter<Zmm, KReg> for Assembler<'_> {
    fn vpmovm2b(&mut self, dst: Zmm, src: KReg) {
        self.emit_n(
            InstId::Vpmovm2b as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovm2dEmitter<Vec, KReg> for Assembler<'_> {
    fn vpmovm2d(&mut self, dst: Vec, src: KReg) {
        self.emit_n(
            InstId::Vpmovm2d as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovm2dEmitter<Xmm, KReg> for Assembler<'_> {
    fn vpmovm2d(&mut self, dst: Xmm, src: KReg) {
        self.emit_n(
            InstId::Vpmovm2d as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovm2dEmitter<Ymm, KReg> for Assembler<'_> {
    fn vpmovm2d(&mut self, dst: Ymm, src: KReg) {
        self.emit_n(
            InstId::Vpmovm2d as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovm2dEmitter<Zmm, KReg> for Assembler<'_> {
    fn vpmovm2d(&mut self, dst: Zmm, src: KReg) {
        self.emit_n(
            InstId::Vpmovm2d as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovm2qEmitter<Vec, KReg> for Assembler<'_> {
    fn vpmovm2q(&mut self, dst: Vec, src: KReg) {
        self.emit_n(
            InstId::Vpmovm2q as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovm2qEmitter<Xmm, KReg> for Assembler<'_> {
    fn vpmovm2q(&mut self, dst: Xmm, src: KReg) {
        self.emit_n(
            InstId::Vpmovm2q as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovm2qEmitter<Ymm, KReg> for Assembler<'_> {
    fn vpmovm2q(&mut self, dst: Ymm, src: KReg) {
        self.emit_n(
            InstId::Vpmovm2q as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovm2qEmitter<Zmm, KReg> for Assembler<'_> {
    fn vpmovm2q(&mut self, dst: Zmm, src: KReg) {
        self.emit_n(
            InstId::Vpmovm2q as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovm2wEmitter<Vec, KReg> for Assembler<'_> {
    fn vpmovm2w(&mut self, dst: Vec, src: KReg) {
        self.emit_n(
            InstId::Vpmovm2w as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovm2wEmitter<Xmm, KReg> for Assembler<'_> {
    fn vpmovm2w(&mut self, dst: Xmm, src: KReg) {
        self.emit_n(
            InstId::Vpmovm2w as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovm2wEmitter<Ymm, KReg> for Assembler<'_> {
    fn vpmovm2w(&mut self, dst: Ymm, src: KReg) {
        self.emit_n(
            InstId::Vpmovm2w as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovm2wEmitter<Zmm, KReg> for Assembler<'_> {
    fn vpmovm2w(&mut self, dst: Zmm, src: KReg) {
        self.emit_n(
            InstId::Vpmovm2w as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW
    }
}

impl VpmovmskbEmitter<Gp, Vec> for Assembler<'_> {
    fn vpmovmskb(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Vpmovmskb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+
    }
}

impl VpmovmskbEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vpmovmskb(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Vpmovmskb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+
    }
}

impl VpmovmskbEmitter<Gpd, Ymm> for Assembler<'_> {
    fn vpmovmskb(&mut self, dst: Gpd, src: Ymm) {
        self.emit_n(
            InstId::Vpmovmskb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+
    }
}

impl Vpmovq2mEmitter<KReg, Vec> for Assembler<'_> {
    fn vpmovq2m(&mut self, dst: KReg, src: Vec) {
        self.emit_n(
            InstId::Vpmovq2m as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovq2mEmitter<KReg, Xmm> for Assembler<'_> {
    fn vpmovq2m(&mut self, dst: KReg, src: Xmm) {
        self.emit_n(
            InstId::Vpmovq2m as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovq2mEmitter<KReg, Ymm> for Assembler<'_> {
    fn vpmovq2m(&mut self, dst: KReg, src: Ymm) {
        self.emit_n(
            InstId::Vpmovq2m as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovq2mEmitter<KReg, Zmm> for Assembler<'_> {
    fn vpmovq2m(&mut self, dst: KReg, src: Zmm) {
        self.emit_n(
            InstId::Vpmovq2m as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_DQ
    }
}

impl VpmovqbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovqb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovqb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqbEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovqb(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqbEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vpmovqb(&mut self, dst: Xmm, src: Zmm) {
        self.emit_n(
            InstId::Vpmovqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqbEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovqb(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpmovqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqbEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovqb(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpmovqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqbEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovqb(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpmovqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqbEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovqb(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpmovqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovqd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovqd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqdEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovqd(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqdEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vpmovqd(&mut self, dst: Ymm, src: Zmm) {
        self.emit_n(
            InstId::Vpmovqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqdEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovqd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpmovqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovqd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpmovqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovqd(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpmovqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqdEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovqd(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpmovqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovqw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovqw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqwEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovqw(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqwEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vpmovqw(&mut self, dst: Xmm, src: Zmm) {
        self.emit_n(
            InstId::Vpmovqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqwEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovqw(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpmovqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqwEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovqw(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpmovqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqwEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovqw(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpmovqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqwEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovqw(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpmovqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsdb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovsdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsdb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdbEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovsdb(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovsdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdbEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vpmovsdb(&mut self, dst: Xmm, src: Zmm) {
        self.emit_n(
            InstId::Vpmovsdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdbEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovsdb(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpmovsdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdbEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovsdb(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdbEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovsdb(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpmovsdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdbEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovsdb(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpmovsdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsdw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovsdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsdw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdwEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovsdw(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovsdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdwEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vpmovsdw(&mut self, dst: Ymm, src: Zmm) {
        self.emit_n(
            InstId::Vpmovsdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdwEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovsdw(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpmovsdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdwEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovsdw(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdwEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovsdw(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpmovsdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdwEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovsdw(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpmovsdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsqb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovsqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsqb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqbEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovsqb(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovsqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqbEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vpmovsqb(&mut self, dst: Xmm, src: Zmm) {
        self.emit_n(
            InstId::Vpmovsqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqbEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovsqb(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpmovsqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqbEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovsqb(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqbEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovsqb(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpmovsqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqbEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovsqb(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpmovsqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsqd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovsqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsqd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqdEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovsqd(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovsqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqdEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vpmovsqd(&mut self, dst: Ymm, src: Zmm) {
        self.emit_n(
            InstId::Vpmovsqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqdEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovsqd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpmovsqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovsqd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovsqd(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpmovsqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqdEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovsqd(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpmovsqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsqw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovsqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsqw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqwEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovsqw(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovsqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqwEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vpmovsqw(&mut self, dst: Xmm, src: Zmm) {
        self.emit_n(
            InstId::Vpmovsqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqwEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovsqw(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpmovsqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqwEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovsqw(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqwEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovsqw(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpmovsqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqwEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovsqw(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpmovsqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovswbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovswb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovswbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovswb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovswbEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovswb(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovswbEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vpmovswb(&mut self, dst: Ymm, src: Zmm) {
        self.emit_n(
            InstId::Vpmovswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovswbEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovswb(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpmovswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovswbEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovswb(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpmovswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovswbEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovswb(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpmovswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovswbEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovswb(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpmovswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovsxbdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsxbd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovsxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsxbd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbdEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovsxbd(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbdEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vpmovsxbd(&mut self, dst: Zmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbdEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovsxbd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovsxbd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovsxbd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovsxbd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsxbq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovsxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsxbq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovsxbq(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbqEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vpmovsxbq(&mut self, dst: Zmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovsxbq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovsxbq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovsxbq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovsxbq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsxbw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovsxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovsxbwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsxbw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovsxbwEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovsxbw(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovsxbwEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vpmovsxbw(&mut self, dst: Zmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovsxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovsxbwEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovsxbw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovsxbwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovsxbw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovsxbwEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovsxbw(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovsxbwEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovsxbw(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovsxdqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsxdq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovsxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxdqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsxdq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxdqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovsxdq(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxdqEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vpmovsxdq(&mut self, dst: Zmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovsxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxdqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovsxdq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxdqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovsxdq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxdqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovsxdq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxdqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovsxdq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsxwd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovsxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsxwd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwdEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovsxwd(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwdEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vpmovsxwd(&mut self, dst: Zmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovsxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwdEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovsxwd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovsxwd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovsxwd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovsxwd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsxwq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovsxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsxwq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovsxwq(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwqEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vpmovsxwq(&mut self, dst: Zmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovsxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovsxwq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovsxwq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovsxwq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovsxwq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovsxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovusdbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovusdb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovusdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovusdb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovusdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdbEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovusdb(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovusdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdbEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vpmovusdb(&mut self, dst: Xmm, src: Zmm) {
        self.emit_n(
            InstId::Vpmovusdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdbEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovusdb(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpmovusdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdbEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovusdb(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpmovusdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdbEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovusdb(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpmovusdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdbEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovusdb(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpmovusdb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovusdw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovusdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovusdw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovusdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdwEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovusdw(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovusdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdwEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vpmovusdw(&mut self, dst: Ymm, src: Zmm) {
        self.emit_n(
            InstId::Vpmovusdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdwEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovusdw(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpmovusdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdwEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovusdw(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpmovusdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdwEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovusdw(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpmovusdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdwEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovusdw(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpmovusdw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovusqb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovusqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovusqb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovusqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqbEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovusqb(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovusqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqbEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vpmovusqb(&mut self, dst: Xmm, src: Zmm) {
        self.emit_n(
            InstId::Vpmovusqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqbEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovusqb(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpmovusqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqbEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovusqb(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpmovusqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqbEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovusqb(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpmovusqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqbEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovusqb(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpmovusqb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovusqd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovusqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovusqd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovusqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqdEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovusqd(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovusqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqdEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vpmovusqd(&mut self, dst: Ymm, src: Zmm) {
        self.emit_n(
            InstId::Vpmovusqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqdEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovusqd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpmovusqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovusqd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpmovusqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovusqd(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpmovusqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqdEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovusqd(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpmovusqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovusqw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovusqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovusqw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovusqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqwEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovusqw(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovusqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqwEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vpmovusqw(&mut self, dst: Xmm, src: Zmm) {
        self.emit_n(
            InstId::Vpmovusqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqwEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovusqw(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpmovusqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqwEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovusqw(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpmovusqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqwEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovusqw(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpmovusqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqwEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovusqw(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpmovusqw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovuswbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovuswb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovuswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovuswbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovuswb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovuswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovuswbEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovuswb(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovuswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovuswbEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vpmovuswb(&mut self, dst: Ymm, src: Zmm) {
        self.emit_n(
            InstId::Vpmovuswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovuswbEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovuswb(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpmovuswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovuswbEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovuswb(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpmovuswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovuswbEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovuswb(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpmovuswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovuswbEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovuswb(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpmovuswb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpmovw2mEmitter<KReg, Vec> for Assembler<'_> {
    fn vpmovw2m(&mut self, dst: KReg, src: Vec) {
        self.emit_n(
            InstId::Vpmovw2m as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovw2mEmitter<KReg, Xmm> for Assembler<'_> {
    fn vpmovw2m(&mut self, dst: KReg, src: Xmm) {
        self.emit_n(
            InstId::Vpmovw2m as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovw2mEmitter<KReg, Ymm> for Assembler<'_> {
    fn vpmovw2m(&mut self, dst: KReg, src: Ymm) {
        self.emit_n(
            InstId::Vpmovw2m as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovw2mEmitter<KReg, Zmm> for Assembler<'_> {
    fn vpmovw2m(&mut self, dst: KReg, src: Zmm) {
        self.emit_n(
            InstId::Vpmovw2m as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW
    }
}

impl VpmovwbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovwb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovwb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovwbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovwb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovwb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovwbEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovwb(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovwb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovwbEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vpmovwb(&mut self, dst: Ymm, src: Zmm) {
        self.emit_n(
            InstId::Vpmovwb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovwbEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovwb(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpmovwb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovwbEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovwb(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpmovwb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovwbEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovwb(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpmovwb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovwbEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovwb(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpmovwb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovzxbdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovzxbd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovzxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovzxbd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovzxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbdEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovzxbd(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovzxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbdEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vpmovzxbd(&mut self, dst: Zmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovzxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbdEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovzxbd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovzxbd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovzxbd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovzxbd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovzxbq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovzxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovzxbq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovzxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovzxbq(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovzxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbqEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vpmovzxbq(&mut self, dst: Zmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovzxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovzxbq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovzxbq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovzxbq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovzxbq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovzxbw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovzxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovzxbwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovzxbw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovzxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovzxbwEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovzxbw(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovzxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovzxbwEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vpmovzxbw(&mut self, dst: Zmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovzxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovzxbwEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovzxbw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovzxbwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovzxbw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovzxbwEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovzxbw(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovzxbwEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovzxbw(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovzxdqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovzxdq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovzxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxdqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovzxdq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovzxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxdqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovzxdq(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovzxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxdqEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vpmovzxdq(&mut self, dst: Zmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovzxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxdqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovzxdq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxdqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovzxdq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxdqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovzxdq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxdqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovzxdq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovzxwd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovzxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovzxwd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovzxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwdEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovzxwd(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovzxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwdEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vpmovzxwd(&mut self, dst: Zmm, src: Ymm) {
        self.emit_n(
            InstId::Vpmovzxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwdEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovzxwd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovzxwd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovzxwd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovzxwd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovzxwq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpmovzxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovzxwq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovzxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovzxwq(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovzxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwqEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vpmovzxwq(&mut self, dst: Zmm, src: Xmm) {
        self.emit_n(
            InstId::Vpmovzxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovzxwq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovzxwq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovzxwq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovzxwq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpmovzxwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmuldqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmuldq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmuldq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpmuldqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmuldq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmuldq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpmuldqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmuldq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmuldq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpmuldqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmuldq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpmuldq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpmuldqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmuldq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmuldq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpmuldqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmuldq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmuldq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpmuldqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmuldq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmuldq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpmuldqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmuldq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmuldq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpmulhrswEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmulhrsw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmulhrsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhrswEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmulhrsw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmulhrsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhrswEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmulhrsw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmulhrsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhrswEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmulhrsw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpmulhrsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhrswEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmulhrsw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmulhrsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhrswEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmulhrsw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmulhrsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhrswEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmulhrsw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmulhrsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhrswEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmulhrsw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmulhrsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhuwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmulhuw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmulhuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhuwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmulhuw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmulhuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhuwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmulhuw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmulhuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhuwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmulhuw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpmulhuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhuwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmulhuw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmulhuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhuwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmulhuw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmulhuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhuwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmulhuw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmulhuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhuwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmulhuw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmulhuw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmulhw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmulhw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmulhw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmulhw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmulhw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmulhw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmulhw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpmulhw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmulhw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmulhw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmulhw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmulhw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmulhw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmulhw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmulhw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmulhw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulldEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmulld(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmulld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmulldEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmulld(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmulld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmulldEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmulld(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmulld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmulldEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmulld(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpmulld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmulldEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmulld(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmulld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmulldEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmulld(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmulld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmulldEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmulld(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmulld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmulldEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmulld(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmulld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmullqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmullq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmullq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VpmullqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmullq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmullq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VpmullqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmullq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmullq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VpmullqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmullq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpmullq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VpmullqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmullq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmullq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VpmullqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmullq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmullq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VpmullqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmullq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmullq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VpmullqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmullq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmullq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VpmullwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmullw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmullw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmullwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmullw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmullw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmullwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmullw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmullw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmullwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmullw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpmullw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmullwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmullw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmullw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmullwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmullw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmullw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmullwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmullw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmullw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmullwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmullw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmullw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmultishiftqbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmultishiftqb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmultishiftqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz|b64}
    }
}

impl VpmultishiftqbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmultishiftqb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmultishiftqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz|b64}
    }
}

impl VpmultishiftqbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmultishiftqb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmultishiftqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz|b64}
    }
}

impl VpmultishiftqbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmultishiftqb(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpmultishiftqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz|b64}
    }
}

impl VpmultishiftqbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmultishiftqb(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmultishiftqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz|b64}
    }
}

impl VpmultishiftqbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmultishiftqb(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmultishiftqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz|b64}
    }
}

impl VpmultishiftqbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmultishiftqb(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmultishiftqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz|b64}
    }
}

impl VpmultishiftqbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmultishiftqb(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmultishiftqb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI{kz|b64}
    }
}

impl VpmuludqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmuludq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpmuludq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpmuludqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmuludq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpmuludq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpmuludqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmuludq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpmuludq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpmuludqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmuludq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpmuludq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpmuludqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmuludq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpmuludq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpmuludqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmuludq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmuludq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpmuludqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmuludq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpmuludq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpmuludqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmuludq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpmuludq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpopcntbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpopcntb(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpopcntb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpopcntb(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpopcntb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntbEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpopcntb(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vpopcntb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntbEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpopcntb(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vpopcntb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntbEmitter<Vec, Mem> for Assembler<'_> {
    fn vpopcntb(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpopcntb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntbEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpopcntb(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpopcntb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntbEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpopcntb(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpopcntb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntbEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpopcntb(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpopcntb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpopcntd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpopcntd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b32}
    }
}

impl VpopcntdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpopcntd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpopcntd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b32}
    }
}

impl VpopcntdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpopcntd(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vpopcntd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b32}
    }
}

impl VpopcntdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpopcntd(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vpopcntd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b32}
    }
}

impl VpopcntdEmitter<Vec, Mem> for Assembler<'_> {
    fn vpopcntd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpopcntd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b32}
    }
}

impl VpopcntdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpopcntd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpopcntd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b32}
    }
}

impl VpopcntdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpopcntd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpopcntd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b32}
    }
}

impl VpopcntdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpopcntd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpopcntd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b32}
    }
}

impl VpopcntqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpopcntq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpopcntq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b64}
    }
}

impl VpopcntqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpopcntq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpopcntq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b64}
    }
}

impl VpopcntqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpopcntq(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vpopcntq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b64}
    }
}

impl VpopcntqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpopcntq(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vpopcntq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b64}
    }
}

impl VpopcntqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpopcntq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpopcntq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b64}
    }
}

impl VpopcntqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpopcntq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpopcntq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b64}
    }
}

impl VpopcntqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpopcntq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpopcntq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b64}
    }
}

impl VpopcntqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpopcntq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpopcntq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b64}
    }
}

impl VpopcntwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpopcntw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vpopcntw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpopcntw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vpopcntw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntwEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpopcntw(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vpopcntw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntwEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpopcntw(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vpopcntw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntwEmitter<Vec, Mem> for Assembler<'_> {
    fn vpopcntw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vpopcntw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpopcntw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vpopcntw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntwEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpopcntw(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vpopcntw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntwEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpopcntw(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vpopcntw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VporEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpor(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpor as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VporEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpor(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpor as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VporEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpor(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpor as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VporEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpor(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpor as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VporEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpor(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpor as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VporEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpor(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpor as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpordEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpord(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpord as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpordEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpord(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpord as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpordEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpord(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpord as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpordEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpord(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpord as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpordEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpord(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpord as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpordEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpord(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpord as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpordEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpord(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpord as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpordEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpord(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpord as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VporqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vporq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vporq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VporqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vporq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vporq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VporqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vporq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vporq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VporqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vporq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vporq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VporqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vporq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vporq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VporqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vporq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vporq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VporqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vporq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vporq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VporqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vporq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vporq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VproldEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vprold(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vprold as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VproldEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vprold(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vprold as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VproldEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vprold(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vprold as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VproldEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vprold(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vprold as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VproldEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vprold(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vprold as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VproldEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vprold(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vprold as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VproldEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vprold(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vprold as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VproldEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vprold(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vprold as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VprolqEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vprolq(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vprolq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprolqEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vprolq(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vprolq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprolqEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vprolq(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vprolq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprolqEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vprolq(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vprolq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VprolqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vprolq(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vprolq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprolqEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vprolq(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vprolq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprolqEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vprolq(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vprolq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprolqEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vprolq(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vprolq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VprolvdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vprolvd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vprolvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprolvdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vprolvd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vprolvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprolvdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vprolvd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vprolvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprolvdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vprolvd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vprolvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprolvdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vprolvd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vprolvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprolvdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vprolvd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vprolvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprolvdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vprolvd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vprolvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprolvdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vprolvd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vprolvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprolvqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vprolvq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vprolvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprolvqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vprolvq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vprolvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprolvqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vprolvq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vprolvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprolvqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vprolvq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vprolvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprolvqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vprolvq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vprolvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprolvqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vprolvq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vprolvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprolvqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vprolvq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vprolvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprolvqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vprolvq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vprolvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprordEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vprord(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vprord as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VprordEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vprord(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vprord as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VprordEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vprord(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vprord as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VprordEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vprord(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vprord as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VprordEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vprord(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vprord as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VprordEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vprord(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vprord as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VprordEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vprord(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vprord as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VprordEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vprord(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vprord as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VprorqEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vprorq(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vprorq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprorqEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vprorq(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vprorq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprorqEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vprorq(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vprorq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprorqEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vprorq(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vprorq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VprorqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vprorq(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vprorq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprorqEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vprorq(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vprorq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprorqEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vprorq(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vprorq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprorqEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vprorq(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vprorq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VprorvdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vprorvd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vprorvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprorvdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vprorvd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vprorvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprorvdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vprorvd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vprorvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprorvdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vprorvd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vprorvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprorvdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vprorvd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vprorvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprorvdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vprorvd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vprorvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprorvdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vprorvd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vprorvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprorvdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vprorvd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vprorvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprorvqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vprorvq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vprorvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprorvqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vprorvq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vprorvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprorvqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vprorvq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vprorvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprorvqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vprorvq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vprorvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprorvqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vprorvq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vprorvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprorvqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vprorvq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vprorvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprorvqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vprorvq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vprorvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprorvqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vprorvq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vprorvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsadbwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsadbw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsadbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW
    }
}

impl VpsadbwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsadbw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsadbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW
    }
}

impl VpsadbwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsadbw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpsadbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW
    }
}

impl VpsadbwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsadbw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpsadbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW
    }
}

impl VpsadbwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsadbw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsadbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW
    }
}

impl VpsadbwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsadbw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsadbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW
    }
}

impl VpsadbwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsadbw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsadbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW
    }
}

impl VpsadbwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsadbw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsadbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW
    }
}

impl VpscatterddEmitter<Mem, Vec> for Assembler<'_> {
    fn vpscatterdd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpscatterdd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterddEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpscatterdd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpscatterdd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterddEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpscatterdd(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpscatterdd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterddEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpscatterdd(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpscatterdd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterdqEmitter<Mem, Vec> for Assembler<'_> {
    fn vpscatterdq(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpscatterdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterdqEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpscatterdq(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpscatterdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterdqEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpscatterdq(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpscatterdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterdqEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpscatterdq(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpscatterdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterqdEmitter<Mem, Vec> for Assembler<'_> {
    fn vpscatterqd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpscatterqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterqdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpscatterqd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpscatterqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterqdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpscatterqd(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpscatterqd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterqqEmitter<Mem, Vec> for Assembler<'_> {
    fn vpscatterqq(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vpscatterqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterqqEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpscatterqq(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vpscatterqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterqqEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpscatterqq(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vpscatterqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterqqEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpscatterqq(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vpscatterqq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpshlddEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpshldd(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpshldd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshlddEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpshldd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpshldd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshlddEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpshldd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vpshldd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshlddEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpshldd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vpshldd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshlddEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpshldd(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpshldd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshlddEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpshldd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpshldd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshlddEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpshldd(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpshldd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshlddEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vpshldd(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpshldd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldqEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpshldq(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpshldq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldqEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpshldq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpshldq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldqEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpshldq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vpshldq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldqEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpshldq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vpshldq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldqEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpshldq(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpshldq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldqEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpshldq(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpshldq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldqEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpshldq(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpshldq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldqEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vpshldq(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpshldq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshldvd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpshldvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshldvd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpshldvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpshldvd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpshldvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpshldvd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpshldvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshldvd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpshldvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshldvd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshldvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpshldvd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpshldvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpshldvd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshldvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshldvq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpshldvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshldvq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpshldvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpshldvq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpshldvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpshldvq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpshldvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshldvq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpshldvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshldvq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshldvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpshldvq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpshldvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpshldvq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshldvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshldvw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpshldvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshldvw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpshldvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpshldvw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpshldvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpshldvw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpshldvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshldvw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpshldvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshldvw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshldvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpshldvw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpshldvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpshldvw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshldvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldwEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpshldw(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpshldw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldwEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpshldw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpshldw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldwEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpshldw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vpshldw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldwEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpshldw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vpshldw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldwEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpshldw(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpshldw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldwEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpshldw(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpshldw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldwEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpshldw(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpshldw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldwEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vpshldw(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpshldw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrddEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpshrdd(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpshrdd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrddEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpshrdd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpshrdd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrddEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpshrdd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vpshrdd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrddEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpshrdd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vpshrdd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrddEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpshrdd(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpshrdd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrddEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpshrdd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpshrdd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrddEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpshrdd(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpshrdd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrddEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vpshrdd(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpshrdd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdqEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpshrdq(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpshrdq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdqEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpshrdq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpshrdq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdqEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpshrdq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vpshrdq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdqEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpshrdq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vpshrdq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdqEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpshrdq(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpshrdq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdqEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpshrdq(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpshrdq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdqEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpshrdq(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpshrdq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdqEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vpshrdq(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpshrdq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshrdvd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpshrdvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshrdvd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpshrdvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpshrdvd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpshrdvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpshrdvd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpshrdvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshrdvd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpshrdvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshrdvd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshrdvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpshrdvd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpshrdvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpshrdvd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshrdvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshrdvq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpshrdvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshrdvq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpshrdvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpshrdvq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpshrdvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpshrdvq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpshrdvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshrdvq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpshrdvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshrdvq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshrdvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpshrdvq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpshrdvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpshrdvq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshrdvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshrdvw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpshrdvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshrdvw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpshrdvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpshrdvw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpshrdvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpshrdvw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpshrdvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshrdvw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpshrdvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshrdvw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshrdvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpshrdvw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpshrdvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpshrdvw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshrdvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdwEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpshrdw(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpshrdw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdwEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpshrdw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpshrdw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdwEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpshrdw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vpshrdw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdwEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpshrdw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vpshrdw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdwEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpshrdw(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpshrdw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdwEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpshrdw(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpshrdw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdwEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpshrdw(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpshrdw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdwEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vpshrdw(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpshrdw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshufbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshufb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpshufb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshufbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshufb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpshufb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshufbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpshufb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpshufb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshufbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpshufb(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpshufb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshufbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshufb(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpshufb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshufbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshufb(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshufb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshufbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpshufb(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpshufb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshufbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpshufb(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshufb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshufbitqmbEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vpshufbitqmb(&mut self, dst: KReg, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpshufbitqmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BITALG{k}
    }
}

impl VpshufbitqmbEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vpshufbitqmb(&mut self, dst: KReg, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpshufbitqmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BITALG{k}
    }
}

impl VpshufbitqmbEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vpshufbitqmb(&mut self, dst: KReg, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpshufbitqmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BITALG{k}
    }
}

impl VpshufbitqmbEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vpshufbitqmb(&mut self, dst: KReg, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpshufbitqmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BITALG{k}
    }
}

impl VpshufbitqmbEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vpshufbitqmb(&mut self, dst: KReg, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpshufbitqmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BITALG{k}
    }
}

impl VpshufbitqmbEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vpshufbitqmb(&mut self, dst: KReg, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshufbitqmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BITALG{k}
    }
}

impl VpshufbitqmbEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vpshufbitqmb(&mut self, dst: KReg, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpshufbitqmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BITALG{k}
    }
}

impl VpshufbitqmbEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vpshufbitqmb(&mut self, dst: KReg, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshufbitqmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BITALG{k}
    }
}

impl VpshufdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpshufd(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpshufd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpshufdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpshufd(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpshufd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpshufdEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpshufd(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vpshufd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpshufdEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpshufd(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vpshufd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpshufdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpshufd(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpshufd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpshufdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpshufd(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpshufd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpshufdEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpshufd(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpshufd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpshufdEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpshufd(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpshufd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpshufhwEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpshufhw(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpshufhw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshufhwEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpshufhw(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpshufhw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshufhwEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpshufhw(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vpshufhw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshufhwEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpshufhw(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vpshufhw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshufhwEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpshufhw(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpshufhw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshufhwEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpshufhw(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpshufhw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshufhwEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpshufhw(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpshufhw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshufhwEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpshufhw(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpshufhw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshuflwEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpshuflw(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpshuflw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshuflwEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpshuflw(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpshuflw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshuflwEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpshuflw(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vpshuflw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshuflwEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpshuflw(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vpshuflw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshuflwEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpshuflw(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpshuflw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshuflwEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpshuflw(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpshuflw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshuflwEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpshuflw(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpshuflw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshuflwEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpshuflw(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpshuflw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsignbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsignb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsignb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpsignbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsignb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsignb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpsignbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsignb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpsignb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpsignbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsignb(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsignb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpsignbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsignb(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsignb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpsignbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsignb(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsignb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpsigndEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsignd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsignd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpsigndEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsignd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsignd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpsigndEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsignd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpsignd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpsigndEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsignd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsignd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpsigndEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsignd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsignd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpsigndEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsignd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsignd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpsignwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsignw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsignw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpsignwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsignw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsignw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpsignwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsignw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpsignw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpsignwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsignw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsignw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpsignwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsignw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsignw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpsignwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsignw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsignw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpslldEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpslld(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpslldEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpslld(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpslldEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpslld(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpslldEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpslld(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpslldEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpslld(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpslldEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpslld(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpslldEmitter<Ymm, Ymm, Xmm> for Assembler<'_> {
    fn vpslld(&mut self, dst: Ymm, src1: Ymm, src2: Xmm) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpslldEmitter<Zmm, Zmm, Xmm> for Assembler<'_> {
    fn vpslld(&mut self, dst: Zmm, src1: Zmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpslldEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpslld(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpslldEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpslld(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpslldEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpslld(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpslldEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpslld(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpslldEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpslld(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpslldEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpslld(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpslldEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpslld(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpslldEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpslld(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VpslldqEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpslldq(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpslldq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX+ AVX512_BW
    }
}

impl<U2: Into<Imm>> VpslldqEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpslldq(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpslldq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW
    }
}

impl<U2: Into<Imm>> VpslldqEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpslldq(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vpslldq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW
    }
}

impl<U2: Into<Imm>> VpslldqEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpslldq(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vpslldq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW
    }
}

impl VpslldqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpslldq(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpslldq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_BW
    }
}

impl<U2: Into<Imm>> VpslldqEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpslldq(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpslldq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_BW
    }
}

impl<U2: Into<Imm>> VpslldqEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpslldq(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpslldq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_BW
    }
}

impl<U2: Into<Imm>> VpslldqEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpslldq(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpslldq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_BW
    }
}

impl VpsllqEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpsllq(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsllqEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpsllq(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsllqEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpsllq(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsllqEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpsllq(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpsllqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsllq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsllqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsllq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsllqEmitter<Ymm, Ymm, Xmm> for Assembler<'_> {
    fn vpsllq(&mut self, dst: Ymm, src1: Ymm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsllqEmitter<Zmm, Zmm, Xmm> for Assembler<'_> {
    fn vpsllq(&mut self, dst: Zmm, src1: Zmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsllqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsllq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsllqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsllq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsllqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsllq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsllqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsllq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsllqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpsllq(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsllqEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpsllq(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsllqEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpsllq(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsllqEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpsllq(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsllvdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsllvd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsllvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsllvdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsllvd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsllvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsllvdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsllvd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpsllvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsllvdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsllvd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpsllvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsllvdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsllvd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsllvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsllvdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsllvd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsllvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsllvdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsllvd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsllvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsllvdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsllvd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsllvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsllvqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsllvq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsllvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsllvqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsllvq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsllvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsllvqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsllvq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpsllvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsllvqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsllvq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpsllvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsllvqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsllvq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsllvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsllvqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsllvq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsllvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsllvqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsllvq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsllvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsllvqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsllvq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsllvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsllvwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsllvw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsllvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsllvwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsllvw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsllvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsllvwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsllvw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpsllvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsllvwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsllvw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpsllvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsllvwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsllvw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsllvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsllvwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsllvw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsllvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsllvwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsllvw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsllvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsllvwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsllvw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsllvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsllwEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpsllw(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsllwEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpsllw(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsllwEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpsllw(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsllwEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpsllw(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsllwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsllw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsllwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsllw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsllwEmitter<Ymm, Ymm, Xmm> for Assembler<'_> {
    fn vpsllw(&mut self, dst: Ymm, src1: Ymm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsllwEmitter<Zmm, Zmm, Xmm> for Assembler<'_> {
    fn vpsllw(&mut self, dst: Zmm, src1: Zmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsllwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsllw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsllwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsllw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsllwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsllw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsllwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsllw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsllwEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpsllw(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsllwEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpsllw(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsllwEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpsllw(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsllwEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpsllw(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl VpsradEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpsrad(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsradEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpsrad(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsradEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpsrad(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsradEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpsrad(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpsradEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsrad(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsradEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsrad(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsradEmitter<Ymm, Ymm, Xmm> for Assembler<'_> {
    fn vpsrad(&mut self, dst: Ymm, src1: Ymm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsradEmitter<Zmm, Zmm, Xmm> for Assembler<'_> {
    fn vpsrad(&mut self, dst: Zmm, src1: Zmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsradEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsrad(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsradEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsrad(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsradEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsrad(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsradEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsrad(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsradEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpsrad(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsradEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpsrad(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsradEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpsrad(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsradEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpsrad(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VpsraqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsraq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpsraqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsraq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpsraqEmitter<Ymm, Ymm, Xmm> for Assembler<'_> {
    fn vpsraq(&mut self, dst: Ymm, src1: Ymm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpsraqEmitter<Zmm, Zmm, Xmm> for Assembler<'_> {
    fn vpsraq(&mut self, dst: Zmm, src1: Zmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpsraqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsraq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpsraqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsraq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpsraqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsraq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpsraqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsraq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpsraqEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpsraq(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsraqEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpsraq(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsraqEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpsraq(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsraqEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpsraq(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsraqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpsraq(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsraqEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpsraq(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsraqEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpsraq(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsraqEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpsraq(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsravdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsravd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsravd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsravdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsravd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsravd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsravdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsravd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpsravd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsravdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsravd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpsravd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsravdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsravd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsravd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsravdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsravd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsravd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsravdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsravd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsravd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsravdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsravd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsravd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsravqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsravq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsravq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsravqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsravq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsravq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsravqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsravq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpsravq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsravqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsravq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpsravq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsravqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsravq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsravq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsravqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsravq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsravq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsravqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsravq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsravq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsravqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsravq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsravq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsravwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsravw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsravw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsravwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsravw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsravw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsravwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsravw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpsravw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsravwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsravw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpsravw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsravwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsravw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsravw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsravwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsravw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsravw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsravwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsravw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsravw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsravwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsravw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsravw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsrawEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpsraw(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrawEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpsraw(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrawEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpsraw(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrawEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpsraw(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrawEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsraw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrawEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsraw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrawEmitter<Ymm, Ymm, Xmm> for Assembler<'_> {
    fn vpsraw(&mut self, dst: Ymm, src1: Ymm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrawEmitter<Zmm, Zmm, Xmm> for Assembler<'_> {
    fn vpsraw(&mut self, dst: Zmm, src1: Zmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrawEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsraw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrawEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsraw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrawEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsraw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrawEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsraw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrawEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpsraw(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrawEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpsraw(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrawEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpsraw(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrawEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpsraw(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl VpsrldEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpsrld(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsrldEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpsrld(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsrldEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpsrld(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsrldEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpsrld(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpsrldEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsrld(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsrldEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsrld(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsrldEmitter<Ymm, Ymm, Xmm> for Assembler<'_> {
    fn vpsrld(&mut self, dst: Ymm, src1: Ymm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsrldEmitter<Zmm, Zmm, Xmm> for Assembler<'_> {
    fn vpsrld(&mut self, dst: Zmm, src1: Zmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsrldEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsrld(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsrldEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsrld(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsrldEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsrld(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsrldEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsrld(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsrldEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpsrld(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsrldEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpsrld(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsrldEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpsrld(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsrldEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpsrld(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VpsrldqEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpsrldq(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpsrldq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX+ AVX512_BW
    }
}

impl<U2: Into<Imm>> VpsrldqEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpsrldq(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpsrldq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW
    }
}

impl<U2: Into<Imm>> VpsrldqEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpsrldq(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vpsrldq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW
    }
}

impl<U2: Into<Imm>> VpsrldqEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpsrldq(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vpsrldq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW
    }
}

impl VpsrldqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpsrldq(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpsrldq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_BW
    }
}

impl<U2: Into<Imm>> VpsrldqEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpsrldq(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsrldq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_BW
    }
}

impl<U2: Into<Imm>> VpsrldqEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpsrldq(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsrldq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_BW
    }
}

impl<U2: Into<Imm>> VpsrldqEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpsrldq(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsrldq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_BW
    }
}

impl VpsrlqEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpsrlq(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsrlqEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpsrlq(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsrlqEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpsrlq(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsrlqEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpsrlq(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpsrlqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsrlq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VpsrlqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsrlq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VpsrlqEmitter<Ymm, Ymm, Xmm> for Assembler<'_> {
    fn vpsrlq(&mut self, dst: Ymm, src1: Ymm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VpsrlqEmitter<Zmm, Zmm, Xmm> for Assembler<'_> {
    fn vpsrlq(&mut self, dst: Zmm, src1: Zmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VpsrlqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsrlq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VpsrlqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsrlq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VpsrlqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsrlq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VpsrlqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsrlq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VpsrlqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpsrlq(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsrlqEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpsrlq(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsrlqEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpsrlq(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsrlqEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpsrlq(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsrlvdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsrlvd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsrlvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsrlvdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsrlvd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsrlvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsrlvdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsrlvd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpsrlvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsrlvdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsrlvd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpsrlvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsrlvdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsrlvd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsrlvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsrlvdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsrlvd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsrlvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsrlvdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsrlvd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsrlvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsrlvdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsrlvd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsrlvd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsrlvqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsrlvq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsrlvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsrlvqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsrlvq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsrlvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsrlvqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsrlvq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpsrlvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsrlvqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsrlvq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpsrlvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsrlvqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsrlvq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsrlvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsrlvqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsrlvq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsrlvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsrlvqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsrlvq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsrlvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsrlvqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsrlvq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsrlvq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsrlvwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsrlvw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsrlvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsrlvwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsrlvw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsrlvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsrlvwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsrlvw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpsrlvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsrlvwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsrlvw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpsrlvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsrlvwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsrlvw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsrlvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsrlvwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsrlvw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsrlvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsrlvwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsrlvw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsrlvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsrlvwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsrlvw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsrlvw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsrlwEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpsrlw(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrlwEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpsrlw(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrlwEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpsrlw(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrlwEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpsrlw(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrlwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsrlw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrlwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsrlw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrlwEmitter<Ymm, Ymm, Xmm> for Assembler<'_> {
    fn vpsrlw(&mut self, dst: Ymm, src1: Ymm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrlwEmitter<Zmm, Zmm, Xmm> for Assembler<'_> {
    fn vpsrlw(&mut self, dst: Zmm, src1: Zmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrlwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsrlw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrlwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsrlw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrlwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsrlw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrlwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsrlw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrlwEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpsrlw(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrlwEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpsrlw(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrlwEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpsrlw(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrlwEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpsrlw(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl VpsubbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsubb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsubb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsubb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsubb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsubb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpsubb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsubb(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpsubb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsubb(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsubb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsubb(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsubb(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsubb(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsubd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsubd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpsubdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsubd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsubd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpsubdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsubd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpsubd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpsubdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsubd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpsubd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpsubdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsubd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsubd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpsubdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsubd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpsubdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsubd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpsubdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsubd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpsubqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsubq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsubq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpsubqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsubq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsubq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpsubqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsubq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpsubq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpsubqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsubq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpsubq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpsubqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsubq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsubq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpsubqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsubq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpsubqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsubq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpsubqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsubq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpsubsbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsubsb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsubsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubsbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsubsb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsubsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubsbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsubsb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpsubsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubsbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsubsb(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpsubsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubsbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsubsb(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsubsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubsbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsubsb(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubsbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsubsb(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubsbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsubsb(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubsb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubswEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsubsw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubswEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsubsw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubswEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsubsw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpsubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubswEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsubsw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpsubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubswEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsubsw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubswEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsubsw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubswEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsubsw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubswEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsubsw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubsw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubusbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsubusb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsubusb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubusbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsubusb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsubusb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubusbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsubusb(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpsubusb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubusbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsubusb(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpsubusb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubusbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsubusb(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsubusb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubusbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsubusb(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubusb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubusbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsubusb(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubusb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubusbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsubusb(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubusb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubuswEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsubusw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsubusw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubuswEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsubusw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsubusw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubuswEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsubusw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpsubusw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubuswEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsubusw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpsubusw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubuswEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsubusw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsubusw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubuswEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsubusw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubusw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubuswEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsubusw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubusw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubuswEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsubusw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubusw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsubw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpsubw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpsubwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsubw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpsubw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpsubwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsubw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpsubw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpsubwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsubw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpsubw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpsubwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsubw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpsubw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpsubwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsubw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpsubwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsubw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpsubwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsubw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpsubw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpternlogdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpternlogd(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpternlogd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VpternlogdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpternlogd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpternlogd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VpternlogdEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpternlogd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vpternlogd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VpternlogdEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpternlogd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vpternlogd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VpternlogdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpternlogd(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpternlogd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VpternlogdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpternlogd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpternlogd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VpternlogdEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpternlogd(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpternlogd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VpternlogdEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vpternlogd(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpternlogd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VpternlogqEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpternlogq(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpternlogq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VpternlogqEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpternlogq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpternlogq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VpternlogqEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpternlogq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vpternlogq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VpternlogqEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpternlogq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vpternlogq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VpternlogqEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpternlogq(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpternlogq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VpternlogqEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpternlogq(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpternlogq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VpternlogqEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpternlogq(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpternlogq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VpternlogqEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vpternlogq(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpternlogq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VptestEmitter<Vec, Vec> for Assembler<'_> {
    fn vptest(&mut self, lhs: Vec, rhs: Vec) {
        self.emit_n(InstId::Vptest as u32, &[lhs.as_operand(), rhs.as_operand()]); // AVX
    }
}

impl VptestEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vptest(&mut self, lhs: Xmm, rhs: Xmm) {
        self.emit_n(InstId::Vptest as u32, &[lhs.as_operand(), rhs.as_operand()]); // AVX
    }
}

impl VptestEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vptest(&mut self, lhs: Ymm, rhs: Ymm) {
        self.emit_n(InstId::Vptest as u32, &[lhs.as_operand(), rhs.as_operand()]); // AVX
    }
}

impl VptestEmitter<Vec, Mem> for Assembler<'_> {
    fn vptest(&mut self, lhs: Vec, rhs: Mem) {
        self.emit_n(InstId::Vptest as u32, &[lhs.as_operand(), rhs.as_operand()]); // AVX
    }
}

impl VptestEmitter<Xmm, Mem> for Assembler<'_> {
    fn vptest(&mut self, lhs: Xmm, rhs: Mem) {
        self.emit_n(InstId::Vptest as u32, &[lhs.as_operand(), rhs.as_operand()]); // AVX
    }
}

impl VptestEmitter<Ymm, Mem> for Assembler<'_> {
    fn vptest(&mut self, lhs: Ymm, rhs: Mem) {
        self.emit_n(InstId::Vptest as u32, &[lhs.as_operand(), rhs.as_operand()]); // AVX
    }
}

impl VptestmbEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vptestmb(&mut self, dst: KReg, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vptestmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmbEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vptestmb(&mut self, dst: KReg, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vptestmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmbEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vptestmb(&mut self, dst: KReg, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vptestmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmbEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vptestmb(&mut self, dst: KReg, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vptestmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmbEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vptestmb(&mut self, dst: KReg, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vptestmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmbEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vptestmb(&mut self, dst: KReg, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vptestmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmbEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vptestmb(&mut self, dst: KReg, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vptestmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmbEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vptestmb(&mut self, dst: KReg, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vptestmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmdEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vptestmd(&mut self, dst: KReg, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vptestmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestmdEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vptestmd(&mut self, dst: KReg, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vptestmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestmdEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vptestmd(&mut self, dst: KReg, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vptestmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestmdEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vptestmd(&mut self, dst: KReg, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vptestmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestmdEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vptestmd(&mut self, dst: KReg, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vptestmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestmdEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vptestmd(&mut self, dst: KReg, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vptestmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestmdEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vptestmd(&mut self, dst: KReg, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vptestmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestmdEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vptestmd(&mut self, dst: KReg, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vptestmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestmqEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vptestmq(&mut self, dst: KReg, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vptestmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestmqEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vptestmq(&mut self, dst: KReg, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vptestmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestmqEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vptestmq(&mut self, dst: KReg, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vptestmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestmqEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vptestmq(&mut self, dst: KReg, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vptestmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestmqEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vptestmq(&mut self, dst: KReg, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vptestmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestmqEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vptestmq(&mut self, dst: KReg, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vptestmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestmqEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vptestmq(&mut self, dst: KReg, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vptestmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestmqEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vptestmq(&mut self, dst: KReg, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vptestmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestmwEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vptestmw(&mut self, dst: KReg, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vptestmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmwEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vptestmw(&mut self, dst: KReg, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vptestmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmwEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vptestmw(&mut self, dst: KReg, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vptestmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmwEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vptestmw(&mut self, dst: KReg, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vptestmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmwEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vptestmw(&mut self, dst: KReg, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vptestmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmwEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vptestmw(&mut self, dst: KReg, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vptestmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmwEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vptestmw(&mut self, dst: KReg, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vptestmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmwEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vptestmw(&mut self, dst: KReg, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vptestmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmbEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vptestnmb(&mut self, dst: KReg, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vptestnmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmbEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vptestnmb(&mut self, dst: KReg, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vptestnmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmbEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vptestnmb(&mut self, dst: KReg, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vptestnmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmbEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vptestnmb(&mut self, dst: KReg, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vptestnmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmbEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vptestnmb(&mut self, dst: KReg, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vptestnmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmbEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vptestnmb(&mut self, dst: KReg, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vptestnmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmbEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vptestnmb(&mut self, dst: KReg, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vptestnmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmbEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vptestnmb(&mut self, dst: KReg, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vptestnmb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmdEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vptestnmd(&mut self, dst: KReg, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vptestnmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestnmdEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vptestnmd(&mut self, dst: KReg, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vptestnmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestnmdEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vptestnmd(&mut self, dst: KReg, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vptestnmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestnmdEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vptestnmd(&mut self, dst: KReg, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vptestnmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestnmdEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vptestnmd(&mut self, dst: KReg, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vptestnmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestnmdEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vptestnmd(&mut self, dst: KReg, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vptestnmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestnmdEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vptestnmd(&mut self, dst: KReg, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vptestnmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestnmdEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vptestnmd(&mut self, dst: KReg, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vptestnmd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestnmqEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vptestnmq(&mut self, dst: KReg, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vptestnmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestnmqEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vptestnmq(&mut self, dst: KReg, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vptestnmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestnmqEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vptestnmq(&mut self, dst: KReg, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vptestnmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestnmqEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vptestnmq(&mut self, dst: KReg, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vptestnmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestnmqEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vptestnmq(&mut self, dst: KReg, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vptestnmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestnmqEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vptestnmq(&mut self, dst: KReg, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vptestnmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestnmqEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vptestnmq(&mut self, dst: KReg, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vptestnmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestnmqEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vptestnmq(&mut self, dst: KReg, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vptestnmq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestnmwEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vptestnmw(&mut self, dst: KReg, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vptestnmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmwEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vptestnmw(&mut self, dst: KReg, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vptestnmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmwEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vptestnmw(&mut self, dst: KReg, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vptestnmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmwEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vptestnmw(&mut self, dst: KReg, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vptestnmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmwEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vptestnmw(&mut self, dst: KReg, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vptestnmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmwEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vptestnmw(&mut self, dst: KReg, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vptestnmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmwEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vptestnmw(&mut self, dst: KReg, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vptestnmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmwEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vptestnmw(&mut self, dst: KReg, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vptestnmw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpunpckhbwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpunpckhbw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpunpckhbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhbwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpunpckhbw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpunpckhbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhbwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpunpckhbw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpunpckhbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhbwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpunpckhbw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpunpckhbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhbwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpunpckhbw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpunpckhbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhbwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpunpckhbw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpckhbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhbwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpunpckhbw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpckhbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhbwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpunpckhbw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpckhbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhdqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpunpckhdq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpunpckhdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckhdqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpunpckhdq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpunpckhdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckhdqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpunpckhdq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpunpckhdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckhdqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpunpckhdq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpunpckhdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckhdqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpunpckhdq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpunpckhdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckhdqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpunpckhdq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpckhdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckhdqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpunpckhdq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpckhdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckhdqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpunpckhdq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpckhdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckhqdqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpunpckhqdq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpunpckhqdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpckhqdqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpunpckhqdq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpunpckhqdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpckhqdqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpunpckhqdq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpunpckhqdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpckhqdqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpunpckhqdq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpunpckhqdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpckhqdqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpunpckhqdq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpunpckhqdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpckhqdqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpunpckhqdq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpckhqdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpckhqdqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpunpckhqdq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpckhqdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpckhqdqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpunpckhqdq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpckhqdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpckhwdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpunpckhwd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpunpckhwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhwdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpunpckhwd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpunpckhwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhwdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpunpckhwd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpunpckhwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhwdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpunpckhwd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpunpckhwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhwdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpunpckhwd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpunpckhwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhwdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpunpckhwd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpckhwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhwdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpunpckhwd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpckhwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhwdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpunpckhwd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpckhwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklbwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpunpcklbw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpunpcklbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklbwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpunpcklbw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpunpcklbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklbwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpunpcklbw(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpunpcklbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklbwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpunpcklbw(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpunpcklbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklbwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpunpcklbw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpunpcklbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklbwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpunpcklbw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpcklbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklbwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpunpcklbw(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpcklbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklbwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpunpcklbw(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpcklbw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckldqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpunpckldq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpunpckldq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckldqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpunpckldq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpunpckldq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckldqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpunpckldq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpunpckldq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckldqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpunpckldq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpunpckldq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckldqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpunpckldq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpunpckldq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckldqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpunpckldq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpckldq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckldqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpunpckldq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpckldq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckldqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpunpckldq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpckldq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpcklqdqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpunpcklqdq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpunpcklqdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpcklqdqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpunpcklqdq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpunpcklqdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpcklqdqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpunpcklqdq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpunpcklqdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpcklqdqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpunpcklqdq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpunpcklqdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpcklqdqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpunpcklqdq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpunpcklqdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpcklqdqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpunpcklqdq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpcklqdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpcklqdqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpunpcklqdq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpcklqdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpcklqdqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpunpcklqdq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpcklqdq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpcklwdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpunpcklwd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpunpcklwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklwdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpunpcklwd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpunpcklwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklwdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpunpcklwd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpunpcklwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklwdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpunpcklwd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpunpcklwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklwdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpunpcklwd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpunpcklwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklwdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpunpcklwd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpcklwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklwdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpunpcklwd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpcklwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklwdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpunpcklwd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpunpcklwd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpxorEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpxor(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpxor as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpxorEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpxor(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpxor as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpxorEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpxor(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpxor as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpxorEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpxor(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpxor as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpxorEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpxor(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpxor as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpxorEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpxor(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpxor as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX+
    }
}

impl VpxordEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpxord(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpxord as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpxordEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpxord(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpxord as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpxordEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpxord(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpxord as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpxordEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpxord(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpxord as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpxordEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpxord(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpxord as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpxordEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpxord(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpxord as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpxordEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpxord(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpxord as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpxordEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpxord(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpxord as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpxorqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpxorq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpxorq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpxorqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpxorq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpxorq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpxorqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpxorq(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpxorq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpxorqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpxorq(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vpxorq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpxorqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpxorq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpxorq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpxorqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpxorq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpxorq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpxorqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpxorq(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpxorq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpxorqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpxorq(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vpxorq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VrangepdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vrangepd(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vrangepd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U3: Into<Imm>> VrangepdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vrangepd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vrangepd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U3: Into<Imm>> VrangepdEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vrangepd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vrangepd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U3: Into<Imm>> VrangepdEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vrangepd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vrangepd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VrangepdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vrangepd(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vrangepd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U3: Into<Imm>> VrangepdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vrangepd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vrangepd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U3: Into<Imm>> VrangepdEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vrangepd(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vrangepd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U3: Into<Imm>> VrangepdEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vrangepd(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vrangepd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VrangepsEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vrangeps(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vrangeps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U3: Into<Imm>> VrangepsEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vrangeps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vrangeps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U3: Into<Imm>> VrangepsEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vrangeps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vrangeps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U3: Into<Imm>> VrangepsEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vrangeps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vrangeps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl VrangepsEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vrangeps(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vrangeps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U3: Into<Imm>> VrangepsEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vrangeps(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vrangeps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U3: Into<Imm>> VrangepsEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vrangeps(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vrangeps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U3: Into<Imm>> VrangepsEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vrangeps(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vrangeps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl VrangesdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vrangesd(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vrangesd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_DQ{kz|sae}
    }
}

impl<U3: Into<Imm>> VrangesdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vrangesd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vrangesd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz|sae}
    }
}

impl VrangesdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vrangesd(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vrangesd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_DQ{kz|sae}
    }
}

impl<U3: Into<Imm>> VrangesdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vrangesd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vrangesd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz|sae}
    }
}

impl VrangessEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vrangess(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vrangess as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_DQ{kz|sae}
    }
}

impl<U3: Into<Imm>> VrangessEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vrangess(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vrangess as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz|sae}
    }
}

impl VrangessEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vrangess(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vrangess as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_DQ{kz|sae}
    }
}

impl<U3: Into<Imm>> VrangessEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vrangess(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vrangess as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz|sae}
    }
}

impl Vrcp14pdEmitter<Vec, Vec> for Assembler<'_> {
    fn vrcp14pd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vrcp14pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrcp14pdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vrcp14pd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vrcp14pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrcp14pdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vrcp14pd(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vrcp14pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrcp14pdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vrcp14pd(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vrcp14pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrcp14pdEmitter<Vec, Mem> for Assembler<'_> {
    fn vrcp14pd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vrcp14pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrcp14pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vrcp14pd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vrcp14pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrcp14pdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vrcp14pd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vrcp14pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrcp14pdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vrcp14pd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vrcp14pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrcp14psEmitter<Vec, Vec> for Assembler<'_> {
    fn vrcp14ps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vrcp14ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrcp14psEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vrcp14ps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vrcp14ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrcp14psEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vrcp14ps(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vrcp14ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrcp14psEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vrcp14ps(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vrcp14ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrcp14psEmitter<Vec, Mem> for Assembler<'_> {
    fn vrcp14ps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vrcp14ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrcp14psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vrcp14ps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vrcp14ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrcp14psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vrcp14ps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vrcp14ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrcp14psEmitter<Zmm, Mem> for Assembler<'_> {
    fn vrcp14ps(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vrcp14ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrcp14sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vrcp14sd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vrcp14sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrcp14sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vrcp14sd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vrcp14sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrcp14sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vrcp14sd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vrcp14sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrcp14sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vrcp14sd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vrcp14sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrcp14ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vrcp14ss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vrcp14ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrcp14ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vrcp14ss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vrcp14ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrcp14ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vrcp14ss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vrcp14ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrcp14ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vrcp14ss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vrcp14ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VrcppsEmitter<Vec, Vec> for Assembler<'_> {
    fn vrcpps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Vrcpps as u32, &[dst.as_operand(), src.as_operand()]); // AVX
    }
}

impl VrcppsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vrcpps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Vrcpps as u32, &[dst.as_operand(), src.as_operand()]); // AVX
    }
}

impl VrcppsEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vrcpps(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(InstId::Vrcpps as u32, &[dst.as_operand(), src.as_operand()]); // AVX
    }
}

impl VrcppsEmitter<Vec, Mem> for Assembler<'_> {
    fn vrcpps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Vrcpps as u32, &[dst.as_operand(), src.as_operand()]); // AVX
    }
}

impl VrcppsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vrcpps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Vrcpps as u32, &[dst.as_operand(), src.as_operand()]); // AVX
    }
}

impl VrcppsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vrcpps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(InstId::Vrcpps as u32, &[dst.as_operand(), src.as_operand()]); // AVX
    }
}

impl VrcpssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vrcpss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vrcpss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VrcpssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vrcpss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vrcpss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VrcpssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vrcpss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vrcpss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VrcpssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vrcpss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vrcpss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VreducepdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vreducepd(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vreducepd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U2: Into<Imm>> VreducepdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vreducepd(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vreducepd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U2: Into<Imm>> VreducepdEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vreducepd(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vreducepd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U2: Into<Imm>> VreducepdEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vreducepd(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vreducepd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VreducepdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vreducepd(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vreducepd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U2: Into<Imm>> VreducepdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vreducepd(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vreducepd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U2: Into<Imm>> VreducepdEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vreducepd(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vreducepd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U2: Into<Imm>> VreducepdEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vreducepd(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vreducepd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VreducepsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vreduceps(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vreduceps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U2: Into<Imm>> VreducepsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vreduceps(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vreduceps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U2: Into<Imm>> VreducepsEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vreduceps(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vreduceps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U2: Into<Imm>> VreducepsEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vreduceps(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vreduceps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl VreducepsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vreduceps(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vreduceps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U2: Into<Imm>> VreducepsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vreduceps(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vreduceps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U2: Into<Imm>> VreducepsEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vreduceps(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vreduceps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U2: Into<Imm>> VreducepsEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vreduceps(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vreduceps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl VreducesdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vreducesd(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vreducesd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> VreducesdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vreducesd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vreducesd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl VreducesdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vreducesd(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vreducesd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> VreducesdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vreducesd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vreducesd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl VreducessEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vreducess(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vreducess as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> VreducessEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vreducess(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vreducess as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl VreducessEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vreducess(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vreducess as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> VreducessEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vreducess(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vreducess as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl VrndscalepdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vrndscalepd(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vrndscalepd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VrndscalepdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vrndscalepd(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vrndscalepd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VrndscalepdEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vrndscalepd(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vrndscalepd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VrndscalepdEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vrndscalepd(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vrndscalepd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VrndscalepdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vrndscalepd(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vrndscalepd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VrndscalepdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vrndscalepd(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vrndscalepd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VrndscalepdEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vrndscalepd(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vrndscalepd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VrndscalepdEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vrndscalepd(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vrndscalepd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VrndscalepsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vrndscaleps(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vrndscaleps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VrndscalepsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vrndscaleps(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vrndscaleps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VrndscalepsEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vrndscaleps(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vrndscaleps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VrndscalepsEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vrndscaleps(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vrndscaleps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VrndscalepsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vrndscaleps(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vrndscaleps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VrndscalepsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vrndscaleps(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vrndscaleps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VrndscalepsEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vrndscaleps(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vrndscaleps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VrndscalepsEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vrndscaleps(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vrndscaleps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VrndscalesdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vrndscalesd(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vrndscalesd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VrndscalesdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vrndscalesd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vrndscalesd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VrndscalesdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vrndscalesd(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vrndscalesd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VrndscalesdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vrndscalesd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vrndscalesd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VrndscalessEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vrndscaless(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vrndscaless as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VrndscalessEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vrndscaless(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vrndscaless as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VrndscalessEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vrndscaless(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vrndscaless as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VrndscalessEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vrndscaless(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vrndscaless as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VroundpdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vroundpd(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vroundpd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX
    }
}

impl<U2: Into<Imm>> VroundpdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vroundpd(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vroundpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX
    }
}

impl<U2: Into<Imm>> VroundpdEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vroundpd(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vroundpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX
    }
}

impl VroundpdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vroundpd(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vroundpd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX
    }
}

impl<U2: Into<Imm>> VroundpdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vroundpd(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vroundpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX
    }
}

impl<U2: Into<Imm>> VroundpdEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vroundpd(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vroundpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX
    }
}

impl VroundpsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vroundps(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vroundps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX
    }
}

impl<U2: Into<Imm>> VroundpsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vroundps(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vroundps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX
    }
}

impl<U2: Into<Imm>> VroundpsEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vroundps(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vroundps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX
    }
}

impl VroundpsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vroundps(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vroundps as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX
    }
}

impl<U2: Into<Imm>> VroundpsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vroundps(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vroundps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX
    }
}

impl<U2: Into<Imm>> VroundpsEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vroundps(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vroundps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // AVX
    }
}

impl VroundsdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vroundsd(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vroundsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VroundsdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vroundsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vroundsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl VroundsdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vroundsd(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vroundsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VroundsdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vroundsd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vroundsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl VroundssEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vroundss(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vroundss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VroundssEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vroundss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vroundss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl VroundssEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vroundss(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vroundss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VroundssEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vroundss(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vroundss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX
    }
}

impl Vrsqrt14pdEmitter<Vec, Vec> for Assembler<'_> {
    fn vrsqrt14pd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vrsqrt14pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrsqrt14pdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vrsqrt14pd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vrsqrt14pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrsqrt14pdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vrsqrt14pd(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vrsqrt14pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrsqrt14pdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vrsqrt14pd(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vrsqrt14pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrsqrt14pdEmitter<Vec, Mem> for Assembler<'_> {
    fn vrsqrt14pd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vrsqrt14pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrsqrt14pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vrsqrt14pd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vrsqrt14pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrsqrt14pdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vrsqrt14pd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vrsqrt14pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrsqrt14pdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vrsqrt14pd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vrsqrt14pd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrsqrt14psEmitter<Vec, Vec> for Assembler<'_> {
    fn vrsqrt14ps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vrsqrt14ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrsqrt14psEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vrsqrt14ps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vrsqrt14ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrsqrt14psEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vrsqrt14ps(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vrsqrt14ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrsqrt14psEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vrsqrt14ps(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vrsqrt14ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrsqrt14psEmitter<Vec, Mem> for Assembler<'_> {
    fn vrsqrt14ps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vrsqrt14ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrsqrt14psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vrsqrt14ps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vrsqrt14ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrsqrt14psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vrsqrt14ps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vrsqrt14ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrsqrt14psEmitter<Zmm, Mem> for Assembler<'_> {
    fn vrsqrt14ps(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vrsqrt14ps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrsqrt14sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vrsqrt14sd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vrsqrt14sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrsqrt14sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vrsqrt14sd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vrsqrt14sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrsqrt14sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vrsqrt14sd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vrsqrt14sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrsqrt14sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vrsqrt14sd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vrsqrt14sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrsqrt14ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vrsqrt14ss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vrsqrt14ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrsqrt14ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vrsqrt14ss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vrsqrt14ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrsqrt14ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vrsqrt14ss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vrsqrt14ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrsqrt14ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vrsqrt14ss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vrsqrt14ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VrsqrtpsEmitter<Vec, Vec> for Assembler<'_> {
    fn vrsqrtps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vrsqrtps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VrsqrtpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vrsqrtps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vrsqrtps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VrsqrtpsEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vrsqrtps(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vrsqrtps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VrsqrtpsEmitter<Vec, Mem> for Assembler<'_> {
    fn vrsqrtps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vrsqrtps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VrsqrtpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vrsqrtps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vrsqrtps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VrsqrtpsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vrsqrtps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vrsqrtps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX
    }
}

impl VrsqrtssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vrsqrtss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vrsqrtss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VrsqrtssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vrsqrtss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vrsqrtss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VrsqrtssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vrsqrtss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vrsqrtss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VrsqrtssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vrsqrtss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vrsqrtss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX
    }
}

impl VscalefpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vscalefpd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vscalefpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VscalefpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vscalefpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vscalefpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VscalefpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vscalefpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vscalefpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VscalefpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vscalefpd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vscalefpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VscalefpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vscalefpd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vscalefpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VscalefpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vscalefpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vscalefpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VscalefpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vscalefpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vscalefpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VscalefpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vscalefpd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vscalefpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VscalefpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vscalefps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vscalefps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VscalefpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vscalefps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vscalefps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VscalefpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vscalefps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vscalefps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VscalefpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vscalefps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vscalefps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VscalefpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vscalefps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vscalefps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VscalefpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vscalefps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vscalefps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VscalefpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vscalefps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vscalefps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VscalefpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vscalefps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vscalefps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VscalefsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vscalefsd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vscalefsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|er}
    }
}

impl VscalefsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vscalefsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vscalefsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|er}
    }
}

impl VscalefsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vscalefsd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vscalefsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|er}
    }
}

impl VscalefsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vscalefsd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vscalefsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|er}
    }
}

impl VscalefssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vscalefss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vscalefss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|er}
    }
}

impl VscalefssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vscalefss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vscalefss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|er}
    }
}

impl VscalefssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vscalefss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vscalefss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|er}
    }
}

impl VscalefssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vscalefss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vscalefss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX512_F{kz|er}
    }
}

impl VscatterdpdEmitter<Mem, Vec> for Assembler<'_> {
    fn vscatterdpd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vscatterdpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterdpdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vscatterdpd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vscatterdpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterdpdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vscatterdpd(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vscatterdpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterdpdEmitter<Mem, Zmm> for Assembler<'_> {
    fn vscatterdpd(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vscatterdpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterdpsEmitter<Mem, Vec> for Assembler<'_> {
    fn vscatterdps(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vscatterdps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterdpsEmitter<Mem, Xmm> for Assembler<'_> {
    fn vscatterdps(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vscatterdps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterdpsEmitter<Mem, Ymm> for Assembler<'_> {
    fn vscatterdps(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vscatterdps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterdpsEmitter<Mem, Zmm> for Assembler<'_> {
    fn vscatterdps(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vscatterdps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterqpdEmitter<Mem, Vec> for Assembler<'_> {
    fn vscatterqpd(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vscatterqpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterqpdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vscatterqpd(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vscatterqpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterqpdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vscatterqpd(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vscatterqpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterqpdEmitter<Mem, Zmm> for Assembler<'_> {
    fn vscatterqpd(&mut self, dst: Mem, src: Zmm) {
        self.emit_n(
            InstId::Vscatterqpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterqpsEmitter<Mem, Vec> for Assembler<'_> {
    fn vscatterqps(&mut self, dst: Mem, src: Vec) {
        self.emit_n(
            InstId::Vscatterqps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterqpsEmitter<Mem, Xmm> for Assembler<'_> {
    fn vscatterqps(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(
            InstId::Vscatterqps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterqpsEmitter<Mem, Ymm> for Assembler<'_> {
    fn vscatterqps(&mut self, dst: Mem, src: Ymm) {
        self.emit_n(
            InstId::Vscatterqps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX512_F{k}
    }
}

impl Vshuff32x4Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vshuff32x4(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vshuff32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> Vshuff32x4Emitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vshuff32x4(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vshuff32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> Vshuff32x4Emitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vshuff32x4(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vshuff32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl Vshuff32x4Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vshuff32x4(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vshuff32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> Vshuff32x4Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vshuff32x4(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vshuff32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> Vshuff32x4Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vshuff32x4(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vshuff32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl Vshuff64x2Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vshuff64x2(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vshuff64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> Vshuff64x2Emitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vshuff64x2(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vshuff64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> Vshuff64x2Emitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vshuff64x2(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vshuff64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl Vshuff64x2Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vshuff64x2(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vshuff64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> Vshuff64x2Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vshuff64x2(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vshuff64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> Vshuff64x2Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vshuff64x2(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vshuff64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl Vshufi32x4Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vshufi32x4(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vshufi32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> Vshufi32x4Emitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vshufi32x4(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vshufi32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> Vshufi32x4Emitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vshufi32x4(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vshufi32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl Vshufi32x4Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vshufi32x4(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vshufi32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> Vshufi32x4Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vshufi32x4(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vshufi32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> Vshufi32x4Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vshufi32x4(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vshufi32x4 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl Vshufi64x2Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vshufi64x2(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vshufi64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> Vshufi64x2Emitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vshufi64x2(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vshufi64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> Vshufi64x2Emitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vshufi64x2(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vshufi64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl Vshufi64x2Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vshufi64x2(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vshufi64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> Vshufi64x2Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vshufi64x2(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vshufi64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> Vshufi64x2Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vshufi64x2(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vshufi64x2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VshufpdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vshufpd(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vshufpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VshufpdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vshufpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vshufpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VshufpdEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vshufpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vshufpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VshufpdEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vshufpd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vshufpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VshufpdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vshufpd(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vshufpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VshufpdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vshufpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vshufpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VshufpdEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vshufpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vshufpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VshufpdEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vshufpd(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vshufpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VshufpsEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vshufps(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vshufps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VshufpsEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vshufps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vshufps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VshufpsEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vshufps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vshufps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VshufpsEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vshufps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vshufps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VshufpsEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vshufps(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vshufps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VshufpsEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vshufps(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vshufps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VshufpsEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vshufps(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vshufps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VshufpsEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vshufps(&mut self, dst: Zmm, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vshufps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsqrtpdEmitter<Vec, Vec> for Assembler<'_> {
    fn vsqrtpd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vsqrtpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsqrtpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vsqrtpd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vsqrtpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsqrtpdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vsqrtpd(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vsqrtpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsqrtpdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vsqrtpd(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vsqrtpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsqrtpdEmitter<Vec, Mem> for Assembler<'_> {
    fn vsqrtpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vsqrtpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsqrtpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vsqrtpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vsqrtpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsqrtpdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vsqrtpd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vsqrtpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsqrtpdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vsqrtpd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vsqrtpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsqrtpsEmitter<Vec, Vec> for Assembler<'_> {
    fn vsqrtps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vsqrtps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsqrtpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vsqrtps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vsqrtps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsqrtpsEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vsqrtps(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vsqrtps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsqrtpsEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vsqrtps(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vsqrtps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsqrtpsEmitter<Vec, Mem> for Assembler<'_> {
    fn vsqrtps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vsqrtps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsqrtpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vsqrtps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vsqrtps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsqrtpsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vsqrtps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vsqrtps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsqrtpsEmitter<Zmm, Mem> for Assembler<'_> {
    fn vsqrtps(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vsqrtps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsqrtsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsqrtsd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vsqrtsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsqrtsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsqrtsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vsqrtsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsqrtsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsqrtsd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vsqrtsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsqrtsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsqrtsd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vsqrtsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsqrtssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsqrtss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vsqrtss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsqrtssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsqrtss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vsqrtss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsqrtssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsqrtss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vsqrtss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsqrtssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsqrtss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vsqrtss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VstmxcsrEmitter<Mem> for Assembler<'_> {
    fn vstmxcsr(&mut self, dst: Mem) {
        self.emit_n(InstId::Vstmxcsr as u32, &[dst.as_operand()]); // AVX
    }
}

impl VsubpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsubpd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vsubpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsubpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsubpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vsubpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsubpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vsubpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vsubpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsubpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vsubpd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vsubpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsubpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsubpd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vsubpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsubpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsubpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vsubpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsubpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vsubpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vsubpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsubpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vsubpd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vsubpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsubpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsubps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vsubps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsubpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsubps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vsubps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsubpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vsubps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vsubps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsubpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vsubps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vsubps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsubpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsubps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vsubps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsubpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsubps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vsubps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsubpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vsubps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vsubps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsubpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vsubps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vsubps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsubsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsubsd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vsubsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsubsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsubsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vsubsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsubsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsubsd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vsubsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsubsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsubsd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vsubsd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsubssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsubss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vsubss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsubssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsubss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vsubss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsubssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsubss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vsubss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsubssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsubss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vsubss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VtestpdEmitter<Vec, Vec> for Assembler<'_> {
    fn vtestpd(&mut self, lhs: Vec, rhs: Vec) {
        self.emit_n(
            InstId::Vtestpd as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX
    }
}

impl VtestpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vtestpd(&mut self, lhs: Xmm, rhs: Xmm) {
        self.emit_n(
            InstId::Vtestpd as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX
    }
}

impl VtestpdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vtestpd(&mut self, lhs: Ymm, rhs: Ymm) {
        self.emit_n(
            InstId::Vtestpd as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX
    }
}

impl VtestpdEmitter<Vec, Mem> for Assembler<'_> {
    fn vtestpd(&mut self, lhs: Vec, rhs: Mem) {
        self.emit_n(
            InstId::Vtestpd as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX
    }
}

impl VtestpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vtestpd(&mut self, lhs: Xmm, rhs: Mem) {
        self.emit_n(
            InstId::Vtestpd as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX
    }
}

impl VtestpdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vtestpd(&mut self, lhs: Ymm, rhs: Mem) {
        self.emit_n(
            InstId::Vtestpd as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX
    }
}

impl VtestpsEmitter<Vec, Vec> for Assembler<'_> {
    fn vtestps(&mut self, lhs: Vec, rhs: Vec) {
        self.emit_n(
            InstId::Vtestps as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX
    }
}

impl VtestpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vtestps(&mut self, lhs: Xmm, rhs: Xmm) {
        self.emit_n(
            InstId::Vtestps as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX
    }
}

impl VtestpsEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vtestps(&mut self, lhs: Ymm, rhs: Ymm) {
        self.emit_n(
            InstId::Vtestps as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX
    }
}

impl VtestpsEmitter<Vec, Mem> for Assembler<'_> {
    fn vtestps(&mut self, lhs: Vec, rhs: Mem) {
        self.emit_n(
            InstId::Vtestps as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX
    }
}

impl VtestpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vtestps(&mut self, lhs: Xmm, rhs: Mem) {
        self.emit_n(
            InstId::Vtestps as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX
    }
}

impl VtestpsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vtestps(&mut self, lhs: Ymm, rhs: Mem) {
        self.emit_n(
            InstId::Vtestps as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX
    }
}

impl VucomisdEmitter<Vec, Vec> for Assembler<'_> {
    fn vucomisd(&mut self, lhs: Vec, rhs: Vec) {
        self.emit_n(
            InstId::Vucomisd as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VucomisdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vucomisd(&mut self, lhs: Xmm, rhs: Xmm) {
        self.emit_n(
            InstId::Vucomisd as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VucomisdEmitter<Vec, Mem> for Assembler<'_> {
    fn vucomisd(&mut self, lhs: Vec, rhs: Mem) {
        self.emit_n(
            InstId::Vucomisd as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VucomisdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vucomisd(&mut self, lhs: Xmm, rhs: Mem) {
        self.emit_n(
            InstId::Vucomisd as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VucomissEmitter<Vec, Vec> for Assembler<'_> {
    fn vucomiss(&mut self, lhs: Vec, rhs: Vec) {
        self.emit_n(
            InstId::Vucomiss as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VucomissEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vucomiss(&mut self, lhs: Xmm, rhs: Xmm) {
        self.emit_n(
            InstId::Vucomiss as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VucomissEmitter<Vec, Mem> for Assembler<'_> {
    fn vucomiss(&mut self, lhs: Vec, rhs: Mem) {
        self.emit_n(
            InstId::Vucomiss as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VucomissEmitter<Xmm, Mem> for Assembler<'_> {
    fn vucomiss(&mut self, lhs: Xmm, rhs: Mem) {
        self.emit_n(
            InstId::Vucomiss as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VunpckhpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vunpckhpd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vunpckhpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpckhpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vunpckhpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vunpckhpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpckhpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vunpckhpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vunpckhpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpckhpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vunpckhpd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vunpckhpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpckhpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vunpckhpd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vunpckhpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpckhpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vunpckhpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vunpckhpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpckhpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vunpckhpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vunpckhpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpckhpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vunpckhpd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vunpckhpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpckhpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vunpckhps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vunpckhps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpckhpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vunpckhps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vunpckhps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpckhpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vunpckhps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vunpckhps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpckhpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vunpckhps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vunpckhps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpckhpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vunpckhps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vunpckhps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpckhpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vunpckhps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vunpckhps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpckhpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vunpckhps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vunpckhps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpckhpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vunpckhps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vunpckhps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpcklpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vunpcklpd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vunpcklpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpcklpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vunpcklpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vunpcklpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpcklpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vunpcklpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vunpcklpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpcklpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vunpcklpd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vunpcklpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpcklpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vunpcklpd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vunpcklpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpcklpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vunpcklpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vunpcklpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpcklpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vunpcklpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vunpcklpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpcklpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vunpcklpd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vunpcklpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpcklpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vunpcklps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vunpcklps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpcklpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vunpcklps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vunpcklps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpcklpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vunpcklps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vunpcklps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpcklpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vunpcklps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vunpcklps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpcklpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vunpcklps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vunpcklps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpcklpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vunpcklps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vunpcklps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpcklpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vunpcklps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vunpcklps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpcklpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vunpcklps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vunpcklps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VxorpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vxorpd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vxorpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VxorpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vxorpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vxorpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VxorpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vxorpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vxorpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VxorpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vxorpd(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vxorpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VxorpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vxorpd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vxorpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VxorpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vxorpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vxorpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VxorpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vxorpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vxorpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VxorpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vxorpd(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vxorpd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VxorpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vxorps(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vxorps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VxorpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vxorps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vxorps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VxorpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vxorps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vxorps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VxorpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vxorps(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vxorps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VxorpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vxorps(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vxorps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VxorpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vxorps(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vxorps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VxorpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vxorps(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vxorps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VxorpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vxorps(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vxorps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VzeroallEmitter for Assembler<'_> {
    fn vzeroall(&mut self) {
        self.emit_n(InstId::Vzeroall as u32, &[]); // AVX
    }
}

impl VzeroupperEmitter for Assembler<'_> {
    fn vzeroupper(&mut self) {
        self.emit_n(InstId::Vzeroupper as u32, &[]); // AVX
    }
}

impl VfmaddpdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddpd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vfmaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vfmaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpdEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Ymm) {
        self.emit_n(
            InstId::Vfmaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmaddpd(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vfmaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmaddpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vfmaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpdEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfmaddpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: Ymm) {
        self.emit_n(
            InstId::Vfmaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpdEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddpd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vfmaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpdEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vfmaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpdEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Mem) {
        self.emit_n(
            InstId::Vfmaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpsEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddps(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vfmaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpsEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vfmaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpsEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Ymm) {
        self.emit_n(
            InstId::Vfmaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpsEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmaddps(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vfmaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpsEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmaddps(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vfmaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpsEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfmaddps(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: Ymm) {
        self.emit_n(
            InstId::Vfmaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpsEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddps(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vfmaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpsEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vfmaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpsEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Mem) {
        self.emit_n(
            InstId::Vfmaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vfmaddsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vfmaddsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmaddsd(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vfmaddsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmaddsd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vfmaddsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsdEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vfmaddsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsdEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vfmaddsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddssEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddss(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vfmaddss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddssEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vfmaddss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddssEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmaddss(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vfmaddss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddssEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmaddss(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vfmaddss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddssEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddss(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vfmaddss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddssEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vfmaddss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsubpd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vfmaddsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsubpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vfmaddsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpdEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsubpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Ymm) {
        self.emit_n(
            InstId::Vfmaddsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmaddsubpd(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vfmaddsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmaddsubpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vfmaddsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpdEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfmaddsubpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: Ymm) {
        self.emit_n(
            InstId::Vfmaddsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpdEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsubpd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vfmaddsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpdEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsubpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vfmaddsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpdEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsubpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Mem) {
        self.emit_n(
            InstId::Vfmaddsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpsEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsubps(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vfmaddsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpsEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsubps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vfmaddsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpsEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsubps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Ymm) {
        self.emit_n(
            InstId::Vfmaddsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpsEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmaddsubps(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vfmaddsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpsEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmaddsubps(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vfmaddsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpsEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfmaddsubps(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: Ymm) {
        self.emit_n(
            InstId::Vfmaddsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpsEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsubps(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vfmaddsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpsEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsubps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vfmaddsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpsEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsubps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Mem) {
        self.emit_n(
            InstId::Vfmaddsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubaddpd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vfmsubaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubaddpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vfmsubaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpdEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubaddpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Ymm) {
        self.emit_n(
            InstId::Vfmsubaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmsubaddpd(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vfmsubaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmsubaddpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vfmsubaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpdEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfmsubaddpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: Ymm) {
        self.emit_n(
            InstId::Vfmsubaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpdEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubaddpd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vfmsubaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpdEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubaddpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vfmsubaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpdEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubaddpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Mem) {
        self.emit_n(
            InstId::Vfmsubaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpsEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubaddps(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vfmsubaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpsEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubaddps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vfmsubaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpsEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubaddps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Ymm) {
        self.emit_n(
            InstId::Vfmsubaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpsEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmsubaddps(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vfmsubaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpsEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmsubaddps(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vfmsubaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpsEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfmsubaddps(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: Ymm) {
        self.emit_n(
            InstId::Vfmsubaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpsEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubaddps(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vfmsubaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpsEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubaddps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vfmsubaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpsEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubaddps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Mem) {
        self.emit_n(
            InstId::Vfmsubaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubpd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vfmsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vfmsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpdEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Ymm) {
        self.emit_n(
            InstId::Vfmsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmsubpd(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vfmsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmsubpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vfmsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpdEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfmsubpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: Ymm) {
        self.emit_n(
            InstId::Vfmsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpdEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubpd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vfmsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpdEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vfmsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpdEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Mem) {
        self.emit_n(
            InstId::Vfmsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpsEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubps(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vfmsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpsEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vfmsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpsEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Ymm) {
        self.emit_n(
            InstId::Vfmsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpsEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmsubps(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vfmsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpsEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmsubps(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vfmsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpsEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfmsubps(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: Ymm) {
        self.emit_n(
            InstId::Vfmsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpsEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubps(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vfmsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpsEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vfmsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpsEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Mem) {
        self.emit_n(
            InstId::Vfmsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubsdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubsd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vfmsubsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubsdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vfmsubsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubsdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmsubsd(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vfmsubsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubsdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmsubsd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vfmsubsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubsdEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubsd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vfmsubsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubsdEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vfmsubsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubssEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubss(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vfmsubss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubssEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vfmsubss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubssEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmsubss(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vfmsubss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubssEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmsubss(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vfmsubss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubssEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubss(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vfmsubss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubssEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vfmsubss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmaddpd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vfnmaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmaddpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vfnmaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpdEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmaddpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Ymm) {
        self.emit_n(
            InstId::Vfnmaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfnmaddpd(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vfnmaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfnmaddpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vfnmaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpdEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfnmaddpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: Ymm) {
        self.emit_n(
            InstId::Vfnmaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpdEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmaddpd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vfnmaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpdEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmaddpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vfnmaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpdEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmaddpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Mem) {
        self.emit_n(
            InstId::Vfnmaddpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpsEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmaddps(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vfnmaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpsEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmaddps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vfnmaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpsEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmaddps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Ymm) {
        self.emit_n(
            InstId::Vfnmaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpsEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfnmaddps(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vfnmaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpsEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfnmaddps(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vfnmaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpsEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfnmaddps(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: Ymm) {
        self.emit_n(
            InstId::Vfnmaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpsEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmaddps(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vfnmaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpsEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmaddps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vfnmaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpsEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmaddps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Mem) {
        self.emit_n(
            InstId::Vfnmaddps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddsdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmaddsd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vfnmaddsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddsdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmaddsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vfnmaddsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddsdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfnmaddsd(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vfnmaddsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddsdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfnmaddsd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vfnmaddsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddsdEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmaddsd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vfnmaddsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddsdEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmaddsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vfnmaddsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddssEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmaddss(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vfnmaddss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddssEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmaddss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vfnmaddss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddssEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfnmaddss(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vfnmaddss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddssEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfnmaddss(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vfnmaddss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddssEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmaddss(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vfnmaddss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddssEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmaddss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vfnmaddss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsubpd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vfnmsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsubpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vfnmsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpdEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsubpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Ymm) {
        self.emit_n(
            InstId::Vfnmsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfnmsubpd(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vfnmsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfnmsubpd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vfnmsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpdEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfnmsubpd(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: Ymm) {
        self.emit_n(
            InstId::Vfnmsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpdEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsubpd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vfnmsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpdEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsubpd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vfnmsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpdEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsubpd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Mem) {
        self.emit_n(
            InstId::Vfnmsubpd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpsEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsubps(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vfnmsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpsEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsubps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vfnmsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpsEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsubps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Ymm) {
        self.emit_n(
            InstId::Vfnmsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpsEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfnmsubps(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vfnmsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpsEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfnmsubps(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vfnmsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpsEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfnmsubps(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: Ymm) {
        self.emit_n(
            InstId::Vfnmsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpsEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsubps(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vfnmsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpsEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsubps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vfnmsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpsEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsubps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Mem) {
        self.emit_n(
            InstId::Vfnmsubps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubsdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsubsd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vfnmsubsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubsdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsubsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vfnmsubsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubsdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfnmsubsd(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vfnmsubsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubsdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfnmsubsd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vfnmsubsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubsdEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsubsd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vfnmsubsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubsdEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsubsd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vfnmsubsd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubssEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsubss(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vfnmsubss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubssEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsubss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vfnmsubss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubssEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfnmsubss(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vfnmsubss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubssEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfnmsubss(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vfnmsubss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubssEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsubss(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vfnmsubss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubssEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsubss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vfnmsubss as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfrczpdEmitter<Vec, Vec> for Assembler<'_> {
    fn vfrczpd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vfrczpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VfrczpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vfrczpd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vfrczpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VfrczpdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vfrczpd(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vfrczpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VfrczpdEmitter<Vec, Mem> for Assembler<'_> {
    fn vfrczpd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vfrczpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VfrczpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vfrczpd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vfrczpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VfrczpdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vfrczpd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vfrczpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VfrczpsEmitter<Vec, Vec> for Assembler<'_> {
    fn vfrczps(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vfrczps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VfrczpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vfrczps(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vfrczps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VfrczpsEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vfrczps(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vfrczps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VfrczpsEmitter<Vec, Mem> for Assembler<'_> {
    fn vfrczps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vfrczps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VfrczpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vfrczps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vfrczps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VfrczpsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vfrczps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vfrczps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VfrczsdEmitter<Vec, Vec> for Assembler<'_> {
    fn vfrczsd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vfrczsd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VfrczsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vfrczsd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vfrczsd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VfrczsdEmitter<Vec, Mem> for Assembler<'_> {
    fn vfrczsd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vfrczsd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VfrczsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vfrczsd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vfrczsd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VfrczssEmitter<Vec, Vec> for Assembler<'_> {
    fn vfrczss(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vfrczss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VfrczssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vfrczss(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vfrczss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VfrczssEmitter<Vec, Mem> for Assembler<'_> {
    fn vfrczss(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vfrczss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VfrczssEmitter<Xmm, Mem> for Assembler<'_> {
    fn vfrczss(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vfrczss as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VpcmovEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpcmov(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vpcmov as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpcmovEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpcmov(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vpcmov as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpcmovEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpcmov(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Ymm) {
        self.emit_n(
            InstId::Vpcmov as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpcmovEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpcmov(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vpcmov as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpcmovEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpcmov(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vpcmov as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpcmovEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vpcmov(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: Ymm) {
        self.emit_n(
            InstId::Vpcmov as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpcmovEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vpcmov(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vpcmov as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpcmovEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpcmov(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vpcmov as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpcmovEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpcmov(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Mem) {
        self.emit_n(
            InstId::Vpcmov as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpcombEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcomb(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpcomb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcombEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcomb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpcomb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcombEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcomb(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpcomb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcombEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcomb(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcomb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcomd(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpcomd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcomd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpcomd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcomd(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpcomd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcomd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcomd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomqEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcomq(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpcomq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomqEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcomq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpcomq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomqEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcomq(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpcomq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomqEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcomq(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcomq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomwEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcomw(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpcomw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomwEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcomw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpcomw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomwEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcomw(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpcomw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomwEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcomw(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcomw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomubEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcomub(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpcomub as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomubEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcomub(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpcomub as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomubEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcomub(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpcomub as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomubEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcomub(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcomub as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomudEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcomud(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpcomud as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomudEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcomud(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpcomud as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomudEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcomud(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpcomud as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomudEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcomud(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcomud as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomuqEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcomuq(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpcomuq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomuqEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcomuq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpcomuq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomuqEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcomuq(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpcomuq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomuqEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcomuq(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcomuq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomuwEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcomuw(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpcomuw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomuwEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcomuw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vpcomuw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomuwEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcomuw(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpcomuw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomuwEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcomuw(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vpcomuw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        ); // XOP
    }
}

impl Vpermil2pdEmitter<Vec, Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpermil2pd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpermil2pd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2pdEmitter<Xmm, Xmm, Xmm, Xmm, U4> for Assembler<'_> {
    fn vpermil2pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm, src4: U4) {
        self.emit_n(
            InstId::Vpermil2pd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
                Into::<Imm>::into(src4).as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2pdEmitter<Ymm, Ymm, Ymm, Ymm, U4> for Assembler<'_> {
    fn vpermil2pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Ymm, src4: U4) {
        self.emit_n(
            InstId::Vpermil2pd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
                Into::<Imm>::into(src4).as_operand(),
            ],
        ); // XOP
    }
}

impl Vpermil2pdEmitter<Vec, Vec, Mem, Vec, Imm> for Assembler<'_> {
    fn vpermil2pd(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpermil2pd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2pdEmitter<Xmm, Xmm, Mem, Xmm, U4> for Assembler<'_> {
    fn vpermil2pd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm, src4: U4) {
        self.emit_n(
            InstId::Vpermil2pd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
                Into::<Imm>::into(src4).as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2pdEmitter<Ymm, Ymm, Mem, Ymm, U4> for Assembler<'_> {
    fn vpermil2pd(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: Ymm, src4: U4) {
        self.emit_n(
            InstId::Vpermil2pd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
                Into::<Imm>::into(src4).as_operand(),
            ],
        ); // XOP
    }
}

impl Vpermil2pdEmitter<Vec, Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpermil2pd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpermil2pd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2pdEmitter<Xmm, Xmm, Xmm, Mem, U4> for Assembler<'_> {
    fn vpermil2pd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem, src4: U4) {
        self.emit_n(
            InstId::Vpermil2pd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
                Into::<Imm>::into(src4).as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2pdEmitter<Ymm, Ymm, Ymm, Mem, U4> for Assembler<'_> {
    fn vpermil2pd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Mem, src4: U4) {
        self.emit_n(
            InstId::Vpermil2pd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
                Into::<Imm>::into(src4).as_operand(),
            ],
        ); // XOP
    }
}

impl Vpermil2psEmitter<Vec, Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpermil2ps(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpermil2ps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2psEmitter<Xmm, Xmm, Xmm, Xmm, U4> for Assembler<'_> {
    fn vpermil2ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm, src4: U4) {
        self.emit_n(
            InstId::Vpermil2ps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
                Into::<Imm>::into(src4).as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2psEmitter<Ymm, Ymm, Ymm, Ymm, U4> for Assembler<'_> {
    fn vpermil2ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Ymm, src4: U4) {
        self.emit_n(
            InstId::Vpermil2ps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
                Into::<Imm>::into(src4).as_operand(),
            ],
        ); // XOP
    }
}

impl Vpermil2psEmitter<Vec, Vec, Mem, Vec, Imm> for Assembler<'_> {
    fn vpermil2ps(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpermil2ps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2psEmitter<Xmm, Xmm, Mem, Xmm, U4> for Assembler<'_> {
    fn vpermil2ps(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm, src4: U4) {
        self.emit_n(
            InstId::Vpermil2ps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
                Into::<Imm>::into(src4).as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2psEmitter<Ymm, Ymm, Mem, Ymm, U4> for Assembler<'_> {
    fn vpermil2ps(&mut self, dst: Ymm, src1: Ymm, src2: Mem, src3: Ymm, src4: U4) {
        self.emit_n(
            InstId::Vpermil2ps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
                Into::<Imm>::into(src4).as_operand(),
            ],
        ); // XOP
    }
}

impl Vpermil2psEmitter<Vec, Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpermil2ps(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpermil2ps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
                imm.as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2psEmitter<Xmm, Xmm, Xmm, Mem, U4> for Assembler<'_> {
    fn vpermil2ps(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem, src4: U4) {
        self.emit_n(
            InstId::Vpermil2ps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
                Into::<Imm>::into(src4).as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2psEmitter<Ymm, Ymm, Ymm, Mem, U4> for Assembler<'_> {
    fn vpermil2ps(&mut self, dst: Ymm, src1: Ymm, src2: Ymm, src3: Mem, src4: U4) {
        self.emit_n(
            InstId::Vpermil2ps as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
                Into::<Imm>::into(src4).as_operand(),
            ],
        ); // XOP
    }
}

impl VphaddbdEmitter<Vec, Vec> for Assembler<'_> {
    fn vphaddbd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vphaddbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddbdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphaddbd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vphaddbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddbdEmitter<Vec, Mem> for Assembler<'_> {
    fn vphaddbd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vphaddbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddbdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphaddbd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vphaddbd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddbqEmitter<Vec, Vec> for Assembler<'_> {
    fn vphaddbq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vphaddbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddbqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphaddbq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vphaddbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddbqEmitter<Vec, Mem> for Assembler<'_> {
    fn vphaddbq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vphaddbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddbqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphaddbq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vphaddbq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddbwEmitter<Vec, Vec> for Assembler<'_> {
    fn vphaddbw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vphaddbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddbwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphaddbw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vphaddbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddbwEmitter<Vec, Mem> for Assembler<'_> {
    fn vphaddbw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vphaddbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddbwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphaddbw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vphaddbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphadddqEmitter<Vec, Vec> for Assembler<'_> {
    fn vphadddq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vphadddq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphadddqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphadddq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vphadddq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphadddqEmitter<Vec, Mem> for Assembler<'_> {
    fn vphadddq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vphadddq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphadddqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphadddq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vphadddq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddwdEmitter<Vec, Vec> for Assembler<'_> {
    fn vphaddwd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vphaddwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddwdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphaddwd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vphaddwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddwdEmitter<Vec, Mem> for Assembler<'_> {
    fn vphaddwd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vphaddwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddwdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphaddwd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vphaddwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddwqEmitter<Vec, Vec> for Assembler<'_> {
    fn vphaddwq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vphaddwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddwqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphaddwq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vphaddwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddwqEmitter<Vec, Mem> for Assembler<'_> {
    fn vphaddwq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vphaddwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddwqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphaddwq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vphaddwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddubdEmitter<Vec, Vec> for Assembler<'_> {
    fn vphaddubd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vphaddubd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddubdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphaddubd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vphaddubd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddubdEmitter<Vec, Mem> for Assembler<'_> {
    fn vphaddubd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vphaddubd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddubdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphaddubd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vphaddubd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddubqEmitter<Vec, Vec> for Assembler<'_> {
    fn vphaddubq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vphaddubq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddubqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphaddubq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vphaddubq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddubqEmitter<Vec, Mem> for Assembler<'_> {
    fn vphaddubq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vphaddubq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddubqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphaddubq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vphaddubq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddubwEmitter<Vec, Vec> for Assembler<'_> {
    fn vphaddubw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vphaddubw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddubwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphaddubw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vphaddubw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddubwEmitter<Vec, Mem> for Assembler<'_> {
    fn vphaddubw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vphaddubw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddubwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphaddubw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vphaddubw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddudqEmitter<Vec, Vec> for Assembler<'_> {
    fn vphaddudq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vphaddudq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddudqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphaddudq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vphaddudq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddudqEmitter<Vec, Mem> for Assembler<'_> {
    fn vphaddudq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vphaddudq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphaddudqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphaddudq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vphaddudq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphadduwdEmitter<Vec, Vec> for Assembler<'_> {
    fn vphadduwd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vphadduwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphadduwdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphadduwd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vphadduwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphadduwdEmitter<Vec, Mem> for Assembler<'_> {
    fn vphadduwd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vphadduwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphadduwdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphadduwd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vphadduwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphadduwqEmitter<Vec, Vec> for Assembler<'_> {
    fn vphadduwq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vphadduwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphadduwqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphadduwq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vphadduwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphadduwqEmitter<Vec, Mem> for Assembler<'_> {
    fn vphadduwq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vphadduwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphadduwqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphadduwq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vphadduwq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphsubbwEmitter<Vec, Vec> for Assembler<'_> {
    fn vphsubbw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vphsubbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphsubbwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphsubbw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vphsubbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphsubbwEmitter<Vec, Mem> for Assembler<'_> {
    fn vphsubbw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vphsubbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphsubbwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphsubbw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vphsubbw as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphsubdqEmitter<Vec, Vec> for Assembler<'_> {
    fn vphsubdq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vphsubdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphsubdqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphsubdq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vphsubdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphsubdqEmitter<Vec, Mem> for Assembler<'_> {
    fn vphsubdq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vphsubdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphsubdqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphsubdq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vphsubdq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphsubwdEmitter<Vec, Vec> for Assembler<'_> {
    fn vphsubwd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vphsubwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphsubwdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphsubwd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vphsubwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphsubwdEmitter<Vec, Mem> for Assembler<'_> {
    fn vphsubwd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vphsubwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VphsubwdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphsubwd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vphsubwd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // XOP
    }
}

impl VpmacsddEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmacsdd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vpmacsdd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsddEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmacsdd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vpmacsdd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsddEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmacsdd(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vpmacsdd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsddEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmacsdd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vpmacsdd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsdqhEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmacsdqh(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vpmacsdqh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsdqhEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmacsdqh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vpmacsdqh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsdqhEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmacsdqh(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vpmacsdqh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsdqhEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmacsdqh(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vpmacsdqh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsdqlEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmacsdql(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vpmacsdql as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsdqlEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmacsdql(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vpmacsdql as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsdqlEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmacsdql(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vpmacsdql as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsdqlEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmacsdql(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vpmacsdql as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacswdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmacswd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vpmacswd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacswdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmacswd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vpmacswd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacswdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmacswd(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vpmacswd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacswdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmacswd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vpmacswd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacswwEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmacsww(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vpmacsww as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacswwEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmacsww(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vpmacsww as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacswwEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmacsww(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vpmacsww as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacswwEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmacsww(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vpmacsww as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssddEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmacssdd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vpmacssdd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssddEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmacssdd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vpmacssdd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssddEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmacssdd(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vpmacssdd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssddEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmacssdd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vpmacssdd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssdqhEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmacssdqh(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vpmacssdqh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssdqhEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmacssdqh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vpmacssdqh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssdqhEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmacssdqh(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vpmacssdqh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssdqhEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmacssdqh(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vpmacssdqh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssdqlEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmacssdql(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vpmacssdql as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssdqlEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmacssdql(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vpmacssdql as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssdqlEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmacssdql(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vpmacssdql as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssdqlEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmacssdql(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vpmacssdql as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsswdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmacsswd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vpmacsswd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsswdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmacsswd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vpmacsswd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsswdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmacsswd(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vpmacsswd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsswdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmacsswd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vpmacsswd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsswwEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmacssww(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vpmacssww as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsswwEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmacssww(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vpmacssww as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsswwEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmacssww(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vpmacssww as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsswwEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmacssww(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vpmacssww as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmadcsswdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmadcsswd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vpmadcsswd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmadcsswdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmadcsswd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vpmadcsswd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmadcsswdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmadcsswd(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vpmadcsswd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmadcsswdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmadcsswd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vpmadcsswd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmadcswdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmadcswd(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vpmadcswd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmadcswdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmadcswd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vpmadcswd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmadcswdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmadcswd(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vpmadcswd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmadcswdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmadcswd(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vpmadcswd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VppermEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpperm(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Vec) {
        self.emit_n(
            InstId::Vpperm as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VppermEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpperm(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Xmm) {
        self.emit_n(
            InstId::Vpperm as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VppermEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpperm(&mut self, dst: Vec, src1: Vec, src2: Mem, src3: Vec) {
        self.emit_n(
            InstId::Vpperm as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VppermEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpperm(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: Xmm) {
        self.emit_n(
            InstId::Vpperm as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VppermEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vpperm(&mut self, dst: Vec, src1: Vec, src2: Vec, src3: Mem) {
        self.emit_n(
            InstId::Vpperm as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VppermEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpperm(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: Mem) {
        self.emit_n(
            InstId::Vpperm as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                src3.as_operand(),
            ],
        ); // XOP
    }
}

impl VprotbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vprotb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vprotb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vprotb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vprotb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotbEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vprotb(&mut self, dst: Vec, src1: Mem, src2: Vec) {
        self.emit_n(
            InstId::Vprotb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotbEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vprotb(&mut self, dst: Xmm, src1: Mem, src2: Xmm) {
        self.emit_n(
            InstId::Vprotb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vprotb(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vprotb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vprotb(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vprotb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotbEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vprotb(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vprotb as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // XOP
    }
}

impl<U2: Into<Imm>> VprotbEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vprotb(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vprotb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // XOP
    }
}

impl VprotbEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vprotb(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vprotb as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // XOP
    }
}

impl<U2: Into<Imm>> VprotbEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vprotb(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vprotb as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // XOP
    }
}

impl VprotdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vprotd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vprotd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vprotd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vprotd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotdEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vprotd(&mut self, dst: Vec, src1: Mem, src2: Vec) {
        self.emit_n(
            InstId::Vprotd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotdEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vprotd(&mut self, dst: Xmm, src1: Mem, src2: Xmm) {
        self.emit_n(
            InstId::Vprotd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vprotd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vprotd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vprotd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vprotd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vprotd(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vprotd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // XOP
    }
}

impl<U2: Into<Imm>> VprotdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vprotd(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vprotd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // XOP
    }
}

impl VprotdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vprotd(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vprotd as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // XOP
    }
}

impl<U2: Into<Imm>> VprotdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vprotd(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vprotd as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // XOP
    }
}

impl VprotqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vprotq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vprotq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vprotq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vprotq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotqEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vprotq(&mut self, dst: Vec, src1: Mem, src2: Vec) {
        self.emit_n(
            InstId::Vprotq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotqEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vprotq(&mut self, dst: Xmm, src1: Mem, src2: Xmm) {
        self.emit_n(
            InstId::Vprotq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vprotq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vprotq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vprotq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vprotq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotqEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vprotq(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vprotq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // XOP
    }
}

impl<U2: Into<Imm>> VprotqEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vprotq(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vprotq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // XOP
    }
}

impl VprotqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vprotq(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vprotq as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // XOP
    }
}

impl<U2: Into<Imm>> VprotqEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vprotq(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vprotq as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // XOP
    }
}

impl VprotwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vprotw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vprotw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vprotw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vprotw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotwEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vprotw(&mut self, dst: Vec, src1: Mem, src2: Vec) {
        self.emit_n(
            InstId::Vprotw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotwEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vprotw(&mut self, dst: Xmm, src1: Mem, src2: Xmm) {
        self.emit_n(
            InstId::Vprotw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vprotw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vprotw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vprotw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vprotw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VprotwEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vprotw(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vprotw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // XOP
    }
}

impl<U2: Into<Imm>> VprotwEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vprotw(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vprotw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // XOP
    }
}

impl VprotwEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vprotw(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vprotw as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // XOP
    }
}

impl<U2: Into<Imm>> VprotwEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vprotw(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vprotw as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        ); // XOP
    }
}

impl VpshabEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshab(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpshab as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshabEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshab(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpshab as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshabEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpshab(&mut self, dst: Vec, src1: Mem, src2: Vec) {
        self.emit_n(
            InstId::Vpshab as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshabEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpshab(&mut self, dst: Xmm, src1: Mem, src2: Xmm) {
        self.emit_n(
            InstId::Vpshab as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshabEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshab(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpshab as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshabEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshab(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshab as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshadEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshad(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpshad as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshadEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshad(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpshad as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshadEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpshad(&mut self, dst: Vec, src1: Mem, src2: Vec) {
        self.emit_n(
            InstId::Vpshad as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshadEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpshad(&mut self, dst: Xmm, src1: Mem, src2: Xmm) {
        self.emit_n(
            InstId::Vpshad as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshadEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshad(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpshad as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshadEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshad(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshad as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshaqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshaq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpshaq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshaqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshaq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpshaq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshaqEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpshaq(&mut self, dst: Vec, src1: Mem, src2: Vec) {
        self.emit_n(
            InstId::Vpshaq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshaqEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpshaq(&mut self, dst: Xmm, src1: Mem, src2: Xmm) {
        self.emit_n(
            InstId::Vpshaq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshaqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshaq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpshaq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshaqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshaq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshaq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshawEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshaw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpshaw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshawEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshaw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpshaw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshawEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpshaw(&mut self, dst: Vec, src1: Mem, src2: Vec) {
        self.emit_n(
            InstId::Vpshaw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshawEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpshaw(&mut self, dst: Xmm, src1: Mem, src2: Xmm) {
        self.emit_n(
            InstId::Vpshaw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshawEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshaw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpshaw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshawEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshaw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshaw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshlbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshlb(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpshlb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshlbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshlb(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpshlb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshlbEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpshlb(&mut self, dst: Vec, src1: Mem, src2: Vec) {
        self.emit_n(
            InstId::Vpshlb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshlbEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpshlb(&mut self, dst: Xmm, src1: Mem, src2: Xmm) {
        self.emit_n(
            InstId::Vpshlb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshlbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshlb(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpshlb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshlbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshlb(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshlb as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshldEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshld(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpshld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshldEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshld(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpshld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshldEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpshld(&mut self, dst: Vec, src1: Mem, src2: Vec) {
        self.emit_n(
            InstId::Vpshld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshldEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpshld(&mut self, dst: Xmm, src1: Mem, src2: Xmm) {
        self.emit_n(
            InstId::Vpshld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshldEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshld(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpshld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshldEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshld(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshld as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshlqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshlq(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpshlq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshlqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshlq(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpshlq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshlqEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpshlq(&mut self, dst: Vec, src1: Mem, src2: Vec) {
        self.emit_n(
            InstId::Vpshlq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshlqEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpshlq(&mut self, dst: Xmm, src1: Mem, src2: Xmm) {
        self.emit_n(
            InstId::Vpshlq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshlqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshlq(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpshlq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshlqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshlq(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshlq as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshlwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshlw(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpshlw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshlwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshlw(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpshlw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshlwEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpshlw(&mut self, dst: Vec, src1: Mem, src2: Vec) {
        self.emit_n(
            InstId::Vpshlw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshlwEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpshlw(&mut self, dst: Xmm, src1: Mem, src2: Xmm) {
        self.emit_n(
            InstId::Vpshlw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshlwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshlw(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpshlw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl VpshlwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshlw(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpshlw as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // XOP
    }
}

impl Vbcstnebf162psEmitter<Vec, Mem> for Assembler<'_> {
    fn vbcstnebf162ps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vbcstnebf162ps as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vbcstnebf162psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vbcstnebf162ps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vbcstnebf162ps as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vbcstnebf162psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vbcstnebf162ps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vbcstnebf162ps as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vbcstnesh2psEmitter<Vec, Mem> for Assembler<'_> {
    fn vbcstnesh2ps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vbcstnesh2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vbcstnesh2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vbcstnesh2ps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vbcstnesh2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vbcstnesh2psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vbcstnesh2ps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vbcstnesh2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtneebf162psEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtneebf162ps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtneebf162ps as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtneebf162psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtneebf162ps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtneebf162ps as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtneebf162psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtneebf162ps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtneebf162ps as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtneeph2psEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtneeph2ps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtneeph2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtneeph2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtneeph2ps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtneeph2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtneeph2psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtneeph2ps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtneeph2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtneobf162psEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtneobf162ps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtneobf162ps as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtneobf162psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtneobf162ps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtneobf162ps as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtneobf162psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtneobf162ps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtneobf162ps as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtneoph2psEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtneoph2ps(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtneoph2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtneoph2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtneoph2ps(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtneoph2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtneoph2psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtneoph2ps(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtneoph2ps as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VpdpbssdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpbssd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpdpbssd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbssdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpbssd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpdpbssd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbssdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpbssd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpdpbssd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbssdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpbssd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbssd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbssdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpbssd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbssd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbssdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpbssd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbssd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbssdsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpbssds(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpdpbssds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbssdsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpbssds(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpdpbssds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbssdsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpbssds(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpdpbssds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbssdsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpbssds(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbssds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbssdsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpbssds(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbssds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbssdsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpbssds(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbssds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbsudEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpbsud(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpdpbsud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbsudEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpbsud(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpdpbsud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbsudEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpbsud(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpdpbsud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbsudEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpbsud(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbsud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbsudEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpbsud(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbsud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbsudEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpbsud(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbsud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbsudsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpbsuds(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpdpbsuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbsudsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpbsuds(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpdpbsuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbsudsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpbsuds(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpdpbsuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbsudsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpbsuds(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbsuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbsudsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpbsuds(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbsuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbsudsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpbsuds(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbsuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbuudEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpbuud(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpdpbuud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbuudEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpbuud(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpdpbuud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbuudEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpbuud(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpdpbuud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbuudEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpbuud(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbuud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbuudEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpbuud(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbuud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbuudEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpbuud(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbuud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbuudsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpbuuds(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpdpbuuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbuudsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpbuuds(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpdpbuuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbuudsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpbuuds(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpdpbuuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbuudsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpbuuds(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbuuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbuudsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpbuuds(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbuuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpbuudsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpbuuds(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpbuuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwsudEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpwsud(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpdpwsud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwsudEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpwsud(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpdpwsud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwsudEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpwsud(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpdpwsud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwsudEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpwsud(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwsud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwsudEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpwsud(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwsud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwsudEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpwsud(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwsud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwsudsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpwsuds(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpdpwsuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwsudsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpwsuds(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpdpwsuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwsudsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpwsuds(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpdpwsuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwsudsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpwsuds(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwsuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwsudsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpwsuds(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwsuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwsudsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpwsuds(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwsuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwusdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpwusd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpdpwusd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwusdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpwusd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpdpwusd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwusdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpwusd(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpdpwusd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwusdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpwusd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwusd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwusdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpwusd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwusd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwusdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpwusd(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwusd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwusdsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpwusds(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpdpwusds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwusdsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpwusds(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpdpwusds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwusdsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpwusds(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpdpwusds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwusdsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpwusds(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwusds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwusdsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpwusds(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwusds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwusdsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpwusds(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwusds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwuudEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpwuud(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpdpwuud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwuudEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpwuud(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpdpwuud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwuudEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpwuud(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpdpwuud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwuudEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpwuud(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwuud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwuudEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpwuud(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwuud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwuudEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpwuud(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwuud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwuudsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpwuuds(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vpdpwuuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwuudsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpwuuds(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vpdpwuuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwuudsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpwuuds(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vpdpwuuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwuudsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpwuuds(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwuuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwuudsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpwuuds(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwuuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VpdpwuudsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpwuuds(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vpdpwuuds as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsha512msg1Emitter<Vec, Vec> for Assembler<'_> {
    fn vsha512msg1(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vsha512msg1 as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vsha512msg1Emitter<Ymm, Xmm> for Assembler<'_> {
    fn vsha512msg1(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vsha512msg1 as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vsha512msg2Emitter<Vec, Vec> for Assembler<'_> {
    fn vsha512msg2(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vsha512msg2 as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vsha512msg2Emitter<Ymm, Ymm> for Assembler<'_> {
    fn vsha512msg2(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vsha512msg2 as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vsha512rnds2Emitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsha512rnds2(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vsha512rnds2 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsha512rnds2Emitter<Ymm, Ymm, Xmm> for Assembler<'_> {
    fn vsha512rnds2(&mut self, dst: Ymm, src1: Ymm, src2: Xmm) {
        self.emit_n(
            InstId::Vsha512rnds2 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsm3msg1Emitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsm3msg1(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vsm3msg1 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsm3msg1Emitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsm3msg1(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vsm3msg1 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsm3msg1Emitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsm3msg1(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vsm3msg1 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsm3msg1Emitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsm3msg1(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vsm3msg1 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsm3msg2Emitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsm3msg2(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vsm3msg2 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsm3msg2Emitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsm3msg2(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vsm3msg2 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsm3msg2Emitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsm3msg2(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vsm3msg2 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsm3msg2Emitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsm3msg2(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vsm3msg2 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsm3rnds2Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vsm3rnds2(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vsm3rnds2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> Vsm3rnds2Emitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vsm3rnds2(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vsm3rnds2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        );
    }
}

impl Vsm3rnds2Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vsm3rnds2(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vsm3rnds2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> Vsm3rnds2Emitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vsm3rnds2(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vsm3rnds2 as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        );
    }
}

impl Vsm4key4Emitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsm4key4(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vsm4key4 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsm4key4Emitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsm4key4(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vsm4key4 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsm4key4Emitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vsm4key4(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vsm4key4 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsm4key4Emitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsm4key4(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vsm4key4 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsm4key4Emitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsm4key4(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vsm4key4 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsm4key4Emitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vsm4key4(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vsm4key4 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsm4rnds4Emitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsm4rnds4(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vsm4rnds4 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsm4rnds4Emitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsm4rnds4(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vsm4rnds4 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsm4rnds4Emitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vsm4rnds4(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vsm4rnds4 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsm4rnds4Emitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsm4rnds4(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vsm4rnds4 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsm4rnds4Emitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsm4rnds4(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vsm4rnds4 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vsm4rnds4Emitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vsm4rnds4(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vsm4rnds4 as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VaddphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaddph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vaddph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VaddphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaddph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vaddph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VaddphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vaddph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vaddph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VaddphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vaddph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vaddph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VaddphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaddph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vaddph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VaddphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaddph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vaddph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VaddphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vaddph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vaddph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VaddphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vaddph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vaddph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VaddshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaddsh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vaddsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VaddshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaddsh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vaddsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VaddshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaddsh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vaddsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VaddshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaddsh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vaddsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VcmpphEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vcmpph(&mut self, dst: KReg, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vcmpph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VcmpphEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vcmpph(&mut self, dst: KReg, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vcmpph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VcmpphEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vcmpph(&mut self, dst: KReg, src1: Ymm, src2: Ymm, src3: U3) {
        self.emit_n(
            InstId::Vcmpph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VcmpphEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vcmpph(&mut self, dst: KReg, src1: Zmm, src2: Zmm, src3: U3) {
        self.emit_n(
            InstId::Vcmpph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        );
    }
}

impl VcmpphEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vcmpph(&mut self, dst: KReg, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vcmpph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VcmpphEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vcmpph(&mut self, dst: KReg, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vcmpph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VcmpphEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vcmpph(&mut self, dst: KReg, src1: Ymm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vcmpph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VcmpphEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vcmpph(&mut self, dst: KReg, src1: Zmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vcmpph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        );
    }
}

impl VcmpshEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vcmpsh(&mut self, dst: KReg, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vcmpsh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VcmpshEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vcmpsh(&mut self, dst: KReg, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vcmpsh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        );
    }
}

impl VcmpshEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vcmpsh(&mut self, dst: KReg, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vcmpsh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VcmpshEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vcmpsh(&mut self, dst: KReg, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vcmpsh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        );
    }
}

impl VcomishEmitter<Vec, Vec> for Assembler<'_> {
    fn vcomish(&mut self, lhs: Vec, rhs: Vec) {
        self.emit_n(
            InstId::Vcomish as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        );
    }
}

impl VcomishEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcomish(&mut self, lhs: Xmm, rhs: Xmm) {
        self.emit_n(
            InstId::Vcomish as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        );
    }
}

impl VcomishEmitter<Vec, Mem> for Assembler<'_> {
    fn vcomish(&mut self, lhs: Vec, rhs: Mem) {
        self.emit_n(
            InstId::Vcomish as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        );
    }
}

impl VcomishEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcomish(&mut self, lhs: Xmm, rhs: Mem) {
        self.emit_n(
            InstId::Vcomish as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        );
    }
}

impl Vcvtdq2phEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtdq2ph(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtdq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtdq2phEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtdq2ph(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtdq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtdq2phEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtdq2ph(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtdq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtdq2phEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvtdq2ph(&mut self, dst: Ymm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtdq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtdq2phEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtdq2ph(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtdq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtdq2phEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtdq2ph(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtdq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtdq2phEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtdq2ph(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtdq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtpd2phEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtpd2ph(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtpd2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtpd2phEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtpd2ph(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtpd2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtpd2phEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtpd2ph(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtpd2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtpd2phEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vcvtpd2ph(&mut self, dst: Xmm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtpd2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtpd2phEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtpd2ph(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtpd2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtpd2phEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtpd2ph(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtpd2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2dqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtph2dq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtph2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2dqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtph2dq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtph2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2dqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtph2dq(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtph2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2dqEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvtph2dq(&mut self, dst: Zmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtph2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2dqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtph2dq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2dqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtph2dq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2dqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtph2dq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2dqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtph2dq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2pdEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtph2pd(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtph2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2pdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtph2pd(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtph2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2pdEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtph2pd(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtph2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2pdEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vcvtph2pd(&mut self, dst: Zmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtph2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2pdEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtph2pd(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtph2pd(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2pdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtph2pd(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2pdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtph2pd(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2pd as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2psxEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtph2psx(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtph2psx as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2psxEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtph2psx(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtph2psx as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2psxEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtph2psx(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtph2psx as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2psxEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvtph2psx(&mut self, dst: Zmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtph2psx as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2psxEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtph2psx(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2psx as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2psxEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtph2psx(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2psx as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2psxEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtph2psx(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2psx as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2psxEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtph2psx(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2psx as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2qqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtph2qq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtph2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2qqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtph2qq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtph2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2qqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtph2qq(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtph2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2qqEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vcvtph2qq(&mut self, dst: Zmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtph2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2qqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtph2qq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2qqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtph2qq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2qqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtph2qq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2qqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtph2qq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2udqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtph2udq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtph2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2udqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtph2udq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtph2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2udqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtph2udq(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtph2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2udqEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvtph2udq(&mut self, dst: Zmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtph2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2udqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtph2udq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2udqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtph2udq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2udqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtph2udq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2udqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtph2udq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2uqqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtph2uqq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtph2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2uqqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtph2uqq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtph2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2uqqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtph2uqq(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtph2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2uqqEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vcvtph2uqq(&mut self, dst: Zmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtph2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2uqqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtph2uqq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2uqqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtph2uqq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2uqqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtph2uqq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2uqqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtph2uqq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2uwEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtph2uw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtph2uw as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2uwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtph2uw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtph2uw as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2uwEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtph2uw(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtph2uw as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2uwEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtph2uw(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtph2uw as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2uwEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtph2uw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2uw as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2uwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtph2uw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2uw as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2uwEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtph2uw(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2uw as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2uwEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtph2uw(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2uw as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2wEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtph2w(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtph2w as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2wEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtph2w(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtph2w as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2wEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtph2w(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtph2w as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2wEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtph2w(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtph2w as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2wEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtph2w(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2w as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2wEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtph2w(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2w as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2wEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtph2w(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2w as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtph2wEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtph2w(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtph2w as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtps2phxEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtps2phx(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtps2phx as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtps2phxEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtps2phx(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtps2phx as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtps2phxEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtps2phx(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtps2phx as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtps2phxEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvtps2phx(&mut self, dst: Ymm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtps2phx as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtps2phxEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtps2phx(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2phx as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtps2phxEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtps2phx(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2phx as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtps2phxEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtps2phx(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtps2phx as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtqq2phEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtqq2ph(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtqq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtqq2phEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtqq2ph(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtqq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtqq2phEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtqq2ph(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtqq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtqq2phEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vcvtqq2ph(&mut self, dst: Xmm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtqq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtqq2phEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtqq2ph(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtqq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtqq2phEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtqq2ph(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtqq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtsd2shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vcvtsd2sh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vcvtsd2sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtsd2shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vcvtsd2sh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vcvtsd2sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtsd2shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtsd2sh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vcvtsd2sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtsd2shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtsd2sh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vcvtsd2sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtsh2sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vcvtsh2sd(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vcvtsh2sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtsh2sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vcvtsh2sd(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vcvtsh2sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtsh2sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtsh2sd(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vcvtsh2sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtsh2sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtsh2sd(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vcvtsh2sd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtsh2siEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvtsh2si(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Vcvtsh2si as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtsh2siEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvtsh2si(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Vcvtsh2si as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtsh2siEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvtsh2si(&mut self, dst: Gpq, src: Xmm) {
        self.emit_n(
            InstId::Vcvtsh2si as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtsh2siEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvtsh2si(&mut self, dst: Gp, src: Mem) {
        self.emit_n(
            InstId::Vcvtsh2si as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtsh2siEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvtsh2si(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(
            InstId::Vcvtsh2si as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtsh2siEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvtsh2si(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(
            InstId::Vcvtsh2si as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtsh2ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vcvtsh2ss(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vcvtsh2ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtsh2ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vcvtsh2ss(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vcvtsh2ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtsh2ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtsh2ss(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vcvtsh2ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtsh2ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtsh2ss(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vcvtsh2ss as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtsh2usiEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvtsh2usi(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Vcvtsh2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtsh2usiEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvtsh2usi(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Vcvtsh2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtsh2usiEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvtsh2usi(&mut self, dst: Gpq, src: Xmm) {
        self.emit_n(
            InstId::Vcvtsh2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtsh2usiEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvtsh2usi(&mut self, dst: Gp, src: Mem) {
        self.emit_n(
            InstId::Vcvtsh2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtsh2usiEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvtsh2usi(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(
            InstId::Vcvtsh2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtsh2usiEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvtsh2usi(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(
            InstId::Vcvtsh2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtsi2shEmitter<Vec, Vec, Gp> for Assembler<'_> {
    fn vcvtsi2sh(&mut self, dst: Vec, src1: Vec, src2: Gp) {
        self.emit_n(
            InstId::Vcvtsi2sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtsi2shEmitter<Xmm, Xmm, Gpd> for Assembler<'_> {
    fn vcvtsi2sh(&mut self, dst: Xmm, src1: Xmm, src2: Gpd) {
        self.emit_n(
            InstId::Vcvtsi2sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtsi2shEmitter<Xmm, Xmm, Gpq> for Assembler<'_> {
    fn vcvtsi2sh(&mut self, dst: Xmm, src1: Xmm, src2: Gpq) {
        self.emit_n(
            InstId::Vcvtsi2sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtsi2shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtsi2sh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vcvtsi2sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtsi2shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtsi2sh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vcvtsi2sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtss2shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vcvtss2sh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vcvtss2sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtss2shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vcvtss2sh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vcvtss2sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtss2shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtss2sh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vcvtss2sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtss2shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtss2sh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vcvtss2sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvttph2dqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttph2dq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvttph2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2dqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttph2dq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttph2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2dqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvttph2dq(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttph2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2dqEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvttph2dq(&mut self, dst: Zmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvttph2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2dqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttph2dq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2dqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttph2dq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2dqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttph2dq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2dqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttph2dq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2dq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2qqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttph2qq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvttph2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2qqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttph2qq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttph2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2qqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvttph2qq(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttph2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2qqEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vcvttph2qq(&mut self, dst: Zmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttph2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2qqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttph2qq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2qqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttph2qq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2qqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttph2qq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2qqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttph2qq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2qq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2udqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttph2udq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvttph2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2udqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttph2udq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttph2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2udqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvttph2udq(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttph2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2udqEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvttph2udq(&mut self, dst: Zmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvttph2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2udqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttph2udq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2udqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttph2udq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2udqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttph2udq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2udqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttph2udq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2udq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2uqqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttph2uqq(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvttph2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2uqqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttph2uqq(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttph2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2uqqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvttph2uqq(&mut self, dst: Ymm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttph2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2uqqEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vcvttph2uqq(&mut self, dst: Zmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttph2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2uqqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttph2uqq(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2uqqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttph2uqq(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2uqqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttph2uqq(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2uqqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttph2uqq(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2uqq as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2uwEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttph2uw(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvttph2uw as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2uwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttph2uw(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttph2uw as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2uwEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvttph2uw(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vcvttph2uw as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2uwEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvttph2uw(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vcvttph2uw as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2uwEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttph2uw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2uw as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2uwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttph2uw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2uw as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2uwEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttph2uw(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2uw as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2uwEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttph2uw(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2uw as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2wEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttph2w(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvttph2w as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2wEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttph2w(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvttph2w as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2wEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvttph2w(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vcvttph2w as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2wEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvttph2w(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vcvttph2w as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2wEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttph2w(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2w as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2wEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttph2w(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2w as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2wEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttph2w(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2w as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttph2wEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttph2w(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvttph2w as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttsh2siEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvttsh2si(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Vcvttsh2si as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttsh2siEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvttsh2si(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Vcvttsh2si as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttsh2siEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvttsh2si(&mut self, dst: Gpq, src: Xmm) {
        self.emit_n(
            InstId::Vcvttsh2si as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttsh2siEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvttsh2si(&mut self, dst: Gp, src: Mem) {
        self.emit_n(
            InstId::Vcvttsh2si as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttsh2siEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvttsh2si(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(
            InstId::Vcvttsh2si as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttsh2siEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvttsh2si(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(
            InstId::Vcvttsh2si as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttsh2usiEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvttsh2usi(&mut self, dst: Gp, src: Vec) {
        self.emit_n(
            InstId::Vcvttsh2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttsh2usiEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvttsh2usi(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(
            InstId::Vcvttsh2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttsh2usiEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvttsh2usi(&mut self, dst: Gpq, src: Xmm) {
        self.emit_n(
            InstId::Vcvttsh2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttsh2usiEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvttsh2usi(&mut self, dst: Gp, src: Mem) {
        self.emit_n(
            InstId::Vcvttsh2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttsh2usiEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvttsh2usi(&mut self, dst: Gpd, src: Mem) {
        self.emit_n(
            InstId::Vcvttsh2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvttsh2usiEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvttsh2usi(&mut self, dst: Gpq, src: Mem) {
        self.emit_n(
            InstId::Vcvttsh2usi as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtudq2phEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtudq2ph(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtudq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtudq2phEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtudq2ph(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtudq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtudq2phEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtudq2ph(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtudq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtudq2phEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvtudq2ph(&mut self, dst: Ymm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtudq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtudq2phEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtudq2ph(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtudq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtudq2phEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtudq2ph(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtudq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtudq2phEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtudq2ph(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtudq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtuqq2phEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtuqq2ph(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtuqq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtuqq2phEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtuqq2ph(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtuqq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtuqq2phEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtuqq2ph(&mut self, dst: Xmm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtuqq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtuqq2phEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vcvtuqq2ph(&mut self, dst: Xmm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtuqq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtuqq2phEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtuqq2ph(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtuqq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtuqq2phEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtuqq2ph(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtuqq2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtusi2shEmitter<Vec, Vec, Gp> for Assembler<'_> {
    fn vcvtusi2sh(&mut self, dst: Vec, src1: Vec, src2: Gp) {
        self.emit_n(
            InstId::Vcvtusi2sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtusi2shEmitter<Xmm, Xmm, Gpd> for Assembler<'_> {
    fn vcvtusi2sh(&mut self, dst: Xmm, src1: Xmm, src2: Gpd) {
        self.emit_n(
            InstId::Vcvtusi2sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtusi2shEmitter<Xmm, Xmm, Gpq> for Assembler<'_> {
    fn vcvtusi2sh(&mut self, dst: Xmm, src1: Xmm, src2: Gpq) {
        self.emit_n(
            InstId::Vcvtusi2sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtusi2shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtusi2sh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vcvtusi2sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtusi2shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtusi2sh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vcvtusi2sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vcvtuw2phEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtuw2ph(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtuw2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtuw2phEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtuw2ph(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtuw2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtuw2phEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtuw2ph(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtuw2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtuw2phEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtuw2ph(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtuw2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtuw2phEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtuw2ph(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtuw2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtuw2phEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtuw2ph(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtuw2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtuw2phEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtuw2ph(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtuw2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtuw2phEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtuw2ph(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtuw2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtw2phEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtw2ph(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vcvtw2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtw2phEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtw2ph(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vcvtw2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtw2phEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtw2ph(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vcvtw2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtw2phEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtw2ph(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vcvtw2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtw2phEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtw2ph(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vcvtw2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtw2phEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtw2ph(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtw2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtw2phEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtw2ph(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vcvtw2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Vcvtw2phEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtw2ph(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vcvtw2ph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VdivphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vdivph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vdivph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VdivphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vdivph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vdivph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VdivphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vdivph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vdivph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VdivphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vdivph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vdivph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VdivphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vdivph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vdivph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VdivphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vdivph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vdivph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VdivphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vdivph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vdivph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VdivphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vdivph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vdivph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VdivshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vdivsh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vdivsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VdivshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vdivsh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vdivsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VdivshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vdivsh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vdivsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VdivshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vdivsh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vdivsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmaddcphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfcmaddcph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfcmaddcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmaddcphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfcmaddcph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfcmaddcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmaddcphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfcmaddcph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfcmaddcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmaddcphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfcmaddcph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfcmaddcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmaddcphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfcmaddcph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfcmaddcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmaddcphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfcmaddcph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfcmaddcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmaddcphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfcmaddcph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfcmaddcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmaddcphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfcmaddcph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfcmaddcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmaddcshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfcmaddcsh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfcmaddcsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmaddcshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfcmaddcsh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfcmaddcsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmaddcshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfcmaddcsh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfcmaddcsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmaddcshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfcmaddcsh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfcmaddcsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmulcphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfcmulcph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfcmulcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmulcphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfcmulcph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfcmulcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmulcphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfcmulcph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfcmulcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmulcphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfcmulcph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfcmulcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmulcphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfcmulcph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfcmulcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmulcphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfcmulcph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfcmulcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmulcphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfcmulcph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfcmulcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmulcphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfcmulcph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfcmulcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmulcshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfcmulcsh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfcmulcsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmulcshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfcmulcsh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfcmulcsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmulcshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfcmulcsh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfcmulcsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfcmulcshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfcmulcsh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfcmulcsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd132phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd132ph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd132phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd132ph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd132phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmadd132ph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd132phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmadd132ph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd132phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd132ph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd132phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd132ph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd132phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmadd132ph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd132phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmadd132ph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd132shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd132sh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmadd132sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd132shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd132sh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmadd132sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd132shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd132sh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd132sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd132shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd132sh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd132sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd213phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd213ph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd213phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd213ph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd213phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmadd213ph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd213phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmadd213ph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd213phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd213ph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd213phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd213ph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd213phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmadd213ph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd213phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmadd213ph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd213shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd213sh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmadd213sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd213shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd213sh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmadd213sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd213shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd213sh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd213sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd213shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd213sh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd213sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd231phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd231ph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd231phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd231ph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd231phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmadd231ph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd231phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmadd231ph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd231phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd231ph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd231phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd231ph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd231phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmadd231ph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd231phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmadd231ph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd231shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd231sh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmadd231sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd231shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd231sh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmadd231sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd231shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd231sh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd231sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmadd231shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd231sh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmadd231sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmaddcphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddcph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmaddcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmaddcphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddcph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmaddcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmaddcphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddcph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmaddcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmaddcphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmaddcph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmaddcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmaddcphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddcph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmaddcphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddcph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmaddcphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddcph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmaddcphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmaddcph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmaddcshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddcsh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmaddcsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmaddcshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddcsh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmaddcsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmaddcshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddcsh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddcsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmaddcshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddcsh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddcsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub132phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsub132ph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmaddsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub132phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsub132ph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmaddsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub132phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsub132ph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmaddsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub132phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmaddsub132ph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmaddsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub132phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsub132ph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub132phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsub132ph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub132phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsub132ph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub132phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmaddsub132ph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub213phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsub213ph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmaddsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub213phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsub213ph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmaddsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub213phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsub213ph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmaddsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub213phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmaddsub213ph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmaddsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub213phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsub213ph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub213phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsub213ph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub213phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsub213ph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub213phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmaddsub213ph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub231phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsub231ph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmaddsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub231phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsub231ph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmaddsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub231phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsub231ph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmaddsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub231phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmaddsub231ph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmaddsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub231phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsub231ph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub231phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsub231ph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub231phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsub231ph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmaddsub231phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmaddsub231ph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub132phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub132ph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub132phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub132ph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub132phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsub132ph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub132phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsub132ph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub132phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub132ph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub132phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub132ph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub132phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsub132ph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub132phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsub132ph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub132shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub132sh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsub132sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub132shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub132sh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsub132sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub132shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub132sh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub132sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub132shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub132sh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub132sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub213phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub213ph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub213phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub213ph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub213phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsub213ph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub213phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsub213ph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub213phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub213ph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub213phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub213ph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub213phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsub213ph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub213phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsub213ph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub213shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub213sh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsub213sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub213shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub213sh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsub213sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub213shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub213sh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub213sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub213shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub213sh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub213sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub231phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub231ph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub231phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub231ph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub231phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsub231ph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub231phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsub231ph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub231phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub231ph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub231phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub231ph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub231phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsub231ph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub231phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsub231ph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub231shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub231sh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsub231sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub231shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub231sh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsub231sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub231shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub231sh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub231sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsub231shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub231sh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsub231sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd132phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubadd132ph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsubadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd132phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubadd132ph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsubadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd132phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubadd132ph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmsubadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd132phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsubadd132ph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmsubadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd132phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubadd132ph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd132phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubadd132ph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd132phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubadd132ph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd132phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsubadd132ph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd213phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubadd213ph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsubadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd213phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubadd213ph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsubadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd213phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubadd213ph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmsubadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd213phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsubadd213ph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmsubadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd213phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubadd213ph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd213phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubadd213ph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd213phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubadd213ph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd213phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsubadd213ph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd231phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubadd231ph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmsubadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd231phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubadd231ph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmsubadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd231phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubadd231ph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmsubadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd231phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsubadd231ph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmsubadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd231phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubadd231ph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd231phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubadd231ph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd231phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubadd231ph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfmsubadd231phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsubadd231ph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmulcphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmulcph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmulcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmulcphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmulcph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmulcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmulcphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmulcph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfmulcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmulcphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmulcph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfmulcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmulcphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmulcph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmulcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmulcphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmulcph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmulcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmulcphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmulcph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfmulcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmulcphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmulcph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmulcph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmulcshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmulcsh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfmulcsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmulcshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmulcsh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfmulcsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmulcshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmulcsh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfmulcsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfmulcshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmulcsh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfmulcsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd132phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd132ph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd132phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd132ph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd132phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmadd132ph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfnmadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd132phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmadd132ph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfnmadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd132phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd132ph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd132phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd132ph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd132phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmadd132ph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd132phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmadd132ph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd132shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd132sh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmadd132sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd132shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd132sh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd132sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd132shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd132sh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd132shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd132sh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd213phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd213ph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd213phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd213ph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd213phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmadd213ph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfnmadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd213phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmadd213ph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfnmadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd213phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd213ph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd213phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd213ph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd213phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmadd213ph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd213phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmadd213ph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd213shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd213sh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmadd213sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd213shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd213sh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd213sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd213shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd213sh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd213shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd213sh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd231phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd231ph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd231phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd231ph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd231phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmadd231ph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfnmadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd231phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmadd231ph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfnmadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd231phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd231ph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd231phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd231ph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd231phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmadd231ph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd231phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmadd231ph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd231shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd231sh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmadd231sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd231shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd231sh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd231sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd231shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd231sh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmadd231shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd231sh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub132phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub132ph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub132phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub132ph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub132phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsub132ph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfnmsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub132phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmsub132ph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfnmsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub132phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub132ph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub132phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub132ph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub132phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsub132ph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub132phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmsub132ph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub132shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub132sh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmsub132sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub132shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub132sh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub132sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub132shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub132sh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub132shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub132sh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub213phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub213ph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub213phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub213ph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub213phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsub213ph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfnmsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub213phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmsub213ph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfnmsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub213phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub213ph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub213phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub213ph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub213phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsub213ph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub213phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmsub213ph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub213shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub213sh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmsub213sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub213shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub213sh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub213sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub213shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub213sh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub213shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub213sh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub231phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub231ph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub231phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub231ph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub231phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsub231ph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vfnmsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub231phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmsub231ph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vfnmsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub231phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub231ph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub231phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub231ph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub231phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsub231ph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub231phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmsub231ph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231ph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub231shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub231sh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vfnmsub231sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub231shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub231sh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub231sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub231shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub231sh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vfnmsub231shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub231sh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231sh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VfpclassphEmitter<KReg, Vec, Imm> for Assembler<'_> {
    fn vfpclassph(&mut self, dst: KReg, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vfpclassph as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        );
    }
}

impl<U2: Into<Imm>> VfpclassphEmitter<KReg, Xmm, U2> for Assembler<'_> {
    fn vfpclassph(&mut self, dst: KReg, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vfpclassph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VfpclassphEmitter<KReg, Ymm, U2> for Assembler<'_> {
    fn vfpclassph(&mut self, dst: KReg, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vfpclassph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VfpclassphEmitter<KReg, Zmm, U2> for Assembler<'_> {
    fn vfpclassph(&mut self, dst: KReg, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vfpclassph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VfpclassphEmitter<KReg, Mem, U2> for Assembler<'_> {
    fn vfpclassph(&mut self, dst: KReg, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vfpclassph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl VfpclassshEmitter<KReg, Vec, Imm> for Assembler<'_> {
    fn vfpclasssh(&mut self, dst: KReg, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vfpclasssh as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        );
    }
}

impl<U2: Into<Imm>> VfpclassshEmitter<KReg, Xmm, U2> for Assembler<'_> {
    fn vfpclasssh(&mut self, dst: KReg, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vfpclasssh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VfpclassshEmitter<KReg, Mem, U2> for Assembler<'_> {
    fn vfpclasssh(&mut self, dst: KReg, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vfpclasssh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl VgetexpphEmitter<Vec, Vec> for Assembler<'_> {
    fn vgetexpph(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vgetexpph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VgetexpphEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vgetexpph(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vgetexpph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VgetexpphEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vgetexpph(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vgetexpph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VgetexpphEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vgetexpph(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vgetexpph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VgetexpphEmitter<Vec, Mem> for Assembler<'_> {
    fn vgetexpph(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vgetexpph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VgetexpphEmitter<Xmm, Mem> for Assembler<'_> {
    fn vgetexpph(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vgetexpph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VgetexpphEmitter<Ymm, Mem> for Assembler<'_> {
    fn vgetexpph(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vgetexpph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VgetexpphEmitter<Zmm, Mem> for Assembler<'_> {
    fn vgetexpph(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vgetexpph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VgetexpshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vgetexpsh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vgetexpsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VgetexpshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vgetexpsh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vgetexpsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VgetexpshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vgetexpsh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vgetexpsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VgetexpshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vgetexpsh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vgetexpsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VgetmantphEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vgetmantph(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vgetmantph as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        );
    }
}

impl<U2: Into<Imm>> VgetmantphEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vgetmantph(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vgetmantph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VgetmantphEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vgetmantph(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vgetmantph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VgetmantphEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vgetmantph(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vgetmantph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl VgetmantphEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vgetmantph(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vgetmantph as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        );
    }
}

impl<U2: Into<Imm>> VgetmantphEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vgetmantph(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vgetmantph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VgetmantphEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vgetmantph(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vgetmantph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VgetmantphEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vgetmantph(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vgetmantph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl VgetmantshEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vgetmantsh(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vgetmantsh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VgetmantshEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vgetmantsh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vgetmantsh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        );
    }
}

impl VgetmantshEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vgetmantsh(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vgetmantsh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VgetmantshEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vgetmantsh(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vgetmantsh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        );
    }
}

impl VmaxphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmaxph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vmaxph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmaxphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmaxph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vmaxph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmaxphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vmaxph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vmaxph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmaxphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vmaxph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vmaxph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmaxphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmaxph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vmaxph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmaxphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmaxph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vmaxph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmaxphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vmaxph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vmaxph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmaxphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vmaxph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vmaxph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmaxshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmaxsh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vmaxsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmaxshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmaxsh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vmaxsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmaxshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmaxsh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vmaxsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmaxshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmaxsh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vmaxsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VminphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vminph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vminph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VminphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vminph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vminph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VminphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vminph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vminph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VminphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vminph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vminph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VminphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vminph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vminph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VminphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vminph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vminph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VminphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vminph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vminph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VminphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vminph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vminph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VminshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vminsh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vminsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VminshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vminsh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vminsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VminshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vminsh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vminsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VminshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vminsh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vminsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmovshEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovsh(&mut self, dst: Mem, src: Vec) {
        self.emit_n(InstId::Vmovsh as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl VmovshEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovsh(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(InstId::Vmovsh as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl VmovshEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovsh(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Vmovsh as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl VmovshEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovsh(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Vmovsh as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl Vmovsh3Emitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmovsh_3(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vmovsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Vmovsh3Emitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmovsh_3(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vmovsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmovwEmitter<Gp, Vec> for Assembler<'_> {
    fn vmovw(&mut self, dst: Gp, src: Vec) {
        self.emit_n(InstId::Vmovw as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl VmovwEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vmovw(&mut self, dst: Gpd, src: Xmm) {
        self.emit_n(InstId::Vmovw as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl VmovwEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovw(&mut self, dst: Mem, src: Vec) {
        self.emit_n(InstId::Vmovw as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl VmovwEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovw(&mut self, dst: Mem, src: Xmm) {
        self.emit_n(InstId::Vmovw as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl VmovwEmitter<Vec, Gp> for Assembler<'_> {
    fn vmovw(&mut self, dst: Vec, src: Gp) {
        self.emit_n(InstId::Vmovw as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl VmovwEmitter<Xmm, Gpd> for Assembler<'_> {
    fn vmovw(&mut self, dst: Xmm, src: Gpd) {
        self.emit_n(InstId::Vmovw as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl VmovwEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovw(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Vmovw as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl VmovwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovw(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Vmovw as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl VmulphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmulph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vmulph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmulphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmulph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vmulph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmulphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vmulph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vmulph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmulphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vmulph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vmulph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmulphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmulph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vmulph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmulphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmulph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vmulph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmulphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vmulph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vmulph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmulphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vmulph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vmulph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmulshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmulsh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vmulsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmulshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmulsh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vmulsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmulshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmulsh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vmulsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VmulshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmulsh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vmulsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VrcpphEmitter<Vec, Vec> for Assembler<'_> {
    fn vrcpph(&mut self, dst: Vec, src: Vec) {
        self.emit_n(InstId::Vrcpph as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl VrcpphEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vrcpph(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(InstId::Vrcpph as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl VrcpphEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vrcpph(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(InstId::Vrcpph as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl VrcpphEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vrcpph(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(InstId::Vrcpph as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl VrcpphEmitter<Vec, Mem> for Assembler<'_> {
    fn vrcpph(&mut self, dst: Vec, src: Mem) {
        self.emit_n(InstId::Vrcpph as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl VrcpphEmitter<Xmm, Mem> for Assembler<'_> {
    fn vrcpph(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(InstId::Vrcpph as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl VrcpphEmitter<Ymm, Mem> for Assembler<'_> {
    fn vrcpph(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(InstId::Vrcpph as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl VrcpphEmitter<Zmm, Mem> for Assembler<'_> {
    fn vrcpph(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(InstId::Vrcpph as u32, &[dst.as_operand(), src.as_operand()]);
    }
}

impl VrcpshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vrcpsh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vrcpsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VrcpshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vrcpsh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vrcpsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VrcpshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vrcpsh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vrcpsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VrcpshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vrcpsh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vrcpsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VreducephEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vreduceph(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vreduceph as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        );
    }
}

impl<U2: Into<Imm>> VreducephEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vreduceph(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vreduceph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VreducephEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vreduceph(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vreduceph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VreducephEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vreduceph(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vreduceph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl VreducephEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vreduceph(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vreduceph as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        );
    }
}

impl<U2: Into<Imm>> VreducephEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vreduceph(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vreduceph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VreducephEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vreduceph(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vreduceph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VreducephEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vreduceph(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vreduceph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl VreduceshEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vreducesh(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vreducesh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VreduceshEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vreducesh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vreducesh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        );
    }
}

impl VreduceshEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vreducesh(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vreducesh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VreduceshEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vreducesh(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vreducesh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        );
    }
}

impl VrndscalephEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vrndscaleph(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vrndscaleph as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        );
    }
}

impl<U2: Into<Imm>> VrndscalephEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vrndscaleph(&mut self, dst: Xmm, src1: Xmm, src2: U2) {
        self.emit_n(
            InstId::Vrndscaleph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VrndscalephEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vrndscaleph(&mut self, dst: Ymm, src1: Ymm, src2: U2) {
        self.emit_n(
            InstId::Vrndscaleph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VrndscalephEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vrndscaleph(&mut self, dst: Zmm, src1: Zmm, src2: U2) {
        self.emit_n(
            InstId::Vrndscaleph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl VrndscalephEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vrndscaleph(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vrndscaleph as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        );
    }
}

impl<U2: Into<Imm>> VrndscalephEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vrndscaleph(&mut self, dst: Xmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vrndscaleph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VrndscalephEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vrndscaleph(&mut self, dst: Ymm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vrndscaleph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VrndscalephEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vrndscaleph(&mut self, dst: Zmm, src1: Mem, src2: U2) {
        self.emit_n(
            InstId::Vrndscaleph as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                Into::<Imm>::into(src2).as_operand(),
            ],
        );
    }
}

impl VrndscaleshEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vrndscalesh(&mut self, dst: Vec, src1: Vec, src2: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vrndscalesh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VrndscaleshEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vrndscalesh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm, src3: U3) {
        self.emit_n(
            InstId::Vrndscalesh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        );
    }
}

impl VrndscaleshEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vrndscalesh(&mut self, dst: Vec, src1: Vec, src2: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vrndscalesh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                imm.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VrndscaleshEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vrndscalesh(&mut self, dst: Xmm, src1: Xmm, src2: Mem, src3: U3) {
        self.emit_n(
            InstId::Vrndscalesh as u32,
            &[
                dst.as_operand(),
                src1.as_operand(),
                src2.as_operand(),
                Into::<Imm>::into(src3).as_operand(),
            ],
        );
    }
}

impl VrsqrtphEmitter<Vec, Vec> for Assembler<'_> {
    fn vrsqrtph(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vrsqrtph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VrsqrtphEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vrsqrtph(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vrsqrtph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VrsqrtphEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vrsqrtph(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vrsqrtph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VrsqrtphEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vrsqrtph(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vrsqrtph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VrsqrtphEmitter<Vec, Mem> for Assembler<'_> {
    fn vrsqrtph(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vrsqrtph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VrsqrtphEmitter<Xmm, Mem> for Assembler<'_> {
    fn vrsqrtph(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vrsqrtph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VrsqrtphEmitter<Ymm, Mem> for Assembler<'_> {
    fn vrsqrtph(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vrsqrtph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VrsqrtphEmitter<Zmm, Mem> for Assembler<'_> {
    fn vrsqrtph(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vrsqrtph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VrsqrtshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vrsqrtsh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vrsqrtsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VrsqrtshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vrsqrtsh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vrsqrtsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VrsqrtshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vrsqrtsh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vrsqrtsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VrsqrtshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vrsqrtsh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vrsqrtsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VscalefphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vscalefph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vscalefph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VscalefphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vscalefph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vscalefph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VscalefphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vscalefph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vscalefph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VscalefphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vscalefph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vscalefph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VscalefphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vscalefph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vscalefph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VscalefphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vscalefph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vscalefph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VscalefphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vscalefph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vscalefph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VscalefphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vscalefph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vscalefph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VscalefshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vscalefsh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vscalefsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VscalefshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vscalefsh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vscalefsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VscalefshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vscalefsh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vscalefsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VscalefshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vscalefsh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vscalefsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VsqrtphEmitter<Vec, Vec> for Assembler<'_> {
    fn vsqrtph(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vsqrtph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VsqrtphEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vsqrtph(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Vsqrtph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VsqrtphEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vsqrtph(&mut self, dst: Ymm, src: Ymm) {
        self.emit_n(
            InstId::Vsqrtph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VsqrtphEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vsqrtph(&mut self, dst: Zmm, src: Zmm) {
        self.emit_n(
            InstId::Vsqrtph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VsqrtphEmitter<Vec, Mem> for Assembler<'_> {
    fn vsqrtph(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Vsqrtph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VsqrtphEmitter<Xmm, Mem> for Assembler<'_> {
    fn vsqrtph(&mut self, dst: Xmm, src: Mem) {
        self.emit_n(
            InstId::Vsqrtph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VsqrtphEmitter<Ymm, Mem> for Assembler<'_> {
    fn vsqrtph(&mut self, dst: Ymm, src: Mem) {
        self.emit_n(
            InstId::Vsqrtph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VsqrtphEmitter<Zmm, Mem> for Assembler<'_> {
    fn vsqrtph(&mut self, dst: Zmm, src: Mem) {
        self.emit_n(
            InstId::Vsqrtph as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl VsqrtshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsqrtsh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vsqrtsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VsqrtshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsqrtsh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vsqrtsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VsqrtshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsqrtsh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vsqrtsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VsqrtshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsqrtsh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vsqrtsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VsubphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsubph(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vsubph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VsubphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsubph(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vsubph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VsubphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vsubph(&mut self, dst: Ymm, src1: Ymm, src2: Ymm) {
        self.emit_n(
            InstId::Vsubph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VsubphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vsubph(&mut self, dst: Zmm, src1: Zmm, src2: Zmm) {
        self.emit_n(
            InstId::Vsubph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VsubphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsubph(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vsubph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VsubphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsubph(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vsubph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VsubphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vsubph(&mut self, dst: Ymm, src1: Ymm, src2: Mem) {
        self.emit_n(
            InstId::Vsubph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VsubphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vsubph(&mut self, dst: Zmm, src1: Zmm, src2: Mem) {
        self.emit_n(
            InstId::Vsubph as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VsubshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsubsh(&mut self, dst: Vec, src1: Vec, src2: Vec) {
        self.emit_n(
            InstId::Vsubsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VsubshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsubsh(&mut self, dst: Xmm, src1: Xmm, src2: Xmm) {
        self.emit_n(
            InstId::Vsubsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VsubshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsubsh(&mut self, dst: Vec, src1: Vec, src2: Mem) {
        self.emit_n(
            InstId::Vsubsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VsubshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsubsh(&mut self, dst: Xmm, src1: Xmm, src2: Mem) {
        self.emit_n(
            InstId::Vsubsh as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl VucomishEmitter<Vec, Vec> for Assembler<'_> {
    fn vucomish(&mut self, lhs: Vec, rhs: Vec) {
        self.emit_n(
            InstId::Vucomish as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        );
    }
}

impl VucomishEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vucomish(&mut self, lhs: Xmm, rhs: Xmm) {
        self.emit_n(
            InstId::Vucomish as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        );
    }
}

impl VucomishEmitter<Vec, Mem> for Assembler<'_> {
    fn vucomish(&mut self, lhs: Vec, rhs: Mem) {
        self.emit_n(
            InstId::Vucomish as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        );
    }
}

impl VucomishEmitter<Xmm, Mem> for Assembler<'_> {
    fn vucomish(&mut self, lhs: Xmm, rhs: Mem) {
        self.emit_n(
            InstId::Vucomish as u32,
            &[lhs.as_operand(), rhs.as_operand()],
        );
    }
}

impl LdtilecfgEmitter<Mem> for Assembler<'_> {
    fn ldtilecfg(&mut self, mem: Mem) {
        self.emit_n(InstId::Ldtilecfg as u32, &[mem.as_operand()]);
    }
}

impl SttilecfgEmitter<Mem> for Assembler<'_> {
    fn sttilecfg(&mut self, dst: Mem) {
        self.emit_n(InstId::Sttilecfg as u32, &[dst.as_operand()]);
    }
}

impl TileloaddEmitter<Tmm, Mem> for Assembler<'_> {
    fn tileloadd(&mut self, dst: Tmm, src: Mem) {
        self.emit_n(
            InstId::Tileloadd as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl Tileloaddt1Emitter<Tmm, Mem> for Assembler<'_> {
    fn tileloaddt1(&mut self, dst: Tmm, src: Mem) {
        self.emit_n(
            InstId::Tileloaddt1 as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl TilereleaseEmitter for Assembler<'_> {
    fn tilerelease(&mut self) {
        self.emit_n(InstId::Tilerelease as u32, &[]);
    }
}

impl TilestoredEmitter<Mem, Tmm> for Assembler<'_> {
    fn tilestored(&mut self, dst: Mem, src: Tmm) {
        self.emit_n(
            InstId::Tilestored as u32,
            &[dst.as_operand(), src.as_operand()],
        );
    }
}

impl TilezeroEmitter<Tmm> for Assembler<'_> {
    fn tilezero(&mut self, dst: Tmm) {
        self.emit_n(InstId::Tilezero as u32, &[dst.as_operand()]);
    }
}

impl Tdpbf16psEmitter<Tmm, Tmm, Tmm> for Assembler<'_> {
    fn tdpbf16ps(&mut self, dst: Tmm, src1: Tmm, src2: Tmm) {
        self.emit_n(
            InstId::Tdpbf16ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Tcmmimfp16psEmitter<Tmm, Tmm, Tmm> for Assembler<'_> {
    fn tcmmimfp16ps(&mut self, dst: Tmm, src1: Tmm, src2: Tmm) {
        self.emit_n(
            InstId::Tcmmimfp16ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Tcmmrlfp16psEmitter<Tmm, Tmm, Tmm> for Assembler<'_> {
    fn tcmmrlfp16ps(&mut self, dst: Tmm, src1: Tmm, src2: Tmm) {
        self.emit_n(
            InstId::Tcmmrlfp16ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Tdpfp16psEmitter<Tmm, Tmm, Tmm> for Assembler<'_> {
    fn tdpfp16ps(&mut self, dst: Tmm, src1: Tmm, src2: Tmm) {
        self.emit_n(
            InstId::Tdpfp16ps as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl TdpbssdEmitter<Tmm, Tmm, Tmm> for Assembler<'_> {
    fn tdpbssd(&mut self, dst: Tmm, src1: Tmm, src2: Tmm) {
        self.emit_n(
            InstId::Tdpbssd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl TdpbsudEmitter<Tmm, Tmm, Tmm> for Assembler<'_> {
    fn tdpbsud(&mut self, dst: Tmm, src1: Tmm, src2: Tmm) {
        self.emit_n(
            InstId::Tdpbsud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl TdpbusdEmitter<Tmm, Tmm, Tmm> for Assembler<'_> {
    fn tdpbusd(&mut self, dst: Tmm, src1: Tmm, src2: Tmm) {
        self.emit_n(
            InstId::Tdpbusd as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl TdpbuudEmitter<Tmm, Tmm, Tmm> for Assembler<'_> {
    fn tdpbuud(&mut self, dst: Tmm, src1: Tmm, src2: Tmm) {
        self.emit_n(
            InstId::Tdpbuud as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        );
    }
}

impl Cbw0Emitter for Assembler<'_> {
    fn cbw_0(&mut self) {
        self.emit_n(InstId::Cbw as u32, &[]); // ANY [IMPLICIT] AX <- Sign Extend AL
    }
}

impl Cdq0Emitter for Assembler<'_> {
    fn cdq_0(&mut self) {
        self.emit_n(InstId::Cdq as u32, &[]); // ANY [IMPLICIT] EDX:EAX <- Sign Extend EAX
    }
}

impl Cdqe0Emitter for Assembler<'_> {
    fn cdqe_0(&mut self) {
        self.emit_n(InstId::Cdqe as u32, &[]); // X64 [IMPLICIT] RAX <- Sign Extend EAX
    }
}

impl Cmpxchg2Emitter<Gp, Gp> for Assembler<'_> {
    fn cmpxchg_2(&mut self, dst: Gp, src: Gp) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // I486 [IMPLICIT]
    }
}

impl Cmpxchg2Emitter<GpbLo, GpbLo> for Assembler<'_> {
    fn cmpxchg_2(&mut self, dst: GpbLo, src: GpbLo) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // I486 [IMPLICIT]
    }
}

impl Cmpxchg2Emitter<GpbHi, GpbLo> for Assembler<'_> {
    fn cmpxchg_2(&mut self, dst: GpbHi, src: GpbLo) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // I486 [IMPLICIT]
    }
}

impl Cmpxchg2Emitter<Gpw, Gpw> for Assembler<'_> {
    fn cmpxchg_2(&mut self, dst: Gpw, src: Gpw) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // I486 [IMPLICIT]
    }
}

impl Cmpxchg2Emitter<Gpd, Gpd> for Assembler<'_> {
    fn cmpxchg_2(&mut self, dst: Gpd, src: Gpd) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // I486 [IMPLICIT]
    }
}

impl Cmpxchg2Emitter<Gpq, Gpq> for Assembler<'_> {
    fn cmpxchg_2(&mut self, dst: Gpq, src: Gpq) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // I486 [IMPLICIT]
    }
}

impl Cmpxchg2Emitter<Mem, Gp> for Assembler<'_> {
    fn cmpxchg_2(&mut self, dst: Mem, src: Gp) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // I486 [IMPLICIT]
    }
}

impl Cmpxchg16b1Emitter<Mem> for Assembler<'_> {
    fn cmpxchg16b_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Cmpxchg16b as u32, &[mem.as_operand()]); // CMPXCHG8B [IMPLICIT] m == RDX:RAX ? m <- RCX:RBX
    }
}

impl Cmpxchg8b1Emitter<Mem> for Assembler<'_> {
    fn cmpxchg8b_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Cmpxchg8b as u32, &[mem.as_operand()]); // CMPXCHG16B[IMPLICIT] m == EDX:EAX ? m <- ECX:EBX
    }
}

impl Cqo0Emitter for Assembler<'_> {
    fn cqo_0(&mut self) {
        self.emit_n(InstId::Cqo as u32, &[]); // X64 [IMPLICIT] RDX:RAX <- Sign Extend RAX
    }
}

impl Cwd0Emitter for Assembler<'_> {
    fn cwd_0(&mut self) {
        self.emit_n(InstId::Cwd as u32, &[]); // ANY [IMPLICIT] DX:AX <- Sign Extend AX
    }
}

impl Cwde0Emitter for Assembler<'_> {
    fn cwde_0(&mut self) {
        self.emit_n(InstId::Cwde as u32, &[]); // ANY [IMPLICIT] EAX <- Sign Extend AX
    }
}

impl Div1Emitter<Gp> for Assembler<'_> {
    fn div_1(&mut self, src: Gp) {
        self.emit_n(InstId::Div as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Div1Emitter<GpbLo> for Assembler<'_> {
    fn div_1(&mut self, src: GpbLo) {
        self.emit_n(InstId::Div as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Div1Emitter<GpbHi> for Assembler<'_> {
    fn div_1(&mut self, src: GpbHi) {
        self.emit_n(InstId::Div as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Div1Emitter<Gpw> for Assembler<'_> {
    fn div_1(&mut self, src: Gpw) {
        self.emit_n(InstId::Div as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Div1Emitter<Gpd> for Assembler<'_> {
    fn div_1(&mut self, src: Gpd) {
        self.emit_n(InstId::Div as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Div1Emitter<Gpq> for Assembler<'_> {
    fn div_1(&mut self, src: Gpq) {
        self.emit_n(InstId::Div as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Div1Emitter<Mem> for Assembler<'_> {
    fn div_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Div as u32, &[mem.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / m8} {xDX[Rem]:xAX[Quot] <- DX:AX / m16|m32|m64}
    }
}

impl Idiv1Emitter<Gp> for Assembler<'_> {
    fn idiv_1(&mut self, src: Gp) {
        self.emit_n(InstId::Idiv as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Idiv1Emitter<GpbLo> for Assembler<'_> {
    fn idiv_1(&mut self, src: GpbLo) {
        self.emit_n(InstId::Idiv as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Idiv1Emitter<GpbHi> for Assembler<'_> {
    fn idiv_1(&mut self, src: GpbHi) {
        self.emit_n(InstId::Idiv as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Idiv1Emitter<Gpw> for Assembler<'_> {
    fn idiv_1(&mut self, src: Gpw) {
        self.emit_n(InstId::Idiv as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Idiv1Emitter<Gpd> for Assembler<'_> {
    fn idiv_1(&mut self, src: Gpd) {
        self.emit_n(InstId::Idiv as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Idiv1Emitter<Gpq> for Assembler<'_> {
    fn idiv_1(&mut self, src: Gpq) {
        self.emit_n(InstId::Idiv as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Idiv1Emitter<Mem> for Assembler<'_> {
    fn idiv_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Idiv as u32, &[mem.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / m8} {xDX[Rem]:xAX[Quot] <- DX:AX / m16|m32|m64}
    }
}

impl Imul1Emitter<Gp> for Assembler<'_> {
    fn imul_1(&mut self, src: Gp) {
        self.emit_n(InstId::Imul as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xAX:xDX <- xAX * r16|r32|r64}
    }
}

impl Imul1Emitter<GpbLo> for Assembler<'_> {
    fn imul_1(&mut self, src: GpbLo) {
        self.emit_n(InstId::Imul as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xAX:xDX <- xAX * r16|r32|r64}
    }
}

impl Imul1Emitter<GpbHi> for Assembler<'_> {
    fn imul_1(&mut self, src: GpbHi) {
        self.emit_n(InstId::Imul as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xAX:xDX <- xAX * r16|r32|r64}
    }
}

impl Imul1Emitter<Gpw> for Assembler<'_> {
    fn imul_1(&mut self, src: Gpw) {
        self.emit_n(InstId::Imul as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xAX:xDX <- xAX * r16|r32|r64}
    }
}

impl Imul1Emitter<Gpd> for Assembler<'_> {
    fn imul_1(&mut self, src: Gpd) {
        self.emit_n(InstId::Imul as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xAX:xDX <- xAX * r16|r32|r64}
    }
}

impl Imul1Emitter<Gpq> for Assembler<'_> {
    fn imul_1(&mut self, src: Gpq) {
        self.emit_n(InstId::Imul as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xAX:xDX <- xAX * r16|r32|r64}
    }
}

impl Imul1Emitter<Mem> for Assembler<'_> {
    fn imul_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Imul as u32, &[mem.as_operand()]); // ANY [IMPLICIT] {AX <- AL * m8} {xAX:xDX <- xAX * m16|m32|m64}
    }
}

impl IretEmitter for Assembler<'_> {
    fn iret(&mut self) {
        self.emit_n(InstId::Iret as u32, &[]); // ANY [IMPLICIT]
    }
}

impl IretdEmitter for Assembler<'_> {
    fn iretd(&mut self) {
        self.emit_n(InstId::Iretd as u32, &[]); // ANY [IMPLICIT]
    }
}

impl IretqEmitter for Assembler<'_> {
    fn iretq(&mut self) {
        self.emit_n(InstId::Iretq as u32, &[]); // X64 [IMPLICIT]
    }
}

impl Jecxz1Emitter<Label> for Assembler<'_> {
    fn jecxz_1(&mut self, target: Label) {
        self.emit_n(InstId::Jecxz as u32, &[target.as_operand()]); // ANY [IMPLICIT] Short jump if CX/ECX/RCX is zero.
    }
}

impl<U0: Into<Imm>> Jecxz1Emitter<U0> for Assembler<'_> {
    fn jecxz_1(&mut self, src: U0) {
        self.emit_n(InstId::Jecxz as u32, &[Into::<Imm>::into(src).as_operand()]); // ANY [IMPLICIT] Short jump if CX/ECX/RCX is zero.
    }
}

impl Loop1Emitter<Label> for Assembler<'_> {
    fn loop_1(&mut self, target: Label) {
        self.emit_n(InstId::Loop as u32, &[target.as_operand()]); // ANY [IMPLICIT] Decrement xCX; short jump if xCX != 0.
    }
}

impl<U0: Into<Imm>> Loop1Emitter<U0> for Assembler<'_> {
    fn loop_1(&mut self, src: U0) {
        self.emit_n(InstId::Loop as u32, &[Into::<Imm>::into(src).as_operand()]); // ANY [IMPLICIT] Decrement xCX; short jump if xCX != 0.
    }
}

impl Loope1Emitter<Label> for Assembler<'_> {
    fn loope_1(&mut self, target: Label) {
        self.emit_n(InstId::Loope as u32, &[target.as_operand()]); // ANY [IMPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 1.
    }
}

impl<U0: Into<Imm>> Loope1Emitter<U0> for Assembler<'_> {
    fn loope_1(&mut self, src: U0) {
        self.emit_n(InstId::Loope as u32, &[Into::<Imm>::into(src).as_operand()]); // ANY [IMPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 1.
    }
}

impl Loopne1Emitter<Label> for Assembler<'_> {
    fn loopne_1(&mut self, target: Label) {
        self.emit_n(InstId::Loopne as u32, &[target.as_operand()]); // ANY [IMPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 0.
    }
}

impl<U0: Into<Imm>> Loopne1Emitter<U0> for Assembler<'_> {
    fn loopne_1(&mut self, src: U0) {
        self.emit_n(
            InstId::Loopne as u32,
            &[Into::<Imm>::into(src).as_operand()],
        ); // ANY [IMPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 0.
    }
}

impl Mul1Emitter<Gp> for Assembler<'_> {
    fn mul_1(&mut self, src: Gp) {
        self.emit_n(InstId::Mul as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xDX:xAX <- xAX * r16|r32|r64}
    }
}

impl Mul1Emitter<GpbLo> for Assembler<'_> {
    fn mul_1(&mut self, src: GpbLo) {
        self.emit_n(InstId::Mul as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xDX:xAX <- xAX * r16|r32|r64}
    }
}

impl Mul1Emitter<GpbHi> for Assembler<'_> {
    fn mul_1(&mut self, src: GpbHi) {
        self.emit_n(InstId::Mul as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xDX:xAX <- xAX * r16|r32|r64}
    }
}

impl Mul1Emitter<Gpw> for Assembler<'_> {
    fn mul_1(&mut self, src: Gpw) {
        self.emit_n(InstId::Mul as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xDX:xAX <- xAX * r16|r32|r64}
    }
}

impl Mul1Emitter<Gpd> for Assembler<'_> {
    fn mul_1(&mut self, src: Gpd) {
        self.emit_n(InstId::Mul as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xDX:xAX <- xAX * r16|r32|r64}
    }
}

impl Mul1Emitter<Gpq> for Assembler<'_> {
    fn mul_1(&mut self, src: Gpq) {
        self.emit_n(InstId::Mul as u32, &[src.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xDX:xAX <- xAX * r16|r32|r64}
    }
}

impl Mul1Emitter<Mem> for Assembler<'_> {
    fn mul_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Mul as u32, &[mem.as_operand()]); // ANY [IMPLICIT] {AX <- AL * m8} {xDX:xAX <- xAX * m16|m32|m64}
    }
}

impl RetEmitter for Assembler<'_> {
    fn ret(&mut self) {
        self.emit_n(InstId::Ret as u32, &[]);
    }
}

impl<U0: Into<Imm>> Ret1Emitter<U0> for Assembler<'_> {
    fn ret_1(&mut self, src: U0) {
        self.emit_n(InstId::Ret as u32, &[Into::<Imm>::into(src).as_operand()]);
    }
}

impl RetfEmitter for Assembler<'_> {
    fn retf(&mut self) {
        self.emit_n(InstId::Retf as u32, &[]);
    }
}

impl<U0: Into<Imm>> Retf1Emitter<U0> for Assembler<'_> {
    fn retf_1(&mut self, src: U0) {
        self.emit_n(InstId::Retf as u32, &[Into::<Imm>::into(src).as_operand()]);
    }
}

impl XlatbEmitter for Assembler<'_> {
    fn xlatb(&mut self) {
        self.emit_n(InstId::Xlatb as u32, &[]); // ANY [IMPLICIT]
    }
}

impl Aaa0Emitter for Assembler<'_> {
    fn aaa_0(&mut self) {
        self.emit_n(InstId::Aaa as u32, &[]); // X86 [IMPLICIT]
    }
}

impl<U0: Into<Imm>> Aad1Emitter<U0> for Assembler<'_> {
    fn aad_1(&mut self, src: U0) {
        self.emit_n(InstId::Aad as u32, &[Into::<Imm>::into(src).as_operand()]); // X86 [IMPLICIT]
    }
}

impl<U0: Into<Imm>> Aam1Emitter<U0> for Assembler<'_> {
    fn aam_1(&mut self, src: U0) {
        self.emit_n(InstId::Aam as u32, &[Into::<Imm>::into(src).as_operand()]); // X86 [IMPLICIT]
    }
}

impl Aas0Emitter for Assembler<'_> {
    fn aas_0(&mut self) {
        self.emit_n(InstId::Aas as u32, &[]); // X86 [IMPLICIT]
    }
}

impl Daa0Emitter for Assembler<'_> {
    fn daa_0(&mut self) {
        self.emit_n(InstId::Daa as u32, &[]); // X86 [IMPLICIT]
    }
}

impl Das0Emitter for Assembler<'_> {
    fn das_0(&mut self) {
        self.emit_n(InstId::Das as u32, &[]); // X86 [IMPLICIT]
    }
}

impl Lahf0Emitter for Assembler<'_> {
    fn lahf_0(&mut self) {
        self.emit_n(InstId::Lahf as u32, &[]); // LAHFSAHF [IMPLICIT] AH <- EFL
    }
}

impl Sahf0Emitter for Assembler<'_> {
    fn sahf_0(&mut self) {
        self.emit_n(InstId::Sahf as u32, &[]); // LAHFSAHF [IMPLICIT] EFL <- AH
    }
}

impl Cpuid0Emitter for Assembler<'_> {
    fn cpuid_0(&mut self) {
        self.emit_n(InstId::Cpuid as u32, &[]); // I486 [IMPLICIT] EAX:EBX:ECX:EDX <- CPUID[EAX:ECX]
    }
}

impl Clzero0Emitter for Assembler<'_> {
    fn clzero_0(&mut self) {
        self.emit_n(InstId::Clzero as u32, &[]); // CLZERO [IMPLICIT]
    }
}

impl Rdpru0Emitter for Assembler<'_> {
    fn rdpru_0(&mut self) {
        self.emit_n(InstId::Rdpru as u32, &[]); // RDPRU [IMPLICIT] EDX:EAX <- PRU[ECX]
    }
}

impl Rdpkru0Emitter for Assembler<'_> {
    fn rdpkru_0(&mut self) {
        self.emit_n(InstId::Rdpkru as u32, &[]); // RDPKRU [IMPLICIT] EDX:EAX <- PKRU[ECX]
    }
}

impl Rdtsc0Emitter for Assembler<'_> {
    fn rdtsc_0(&mut self) {
        self.emit_n(InstId::Rdtsc as u32, &[]); // RDTSC [IMPLICIT] EDX:EAX <- CNT
    }
}

impl Rdtscp0Emitter for Assembler<'_> {
    fn rdtscp_0(&mut self) {
        self.emit_n(InstId::Rdtscp as u32, &[]); // RDTSCP [IMPLICIT] EDX:EAX:EXC <- CNT
    }
}

impl Mulx3Emitter<Gp, Gp, Gp> for Assembler<'_> {
    fn mulx_3(&mut self, dst: Gp, src1: Gp, src2: Gp) {
        self.emit_n(
            InstId::Mulx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2 [IMPLICIT]
    }
}

impl Mulx3Emitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn mulx_3(&mut self, dst: Gpd, src1: Gpd, src2: Gpd) {
        self.emit_n(
            InstId::Mulx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2 [IMPLICIT]
    }
}

impl Mulx3Emitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn mulx_3(&mut self, dst: Gpq, src1: Gpq, src2: Gpq) {
        self.emit_n(
            InstId::Mulx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2 [IMPLICIT]
    }
}

impl Mulx3Emitter<Gp, Gp, Mem> for Assembler<'_> {
    fn mulx_3(&mut self, dst: Gp, src1: Gp, src2: Mem) {
        self.emit_n(
            InstId::Mulx as u32,
            &[dst.as_operand(), src1.as_operand(), src2.as_operand()],
        ); // BMI2 [IMPLICIT]
    }
}

impl Xgetbv0Emitter for Assembler<'_> {
    fn xgetbv_0(&mut self) {
        self.emit_n(InstId::Xgetbv as u32, &[]); // XSAVE [IMPLICIT] EDX:EAX <- XCR[ECX]
    }
}

impl Xrstor1Emitter<Mem> for Assembler<'_> {
    fn xrstor_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Xrstor as u32, &[mem.as_operand()]); // XSAVE [IMPLICIT]
    }
}

impl Xrstor641Emitter<Mem> for Assembler<'_> {
    fn xrstor64_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Xrstor64 as u32, &[mem.as_operand()]); // XSAVE+X64 [IMPLICIT]
    }
}

impl Xrstors1Emitter<Mem> for Assembler<'_> {
    fn xrstors_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Xrstors as u32, &[mem.as_operand()]); // XSAVE [IMPLICIT]
    }
}

impl Xrstors641Emitter<Mem> for Assembler<'_> {
    fn xrstors64_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Xrstors64 as u32, &[mem.as_operand()]); // XSAVE+X64 [IMPLICIT]
    }
}

impl Xsave1Emitter<Mem> for Assembler<'_> {
    fn xsave_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Xsave as u32, &[mem.as_operand()]); // XSAVE [IMPLICIT]
    }
}

impl Xsave641Emitter<Mem> for Assembler<'_> {
    fn xsave64_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Xsave64 as u32, &[mem.as_operand()]); // XSAVE+X64 [IMPLICIT]
    }
}

impl Xsavec1Emitter<Mem> for Assembler<'_> {
    fn xsavec_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Xsavec as u32, &[mem.as_operand()]); // XSAVE [IMPLICIT]
    }
}

impl Xsavec641Emitter<Mem> for Assembler<'_> {
    fn xsavec64_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Xsavec64 as u32, &[mem.as_operand()]); // XSAVE+X64 [IMPLICIT]
    }
}

impl Xsaveopt1Emitter<Mem> for Assembler<'_> {
    fn xsaveopt_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Xsaveopt as u32, &[mem.as_operand()]); // XSAVE [IMPLICIT]
    }
}

impl Xsaveopt641Emitter<Mem> for Assembler<'_> {
    fn xsaveopt64_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Xsaveopt64 as u32, &[mem.as_operand()]); // XSAVE+X64 [IMPLICIT]
    }
}

impl Xsaves1Emitter<Mem> for Assembler<'_> {
    fn xsaves_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Xsaves as u32, &[mem.as_operand()]); // XSAVE [IMPLICIT]
    }
}

impl Xsaves641Emitter<Mem> for Assembler<'_> {
    fn xsaves64_1(&mut self, mem: Mem) {
        self.emit_n(InstId::Xsaves64 as u32, &[mem.as_operand()]); // XSAVE+X64 [IMPLICIT]
    }
}

impl SyscallEmitter for Assembler<'_> {
    fn syscall(&mut self) {
        self.emit_n(InstId::Syscall as u32, &[]); // X64 [IMPLICIT]
    }
}

impl SysenterEmitter for Assembler<'_> {
    fn sysenter(&mut self) {
        self.emit_n(InstId::Sysenter as u32, &[]); // X64 [IMPLICIT]
    }
}

impl Hreset1Emitter<Imm> for Assembler<'_> {
    fn hreset_1(&mut self, imm: Imm) {
        self.emit_n(InstId::Hreset as u32, &[imm.as_operand()]); // HRESET [IMPLICIT]
    }
}

impl SeamcallEmitter for Assembler<'_> {
    fn seamcall(&mut self) {
        self.emit_n(InstId::Seamcall as u32, &[]);
    }
}

impl SeamopsEmitter for Assembler<'_> {
    fn seamops(&mut self) {
        self.emit_n(InstId::Seamops as u32, &[]);
    }
}

impl SeamretEmitter for Assembler<'_> {
    fn seamret(&mut self) {
        self.emit_n(InstId::Seamret as u32, &[]);
    }
}

impl TdcallEmitter for Assembler<'_> {
    fn tdcall(&mut self) {
        self.emit_n(InstId::Tdcall as u32, &[]);
    }
}

impl PconfigEmitter for Assembler<'_> {
    fn pconfig(&mut self) {
        self.emit_n(InstId::Pconfig as u32, &[]); // PCONFIG [IMPLICIT]
    }
}

impl Rdmsr0Emitter for Assembler<'_> {
    fn rdmsr_0(&mut self) {
        self.emit_n(InstId::Rdmsr as u32, &[]); // ANY [IMPLICIT]
    }
}

impl Rdpmc0Emitter for Assembler<'_> {
    fn rdpmc_0(&mut self) {
        self.emit_n(InstId::Rdpmc as u32, &[]); // ANY [IMPLICIT]
    }
}

impl SysexitEmitter for Assembler<'_> {
    fn sysexit(&mut self) {
        self.emit_n(InstId::Sysexit as u32, &[]); // X64 [IMPLICIT]
    }
}

impl SysexitqEmitter for Assembler<'_> {
    fn sysexitq(&mut self) {
        self.emit_n(InstId::Sysexitq as u32, &[]); // X64 [IMPLICIT]
    }
}

impl SysretEmitter for Assembler<'_> {
    fn sysret(&mut self) {
        self.emit_n(InstId::Sysret as u32, &[]); // X64 [IMPLICIT]
    }
}

impl SysretqEmitter for Assembler<'_> {
    fn sysretq(&mut self) {
        self.emit_n(InstId::Sysretq as u32, &[]); // X64 [IMPLICIT]
    }
}

impl Wrmsr0Emitter for Assembler<'_> {
    fn wrmsr_0(&mut self) {
        self.emit_n(InstId::Wrmsr as u32, &[]); // ANY [IMPLICIT]
    }
}

impl Xsetbv0Emitter for Assembler<'_> {
    fn xsetbv_0(&mut self) {
        self.emit_n(InstId::Xsetbv as u32, &[]); // XSAVE [IMPLICIT] XCR[ECX] <- EDX:EAX
    }
}

impl Monitor0Emitter for Assembler<'_> {
    fn monitor_0(&mut self) {
        self.emit_n(InstId::Monitor as u32, &[]);
    }
}

impl Monitorx0Emitter for Assembler<'_> {
    fn monitorx_0(&mut self) {
        self.emit_n(InstId::Monitorx as u32, &[]);
    }
}

impl Mwait0Emitter for Assembler<'_> {
    fn mwait_0(&mut self) {
        self.emit_n(InstId::Mwait as u32, &[]);
    }
}

impl Mwaitx0Emitter for Assembler<'_> {
    fn mwaitx_0(&mut self) {
        self.emit_n(InstId::Mwaitx as u32, &[]);
    }
}

impl Tpause1Emitter<Gp> for Assembler<'_> {
    fn tpause_1(&mut self, src: Gp) {
        self.emit_n(InstId::Tpause as u32, &[src.as_operand()]);
    }
}

impl Tpause1Emitter<Gpd> for Assembler<'_> {
    fn tpause_1(&mut self, src: Gpd) {
        self.emit_n(InstId::Tpause as u32, &[src.as_operand()]);
    }
}

impl Umwait1Emitter<Gp> for Assembler<'_> {
    fn umwait_1(&mut self, src: Gp) {
        self.emit_n(InstId::Umwait as u32, &[src.as_operand()]);
    }
}

impl Umwait1Emitter<Gpd> for Assembler<'_> {
    fn umwait_1(&mut self, src: Gpd) {
        self.emit_n(InstId::Umwait as u32, &[src.as_operand()]);
    }
}

impl Blendvpd2Emitter<Vec, Vec> for Assembler<'_> {
    fn blendvpd_2(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Blendvpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Blendvpd2Emitter<Xmm, Xmm> for Assembler<'_> {
    fn blendvpd_2(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Blendvpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Blendvpd2Emitter<Vec, Mem> for Assembler<'_> {
    fn blendvpd_2(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Blendvpd as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Blendvps2Emitter<Vec, Vec> for Assembler<'_> {
    fn blendvps_2(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Blendvps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Blendvps2Emitter<Xmm, Xmm> for Assembler<'_> {
    fn blendvps_2(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Blendvps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Blendvps2Emitter<Vec, Mem> for Assembler<'_> {
    fn blendvps_2(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Blendvps as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Pblendvb2Emitter<Vec, Vec> for Assembler<'_> {
    fn pblendvb_2(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Pblendvb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Pblendvb2Emitter<Xmm, Xmm> for Assembler<'_> {
    fn pblendvb_2(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Pblendvb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Pblendvb2Emitter<Vec, Mem> for Assembler<'_> {
    fn pblendvb_2(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Pblendvb as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Maskmovq2Emitter<Mm, Mm> for Assembler<'_> {
    fn maskmovq_2(&mut self, dst: Mm, src: Mm) {
        self.emit_n(
            InstId::Maskmovq as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE [IMPLICIT]
    }
}

impl Maskmovdqu2Emitter<Vec, Vec> for Assembler<'_> {
    fn maskmovdqu_2(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Maskmovdqu as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SSE2 [IMPLICIT]
    }
}

impl Pcmpestri3Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn pcmpestri_3(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Pcmpestri as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Pcmpestri3Emitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pcmpestri_3(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Pcmpestri as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Pcmpestrm3Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn pcmpestrm_3(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Pcmpestrm as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Pcmpestrm3Emitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pcmpestrm_3(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Pcmpestrm as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Pcmpistri3Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn pcmpistri_3(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Pcmpistri as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Pcmpistri3Emitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pcmpistri_3(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Pcmpistri as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Pcmpistrm3Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn pcmpistrm_3(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Pcmpistrm as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Pcmpistrm3Emitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pcmpistrm_3(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Pcmpistrm as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Sha256rnds22Emitter<Vec, Vec> for Assembler<'_> {
    fn sha256rnds2_2(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Sha256rnds2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SHA [IMPLICIT]
    }
}

impl Sha256rnds22Emitter<Xmm, Xmm> for Assembler<'_> {
    fn sha256rnds2_2(&mut self, dst: Xmm, src: Xmm) {
        self.emit_n(
            InstId::Sha256rnds2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SHA [IMPLICIT]
    }
}

impl Sha256rnds22Emitter<Vec, Mem> for Assembler<'_> {
    fn sha256rnds2_2(&mut self, dst: Vec, src: Mem) {
        self.emit_n(
            InstId::Sha256rnds2 as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // SHA [IMPLICIT]
    }
}

impl Vmaskmovdqu2Emitter<Vec, Vec> for Assembler<'_> {
    fn vmaskmovdqu_2(&mut self, dst: Vec, src: Vec) {
        self.emit_n(
            InstId::Vmaskmovdqu as u32,
            &[dst.as_operand(), src.as_operand()],
        ); // AVX [IMPLICIT]
    }
}

impl Vpcmpestri3Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpestri_3(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpestri as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX [IMPLICIT]
    }
}

impl Vpcmpestri3Emitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpestri_3(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpestri as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX [IMPLICIT]
    }
}

impl Vpcmpestrm3Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpestrm_3(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpestrm as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX [IMPLICIT]
    }
}

impl Vpcmpestrm3Emitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpestrm_3(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpestrm as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX [IMPLICIT]
    }
}

impl Vpcmpistri3Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpistri_3(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpistri as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX [IMPLICIT]
    }
}

impl Vpcmpistri3Emitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpistri_3(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpistri as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX [IMPLICIT]
    }
}

impl Vpcmpistrm3Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpistrm_3(&mut self, dst: Vec, src: Vec, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpistrm as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX [IMPLICIT]
    }
}

impl Vpcmpistrm3Emitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpistrm_3(&mut self, dst: Vec, src: Mem, imm: Imm) {
        self.emit_n(
            InstId::Vpcmpistrm as u32,
            &[dst.as_operand(), src.as_operand(), imm.as_operand()],
        ); // AVX [IMPLICIT]
    }
}
