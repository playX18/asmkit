pub trait X86AVX512FEmitter: Emitter {
    /// Emits `KANDNWKKK`.
    fn kandnwkkk(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(KANDNWKKK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `KANDWKKK`.
    fn kandwkkk(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(KANDWKKK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `KMOVWK`.
    fn kmovwk(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mask() && op1.is_mem() {
            self.emit(KMOVWKM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_mask() {
            self.emit(KMOVWMK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mask() && op1.is_gp() {
            self.emit(KMOVWKR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_gp() && op1.is_mask() {
            self.emit(KMOVWRK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for KMOVWK");
        }
    }
    /// Emits `KMOVWKK`.
    fn kmovwkk(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(KMOVWKK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `KNOTWKK`.
    fn knotwkk(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(KNOTWKK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `KORTESTWKK`.
    fn kortestwkk(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(KORTESTWKK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `KORWKKK`.
    fn korwkkk(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(KORWKKK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `KSHIFTLWKKI`.
    fn kshiftlwkk(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(KSHIFTLWKKI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `KSHIFTRWKKI`.
    fn kshiftrwkk(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(KSHIFTRWKKI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `KUNPCKBWKKK`.
    fn kunpckbwkkk(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(KUNPCKBWKKK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `KXNORWKKK`.
    fn kxnorwkkk(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(KXNORWKKK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `KXORWKKK`.
    fn kxorwkkk(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(KXORWKKK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPD128`.
    fn vaddpd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDPD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDPD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDPD128");
        }
    }
    /// Emits `VADDPD128_MASK`.
    fn vaddpd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDPD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDPD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDPD128_MASK");
        }
    }
    /// Emits `VADDPD128_MASKZ`.
    fn vaddpd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDPD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDPD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDPD128_MASKZ");
        }
    }
    /// Emits `VADDPD128RRB`.
    fn vaddpd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPD128RRB_MASK`.
    fn vaddpd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPD128RRB_MASKZ`.
    fn vaddpd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPD256`.
    fn vaddpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDPD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDPD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDPD256");
        }
    }
    /// Emits `VADDPD256_MASK`.
    fn vaddpd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDPD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDPD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDPD256_MASK");
        }
    }
    /// Emits `VADDPD256_MASKZ`.
    fn vaddpd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDPD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDPD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDPD256_MASKZ");
        }
    }
    /// Emits `VADDPD256RRB`.
    fn vaddpd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPD256RRB_MASK`.
    fn vaddpd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPD256RRB_MASKZ`.
    fn vaddpd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPD512`.
    fn vaddpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDPD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDPD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDPD512");
        }
    }
    /// Emits `VADDPD512RRR_ER`.
    fn vaddpd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPD512_MASK`.
    fn vaddpd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDPD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDPD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDPD512_MASK");
        }
    }
    /// Emits `VADDPD512RRR_MASK_ER`.
    fn vaddpd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPD512_MASKZ`.
    fn vaddpd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDPD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDPD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDPD512_MASKZ");
        }
    }
    /// Emits `VADDPD512RRR_MASKZ_ER`.
    fn vaddpd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPD512RRB`.
    fn vaddpd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPD512RRB_MASK`.
    fn vaddpd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPD512RRB_MASKZ`.
    fn vaddpd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPS128`.
    fn vaddps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDPS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDPS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDPS128");
        }
    }
    /// Emits `VADDPS128_MASK`.
    fn vaddps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDPS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDPS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDPS128_MASK");
        }
    }
    /// Emits `VADDPS128_MASKZ`.
    fn vaddps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDPS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDPS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDPS128_MASKZ");
        }
    }
    /// Emits `VADDPS128RRB`.
    fn vaddps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPS128RRB_MASK`.
    fn vaddps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPS128RRB_MASKZ`.
    fn vaddps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPS256`.
    fn vaddps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDPS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDPS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDPS256");
        }
    }
    /// Emits `VADDPS256_MASK`.
    fn vaddps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDPS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDPS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDPS256_MASK");
        }
    }
    /// Emits `VADDPS256_MASKZ`.
    fn vaddps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDPS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDPS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDPS256_MASKZ");
        }
    }
    /// Emits `VADDPS256RRB`.
    fn vaddps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPS256RRB_MASK`.
    fn vaddps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPS256RRB_MASKZ`.
    fn vaddps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPS512`.
    fn vaddps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDPS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDPS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDPS512");
        }
    }
    /// Emits `VADDPS512RRR_ER`.
    fn vaddps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPS512_MASK`.
    fn vaddps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDPS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDPS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDPS512_MASK");
        }
    }
    /// Emits `VADDPS512RRR_MASK_ER`.
    fn vaddps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPS512_MASKZ`.
    fn vaddps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDPS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDPS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDPS512_MASKZ");
        }
    }
    /// Emits `VADDPS512RRR_MASKZ_ER`.
    fn vaddps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPS512RRB`.
    fn vaddps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPS512RRB_MASK`.
    fn vaddps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDPS512RRB_MASKZ`.
    fn vaddps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDPS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDSD`.
    fn vaddsd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDSDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDSDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDSD");
        }
    }
    /// Emits `VADDSDRRR_ER`.
    fn vaddsd_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDSDRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDSD_MASK`.
    fn vaddsd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDSDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDSDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDSD_MASK");
        }
    }
    /// Emits `VADDSDRRR_MASK_ER`.
    fn vaddsd_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDSDRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDSD_MASKZ`.
    fn vaddsd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDSDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDSDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDSD_MASKZ");
        }
    }
    /// Emits `VADDSDRRR_MASKZ_ER`.
    fn vaddsd_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDSDRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDSS`.
    fn vaddss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDSSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDSSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDSS");
        }
    }
    /// Emits `VADDSSRRR_ER`.
    fn vaddss_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDSSRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDSS_MASK`.
    fn vaddss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDSSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDSSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDSS_MASK");
        }
    }
    /// Emits `VADDSSRRR_MASK_ER`.
    fn vaddss_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDSSRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VADDSS_MASKZ`.
    fn vaddss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VADDSSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VADDSSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VADDSS_MASKZ");
        }
    }
    /// Emits `VADDSSRRR_MASKZ_ER`.
    fn vaddss_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VADDSSRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VALIGND128`.
    fn valignd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VALIGND128RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VALIGND128RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VALIGND128");
        }
    }
    /// Emits `VALIGND128_MASK`.
    fn valignd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VALIGND128RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VALIGND128RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VALIGND128_MASK");
        }
    }
    /// Emits `VALIGND128_MASKZ`.
    fn valignd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VALIGND128RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VALIGND128RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VALIGND128_MASKZ");
        }
    }
    /// Emits `VALIGND128RRBI`.
    fn valignd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VALIGND128RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VALIGND128RRBI_MASK`.
    fn valignd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VALIGND128RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VALIGND128RRBI_MASKZ`.
    fn valignd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VALIGND128RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VALIGND256`.
    fn valignd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VALIGND256RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VALIGND256RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VALIGND256");
        }
    }
    /// Emits `VALIGND256_MASK`.
    fn valignd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VALIGND256RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VALIGND256RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VALIGND256_MASK");
        }
    }
    /// Emits `VALIGND256_MASKZ`.
    fn valignd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VALIGND256RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VALIGND256RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VALIGND256_MASKZ");
        }
    }
    /// Emits `VALIGND256RRBI`.
    fn valignd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VALIGND256RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VALIGND256RRBI_MASK`.
    fn valignd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VALIGND256RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VALIGND256RRBI_MASKZ`.
    fn valignd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VALIGND256RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VALIGND512`.
    fn valignd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VALIGND512RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VALIGND512RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VALIGND512");
        }
    }
    /// Emits `VALIGND512_MASK`.
    fn valignd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VALIGND512RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VALIGND512RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VALIGND512_MASK");
        }
    }
    /// Emits `VALIGND512_MASKZ`.
    fn valignd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VALIGND512RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VALIGND512RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VALIGND512_MASKZ");
        }
    }
    /// Emits `VALIGND512RRBI`.
    fn valignd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VALIGND512RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VALIGND512RRBI_MASK`.
    fn valignd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VALIGND512RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VALIGND512RRBI_MASKZ`.
    fn valignd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VALIGND512RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VALIGNQ128`.
    fn valignq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VALIGNQ128RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VALIGNQ128RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VALIGNQ128");
        }
    }
    /// Emits `VALIGNQ128_MASK`.
    fn valignq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VALIGNQ128RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VALIGNQ128RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VALIGNQ128_MASK");
        }
    }
    /// Emits `VALIGNQ128_MASKZ`.
    fn valignq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VALIGNQ128RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VALIGNQ128RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VALIGNQ128_MASKZ");
        }
    }
    /// Emits `VALIGNQ128RRBI`.
    fn valignq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VALIGNQ128RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VALIGNQ128RRBI_MASK`.
    fn valignq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VALIGNQ128RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VALIGNQ128RRBI_MASKZ`.
    fn valignq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VALIGNQ128RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VALIGNQ256`.
    fn valignq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VALIGNQ256RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VALIGNQ256RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VALIGNQ256");
        }
    }
    /// Emits `VALIGNQ256_MASK`.
    fn valignq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VALIGNQ256RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VALIGNQ256RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VALIGNQ256_MASK");
        }
    }
    /// Emits `VALIGNQ256_MASKZ`.
    fn valignq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VALIGNQ256RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VALIGNQ256RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VALIGNQ256_MASKZ");
        }
    }
    /// Emits `VALIGNQ256RRBI`.
    fn valignq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VALIGNQ256RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VALIGNQ256RRBI_MASK`.
    fn valignq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VALIGNQ256RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VALIGNQ256RRBI_MASKZ`.
    fn valignq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VALIGNQ256RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VALIGNQ512`.
    fn valignq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VALIGNQ512RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VALIGNQ512RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VALIGNQ512");
        }
    }
    /// Emits `VALIGNQ512_MASK`.
    fn valignq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VALIGNQ512RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VALIGNQ512RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VALIGNQ512_MASK");
        }
    }
    /// Emits `VALIGNQ512_MASKZ`.
    fn valignq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VALIGNQ512RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VALIGNQ512RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VALIGNQ512_MASKZ");
        }
    }
    /// Emits `VALIGNQ512RRBI`.
    fn valignq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VALIGNQ512RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VALIGNQ512RRBI_MASK`.
    fn valignq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VALIGNQ512RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VALIGNQ512RRBI_MASKZ`.
    fn valignq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VALIGNQ512RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VBLENDMPD128`.
    fn vblendmpd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VBLENDMPD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VBLENDMPD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VBLENDMPD128");
        }
    }
    /// Emits `VBLENDMPD128_MASK`.
    fn vblendmpd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VBLENDMPD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VBLENDMPD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VBLENDMPD128_MASK");
        }
    }
    /// Emits `VBLENDMPD128_MASKZ`.
    fn vblendmpd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VBLENDMPD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VBLENDMPD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VBLENDMPD128_MASKZ");
        }
    }
    /// Emits `VBLENDMPD128RRB`.
    fn vblendmpd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VBLENDMPD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VBLENDMPD128RRB_MASK`.
    fn vblendmpd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VBLENDMPD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VBLENDMPD128RRB_MASKZ`.
    fn vblendmpd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VBLENDMPD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VBLENDMPD256`.
    fn vblendmpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VBLENDMPD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VBLENDMPD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VBLENDMPD256");
        }
    }
    /// Emits `VBLENDMPD256_MASK`.
    fn vblendmpd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VBLENDMPD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VBLENDMPD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VBLENDMPD256_MASK");
        }
    }
    /// Emits `VBLENDMPD256_MASKZ`.
    fn vblendmpd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VBLENDMPD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VBLENDMPD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VBLENDMPD256_MASKZ");
        }
    }
    /// Emits `VBLENDMPD256RRB`.
    fn vblendmpd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VBLENDMPD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VBLENDMPD256RRB_MASK`.
    fn vblendmpd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VBLENDMPD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VBLENDMPD256RRB_MASKZ`.
    fn vblendmpd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VBLENDMPD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VBLENDMPD512`.
    fn vblendmpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VBLENDMPD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VBLENDMPD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VBLENDMPD512");
        }
    }
    /// Emits `VBLENDMPD512_MASK`.
    fn vblendmpd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VBLENDMPD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VBLENDMPD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VBLENDMPD512_MASK");
        }
    }
    /// Emits `VBLENDMPD512_MASKZ`.
    fn vblendmpd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VBLENDMPD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VBLENDMPD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VBLENDMPD512_MASKZ");
        }
    }
    /// Emits `VBLENDMPD512RRB`.
    fn vblendmpd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VBLENDMPD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VBLENDMPD512RRB_MASK`.
    fn vblendmpd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VBLENDMPD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VBLENDMPD512RRB_MASKZ`.
    fn vblendmpd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VBLENDMPD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VBLENDMPS128`.
    fn vblendmps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VBLENDMPS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VBLENDMPS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VBLENDMPS128");
        }
    }
    /// Emits `VBLENDMPS128_MASK`.
    fn vblendmps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VBLENDMPS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VBLENDMPS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VBLENDMPS128_MASK");
        }
    }
    /// Emits `VBLENDMPS128_MASKZ`.
    fn vblendmps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VBLENDMPS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VBLENDMPS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VBLENDMPS128_MASKZ");
        }
    }
    /// Emits `VBLENDMPS128RRB`.
    fn vblendmps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VBLENDMPS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VBLENDMPS128RRB_MASK`.
    fn vblendmps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VBLENDMPS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VBLENDMPS128RRB_MASKZ`.
    fn vblendmps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VBLENDMPS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VBLENDMPS256`.
    fn vblendmps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VBLENDMPS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VBLENDMPS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VBLENDMPS256");
        }
    }
    /// Emits `VBLENDMPS256_MASK`.
    fn vblendmps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VBLENDMPS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VBLENDMPS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VBLENDMPS256_MASK");
        }
    }
    /// Emits `VBLENDMPS256_MASKZ`.
    fn vblendmps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VBLENDMPS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VBLENDMPS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VBLENDMPS256_MASKZ");
        }
    }
    /// Emits `VBLENDMPS256RRB`.
    fn vblendmps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VBLENDMPS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VBLENDMPS256RRB_MASK`.
    fn vblendmps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VBLENDMPS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VBLENDMPS256RRB_MASKZ`.
    fn vblendmps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VBLENDMPS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VBLENDMPS512`.
    fn vblendmps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VBLENDMPS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VBLENDMPS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VBLENDMPS512");
        }
    }
    /// Emits `VBLENDMPS512_MASK`.
    fn vblendmps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VBLENDMPS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VBLENDMPS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VBLENDMPS512_MASK");
        }
    }
    /// Emits `VBLENDMPS512_MASKZ`.
    fn vblendmps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VBLENDMPS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VBLENDMPS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VBLENDMPS512_MASKZ");
        }
    }
    /// Emits `VBLENDMPS512RRB`.
    fn vblendmps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VBLENDMPS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VBLENDMPS512RRB_MASK`.
    fn vblendmps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VBLENDMPS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VBLENDMPS512RRB_MASKZ`.
    fn vblendmps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VBLENDMPS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VBROADCASTF32X4_256RM`.
    fn vbroadcastf32x4_256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VBROADCASTF32X4_256RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VBROADCASTF32X4_256RM_MASK`.
    fn vbroadcastf32x4_256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VBROADCASTF32X4_256RM_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VBROADCASTF32X4_256RM_MASKZ`.
    fn vbroadcastf32x4_256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VBROADCASTF32X4_256RM_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VBROADCASTF32X4_512RM`.
    fn vbroadcastf32x4_512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VBROADCASTF32X4_512RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VBROADCASTF32X4_512RM_MASK`.
    fn vbroadcastf32x4_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VBROADCASTF32X4_512RM_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VBROADCASTF32X4_512RM_MASKZ`.
    fn vbroadcastf32x4_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VBROADCASTF32X4_512RM_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VBROADCASTF64X4_512RM`.
    fn vbroadcastf64x4_512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VBROADCASTF64X4_512RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VBROADCASTF64X4_512RM_MASK`.
    fn vbroadcastf64x4_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VBROADCASTF64X4_512RM_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VBROADCASTF64X4_512RM_MASKZ`.
    fn vbroadcastf64x4_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VBROADCASTF64X4_512RM_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VBROADCASTI64X4_512RM`.
    fn vbroadcasti64x4_512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VBROADCASTI64X4_512RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VBROADCASTI64X4_512RM_MASK`.
    fn vbroadcasti64x4_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VBROADCASTI64X4_512RM_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VBROADCASTI64X4_512RM_MASKZ`.
    fn vbroadcasti64x4_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VBROADCASTI64X4_512RM_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VBROADCASTSD256`.
    fn vbroadcastsd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VBROADCASTSD256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VBROADCASTSD256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VBROADCASTSD256");
        }
    }
    /// Emits `VBROADCASTSD256_MASK`.
    fn vbroadcastsd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VBROADCASTSD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VBROADCASTSD256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VBROADCASTSD256_MASK");
        }
    }
    /// Emits `VBROADCASTSD256_MASKZ`.
    fn vbroadcastsd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VBROADCASTSD256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VBROADCASTSD256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VBROADCASTSD256_MASKZ");
        }
    }
    /// Emits `VBROADCASTSD512`.
    fn vbroadcastsd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VBROADCASTSD512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VBROADCASTSD512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VBROADCASTSD512");
        }
    }
    /// Emits `VBROADCASTSD512_MASK`.
    fn vbroadcastsd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VBROADCASTSD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VBROADCASTSD512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VBROADCASTSD512_MASK");
        }
    }
    /// Emits `VBROADCASTSD512_MASKZ`.
    fn vbroadcastsd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VBROADCASTSD512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VBROADCASTSD512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VBROADCASTSD512_MASKZ");
        }
    }
    /// Emits `VBROADCASTSS128`.
    fn vbroadcastss128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VBROADCASTSS128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VBROADCASTSS128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VBROADCASTSS128");
        }
    }
    /// Emits `VBROADCASTSS128_MASK`.
    fn vbroadcastss128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VBROADCASTSS128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VBROADCASTSS128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VBROADCASTSS128_MASK");
        }
    }
    /// Emits `VBROADCASTSS128_MASKZ`.
    fn vbroadcastss128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VBROADCASTSS128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VBROADCASTSS128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VBROADCASTSS128_MASKZ");
        }
    }
    /// Emits `VBROADCASTSS256`.
    fn vbroadcastss256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VBROADCASTSS256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VBROADCASTSS256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VBROADCASTSS256");
        }
    }
    /// Emits `VBROADCASTSS256_MASK`.
    fn vbroadcastss256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VBROADCASTSS256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VBROADCASTSS256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VBROADCASTSS256_MASK");
        }
    }
    /// Emits `VBROADCASTSS256_MASKZ`.
    fn vbroadcastss256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VBROADCASTSS256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VBROADCASTSS256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VBROADCASTSS256_MASKZ");
        }
    }
    /// Emits `VBROADCASTSS512`.
    fn vbroadcastss512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VBROADCASTSS512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VBROADCASTSS512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VBROADCASTSS512");
        }
    }
    /// Emits `VBROADCASTSS512_MASK`.
    fn vbroadcastss512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VBROADCASTSS512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VBROADCASTSS512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VBROADCASTSS512_MASK");
        }
    }
    /// Emits `VBROADCASTSS512_MASKZ`.
    fn vbroadcastss512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VBROADCASTSS512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VBROADCASTSS512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VBROADCASTSS512_MASKZ");
        }
    }
    /// Emits `VCMPPD128K`.
    fn vcmppd128k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VCMPPD128KRRI, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VCMPPD128KRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VCMPPD128K");
        }
    }
    /// Emits `VCMPPD128K_MASK`.
    fn vcmppd128k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VCMPPD128KRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VCMPPD128KRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VCMPPD128K_MASK");
        }
    }
    /// Emits `VCMPPD128KRBI`.
    fn vcmppd128kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VCMPPD128KRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VCMPPD128KRBI_MASK`.
    fn vcmppd128kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VCMPPD128KRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VCMPPD256K`.
    fn vcmppd256k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VCMPPD256KRRI, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VCMPPD256KRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VCMPPD256K");
        }
    }
    /// Emits `VCMPPD256K_MASK`.
    fn vcmppd256k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VCMPPD256KRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VCMPPD256KRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VCMPPD256K_MASK");
        }
    }
    /// Emits `VCMPPD256KRBI`.
    fn vcmppd256kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VCMPPD256KRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VCMPPD256KRBI_MASK`.
    fn vcmppd256kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VCMPPD256KRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VCMPPD512K`.
    fn vcmppd512k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VCMPPD512KRRI, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VCMPPD512KRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VCMPPD512K");
        }
    }
    /// Emits `VCMPPD512K_MASK`.
    fn vcmppd512k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VCMPPD512KRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VCMPPD512KRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VCMPPD512K_MASK");
        }
    }
    /// Emits `VCMPPD512KRRI_MASK_SAE`.
    fn vcmppd512k_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VCMPPD512KRRI_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VCMPPD512KRRI_SAE`.
    fn vcmppd512k_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VCMPPD512KRRI_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VCMPPD512KRBI`.
    fn vcmppd512kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VCMPPD512KRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VCMPPD512KRBI_MASK`.
    fn vcmppd512kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VCMPPD512KRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VCMPPS128K`.
    fn vcmpps128k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VCMPPS128KRRI, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VCMPPS128KRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VCMPPS128K");
        }
    }
    /// Emits `VCMPPS128K_MASK`.
    fn vcmpps128k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VCMPPS128KRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VCMPPS128KRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VCMPPS128K_MASK");
        }
    }
    /// Emits `VCMPPS128KRBI`.
    fn vcmpps128kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VCMPPS128KRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VCMPPS128KRBI_MASK`.
    fn vcmpps128kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VCMPPS128KRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VCMPPS256K`.
    fn vcmpps256k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VCMPPS256KRRI, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VCMPPS256KRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VCMPPS256K");
        }
    }
    /// Emits `VCMPPS256K_MASK`.
    fn vcmpps256k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VCMPPS256KRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VCMPPS256KRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VCMPPS256K_MASK");
        }
    }
    /// Emits `VCMPPS256KRBI`.
    fn vcmpps256kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VCMPPS256KRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VCMPPS256KRBI_MASK`.
    fn vcmpps256kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VCMPPS256KRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VCMPPS512K`.
    fn vcmpps512k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VCMPPS512KRRI, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VCMPPS512KRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VCMPPS512K");
        }
    }
    /// Emits `VCMPPS512K_MASK`.
    fn vcmpps512k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VCMPPS512KRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VCMPPS512KRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VCMPPS512K_MASK");
        }
    }
    /// Emits `VCMPPS512KRRI_MASK_SAE`.
    fn vcmpps512k_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VCMPPS512KRRI_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VCMPPS512KRRI_SAE`.
    fn vcmpps512k_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VCMPPS512KRRI_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VCMPPS512KRBI`.
    fn vcmpps512kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VCMPPS512KRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VCMPPS512KRBI_MASK`.
    fn vcmpps512kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VCMPPS512KRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VCMPSDK`.
    fn vcmpsdk(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VCMPSDKRRI, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VCMPSDKRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VCMPSDK");
        }
    }
    /// Emits `VCMPSDK_MASK`.
    fn vcmpsdk_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VCMPSDKRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VCMPSDKRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VCMPSDK_MASK");
        }
    }
    /// Emits `VCMPSDKRRI_MASK_SAE`.
    fn vcmpsdk_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VCMPSDKRRI_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VCMPSDKRRI_SAE`.
    fn vcmpsdk_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VCMPSDKRRI_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VCMPSSK`.
    fn vcmpssk(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VCMPSSKRRI, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VCMPSSKRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VCMPSSK");
        }
    }
    /// Emits `VCMPSSK_MASK`.
    fn vcmpssk_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VCMPSSKRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VCMPSSKRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VCMPSSK_MASK");
        }
    }
    /// Emits `VCMPSSKRRI_MASK_SAE`.
    fn vcmpssk_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VCMPSSKRRI_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VCMPSSKRRI_SAE`.
    fn vcmpssk_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VCMPSSKRRI_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VCOMISD`.
    fn vcomisd(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCOMISDRR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCOMISDRM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCOMISD");
        }
    }
    /// Emits `VCOMISDRR_SAE`.
    fn vcomisd_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCOMISDRR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCOMISS`.
    fn vcomiss(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCOMISSRR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCOMISSRM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCOMISS");
        }
    }
    /// Emits `VCOMISSRR_SAE`.
    fn vcomiss_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCOMISSRR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCOMPRESSPD128`.
    fn vcompresspd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VCOMPRESSPD128MR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VCOMPRESSPD128RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCOMPRESSPD128");
        }
    }
    /// Emits `VCOMPRESSPD128_MASK`.
    fn vcompresspd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VCOMPRESSPD128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VCOMPRESSPD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCOMPRESSPD128_MASK");
        }
    }
    /// Emits `VCOMPRESSPD128RR_MASKZ`.
    fn vcompresspd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCOMPRESSPD128RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCOMPRESSPD256`.
    fn vcompresspd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VCOMPRESSPD256MR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VCOMPRESSPD256RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCOMPRESSPD256");
        }
    }
    /// Emits `VCOMPRESSPD256_MASK`.
    fn vcompresspd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VCOMPRESSPD256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VCOMPRESSPD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCOMPRESSPD256_MASK");
        }
    }
    /// Emits `VCOMPRESSPD256RR_MASKZ`.
    fn vcompresspd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCOMPRESSPD256RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCOMPRESSPD512`.
    fn vcompresspd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VCOMPRESSPD512MR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VCOMPRESSPD512RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCOMPRESSPD512");
        }
    }
    /// Emits `VCOMPRESSPD512_MASK`.
    fn vcompresspd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VCOMPRESSPD512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VCOMPRESSPD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCOMPRESSPD512_MASK");
        }
    }
    /// Emits `VCOMPRESSPD512RR_MASKZ`.
    fn vcompresspd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCOMPRESSPD512RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCOMPRESSPS128`.
    fn vcompressps128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VCOMPRESSPS128MR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VCOMPRESSPS128RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCOMPRESSPS128");
        }
    }
    /// Emits `VCOMPRESSPS128_MASK`.
    fn vcompressps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VCOMPRESSPS128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VCOMPRESSPS128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCOMPRESSPS128_MASK");
        }
    }
    /// Emits `VCOMPRESSPS128RR_MASKZ`.
    fn vcompressps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCOMPRESSPS128RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCOMPRESSPS256`.
    fn vcompressps256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VCOMPRESSPS256MR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VCOMPRESSPS256RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCOMPRESSPS256");
        }
    }
    /// Emits `VCOMPRESSPS256_MASK`.
    fn vcompressps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VCOMPRESSPS256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VCOMPRESSPS256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCOMPRESSPS256_MASK");
        }
    }
    /// Emits `VCOMPRESSPS256RR_MASKZ`.
    fn vcompressps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCOMPRESSPS256RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCOMPRESSPS512`.
    fn vcompressps512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VCOMPRESSPS512MR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VCOMPRESSPS512RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCOMPRESSPS512");
        }
    }
    /// Emits `VCOMPRESSPS512_MASK`.
    fn vcompressps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VCOMPRESSPS512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VCOMPRESSPS512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCOMPRESSPS512_MASK");
        }
    }
    /// Emits `VCOMPRESSPS512RR_MASKZ`.
    fn vcompressps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCOMPRESSPS512RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PD128`.
    fn vcvtdq2pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTDQ2PD128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTDQ2PD128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTDQ2PD128");
        }
    }
    /// Emits `VCVTDQ2PD128_MASK`.
    fn vcvtdq2pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTDQ2PD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTDQ2PD128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTDQ2PD128_MASK");
        }
    }
    /// Emits `VCVTDQ2PD128_MASKZ`.
    fn vcvtdq2pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTDQ2PD128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTDQ2PD128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTDQ2PD128_MASKZ");
        }
    }
    /// Emits `VCVTDQ2PD128RB`.
    fn vcvtdq2pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PD128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PD128RB_MASK`.
    fn vcvtdq2pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PD128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PD128RB_MASKZ`.
    fn vcvtdq2pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PD128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PD256`.
    fn vcvtdq2pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTDQ2PD256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTDQ2PD256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTDQ2PD256");
        }
    }
    /// Emits `VCVTDQ2PD256_MASK`.
    fn vcvtdq2pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTDQ2PD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTDQ2PD256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTDQ2PD256_MASK");
        }
    }
    /// Emits `VCVTDQ2PD256_MASKZ`.
    fn vcvtdq2pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTDQ2PD256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTDQ2PD256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTDQ2PD256_MASKZ");
        }
    }
    /// Emits `VCVTDQ2PD256RB`.
    fn vcvtdq2pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PD256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PD256RB_MASK`.
    fn vcvtdq2pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PD256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PD256RB_MASKZ`.
    fn vcvtdq2pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PD256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PD512`.
    fn vcvtdq2pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTDQ2PD512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTDQ2PD512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTDQ2PD512");
        }
    }
    /// Emits `VCVTDQ2PD512_MASK`.
    fn vcvtdq2pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTDQ2PD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTDQ2PD512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTDQ2PD512_MASK");
        }
    }
    /// Emits `VCVTDQ2PD512RR_MASK_SAE`.
    fn vcvtdq2pd512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PD512RR_MASK_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PD512_MASKZ`.
    fn vcvtdq2pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTDQ2PD512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTDQ2PD512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTDQ2PD512_MASKZ");
        }
    }
    /// Emits `VCVTDQ2PD512RR_MASKZ_SAE`.
    fn vcvtdq2pd512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PD512RR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PD512RR_SAE`.
    fn vcvtdq2pd512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PD512RR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PD512RB`.
    fn vcvtdq2pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PD512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PD512RB_MASK`.
    fn vcvtdq2pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PD512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PD512RB_MASKZ`.
    fn vcvtdq2pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PD512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PS128`.
    fn vcvtdq2ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTDQ2PS128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTDQ2PS128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTDQ2PS128");
        }
    }
    /// Emits `VCVTDQ2PS128_MASK`.
    fn vcvtdq2ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTDQ2PS128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTDQ2PS128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTDQ2PS128_MASK");
        }
    }
    /// Emits `VCVTDQ2PS128_MASKZ`.
    fn vcvtdq2ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTDQ2PS128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTDQ2PS128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTDQ2PS128_MASKZ");
        }
    }
    /// Emits `VCVTDQ2PS128RB`.
    fn vcvtdq2ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PS128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PS128RB_MASK`.
    fn vcvtdq2ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PS128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PS128RB_MASKZ`.
    fn vcvtdq2ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PS128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PS256`.
    fn vcvtdq2ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTDQ2PS256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTDQ2PS256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTDQ2PS256");
        }
    }
    /// Emits `VCVTDQ2PS256_MASK`.
    fn vcvtdq2ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTDQ2PS256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTDQ2PS256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTDQ2PS256_MASK");
        }
    }
    /// Emits `VCVTDQ2PS256_MASKZ`.
    fn vcvtdq2ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTDQ2PS256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTDQ2PS256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTDQ2PS256_MASKZ");
        }
    }
    /// Emits `VCVTDQ2PS256RB`.
    fn vcvtdq2ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PS256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PS256RB_MASK`.
    fn vcvtdq2ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PS256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PS256RB_MASKZ`.
    fn vcvtdq2ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PS256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PS512`.
    fn vcvtdq2ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTDQ2PS512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTDQ2PS512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTDQ2PS512");
        }
    }
    /// Emits `VCVTDQ2PS512RR_ER`.
    fn vcvtdq2ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PS512RR_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PS512_MASK`.
    fn vcvtdq2ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTDQ2PS512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTDQ2PS512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTDQ2PS512_MASK");
        }
    }
    /// Emits `VCVTDQ2PS512RR_MASK_ER`.
    fn vcvtdq2ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PS512RR_MASK_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PS512_MASKZ`.
    fn vcvtdq2ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTDQ2PS512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTDQ2PS512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTDQ2PS512_MASKZ");
        }
    }
    /// Emits `VCVTDQ2PS512RR_MASKZ_ER`.
    fn vcvtdq2ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PS512RR_MASKZ_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PS512RB`.
    fn vcvtdq2ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PS512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PS512RB_MASK`.
    fn vcvtdq2ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PS512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTDQ2PS512RB_MASKZ`.
    fn vcvtdq2ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTDQ2PS512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2DQ128`.
    fn vcvtpd2dq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2DQ128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2DQ128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2DQ128");
        }
    }
    /// Emits `VCVTPD2DQ128_MASK`.
    fn vcvtpd2dq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2DQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2DQ128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2DQ128_MASK");
        }
    }
    /// Emits `VCVTPD2DQ128_MASKZ`.
    fn vcvtpd2dq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2DQ128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2DQ128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2DQ128_MASKZ");
        }
    }
    /// Emits `VCVTPD2DQ128RB`.
    fn vcvtpd2dq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2DQ128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2DQ128RB_MASK`.
    fn vcvtpd2dq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2DQ128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2DQ128RB_MASKZ`.
    fn vcvtpd2dq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2DQ128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2DQ256`.
    fn vcvtpd2dq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2DQ256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2DQ256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2DQ256");
        }
    }
    /// Emits `VCVTPD2DQ256_MASK`.
    fn vcvtpd2dq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2DQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2DQ256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2DQ256_MASK");
        }
    }
    /// Emits `VCVTPD2DQ256_MASKZ`.
    fn vcvtpd2dq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2DQ256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2DQ256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2DQ256_MASKZ");
        }
    }
    /// Emits `VCVTPD2DQ256RB`.
    fn vcvtpd2dq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2DQ256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2DQ256RB_MASK`.
    fn vcvtpd2dq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2DQ256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2DQ256RB_MASKZ`.
    fn vcvtpd2dq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2DQ256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2DQ512`.
    fn vcvtpd2dq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2DQ512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2DQ512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2DQ512");
        }
    }
    /// Emits `VCVTPD2DQ512RR_ER`.
    fn vcvtpd2dq512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2DQ512RR_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2DQ512_MASK`.
    fn vcvtpd2dq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2DQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2DQ512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2DQ512_MASK");
        }
    }
    /// Emits `VCVTPD2DQ512RR_MASK_ER`.
    fn vcvtpd2dq512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2DQ512RR_MASK_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2DQ512_MASKZ`.
    fn vcvtpd2dq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2DQ512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2DQ512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2DQ512_MASKZ");
        }
    }
    /// Emits `VCVTPD2DQ512RR_MASKZ_ER`.
    fn vcvtpd2dq512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2DQ512RR_MASKZ_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2DQ512RB`.
    fn vcvtpd2dq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2DQ512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2DQ512RB_MASK`.
    fn vcvtpd2dq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2DQ512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2DQ512RB_MASKZ`.
    fn vcvtpd2dq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2DQ512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2PS128`.
    fn vcvtpd2ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2PS128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2PS128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2PS128");
        }
    }
    /// Emits `VCVTPD2PS128_MASK`.
    fn vcvtpd2ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2PS128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2PS128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2PS128_MASK");
        }
    }
    /// Emits `VCVTPD2PS128_MASKZ`.
    fn vcvtpd2ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2PS128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2PS128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2PS128_MASKZ");
        }
    }
    /// Emits `VCVTPD2PS128RB`.
    fn vcvtpd2ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2PS128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2PS128RB_MASK`.
    fn vcvtpd2ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2PS128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2PS128RB_MASKZ`.
    fn vcvtpd2ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2PS128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2PS256`.
    fn vcvtpd2ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2PS256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2PS256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2PS256");
        }
    }
    /// Emits `VCVTPD2PS256_MASK`.
    fn vcvtpd2ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2PS256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2PS256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2PS256_MASK");
        }
    }
    /// Emits `VCVTPD2PS256_MASKZ`.
    fn vcvtpd2ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2PS256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2PS256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2PS256_MASKZ");
        }
    }
    /// Emits `VCVTPD2PS256RB`.
    fn vcvtpd2ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2PS256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2PS256RB_MASK`.
    fn vcvtpd2ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2PS256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2PS256RB_MASKZ`.
    fn vcvtpd2ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2PS256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2PS512`.
    fn vcvtpd2ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2PS512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2PS512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2PS512");
        }
    }
    /// Emits `VCVTPD2PS512RR_ER`.
    fn vcvtpd2ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2PS512RR_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2PS512_MASK`.
    fn vcvtpd2ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2PS512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2PS512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2PS512_MASK");
        }
    }
    /// Emits `VCVTPD2PS512RR_MASK_ER`.
    fn vcvtpd2ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2PS512RR_MASK_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2PS512_MASKZ`.
    fn vcvtpd2ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2PS512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2PS512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2PS512_MASKZ");
        }
    }
    /// Emits `VCVTPD2PS512RR_MASKZ_ER`.
    fn vcvtpd2ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2PS512RR_MASKZ_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2PS512RB`.
    fn vcvtpd2ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2PS512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2PS512RB_MASK`.
    fn vcvtpd2ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2PS512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2PS512RB_MASKZ`.
    fn vcvtpd2ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2PS512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UDQ128`.
    fn vcvtpd2udq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2UDQ128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2UDQ128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2UDQ128");
        }
    }
    /// Emits `VCVTPD2UDQ128_MASK`.
    fn vcvtpd2udq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2UDQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2UDQ128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2UDQ128_MASK");
        }
    }
    /// Emits `VCVTPD2UDQ128_MASKZ`.
    fn vcvtpd2udq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2UDQ128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2UDQ128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2UDQ128_MASKZ");
        }
    }
    /// Emits `VCVTPD2UDQ128RB`.
    fn vcvtpd2udq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UDQ128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UDQ128RB_MASK`.
    fn vcvtpd2udq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UDQ128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UDQ128RB_MASKZ`.
    fn vcvtpd2udq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UDQ128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UDQ256`.
    fn vcvtpd2udq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2UDQ256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2UDQ256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2UDQ256");
        }
    }
    /// Emits `VCVTPD2UDQ256_MASK`.
    fn vcvtpd2udq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2UDQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2UDQ256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2UDQ256_MASK");
        }
    }
    /// Emits `VCVTPD2UDQ256_MASKZ`.
    fn vcvtpd2udq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2UDQ256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2UDQ256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2UDQ256_MASKZ");
        }
    }
    /// Emits `VCVTPD2UDQ256RB`.
    fn vcvtpd2udq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UDQ256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UDQ256RB_MASK`.
    fn vcvtpd2udq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UDQ256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UDQ256RB_MASKZ`.
    fn vcvtpd2udq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UDQ256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UDQ512`.
    fn vcvtpd2udq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2UDQ512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2UDQ512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2UDQ512");
        }
    }
    /// Emits `VCVTPD2UDQ512RR_ER`.
    fn vcvtpd2udq512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UDQ512RR_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UDQ512_MASK`.
    fn vcvtpd2udq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2UDQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2UDQ512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2UDQ512_MASK");
        }
    }
    /// Emits `VCVTPD2UDQ512RR_MASK_ER`.
    fn vcvtpd2udq512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UDQ512RR_MASK_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UDQ512_MASKZ`.
    fn vcvtpd2udq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2UDQ512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2UDQ512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2UDQ512_MASKZ");
        }
    }
    /// Emits `VCVTPD2UDQ512RR_MASKZ_ER`.
    fn vcvtpd2udq512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UDQ512RR_MASKZ_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UDQ512RB`.
    fn vcvtpd2udq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UDQ512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UDQ512RB_MASK`.
    fn vcvtpd2udq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UDQ512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UDQ512RB_MASKZ`.
    fn vcvtpd2udq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UDQ512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UQQ128`.
    fn vcvtpd2uqq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2UQQ128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2UQQ128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2UQQ128");
        }
    }
    /// Emits `VCVTPD2UQQ128_MASK`.
    fn vcvtpd2uqq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2UQQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2UQQ128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2UQQ128_MASK");
        }
    }
    /// Emits `VCVTPD2UQQ128_MASKZ`.
    fn vcvtpd2uqq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2UQQ128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2UQQ128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2UQQ128_MASKZ");
        }
    }
    /// Emits `VCVTPD2UQQ128RB`.
    fn vcvtpd2uqq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UQQ128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UQQ128RB_MASK`.
    fn vcvtpd2uqq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UQQ128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UQQ128RB_MASKZ`.
    fn vcvtpd2uqq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UQQ128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UQQ256`.
    fn vcvtpd2uqq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2UQQ256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2UQQ256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2UQQ256");
        }
    }
    /// Emits `VCVTPD2UQQ256_MASK`.
    fn vcvtpd2uqq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2UQQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2UQQ256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2UQQ256_MASK");
        }
    }
    /// Emits `VCVTPD2UQQ256_MASKZ`.
    fn vcvtpd2uqq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2UQQ256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2UQQ256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2UQQ256_MASKZ");
        }
    }
    /// Emits `VCVTPD2UQQ256RB`.
    fn vcvtpd2uqq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UQQ256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UQQ256RB_MASK`.
    fn vcvtpd2uqq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UQQ256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UQQ256RB_MASKZ`.
    fn vcvtpd2uqq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UQQ256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UQQ512`.
    fn vcvtpd2uqq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2UQQ512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2UQQ512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2UQQ512");
        }
    }
    /// Emits `VCVTPD2UQQ512RR_ER`.
    fn vcvtpd2uqq512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UQQ512RR_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UQQ512_MASK`.
    fn vcvtpd2uqq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2UQQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2UQQ512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2UQQ512_MASK");
        }
    }
    /// Emits `VCVTPD2UQQ512RR_MASK_ER`.
    fn vcvtpd2uqq512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UQQ512RR_MASK_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UQQ512_MASKZ`.
    fn vcvtpd2uqq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPD2UQQ512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPD2UQQ512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPD2UQQ512_MASKZ");
        }
    }
    /// Emits `VCVTPD2UQQ512RR_MASKZ_ER`.
    fn vcvtpd2uqq512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UQQ512RR_MASKZ_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UQQ512RB`.
    fn vcvtpd2uqq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UQQ512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UQQ512RB_MASK`.
    fn vcvtpd2uqq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UQQ512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPD2UQQ512RB_MASKZ`.
    fn vcvtpd2uqq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPD2UQQ512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPH2PS128`.
    fn vcvtph2ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPH2PS128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPH2PS128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPH2PS128");
        }
    }
    /// Emits `VCVTPH2PS128_MASK`.
    fn vcvtph2ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPH2PS128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPH2PS128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPH2PS128_MASK");
        }
    }
    /// Emits `VCVTPH2PS128_MASKZ`.
    fn vcvtph2ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPH2PS128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPH2PS128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPH2PS128_MASKZ");
        }
    }
    /// Emits `VCVTPH2PS256`.
    fn vcvtph2ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPH2PS256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPH2PS256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPH2PS256");
        }
    }
    /// Emits `VCVTPH2PS256_MASK`.
    fn vcvtph2ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPH2PS256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPH2PS256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPH2PS256_MASK");
        }
    }
    /// Emits `VCVTPH2PS256_MASKZ`.
    fn vcvtph2ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPH2PS256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPH2PS256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPH2PS256_MASKZ");
        }
    }
    /// Emits `VCVTPH2PS512`.
    fn vcvtph2ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPH2PS512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPH2PS512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPH2PS512");
        }
    }
    /// Emits `VCVTPH2PS512_MASK`.
    fn vcvtph2ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPH2PS512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPH2PS512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPH2PS512_MASK");
        }
    }
    /// Emits `VCVTPH2PS512RR_MASK_SAE`.
    fn vcvtph2ps512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPH2PS512RR_MASK_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPH2PS512_MASKZ`.
    fn vcvtph2ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPH2PS512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPH2PS512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPH2PS512_MASKZ");
        }
    }
    /// Emits `VCVTPH2PS512RR_MASKZ_SAE`.
    fn vcvtph2ps512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPH2PS512RR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPH2PS512RR_SAE`.
    fn vcvtph2ps512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPH2PS512RR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2DQ128`.
    fn vcvtps2dq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2DQ128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2DQ128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2DQ128");
        }
    }
    /// Emits `VCVTPS2DQ128_MASK`.
    fn vcvtps2dq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2DQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2DQ128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2DQ128_MASK");
        }
    }
    /// Emits `VCVTPS2DQ128_MASKZ`.
    fn vcvtps2dq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2DQ128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2DQ128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2DQ128_MASKZ");
        }
    }
    /// Emits `VCVTPS2DQ128RB`.
    fn vcvtps2dq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2DQ128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2DQ128RB_MASK`.
    fn vcvtps2dq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2DQ128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2DQ128RB_MASKZ`.
    fn vcvtps2dq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2DQ128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2DQ256`.
    fn vcvtps2dq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2DQ256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2DQ256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2DQ256");
        }
    }
    /// Emits `VCVTPS2DQ256_MASK`.
    fn vcvtps2dq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2DQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2DQ256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2DQ256_MASK");
        }
    }
    /// Emits `VCVTPS2DQ256_MASKZ`.
    fn vcvtps2dq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2DQ256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2DQ256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2DQ256_MASKZ");
        }
    }
    /// Emits `VCVTPS2DQ256RB`.
    fn vcvtps2dq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2DQ256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2DQ256RB_MASK`.
    fn vcvtps2dq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2DQ256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2DQ256RB_MASKZ`.
    fn vcvtps2dq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2DQ256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2DQ512`.
    fn vcvtps2dq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2DQ512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2DQ512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2DQ512");
        }
    }
    /// Emits `VCVTPS2DQ512RR_ER`.
    fn vcvtps2dq512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2DQ512RR_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2DQ512_MASK`.
    fn vcvtps2dq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2DQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2DQ512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2DQ512_MASK");
        }
    }
    /// Emits `VCVTPS2DQ512RR_MASK_ER`.
    fn vcvtps2dq512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2DQ512RR_MASK_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2DQ512_MASKZ`.
    fn vcvtps2dq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2DQ512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2DQ512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2DQ512_MASKZ");
        }
    }
    /// Emits `VCVTPS2DQ512RR_MASKZ_ER`.
    fn vcvtps2dq512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2DQ512RR_MASKZ_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2DQ512RB`.
    fn vcvtps2dq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2DQ512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2DQ512RB_MASK`.
    fn vcvtps2dq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2DQ512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2DQ512RB_MASKZ`.
    fn vcvtps2dq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2DQ512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2PD128`.
    fn vcvtps2pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2PD128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2PD128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2PD128");
        }
    }
    /// Emits `VCVTPS2PD128_MASK`.
    fn vcvtps2pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2PD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2PD128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2PD128_MASK");
        }
    }
    /// Emits `VCVTPS2PD128_MASKZ`.
    fn vcvtps2pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2PD128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2PD128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2PD128_MASKZ");
        }
    }
    /// Emits `VCVTPS2PD128RB`.
    fn vcvtps2pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2PD128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2PD128RB_MASK`.
    fn vcvtps2pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2PD128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2PD128RB_MASKZ`.
    fn vcvtps2pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2PD128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2PD256`.
    fn vcvtps2pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2PD256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2PD256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2PD256");
        }
    }
    /// Emits `VCVTPS2PD256_MASK`.
    fn vcvtps2pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2PD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2PD256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2PD256_MASK");
        }
    }
    /// Emits `VCVTPS2PD256_MASKZ`.
    fn vcvtps2pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2PD256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2PD256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2PD256_MASKZ");
        }
    }
    /// Emits `VCVTPS2PD256RB`.
    fn vcvtps2pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2PD256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2PD256RB_MASK`.
    fn vcvtps2pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2PD256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2PD256RB_MASKZ`.
    fn vcvtps2pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2PD256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2PD512`.
    fn vcvtps2pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2PD512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2PD512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2PD512");
        }
    }
    /// Emits `VCVTPS2PD512_MASK`.
    fn vcvtps2pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2PD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2PD512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2PD512_MASK");
        }
    }
    /// Emits `VCVTPS2PD512RR_MASK_SAE`.
    fn vcvtps2pd512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2PD512RR_MASK_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2PD512_MASKZ`.
    fn vcvtps2pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2PD512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2PD512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2PD512_MASKZ");
        }
    }
    /// Emits `VCVTPS2PD512RR_MASKZ_SAE`.
    fn vcvtps2pd512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2PD512RR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2PD512RR_SAE`.
    fn vcvtps2pd512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2PD512RR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2PD512RB`.
    fn vcvtps2pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2PD512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2PD512RB_MASK`.
    fn vcvtps2pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2PD512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2PD512RB_MASKZ`.
    fn vcvtps2pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2PD512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2PH128`.
    fn vcvtps2ph128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VCVTPS2PH128RRI, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VCVTPS2PH128MRI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2PH128");
        }
    }
    /// Emits `VCVTPS2PH128_MASK`.
    fn vcvtps2ph128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VCVTPS2PH128RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VCVTPS2PH128MRI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2PH128_MASK");
        }
    }
    /// Emits `VCVTPS2PH128RRI_MASKZ`.
    fn vcvtps2ph128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VCVTPS2PH128RRI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VCVTPS2PH256`.
    fn vcvtps2ph256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VCVTPS2PH256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VCVTPS2PH256MRI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2PH256");
        }
    }
    /// Emits `VCVTPS2PH256_MASK`.
    fn vcvtps2ph256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VCVTPS2PH256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VCVTPS2PH256MRI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2PH256_MASK");
        }
    }
    /// Emits `VCVTPS2PH256RRI_MASKZ`.
    fn vcvtps2ph256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VCVTPS2PH256RRI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VCVTPS2PH512`.
    fn vcvtps2ph512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VCVTPS2PH512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VCVTPS2PH512MRI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2PH512");
        }
    }
    /// Emits `VCVTPS2PH512_MASK`.
    fn vcvtps2ph512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VCVTPS2PH512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VCVTPS2PH512MRI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2PH512_MASK");
        }
    }
    /// Emits `VCVTPS2PH512RRI_MASK_SAE`.
    fn vcvtps2ph512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VCVTPS2PH512RRI_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VCVTPS2PH512RRI_MASKZ`.
    fn vcvtps2ph512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VCVTPS2PH512RRI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VCVTPS2PH512RRI_MASKZ_SAE`.
    fn vcvtps2ph512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VCVTPS2PH512RRI_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VCVTPS2PH512RRI_SAE`.
    fn vcvtps2ph512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VCVTPS2PH512RRI_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VCVTPS2UDQ128`.
    fn vcvtps2udq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2UDQ128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2UDQ128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2UDQ128");
        }
    }
    /// Emits `VCVTPS2UDQ128_MASK`.
    fn vcvtps2udq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2UDQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2UDQ128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2UDQ128_MASK");
        }
    }
    /// Emits `VCVTPS2UDQ128_MASKZ`.
    fn vcvtps2udq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2UDQ128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2UDQ128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2UDQ128_MASKZ");
        }
    }
    /// Emits `VCVTPS2UDQ128RB`.
    fn vcvtps2udq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UDQ128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UDQ128RB_MASK`.
    fn vcvtps2udq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UDQ128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UDQ128RB_MASKZ`.
    fn vcvtps2udq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UDQ128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UDQ256`.
    fn vcvtps2udq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2UDQ256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2UDQ256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2UDQ256");
        }
    }
    /// Emits `VCVTPS2UDQ256_MASK`.
    fn vcvtps2udq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2UDQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2UDQ256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2UDQ256_MASK");
        }
    }
    /// Emits `VCVTPS2UDQ256_MASKZ`.
    fn vcvtps2udq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2UDQ256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2UDQ256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2UDQ256_MASKZ");
        }
    }
    /// Emits `VCVTPS2UDQ256RB`.
    fn vcvtps2udq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UDQ256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UDQ256RB_MASK`.
    fn vcvtps2udq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UDQ256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UDQ256RB_MASKZ`.
    fn vcvtps2udq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UDQ256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UDQ512`.
    fn vcvtps2udq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2UDQ512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2UDQ512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2UDQ512");
        }
    }
    /// Emits `VCVTPS2UDQ512RR_ER`.
    fn vcvtps2udq512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UDQ512RR_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UDQ512_MASK`.
    fn vcvtps2udq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2UDQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2UDQ512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2UDQ512_MASK");
        }
    }
    /// Emits `VCVTPS2UDQ512RR_MASK_ER`.
    fn vcvtps2udq512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UDQ512RR_MASK_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UDQ512_MASKZ`.
    fn vcvtps2udq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2UDQ512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2UDQ512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2UDQ512_MASKZ");
        }
    }
    /// Emits `VCVTPS2UDQ512RR_MASKZ_ER`.
    fn vcvtps2udq512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UDQ512RR_MASKZ_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UDQ512RB`.
    fn vcvtps2udq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UDQ512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UDQ512RB_MASK`.
    fn vcvtps2udq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UDQ512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UDQ512RB_MASKZ`.
    fn vcvtps2udq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UDQ512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UQQ128`.
    fn vcvtps2uqq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2UQQ128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2UQQ128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2UQQ128");
        }
    }
    /// Emits `VCVTPS2UQQ128_MASK`.
    fn vcvtps2uqq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2UQQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2UQQ128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2UQQ128_MASK");
        }
    }
    /// Emits `VCVTPS2UQQ128_MASKZ`.
    fn vcvtps2uqq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2UQQ128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2UQQ128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2UQQ128_MASKZ");
        }
    }
    /// Emits `VCVTPS2UQQ128RB`.
    fn vcvtps2uqq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UQQ128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UQQ128RB_MASK`.
    fn vcvtps2uqq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UQQ128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UQQ128RB_MASKZ`.
    fn vcvtps2uqq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UQQ128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UQQ256`.
    fn vcvtps2uqq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2UQQ256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2UQQ256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2UQQ256");
        }
    }
    /// Emits `VCVTPS2UQQ256_MASK`.
    fn vcvtps2uqq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2UQQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2UQQ256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2UQQ256_MASK");
        }
    }
    /// Emits `VCVTPS2UQQ256_MASKZ`.
    fn vcvtps2uqq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2UQQ256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2UQQ256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2UQQ256_MASKZ");
        }
    }
    /// Emits `VCVTPS2UQQ256RB`.
    fn vcvtps2uqq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UQQ256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UQQ256RB_MASK`.
    fn vcvtps2uqq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UQQ256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UQQ256RB_MASKZ`.
    fn vcvtps2uqq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UQQ256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UQQ512`.
    fn vcvtps2uqq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2UQQ512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2UQQ512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2UQQ512");
        }
    }
    /// Emits `VCVTPS2UQQ512RR_ER`.
    fn vcvtps2uqq512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UQQ512RR_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UQQ512_MASK`.
    fn vcvtps2uqq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2UQQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2UQQ512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2UQQ512_MASK");
        }
    }
    /// Emits `VCVTPS2UQQ512RR_MASK_ER`.
    fn vcvtps2uqq512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UQQ512RR_MASK_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UQQ512_MASKZ`.
    fn vcvtps2uqq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTPS2UQQ512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTPS2UQQ512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTPS2UQQ512_MASKZ");
        }
    }
    /// Emits `VCVTPS2UQQ512RR_MASKZ_ER`.
    fn vcvtps2uqq512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UQQ512RR_MASKZ_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UQQ512RB`.
    fn vcvtps2uqq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UQQ512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UQQ512RB_MASK`.
    fn vcvtps2uqq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UQQ512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTPS2UQQ512RB_MASKZ`.
    fn vcvtps2uqq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTPS2UQQ512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTSD2SI32`.
    fn vcvtsd2si32(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_gp() && op1.is_vec() {
            self.emit(VCVTSD2SI32RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_gp() && op1.is_mem() {
            self.emit(VCVTSD2SI32RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTSD2SI32");
        }
    }
    /// Emits `VCVTSD2SI32RR_ER`.
    fn vcvtsd2si32_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTSD2SI32RR_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTSD2SI64`.
    fn vcvtsd2si64(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_gp() && op1.is_vec() {
            self.emit(VCVTSD2SI64RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_gp() && op1.is_mem() {
            self.emit(VCVTSD2SI64RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTSD2SI64");
        }
    }
    /// Emits `VCVTSD2SI64RR_ER`.
    fn vcvtsd2si64_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTSD2SI64RR_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTSD2SS`.
    fn vcvtsd2ss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VCVTSD2SSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VCVTSD2SSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTSD2SS");
        }
    }
    /// Emits `VCVTSD2SSRRR_ER`.
    fn vcvtsd2ss_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VCVTSD2SSRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VCVTSD2SS_MASK`.
    fn vcvtsd2ss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VCVTSD2SSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VCVTSD2SSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTSD2SS_MASK");
        }
    }
    /// Emits `VCVTSD2SSRRR_MASK_ER`.
    fn vcvtsd2ss_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VCVTSD2SSRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VCVTSD2SS_MASKZ`.
    fn vcvtsd2ss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VCVTSD2SSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VCVTSD2SSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTSD2SS_MASKZ");
        }
    }
    /// Emits `VCVTSD2SSRRR_MASKZ_ER`.
    fn vcvtsd2ss_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VCVTSD2SSRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VCVTSD2USI32`.
    fn vcvtsd2usi32(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_gp() && op1.is_vec() {
            self.emit(VCVTSD2USI32RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_gp() && op1.is_mem() {
            self.emit(VCVTSD2USI32RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTSD2USI32");
        }
    }
    /// Emits `VCVTSD2USI32RR_ER`.
    fn vcvtsd2usi32_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTSD2USI32RR_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTSD2USI64`.
    fn vcvtsd2usi64(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_gp() && op1.is_vec() {
            self.emit(VCVTSD2USI64RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_gp() && op1.is_mem() {
            self.emit(VCVTSD2USI64RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTSD2USI64");
        }
    }
    /// Emits `VCVTSD2USI64RR_ER`.
    fn vcvtsd2usi64_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTSD2USI64RR_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTSI2SD32`.
    fn vcvtsi2sd32(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_gp() {
            self.emit(VCVTSI2SD32RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VCVTSI2SD32RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTSI2SD32");
        }
    }
    /// Emits `VCVTSI2SD32RRR_ER`.
    fn vcvtsi2sd32_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VCVTSI2SD32RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VCVTSI2SD64`.
    fn vcvtsi2sd64(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_gp() {
            self.emit(VCVTSI2SD64RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VCVTSI2SD64RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTSI2SD64");
        }
    }
    /// Emits `VCVTSI2SD64RRR_ER`.
    fn vcvtsi2sd64_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VCVTSI2SD64RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VCVTSI2SS32`.
    fn vcvtsi2ss32(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_gp() {
            self.emit(VCVTSI2SS32RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VCVTSI2SS32RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTSI2SS32");
        }
    }
    /// Emits `VCVTSI2SS32RRR_ER`.
    fn vcvtsi2ss32_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VCVTSI2SS32RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VCVTSI2SS64`.
    fn vcvtsi2ss64(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_gp() {
            self.emit(VCVTSI2SS64RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VCVTSI2SS64RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTSI2SS64");
        }
    }
    /// Emits `VCVTSI2SS64RRR_ER`.
    fn vcvtsi2ss64_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VCVTSI2SS64RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VCVTSS2SD`.
    fn vcvtss2sd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VCVTSS2SDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VCVTSS2SDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTSS2SD");
        }
    }
    /// Emits `VCVTSS2SD_MASK`.
    fn vcvtss2sd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VCVTSS2SDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VCVTSS2SDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTSS2SD_MASK");
        }
    }
    /// Emits `VCVTSS2SDRRR_MASK_SAE`.
    fn vcvtss2sd_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VCVTSS2SDRRR_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VCVTSS2SD_MASKZ`.
    fn vcvtss2sd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VCVTSS2SDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VCVTSS2SDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTSS2SD_MASKZ");
        }
    }
    /// Emits `VCVTSS2SDRRR_MASKZ_SAE`.
    fn vcvtss2sd_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VCVTSS2SDRRR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VCVTSS2SDRRR_SAE`.
    fn vcvtss2sd_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VCVTSS2SDRRR_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VCVTSS2SI32`.
    fn vcvtss2si32(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_gp() && op1.is_vec() {
            self.emit(VCVTSS2SI32RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_gp() && op1.is_mem() {
            self.emit(VCVTSS2SI32RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTSS2SI32");
        }
    }
    /// Emits `VCVTSS2SI32RR_ER`.
    fn vcvtss2si32_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTSS2SI32RR_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTSS2SI64`.
    fn vcvtss2si64(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_gp() && op1.is_vec() {
            self.emit(VCVTSS2SI64RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_gp() && op1.is_mem() {
            self.emit(VCVTSS2SI64RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTSS2SI64");
        }
    }
    /// Emits `VCVTSS2SI64RR_ER`.
    fn vcvtss2si64_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTSS2SI64RR_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTSS2USI32`.
    fn vcvtss2usi32(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_gp() && op1.is_vec() {
            self.emit(VCVTSS2USI32RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_gp() && op1.is_mem() {
            self.emit(VCVTSS2USI32RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTSS2USI32");
        }
    }
    /// Emits `VCVTSS2USI32RR_ER`.
    fn vcvtss2usi32_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTSS2USI32RR_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTSS2USI64`.
    fn vcvtss2usi64(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_gp() && op1.is_vec() {
            self.emit(VCVTSS2USI64RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_gp() && op1.is_mem() {
            self.emit(VCVTSS2USI64RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTSS2USI64");
        }
    }
    /// Emits `VCVTSS2USI64RR_ER`.
    fn vcvtss2usi64_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTSS2USI64RR_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2DQ128`.
    fn vcvttpd2dq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2DQ128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2DQ128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2DQ128");
        }
    }
    /// Emits `VCVTTPD2DQ128_MASK`.
    fn vcvttpd2dq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2DQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2DQ128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2DQ128_MASK");
        }
    }
    /// Emits `VCVTTPD2DQ128_MASKZ`.
    fn vcvttpd2dq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2DQ128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2DQ128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2DQ128_MASKZ");
        }
    }
    /// Emits `VCVTTPD2DQ128RB`.
    fn vcvttpd2dq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2DQ128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2DQ128RB_MASK`.
    fn vcvttpd2dq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2DQ128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2DQ128RB_MASKZ`.
    fn vcvttpd2dq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2DQ128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2DQ256`.
    fn vcvttpd2dq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2DQ256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2DQ256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2DQ256");
        }
    }
    /// Emits `VCVTTPD2DQ256_MASK`.
    fn vcvttpd2dq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2DQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2DQ256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2DQ256_MASK");
        }
    }
    /// Emits `VCVTTPD2DQ256_MASKZ`.
    fn vcvttpd2dq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2DQ256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2DQ256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2DQ256_MASKZ");
        }
    }
    /// Emits `VCVTTPD2DQ256RB`.
    fn vcvttpd2dq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2DQ256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2DQ256RB_MASK`.
    fn vcvttpd2dq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2DQ256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2DQ256RB_MASKZ`.
    fn vcvttpd2dq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2DQ256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2DQ512`.
    fn vcvttpd2dq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2DQ512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2DQ512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2DQ512");
        }
    }
    /// Emits `VCVTTPD2DQ512_MASK`.
    fn vcvttpd2dq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2DQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2DQ512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2DQ512_MASK");
        }
    }
    /// Emits `VCVTTPD2DQ512RR_MASK_SAE`.
    fn vcvttpd2dq512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2DQ512RR_MASK_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2DQ512_MASKZ`.
    fn vcvttpd2dq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2DQ512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2DQ512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2DQ512_MASKZ");
        }
    }
    /// Emits `VCVTTPD2DQ512RR_MASKZ_SAE`.
    fn vcvttpd2dq512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2DQ512RR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2DQ512RR_SAE`.
    fn vcvttpd2dq512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2DQ512RR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2DQ512RB`.
    fn vcvttpd2dq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2DQ512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2DQ512RB_MASK`.
    fn vcvttpd2dq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2DQ512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2DQ512RB_MASKZ`.
    fn vcvttpd2dq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2DQ512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UDQ128`.
    fn vcvttpd2udq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2UDQ128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2UDQ128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2UDQ128");
        }
    }
    /// Emits `VCVTTPD2UDQ128_MASK`.
    fn vcvttpd2udq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2UDQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2UDQ128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2UDQ128_MASK");
        }
    }
    /// Emits `VCVTTPD2UDQ128_MASKZ`.
    fn vcvttpd2udq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2UDQ128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2UDQ128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2UDQ128_MASKZ");
        }
    }
    /// Emits `VCVTTPD2UDQ128RB`.
    fn vcvttpd2udq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UDQ128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UDQ128RB_MASK`.
    fn vcvttpd2udq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UDQ128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UDQ128RB_MASKZ`.
    fn vcvttpd2udq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UDQ128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UDQ256`.
    fn vcvttpd2udq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2UDQ256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2UDQ256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2UDQ256");
        }
    }
    /// Emits `VCVTTPD2UDQ256_MASK`.
    fn vcvttpd2udq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2UDQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2UDQ256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2UDQ256_MASK");
        }
    }
    /// Emits `VCVTTPD2UDQ256_MASKZ`.
    fn vcvttpd2udq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2UDQ256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2UDQ256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2UDQ256_MASKZ");
        }
    }
    /// Emits `VCVTTPD2UDQ256RB`.
    fn vcvttpd2udq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UDQ256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UDQ256RB_MASK`.
    fn vcvttpd2udq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UDQ256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UDQ256RB_MASKZ`.
    fn vcvttpd2udq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UDQ256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UDQ512`.
    fn vcvttpd2udq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2UDQ512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2UDQ512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2UDQ512");
        }
    }
    /// Emits `VCVTTPD2UDQ512_MASK`.
    fn vcvttpd2udq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2UDQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2UDQ512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2UDQ512_MASK");
        }
    }
    /// Emits `VCVTTPD2UDQ512RR_MASK_SAE`.
    fn vcvttpd2udq512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UDQ512RR_MASK_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UDQ512_MASKZ`.
    fn vcvttpd2udq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2UDQ512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2UDQ512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2UDQ512_MASKZ");
        }
    }
    /// Emits `VCVTTPD2UDQ512RR_MASKZ_SAE`.
    fn vcvttpd2udq512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UDQ512RR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UDQ512RR_SAE`.
    fn vcvttpd2udq512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UDQ512RR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UDQ512RB`.
    fn vcvttpd2udq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UDQ512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UDQ512RB_MASK`.
    fn vcvttpd2udq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UDQ512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UDQ512RB_MASKZ`.
    fn vcvttpd2udq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UDQ512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UQQ128`.
    fn vcvttpd2uqq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2UQQ128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2UQQ128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2UQQ128");
        }
    }
    /// Emits `VCVTTPD2UQQ128_MASK`.
    fn vcvttpd2uqq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2UQQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2UQQ128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2UQQ128_MASK");
        }
    }
    /// Emits `VCVTTPD2UQQ128_MASKZ`.
    fn vcvttpd2uqq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2UQQ128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2UQQ128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2UQQ128_MASKZ");
        }
    }
    /// Emits `VCVTTPD2UQQ128RB`.
    fn vcvttpd2uqq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UQQ128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UQQ128RB_MASK`.
    fn vcvttpd2uqq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UQQ128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UQQ128RB_MASKZ`.
    fn vcvttpd2uqq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UQQ128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UQQ256`.
    fn vcvttpd2uqq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2UQQ256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2UQQ256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2UQQ256");
        }
    }
    /// Emits `VCVTTPD2UQQ256_MASK`.
    fn vcvttpd2uqq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2UQQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2UQQ256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2UQQ256_MASK");
        }
    }
    /// Emits `VCVTTPD2UQQ256_MASKZ`.
    fn vcvttpd2uqq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2UQQ256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2UQQ256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2UQQ256_MASKZ");
        }
    }
    /// Emits `VCVTTPD2UQQ256RB`.
    fn vcvttpd2uqq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UQQ256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UQQ256RB_MASK`.
    fn vcvttpd2uqq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UQQ256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UQQ256RB_MASKZ`.
    fn vcvttpd2uqq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UQQ256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UQQ512`.
    fn vcvttpd2uqq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2UQQ512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2UQQ512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2UQQ512");
        }
    }
    /// Emits `VCVTTPD2UQQ512_MASK`.
    fn vcvttpd2uqq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2UQQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2UQQ512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2UQQ512_MASK");
        }
    }
    /// Emits `VCVTTPD2UQQ512RR_MASK_SAE`.
    fn vcvttpd2uqq512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UQQ512RR_MASK_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UQQ512_MASKZ`.
    fn vcvttpd2uqq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPD2UQQ512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPD2UQQ512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPD2UQQ512_MASKZ");
        }
    }
    /// Emits `VCVTTPD2UQQ512RR_MASKZ_SAE`.
    fn vcvttpd2uqq512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UQQ512RR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UQQ512RR_SAE`.
    fn vcvttpd2uqq512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UQQ512RR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UQQ512RB`.
    fn vcvttpd2uqq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UQQ512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UQQ512RB_MASK`.
    fn vcvttpd2uqq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UQQ512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPD2UQQ512RB_MASKZ`.
    fn vcvttpd2uqq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPD2UQQ512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2DQ128`.
    fn vcvttps2dq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2DQ128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2DQ128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2DQ128");
        }
    }
    /// Emits `VCVTTPS2DQ128_MASK`.
    fn vcvttps2dq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2DQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2DQ128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2DQ128_MASK");
        }
    }
    /// Emits `VCVTTPS2DQ128_MASKZ`.
    fn vcvttps2dq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2DQ128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2DQ128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2DQ128_MASKZ");
        }
    }
    /// Emits `VCVTTPS2DQ128RB`.
    fn vcvttps2dq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2DQ128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2DQ128RB_MASK`.
    fn vcvttps2dq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2DQ128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2DQ128RB_MASKZ`.
    fn vcvttps2dq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2DQ128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2DQ256`.
    fn vcvttps2dq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2DQ256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2DQ256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2DQ256");
        }
    }
    /// Emits `VCVTTPS2DQ256_MASK`.
    fn vcvttps2dq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2DQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2DQ256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2DQ256_MASK");
        }
    }
    /// Emits `VCVTTPS2DQ256_MASKZ`.
    fn vcvttps2dq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2DQ256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2DQ256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2DQ256_MASKZ");
        }
    }
    /// Emits `VCVTTPS2DQ256RB`.
    fn vcvttps2dq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2DQ256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2DQ256RB_MASK`.
    fn vcvttps2dq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2DQ256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2DQ256RB_MASKZ`.
    fn vcvttps2dq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2DQ256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2DQ512`.
    fn vcvttps2dq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2DQ512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2DQ512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2DQ512");
        }
    }
    /// Emits `VCVTTPS2DQ512_MASK`.
    fn vcvttps2dq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2DQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2DQ512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2DQ512_MASK");
        }
    }
    /// Emits `VCVTTPS2DQ512RR_MASK_SAE`.
    fn vcvttps2dq512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2DQ512RR_MASK_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2DQ512_MASKZ`.
    fn vcvttps2dq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2DQ512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2DQ512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2DQ512_MASKZ");
        }
    }
    /// Emits `VCVTTPS2DQ512RR_MASKZ_SAE`.
    fn vcvttps2dq512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2DQ512RR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2DQ512RR_SAE`.
    fn vcvttps2dq512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2DQ512RR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2DQ512RB`.
    fn vcvttps2dq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2DQ512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2DQ512RB_MASK`.
    fn vcvttps2dq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2DQ512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2DQ512RB_MASKZ`.
    fn vcvttps2dq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2DQ512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UDQ128`.
    fn vcvttps2udq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2UDQ128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2UDQ128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2UDQ128");
        }
    }
    /// Emits `VCVTTPS2UDQ128_MASK`.
    fn vcvttps2udq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2UDQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2UDQ128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2UDQ128_MASK");
        }
    }
    /// Emits `VCVTTPS2UDQ128_MASKZ`.
    fn vcvttps2udq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2UDQ128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2UDQ128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2UDQ128_MASKZ");
        }
    }
    /// Emits `VCVTTPS2UDQ128RB`.
    fn vcvttps2udq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UDQ128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UDQ128RB_MASK`.
    fn vcvttps2udq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UDQ128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UDQ128RB_MASKZ`.
    fn vcvttps2udq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UDQ128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UDQ256`.
    fn vcvttps2udq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2UDQ256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2UDQ256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2UDQ256");
        }
    }
    /// Emits `VCVTTPS2UDQ256_MASK`.
    fn vcvttps2udq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2UDQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2UDQ256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2UDQ256_MASK");
        }
    }
    /// Emits `VCVTTPS2UDQ256_MASKZ`.
    fn vcvttps2udq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2UDQ256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2UDQ256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2UDQ256_MASKZ");
        }
    }
    /// Emits `VCVTTPS2UDQ256RB`.
    fn vcvttps2udq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UDQ256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UDQ256RB_MASK`.
    fn vcvttps2udq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UDQ256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UDQ256RB_MASKZ`.
    fn vcvttps2udq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UDQ256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UDQ512`.
    fn vcvttps2udq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2UDQ512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2UDQ512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2UDQ512");
        }
    }
    /// Emits `VCVTTPS2UDQ512_MASK`.
    fn vcvttps2udq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2UDQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2UDQ512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2UDQ512_MASK");
        }
    }
    /// Emits `VCVTTPS2UDQ512RR_MASK_SAE`.
    fn vcvttps2udq512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UDQ512RR_MASK_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UDQ512_MASKZ`.
    fn vcvttps2udq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2UDQ512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2UDQ512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2UDQ512_MASKZ");
        }
    }
    /// Emits `VCVTTPS2UDQ512RR_MASKZ_SAE`.
    fn vcvttps2udq512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UDQ512RR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UDQ512RR_SAE`.
    fn vcvttps2udq512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UDQ512RR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UDQ512RB`.
    fn vcvttps2udq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UDQ512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UDQ512RB_MASK`.
    fn vcvttps2udq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UDQ512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UDQ512RB_MASKZ`.
    fn vcvttps2udq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UDQ512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UQQ128`.
    fn vcvttps2uqq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2UQQ128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2UQQ128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2UQQ128");
        }
    }
    /// Emits `VCVTTPS2UQQ128_MASK`.
    fn vcvttps2uqq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2UQQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2UQQ128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2UQQ128_MASK");
        }
    }
    /// Emits `VCVTTPS2UQQ128_MASKZ`.
    fn vcvttps2uqq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2UQQ128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2UQQ128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2UQQ128_MASKZ");
        }
    }
    /// Emits `VCVTTPS2UQQ128RB`.
    fn vcvttps2uqq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UQQ128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UQQ128RB_MASK`.
    fn vcvttps2uqq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UQQ128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UQQ128RB_MASKZ`.
    fn vcvttps2uqq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UQQ128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UQQ256`.
    fn vcvttps2uqq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2UQQ256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2UQQ256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2UQQ256");
        }
    }
    /// Emits `VCVTTPS2UQQ256_MASK`.
    fn vcvttps2uqq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2UQQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2UQQ256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2UQQ256_MASK");
        }
    }
    /// Emits `VCVTTPS2UQQ256_MASKZ`.
    fn vcvttps2uqq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2UQQ256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2UQQ256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2UQQ256_MASKZ");
        }
    }
    /// Emits `VCVTTPS2UQQ256RB`.
    fn vcvttps2uqq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UQQ256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UQQ256RB_MASK`.
    fn vcvttps2uqq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UQQ256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UQQ256RB_MASKZ`.
    fn vcvttps2uqq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UQQ256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UQQ512`.
    fn vcvttps2uqq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2UQQ512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2UQQ512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2UQQ512");
        }
    }
    /// Emits `VCVTTPS2UQQ512_MASK`.
    fn vcvttps2uqq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2UQQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2UQQ512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2UQQ512_MASK");
        }
    }
    /// Emits `VCVTTPS2UQQ512RR_MASK_SAE`.
    fn vcvttps2uqq512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UQQ512RR_MASK_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UQQ512_MASKZ`.
    fn vcvttps2uqq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTTPS2UQQ512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTTPS2UQQ512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTPS2UQQ512_MASKZ");
        }
    }
    /// Emits `VCVTTPS2UQQ512RR_MASKZ_SAE`.
    fn vcvttps2uqq512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UQQ512RR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UQQ512RR_SAE`.
    fn vcvttps2uqq512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UQQ512RR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UQQ512RB`.
    fn vcvttps2uqq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UQQ512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UQQ512RB_MASK`.
    fn vcvttps2uqq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UQQ512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTPS2UQQ512RB_MASKZ`.
    fn vcvttps2uqq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTPS2UQQ512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTSD2SI32`.
    fn vcvttsd2si32(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_gp() && op1.is_vec() {
            self.emit(VCVTTSD2SI32RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_gp() && op1.is_mem() {
            self.emit(VCVTTSD2SI32RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTSD2SI32");
        }
    }
    /// Emits `VCVTTSD2SI32RR_SAE`.
    fn vcvttsd2si32_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTSD2SI32RR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTSD2SI64`.
    fn vcvttsd2si64(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_gp() && op1.is_vec() {
            self.emit(VCVTTSD2SI64RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_gp() && op1.is_mem() {
            self.emit(VCVTTSD2SI64RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTSD2SI64");
        }
    }
    /// Emits `VCVTTSD2SI64RR_SAE`.
    fn vcvttsd2si64_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTSD2SI64RR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTSD2USI32`.
    fn vcvttsd2usi32(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_gp() && op1.is_vec() {
            self.emit(VCVTTSD2USI32RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_gp() && op1.is_mem() {
            self.emit(VCVTTSD2USI32RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTSD2USI32");
        }
    }
    /// Emits `VCVTTSD2USI32RR_SAE`.
    fn vcvttsd2usi32_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTSD2USI32RR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTSD2USI64`.
    fn vcvttsd2usi64(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_gp() && op1.is_vec() {
            self.emit(VCVTTSD2USI64RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_gp() && op1.is_mem() {
            self.emit(VCVTTSD2USI64RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTSD2USI64");
        }
    }
    /// Emits `VCVTTSD2USI64RR_SAE`.
    fn vcvttsd2usi64_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTSD2USI64RR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTSS2SI32`.
    fn vcvttss2si32(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_gp() && op1.is_vec() {
            self.emit(VCVTTSS2SI32RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_gp() && op1.is_mem() {
            self.emit(VCVTTSS2SI32RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTSS2SI32");
        }
    }
    /// Emits `VCVTTSS2SI32RR_SAE`.
    fn vcvttss2si32_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTSS2SI32RR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTSS2SI64`.
    fn vcvttss2si64(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_gp() && op1.is_vec() {
            self.emit(VCVTTSS2SI64RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_gp() && op1.is_mem() {
            self.emit(VCVTTSS2SI64RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTSS2SI64");
        }
    }
    /// Emits `VCVTTSS2SI64RR_SAE`.
    fn vcvttss2si64_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTSS2SI64RR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTSS2USI32`.
    fn vcvttss2usi32(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_gp() && op1.is_vec() {
            self.emit(VCVTTSS2USI32RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_gp() && op1.is_mem() {
            self.emit(VCVTTSS2USI32RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTSS2USI32");
        }
    }
    /// Emits `VCVTTSS2USI32RR_SAE`.
    fn vcvttss2usi32_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTSS2USI32RR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTTSS2USI64`.
    fn vcvttss2usi64(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_gp() && op1.is_vec() {
            self.emit(VCVTTSS2USI64RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_gp() && op1.is_mem() {
            self.emit(VCVTTSS2USI64RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTTSS2USI64");
        }
    }
    /// Emits `VCVTTSS2USI64RR_SAE`.
    fn vcvttss2usi64_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTTSS2USI64RR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PD128`.
    fn vcvtudq2pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUDQ2PD128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUDQ2PD128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUDQ2PD128");
        }
    }
    /// Emits `VCVTUDQ2PD128_MASK`.
    fn vcvtudq2pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUDQ2PD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUDQ2PD128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUDQ2PD128_MASK");
        }
    }
    /// Emits `VCVTUDQ2PD128_MASKZ`.
    fn vcvtudq2pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUDQ2PD128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUDQ2PD128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUDQ2PD128_MASKZ");
        }
    }
    /// Emits `VCVTUDQ2PD128RB`.
    fn vcvtudq2pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PD128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PD128RB_MASK`.
    fn vcvtudq2pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PD128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PD128RB_MASKZ`.
    fn vcvtudq2pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PD128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PD256`.
    fn vcvtudq2pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUDQ2PD256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUDQ2PD256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUDQ2PD256");
        }
    }
    /// Emits `VCVTUDQ2PD256_MASK`.
    fn vcvtudq2pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUDQ2PD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUDQ2PD256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUDQ2PD256_MASK");
        }
    }
    /// Emits `VCVTUDQ2PD256_MASKZ`.
    fn vcvtudq2pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUDQ2PD256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUDQ2PD256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUDQ2PD256_MASKZ");
        }
    }
    /// Emits `VCVTUDQ2PD256RB`.
    fn vcvtudq2pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PD256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PD256RB_MASK`.
    fn vcvtudq2pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PD256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PD256RB_MASKZ`.
    fn vcvtudq2pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PD256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PD512`.
    fn vcvtudq2pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUDQ2PD512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUDQ2PD512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUDQ2PD512");
        }
    }
    /// Emits `VCVTUDQ2PD512_MASK`.
    fn vcvtudq2pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUDQ2PD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUDQ2PD512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUDQ2PD512_MASK");
        }
    }
    /// Emits `VCVTUDQ2PD512RR_MASK_SAE`.
    fn vcvtudq2pd512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PD512RR_MASK_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PD512_MASKZ`.
    fn vcvtudq2pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUDQ2PD512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUDQ2PD512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUDQ2PD512_MASKZ");
        }
    }
    /// Emits `VCVTUDQ2PD512RR_MASKZ_SAE`.
    fn vcvtudq2pd512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PD512RR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PD512RR_SAE`.
    fn vcvtudq2pd512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PD512RR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PD512RB`.
    fn vcvtudq2pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PD512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PD512RB_MASK`.
    fn vcvtudq2pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PD512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PD512RB_MASKZ`.
    fn vcvtudq2pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PD512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PS128`.
    fn vcvtudq2ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUDQ2PS128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUDQ2PS128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUDQ2PS128");
        }
    }
    /// Emits `VCVTUDQ2PS128_MASK`.
    fn vcvtudq2ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUDQ2PS128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUDQ2PS128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUDQ2PS128_MASK");
        }
    }
    /// Emits `VCVTUDQ2PS128_MASKZ`.
    fn vcvtudq2ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUDQ2PS128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUDQ2PS128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUDQ2PS128_MASKZ");
        }
    }
    /// Emits `VCVTUDQ2PS128RB`.
    fn vcvtudq2ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PS128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PS128RB_MASK`.
    fn vcvtudq2ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PS128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PS128RB_MASKZ`.
    fn vcvtudq2ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PS128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PS256`.
    fn vcvtudq2ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUDQ2PS256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUDQ2PS256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUDQ2PS256");
        }
    }
    /// Emits `VCVTUDQ2PS256_MASK`.
    fn vcvtudq2ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUDQ2PS256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUDQ2PS256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUDQ2PS256_MASK");
        }
    }
    /// Emits `VCVTUDQ2PS256_MASKZ`.
    fn vcvtudq2ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUDQ2PS256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUDQ2PS256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUDQ2PS256_MASKZ");
        }
    }
    /// Emits `VCVTUDQ2PS256RB`.
    fn vcvtudq2ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PS256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PS256RB_MASK`.
    fn vcvtudq2ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PS256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PS256RB_MASKZ`.
    fn vcvtudq2ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PS256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PS512`.
    fn vcvtudq2ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUDQ2PS512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUDQ2PS512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUDQ2PS512");
        }
    }
    /// Emits `VCVTUDQ2PS512RR_ER`.
    fn vcvtudq2ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PS512RR_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PS512_MASK`.
    fn vcvtudq2ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUDQ2PS512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUDQ2PS512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUDQ2PS512_MASK");
        }
    }
    /// Emits `VCVTUDQ2PS512RR_MASK_ER`.
    fn vcvtudq2ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PS512RR_MASK_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PS512_MASKZ`.
    fn vcvtudq2ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUDQ2PS512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUDQ2PS512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUDQ2PS512_MASKZ");
        }
    }
    /// Emits `VCVTUDQ2PS512RR_MASKZ_ER`.
    fn vcvtudq2ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PS512RR_MASKZ_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PS512RB`.
    fn vcvtudq2ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PS512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PS512RB_MASK`.
    fn vcvtudq2ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PS512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUDQ2PS512RB_MASKZ`.
    fn vcvtudq2ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUDQ2PS512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PD128`.
    fn vcvtuqq2pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUQQ2PD128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUQQ2PD128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUQQ2PD128");
        }
    }
    /// Emits `VCVTUQQ2PD128_MASK`.
    fn vcvtuqq2pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUQQ2PD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUQQ2PD128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUQQ2PD128_MASK");
        }
    }
    /// Emits `VCVTUQQ2PD128_MASKZ`.
    fn vcvtuqq2pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUQQ2PD128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUQQ2PD128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUQQ2PD128_MASKZ");
        }
    }
    /// Emits `VCVTUQQ2PD128RB`.
    fn vcvtuqq2pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PD128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PD128RB_MASK`.
    fn vcvtuqq2pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PD128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PD128RB_MASKZ`.
    fn vcvtuqq2pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PD128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PD256`.
    fn vcvtuqq2pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUQQ2PD256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUQQ2PD256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUQQ2PD256");
        }
    }
    /// Emits `VCVTUQQ2PD256_MASK`.
    fn vcvtuqq2pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUQQ2PD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUQQ2PD256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUQQ2PD256_MASK");
        }
    }
    /// Emits `VCVTUQQ2PD256_MASKZ`.
    fn vcvtuqq2pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUQQ2PD256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUQQ2PD256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUQQ2PD256_MASKZ");
        }
    }
    /// Emits `VCVTUQQ2PD256RB`.
    fn vcvtuqq2pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PD256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PD256RB_MASK`.
    fn vcvtuqq2pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PD256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PD256RB_MASKZ`.
    fn vcvtuqq2pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PD256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PD512`.
    fn vcvtuqq2pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUQQ2PD512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUQQ2PD512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUQQ2PD512");
        }
    }
    /// Emits `VCVTUQQ2PD512RR_ER`.
    fn vcvtuqq2pd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PD512RR_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PD512_MASK`.
    fn vcvtuqq2pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUQQ2PD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUQQ2PD512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUQQ2PD512_MASK");
        }
    }
    /// Emits `VCVTUQQ2PD512RR_MASK_ER`.
    fn vcvtuqq2pd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PD512RR_MASK_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PD512_MASKZ`.
    fn vcvtuqq2pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUQQ2PD512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUQQ2PD512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUQQ2PD512_MASKZ");
        }
    }
    /// Emits `VCVTUQQ2PD512RR_MASKZ_ER`.
    fn vcvtuqq2pd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PD512RR_MASKZ_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PD512RB`.
    fn vcvtuqq2pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PD512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PD512RB_MASK`.
    fn vcvtuqq2pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PD512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PD512RB_MASKZ`.
    fn vcvtuqq2pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PD512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PS128`.
    fn vcvtuqq2ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUQQ2PS128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUQQ2PS128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUQQ2PS128");
        }
    }
    /// Emits `VCVTUQQ2PS128_MASK`.
    fn vcvtuqq2ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUQQ2PS128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUQQ2PS128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUQQ2PS128_MASK");
        }
    }
    /// Emits `VCVTUQQ2PS128_MASKZ`.
    fn vcvtuqq2ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUQQ2PS128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUQQ2PS128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUQQ2PS128_MASKZ");
        }
    }
    /// Emits `VCVTUQQ2PS128RB`.
    fn vcvtuqq2ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PS128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PS128RB_MASK`.
    fn vcvtuqq2ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PS128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PS128RB_MASKZ`.
    fn vcvtuqq2ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PS128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PS256`.
    fn vcvtuqq2ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUQQ2PS256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUQQ2PS256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUQQ2PS256");
        }
    }
    /// Emits `VCVTUQQ2PS256_MASK`.
    fn vcvtuqq2ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUQQ2PS256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUQQ2PS256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUQQ2PS256_MASK");
        }
    }
    /// Emits `VCVTUQQ2PS256_MASKZ`.
    fn vcvtuqq2ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUQQ2PS256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUQQ2PS256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUQQ2PS256_MASKZ");
        }
    }
    /// Emits `VCVTUQQ2PS256RB`.
    fn vcvtuqq2ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PS256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PS256RB_MASK`.
    fn vcvtuqq2ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PS256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PS256RB_MASKZ`.
    fn vcvtuqq2ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PS256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PS512`.
    fn vcvtuqq2ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUQQ2PS512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUQQ2PS512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUQQ2PS512");
        }
    }
    /// Emits `VCVTUQQ2PS512RR_ER`.
    fn vcvtuqq2ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PS512RR_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PS512_MASK`.
    fn vcvtuqq2ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUQQ2PS512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUQQ2PS512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUQQ2PS512_MASK");
        }
    }
    /// Emits `VCVTUQQ2PS512RR_MASK_ER`.
    fn vcvtuqq2ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PS512RR_MASK_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PS512_MASKZ`.
    fn vcvtuqq2ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VCVTUQQ2PS512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VCVTUQQ2PS512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUQQ2PS512_MASKZ");
        }
    }
    /// Emits `VCVTUQQ2PS512RR_MASKZ_ER`.
    fn vcvtuqq2ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PS512RR_MASKZ_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PS512RB`.
    fn vcvtuqq2ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PS512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PS512RB_MASK`.
    fn vcvtuqq2ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PS512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUQQ2PS512RB_MASKZ`.
    fn vcvtuqq2ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VCVTUQQ2PS512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VCVTUSI2SD32`.
    fn vcvtusi2sd32(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_gp() {
            self.emit(VCVTUSI2SD32RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VCVTUSI2SD32RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUSI2SD32");
        }
    }
    /// Emits `VCVTUSI2SD32RRR_ER`.
    fn vcvtusi2sd32_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VCVTUSI2SD32RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VCVTUSI2SD64`.
    fn vcvtusi2sd64(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_gp() {
            self.emit(VCVTUSI2SD64RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VCVTUSI2SD64RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUSI2SD64");
        }
    }
    /// Emits `VCVTUSI2SD64RRR_ER`.
    fn vcvtusi2sd64_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VCVTUSI2SD64RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VCVTUSI2SS32`.
    fn vcvtusi2ss32(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_gp() {
            self.emit(VCVTUSI2SS32RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VCVTUSI2SS32RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUSI2SS32");
        }
    }
    /// Emits `VCVTUSI2SS32RRR_ER`.
    fn vcvtusi2ss32_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VCVTUSI2SS32RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VCVTUSI2SS64`.
    fn vcvtusi2ss64(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_gp() {
            self.emit(VCVTUSI2SS64RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VCVTUSI2SS64RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VCVTUSI2SS64");
        }
    }
    /// Emits `VCVTUSI2SS64RRR_ER`.
    fn vcvtusi2ss64_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VCVTUSI2SS64RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPD128`.
    fn vdivpd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVPD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVPD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVPD128");
        }
    }
    /// Emits `VDIVPD128_MASK`.
    fn vdivpd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVPD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVPD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVPD128_MASK");
        }
    }
    /// Emits `VDIVPD128_MASKZ`.
    fn vdivpd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVPD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVPD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVPD128_MASKZ");
        }
    }
    /// Emits `VDIVPD128RRB`.
    fn vdivpd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPD128RRB_MASK`.
    fn vdivpd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPD128RRB_MASKZ`.
    fn vdivpd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPD256`.
    fn vdivpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVPD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVPD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVPD256");
        }
    }
    /// Emits `VDIVPD256_MASK`.
    fn vdivpd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVPD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVPD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVPD256_MASK");
        }
    }
    /// Emits `VDIVPD256_MASKZ`.
    fn vdivpd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVPD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVPD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVPD256_MASKZ");
        }
    }
    /// Emits `VDIVPD256RRB`.
    fn vdivpd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPD256RRB_MASK`.
    fn vdivpd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPD256RRB_MASKZ`.
    fn vdivpd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPD512`.
    fn vdivpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVPD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVPD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVPD512");
        }
    }
    /// Emits `VDIVPD512RRR_ER`.
    fn vdivpd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPD512_MASK`.
    fn vdivpd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVPD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVPD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVPD512_MASK");
        }
    }
    /// Emits `VDIVPD512RRR_MASK_ER`.
    fn vdivpd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPD512_MASKZ`.
    fn vdivpd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVPD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVPD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVPD512_MASKZ");
        }
    }
    /// Emits `VDIVPD512RRR_MASKZ_ER`.
    fn vdivpd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPD512RRB`.
    fn vdivpd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPD512RRB_MASK`.
    fn vdivpd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPD512RRB_MASKZ`.
    fn vdivpd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPS128`.
    fn vdivps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVPS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVPS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVPS128");
        }
    }
    /// Emits `VDIVPS128_MASK`.
    fn vdivps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVPS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVPS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVPS128_MASK");
        }
    }
    /// Emits `VDIVPS128_MASKZ`.
    fn vdivps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVPS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVPS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVPS128_MASKZ");
        }
    }
    /// Emits `VDIVPS128RRB`.
    fn vdivps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPS128RRB_MASK`.
    fn vdivps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPS128RRB_MASKZ`.
    fn vdivps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPS256`.
    fn vdivps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVPS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVPS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVPS256");
        }
    }
    /// Emits `VDIVPS256_MASK`.
    fn vdivps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVPS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVPS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVPS256_MASK");
        }
    }
    /// Emits `VDIVPS256_MASKZ`.
    fn vdivps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVPS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVPS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVPS256_MASKZ");
        }
    }
    /// Emits `VDIVPS256RRB`.
    fn vdivps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPS256RRB_MASK`.
    fn vdivps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPS256RRB_MASKZ`.
    fn vdivps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPS512`.
    fn vdivps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVPS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVPS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVPS512");
        }
    }
    /// Emits `VDIVPS512RRR_ER`.
    fn vdivps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPS512_MASK`.
    fn vdivps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVPS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVPS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVPS512_MASK");
        }
    }
    /// Emits `VDIVPS512RRR_MASK_ER`.
    fn vdivps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPS512_MASKZ`.
    fn vdivps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVPS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVPS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVPS512_MASKZ");
        }
    }
    /// Emits `VDIVPS512RRR_MASKZ_ER`.
    fn vdivps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPS512RRB`.
    fn vdivps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPS512RRB_MASK`.
    fn vdivps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVPS512RRB_MASKZ`.
    fn vdivps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVPS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVSD`.
    fn vdivsd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVSDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVSDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVSD");
        }
    }
    /// Emits `VDIVSDRRR_ER`.
    fn vdivsd_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVSDRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVSD_MASK`.
    fn vdivsd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVSDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVSDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVSD_MASK");
        }
    }
    /// Emits `VDIVSDRRR_MASK_ER`.
    fn vdivsd_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVSDRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVSD_MASKZ`.
    fn vdivsd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVSDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVSDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVSD_MASKZ");
        }
    }
    /// Emits `VDIVSDRRR_MASKZ_ER`.
    fn vdivsd_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVSDRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVSS`.
    fn vdivss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVSSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVSSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVSS");
        }
    }
    /// Emits `VDIVSSRRR_ER`.
    fn vdivss_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVSSRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVSS_MASK`.
    fn vdivss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVSSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVSSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVSS_MASK");
        }
    }
    /// Emits `VDIVSSRRR_MASK_ER`.
    fn vdivss_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVSSRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VDIVSS_MASKZ`.
    fn vdivss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VDIVSSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VDIVSSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VDIVSS_MASKZ");
        }
    }
    /// Emits `VDIVSSRRR_MASKZ_ER`.
    fn vdivss_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VDIVSSRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VEXPANDPD128`.
    fn vexpandpd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VEXPANDPD128RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VEXPANDPD128RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXPANDPD128");
        }
    }
    /// Emits `VEXPANDPD128_MASK`.
    fn vexpandpd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VEXPANDPD128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VEXPANDPD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXPANDPD128_MASK");
        }
    }
    /// Emits `VEXPANDPD128_MASKZ`.
    fn vexpandpd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VEXPANDPD128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VEXPANDPD128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXPANDPD128_MASKZ");
        }
    }
    /// Emits `VEXPANDPD256`.
    fn vexpandpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VEXPANDPD256RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VEXPANDPD256RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXPANDPD256");
        }
    }
    /// Emits `VEXPANDPD256_MASK`.
    fn vexpandpd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VEXPANDPD256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VEXPANDPD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXPANDPD256_MASK");
        }
    }
    /// Emits `VEXPANDPD256_MASKZ`.
    fn vexpandpd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VEXPANDPD256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VEXPANDPD256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXPANDPD256_MASKZ");
        }
    }
    /// Emits `VEXPANDPD512`.
    fn vexpandpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VEXPANDPD512RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VEXPANDPD512RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXPANDPD512");
        }
    }
    /// Emits `VEXPANDPD512_MASK`.
    fn vexpandpd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VEXPANDPD512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VEXPANDPD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXPANDPD512_MASK");
        }
    }
    /// Emits `VEXPANDPD512_MASKZ`.
    fn vexpandpd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VEXPANDPD512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VEXPANDPD512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXPANDPD512_MASKZ");
        }
    }
    /// Emits `VEXPANDPS128`.
    fn vexpandps128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VEXPANDPS128RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VEXPANDPS128RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXPANDPS128");
        }
    }
    /// Emits `VEXPANDPS128_MASK`.
    fn vexpandps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VEXPANDPS128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VEXPANDPS128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXPANDPS128_MASK");
        }
    }
    /// Emits `VEXPANDPS128_MASKZ`.
    fn vexpandps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VEXPANDPS128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VEXPANDPS128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXPANDPS128_MASKZ");
        }
    }
    /// Emits `VEXPANDPS256`.
    fn vexpandps256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VEXPANDPS256RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VEXPANDPS256RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXPANDPS256");
        }
    }
    /// Emits `VEXPANDPS256_MASK`.
    fn vexpandps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VEXPANDPS256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VEXPANDPS256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXPANDPS256_MASK");
        }
    }
    /// Emits `VEXPANDPS256_MASKZ`.
    fn vexpandps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VEXPANDPS256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VEXPANDPS256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXPANDPS256_MASKZ");
        }
    }
    /// Emits `VEXPANDPS512`.
    fn vexpandps512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VEXPANDPS512RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VEXPANDPS512RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXPANDPS512");
        }
    }
    /// Emits `VEXPANDPS512_MASK`.
    fn vexpandps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VEXPANDPS512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VEXPANDPS512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXPANDPS512_MASK");
        }
    }
    /// Emits `VEXPANDPS512_MASKZ`.
    fn vexpandps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VEXPANDPS512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VEXPANDPS512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXPANDPS512_MASKZ");
        }
    }
    /// Emits `VEXTRACTF32X4_256`.
    fn vextractf32x4_256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF32X4_256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF32X4_256MRI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTF32X4_256");
        }
    }
    /// Emits `VEXTRACTF32X4_256_MASK`.
    fn vextractf32x4_256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF32X4_256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF32X4_256MRI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTF32X4_256_MASK");
        }
    }
    /// Emits `VEXTRACTF32X4_256RRI_MASKZ`.
    fn vextractf32x4_256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VEXTRACTF32X4_256RRI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VEXTRACTF32X4_512`.
    fn vextractf32x4_512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF32X4_512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF32X4_512MRI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTF32X4_512");
        }
    }
    /// Emits `VEXTRACTF32X4_512_MASK`.
    fn vextractf32x4_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF32X4_512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF32X4_512MRI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTF32X4_512_MASK");
        }
    }
    /// Emits `VEXTRACTF32X4_512RRI_MASKZ`.
    fn vextractf32x4_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VEXTRACTF32X4_512RRI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VEXTRACTF32X8_512`.
    fn vextractf32x8_512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF32X8_512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF32X8_512MRI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTF32X8_512");
        }
    }
    /// Emits `VEXTRACTF32X8_512_MASK`.
    fn vextractf32x8_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF32X8_512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF32X8_512MRI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTF32X8_512_MASK");
        }
    }
    /// Emits `VEXTRACTF32X8_512RRI_MASKZ`.
    fn vextractf32x8_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VEXTRACTF32X8_512RRI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VEXTRACTF64X2_256`.
    fn vextractf64x2_256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF64X2_256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF64X2_256MRI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTF64X2_256");
        }
    }
    /// Emits `VEXTRACTF64X2_256_MASK`.
    fn vextractf64x2_256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF64X2_256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF64X2_256MRI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTF64X2_256_MASK");
        }
    }
    /// Emits `VEXTRACTF64X2_256RRI_MASKZ`.
    fn vextractf64x2_256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VEXTRACTF64X2_256RRI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VEXTRACTF64X2_512`.
    fn vextractf64x2_512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF64X2_512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF64X2_512MRI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTF64X2_512");
        }
    }
    /// Emits `VEXTRACTF64X2_512_MASK`.
    fn vextractf64x2_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF64X2_512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF64X2_512MRI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTF64X2_512_MASK");
        }
    }
    /// Emits `VEXTRACTF64X2_512RRI_MASKZ`.
    fn vextractf64x2_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VEXTRACTF64X2_512RRI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VEXTRACTF64X4_512`.
    fn vextractf64x4_512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF64X4_512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF64X4_512MRI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTF64X4_512");
        }
    }
    /// Emits `VEXTRACTF64X4_512_MASK`.
    fn vextractf64x4_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF64X4_512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTF64X4_512MRI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTF64X4_512_MASK");
        }
    }
    /// Emits `VEXTRACTF64X4_512RRI_MASKZ`.
    fn vextractf64x4_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VEXTRACTF64X4_512RRI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VEXTRACTI32X4_256`.
    fn vextracti32x4_256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI32X4_256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI32X4_256MRI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTI32X4_256");
        }
    }
    /// Emits `VEXTRACTI32X4_256_MASK`.
    fn vextracti32x4_256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI32X4_256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI32X4_256MRI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTI32X4_256_MASK");
        }
    }
    /// Emits `VEXTRACTI32X4_256RRI_MASKZ`.
    fn vextracti32x4_256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VEXTRACTI32X4_256RRI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VEXTRACTI32X4_512`.
    fn vextracti32x4_512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI32X4_512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI32X4_512MRI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTI32X4_512");
        }
    }
    /// Emits `VEXTRACTI32X4_512_MASK`.
    fn vextracti32x4_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI32X4_512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI32X4_512MRI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTI32X4_512_MASK");
        }
    }
    /// Emits `VEXTRACTI32X4_512RRI_MASKZ`.
    fn vextracti32x4_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VEXTRACTI32X4_512RRI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VEXTRACTI32X8_512`.
    fn vextracti32x8_512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI32X8_512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI32X8_512MRI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTI32X8_512");
        }
    }
    /// Emits `VEXTRACTI32X8_512_MASK`.
    fn vextracti32x8_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI32X8_512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI32X8_512MRI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTI32X8_512_MASK");
        }
    }
    /// Emits `VEXTRACTI32X8_512RRI_MASKZ`.
    fn vextracti32x8_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VEXTRACTI32X8_512RRI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VEXTRACTI64X2_256`.
    fn vextracti64x2_256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI64X2_256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI64X2_256MRI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTI64X2_256");
        }
    }
    /// Emits `VEXTRACTI64X2_256_MASK`.
    fn vextracti64x2_256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI64X2_256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI64X2_256MRI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTI64X2_256_MASK");
        }
    }
    /// Emits `VEXTRACTI64X2_256RRI_MASKZ`.
    fn vextracti64x2_256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VEXTRACTI64X2_256RRI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VEXTRACTI64X2_512`.
    fn vextracti64x2_512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI64X2_512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI64X2_512MRI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTI64X2_512");
        }
    }
    /// Emits `VEXTRACTI64X2_512_MASK`.
    fn vextracti64x2_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI64X2_512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI64X2_512MRI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTI64X2_512_MASK");
        }
    }
    /// Emits `VEXTRACTI64X2_512RRI_MASKZ`.
    fn vextracti64x2_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VEXTRACTI64X2_512RRI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VEXTRACTI64X4_512`.
    fn vextracti64x4_512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI64X4_512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI64X4_512MRI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTI64X4_512");
        }
    }
    /// Emits `VEXTRACTI64X4_512_MASK`.
    fn vextracti64x4_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI64X4_512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTI64X4_512MRI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTI64X4_512_MASK");
        }
    }
    /// Emits `VEXTRACTI64X4_512RRI_MASKZ`.
    fn vextracti64x4_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VEXTRACTI64X4_512RRI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VEXTRACTPS`.
    fn vextractps(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_gp() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTPSRRI, op0,op1,op2,&NOREG);
        } else if op0.is_mem() && op1.is_vec() && op2.is_imm() {
            self.emit(VEXTRACTPSMRI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VEXTRACTPS");
        }
    }
    /// Emits `VFIXUPIMMPD128`.
    fn vfixupimmpd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMPD128RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMPD128RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMPD128");
        }
    }
    /// Emits `VFIXUPIMMPD128_MASK`.
    fn vfixupimmpd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMPD128RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMPD128RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMPD128_MASK");
        }
    }
    /// Emits `VFIXUPIMMPD128_MASKZ`.
    fn vfixupimmpd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMPD128RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMPD128RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMPD128_MASKZ");
        }
    }
    /// Emits `VFIXUPIMMPD128RRBI`.
    fn vfixupimmpd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPD128RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPD128RRBI_MASK`.
    fn vfixupimmpd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPD128RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPD128RRBI_MASKZ`.
    fn vfixupimmpd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPD128RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPD256`.
    fn vfixupimmpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMPD256RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMPD256RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMPD256");
        }
    }
    /// Emits `VFIXUPIMMPD256_MASK`.
    fn vfixupimmpd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMPD256RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMPD256RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMPD256_MASK");
        }
    }
    /// Emits `VFIXUPIMMPD256_MASKZ`.
    fn vfixupimmpd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMPD256RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMPD256RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMPD256_MASKZ");
        }
    }
    /// Emits `VFIXUPIMMPD256RRBI`.
    fn vfixupimmpd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPD256RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPD256RRBI_MASK`.
    fn vfixupimmpd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPD256RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPD256RRBI_MASKZ`.
    fn vfixupimmpd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPD256RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPD512`.
    fn vfixupimmpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMPD512RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMPD512RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMPD512");
        }
    }
    /// Emits `VFIXUPIMMPD512_MASK`.
    fn vfixupimmpd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMPD512RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMPD512RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMPD512_MASK");
        }
    }
    /// Emits `VFIXUPIMMPD512RRRI_MASK_SAE`.
    fn vfixupimmpd512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPD512RRRI_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPD512_MASKZ`.
    fn vfixupimmpd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMPD512RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMPD512RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMPD512_MASKZ");
        }
    }
    /// Emits `VFIXUPIMMPD512RRRI_MASKZ_SAE`.
    fn vfixupimmpd512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPD512RRRI_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPD512RRRI_SAE`.
    fn vfixupimmpd512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPD512RRRI_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPD512RRBI`.
    fn vfixupimmpd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPD512RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPD512RRBI_MASK`.
    fn vfixupimmpd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPD512RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPD512RRBI_MASKZ`.
    fn vfixupimmpd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPD512RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPS128`.
    fn vfixupimmps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMPS128RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMPS128RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMPS128");
        }
    }
    /// Emits `VFIXUPIMMPS128_MASK`.
    fn vfixupimmps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMPS128RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMPS128RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMPS128_MASK");
        }
    }
    /// Emits `VFIXUPIMMPS128_MASKZ`.
    fn vfixupimmps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMPS128RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMPS128RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMPS128_MASKZ");
        }
    }
    /// Emits `VFIXUPIMMPS128RRBI`.
    fn vfixupimmps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPS128RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPS128RRBI_MASK`.
    fn vfixupimmps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPS128RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPS128RRBI_MASKZ`.
    fn vfixupimmps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPS128RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPS256`.
    fn vfixupimmps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMPS256RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMPS256RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMPS256");
        }
    }
    /// Emits `VFIXUPIMMPS256_MASK`.
    fn vfixupimmps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMPS256RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMPS256RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMPS256_MASK");
        }
    }
    /// Emits `VFIXUPIMMPS256_MASKZ`.
    fn vfixupimmps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMPS256RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMPS256RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMPS256_MASKZ");
        }
    }
    /// Emits `VFIXUPIMMPS256RRBI`.
    fn vfixupimmps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPS256RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPS256RRBI_MASK`.
    fn vfixupimmps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPS256RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPS256RRBI_MASKZ`.
    fn vfixupimmps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPS256RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPS512`.
    fn vfixupimmps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMPS512RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMPS512RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMPS512");
        }
    }
    /// Emits `VFIXUPIMMPS512_MASK`.
    fn vfixupimmps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMPS512RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMPS512RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMPS512_MASK");
        }
    }
    /// Emits `VFIXUPIMMPS512RRRI_MASK_SAE`.
    fn vfixupimmps512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPS512RRRI_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPS512_MASKZ`.
    fn vfixupimmps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMPS512RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMPS512RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMPS512_MASKZ");
        }
    }
    /// Emits `VFIXUPIMMPS512RRRI_MASKZ_SAE`.
    fn vfixupimmps512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPS512RRRI_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPS512RRRI_SAE`.
    fn vfixupimmps512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPS512RRRI_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPS512RRBI`.
    fn vfixupimmps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPS512RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPS512RRBI_MASK`.
    fn vfixupimmps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPS512RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMPS512RRBI_MASKZ`.
    fn vfixupimmps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMPS512RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMSD`.
    fn vfixupimmsd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMSDRRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMSDRRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMSD");
        }
    }
    /// Emits `VFIXUPIMMSD_MASK`.
    fn vfixupimmsd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMSDRRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMSDRRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMSD_MASK");
        }
    }
    /// Emits `VFIXUPIMMSDRRRI_MASK_SAE`.
    fn vfixupimmsd_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMSDRRRI_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMSD_MASKZ`.
    fn vfixupimmsd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMSDRRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMSDRRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMSD_MASKZ");
        }
    }
    /// Emits `VFIXUPIMMSDRRRI_MASKZ_SAE`.
    fn vfixupimmsd_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMSDRRRI_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMSDRRRI_SAE`.
    fn vfixupimmsd_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMSDRRRI_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMSS`.
    fn vfixupimmss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMSSRRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMSSRRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMSS");
        }
    }
    /// Emits `VFIXUPIMMSS_MASK`.
    fn vfixupimmss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMSSRRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMSSRRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMSS_MASK");
        }
    }
    /// Emits `VFIXUPIMMSSRRRI_MASK_SAE`.
    fn vfixupimmss_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMSSRRRI_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMSS_MASKZ`.
    fn vfixupimmss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VFIXUPIMMSSRRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VFIXUPIMMSSRRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VFIXUPIMMSS_MASKZ");
        }
    }
    /// Emits `VFIXUPIMMSSRRRI_MASKZ_SAE`.
    fn vfixupimmss_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMSSRRRI_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFIXUPIMMSSRRRI_SAE`.
    fn vfixupimmss_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VFIXUPIMMSSRRRI_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VFMADD132PD128`.
    fn vfmadd132pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132PD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132PD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132PD128");
        }
    }
    /// Emits `VFMADD132PD128_MASK`.
    fn vfmadd132pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132PD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132PD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132PD128_MASK");
        }
    }
    /// Emits `VFMADD132PD128_MASKZ`.
    fn vfmadd132pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132PD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132PD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132PD128_MASKZ");
        }
    }
    /// Emits `VFMADD132PD128RRB`.
    fn vfmadd132pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PD128RRB_MASK`.
    fn vfmadd132pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PD128RRB_MASKZ`.
    fn vfmadd132pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PD256`.
    fn vfmadd132pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132PD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132PD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132PD256");
        }
    }
    /// Emits `VFMADD132PD256_MASK`.
    fn vfmadd132pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132PD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132PD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132PD256_MASK");
        }
    }
    /// Emits `VFMADD132PD256_MASKZ`.
    fn vfmadd132pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132PD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132PD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132PD256_MASKZ");
        }
    }
    /// Emits `VFMADD132PD256RRB`.
    fn vfmadd132pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PD256RRB_MASK`.
    fn vfmadd132pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PD256RRB_MASKZ`.
    fn vfmadd132pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PD512`.
    fn vfmadd132pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132PD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132PD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132PD512");
        }
    }
    /// Emits `VFMADD132PD512RRR_ER`.
    fn vfmadd132pd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PD512_MASK`.
    fn vfmadd132pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132PD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132PD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132PD512_MASK");
        }
    }
    /// Emits `VFMADD132PD512RRR_MASK_ER`.
    fn vfmadd132pd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PD512_MASKZ`.
    fn vfmadd132pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132PD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132PD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132PD512_MASKZ");
        }
    }
    /// Emits `VFMADD132PD512RRR_MASKZ_ER`.
    fn vfmadd132pd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PD512RRB`.
    fn vfmadd132pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PD512RRB_MASK`.
    fn vfmadd132pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PD512RRB_MASKZ`.
    fn vfmadd132pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PS128`.
    fn vfmadd132ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132PS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132PS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132PS128");
        }
    }
    /// Emits `VFMADD132PS128_MASK`.
    fn vfmadd132ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132PS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132PS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132PS128_MASK");
        }
    }
    /// Emits `VFMADD132PS128_MASKZ`.
    fn vfmadd132ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132PS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132PS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132PS128_MASKZ");
        }
    }
    /// Emits `VFMADD132PS128RRB`.
    fn vfmadd132ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PS128RRB_MASK`.
    fn vfmadd132ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PS128RRB_MASKZ`.
    fn vfmadd132ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PS256`.
    fn vfmadd132ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132PS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132PS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132PS256");
        }
    }
    /// Emits `VFMADD132PS256_MASK`.
    fn vfmadd132ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132PS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132PS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132PS256_MASK");
        }
    }
    /// Emits `VFMADD132PS256_MASKZ`.
    fn vfmadd132ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132PS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132PS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132PS256_MASKZ");
        }
    }
    /// Emits `VFMADD132PS256RRB`.
    fn vfmadd132ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PS256RRB_MASK`.
    fn vfmadd132ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PS256RRB_MASKZ`.
    fn vfmadd132ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PS512`.
    fn vfmadd132ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132PS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132PS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132PS512");
        }
    }
    /// Emits `VFMADD132PS512RRR_ER`.
    fn vfmadd132ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PS512_MASK`.
    fn vfmadd132ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132PS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132PS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132PS512_MASK");
        }
    }
    /// Emits `VFMADD132PS512RRR_MASK_ER`.
    fn vfmadd132ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PS512_MASKZ`.
    fn vfmadd132ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132PS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132PS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132PS512_MASKZ");
        }
    }
    /// Emits `VFMADD132PS512RRR_MASKZ_ER`.
    fn vfmadd132ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PS512RRB`.
    fn vfmadd132ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PS512RRB_MASK`.
    fn vfmadd132ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132PS512RRB_MASKZ`.
    fn vfmadd132ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132PS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132SD`.
    fn vfmadd132sd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132SDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132SDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132SD");
        }
    }
    /// Emits `VFMADD132SDRRR_ER`.
    fn vfmadd132sd_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132SDRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132SD_MASK`.
    fn vfmadd132sd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132SDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132SDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132SD_MASK");
        }
    }
    /// Emits `VFMADD132SDRRR_MASK_ER`.
    fn vfmadd132sd_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132SDRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132SD_MASKZ`.
    fn vfmadd132sd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132SDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132SDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132SD_MASKZ");
        }
    }
    /// Emits `VFMADD132SDRRR_MASKZ_ER`.
    fn vfmadd132sd_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132SDRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132SS`.
    fn vfmadd132ss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132SSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132SSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132SS");
        }
    }
    /// Emits `VFMADD132SSRRR_ER`.
    fn vfmadd132ss_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132SSRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132SS_MASK`.
    fn vfmadd132ss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132SSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132SSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132SS_MASK");
        }
    }
    /// Emits `VFMADD132SSRRR_MASK_ER`.
    fn vfmadd132ss_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132SSRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD132SS_MASKZ`.
    fn vfmadd132ss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD132SSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD132SSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD132SS_MASKZ");
        }
    }
    /// Emits `VFMADD132SSRRR_MASKZ_ER`.
    fn vfmadd132ss_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD132SSRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PD128`.
    fn vfmadd213pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213PD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213PD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213PD128");
        }
    }
    /// Emits `VFMADD213PD128_MASK`.
    fn vfmadd213pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213PD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213PD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213PD128_MASK");
        }
    }
    /// Emits `VFMADD213PD128_MASKZ`.
    fn vfmadd213pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213PD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213PD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213PD128_MASKZ");
        }
    }
    /// Emits `VFMADD213PD128RRB`.
    fn vfmadd213pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PD128RRB_MASK`.
    fn vfmadd213pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PD128RRB_MASKZ`.
    fn vfmadd213pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PD256`.
    fn vfmadd213pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213PD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213PD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213PD256");
        }
    }
    /// Emits `VFMADD213PD256_MASK`.
    fn vfmadd213pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213PD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213PD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213PD256_MASK");
        }
    }
    /// Emits `VFMADD213PD256_MASKZ`.
    fn vfmadd213pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213PD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213PD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213PD256_MASKZ");
        }
    }
    /// Emits `VFMADD213PD256RRB`.
    fn vfmadd213pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PD256RRB_MASK`.
    fn vfmadd213pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PD256RRB_MASKZ`.
    fn vfmadd213pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PD512`.
    fn vfmadd213pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213PD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213PD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213PD512");
        }
    }
    /// Emits `VFMADD213PD512RRR_ER`.
    fn vfmadd213pd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PD512_MASK`.
    fn vfmadd213pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213PD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213PD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213PD512_MASK");
        }
    }
    /// Emits `VFMADD213PD512RRR_MASK_ER`.
    fn vfmadd213pd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PD512_MASKZ`.
    fn vfmadd213pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213PD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213PD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213PD512_MASKZ");
        }
    }
    /// Emits `VFMADD213PD512RRR_MASKZ_ER`.
    fn vfmadd213pd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PD512RRB`.
    fn vfmadd213pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PD512RRB_MASK`.
    fn vfmadd213pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PD512RRB_MASKZ`.
    fn vfmadd213pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PS128`.
    fn vfmadd213ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213PS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213PS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213PS128");
        }
    }
    /// Emits `VFMADD213PS128_MASK`.
    fn vfmadd213ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213PS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213PS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213PS128_MASK");
        }
    }
    /// Emits `VFMADD213PS128_MASKZ`.
    fn vfmadd213ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213PS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213PS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213PS128_MASKZ");
        }
    }
    /// Emits `VFMADD213PS128RRB`.
    fn vfmadd213ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PS128RRB_MASK`.
    fn vfmadd213ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PS128RRB_MASKZ`.
    fn vfmadd213ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PS256`.
    fn vfmadd213ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213PS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213PS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213PS256");
        }
    }
    /// Emits `VFMADD213PS256_MASK`.
    fn vfmadd213ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213PS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213PS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213PS256_MASK");
        }
    }
    /// Emits `VFMADD213PS256_MASKZ`.
    fn vfmadd213ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213PS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213PS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213PS256_MASKZ");
        }
    }
    /// Emits `VFMADD213PS256RRB`.
    fn vfmadd213ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PS256RRB_MASK`.
    fn vfmadd213ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PS256RRB_MASKZ`.
    fn vfmadd213ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PS512`.
    fn vfmadd213ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213PS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213PS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213PS512");
        }
    }
    /// Emits `VFMADD213PS512RRR_ER`.
    fn vfmadd213ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PS512_MASK`.
    fn vfmadd213ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213PS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213PS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213PS512_MASK");
        }
    }
    /// Emits `VFMADD213PS512RRR_MASK_ER`.
    fn vfmadd213ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PS512_MASKZ`.
    fn vfmadd213ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213PS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213PS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213PS512_MASKZ");
        }
    }
    /// Emits `VFMADD213PS512RRR_MASKZ_ER`.
    fn vfmadd213ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PS512RRB`.
    fn vfmadd213ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PS512RRB_MASK`.
    fn vfmadd213ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213PS512RRB_MASKZ`.
    fn vfmadd213ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213PS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213SD`.
    fn vfmadd213sd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213SDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213SDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213SD");
        }
    }
    /// Emits `VFMADD213SDRRR_ER`.
    fn vfmadd213sd_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213SDRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213SD_MASK`.
    fn vfmadd213sd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213SDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213SDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213SD_MASK");
        }
    }
    /// Emits `VFMADD213SDRRR_MASK_ER`.
    fn vfmadd213sd_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213SDRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213SD_MASKZ`.
    fn vfmadd213sd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213SDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213SDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213SD_MASKZ");
        }
    }
    /// Emits `VFMADD213SDRRR_MASKZ_ER`.
    fn vfmadd213sd_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213SDRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213SS`.
    fn vfmadd213ss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213SSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213SSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213SS");
        }
    }
    /// Emits `VFMADD213SSRRR_ER`.
    fn vfmadd213ss_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213SSRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213SS_MASK`.
    fn vfmadd213ss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213SSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213SSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213SS_MASK");
        }
    }
    /// Emits `VFMADD213SSRRR_MASK_ER`.
    fn vfmadd213ss_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213SSRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD213SS_MASKZ`.
    fn vfmadd213ss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD213SSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD213SSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD213SS_MASKZ");
        }
    }
    /// Emits `VFMADD213SSRRR_MASKZ_ER`.
    fn vfmadd213ss_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD213SSRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PD128`.
    fn vfmadd231pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231PD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231PD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231PD128");
        }
    }
    /// Emits `VFMADD231PD128_MASK`.
    fn vfmadd231pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231PD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231PD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231PD128_MASK");
        }
    }
    /// Emits `VFMADD231PD128_MASKZ`.
    fn vfmadd231pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231PD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231PD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231PD128_MASKZ");
        }
    }
    /// Emits `VFMADD231PD128RRB`.
    fn vfmadd231pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PD128RRB_MASK`.
    fn vfmadd231pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PD128RRB_MASKZ`.
    fn vfmadd231pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PD256`.
    fn vfmadd231pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231PD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231PD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231PD256");
        }
    }
    /// Emits `VFMADD231PD256_MASK`.
    fn vfmadd231pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231PD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231PD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231PD256_MASK");
        }
    }
    /// Emits `VFMADD231PD256_MASKZ`.
    fn vfmadd231pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231PD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231PD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231PD256_MASKZ");
        }
    }
    /// Emits `VFMADD231PD256RRB`.
    fn vfmadd231pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PD256RRB_MASK`.
    fn vfmadd231pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PD256RRB_MASKZ`.
    fn vfmadd231pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PD512`.
    fn vfmadd231pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231PD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231PD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231PD512");
        }
    }
    /// Emits `VFMADD231PD512RRR_ER`.
    fn vfmadd231pd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PD512_MASK`.
    fn vfmadd231pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231PD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231PD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231PD512_MASK");
        }
    }
    /// Emits `VFMADD231PD512RRR_MASK_ER`.
    fn vfmadd231pd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PD512_MASKZ`.
    fn vfmadd231pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231PD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231PD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231PD512_MASKZ");
        }
    }
    /// Emits `VFMADD231PD512RRR_MASKZ_ER`.
    fn vfmadd231pd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PD512RRB`.
    fn vfmadd231pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PD512RRB_MASK`.
    fn vfmadd231pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PD512RRB_MASKZ`.
    fn vfmadd231pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PS128`.
    fn vfmadd231ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231PS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231PS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231PS128");
        }
    }
    /// Emits `VFMADD231PS128_MASK`.
    fn vfmadd231ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231PS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231PS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231PS128_MASK");
        }
    }
    /// Emits `VFMADD231PS128_MASKZ`.
    fn vfmadd231ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231PS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231PS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231PS128_MASKZ");
        }
    }
    /// Emits `VFMADD231PS128RRB`.
    fn vfmadd231ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PS128RRB_MASK`.
    fn vfmadd231ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PS128RRB_MASKZ`.
    fn vfmadd231ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PS256`.
    fn vfmadd231ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231PS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231PS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231PS256");
        }
    }
    /// Emits `VFMADD231PS256_MASK`.
    fn vfmadd231ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231PS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231PS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231PS256_MASK");
        }
    }
    /// Emits `VFMADD231PS256_MASKZ`.
    fn vfmadd231ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231PS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231PS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231PS256_MASKZ");
        }
    }
    /// Emits `VFMADD231PS256RRB`.
    fn vfmadd231ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PS256RRB_MASK`.
    fn vfmadd231ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PS256RRB_MASKZ`.
    fn vfmadd231ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PS512`.
    fn vfmadd231ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231PS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231PS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231PS512");
        }
    }
    /// Emits `VFMADD231PS512RRR_ER`.
    fn vfmadd231ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PS512_MASK`.
    fn vfmadd231ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231PS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231PS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231PS512_MASK");
        }
    }
    /// Emits `VFMADD231PS512RRR_MASK_ER`.
    fn vfmadd231ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PS512_MASKZ`.
    fn vfmadd231ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231PS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231PS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231PS512_MASKZ");
        }
    }
    /// Emits `VFMADD231PS512RRR_MASKZ_ER`.
    fn vfmadd231ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PS512RRB`.
    fn vfmadd231ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PS512RRB_MASK`.
    fn vfmadd231ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231PS512RRB_MASKZ`.
    fn vfmadd231ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231PS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231SD`.
    fn vfmadd231sd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231SDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231SDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231SD");
        }
    }
    /// Emits `VFMADD231SDRRR_ER`.
    fn vfmadd231sd_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231SDRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231SD_MASK`.
    fn vfmadd231sd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231SDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231SDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231SD_MASK");
        }
    }
    /// Emits `VFMADD231SDRRR_MASK_ER`.
    fn vfmadd231sd_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231SDRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231SD_MASKZ`.
    fn vfmadd231sd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231SDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231SDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231SD_MASKZ");
        }
    }
    /// Emits `VFMADD231SDRRR_MASKZ_ER`.
    fn vfmadd231sd_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231SDRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231SS`.
    fn vfmadd231ss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231SSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231SSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231SS");
        }
    }
    /// Emits `VFMADD231SSRRR_ER`.
    fn vfmadd231ss_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231SSRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231SS_MASK`.
    fn vfmadd231ss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231SSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231SSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231SS_MASK");
        }
    }
    /// Emits `VFMADD231SSRRR_MASK_ER`.
    fn vfmadd231ss_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231SSRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADD231SS_MASKZ`.
    fn vfmadd231ss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADD231SSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADD231SSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADD231SS_MASKZ");
        }
    }
    /// Emits `VFMADD231SSRRR_MASKZ_ER`.
    fn vfmadd231ss_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADD231SSRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PD128`.
    fn vfmaddsub132pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB132PD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB132PD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB132PD128");
        }
    }
    /// Emits `VFMADDSUB132PD128_MASK`.
    fn vfmaddsub132pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB132PD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB132PD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB132PD128_MASK");
        }
    }
    /// Emits `VFMADDSUB132PD128_MASKZ`.
    fn vfmaddsub132pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB132PD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB132PD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB132PD128_MASKZ");
        }
    }
    /// Emits `VFMADDSUB132PD128RRB`.
    fn vfmaddsub132pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PD128RRB_MASK`.
    fn vfmaddsub132pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PD128RRB_MASKZ`.
    fn vfmaddsub132pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PD256`.
    fn vfmaddsub132pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB132PD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB132PD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB132PD256");
        }
    }
    /// Emits `VFMADDSUB132PD256_MASK`.
    fn vfmaddsub132pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB132PD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB132PD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB132PD256_MASK");
        }
    }
    /// Emits `VFMADDSUB132PD256_MASKZ`.
    fn vfmaddsub132pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB132PD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB132PD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB132PD256_MASKZ");
        }
    }
    /// Emits `VFMADDSUB132PD256RRB`.
    fn vfmaddsub132pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PD256RRB_MASK`.
    fn vfmaddsub132pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PD256RRB_MASKZ`.
    fn vfmaddsub132pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PD512`.
    fn vfmaddsub132pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB132PD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB132PD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB132PD512");
        }
    }
    /// Emits `VFMADDSUB132PD512RRR_ER`.
    fn vfmaddsub132pd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PD512_MASK`.
    fn vfmaddsub132pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB132PD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB132PD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB132PD512_MASK");
        }
    }
    /// Emits `VFMADDSUB132PD512RRR_MASK_ER`.
    fn vfmaddsub132pd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PD512_MASKZ`.
    fn vfmaddsub132pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB132PD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB132PD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB132PD512_MASKZ");
        }
    }
    /// Emits `VFMADDSUB132PD512RRR_MASKZ_ER`.
    fn vfmaddsub132pd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PD512RRB`.
    fn vfmaddsub132pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PD512RRB_MASK`.
    fn vfmaddsub132pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PD512RRB_MASKZ`.
    fn vfmaddsub132pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PS128`.
    fn vfmaddsub132ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB132PS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB132PS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB132PS128");
        }
    }
    /// Emits `VFMADDSUB132PS128_MASK`.
    fn vfmaddsub132ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB132PS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB132PS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB132PS128_MASK");
        }
    }
    /// Emits `VFMADDSUB132PS128_MASKZ`.
    fn vfmaddsub132ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB132PS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB132PS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB132PS128_MASKZ");
        }
    }
    /// Emits `VFMADDSUB132PS128RRB`.
    fn vfmaddsub132ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PS128RRB_MASK`.
    fn vfmaddsub132ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PS128RRB_MASKZ`.
    fn vfmaddsub132ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PS256`.
    fn vfmaddsub132ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB132PS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB132PS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB132PS256");
        }
    }
    /// Emits `VFMADDSUB132PS256_MASK`.
    fn vfmaddsub132ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB132PS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB132PS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB132PS256_MASK");
        }
    }
    /// Emits `VFMADDSUB132PS256_MASKZ`.
    fn vfmaddsub132ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB132PS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB132PS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB132PS256_MASKZ");
        }
    }
    /// Emits `VFMADDSUB132PS256RRB`.
    fn vfmaddsub132ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PS256RRB_MASK`.
    fn vfmaddsub132ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PS256RRB_MASKZ`.
    fn vfmaddsub132ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PS512`.
    fn vfmaddsub132ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB132PS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB132PS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB132PS512");
        }
    }
    /// Emits `VFMADDSUB132PS512RRR_ER`.
    fn vfmaddsub132ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PS512_MASK`.
    fn vfmaddsub132ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB132PS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB132PS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB132PS512_MASK");
        }
    }
    /// Emits `VFMADDSUB132PS512RRR_MASK_ER`.
    fn vfmaddsub132ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PS512_MASKZ`.
    fn vfmaddsub132ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB132PS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB132PS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB132PS512_MASKZ");
        }
    }
    /// Emits `VFMADDSUB132PS512RRR_MASKZ_ER`.
    fn vfmaddsub132ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PS512RRB`.
    fn vfmaddsub132ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PS512RRB_MASK`.
    fn vfmaddsub132ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB132PS512RRB_MASKZ`.
    fn vfmaddsub132ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB132PS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PD128`.
    fn vfmaddsub213pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB213PD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB213PD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB213PD128");
        }
    }
    /// Emits `VFMADDSUB213PD128_MASK`.
    fn vfmaddsub213pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB213PD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB213PD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB213PD128_MASK");
        }
    }
    /// Emits `VFMADDSUB213PD128_MASKZ`.
    fn vfmaddsub213pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB213PD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB213PD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB213PD128_MASKZ");
        }
    }
    /// Emits `VFMADDSUB213PD128RRB`.
    fn vfmaddsub213pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PD128RRB_MASK`.
    fn vfmaddsub213pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PD128RRB_MASKZ`.
    fn vfmaddsub213pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PD256`.
    fn vfmaddsub213pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB213PD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB213PD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB213PD256");
        }
    }
    /// Emits `VFMADDSUB213PD256_MASK`.
    fn vfmaddsub213pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB213PD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB213PD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB213PD256_MASK");
        }
    }
    /// Emits `VFMADDSUB213PD256_MASKZ`.
    fn vfmaddsub213pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB213PD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB213PD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB213PD256_MASKZ");
        }
    }
    /// Emits `VFMADDSUB213PD256RRB`.
    fn vfmaddsub213pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PD256RRB_MASK`.
    fn vfmaddsub213pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PD256RRB_MASKZ`.
    fn vfmaddsub213pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PD512`.
    fn vfmaddsub213pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB213PD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB213PD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB213PD512");
        }
    }
    /// Emits `VFMADDSUB213PD512RRR_ER`.
    fn vfmaddsub213pd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PD512_MASK`.
    fn vfmaddsub213pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB213PD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB213PD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB213PD512_MASK");
        }
    }
    /// Emits `VFMADDSUB213PD512RRR_MASK_ER`.
    fn vfmaddsub213pd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PD512_MASKZ`.
    fn vfmaddsub213pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB213PD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB213PD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB213PD512_MASKZ");
        }
    }
    /// Emits `VFMADDSUB213PD512RRR_MASKZ_ER`.
    fn vfmaddsub213pd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PD512RRB`.
    fn vfmaddsub213pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PD512RRB_MASK`.
    fn vfmaddsub213pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PD512RRB_MASKZ`.
    fn vfmaddsub213pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PS128`.
    fn vfmaddsub213ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB213PS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB213PS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB213PS128");
        }
    }
    /// Emits `VFMADDSUB213PS128_MASK`.
    fn vfmaddsub213ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB213PS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB213PS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB213PS128_MASK");
        }
    }
    /// Emits `VFMADDSUB213PS128_MASKZ`.
    fn vfmaddsub213ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB213PS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB213PS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB213PS128_MASKZ");
        }
    }
    /// Emits `VFMADDSUB213PS128RRB`.
    fn vfmaddsub213ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PS128RRB_MASK`.
    fn vfmaddsub213ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PS128RRB_MASKZ`.
    fn vfmaddsub213ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PS256`.
    fn vfmaddsub213ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB213PS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB213PS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB213PS256");
        }
    }
    /// Emits `VFMADDSUB213PS256_MASK`.
    fn vfmaddsub213ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB213PS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB213PS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB213PS256_MASK");
        }
    }
    /// Emits `VFMADDSUB213PS256_MASKZ`.
    fn vfmaddsub213ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB213PS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB213PS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB213PS256_MASKZ");
        }
    }
    /// Emits `VFMADDSUB213PS256RRB`.
    fn vfmaddsub213ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PS256RRB_MASK`.
    fn vfmaddsub213ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PS256RRB_MASKZ`.
    fn vfmaddsub213ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PS512`.
    fn vfmaddsub213ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB213PS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB213PS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB213PS512");
        }
    }
    /// Emits `VFMADDSUB213PS512RRR_ER`.
    fn vfmaddsub213ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PS512_MASK`.
    fn vfmaddsub213ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB213PS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB213PS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB213PS512_MASK");
        }
    }
    /// Emits `VFMADDSUB213PS512RRR_MASK_ER`.
    fn vfmaddsub213ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PS512_MASKZ`.
    fn vfmaddsub213ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB213PS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB213PS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB213PS512_MASKZ");
        }
    }
    /// Emits `VFMADDSUB213PS512RRR_MASKZ_ER`.
    fn vfmaddsub213ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PS512RRB`.
    fn vfmaddsub213ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PS512RRB_MASK`.
    fn vfmaddsub213ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB213PS512RRB_MASKZ`.
    fn vfmaddsub213ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB213PS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PD128`.
    fn vfmaddsub231pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB231PD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB231PD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB231PD128");
        }
    }
    /// Emits `VFMADDSUB231PD128_MASK`.
    fn vfmaddsub231pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB231PD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB231PD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB231PD128_MASK");
        }
    }
    /// Emits `VFMADDSUB231PD128_MASKZ`.
    fn vfmaddsub231pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB231PD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB231PD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB231PD128_MASKZ");
        }
    }
    /// Emits `VFMADDSUB231PD128RRB`.
    fn vfmaddsub231pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PD128RRB_MASK`.
    fn vfmaddsub231pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PD128RRB_MASKZ`.
    fn vfmaddsub231pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PD256`.
    fn vfmaddsub231pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB231PD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB231PD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB231PD256");
        }
    }
    /// Emits `VFMADDSUB231PD256_MASK`.
    fn vfmaddsub231pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB231PD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB231PD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB231PD256_MASK");
        }
    }
    /// Emits `VFMADDSUB231PD256_MASKZ`.
    fn vfmaddsub231pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB231PD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB231PD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB231PD256_MASKZ");
        }
    }
    /// Emits `VFMADDSUB231PD256RRB`.
    fn vfmaddsub231pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PD256RRB_MASK`.
    fn vfmaddsub231pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PD256RRB_MASKZ`.
    fn vfmaddsub231pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PD512`.
    fn vfmaddsub231pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB231PD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB231PD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB231PD512");
        }
    }
    /// Emits `VFMADDSUB231PD512RRR_ER`.
    fn vfmaddsub231pd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PD512_MASK`.
    fn vfmaddsub231pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB231PD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB231PD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB231PD512_MASK");
        }
    }
    /// Emits `VFMADDSUB231PD512RRR_MASK_ER`.
    fn vfmaddsub231pd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PD512_MASKZ`.
    fn vfmaddsub231pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB231PD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB231PD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB231PD512_MASKZ");
        }
    }
    /// Emits `VFMADDSUB231PD512RRR_MASKZ_ER`.
    fn vfmaddsub231pd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PD512RRB`.
    fn vfmaddsub231pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PD512RRB_MASK`.
    fn vfmaddsub231pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PD512RRB_MASKZ`.
    fn vfmaddsub231pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PS128`.
    fn vfmaddsub231ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB231PS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB231PS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB231PS128");
        }
    }
    /// Emits `VFMADDSUB231PS128_MASK`.
    fn vfmaddsub231ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB231PS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB231PS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB231PS128_MASK");
        }
    }
    /// Emits `VFMADDSUB231PS128_MASKZ`.
    fn vfmaddsub231ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB231PS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB231PS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB231PS128_MASKZ");
        }
    }
    /// Emits `VFMADDSUB231PS128RRB`.
    fn vfmaddsub231ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PS128RRB_MASK`.
    fn vfmaddsub231ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PS128RRB_MASKZ`.
    fn vfmaddsub231ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PS256`.
    fn vfmaddsub231ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB231PS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB231PS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB231PS256");
        }
    }
    /// Emits `VFMADDSUB231PS256_MASK`.
    fn vfmaddsub231ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB231PS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB231PS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB231PS256_MASK");
        }
    }
    /// Emits `VFMADDSUB231PS256_MASKZ`.
    fn vfmaddsub231ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB231PS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB231PS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB231PS256_MASKZ");
        }
    }
    /// Emits `VFMADDSUB231PS256RRB`.
    fn vfmaddsub231ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PS256RRB_MASK`.
    fn vfmaddsub231ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PS256RRB_MASKZ`.
    fn vfmaddsub231ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PS512`.
    fn vfmaddsub231ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB231PS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB231PS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB231PS512");
        }
    }
    /// Emits `VFMADDSUB231PS512RRR_ER`.
    fn vfmaddsub231ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PS512_MASK`.
    fn vfmaddsub231ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB231PS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB231PS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB231PS512_MASK");
        }
    }
    /// Emits `VFMADDSUB231PS512RRR_MASK_ER`.
    fn vfmaddsub231ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PS512_MASKZ`.
    fn vfmaddsub231ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMADDSUB231PS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMADDSUB231PS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMADDSUB231PS512_MASKZ");
        }
    }
    /// Emits `VFMADDSUB231PS512RRR_MASKZ_ER`.
    fn vfmaddsub231ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PS512RRB`.
    fn vfmaddsub231ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PS512RRB_MASK`.
    fn vfmaddsub231ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMADDSUB231PS512RRB_MASKZ`.
    fn vfmaddsub231ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMADDSUB231PS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PD128`.
    fn vfmsub132pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132PD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132PD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132PD128");
        }
    }
    /// Emits `VFMSUB132PD128_MASK`.
    fn vfmsub132pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132PD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132PD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132PD128_MASK");
        }
    }
    /// Emits `VFMSUB132PD128_MASKZ`.
    fn vfmsub132pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132PD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132PD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132PD128_MASKZ");
        }
    }
    /// Emits `VFMSUB132PD128RRB`.
    fn vfmsub132pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PD128RRB_MASK`.
    fn vfmsub132pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PD128RRB_MASKZ`.
    fn vfmsub132pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PD256`.
    fn vfmsub132pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132PD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132PD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132PD256");
        }
    }
    /// Emits `VFMSUB132PD256_MASK`.
    fn vfmsub132pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132PD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132PD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132PD256_MASK");
        }
    }
    /// Emits `VFMSUB132PD256_MASKZ`.
    fn vfmsub132pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132PD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132PD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132PD256_MASKZ");
        }
    }
    /// Emits `VFMSUB132PD256RRB`.
    fn vfmsub132pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PD256RRB_MASK`.
    fn vfmsub132pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PD256RRB_MASKZ`.
    fn vfmsub132pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PD512`.
    fn vfmsub132pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132PD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132PD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132PD512");
        }
    }
    /// Emits `VFMSUB132PD512RRR_ER`.
    fn vfmsub132pd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PD512_MASK`.
    fn vfmsub132pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132PD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132PD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132PD512_MASK");
        }
    }
    /// Emits `VFMSUB132PD512RRR_MASK_ER`.
    fn vfmsub132pd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PD512_MASKZ`.
    fn vfmsub132pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132PD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132PD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132PD512_MASKZ");
        }
    }
    /// Emits `VFMSUB132PD512RRR_MASKZ_ER`.
    fn vfmsub132pd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PD512RRB`.
    fn vfmsub132pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PD512RRB_MASK`.
    fn vfmsub132pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PD512RRB_MASKZ`.
    fn vfmsub132pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PS128`.
    fn vfmsub132ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132PS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132PS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132PS128");
        }
    }
    /// Emits `VFMSUB132PS128_MASK`.
    fn vfmsub132ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132PS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132PS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132PS128_MASK");
        }
    }
    /// Emits `VFMSUB132PS128_MASKZ`.
    fn vfmsub132ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132PS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132PS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132PS128_MASKZ");
        }
    }
    /// Emits `VFMSUB132PS128RRB`.
    fn vfmsub132ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PS128RRB_MASK`.
    fn vfmsub132ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PS128RRB_MASKZ`.
    fn vfmsub132ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PS256`.
    fn vfmsub132ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132PS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132PS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132PS256");
        }
    }
    /// Emits `VFMSUB132PS256_MASK`.
    fn vfmsub132ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132PS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132PS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132PS256_MASK");
        }
    }
    /// Emits `VFMSUB132PS256_MASKZ`.
    fn vfmsub132ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132PS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132PS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132PS256_MASKZ");
        }
    }
    /// Emits `VFMSUB132PS256RRB`.
    fn vfmsub132ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PS256RRB_MASK`.
    fn vfmsub132ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PS256RRB_MASKZ`.
    fn vfmsub132ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PS512`.
    fn vfmsub132ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132PS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132PS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132PS512");
        }
    }
    /// Emits `VFMSUB132PS512RRR_ER`.
    fn vfmsub132ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PS512_MASK`.
    fn vfmsub132ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132PS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132PS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132PS512_MASK");
        }
    }
    /// Emits `VFMSUB132PS512RRR_MASK_ER`.
    fn vfmsub132ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PS512_MASKZ`.
    fn vfmsub132ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132PS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132PS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132PS512_MASKZ");
        }
    }
    /// Emits `VFMSUB132PS512RRR_MASKZ_ER`.
    fn vfmsub132ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PS512RRB`.
    fn vfmsub132ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PS512RRB_MASK`.
    fn vfmsub132ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132PS512RRB_MASKZ`.
    fn vfmsub132ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132PS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132SD`.
    fn vfmsub132sd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132SDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132SDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132SD");
        }
    }
    /// Emits `VFMSUB132SDRRR_ER`.
    fn vfmsub132sd_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132SDRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132SD_MASK`.
    fn vfmsub132sd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132SDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132SDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132SD_MASK");
        }
    }
    /// Emits `VFMSUB132SDRRR_MASK_ER`.
    fn vfmsub132sd_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132SDRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132SD_MASKZ`.
    fn vfmsub132sd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132SDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132SDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132SD_MASKZ");
        }
    }
    /// Emits `VFMSUB132SDRRR_MASKZ_ER`.
    fn vfmsub132sd_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132SDRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132SS`.
    fn vfmsub132ss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132SSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132SSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132SS");
        }
    }
    /// Emits `VFMSUB132SSRRR_ER`.
    fn vfmsub132ss_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132SSRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132SS_MASK`.
    fn vfmsub132ss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132SSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132SSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132SS_MASK");
        }
    }
    /// Emits `VFMSUB132SSRRR_MASK_ER`.
    fn vfmsub132ss_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132SSRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB132SS_MASKZ`.
    fn vfmsub132ss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB132SSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB132SSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB132SS_MASKZ");
        }
    }
    /// Emits `VFMSUB132SSRRR_MASKZ_ER`.
    fn vfmsub132ss_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB132SSRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PD128`.
    fn vfmsub213pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213PD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213PD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213PD128");
        }
    }
    /// Emits `VFMSUB213PD128_MASK`.
    fn vfmsub213pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213PD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213PD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213PD128_MASK");
        }
    }
    /// Emits `VFMSUB213PD128_MASKZ`.
    fn vfmsub213pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213PD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213PD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213PD128_MASKZ");
        }
    }
    /// Emits `VFMSUB213PD128RRB`.
    fn vfmsub213pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PD128RRB_MASK`.
    fn vfmsub213pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PD128RRB_MASKZ`.
    fn vfmsub213pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PD256`.
    fn vfmsub213pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213PD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213PD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213PD256");
        }
    }
    /// Emits `VFMSUB213PD256_MASK`.
    fn vfmsub213pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213PD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213PD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213PD256_MASK");
        }
    }
    /// Emits `VFMSUB213PD256_MASKZ`.
    fn vfmsub213pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213PD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213PD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213PD256_MASKZ");
        }
    }
    /// Emits `VFMSUB213PD256RRB`.
    fn vfmsub213pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PD256RRB_MASK`.
    fn vfmsub213pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PD256RRB_MASKZ`.
    fn vfmsub213pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PD512`.
    fn vfmsub213pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213PD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213PD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213PD512");
        }
    }
    /// Emits `VFMSUB213PD512RRR_ER`.
    fn vfmsub213pd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PD512_MASK`.
    fn vfmsub213pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213PD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213PD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213PD512_MASK");
        }
    }
    /// Emits `VFMSUB213PD512RRR_MASK_ER`.
    fn vfmsub213pd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PD512_MASKZ`.
    fn vfmsub213pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213PD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213PD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213PD512_MASKZ");
        }
    }
    /// Emits `VFMSUB213PD512RRR_MASKZ_ER`.
    fn vfmsub213pd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PD512RRB`.
    fn vfmsub213pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PD512RRB_MASK`.
    fn vfmsub213pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PD512RRB_MASKZ`.
    fn vfmsub213pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PS128`.
    fn vfmsub213ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213PS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213PS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213PS128");
        }
    }
    /// Emits `VFMSUB213PS128_MASK`.
    fn vfmsub213ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213PS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213PS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213PS128_MASK");
        }
    }
    /// Emits `VFMSUB213PS128_MASKZ`.
    fn vfmsub213ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213PS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213PS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213PS128_MASKZ");
        }
    }
    /// Emits `VFMSUB213PS128RRB`.
    fn vfmsub213ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PS128RRB_MASK`.
    fn vfmsub213ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PS128RRB_MASKZ`.
    fn vfmsub213ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PS256`.
    fn vfmsub213ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213PS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213PS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213PS256");
        }
    }
    /// Emits `VFMSUB213PS256_MASK`.
    fn vfmsub213ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213PS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213PS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213PS256_MASK");
        }
    }
    /// Emits `VFMSUB213PS256_MASKZ`.
    fn vfmsub213ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213PS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213PS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213PS256_MASKZ");
        }
    }
    /// Emits `VFMSUB213PS256RRB`.
    fn vfmsub213ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PS256RRB_MASK`.
    fn vfmsub213ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PS256RRB_MASKZ`.
    fn vfmsub213ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PS512`.
    fn vfmsub213ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213PS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213PS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213PS512");
        }
    }
    /// Emits `VFMSUB213PS512RRR_ER`.
    fn vfmsub213ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PS512_MASK`.
    fn vfmsub213ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213PS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213PS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213PS512_MASK");
        }
    }
    /// Emits `VFMSUB213PS512RRR_MASK_ER`.
    fn vfmsub213ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PS512_MASKZ`.
    fn vfmsub213ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213PS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213PS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213PS512_MASKZ");
        }
    }
    /// Emits `VFMSUB213PS512RRR_MASKZ_ER`.
    fn vfmsub213ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PS512RRB`.
    fn vfmsub213ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PS512RRB_MASK`.
    fn vfmsub213ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213PS512RRB_MASKZ`.
    fn vfmsub213ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213PS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213SD`.
    fn vfmsub213sd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213SDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213SDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213SD");
        }
    }
    /// Emits `VFMSUB213SDRRR_ER`.
    fn vfmsub213sd_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213SDRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213SD_MASK`.
    fn vfmsub213sd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213SDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213SDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213SD_MASK");
        }
    }
    /// Emits `VFMSUB213SDRRR_MASK_ER`.
    fn vfmsub213sd_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213SDRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213SD_MASKZ`.
    fn vfmsub213sd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213SDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213SDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213SD_MASKZ");
        }
    }
    /// Emits `VFMSUB213SDRRR_MASKZ_ER`.
    fn vfmsub213sd_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213SDRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213SS`.
    fn vfmsub213ss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213SSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213SSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213SS");
        }
    }
    /// Emits `VFMSUB213SSRRR_ER`.
    fn vfmsub213ss_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213SSRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213SS_MASK`.
    fn vfmsub213ss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213SSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213SSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213SS_MASK");
        }
    }
    /// Emits `VFMSUB213SSRRR_MASK_ER`.
    fn vfmsub213ss_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213SSRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB213SS_MASKZ`.
    fn vfmsub213ss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB213SSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB213SSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB213SS_MASKZ");
        }
    }
    /// Emits `VFMSUB213SSRRR_MASKZ_ER`.
    fn vfmsub213ss_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB213SSRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PD128`.
    fn vfmsub231pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231PD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231PD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231PD128");
        }
    }
    /// Emits `VFMSUB231PD128_MASK`.
    fn vfmsub231pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231PD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231PD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231PD128_MASK");
        }
    }
    /// Emits `VFMSUB231PD128_MASKZ`.
    fn vfmsub231pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231PD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231PD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231PD128_MASKZ");
        }
    }
    /// Emits `VFMSUB231PD128RRB`.
    fn vfmsub231pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PD128RRB_MASK`.
    fn vfmsub231pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PD128RRB_MASKZ`.
    fn vfmsub231pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PD256`.
    fn vfmsub231pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231PD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231PD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231PD256");
        }
    }
    /// Emits `VFMSUB231PD256_MASK`.
    fn vfmsub231pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231PD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231PD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231PD256_MASK");
        }
    }
    /// Emits `VFMSUB231PD256_MASKZ`.
    fn vfmsub231pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231PD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231PD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231PD256_MASKZ");
        }
    }
    /// Emits `VFMSUB231PD256RRB`.
    fn vfmsub231pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PD256RRB_MASK`.
    fn vfmsub231pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PD256RRB_MASKZ`.
    fn vfmsub231pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PD512`.
    fn vfmsub231pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231PD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231PD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231PD512");
        }
    }
    /// Emits `VFMSUB231PD512RRR_ER`.
    fn vfmsub231pd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PD512_MASK`.
    fn vfmsub231pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231PD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231PD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231PD512_MASK");
        }
    }
    /// Emits `VFMSUB231PD512RRR_MASK_ER`.
    fn vfmsub231pd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PD512_MASKZ`.
    fn vfmsub231pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231PD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231PD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231PD512_MASKZ");
        }
    }
    /// Emits `VFMSUB231PD512RRR_MASKZ_ER`.
    fn vfmsub231pd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PD512RRB`.
    fn vfmsub231pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PD512RRB_MASK`.
    fn vfmsub231pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PD512RRB_MASKZ`.
    fn vfmsub231pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PS128`.
    fn vfmsub231ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231PS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231PS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231PS128");
        }
    }
    /// Emits `VFMSUB231PS128_MASK`.
    fn vfmsub231ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231PS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231PS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231PS128_MASK");
        }
    }
    /// Emits `VFMSUB231PS128_MASKZ`.
    fn vfmsub231ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231PS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231PS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231PS128_MASKZ");
        }
    }
    /// Emits `VFMSUB231PS128RRB`.
    fn vfmsub231ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PS128RRB_MASK`.
    fn vfmsub231ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PS128RRB_MASKZ`.
    fn vfmsub231ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PS256`.
    fn vfmsub231ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231PS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231PS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231PS256");
        }
    }
    /// Emits `VFMSUB231PS256_MASK`.
    fn vfmsub231ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231PS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231PS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231PS256_MASK");
        }
    }
    /// Emits `VFMSUB231PS256_MASKZ`.
    fn vfmsub231ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231PS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231PS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231PS256_MASKZ");
        }
    }
    /// Emits `VFMSUB231PS256RRB`.
    fn vfmsub231ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PS256RRB_MASK`.
    fn vfmsub231ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PS256RRB_MASKZ`.
    fn vfmsub231ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PS512`.
    fn vfmsub231ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231PS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231PS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231PS512");
        }
    }
    /// Emits `VFMSUB231PS512RRR_ER`.
    fn vfmsub231ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PS512_MASK`.
    fn vfmsub231ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231PS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231PS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231PS512_MASK");
        }
    }
    /// Emits `VFMSUB231PS512RRR_MASK_ER`.
    fn vfmsub231ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PS512_MASKZ`.
    fn vfmsub231ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231PS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231PS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231PS512_MASKZ");
        }
    }
    /// Emits `VFMSUB231PS512RRR_MASKZ_ER`.
    fn vfmsub231ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PS512RRB`.
    fn vfmsub231ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PS512RRB_MASK`.
    fn vfmsub231ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231PS512RRB_MASKZ`.
    fn vfmsub231ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231PS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231SD`.
    fn vfmsub231sd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231SDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231SDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231SD");
        }
    }
    /// Emits `VFMSUB231SDRRR_ER`.
    fn vfmsub231sd_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231SDRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231SD_MASK`.
    fn vfmsub231sd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231SDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231SDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231SD_MASK");
        }
    }
    /// Emits `VFMSUB231SDRRR_MASK_ER`.
    fn vfmsub231sd_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231SDRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231SD_MASKZ`.
    fn vfmsub231sd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231SDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231SDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231SD_MASKZ");
        }
    }
    /// Emits `VFMSUB231SDRRR_MASKZ_ER`.
    fn vfmsub231sd_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231SDRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231SS`.
    fn vfmsub231ss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231SSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231SSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231SS");
        }
    }
    /// Emits `VFMSUB231SSRRR_ER`.
    fn vfmsub231ss_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231SSRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231SS_MASK`.
    fn vfmsub231ss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231SSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231SSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231SS_MASK");
        }
    }
    /// Emits `VFMSUB231SSRRR_MASK_ER`.
    fn vfmsub231ss_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231SSRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUB231SS_MASKZ`.
    fn vfmsub231ss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUB231SSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUB231SSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUB231SS_MASKZ");
        }
    }
    /// Emits `VFMSUB231SSRRR_MASKZ_ER`.
    fn vfmsub231ss_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUB231SSRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PD128`.
    fn vfmsubadd132pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD132PD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD132PD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD132PD128");
        }
    }
    /// Emits `VFMSUBADD132PD128_MASK`.
    fn vfmsubadd132pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD132PD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD132PD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD132PD128_MASK");
        }
    }
    /// Emits `VFMSUBADD132PD128_MASKZ`.
    fn vfmsubadd132pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD132PD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD132PD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD132PD128_MASKZ");
        }
    }
    /// Emits `VFMSUBADD132PD128RRB`.
    fn vfmsubadd132pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PD128RRB_MASK`.
    fn vfmsubadd132pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PD128RRB_MASKZ`.
    fn vfmsubadd132pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PD256`.
    fn vfmsubadd132pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD132PD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD132PD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD132PD256");
        }
    }
    /// Emits `VFMSUBADD132PD256_MASK`.
    fn vfmsubadd132pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD132PD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD132PD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD132PD256_MASK");
        }
    }
    /// Emits `VFMSUBADD132PD256_MASKZ`.
    fn vfmsubadd132pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD132PD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD132PD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD132PD256_MASKZ");
        }
    }
    /// Emits `VFMSUBADD132PD256RRB`.
    fn vfmsubadd132pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PD256RRB_MASK`.
    fn vfmsubadd132pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PD256RRB_MASKZ`.
    fn vfmsubadd132pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PD512`.
    fn vfmsubadd132pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD132PD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD132PD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD132PD512");
        }
    }
    /// Emits `VFMSUBADD132PD512RRR_ER`.
    fn vfmsubadd132pd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PD512_MASK`.
    fn vfmsubadd132pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD132PD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD132PD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD132PD512_MASK");
        }
    }
    /// Emits `VFMSUBADD132PD512RRR_MASK_ER`.
    fn vfmsubadd132pd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PD512_MASKZ`.
    fn vfmsubadd132pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD132PD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD132PD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD132PD512_MASKZ");
        }
    }
    /// Emits `VFMSUBADD132PD512RRR_MASKZ_ER`.
    fn vfmsubadd132pd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PD512RRB`.
    fn vfmsubadd132pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PD512RRB_MASK`.
    fn vfmsubadd132pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PD512RRB_MASKZ`.
    fn vfmsubadd132pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PS128`.
    fn vfmsubadd132ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD132PS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD132PS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD132PS128");
        }
    }
    /// Emits `VFMSUBADD132PS128_MASK`.
    fn vfmsubadd132ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD132PS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD132PS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD132PS128_MASK");
        }
    }
    /// Emits `VFMSUBADD132PS128_MASKZ`.
    fn vfmsubadd132ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD132PS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD132PS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD132PS128_MASKZ");
        }
    }
    /// Emits `VFMSUBADD132PS128RRB`.
    fn vfmsubadd132ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PS128RRB_MASK`.
    fn vfmsubadd132ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PS128RRB_MASKZ`.
    fn vfmsubadd132ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PS256`.
    fn vfmsubadd132ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD132PS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD132PS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD132PS256");
        }
    }
    /// Emits `VFMSUBADD132PS256_MASK`.
    fn vfmsubadd132ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD132PS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD132PS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD132PS256_MASK");
        }
    }
    /// Emits `VFMSUBADD132PS256_MASKZ`.
    fn vfmsubadd132ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD132PS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD132PS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD132PS256_MASKZ");
        }
    }
    /// Emits `VFMSUBADD132PS256RRB`.
    fn vfmsubadd132ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PS256RRB_MASK`.
    fn vfmsubadd132ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PS256RRB_MASKZ`.
    fn vfmsubadd132ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PS512`.
    fn vfmsubadd132ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD132PS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD132PS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD132PS512");
        }
    }
    /// Emits `VFMSUBADD132PS512RRR_ER`.
    fn vfmsubadd132ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PS512_MASK`.
    fn vfmsubadd132ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD132PS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD132PS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD132PS512_MASK");
        }
    }
    /// Emits `VFMSUBADD132PS512RRR_MASK_ER`.
    fn vfmsubadd132ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PS512_MASKZ`.
    fn vfmsubadd132ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD132PS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD132PS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD132PS512_MASKZ");
        }
    }
    /// Emits `VFMSUBADD132PS512RRR_MASKZ_ER`.
    fn vfmsubadd132ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PS512RRB`.
    fn vfmsubadd132ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PS512RRB_MASK`.
    fn vfmsubadd132ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD132PS512RRB_MASKZ`.
    fn vfmsubadd132ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD132PS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PD128`.
    fn vfmsubadd213pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD213PD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD213PD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD213PD128");
        }
    }
    /// Emits `VFMSUBADD213PD128_MASK`.
    fn vfmsubadd213pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD213PD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD213PD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD213PD128_MASK");
        }
    }
    /// Emits `VFMSUBADD213PD128_MASKZ`.
    fn vfmsubadd213pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD213PD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD213PD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD213PD128_MASKZ");
        }
    }
    /// Emits `VFMSUBADD213PD128RRB`.
    fn vfmsubadd213pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PD128RRB_MASK`.
    fn vfmsubadd213pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PD128RRB_MASKZ`.
    fn vfmsubadd213pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PD256`.
    fn vfmsubadd213pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD213PD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD213PD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD213PD256");
        }
    }
    /// Emits `VFMSUBADD213PD256_MASK`.
    fn vfmsubadd213pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD213PD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD213PD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD213PD256_MASK");
        }
    }
    /// Emits `VFMSUBADD213PD256_MASKZ`.
    fn vfmsubadd213pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD213PD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD213PD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD213PD256_MASKZ");
        }
    }
    /// Emits `VFMSUBADD213PD256RRB`.
    fn vfmsubadd213pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PD256RRB_MASK`.
    fn vfmsubadd213pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PD256RRB_MASKZ`.
    fn vfmsubadd213pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PD512`.
    fn vfmsubadd213pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD213PD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD213PD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD213PD512");
        }
    }
    /// Emits `VFMSUBADD213PD512RRR_ER`.
    fn vfmsubadd213pd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PD512_MASK`.
    fn vfmsubadd213pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD213PD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD213PD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD213PD512_MASK");
        }
    }
    /// Emits `VFMSUBADD213PD512RRR_MASK_ER`.
    fn vfmsubadd213pd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PD512_MASKZ`.
    fn vfmsubadd213pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD213PD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD213PD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD213PD512_MASKZ");
        }
    }
    /// Emits `VFMSUBADD213PD512RRR_MASKZ_ER`.
    fn vfmsubadd213pd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PD512RRB`.
    fn vfmsubadd213pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PD512RRB_MASK`.
    fn vfmsubadd213pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PD512RRB_MASKZ`.
    fn vfmsubadd213pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PS128`.
    fn vfmsubadd213ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD213PS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD213PS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD213PS128");
        }
    }
    /// Emits `VFMSUBADD213PS128_MASK`.
    fn vfmsubadd213ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD213PS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD213PS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD213PS128_MASK");
        }
    }
    /// Emits `VFMSUBADD213PS128_MASKZ`.
    fn vfmsubadd213ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD213PS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD213PS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD213PS128_MASKZ");
        }
    }
    /// Emits `VFMSUBADD213PS128RRB`.
    fn vfmsubadd213ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PS128RRB_MASK`.
    fn vfmsubadd213ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PS128RRB_MASKZ`.
    fn vfmsubadd213ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PS256`.
    fn vfmsubadd213ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD213PS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD213PS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD213PS256");
        }
    }
    /// Emits `VFMSUBADD213PS256_MASK`.
    fn vfmsubadd213ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD213PS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD213PS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD213PS256_MASK");
        }
    }
    /// Emits `VFMSUBADD213PS256_MASKZ`.
    fn vfmsubadd213ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD213PS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD213PS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD213PS256_MASKZ");
        }
    }
    /// Emits `VFMSUBADD213PS256RRB`.
    fn vfmsubadd213ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PS256RRB_MASK`.
    fn vfmsubadd213ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PS256RRB_MASKZ`.
    fn vfmsubadd213ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PS512`.
    fn vfmsubadd213ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD213PS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD213PS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD213PS512");
        }
    }
    /// Emits `VFMSUBADD213PS512RRR_ER`.
    fn vfmsubadd213ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PS512_MASK`.
    fn vfmsubadd213ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD213PS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD213PS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD213PS512_MASK");
        }
    }
    /// Emits `VFMSUBADD213PS512RRR_MASK_ER`.
    fn vfmsubadd213ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PS512_MASKZ`.
    fn vfmsubadd213ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD213PS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD213PS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD213PS512_MASKZ");
        }
    }
    /// Emits `VFMSUBADD213PS512RRR_MASKZ_ER`.
    fn vfmsubadd213ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PS512RRB`.
    fn vfmsubadd213ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PS512RRB_MASK`.
    fn vfmsubadd213ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD213PS512RRB_MASKZ`.
    fn vfmsubadd213ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD213PS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PD128`.
    fn vfmsubadd231pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD231PD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD231PD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD231PD128");
        }
    }
    /// Emits `VFMSUBADD231PD128_MASK`.
    fn vfmsubadd231pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD231PD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD231PD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD231PD128_MASK");
        }
    }
    /// Emits `VFMSUBADD231PD128_MASKZ`.
    fn vfmsubadd231pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD231PD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD231PD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD231PD128_MASKZ");
        }
    }
    /// Emits `VFMSUBADD231PD128RRB`.
    fn vfmsubadd231pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PD128RRB_MASK`.
    fn vfmsubadd231pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PD128RRB_MASKZ`.
    fn vfmsubadd231pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PD256`.
    fn vfmsubadd231pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD231PD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD231PD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD231PD256");
        }
    }
    /// Emits `VFMSUBADD231PD256_MASK`.
    fn vfmsubadd231pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD231PD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD231PD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD231PD256_MASK");
        }
    }
    /// Emits `VFMSUBADD231PD256_MASKZ`.
    fn vfmsubadd231pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD231PD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD231PD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD231PD256_MASKZ");
        }
    }
    /// Emits `VFMSUBADD231PD256RRB`.
    fn vfmsubadd231pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PD256RRB_MASK`.
    fn vfmsubadd231pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PD256RRB_MASKZ`.
    fn vfmsubadd231pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PD512`.
    fn vfmsubadd231pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD231PD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD231PD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD231PD512");
        }
    }
    /// Emits `VFMSUBADD231PD512RRR_ER`.
    fn vfmsubadd231pd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PD512_MASK`.
    fn vfmsubadd231pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD231PD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD231PD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD231PD512_MASK");
        }
    }
    /// Emits `VFMSUBADD231PD512RRR_MASK_ER`.
    fn vfmsubadd231pd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PD512_MASKZ`.
    fn vfmsubadd231pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD231PD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD231PD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD231PD512_MASKZ");
        }
    }
    /// Emits `VFMSUBADD231PD512RRR_MASKZ_ER`.
    fn vfmsubadd231pd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PD512RRB`.
    fn vfmsubadd231pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PD512RRB_MASK`.
    fn vfmsubadd231pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PD512RRB_MASKZ`.
    fn vfmsubadd231pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PS128`.
    fn vfmsubadd231ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD231PS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD231PS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD231PS128");
        }
    }
    /// Emits `VFMSUBADD231PS128_MASK`.
    fn vfmsubadd231ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD231PS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD231PS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD231PS128_MASK");
        }
    }
    /// Emits `VFMSUBADD231PS128_MASKZ`.
    fn vfmsubadd231ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD231PS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD231PS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD231PS128_MASKZ");
        }
    }
    /// Emits `VFMSUBADD231PS128RRB`.
    fn vfmsubadd231ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PS128RRB_MASK`.
    fn vfmsubadd231ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PS128RRB_MASKZ`.
    fn vfmsubadd231ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PS256`.
    fn vfmsubadd231ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD231PS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD231PS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD231PS256");
        }
    }
    /// Emits `VFMSUBADD231PS256_MASK`.
    fn vfmsubadd231ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD231PS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD231PS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD231PS256_MASK");
        }
    }
    /// Emits `VFMSUBADD231PS256_MASKZ`.
    fn vfmsubadd231ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD231PS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD231PS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD231PS256_MASKZ");
        }
    }
    /// Emits `VFMSUBADD231PS256RRB`.
    fn vfmsubadd231ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PS256RRB_MASK`.
    fn vfmsubadd231ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PS256RRB_MASKZ`.
    fn vfmsubadd231ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PS512`.
    fn vfmsubadd231ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD231PS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD231PS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD231PS512");
        }
    }
    /// Emits `VFMSUBADD231PS512RRR_ER`.
    fn vfmsubadd231ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PS512_MASK`.
    fn vfmsubadd231ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD231PS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD231PS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD231PS512_MASK");
        }
    }
    /// Emits `VFMSUBADD231PS512RRR_MASK_ER`.
    fn vfmsubadd231ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PS512_MASKZ`.
    fn vfmsubadd231ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFMSUBADD231PS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFMSUBADD231PS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFMSUBADD231PS512_MASKZ");
        }
    }
    /// Emits `VFMSUBADD231PS512RRR_MASKZ_ER`.
    fn vfmsubadd231ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PS512RRB`.
    fn vfmsubadd231ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PS512RRB_MASK`.
    fn vfmsubadd231ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFMSUBADD231PS512RRB_MASKZ`.
    fn vfmsubadd231ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFMSUBADD231PS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PD128`.
    fn vfnmadd132pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132PD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132PD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132PD128");
        }
    }
    /// Emits `VFNMADD132PD128_MASK`.
    fn vfnmadd132pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132PD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132PD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132PD128_MASK");
        }
    }
    /// Emits `VFNMADD132PD128_MASKZ`.
    fn vfnmadd132pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132PD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132PD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132PD128_MASKZ");
        }
    }
    /// Emits `VFNMADD132PD128RRB`.
    fn vfnmadd132pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PD128RRB_MASK`.
    fn vfnmadd132pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PD128RRB_MASKZ`.
    fn vfnmadd132pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PD256`.
    fn vfnmadd132pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132PD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132PD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132PD256");
        }
    }
    /// Emits `VFNMADD132PD256_MASK`.
    fn vfnmadd132pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132PD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132PD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132PD256_MASK");
        }
    }
    /// Emits `VFNMADD132PD256_MASKZ`.
    fn vfnmadd132pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132PD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132PD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132PD256_MASKZ");
        }
    }
    /// Emits `VFNMADD132PD256RRB`.
    fn vfnmadd132pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PD256RRB_MASK`.
    fn vfnmadd132pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PD256RRB_MASKZ`.
    fn vfnmadd132pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PD512`.
    fn vfnmadd132pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132PD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132PD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132PD512");
        }
    }
    /// Emits `VFNMADD132PD512RRR_ER`.
    fn vfnmadd132pd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PD512_MASK`.
    fn vfnmadd132pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132PD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132PD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132PD512_MASK");
        }
    }
    /// Emits `VFNMADD132PD512RRR_MASK_ER`.
    fn vfnmadd132pd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PD512_MASKZ`.
    fn vfnmadd132pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132PD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132PD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132PD512_MASKZ");
        }
    }
    /// Emits `VFNMADD132PD512RRR_MASKZ_ER`.
    fn vfnmadd132pd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PD512RRB`.
    fn vfnmadd132pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PD512RRB_MASK`.
    fn vfnmadd132pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PD512RRB_MASKZ`.
    fn vfnmadd132pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PS128`.
    fn vfnmadd132ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132PS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132PS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132PS128");
        }
    }
    /// Emits `VFNMADD132PS128_MASK`.
    fn vfnmadd132ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132PS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132PS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132PS128_MASK");
        }
    }
    /// Emits `VFNMADD132PS128_MASKZ`.
    fn vfnmadd132ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132PS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132PS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132PS128_MASKZ");
        }
    }
    /// Emits `VFNMADD132PS128RRB`.
    fn vfnmadd132ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PS128RRB_MASK`.
    fn vfnmadd132ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PS128RRB_MASKZ`.
    fn vfnmadd132ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PS256`.
    fn vfnmadd132ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132PS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132PS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132PS256");
        }
    }
    /// Emits `VFNMADD132PS256_MASK`.
    fn vfnmadd132ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132PS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132PS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132PS256_MASK");
        }
    }
    /// Emits `VFNMADD132PS256_MASKZ`.
    fn vfnmadd132ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132PS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132PS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132PS256_MASKZ");
        }
    }
    /// Emits `VFNMADD132PS256RRB`.
    fn vfnmadd132ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PS256RRB_MASK`.
    fn vfnmadd132ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PS256RRB_MASKZ`.
    fn vfnmadd132ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PS512`.
    fn vfnmadd132ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132PS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132PS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132PS512");
        }
    }
    /// Emits `VFNMADD132PS512RRR_ER`.
    fn vfnmadd132ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PS512_MASK`.
    fn vfnmadd132ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132PS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132PS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132PS512_MASK");
        }
    }
    /// Emits `VFNMADD132PS512RRR_MASK_ER`.
    fn vfnmadd132ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PS512_MASKZ`.
    fn vfnmadd132ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132PS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132PS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132PS512_MASKZ");
        }
    }
    /// Emits `VFNMADD132PS512RRR_MASKZ_ER`.
    fn vfnmadd132ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PS512RRB`.
    fn vfnmadd132ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PS512RRB_MASK`.
    fn vfnmadd132ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132PS512RRB_MASKZ`.
    fn vfnmadd132ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132PS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132SD`.
    fn vfnmadd132sd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132SDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132SDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132SD");
        }
    }
    /// Emits `VFNMADD132SDRRR_ER`.
    fn vfnmadd132sd_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132SDRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132SD_MASK`.
    fn vfnmadd132sd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132SDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132SDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132SD_MASK");
        }
    }
    /// Emits `VFNMADD132SDRRR_MASK_ER`.
    fn vfnmadd132sd_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132SDRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132SD_MASKZ`.
    fn vfnmadd132sd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132SDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132SDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132SD_MASKZ");
        }
    }
    /// Emits `VFNMADD132SDRRR_MASKZ_ER`.
    fn vfnmadd132sd_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132SDRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132SS`.
    fn vfnmadd132ss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132SSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132SSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132SS");
        }
    }
    /// Emits `VFNMADD132SSRRR_ER`.
    fn vfnmadd132ss_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132SSRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132SS_MASK`.
    fn vfnmadd132ss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132SSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132SSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132SS_MASK");
        }
    }
    /// Emits `VFNMADD132SSRRR_MASK_ER`.
    fn vfnmadd132ss_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132SSRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD132SS_MASKZ`.
    fn vfnmadd132ss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD132SSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD132SSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD132SS_MASKZ");
        }
    }
    /// Emits `VFNMADD132SSRRR_MASKZ_ER`.
    fn vfnmadd132ss_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD132SSRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PD128`.
    fn vfnmadd213pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213PD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213PD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213PD128");
        }
    }
    /// Emits `VFNMADD213PD128_MASK`.
    fn vfnmadd213pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213PD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213PD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213PD128_MASK");
        }
    }
    /// Emits `VFNMADD213PD128_MASKZ`.
    fn vfnmadd213pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213PD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213PD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213PD128_MASKZ");
        }
    }
    /// Emits `VFNMADD213PD128RRB`.
    fn vfnmadd213pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PD128RRB_MASK`.
    fn vfnmadd213pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PD128RRB_MASKZ`.
    fn vfnmadd213pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PD256`.
    fn vfnmadd213pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213PD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213PD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213PD256");
        }
    }
    /// Emits `VFNMADD213PD256_MASK`.
    fn vfnmadd213pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213PD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213PD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213PD256_MASK");
        }
    }
    /// Emits `VFNMADD213PD256_MASKZ`.
    fn vfnmadd213pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213PD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213PD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213PD256_MASKZ");
        }
    }
    /// Emits `VFNMADD213PD256RRB`.
    fn vfnmadd213pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PD256RRB_MASK`.
    fn vfnmadd213pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PD256RRB_MASKZ`.
    fn vfnmadd213pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PD512`.
    fn vfnmadd213pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213PD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213PD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213PD512");
        }
    }
    /// Emits `VFNMADD213PD512RRR_ER`.
    fn vfnmadd213pd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PD512_MASK`.
    fn vfnmadd213pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213PD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213PD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213PD512_MASK");
        }
    }
    /// Emits `VFNMADD213PD512RRR_MASK_ER`.
    fn vfnmadd213pd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PD512_MASKZ`.
    fn vfnmadd213pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213PD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213PD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213PD512_MASKZ");
        }
    }
    /// Emits `VFNMADD213PD512RRR_MASKZ_ER`.
    fn vfnmadd213pd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PD512RRB`.
    fn vfnmadd213pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PD512RRB_MASK`.
    fn vfnmadd213pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PD512RRB_MASKZ`.
    fn vfnmadd213pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PS128`.
    fn vfnmadd213ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213PS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213PS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213PS128");
        }
    }
    /// Emits `VFNMADD213PS128_MASK`.
    fn vfnmadd213ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213PS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213PS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213PS128_MASK");
        }
    }
    /// Emits `VFNMADD213PS128_MASKZ`.
    fn vfnmadd213ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213PS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213PS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213PS128_MASKZ");
        }
    }
    /// Emits `VFNMADD213PS128RRB`.
    fn vfnmadd213ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PS128RRB_MASK`.
    fn vfnmadd213ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PS128RRB_MASKZ`.
    fn vfnmadd213ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PS256`.
    fn vfnmadd213ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213PS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213PS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213PS256");
        }
    }
    /// Emits `VFNMADD213PS256_MASK`.
    fn vfnmadd213ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213PS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213PS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213PS256_MASK");
        }
    }
    /// Emits `VFNMADD213PS256_MASKZ`.
    fn vfnmadd213ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213PS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213PS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213PS256_MASKZ");
        }
    }
    /// Emits `VFNMADD213PS256RRB`.
    fn vfnmadd213ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PS256RRB_MASK`.
    fn vfnmadd213ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PS256RRB_MASKZ`.
    fn vfnmadd213ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PS512`.
    fn vfnmadd213ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213PS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213PS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213PS512");
        }
    }
    /// Emits `VFNMADD213PS512RRR_ER`.
    fn vfnmadd213ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PS512_MASK`.
    fn vfnmadd213ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213PS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213PS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213PS512_MASK");
        }
    }
    /// Emits `VFNMADD213PS512RRR_MASK_ER`.
    fn vfnmadd213ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PS512_MASKZ`.
    fn vfnmadd213ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213PS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213PS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213PS512_MASKZ");
        }
    }
    /// Emits `VFNMADD213PS512RRR_MASKZ_ER`.
    fn vfnmadd213ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PS512RRB`.
    fn vfnmadd213ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PS512RRB_MASK`.
    fn vfnmadd213ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213PS512RRB_MASKZ`.
    fn vfnmadd213ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213PS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213SD`.
    fn vfnmadd213sd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213SDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213SDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213SD");
        }
    }
    /// Emits `VFNMADD213SDRRR_ER`.
    fn vfnmadd213sd_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213SDRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213SD_MASK`.
    fn vfnmadd213sd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213SDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213SDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213SD_MASK");
        }
    }
    /// Emits `VFNMADD213SDRRR_MASK_ER`.
    fn vfnmadd213sd_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213SDRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213SD_MASKZ`.
    fn vfnmadd213sd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213SDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213SDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213SD_MASKZ");
        }
    }
    /// Emits `VFNMADD213SDRRR_MASKZ_ER`.
    fn vfnmadd213sd_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213SDRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213SS`.
    fn vfnmadd213ss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213SSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213SSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213SS");
        }
    }
    /// Emits `VFNMADD213SSRRR_ER`.
    fn vfnmadd213ss_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213SSRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213SS_MASK`.
    fn vfnmadd213ss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213SSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213SSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213SS_MASK");
        }
    }
    /// Emits `VFNMADD213SSRRR_MASK_ER`.
    fn vfnmadd213ss_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213SSRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD213SS_MASKZ`.
    fn vfnmadd213ss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD213SSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD213SSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD213SS_MASKZ");
        }
    }
    /// Emits `VFNMADD213SSRRR_MASKZ_ER`.
    fn vfnmadd213ss_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD213SSRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PD128`.
    fn vfnmadd231pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231PD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231PD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231PD128");
        }
    }
    /// Emits `VFNMADD231PD128_MASK`.
    fn vfnmadd231pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231PD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231PD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231PD128_MASK");
        }
    }
    /// Emits `VFNMADD231PD128_MASKZ`.
    fn vfnmadd231pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231PD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231PD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231PD128_MASKZ");
        }
    }
    /// Emits `VFNMADD231PD128RRB`.
    fn vfnmadd231pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PD128RRB_MASK`.
    fn vfnmadd231pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PD128RRB_MASKZ`.
    fn vfnmadd231pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PD256`.
    fn vfnmadd231pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231PD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231PD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231PD256");
        }
    }
    /// Emits `VFNMADD231PD256_MASK`.
    fn vfnmadd231pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231PD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231PD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231PD256_MASK");
        }
    }
    /// Emits `VFNMADD231PD256_MASKZ`.
    fn vfnmadd231pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231PD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231PD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231PD256_MASKZ");
        }
    }
    /// Emits `VFNMADD231PD256RRB`.
    fn vfnmadd231pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PD256RRB_MASK`.
    fn vfnmadd231pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PD256RRB_MASKZ`.
    fn vfnmadd231pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PD512`.
    fn vfnmadd231pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231PD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231PD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231PD512");
        }
    }
    /// Emits `VFNMADD231PD512RRR_ER`.
    fn vfnmadd231pd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PD512_MASK`.
    fn vfnmadd231pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231PD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231PD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231PD512_MASK");
        }
    }
    /// Emits `VFNMADD231PD512RRR_MASK_ER`.
    fn vfnmadd231pd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PD512_MASKZ`.
    fn vfnmadd231pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231PD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231PD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231PD512_MASKZ");
        }
    }
    /// Emits `VFNMADD231PD512RRR_MASKZ_ER`.
    fn vfnmadd231pd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PD512RRB`.
    fn vfnmadd231pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PD512RRB_MASK`.
    fn vfnmadd231pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PD512RRB_MASKZ`.
    fn vfnmadd231pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PS128`.
    fn vfnmadd231ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231PS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231PS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231PS128");
        }
    }
    /// Emits `VFNMADD231PS128_MASK`.
    fn vfnmadd231ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231PS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231PS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231PS128_MASK");
        }
    }
    /// Emits `VFNMADD231PS128_MASKZ`.
    fn vfnmadd231ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231PS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231PS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231PS128_MASKZ");
        }
    }
    /// Emits `VFNMADD231PS128RRB`.
    fn vfnmadd231ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PS128RRB_MASK`.
    fn vfnmadd231ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PS128RRB_MASKZ`.
    fn vfnmadd231ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PS256`.
    fn vfnmadd231ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231PS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231PS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231PS256");
        }
    }
    /// Emits `VFNMADD231PS256_MASK`.
    fn vfnmadd231ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231PS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231PS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231PS256_MASK");
        }
    }
    /// Emits `VFNMADD231PS256_MASKZ`.
    fn vfnmadd231ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231PS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231PS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231PS256_MASKZ");
        }
    }
    /// Emits `VFNMADD231PS256RRB`.
    fn vfnmadd231ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PS256RRB_MASK`.
    fn vfnmadd231ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PS256RRB_MASKZ`.
    fn vfnmadd231ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PS512`.
    fn vfnmadd231ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231PS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231PS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231PS512");
        }
    }
    /// Emits `VFNMADD231PS512RRR_ER`.
    fn vfnmadd231ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PS512_MASK`.
    fn vfnmadd231ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231PS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231PS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231PS512_MASK");
        }
    }
    /// Emits `VFNMADD231PS512RRR_MASK_ER`.
    fn vfnmadd231ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PS512_MASKZ`.
    fn vfnmadd231ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231PS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231PS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231PS512_MASKZ");
        }
    }
    /// Emits `VFNMADD231PS512RRR_MASKZ_ER`.
    fn vfnmadd231ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PS512RRB`.
    fn vfnmadd231ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PS512RRB_MASK`.
    fn vfnmadd231ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231PS512RRB_MASKZ`.
    fn vfnmadd231ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231PS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231SD`.
    fn vfnmadd231sd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231SDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231SDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231SD");
        }
    }
    /// Emits `VFNMADD231SDRRR_ER`.
    fn vfnmadd231sd_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231SDRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231SD_MASK`.
    fn vfnmadd231sd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231SDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231SDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231SD_MASK");
        }
    }
    /// Emits `VFNMADD231SDRRR_MASK_ER`.
    fn vfnmadd231sd_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231SDRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231SD_MASKZ`.
    fn vfnmadd231sd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231SDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231SDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231SD_MASKZ");
        }
    }
    /// Emits `VFNMADD231SDRRR_MASKZ_ER`.
    fn vfnmadd231sd_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231SDRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231SS`.
    fn vfnmadd231ss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231SSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231SSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231SS");
        }
    }
    /// Emits `VFNMADD231SSRRR_ER`.
    fn vfnmadd231ss_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231SSRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231SS_MASK`.
    fn vfnmadd231ss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231SSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231SSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231SS_MASK");
        }
    }
    /// Emits `VFNMADD231SSRRR_MASK_ER`.
    fn vfnmadd231ss_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231SSRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMADD231SS_MASKZ`.
    fn vfnmadd231ss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMADD231SSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMADD231SSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMADD231SS_MASKZ");
        }
    }
    /// Emits `VFNMADD231SSRRR_MASKZ_ER`.
    fn vfnmadd231ss_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMADD231SSRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PD128`.
    fn vfnmsub132pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132PD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132PD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132PD128");
        }
    }
    /// Emits `VFNMSUB132PD128_MASK`.
    fn vfnmsub132pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132PD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132PD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132PD128_MASK");
        }
    }
    /// Emits `VFNMSUB132PD128_MASKZ`.
    fn vfnmsub132pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132PD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132PD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132PD128_MASKZ");
        }
    }
    /// Emits `VFNMSUB132PD128RRB`.
    fn vfnmsub132pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PD128RRB_MASK`.
    fn vfnmsub132pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PD128RRB_MASKZ`.
    fn vfnmsub132pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PD256`.
    fn vfnmsub132pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132PD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132PD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132PD256");
        }
    }
    /// Emits `VFNMSUB132PD256_MASK`.
    fn vfnmsub132pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132PD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132PD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132PD256_MASK");
        }
    }
    /// Emits `VFNMSUB132PD256_MASKZ`.
    fn vfnmsub132pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132PD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132PD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132PD256_MASKZ");
        }
    }
    /// Emits `VFNMSUB132PD256RRB`.
    fn vfnmsub132pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PD256RRB_MASK`.
    fn vfnmsub132pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PD256RRB_MASKZ`.
    fn vfnmsub132pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PD512`.
    fn vfnmsub132pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132PD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132PD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132PD512");
        }
    }
    /// Emits `VFNMSUB132PD512RRR_ER`.
    fn vfnmsub132pd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PD512_MASK`.
    fn vfnmsub132pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132PD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132PD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132PD512_MASK");
        }
    }
    /// Emits `VFNMSUB132PD512RRR_MASK_ER`.
    fn vfnmsub132pd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PD512_MASKZ`.
    fn vfnmsub132pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132PD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132PD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132PD512_MASKZ");
        }
    }
    /// Emits `VFNMSUB132PD512RRR_MASKZ_ER`.
    fn vfnmsub132pd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PD512RRB`.
    fn vfnmsub132pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PD512RRB_MASK`.
    fn vfnmsub132pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PD512RRB_MASKZ`.
    fn vfnmsub132pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PS128`.
    fn vfnmsub132ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132PS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132PS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132PS128");
        }
    }
    /// Emits `VFNMSUB132PS128_MASK`.
    fn vfnmsub132ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132PS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132PS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132PS128_MASK");
        }
    }
    /// Emits `VFNMSUB132PS128_MASKZ`.
    fn vfnmsub132ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132PS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132PS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132PS128_MASKZ");
        }
    }
    /// Emits `VFNMSUB132PS128RRB`.
    fn vfnmsub132ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PS128RRB_MASK`.
    fn vfnmsub132ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PS128RRB_MASKZ`.
    fn vfnmsub132ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PS256`.
    fn vfnmsub132ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132PS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132PS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132PS256");
        }
    }
    /// Emits `VFNMSUB132PS256_MASK`.
    fn vfnmsub132ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132PS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132PS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132PS256_MASK");
        }
    }
    /// Emits `VFNMSUB132PS256_MASKZ`.
    fn vfnmsub132ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132PS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132PS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132PS256_MASKZ");
        }
    }
    /// Emits `VFNMSUB132PS256RRB`.
    fn vfnmsub132ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PS256RRB_MASK`.
    fn vfnmsub132ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PS256RRB_MASKZ`.
    fn vfnmsub132ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PS512`.
    fn vfnmsub132ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132PS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132PS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132PS512");
        }
    }
    /// Emits `VFNMSUB132PS512RRR_ER`.
    fn vfnmsub132ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PS512_MASK`.
    fn vfnmsub132ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132PS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132PS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132PS512_MASK");
        }
    }
    /// Emits `VFNMSUB132PS512RRR_MASK_ER`.
    fn vfnmsub132ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PS512_MASKZ`.
    fn vfnmsub132ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132PS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132PS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132PS512_MASKZ");
        }
    }
    /// Emits `VFNMSUB132PS512RRR_MASKZ_ER`.
    fn vfnmsub132ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PS512RRB`.
    fn vfnmsub132ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PS512RRB_MASK`.
    fn vfnmsub132ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132PS512RRB_MASKZ`.
    fn vfnmsub132ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132PS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132SD`.
    fn vfnmsub132sd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132SDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132SDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132SD");
        }
    }
    /// Emits `VFNMSUB132SDRRR_ER`.
    fn vfnmsub132sd_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132SDRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132SD_MASK`.
    fn vfnmsub132sd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132SDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132SDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132SD_MASK");
        }
    }
    /// Emits `VFNMSUB132SDRRR_MASK_ER`.
    fn vfnmsub132sd_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132SDRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132SD_MASKZ`.
    fn vfnmsub132sd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132SDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132SDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132SD_MASKZ");
        }
    }
    /// Emits `VFNMSUB132SDRRR_MASKZ_ER`.
    fn vfnmsub132sd_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132SDRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132SS`.
    fn vfnmsub132ss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132SSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132SSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132SS");
        }
    }
    /// Emits `VFNMSUB132SSRRR_ER`.
    fn vfnmsub132ss_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132SSRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132SS_MASK`.
    fn vfnmsub132ss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132SSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132SSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132SS_MASK");
        }
    }
    /// Emits `VFNMSUB132SSRRR_MASK_ER`.
    fn vfnmsub132ss_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132SSRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB132SS_MASKZ`.
    fn vfnmsub132ss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB132SSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB132SSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB132SS_MASKZ");
        }
    }
    /// Emits `VFNMSUB132SSRRR_MASKZ_ER`.
    fn vfnmsub132ss_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB132SSRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PD128`.
    fn vfnmsub213pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213PD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213PD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213PD128");
        }
    }
    /// Emits `VFNMSUB213PD128_MASK`.
    fn vfnmsub213pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213PD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213PD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213PD128_MASK");
        }
    }
    /// Emits `VFNMSUB213PD128_MASKZ`.
    fn vfnmsub213pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213PD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213PD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213PD128_MASKZ");
        }
    }
    /// Emits `VFNMSUB213PD128RRB`.
    fn vfnmsub213pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PD128RRB_MASK`.
    fn vfnmsub213pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PD128RRB_MASKZ`.
    fn vfnmsub213pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PD256`.
    fn vfnmsub213pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213PD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213PD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213PD256");
        }
    }
    /// Emits `VFNMSUB213PD256_MASK`.
    fn vfnmsub213pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213PD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213PD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213PD256_MASK");
        }
    }
    /// Emits `VFNMSUB213PD256_MASKZ`.
    fn vfnmsub213pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213PD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213PD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213PD256_MASKZ");
        }
    }
    /// Emits `VFNMSUB213PD256RRB`.
    fn vfnmsub213pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PD256RRB_MASK`.
    fn vfnmsub213pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PD256RRB_MASKZ`.
    fn vfnmsub213pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PD512`.
    fn vfnmsub213pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213PD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213PD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213PD512");
        }
    }
    /// Emits `VFNMSUB213PD512RRR_ER`.
    fn vfnmsub213pd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PD512_MASK`.
    fn vfnmsub213pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213PD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213PD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213PD512_MASK");
        }
    }
    /// Emits `VFNMSUB213PD512RRR_MASK_ER`.
    fn vfnmsub213pd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PD512_MASKZ`.
    fn vfnmsub213pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213PD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213PD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213PD512_MASKZ");
        }
    }
    /// Emits `VFNMSUB213PD512RRR_MASKZ_ER`.
    fn vfnmsub213pd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PD512RRB`.
    fn vfnmsub213pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PD512RRB_MASK`.
    fn vfnmsub213pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PD512RRB_MASKZ`.
    fn vfnmsub213pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PS128`.
    fn vfnmsub213ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213PS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213PS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213PS128");
        }
    }
    /// Emits `VFNMSUB213PS128_MASK`.
    fn vfnmsub213ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213PS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213PS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213PS128_MASK");
        }
    }
    /// Emits `VFNMSUB213PS128_MASKZ`.
    fn vfnmsub213ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213PS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213PS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213PS128_MASKZ");
        }
    }
    /// Emits `VFNMSUB213PS128RRB`.
    fn vfnmsub213ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PS128RRB_MASK`.
    fn vfnmsub213ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PS128RRB_MASKZ`.
    fn vfnmsub213ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PS256`.
    fn vfnmsub213ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213PS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213PS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213PS256");
        }
    }
    /// Emits `VFNMSUB213PS256_MASK`.
    fn vfnmsub213ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213PS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213PS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213PS256_MASK");
        }
    }
    /// Emits `VFNMSUB213PS256_MASKZ`.
    fn vfnmsub213ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213PS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213PS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213PS256_MASKZ");
        }
    }
    /// Emits `VFNMSUB213PS256RRB`.
    fn vfnmsub213ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PS256RRB_MASK`.
    fn vfnmsub213ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PS256RRB_MASKZ`.
    fn vfnmsub213ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PS512`.
    fn vfnmsub213ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213PS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213PS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213PS512");
        }
    }
    /// Emits `VFNMSUB213PS512RRR_ER`.
    fn vfnmsub213ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PS512_MASK`.
    fn vfnmsub213ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213PS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213PS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213PS512_MASK");
        }
    }
    /// Emits `VFNMSUB213PS512RRR_MASK_ER`.
    fn vfnmsub213ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PS512_MASKZ`.
    fn vfnmsub213ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213PS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213PS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213PS512_MASKZ");
        }
    }
    /// Emits `VFNMSUB213PS512RRR_MASKZ_ER`.
    fn vfnmsub213ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PS512RRB`.
    fn vfnmsub213ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PS512RRB_MASK`.
    fn vfnmsub213ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213PS512RRB_MASKZ`.
    fn vfnmsub213ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213PS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213SD`.
    fn vfnmsub213sd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213SDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213SDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213SD");
        }
    }
    /// Emits `VFNMSUB213SDRRR_ER`.
    fn vfnmsub213sd_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213SDRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213SD_MASK`.
    fn vfnmsub213sd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213SDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213SDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213SD_MASK");
        }
    }
    /// Emits `VFNMSUB213SDRRR_MASK_ER`.
    fn vfnmsub213sd_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213SDRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213SD_MASKZ`.
    fn vfnmsub213sd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213SDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213SDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213SD_MASKZ");
        }
    }
    /// Emits `VFNMSUB213SDRRR_MASKZ_ER`.
    fn vfnmsub213sd_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213SDRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213SS`.
    fn vfnmsub213ss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213SSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213SSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213SS");
        }
    }
    /// Emits `VFNMSUB213SSRRR_ER`.
    fn vfnmsub213ss_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213SSRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213SS_MASK`.
    fn vfnmsub213ss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213SSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213SSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213SS_MASK");
        }
    }
    /// Emits `VFNMSUB213SSRRR_MASK_ER`.
    fn vfnmsub213ss_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213SSRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB213SS_MASKZ`.
    fn vfnmsub213ss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB213SSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB213SSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB213SS_MASKZ");
        }
    }
    /// Emits `VFNMSUB213SSRRR_MASKZ_ER`.
    fn vfnmsub213ss_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB213SSRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PD128`.
    fn vfnmsub231pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231PD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231PD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231PD128");
        }
    }
    /// Emits `VFNMSUB231PD128_MASK`.
    fn vfnmsub231pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231PD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231PD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231PD128_MASK");
        }
    }
    /// Emits `VFNMSUB231PD128_MASKZ`.
    fn vfnmsub231pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231PD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231PD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231PD128_MASKZ");
        }
    }
    /// Emits `VFNMSUB231PD128RRB`.
    fn vfnmsub231pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PD128RRB_MASK`.
    fn vfnmsub231pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PD128RRB_MASKZ`.
    fn vfnmsub231pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PD256`.
    fn vfnmsub231pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231PD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231PD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231PD256");
        }
    }
    /// Emits `VFNMSUB231PD256_MASK`.
    fn vfnmsub231pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231PD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231PD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231PD256_MASK");
        }
    }
    /// Emits `VFNMSUB231PD256_MASKZ`.
    fn vfnmsub231pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231PD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231PD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231PD256_MASKZ");
        }
    }
    /// Emits `VFNMSUB231PD256RRB`.
    fn vfnmsub231pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PD256RRB_MASK`.
    fn vfnmsub231pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PD256RRB_MASKZ`.
    fn vfnmsub231pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PD512`.
    fn vfnmsub231pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231PD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231PD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231PD512");
        }
    }
    /// Emits `VFNMSUB231PD512RRR_ER`.
    fn vfnmsub231pd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PD512_MASK`.
    fn vfnmsub231pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231PD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231PD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231PD512_MASK");
        }
    }
    /// Emits `VFNMSUB231PD512RRR_MASK_ER`.
    fn vfnmsub231pd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PD512_MASKZ`.
    fn vfnmsub231pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231PD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231PD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231PD512_MASKZ");
        }
    }
    /// Emits `VFNMSUB231PD512RRR_MASKZ_ER`.
    fn vfnmsub231pd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PD512RRB`.
    fn vfnmsub231pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PD512RRB_MASK`.
    fn vfnmsub231pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PD512RRB_MASKZ`.
    fn vfnmsub231pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PS128`.
    fn vfnmsub231ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231PS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231PS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231PS128");
        }
    }
    /// Emits `VFNMSUB231PS128_MASK`.
    fn vfnmsub231ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231PS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231PS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231PS128_MASK");
        }
    }
    /// Emits `VFNMSUB231PS128_MASKZ`.
    fn vfnmsub231ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231PS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231PS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231PS128_MASKZ");
        }
    }
    /// Emits `VFNMSUB231PS128RRB`.
    fn vfnmsub231ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PS128RRB_MASK`.
    fn vfnmsub231ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PS128RRB_MASKZ`.
    fn vfnmsub231ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PS256`.
    fn vfnmsub231ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231PS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231PS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231PS256");
        }
    }
    /// Emits `VFNMSUB231PS256_MASK`.
    fn vfnmsub231ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231PS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231PS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231PS256_MASK");
        }
    }
    /// Emits `VFNMSUB231PS256_MASKZ`.
    fn vfnmsub231ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231PS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231PS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231PS256_MASKZ");
        }
    }
    /// Emits `VFNMSUB231PS256RRB`.
    fn vfnmsub231ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PS256RRB_MASK`.
    fn vfnmsub231ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PS256RRB_MASKZ`.
    fn vfnmsub231ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PS512`.
    fn vfnmsub231ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231PS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231PS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231PS512");
        }
    }
    /// Emits `VFNMSUB231PS512RRR_ER`.
    fn vfnmsub231ps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PS512_MASK`.
    fn vfnmsub231ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231PS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231PS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231PS512_MASK");
        }
    }
    /// Emits `VFNMSUB231PS512RRR_MASK_ER`.
    fn vfnmsub231ps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PS512_MASKZ`.
    fn vfnmsub231ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231PS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231PS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231PS512_MASKZ");
        }
    }
    /// Emits `VFNMSUB231PS512RRR_MASKZ_ER`.
    fn vfnmsub231ps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PS512RRB`.
    fn vfnmsub231ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PS512RRB_MASK`.
    fn vfnmsub231ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231PS512RRB_MASKZ`.
    fn vfnmsub231ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231PS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231SD`.
    fn vfnmsub231sd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231SDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231SDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231SD");
        }
    }
    /// Emits `VFNMSUB231SDRRR_ER`.
    fn vfnmsub231sd_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231SDRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231SD_MASK`.
    fn vfnmsub231sd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231SDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231SDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231SD_MASK");
        }
    }
    /// Emits `VFNMSUB231SDRRR_MASK_ER`.
    fn vfnmsub231sd_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231SDRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231SD_MASKZ`.
    fn vfnmsub231sd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231SDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231SDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231SD_MASKZ");
        }
    }
    /// Emits `VFNMSUB231SDRRR_MASKZ_ER`.
    fn vfnmsub231sd_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231SDRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231SS`.
    fn vfnmsub231ss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231SSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231SSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231SS");
        }
    }
    /// Emits `VFNMSUB231SSRRR_ER`.
    fn vfnmsub231ss_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231SSRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231SS_MASK`.
    fn vfnmsub231ss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231SSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231SSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231SS_MASK");
        }
    }
    /// Emits `VFNMSUB231SSRRR_MASK_ER`.
    fn vfnmsub231ss_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231SSRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VFNMSUB231SS_MASKZ`.
    fn vfnmsub231ss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VFNMSUB231SSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VFNMSUB231SSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VFNMSUB231SS_MASKZ");
        }
    }
    /// Emits `VFNMSUB231SSRRR_MASKZ_ER`.
    fn vfnmsub231ss_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VFNMSUB231SSRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGATHERDPD128RM`.
    fn vgatherdpd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGATHERDPD128RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGATHERDPD256RM`.
    fn vgatherdpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGATHERDPD256RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGATHERDPD512RM`.
    fn vgatherdpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGATHERDPD512RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGATHERDPS128RM`.
    fn vgatherdps128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGATHERDPS128RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGATHERDPS256RM`.
    fn vgatherdps256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGATHERDPS256RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGATHERDPS512RM`.
    fn vgatherdps512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGATHERDPS512RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGATHERQPD128RM`.
    fn vgatherqpd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGATHERQPD128RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGATHERQPD256RM`.
    fn vgatherqpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGATHERQPD256RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGATHERQPD512RM`.
    fn vgatherqpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGATHERQPD512RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGATHERQPS128RM`.
    fn vgatherqps128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGATHERQPS128RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGATHERQPS256RM`.
    fn vgatherqps256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGATHERQPS256RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGATHERQPS512RM`.
    fn vgatherqps512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGATHERQPS512RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPD128`.
    fn vgetexppd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VGETEXPPD128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VGETEXPPD128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPPD128");
        }
    }
    /// Emits `VGETEXPPD128_MASK`.
    fn vgetexppd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VGETEXPPD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VGETEXPPD128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPPD128_MASK");
        }
    }
    /// Emits `VGETEXPPD128_MASKZ`.
    fn vgetexppd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VGETEXPPD128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VGETEXPPD128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPPD128_MASKZ");
        }
    }
    /// Emits `VGETEXPPD128RB`.
    fn vgetexppd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPD128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPD128RB_MASK`.
    fn vgetexppd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPD128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPD128RB_MASKZ`.
    fn vgetexppd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPD128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPD256`.
    fn vgetexppd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VGETEXPPD256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VGETEXPPD256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPPD256");
        }
    }
    /// Emits `VGETEXPPD256_MASK`.
    fn vgetexppd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VGETEXPPD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VGETEXPPD256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPPD256_MASK");
        }
    }
    /// Emits `VGETEXPPD256_MASKZ`.
    fn vgetexppd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VGETEXPPD256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VGETEXPPD256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPPD256_MASKZ");
        }
    }
    /// Emits `VGETEXPPD256RB`.
    fn vgetexppd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPD256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPD256RB_MASK`.
    fn vgetexppd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPD256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPD256RB_MASKZ`.
    fn vgetexppd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPD256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPD512`.
    fn vgetexppd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VGETEXPPD512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VGETEXPPD512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPPD512");
        }
    }
    /// Emits `VGETEXPPD512_MASK`.
    fn vgetexppd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VGETEXPPD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VGETEXPPD512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPPD512_MASK");
        }
    }
    /// Emits `VGETEXPPD512RR_MASK_SAE`.
    fn vgetexppd512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPD512RR_MASK_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPD512_MASKZ`.
    fn vgetexppd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VGETEXPPD512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VGETEXPPD512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPPD512_MASKZ");
        }
    }
    /// Emits `VGETEXPPD512RR_MASKZ_SAE`.
    fn vgetexppd512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPD512RR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPD512RR_SAE`.
    fn vgetexppd512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPD512RR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPD512RB`.
    fn vgetexppd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPD512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPD512RB_MASK`.
    fn vgetexppd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPD512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPD512RB_MASKZ`.
    fn vgetexppd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPD512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPS128`.
    fn vgetexpps128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VGETEXPPS128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VGETEXPPS128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPPS128");
        }
    }
    /// Emits `VGETEXPPS128_MASK`.
    fn vgetexpps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VGETEXPPS128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VGETEXPPS128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPPS128_MASK");
        }
    }
    /// Emits `VGETEXPPS128_MASKZ`.
    fn vgetexpps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VGETEXPPS128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VGETEXPPS128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPPS128_MASKZ");
        }
    }
    /// Emits `VGETEXPPS128RB`.
    fn vgetexpps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPS128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPS128RB_MASK`.
    fn vgetexpps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPS128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPS128RB_MASKZ`.
    fn vgetexpps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPS128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPS256`.
    fn vgetexpps256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VGETEXPPS256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VGETEXPPS256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPPS256");
        }
    }
    /// Emits `VGETEXPPS256_MASK`.
    fn vgetexpps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VGETEXPPS256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VGETEXPPS256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPPS256_MASK");
        }
    }
    /// Emits `VGETEXPPS256_MASKZ`.
    fn vgetexpps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VGETEXPPS256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VGETEXPPS256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPPS256_MASKZ");
        }
    }
    /// Emits `VGETEXPPS256RB`.
    fn vgetexpps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPS256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPS256RB_MASK`.
    fn vgetexpps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPS256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPS256RB_MASKZ`.
    fn vgetexpps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPS256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPS512`.
    fn vgetexpps512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VGETEXPPS512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VGETEXPPS512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPPS512");
        }
    }
    /// Emits `VGETEXPPS512_MASK`.
    fn vgetexpps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VGETEXPPS512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VGETEXPPS512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPPS512_MASK");
        }
    }
    /// Emits `VGETEXPPS512RR_MASK_SAE`.
    fn vgetexpps512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPS512RR_MASK_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPS512_MASKZ`.
    fn vgetexpps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VGETEXPPS512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VGETEXPPS512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPPS512_MASKZ");
        }
    }
    /// Emits `VGETEXPPS512RR_MASKZ_SAE`.
    fn vgetexpps512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPS512RR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPS512RR_SAE`.
    fn vgetexpps512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPS512RR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPS512RB`.
    fn vgetexpps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPS512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPS512RB_MASK`.
    fn vgetexpps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPS512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPPS512RB_MASKZ`.
    fn vgetexpps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VGETEXPPS512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VGETEXPSD`.
    fn vgetexpsd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VGETEXPSDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VGETEXPSDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPSD");
        }
    }
    /// Emits `VGETEXPSD_MASK`.
    fn vgetexpsd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VGETEXPSDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VGETEXPSDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPSD_MASK");
        }
    }
    /// Emits `VGETEXPSDRRR_MASK_SAE`.
    fn vgetexpsd_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETEXPSDRRR_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETEXPSD_MASKZ`.
    fn vgetexpsd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VGETEXPSDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VGETEXPSDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPSD_MASKZ");
        }
    }
    /// Emits `VGETEXPSDRRR_MASKZ_SAE`.
    fn vgetexpsd_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETEXPSDRRR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETEXPSDRRR_SAE`.
    fn vgetexpsd_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETEXPSDRRR_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETEXPSS`.
    fn vgetexpss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VGETEXPSSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VGETEXPSSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPSS");
        }
    }
    /// Emits `VGETEXPSS_MASK`.
    fn vgetexpss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VGETEXPSSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VGETEXPSSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPSS_MASK");
        }
    }
    /// Emits `VGETEXPSSRRR_MASK_SAE`.
    fn vgetexpss_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETEXPSSRRR_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETEXPSS_MASKZ`.
    fn vgetexpss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VGETEXPSSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VGETEXPSSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETEXPSS_MASKZ");
        }
    }
    /// Emits `VGETEXPSSRRR_MASKZ_SAE`.
    fn vgetexpss_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETEXPSSRRR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETEXPSSRRR_SAE`.
    fn vgetexpss_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETEXPSSRRR_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPD128`.
    fn vgetmantpd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VGETMANTPD128RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VGETMANTPD128RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETMANTPD128");
        }
    }
    /// Emits `VGETMANTPD128_MASK`.
    fn vgetmantpd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VGETMANTPD128RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VGETMANTPD128RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETMANTPD128_MASK");
        }
    }
    /// Emits `VGETMANTPD128_MASKZ`.
    fn vgetmantpd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VGETMANTPD128RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VGETMANTPD128RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETMANTPD128_MASKZ");
        }
    }
    /// Emits `VGETMANTPD128RBI`.
    fn vgetmantpd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPD128RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPD128RBI_MASK`.
    fn vgetmantpd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPD128RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPD128RBI_MASKZ`.
    fn vgetmantpd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPD128RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPD256`.
    fn vgetmantpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VGETMANTPD256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VGETMANTPD256RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETMANTPD256");
        }
    }
    /// Emits `VGETMANTPD256_MASK`.
    fn vgetmantpd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VGETMANTPD256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VGETMANTPD256RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETMANTPD256_MASK");
        }
    }
    /// Emits `VGETMANTPD256_MASKZ`.
    fn vgetmantpd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VGETMANTPD256RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VGETMANTPD256RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETMANTPD256_MASKZ");
        }
    }
    /// Emits `VGETMANTPD256RBI`.
    fn vgetmantpd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPD256RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPD256RBI_MASK`.
    fn vgetmantpd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPD256RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPD256RBI_MASKZ`.
    fn vgetmantpd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPD256RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPD512`.
    fn vgetmantpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VGETMANTPD512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VGETMANTPD512RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETMANTPD512");
        }
    }
    /// Emits `VGETMANTPD512_MASK`.
    fn vgetmantpd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VGETMANTPD512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VGETMANTPD512RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETMANTPD512_MASK");
        }
    }
    /// Emits `VGETMANTPD512RRI_MASK_SAE`.
    fn vgetmantpd512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPD512RRI_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPD512_MASKZ`.
    fn vgetmantpd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VGETMANTPD512RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VGETMANTPD512RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETMANTPD512_MASKZ");
        }
    }
    /// Emits `VGETMANTPD512RRI_MASKZ_SAE`.
    fn vgetmantpd512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPD512RRI_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPD512RRI_SAE`.
    fn vgetmantpd512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPD512RRI_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPD512RBI`.
    fn vgetmantpd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPD512RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPD512RBI_MASK`.
    fn vgetmantpd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPD512RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPD512RBI_MASKZ`.
    fn vgetmantpd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPD512RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPS128`.
    fn vgetmantps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VGETMANTPS128RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VGETMANTPS128RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETMANTPS128");
        }
    }
    /// Emits `VGETMANTPS128_MASK`.
    fn vgetmantps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VGETMANTPS128RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VGETMANTPS128RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETMANTPS128_MASK");
        }
    }
    /// Emits `VGETMANTPS128_MASKZ`.
    fn vgetmantps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VGETMANTPS128RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VGETMANTPS128RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETMANTPS128_MASKZ");
        }
    }
    /// Emits `VGETMANTPS128RBI`.
    fn vgetmantps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPS128RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPS128RBI_MASK`.
    fn vgetmantps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPS128RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPS128RBI_MASKZ`.
    fn vgetmantps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPS128RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPS256`.
    fn vgetmantps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VGETMANTPS256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VGETMANTPS256RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETMANTPS256");
        }
    }
    /// Emits `VGETMANTPS256_MASK`.
    fn vgetmantps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VGETMANTPS256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VGETMANTPS256RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETMANTPS256_MASK");
        }
    }
    /// Emits `VGETMANTPS256_MASKZ`.
    fn vgetmantps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VGETMANTPS256RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VGETMANTPS256RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETMANTPS256_MASKZ");
        }
    }
    /// Emits `VGETMANTPS256RBI`.
    fn vgetmantps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPS256RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPS256RBI_MASK`.
    fn vgetmantps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPS256RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPS256RBI_MASKZ`.
    fn vgetmantps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPS256RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPS512`.
    fn vgetmantps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VGETMANTPS512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VGETMANTPS512RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETMANTPS512");
        }
    }
    /// Emits `VGETMANTPS512_MASK`.
    fn vgetmantps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VGETMANTPS512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VGETMANTPS512RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETMANTPS512_MASK");
        }
    }
    /// Emits `VGETMANTPS512RRI_MASK_SAE`.
    fn vgetmantps512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPS512RRI_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPS512_MASKZ`.
    fn vgetmantps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VGETMANTPS512RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VGETMANTPS512RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VGETMANTPS512_MASKZ");
        }
    }
    /// Emits `VGETMANTPS512RRI_MASKZ_SAE`.
    fn vgetmantps512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPS512RRI_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPS512RRI_SAE`.
    fn vgetmantps512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPS512RRI_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPS512RBI`.
    fn vgetmantps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPS512RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPS512RBI_MASK`.
    fn vgetmantps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPS512RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTPS512RBI_MASKZ`.
    fn vgetmantps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VGETMANTPS512RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VGETMANTSD`.
    fn vgetmantsd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VGETMANTSDRRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VGETMANTSDRRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VGETMANTSD");
        }
    }
    /// Emits `VGETMANTSD_MASK`.
    fn vgetmantsd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VGETMANTSDRRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VGETMANTSDRRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VGETMANTSD_MASK");
        }
    }
    /// Emits `VGETMANTSDRRRI_MASK_SAE`.
    fn vgetmantsd_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VGETMANTSDRRRI_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VGETMANTSD_MASKZ`.
    fn vgetmantsd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VGETMANTSDRRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VGETMANTSDRRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VGETMANTSD_MASKZ");
        }
    }
    /// Emits `VGETMANTSDRRRI_MASKZ_SAE`.
    fn vgetmantsd_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VGETMANTSDRRRI_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VGETMANTSDRRRI_SAE`.
    fn vgetmantsd_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VGETMANTSDRRRI_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VGETMANTSS`.
    fn vgetmantss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VGETMANTSSRRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VGETMANTSSRRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VGETMANTSS");
        }
    }
    /// Emits `VGETMANTSS_MASK`.
    fn vgetmantss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VGETMANTSSRRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VGETMANTSSRRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VGETMANTSS_MASK");
        }
    }
    /// Emits `VGETMANTSSRRRI_MASK_SAE`.
    fn vgetmantss_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VGETMANTSSRRRI_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VGETMANTSS_MASKZ`.
    fn vgetmantss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VGETMANTSSRRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VGETMANTSSRRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VGETMANTSS_MASKZ");
        }
    }
    /// Emits `VGETMANTSSRRRI_MASKZ_SAE`.
    fn vgetmantss_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VGETMANTSSRRRI_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VGETMANTSSRRRI_SAE`.
    fn vgetmantss_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VGETMANTSSRRRI_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VINSERTF32X4_256`.
    fn vinsertf32x4_256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VINSERTF32X4_256RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VINSERTF32X4_256RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VINSERTF32X4_256");
        }
    }
    /// Emits `VINSERTF32X4_256_MASK`.
    fn vinsertf32x4_256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VINSERTF32X4_256RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VINSERTF32X4_256RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VINSERTF32X4_256_MASK");
        }
    }
    /// Emits `VINSERTF32X4_256_MASKZ`.
    fn vinsertf32x4_256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VINSERTF32X4_256RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VINSERTF32X4_256RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VINSERTF32X4_256_MASKZ");
        }
    }
    /// Emits `VINSERTF32X4_512`.
    fn vinsertf32x4_512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VINSERTF32X4_512RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VINSERTF32X4_512RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VINSERTF32X4_512");
        }
    }
    /// Emits `VINSERTF32X4_512_MASK`.
    fn vinsertf32x4_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VINSERTF32X4_512RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VINSERTF32X4_512RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VINSERTF32X4_512_MASK");
        }
    }
    /// Emits `VINSERTF32X4_512_MASKZ`.
    fn vinsertf32x4_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VINSERTF32X4_512RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VINSERTF32X4_512RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VINSERTF32X4_512_MASKZ");
        }
    }
    /// Emits `VINSERTF64X4_512`.
    fn vinsertf64x4_512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VINSERTF64X4_512RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VINSERTF64X4_512RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VINSERTF64X4_512");
        }
    }
    /// Emits `VINSERTF64X4_512_MASK`.
    fn vinsertf64x4_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VINSERTF64X4_512RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VINSERTF64X4_512RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VINSERTF64X4_512_MASK");
        }
    }
    /// Emits `VINSERTF64X4_512_MASKZ`.
    fn vinsertf64x4_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VINSERTF64X4_512RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VINSERTF64X4_512RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VINSERTF64X4_512_MASKZ");
        }
    }
    /// Emits `VINSERTI32X4_256`.
    fn vinserti32x4_256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VINSERTI32X4_256RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VINSERTI32X4_256RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VINSERTI32X4_256");
        }
    }
    /// Emits `VINSERTI32X4_256_MASK`.
    fn vinserti32x4_256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VINSERTI32X4_256RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VINSERTI32X4_256RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VINSERTI32X4_256_MASK");
        }
    }
    /// Emits `VINSERTI32X4_256_MASKZ`.
    fn vinserti32x4_256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VINSERTI32X4_256RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VINSERTI32X4_256RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VINSERTI32X4_256_MASKZ");
        }
    }
    /// Emits `VINSERTI32X4_512`.
    fn vinserti32x4_512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VINSERTI32X4_512RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VINSERTI32X4_512RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VINSERTI32X4_512");
        }
    }
    /// Emits `VINSERTI32X4_512_MASK`.
    fn vinserti32x4_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VINSERTI32X4_512RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VINSERTI32X4_512RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VINSERTI32X4_512_MASK");
        }
    }
    /// Emits `VINSERTI32X4_512_MASKZ`.
    fn vinserti32x4_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VINSERTI32X4_512RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VINSERTI32X4_512RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VINSERTI32X4_512_MASKZ");
        }
    }
    /// Emits `VINSERTI64X4_512`.
    fn vinserti64x4_512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VINSERTI64X4_512RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VINSERTI64X4_512RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VINSERTI64X4_512");
        }
    }
    /// Emits `VINSERTI64X4_512_MASK`.
    fn vinserti64x4_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VINSERTI64X4_512RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VINSERTI64X4_512RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VINSERTI64X4_512_MASK");
        }
    }
    /// Emits `VINSERTI64X4_512_MASKZ`.
    fn vinserti64x4_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VINSERTI64X4_512RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VINSERTI64X4_512RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VINSERTI64X4_512_MASKZ");
        }
    }
    /// Emits `VINSERTPS`.
    fn vinsertps(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VINSERTPSRRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VINSERTPSRRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VINSERTPS");
        }
    }
    /// Emits `VMAXPD128`.
    fn vmaxpd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXPD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXPD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXPD128");
        }
    }
    /// Emits `VMAXPD128_MASK`.
    fn vmaxpd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXPD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXPD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXPD128_MASK");
        }
    }
    /// Emits `VMAXPD128_MASKZ`.
    fn vmaxpd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXPD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXPD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXPD128_MASKZ");
        }
    }
    /// Emits `VMAXPD128RRB`.
    fn vmaxpd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPD128RRB_MASK`.
    fn vmaxpd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPD128RRB_MASKZ`.
    fn vmaxpd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPD256`.
    fn vmaxpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXPD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXPD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXPD256");
        }
    }
    /// Emits `VMAXPD256_MASK`.
    fn vmaxpd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXPD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXPD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXPD256_MASK");
        }
    }
    /// Emits `VMAXPD256_MASKZ`.
    fn vmaxpd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXPD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXPD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXPD256_MASKZ");
        }
    }
    /// Emits `VMAXPD256RRB`.
    fn vmaxpd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPD256RRB_MASK`.
    fn vmaxpd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPD256RRB_MASKZ`.
    fn vmaxpd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPD512`.
    fn vmaxpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXPD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXPD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXPD512");
        }
    }
    /// Emits `VMAXPD512_MASK`.
    fn vmaxpd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXPD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXPD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXPD512_MASK");
        }
    }
    /// Emits `VMAXPD512RRR_MASK_SAE`.
    fn vmaxpd512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPD512RRR_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPD512_MASKZ`.
    fn vmaxpd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXPD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXPD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXPD512_MASKZ");
        }
    }
    /// Emits `VMAXPD512RRR_MASKZ_SAE`.
    fn vmaxpd512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPD512RRR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPD512RRR_SAE`.
    fn vmaxpd512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPD512RRR_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPD512RRB`.
    fn vmaxpd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPD512RRB_MASK`.
    fn vmaxpd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPD512RRB_MASKZ`.
    fn vmaxpd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPS128`.
    fn vmaxps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXPS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXPS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXPS128");
        }
    }
    /// Emits `VMAXPS128_MASK`.
    fn vmaxps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXPS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXPS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXPS128_MASK");
        }
    }
    /// Emits `VMAXPS128_MASKZ`.
    fn vmaxps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXPS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXPS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXPS128_MASKZ");
        }
    }
    /// Emits `VMAXPS128RRB`.
    fn vmaxps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPS128RRB_MASK`.
    fn vmaxps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPS128RRB_MASKZ`.
    fn vmaxps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPS256`.
    fn vmaxps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXPS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXPS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXPS256");
        }
    }
    /// Emits `VMAXPS256_MASK`.
    fn vmaxps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXPS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXPS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXPS256_MASK");
        }
    }
    /// Emits `VMAXPS256_MASKZ`.
    fn vmaxps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXPS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXPS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXPS256_MASKZ");
        }
    }
    /// Emits `VMAXPS256RRB`.
    fn vmaxps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPS256RRB_MASK`.
    fn vmaxps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPS256RRB_MASKZ`.
    fn vmaxps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPS512`.
    fn vmaxps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXPS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXPS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXPS512");
        }
    }
    /// Emits `VMAXPS512_MASK`.
    fn vmaxps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXPS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXPS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXPS512_MASK");
        }
    }
    /// Emits `VMAXPS512RRR_MASK_SAE`.
    fn vmaxps512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPS512RRR_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPS512_MASKZ`.
    fn vmaxps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXPS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXPS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXPS512_MASKZ");
        }
    }
    /// Emits `VMAXPS512RRR_MASKZ_SAE`.
    fn vmaxps512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPS512RRR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPS512RRR_SAE`.
    fn vmaxps512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPS512RRR_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPS512RRB`.
    fn vmaxps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPS512RRB_MASK`.
    fn vmaxps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXPS512RRB_MASKZ`.
    fn vmaxps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXPS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXSD`.
    fn vmaxsd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXSDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXSDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXSD");
        }
    }
    /// Emits `VMAXSD_MASK`.
    fn vmaxsd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXSDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXSDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXSD_MASK");
        }
    }
    /// Emits `VMAXSDRRR_MASK_SAE`.
    fn vmaxsd_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXSDRRR_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXSD_MASKZ`.
    fn vmaxsd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXSDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXSDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXSD_MASKZ");
        }
    }
    /// Emits `VMAXSDRRR_MASKZ_SAE`.
    fn vmaxsd_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXSDRRR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXSDRRR_SAE`.
    fn vmaxsd_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXSDRRR_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXSS`.
    fn vmaxss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXSSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXSSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXSS");
        }
    }
    /// Emits `VMAXSS_MASK`.
    fn vmaxss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXSSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXSSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXSS_MASK");
        }
    }
    /// Emits `VMAXSSRRR_MASK_SAE`.
    fn vmaxss_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXSSRRR_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXSS_MASKZ`.
    fn vmaxss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMAXSSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMAXSSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMAXSS_MASKZ");
        }
    }
    /// Emits `VMAXSSRRR_MASKZ_SAE`.
    fn vmaxss_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXSSRRR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMAXSSRRR_SAE`.
    fn vmaxss_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMAXSSRRR_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPD128`.
    fn vminpd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINPD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINPD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINPD128");
        }
    }
    /// Emits `VMINPD128_MASK`.
    fn vminpd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINPD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINPD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINPD128_MASK");
        }
    }
    /// Emits `VMINPD128_MASKZ`.
    fn vminpd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINPD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINPD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINPD128_MASKZ");
        }
    }
    /// Emits `VMINPD128RRB`.
    fn vminpd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPD128RRB_MASK`.
    fn vminpd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPD128RRB_MASKZ`.
    fn vminpd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPD256`.
    fn vminpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINPD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINPD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINPD256");
        }
    }
    /// Emits `VMINPD256_MASK`.
    fn vminpd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINPD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINPD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINPD256_MASK");
        }
    }
    /// Emits `VMINPD256_MASKZ`.
    fn vminpd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINPD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINPD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINPD256_MASKZ");
        }
    }
    /// Emits `VMINPD256RRB`.
    fn vminpd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPD256RRB_MASK`.
    fn vminpd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPD256RRB_MASKZ`.
    fn vminpd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPD512`.
    fn vminpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINPD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINPD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINPD512");
        }
    }
    /// Emits `VMINPD512_MASK`.
    fn vminpd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINPD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINPD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINPD512_MASK");
        }
    }
    /// Emits `VMINPD512RRR_MASK_SAE`.
    fn vminpd512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPD512RRR_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPD512_MASKZ`.
    fn vminpd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINPD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINPD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINPD512_MASKZ");
        }
    }
    /// Emits `VMINPD512RRR_MASKZ_SAE`.
    fn vminpd512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPD512RRR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPD512RRR_SAE`.
    fn vminpd512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPD512RRR_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPD512RRB`.
    fn vminpd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPD512RRB_MASK`.
    fn vminpd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPD512RRB_MASKZ`.
    fn vminpd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPS128`.
    fn vminps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINPS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINPS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINPS128");
        }
    }
    /// Emits `VMINPS128_MASK`.
    fn vminps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINPS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINPS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINPS128_MASK");
        }
    }
    /// Emits `VMINPS128_MASKZ`.
    fn vminps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINPS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINPS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINPS128_MASKZ");
        }
    }
    /// Emits `VMINPS128RRB`.
    fn vminps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPS128RRB_MASK`.
    fn vminps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPS128RRB_MASKZ`.
    fn vminps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPS256`.
    fn vminps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINPS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINPS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINPS256");
        }
    }
    /// Emits `VMINPS256_MASK`.
    fn vminps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINPS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINPS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINPS256_MASK");
        }
    }
    /// Emits `VMINPS256_MASKZ`.
    fn vminps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINPS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINPS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINPS256_MASKZ");
        }
    }
    /// Emits `VMINPS256RRB`.
    fn vminps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPS256RRB_MASK`.
    fn vminps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPS256RRB_MASKZ`.
    fn vminps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPS512`.
    fn vminps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINPS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINPS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINPS512");
        }
    }
    /// Emits `VMINPS512_MASK`.
    fn vminps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINPS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINPS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINPS512_MASK");
        }
    }
    /// Emits `VMINPS512RRR_MASK_SAE`.
    fn vminps512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPS512RRR_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPS512_MASKZ`.
    fn vminps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINPS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINPS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINPS512_MASKZ");
        }
    }
    /// Emits `VMINPS512RRR_MASKZ_SAE`.
    fn vminps512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPS512RRR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPS512RRR_SAE`.
    fn vminps512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPS512RRR_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPS512RRB`.
    fn vminps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPS512RRB_MASK`.
    fn vminps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINPS512RRB_MASKZ`.
    fn vminps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINPS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINSD`.
    fn vminsd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINSDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINSDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINSD");
        }
    }
    /// Emits `VMINSD_MASK`.
    fn vminsd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINSDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINSDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINSD_MASK");
        }
    }
    /// Emits `VMINSDRRR_MASK_SAE`.
    fn vminsd_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINSDRRR_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINSD_MASKZ`.
    fn vminsd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINSDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINSDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINSD_MASKZ");
        }
    }
    /// Emits `VMINSDRRR_MASKZ_SAE`.
    fn vminsd_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINSDRRR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINSDRRR_SAE`.
    fn vminsd_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINSDRRR_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINSS`.
    fn vminss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINSSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINSSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINSS");
        }
    }
    /// Emits `VMINSS_MASK`.
    fn vminss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINSSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINSSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINSS_MASK");
        }
    }
    /// Emits `VMINSSRRR_MASK_SAE`.
    fn vminss_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINSSRRR_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINSS_MASKZ`.
    fn vminss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMINSSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMINSSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMINSS_MASKZ");
        }
    }
    /// Emits `VMINSSRRR_MASKZ_SAE`.
    fn vminss_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINSSRRR_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMINSSRRR_SAE`.
    fn vminss_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMINSSRRR_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMOVAPD128`.
    fn vmovapd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVAPD128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVAPD128RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVAPD128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVAPD128");
        }
    }
    /// Emits `VMOVAPD128_MASK`.
    fn vmovapd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVAPD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVAPD128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVAPD128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVAPD128_MASK");
        }
    }
    /// Emits `VMOVAPD128_MASKZ`.
    fn vmovapd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVAPD128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVAPD128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVAPD128_MASKZ");
        }
    }
    /// Emits `VMOVAPD256`.
    fn vmovapd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVAPD256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVAPD256RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVAPD256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVAPD256");
        }
    }
    /// Emits `VMOVAPD256_MASK`.
    fn vmovapd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVAPD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVAPD256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVAPD256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVAPD256_MASK");
        }
    }
    /// Emits `VMOVAPD256_MASKZ`.
    fn vmovapd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVAPD256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVAPD256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVAPD256_MASKZ");
        }
    }
    /// Emits `VMOVAPD512`.
    fn vmovapd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVAPD512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVAPD512RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVAPD512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVAPD512");
        }
    }
    /// Emits `VMOVAPD512_MASK`.
    fn vmovapd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVAPD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVAPD512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVAPD512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVAPD512_MASK");
        }
    }
    /// Emits `VMOVAPD512_MASKZ`.
    fn vmovapd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVAPD512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVAPD512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVAPD512_MASKZ");
        }
    }
    /// Emits `VMOVAPS128`.
    fn vmovaps128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVAPS128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVAPS128RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVAPS128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVAPS128");
        }
    }
    /// Emits `VMOVAPS128_MASK`.
    fn vmovaps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVAPS128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVAPS128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVAPS128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVAPS128_MASK");
        }
    }
    /// Emits `VMOVAPS128_MASKZ`.
    fn vmovaps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVAPS128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVAPS128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVAPS128_MASKZ");
        }
    }
    /// Emits `VMOVAPS256`.
    fn vmovaps256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVAPS256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVAPS256RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVAPS256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVAPS256");
        }
    }
    /// Emits `VMOVAPS256_MASK`.
    fn vmovaps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVAPS256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVAPS256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVAPS256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVAPS256_MASK");
        }
    }
    /// Emits `VMOVAPS256_MASKZ`.
    fn vmovaps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVAPS256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVAPS256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVAPS256_MASKZ");
        }
    }
    /// Emits `VMOVAPS512`.
    fn vmovaps512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVAPS512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVAPS512RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVAPS512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVAPS512");
        }
    }
    /// Emits `VMOVAPS512_MASK`.
    fn vmovaps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVAPS512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVAPS512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVAPS512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVAPS512_MASK");
        }
    }
    /// Emits `VMOVAPS512_MASKZ`.
    fn vmovaps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVAPS512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVAPS512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVAPS512_MASKZ");
        }
    }
    /// Emits `VMOVD`.
    fn vmovd(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDRM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDMR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVD");
        }
    }
    /// Emits `VMOVDDUP128`.
    fn vmovddup128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDDUP128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDDUP128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDDUP128");
        }
    }
    /// Emits `VMOVDDUP128_MASK`.
    fn vmovddup128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDDUP128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDDUP128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDDUP128_MASK");
        }
    }
    /// Emits `VMOVDDUP128_MASKZ`.
    fn vmovddup128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDDUP128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDDUP128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDDUP128_MASKZ");
        }
    }
    /// Emits `VMOVDDUP256`.
    fn vmovddup256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDDUP256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDDUP256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDDUP256");
        }
    }
    /// Emits `VMOVDDUP256_MASK`.
    fn vmovddup256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDDUP256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDDUP256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDDUP256_MASK");
        }
    }
    /// Emits `VMOVDDUP256_MASKZ`.
    fn vmovddup256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDDUP256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDDUP256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDDUP256_MASKZ");
        }
    }
    /// Emits `VMOVDDUP512`.
    fn vmovddup512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDDUP512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDDUP512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDDUP512");
        }
    }
    /// Emits `VMOVDDUP512_MASK`.
    fn vmovddup512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDDUP512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDDUP512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDDUP512_MASK");
        }
    }
    /// Emits `VMOVDDUP512_MASKZ`.
    fn vmovddup512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDDUP512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDDUP512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDDUP512_MASKZ");
        }
    }
    /// Emits `VMOVDQA32_128`.
    fn vmovdqa32_128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQA32_128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQA32_128RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQA32_128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQA32_128");
        }
    }
    /// Emits `VMOVDQA32_128_MASK`.
    fn vmovdqa32_128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQA32_128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQA32_128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQA32_128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQA32_128_MASK");
        }
    }
    /// Emits `VMOVDQA32_128_MASKZ`.
    fn vmovdqa32_128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQA32_128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQA32_128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQA32_128_MASKZ");
        }
    }
    /// Emits `VMOVDQA32_256`.
    fn vmovdqa32_256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQA32_256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQA32_256RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQA32_256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQA32_256");
        }
    }
    /// Emits `VMOVDQA32_256_MASK`.
    fn vmovdqa32_256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQA32_256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQA32_256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQA32_256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQA32_256_MASK");
        }
    }
    /// Emits `VMOVDQA32_256_MASKZ`.
    fn vmovdqa32_256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQA32_256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQA32_256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQA32_256_MASKZ");
        }
    }
    /// Emits `VMOVDQA32_512`.
    fn vmovdqa32_512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQA32_512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQA32_512RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQA32_512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQA32_512");
        }
    }
    /// Emits `VMOVDQA32_512_MASK`.
    fn vmovdqa32_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQA32_512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQA32_512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQA32_512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQA32_512_MASK");
        }
    }
    /// Emits `VMOVDQA32_512_MASKZ`.
    fn vmovdqa32_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQA32_512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQA32_512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQA32_512_MASKZ");
        }
    }
    /// Emits `VMOVDQA64_128`.
    fn vmovdqa64_128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQA64_128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQA64_128RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQA64_128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQA64_128");
        }
    }
    /// Emits `VMOVDQA64_128_MASK`.
    fn vmovdqa64_128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQA64_128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQA64_128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQA64_128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQA64_128_MASK");
        }
    }
    /// Emits `VMOVDQA64_128_MASKZ`.
    fn vmovdqa64_128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQA64_128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQA64_128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQA64_128_MASKZ");
        }
    }
    /// Emits `VMOVDQA64_256`.
    fn vmovdqa64_256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQA64_256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQA64_256RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQA64_256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQA64_256");
        }
    }
    /// Emits `VMOVDQA64_256_MASK`.
    fn vmovdqa64_256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQA64_256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQA64_256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQA64_256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQA64_256_MASK");
        }
    }
    /// Emits `VMOVDQA64_256_MASKZ`.
    fn vmovdqa64_256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQA64_256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQA64_256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQA64_256_MASKZ");
        }
    }
    /// Emits `VMOVDQA64_512`.
    fn vmovdqa64_512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQA64_512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQA64_512RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQA64_512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQA64_512");
        }
    }
    /// Emits `VMOVDQA64_512_MASK`.
    fn vmovdqa64_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQA64_512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQA64_512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQA64_512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQA64_512_MASK");
        }
    }
    /// Emits `VMOVDQA64_512_MASKZ`.
    fn vmovdqa64_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQA64_512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQA64_512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQA64_512_MASKZ");
        }
    }
    /// Emits `VMOVDQU32_128`.
    fn vmovdqu32_128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQU32_128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQU32_128RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQU32_128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQU32_128");
        }
    }
    /// Emits `VMOVDQU32_128_MASK`.
    fn vmovdqu32_128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQU32_128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQU32_128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQU32_128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQU32_128_MASK");
        }
    }
    /// Emits `VMOVDQU32_128_MASKZ`.
    fn vmovdqu32_128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQU32_128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQU32_128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQU32_128_MASKZ");
        }
    }
    /// Emits `VMOVDQU32_256`.
    fn vmovdqu32_256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQU32_256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQU32_256RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQU32_256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQU32_256");
        }
    }
    /// Emits `VMOVDQU32_256_MASK`.
    fn vmovdqu32_256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQU32_256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQU32_256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQU32_256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQU32_256_MASK");
        }
    }
    /// Emits `VMOVDQU32_256_MASKZ`.
    fn vmovdqu32_256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQU32_256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQU32_256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQU32_256_MASKZ");
        }
    }
    /// Emits `VMOVDQU32_512`.
    fn vmovdqu32_512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQU32_512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQU32_512RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQU32_512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQU32_512");
        }
    }
    /// Emits `VMOVDQU32_512_MASK`.
    fn vmovdqu32_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQU32_512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQU32_512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQU32_512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQU32_512_MASK");
        }
    }
    /// Emits `VMOVDQU32_512_MASKZ`.
    fn vmovdqu32_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQU32_512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQU32_512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQU32_512_MASKZ");
        }
    }
    /// Emits `VMOVDQU64_128`.
    fn vmovdqu64_128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQU64_128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQU64_128RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQU64_128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQU64_128");
        }
    }
    /// Emits `VMOVDQU64_128_MASK`.
    fn vmovdqu64_128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQU64_128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQU64_128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQU64_128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQU64_128_MASK");
        }
    }
    /// Emits `VMOVDQU64_128_MASKZ`.
    fn vmovdqu64_128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQU64_128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQU64_128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQU64_128_MASKZ");
        }
    }
    /// Emits `VMOVDQU64_256`.
    fn vmovdqu64_256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQU64_256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQU64_256RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQU64_256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQU64_256");
        }
    }
    /// Emits `VMOVDQU64_256_MASK`.
    fn vmovdqu64_256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQU64_256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQU64_256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQU64_256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQU64_256_MASK");
        }
    }
    /// Emits `VMOVDQU64_256_MASKZ`.
    fn vmovdqu64_256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQU64_256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQU64_256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQU64_256_MASKZ");
        }
    }
    /// Emits `VMOVDQU64_512`.
    fn vmovdqu64_512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQU64_512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQU64_512RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQU64_512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQU64_512");
        }
    }
    /// Emits `VMOVDQU64_512_MASK`.
    fn vmovdqu64_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQU64_512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQU64_512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVDQU64_512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQU64_512_MASK");
        }
    }
    /// Emits `VMOVDQU64_512_MASKZ`.
    fn vmovdqu64_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVDQU64_512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVDQU64_512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVDQU64_512_MASKZ");
        }
    }
    /// Emits `VMOVD_G2XRR`.
    fn vmovd_g2x(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VMOVD_G2XRR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VMOVD_X2GRR`.
    fn vmovd_x2g(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VMOVD_X2GRR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VMOVHLPSRRR`.
    fn vmovhlps(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMOVHLPSRRR, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMOVHPDMR`.
    fn vmovhpd(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VMOVHPDMR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VMOVHPDRRM`.
    fn vmovhpd_3(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMOVHPDRRM, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMOVHPSMR`.
    fn vmovhps(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VMOVHPSMR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VMOVHPSRRM`.
    fn vmovhps_3(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMOVHPSRRM, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMOVLHPSRRR`.
    fn vmovlhps(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMOVLHPSRRR, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMOVLPDMR`.
    fn vmovlpd(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VMOVLPDMR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VMOVLPDRRM`.
    fn vmovlpd_3(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMOVLPDRRM, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMOVLPSMR`.
    fn vmovlps(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VMOVLPSMR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VMOVLPSRRM`.
    fn vmovlps_3(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMOVLPSRRM, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMOVNTDQ128MR`.
    fn vmovntdq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VMOVNTDQ128MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VMOVNTDQ256MR`.
    fn vmovntdq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VMOVNTDQ256MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VMOVNTDQ512MR`.
    fn vmovntdq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VMOVNTDQ512MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VMOVNTDQA128RM`.
    fn vmovntdqa128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VMOVNTDQA128RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VMOVNTDQA256RM`.
    fn vmovntdqa256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VMOVNTDQA256RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VMOVNTDQA512RM`.
    fn vmovntdqa512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VMOVNTDQA512RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VMOVNTPD128MR`.
    fn vmovntpd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VMOVNTPD128MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VMOVNTPD256MR`.
    fn vmovntpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VMOVNTPD256MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VMOVNTPD512MR`.
    fn vmovntpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VMOVNTPD512MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VMOVNTPS128MR`.
    fn vmovntps128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VMOVNTPS128MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VMOVNTPS256MR`.
    fn vmovntps256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VMOVNTPS256MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VMOVNTPS512MR`.
    fn vmovntps512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VMOVNTPS512MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VMOVQ`.
    fn vmovq(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVQRR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVQRM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVQMR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVQ");
        }
    }
    /// Emits `VMOVQ_G2X`.
    fn vmovq_g2x(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_gp() {
            self.emit(VMOVQ_G2XRR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVQ_G2XRM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVQ_G2X");
        }
    }
    /// Emits `VMOVQ_X2G`.
    fn vmovq_x2g(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_gp() && op1.is_vec() {
            self.emit(VMOVQ_X2GRR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVQ_X2GMR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVQ_X2G");
        }
    }
    /// Emits `VMOVSD`.
    fn vmovsd(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSDRM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVSDMR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSD");
        }
    }
    /// Emits `VMOVSDRRR`.
    fn vmovsd_3(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMOVSDRRR, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMOVSD_MASK`.
    fn vmovsd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSDRM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVSDMR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSD_MASK");
        }
    }
    /// Emits `VMOVSDRRR_MASK`.
    fn vmovsd_mask_3(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMOVSDRRR_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMOVSDRM_MASKZ`.
    fn vmovsd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VMOVSDRM_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VMOVSDRRR_MASKZ`.
    fn vmovsd_maskz_3(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMOVSDRRR_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMOVSHDUP128`.
    fn vmovshdup128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVSHDUP128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSHDUP128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSHDUP128");
        }
    }
    /// Emits `VMOVSHDUP128_MASK`.
    fn vmovshdup128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVSHDUP128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSHDUP128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSHDUP128_MASK");
        }
    }
    /// Emits `VMOVSHDUP128_MASKZ`.
    fn vmovshdup128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVSHDUP128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSHDUP128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSHDUP128_MASKZ");
        }
    }
    /// Emits `VMOVSHDUP256`.
    fn vmovshdup256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVSHDUP256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSHDUP256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSHDUP256");
        }
    }
    /// Emits `VMOVSHDUP256_MASK`.
    fn vmovshdup256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVSHDUP256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSHDUP256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSHDUP256_MASK");
        }
    }
    /// Emits `VMOVSHDUP256_MASKZ`.
    fn vmovshdup256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVSHDUP256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSHDUP256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSHDUP256_MASKZ");
        }
    }
    /// Emits `VMOVSHDUP512`.
    fn vmovshdup512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVSHDUP512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSHDUP512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSHDUP512");
        }
    }
    /// Emits `VMOVSHDUP512_MASK`.
    fn vmovshdup512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVSHDUP512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSHDUP512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSHDUP512_MASK");
        }
    }
    /// Emits `VMOVSHDUP512_MASKZ`.
    fn vmovshdup512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVSHDUP512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSHDUP512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSHDUP512_MASKZ");
        }
    }
    /// Emits `VMOVSLDUP128`.
    fn vmovsldup128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVSLDUP128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSLDUP128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSLDUP128");
        }
    }
    /// Emits `VMOVSLDUP128_MASK`.
    fn vmovsldup128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVSLDUP128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSLDUP128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSLDUP128_MASK");
        }
    }
    /// Emits `VMOVSLDUP128_MASKZ`.
    fn vmovsldup128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVSLDUP128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSLDUP128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSLDUP128_MASKZ");
        }
    }
    /// Emits `VMOVSLDUP256`.
    fn vmovsldup256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVSLDUP256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSLDUP256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSLDUP256");
        }
    }
    /// Emits `VMOVSLDUP256_MASK`.
    fn vmovsldup256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVSLDUP256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSLDUP256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSLDUP256_MASK");
        }
    }
    /// Emits `VMOVSLDUP256_MASKZ`.
    fn vmovsldup256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVSLDUP256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSLDUP256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSLDUP256_MASKZ");
        }
    }
    /// Emits `VMOVSLDUP512`.
    fn vmovsldup512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVSLDUP512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSLDUP512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSLDUP512");
        }
    }
    /// Emits `VMOVSLDUP512_MASK`.
    fn vmovsldup512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVSLDUP512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSLDUP512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSLDUP512_MASK");
        }
    }
    /// Emits `VMOVSLDUP512_MASKZ`.
    fn vmovsldup512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVSLDUP512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSLDUP512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSLDUP512_MASKZ");
        }
    }
    /// Emits `VMOVSS`.
    fn vmovss(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSSRM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVSSMR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSS");
        }
    }
    /// Emits `VMOVSSRRR`.
    fn vmovss_3(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMOVSSRRR, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMOVSS_MASK`.
    fn vmovss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVSSRM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVSSMR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVSS_MASK");
        }
    }
    /// Emits `VMOVSSRRR_MASK`.
    fn vmovss_mask_3(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMOVSSRRR_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMOVSSRM_MASKZ`.
    fn vmovss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VMOVSSRM_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VMOVSSRRR_MASKZ`.
    fn vmovss_maskz_3(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMOVSSRRR_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMOVUPD128`.
    fn vmovupd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVUPD128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVUPD128RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVUPD128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVUPD128");
        }
    }
    /// Emits `VMOVUPD128_MASK`.
    fn vmovupd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVUPD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVUPD128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVUPD128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVUPD128_MASK");
        }
    }
    /// Emits `VMOVUPD128_MASKZ`.
    fn vmovupd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVUPD128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVUPD128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVUPD128_MASKZ");
        }
    }
    /// Emits `VMOVUPD256`.
    fn vmovupd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVUPD256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVUPD256RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVUPD256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVUPD256");
        }
    }
    /// Emits `VMOVUPD256_MASK`.
    fn vmovupd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVUPD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVUPD256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVUPD256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVUPD256_MASK");
        }
    }
    /// Emits `VMOVUPD256_MASKZ`.
    fn vmovupd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVUPD256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVUPD256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVUPD256_MASKZ");
        }
    }
    /// Emits `VMOVUPD512`.
    fn vmovupd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVUPD512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVUPD512RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVUPD512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVUPD512");
        }
    }
    /// Emits `VMOVUPD512_MASK`.
    fn vmovupd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVUPD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVUPD512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVUPD512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVUPD512_MASK");
        }
    }
    /// Emits `VMOVUPD512_MASKZ`.
    fn vmovupd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVUPD512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVUPD512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVUPD512_MASKZ");
        }
    }
    /// Emits `VMOVUPS128`.
    fn vmovups128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVUPS128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVUPS128RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVUPS128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVUPS128");
        }
    }
    /// Emits `VMOVUPS128_MASK`.
    fn vmovups128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVUPS128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVUPS128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVUPS128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVUPS128_MASK");
        }
    }
    /// Emits `VMOVUPS128_MASKZ`.
    fn vmovups128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVUPS128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVUPS128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVUPS128_MASKZ");
        }
    }
    /// Emits `VMOVUPS256`.
    fn vmovups256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVUPS256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVUPS256RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVUPS256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVUPS256");
        }
    }
    /// Emits `VMOVUPS256_MASK`.
    fn vmovups256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVUPS256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVUPS256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVUPS256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVUPS256_MASK");
        }
    }
    /// Emits `VMOVUPS256_MASKZ`.
    fn vmovups256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVUPS256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVUPS256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVUPS256_MASKZ");
        }
    }
    /// Emits `VMOVUPS512`.
    fn vmovups512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVUPS512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVUPS512RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVUPS512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVUPS512");
        }
    }
    /// Emits `VMOVUPS512_MASK`.
    fn vmovups512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVUPS512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVUPS512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VMOVUPS512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVUPS512_MASK");
        }
    }
    /// Emits `VMOVUPS512_MASKZ`.
    fn vmovups512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VMOVUPS512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VMOVUPS512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VMOVUPS512_MASKZ");
        }
    }
    /// Emits `VMULPD128`.
    fn vmulpd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULPD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULPD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULPD128");
        }
    }
    /// Emits `VMULPD128_MASK`.
    fn vmulpd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULPD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULPD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULPD128_MASK");
        }
    }
    /// Emits `VMULPD128_MASKZ`.
    fn vmulpd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULPD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULPD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULPD128_MASKZ");
        }
    }
    /// Emits `VMULPD128RRB`.
    fn vmulpd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPD128RRB_MASK`.
    fn vmulpd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPD128RRB_MASKZ`.
    fn vmulpd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPD256`.
    fn vmulpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULPD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULPD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULPD256");
        }
    }
    /// Emits `VMULPD256_MASK`.
    fn vmulpd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULPD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULPD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULPD256_MASK");
        }
    }
    /// Emits `VMULPD256_MASKZ`.
    fn vmulpd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULPD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULPD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULPD256_MASKZ");
        }
    }
    /// Emits `VMULPD256RRB`.
    fn vmulpd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPD256RRB_MASK`.
    fn vmulpd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPD256RRB_MASKZ`.
    fn vmulpd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPD512`.
    fn vmulpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULPD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULPD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULPD512");
        }
    }
    /// Emits `VMULPD512RRR_ER`.
    fn vmulpd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPD512_MASK`.
    fn vmulpd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULPD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULPD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULPD512_MASK");
        }
    }
    /// Emits `VMULPD512RRR_MASK_ER`.
    fn vmulpd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPD512_MASKZ`.
    fn vmulpd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULPD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULPD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULPD512_MASKZ");
        }
    }
    /// Emits `VMULPD512RRR_MASKZ_ER`.
    fn vmulpd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPD512RRB`.
    fn vmulpd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPD512RRB_MASK`.
    fn vmulpd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPD512RRB_MASKZ`.
    fn vmulpd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPS128`.
    fn vmulps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULPS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULPS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULPS128");
        }
    }
    /// Emits `VMULPS128_MASK`.
    fn vmulps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULPS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULPS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULPS128_MASK");
        }
    }
    /// Emits `VMULPS128_MASKZ`.
    fn vmulps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULPS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULPS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULPS128_MASKZ");
        }
    }
    /// Emits `VMULPS128RRB`.
    fn vmulps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPS128RRB_MASK`.
    fn vmulps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPS128RRB_MASKZ`.
    fn vmulps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPS256`.
    fn vmulps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULPS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULPS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULPS256");
        }
    }
    /// Emits `VMULPS256_MASK`.
    fn vmulps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULPS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULPS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULPS256_MASK");
        }
    }
    /// Emits `VMULPS256_MASKZ`.
    fn vmulps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULPS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULPS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULPS256_MASKZ");
        }
    }
    /// Emits `VMULPS256RRB`.
    fn vmulps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPS256RRB_MASK`.
    fn vmulps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPS256RRB_MASKZ`.
    fn vmulps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPS512`.
    fn vmulps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULPS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULPS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULPS512");
        }
    }
    /// Emits `VMULPS512RRR_ER`.
    fn vmulps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPS512_MASK`.
    fn vmulps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULPS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULPS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULPS512_MASK");
        }
    }
    /// Emits `VMULPS512RRR_MASK_ER`.
    fn vmulps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPS512_MASKZ`.
    fn vmulps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULPS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULPS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULPS512_MASKZ");
        }
    }
    /// Emits `VMULPS512RRR_MASKZ_ER`.
    fn vmulps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPS512RRB`.
    fn vmulps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPS512RRB_MASK`.
    fn vmulps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULPS512RRB_MASKZ`.
    fn vmulps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULPS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULSD`.
    fn vmulsd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULSDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULSDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULSD");
        }
    }
    /// Emits `VMULSDRRR_ER`.
    fn vmulsd_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULSDRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULSD_MASK`.
    fn vmulsd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULSDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULSDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULSD_MASK");
        }
    }
    /// Emits `VMULSDRRR_MASK_ER`.
    fn vmulsd_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULSDRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULSD_MASKZ`.
    fn vmulsd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULSDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULSDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULSD_MASKZ");
        }
    }
    /// Emits `VMULSDRRR_MASKZ_ER`.
    fn vmulsd_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULSDRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULSS`.
    fn vmulss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULSSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULSSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULSS");
        }
    }
    /// Emits `VMULSSRRR_ER`.
    fn vmulss_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULSSRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULSS_MASK`.
    fn vmulss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULSSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULSSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULSS_MASK");
        }
    }
    /// Emits `VMULSSRRR_MASK_ER`.
    fn vmulss_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULSSRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VMULSS_MASKZ`.
    fn vmulss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VMULSSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VMULSSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VMULSS_MASKZ");
        }
    }
    /// Emits `VMULSSRRR_MASKZ_ER`.
    fn vmulss_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VMULSSRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPABSD128`.
    fn vpabsd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPABSD128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPABSD128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPABSD128");
        }
    }
    /// Emits `VPABSD128_MASK`.
    fn vpabsd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPABSD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPABSD128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPABSD128_MASK");
        }
    }
    /// Emits `VPABSD128_MASKZ`.
    fn vpabsd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPABSD128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPABSD128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPABSD128_MASKZ");
        }
    }
    /// Emits `VPABSD128RB`.
    fn vpabsd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPABSD128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPABSD128RB_MASK`.
    fn vpabsd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPABSD128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPABSD128RB_MASKZ`.
    fn vpabsd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPABSD128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPABSD256`.
    fn vpabsd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPABSD256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPABSD256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPABSD256");
        }
    }
    /// Emits `VPABSD256_MASK`.
    fn vpabsd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPABSD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPABSD256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPABSD256_MASK");
        }
    }
    /// Emits `VPABSD256_MASKZ`.
    fn vpabsd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPABSD256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPABSD256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPABSD256_MASKZ");
        }
    }
    /// Emits `VPABSD256RB`.
    fn vpabsd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPABSD256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPABSD256RB_MASK`.
    fn vpabsd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPABSD256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPABSD256RB_MASKZ`.
    fn vpabsd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPABSD256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPABSD512`.
    fn vpabsd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPABSD512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPABSD512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPABSD512");
        }
    }
    /// Emits `VPABSD512_MASK`.
    fn vpabsd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPABSD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPABSD512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPABSD512_MASK");
        }
    }
    /// Emits `VPABSD512_MASKZ`.
    fn vpabsd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPABSD512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPABSD512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPABSD512_MASKZ");
        }
    }
    /// Emits `VPABSD512RB`.
    fn vpabsd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPABSD512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPABSD512RB_MASK`.
    fn vpabsd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPABSD512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPABSD512RB_MASKZ`.
    fn vpabsd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPABSD512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPABSQ128`.
    fn vpabsq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPABSQ128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPABSQ128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPABSQ128");
        }
    }
    /// Emits `VPABSQ128_MASK`.
    fn vpabsq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPABSQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPABSQ128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPABSQ128_MASK");
        }
    }
    /// Emits `VPABSQ128_MASKZ`.
    fn vpabsq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPABSQ128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPABSQ128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPABSQ128_MASKZ");
        }
    }
    /// Emits `VPABSQ128RB`.
    fn vpabsq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPABSQ128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPABSQ128RB_MASK`.
    fn vpabsq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPABSQ128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPABSQ128RB_MASKZ`.
    fn vpabsq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPABSQ128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPABSQ256`.
    fn vpabsq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPABSQ256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPABSQ256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPABSQ256");
        }
    }
    /// Emits `VPABSQ256_MASK`.
    fn vpabsq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPABSQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPABSQ256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPABSQ256_MASK");
        }
    }
    /// Emits `VPABSQ256_MASKZ`.
    fn vpabsq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPABSQ256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPABSQ256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPABSQ256_MASKZ");
        }
    }
    /// Emits `VPABSQ256RB`.
    fn vpabsq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPABSQ256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPABSQ256RB_MASK`.
    fn vpabsq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPABSQ256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPABSQ256RB_MASKZ`.
    fn vpabsq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPABSQ256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPABSQ512`.
    fn vpabsq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPABSQ512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPABSQ512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPABSQ512");
        }
    }
    /// Emits `VPABSQ512_MASK`.
    fn vpabsq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPABSQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPABSQ512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPABSQ512_MASK");
        }
    }
    /// Emits `VPABSQ512_MASKZ`.
    fn vpabsq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPABSQ512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPABSQ512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPABSQ512_MASKZ");
        }
    }
    /// Emits `VPABSQ512RB`.
    fn vpabsq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPABSQ512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPABSQ512RB_MASK`.
    fn vpabsq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPABSQ512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPABSQ512RB_MASKZ`.
    fn vpabsq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPABSQ512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPADDD128`.
    fn vpaddd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPADDD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPADDD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPADDD128");
        }
    }
    /// Emits `VPADDD128_MASK`.
    fn vpaddd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPADDD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPADDD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPADDD128_MASK");
        }
    }
    /// Emits `VPADDD128_MASKZ`.
    fn vpaddd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPADDD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPADDD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPADDD128_MASKZ");
        }
    }
    /// Emits `VPADDD128RRB`.
    fn vpaddd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPADDD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPADDD128RRB_MASK`.
    fn vpaddd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPADDD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPADDD128RRB_MASKZ`.
    fn vpaddd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPADDD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPADDD256`.
    fn vpaddd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPADDD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPADDD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPADDD256");
        }
    }
    /// Emits `VPADDD256_MASK`.
    fn vpaddd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPADDD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPADDD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPADDD256_MASK");
        }
    }
    /// Emits `VPADDD256_MASKZ`.
    fn vpaddd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPADDD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPADDD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPADDD256_MASKZ");
        }
    }
    /// Emits `VPADDD256RRB`.
    fn vpaddd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPADDD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPADDD256RRB_MASK`.
    fn vpaddd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPADDD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPADDD256RRB_MASKZ`.
    fn vpaddd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPADDD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPADDD512`.
    fn vpaddd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPADDD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPADDD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPADDD512");
        }
    }
    /// Emits `VPADDD512_MASK`.
    fn vpaddd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPADDD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPADDD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPADDD512_MASK");
        }
    }
    /// Emits `VPADDD512_MASKZ`.
    fn vpaddd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPADDD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPADDD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPADDD512_MASKZ");
        }
    }
    /// Emits `VPADDD512RRB`.
    fn vpaddd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPADDD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPADDD512RRB_MASK`.
    fn vpaddd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPADDD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPADDD512RRB_MASKZ`.
    fn vpaddd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPADDD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPADDQ128`.
    fn vpaddq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPADDQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPADDQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPADDQ128");
        }
    }
    /// Emits `VPADDQ128_MASK`.
    fn vpaddq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPADDQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPADDQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPADDQ128_MASK");
        }
    }
    /// Emits `VPADDQ128_MASKZ`.
    fn vpaddq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPADDQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPADDQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPADDQ128_MASKZ");
        }
    }
    /// Emits `VPADDQ128RRB`.
    fn vpaddq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPADDQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPADDQ128RRB_MASK`.
    fn vpaddq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPADDQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPADDQ128RRB_MASKZ`.
    fn vpaddq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPADDQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPADDQ256`.
    fn vpaddq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPADDQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPADDQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPADDQ256");
        }
    }
    /// Emits `VPADDQ256_MASK`.
    fn vpaddq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPADDQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPADDQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPADDQ256_MASK");
        }
    }
    /// Emits `VPADDQ256_MASKZ`.
    fn vpaddq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPADDQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPADDQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPADDQ256_MASKZ");
        }
    }
    /// Emits `VPADDQ256RRB`.
    fn vpaddq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPADDQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPADDQ256RRB_MASK`.
    fn vpaddq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPADDQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPADDQ256RRB_MASKZ`.
    fn vpaddq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPADDQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPADDQ512`.
    fn vpaddq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPADDQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPADDQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPADDQ512");
        }
    }
    /// Emits `VPADDQ512_MASK`.
    fn vpaddq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPADDQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPADDQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPADDQ512_MASK");
        }
    }
    /// Emits `VPADDQ512_MASKZ`.
    fn vpaddq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPADDQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPADDQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPADDQ512_MASKZ");
        }
    }
    /// Emits `VPADDQ512RRB`.
    fn vpaddq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPADDQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPADDQ512RRB_MASK`.
    fn vpaddq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPADDQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPADDQ512RRB_MASKZ`.
    fn vpaddq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPADDQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDD128`.
    fn vpandd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDD128");
        }
    }
    /// Emits `VPANDD128_MASK`.
    fn vpandd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDD128_MASK");
        }
    }
    /// Emits `VPANDD128_MASKZ`.
    fn vpandd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDD128_MASKZ");
        }
    }
    /// Emits `VPANDD128RRB`.
    fn vpandd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDD128RRB_MASK`.
    fn vpandd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDD128RRB_MASKZ`.
    fn vpandd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDD256`.
    fn vpandd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDD256");
        }
    }
    /// Emits `VPANDD256_MASK`.
    fn vpandd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDD256_MASK");
        }
    }
    /// Emits `VPANDD256_MASKZ`.
    fn vpandd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDD256_MASKZ");
        }
    }
    /// Emits `VPANDD256RRB`.
    fn vpandd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDD256RRB_MASK`.
    fn vpandd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDD256RRB_MASKZ`.
    fn vpandd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDD512`.
    fn vpandd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDD512");
        }
    }
    /// Emits `VPANDD512_MASK`.
    fn vpandd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDD512_MASK");
        }
    }
    /// Emits `VPANDD512_MASKZ`.
    fn vpandd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDD512_MASKZ");
        }
    }
    /// Emits `VPANDD512RRB`.
    fn vpandd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDD512RRB_MASK`.
    fn vpandd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDD512RRB_MASKZ`.
    fn vpandd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDND128`.
    fn vpandnd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDND128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDND128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDND128");
        }
    }
    /// Emits `VPANDND128_MASK`.
    fn vpandnd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDND128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDND128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDND128_MASK");
        }
    }
    /// Emits `VPANDND128_MASKZ`.
    fn vpandnd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDND128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDND128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDND128_MASKZ");
        }
    }
    /// Emits `VPANDND128RRB`.
    fn vpandnd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDND128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDND128RRB_MASK`.
    fn vpandnd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDND128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDND128RRB_MASKZ`.
    fn vpandnd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDND128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDND256`.
    fn vpandnd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDND256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDND256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDND256");
        }
    }
    /// Emits `VPANDND256_MASK`.
    fn vpandnd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDND256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDND256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDND256_MASK");
        }
    }
    /// Emits `VPANDND256_MASKZ`.
    fn vpandnd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDND256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDND256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDND256_MASKZ");
        }
    }
    /// Emits `VPANDND256RRB`.
    fn vpandnd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDND256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDND256RRB_MASK`.
    fn vpandnd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDND256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDND256RRB_MASKZ`.
    fn vpandnd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDND256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDND512`.
    fn vpandnd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDND512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDND512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDND512");
        }
    }
    /// Emits `VPANDND512_MASK`.
    fn vpandnd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDND512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDND512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDND512_MASK");
        }
    }
    /// Emits `VPANDND512_MASKZ`.
    fn vpandnd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDND512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDND512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDND512_MASKZ");
        }
    }
    /// Emits `VPANDND512RRB`.
    fn vpandnd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDND512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDND512RRB_MASK`.
    fn vpandnd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDND512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDND512RRB_MASKZ`.
    fn vpandnd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDND512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDNQ128`.
    fn vpandnq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDNQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDNQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDNQ128");
        }
    }
    /// Emits `VPANDNQ128_MASK`.
    fn vpandnq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDNQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDNQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDNQ128_MASK");
        }
    }
    /// Emits `VPANDNQ128_MASKZ`.
    fn vpandnq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDNQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDNQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDNQ128_MASKZ");
        }
    }
    /// Emits `VPANDNQ128RRB`.
    fn vpandnq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDNQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDNQ128RRB_MASK`.
    fn vpandnq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDNQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDNQ128RRB_MASKZ`.
    fn vpandnq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDNQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDNQ256`.
    fn vpandnq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDNQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDNQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDNQ256");
        }
    }
    /// Emits `VPANDNQ256_MASK`.
    fn vpandnq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDNQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDNQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDNQ256_MASK");
        }
    }
    /// Emits `VPANDNQ256_MASKZ`.
    fn vpandnq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDNQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDNQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDNQ256_MASKZ");
        }
    }
    /// Emits `VPANDNQ256RRB`.
    fn vpandnq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDNQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDNQ256RRB_MASK`.
    fn vpandnq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDNQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDNQ256RRB_MASKZ`.
    fn vpandnq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDNQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDNQ512`.
    fn vpandnq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDNQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDNQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDNQ512");
        }
    }
    /// Emits `VPANDNQ512_MASK`.
    fn vpandnq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDNQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDNQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDNQ512_MASK");
        }
    }
    /// Emits `VPANDNQ512_MASKZ`.
    fn vpandnq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDNQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDNQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDNQ512_MASKZ");
        }
    }
    /// Emits `VPANDNQ512RRB`.
    fn vpandnq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDNQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDNQ512RRB_MASK`.
    fn vpandnq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDNQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDNQ512RRB_MASKZ`.
    fn vpandnq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDNQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDQ128`.
    fn vpandq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDQ128");
        }
    }
    /// Emits `VPANDQ128_MASK`.
    fn vpandq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDQ128_MASK");
        }
    }
    /// Emits `VPANDQ128_MASKZ`.
    fn vpandq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDQ128_MASKZ");
        }
    }
    /// Emits `VPANDQ128RRB`.
    fn vpandq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDQ128RRB_MASK`.
    fn vpandq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDQ128RRB_MASKZ`.
    fn vpandq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDQ256`.
    fn vpandq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDQ256");
        }
    }
    /// Emits `VPANDQ256_MASK`.
    fn vpandq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDQ256_MASK");
        }
    }
    /// Emits `VPANDQ256_MASKZ`.
    fn vpandq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDQ256_MASKZ");
        }
    }
    /// Emits `VPANDQ256RRB`.
    fn vpandq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDQ256RRB_MASK`.
    fn vpandq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDQ256RRB_MASKZ`.
    fn vpandq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDQ512`.
    fn vpandq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDQ512");
        }
    }
    /// Emits `VPANDQ512_MASK`.
    fn vpandq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDQ512_MASK");
        }
    }
    /// Emits `VPANDQ512_MASKZ`.
    fn vpandq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPANDQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPANDQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPANDQ512_MASKZ");
        }
    }
    /// Emits `VPANDQ512RRB`.
    fn vpandq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDQ512RRB_MASK`.
    fn vpandq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPANDQ512RRB_MASKZ`.
    fn vpandq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPANDQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPBLENDMD128`.
    fn vpblendmd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPBLENDMD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPBLENDMD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBLENDMD128");
        }
    }
    /// Emits `VPBLENDMD128_MASK`.
    fn vpblendmd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPBLENDMD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPBLENDMD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBLENDMD128_MASK");
        }
    }
    /// Emits `VPBLENDMD128_MASKZ`.
    fn vpblendmd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPBLENDMD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPBLENDMD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBLENDMD128_MASKZ");
        }
    }
    /// Emits `VPBLENDMD128RRB`.
    fn vpblendmd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPBLENDMD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPBLENDMD128RRB_MASK`.
    fn vpblendmd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPBLENDMD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPBLENDMD128RRB_MASKZ`.
    fn vpblendmd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPBLENDMD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPBLENDMD256`.
    fn vpblendmd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPBLENDMD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPBLENDMD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBLENDMD256");
        }
    }
    /// Emits `VPBLENDMD256_MASK`.
    fn vpblendmd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPBLENDMD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPBLENDMD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBLENDMD256_MASK");
        }
    }
    /// Emits `VPBLENDMD256_MASKZ`.
    fn vpblendmd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPBLENDMD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPBLENDMD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBLENDMD256_MASKZ");
        }
    }
    /// Emits `VPBLENDMD256RRB`.
    fn vpblendmd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPBLENDMD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPBLENDMD256RRB_MASK`.
    fn vpblendmd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPBLENDMD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPBLENDMD256RRB_MASKZ`.
    fn vpblendmd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPBLENDMD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPBLENDMD512`.
    fn vpblendmd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPBLENDMD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPBLENDMD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBLENDMD512");
        }
    }
    /// Emits `VPBLENDMD512_MASK`.
    fn vpblendmd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPBLENDMD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPBLENDMD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBLENDMD512_MASK");
        }
    }
    /// Emits `VPBLENDMD512_MASKZ`.
    fn vpblendmd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPBLENDMD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPBLENDMD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBLENDMD512_MASKZ");
        }
    }
    /// Emits `VPBLENDMD512RRB`.
    fn vpblendmd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPBLENDMD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPBLENDMD512RRB_MASK`.
    fn vpblendmd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPBLENDMD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPBLENDMD512RRB_MASKZ`.
    fn vpblendmd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPBLENDMD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPBLENDMQ128`.
    fn vpblendmq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPBLENDMQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPBLENDMQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBLENDMQ128");
        }
    }
    /// Emits `VPBLENDMQ128_MASK`.
    fn vpblendmq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPBLENDMQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPBLENDMQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBLENDMQ128_MASK");
        }
    }
    /// Emits `VPBLENDMQ128_MASKZ`.
    fn vpblendmq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPBLENDMQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPBLENDMQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBLENDMQ128_MASKZ");
        }
    }
    /// Emits `VPBLENDMQ128RRB`.
    fn vpblendmq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPBLENDMQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPBLENDMQ128RRB_MASK`.
    fn vpblendmq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPBLENDMQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPBLENDMQ128RRB_MASKZ`.
    fn vpblendmq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPBLENDMQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPBLENDMQ256`.
    fn vpblendmq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPBLENDMQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPBLENDMQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBLENDMQ256");
        }
    }
    /// Emits `VPBLENDMQ256_MASK`.
    fn vpblendmq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPBLENDMQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPBLENDMQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBLENDMQ256_MASK");
        }
    }
    /// Emits `VPBLENDMQ256_MASKZ`.
    fn vpblendmq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPBLENDMQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPBLENDMQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBLENDMQ256_MASKZ");
        }
    }
    /// Emits `VPBLENDMQ256RRB`.
    fn vpblendmq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPBLENDMQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPBLENDMQ256RRB_MASK`.
    fn vpblendmq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPBLENDMQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPBLENDMQ256RRB_MASKZ`.
    fn vpblendmq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPBLENDMQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPBLENDMQ512`.
    fn vpblendmq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPBLENDMQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPBLENDMQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBLENDMQ512");
        }
    }
    /// Emits `VPBLENDMQ512_MASK`.
    fn vpblendmq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPBLENDMQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPBLENDMQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBLENDMQ512_MASK");
        }
    }
    /// Emits `VPBLENDMQ512_MASKZ`.
    fn vpblendmq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPBLENDMQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPBLENDMQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBLENDMQ512_MASKZ");
        }
    }
    /// Emits `VPBLENDMQ512RRB`.
    fn vpblendmq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPBLENDMQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPBLENDMQ512RRB_MASK`.
    fn vpblendmq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPBLENDMQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPBLENDMQ512RRB_MASKZ`.
    fn vpblendmq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPBLENDMQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPBROADCASTD128`.
    fn vpbroadcastd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPBROADCASTD128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPBROADCASTD128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBROADCASTD128");
        }
    }
    /// Emits `VPBROADCASTD128_MASK`.
    fn vpbroadcastd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPBROADCASTD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPBROADCASTD128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBROADCASTD128_MASK");
        }
    }
    /// Emits `VPBROADCASTD128_MASKZ`.
    fn vpbroadcastd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPBROADCASTD128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPBROADCASTD128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBROADCASTD128_MASKZ");
        }
    }
    /// Emits `VPBROADCASTD256`.
    fn vpbroadcastd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPBROADCASTD256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPBROADCASTD256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBROADCASTD256");
        }
    }
    /// Emits `VPBROADCASTD256_MASK`.
    fn vpbroadcastd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPBROADCASTD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPBROADCASTD256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBROADCASTD256_MASK");
        }
    }
    /// Emits `VPBROADCASTD256_MASKZ`.
    fn vpbroadcastd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPBROADCASTD256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPBROADCASTD256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBROADCASTD256_MASKZ");
        }
    }
    /// Emits `VPBROADCASTD512`.
    fn vpbroadcastd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPBROADCASTD512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPBROADCASTD512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBROADCASTD512");
        }
    }
    /// Emits `VPBROADCASTD512_MASK`.
    fn vpbroadcastd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPBROADCASTD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPBROADCASTD512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBROADCASTD512_MASK");
        }
    }
    /// Emits `VPBROADCASTD512_MASKZ`.
    fn vpbroadcastd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPBROADCASTD512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPBROADCASTD512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBROADCASTD512_MASKZ");
        }
    }
    /// Emits `VPBROADCASTD_GP128RR`.
    fn vpbroadcastd_gp128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPBROADCASTD_GP128RR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPBROADCASTD_GP128RR_MASK`.
    fn vpbroadcastd_gp128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPBROADCASTD_GP128RR_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPBROADCASTD_GP128RR_MASKZ`.
    fn vpbroadcastd_gp128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPBROADCASTD_GP128RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPBROADCASTD_GP256RR`.
    fn vpbroadcastd_gp256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPBROADCASTD_GP256RR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPBROADCASTD_GP256RR_MASK`.
    fn vpbroadcastd_gp256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPBROADCASTD_GP256RR_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPBROADCASTD_GP256RR_MASKZ`.
    fn vpbroadcastd_gp256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPBROADCASTD_GP256RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPBROADCASTD_GP512RR`.
    fn vpbroadcastd_gp512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPBROADCASTD_GP512RR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPBROADCASTD_GP512RR_MASK`.
    fn vpbroadcastd_gp512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPBROADCASTD_GP512RR_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPBROADCASTD_GP512RR_MASKZ`.
    fn vpbroadcastd_gp512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPBROADCASTD_GP512RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPBROADCASTQ128`.
    fn vpbroadcastq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPBROADCASTQ128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPBROADCASTQ128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBROADCASTQ128");
        }
    }
    /// Emits `VPBROADCASTQ128_MASK`.
    fn vpbroadcastq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPBROADCASTQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPBROADCASTQ128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBROADCASTQ128_MASK");
        }
    }
    /// Emits `VPBROADCASTQ128_MASKZ`.
    fn vpbroadcastq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPBROADCASTQ128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPBROADCASTQ128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBROADCASTQ128_MASKZ");
        }
    }
    /// Emits `VPBROADCASTQ256`.
    fn vpbroadcastq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPBROADCASTQ256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPBROADCASTQ256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBROADCASTQ256");
        }
    }
    /// Emits `VPBROADCASTQ256_MASK`.
    fn vpbroadcastq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPBROADCASTQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPBROADCASTQ256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBROADCASTQ256_MASK");
        }
    }
    /// Emits `VPBROADCASTQ256_MASKZ`.
    fn vpbroadcastq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPBROADCASTQ256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPBROADCASTQ256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBROADCASTQ256_MASKZ");
        }
    }
    /// Emits `VPBROADCASTQ512`.
    fn vpbroadcastq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPBROADCASTQ512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPBROADCASTQ512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBROADCASTQ512");
        }
    }
    /// Emits `VPBROADCASTQ512_MASK`.
    fn vpbroadcastq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPBROADCASTQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPBROADCASTQ512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBROADCASTQ512_MASK");
        }
    }
    /// Emits `VPBROADCASTQ512_MASKZ`.
    fn vpbroadcastq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPBROADCASTQ512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPBROADCASTQ512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPBROADCASTQ512_MASKZ");
        }
    }
    /// Emits `VPBROADCASTQ_GP128RR`.
    fn vpbroadcastq_gp128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPBROADCASTQ_GP128RR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPBROADCASTQ_GP128RR_MASK`.
    fn vpbroadcastq_gp128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPBROADCASTQ_GP128RR_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPBROADCASTQ_GP128RR_MASKZ`.
    fn vpbroadcastq_gp128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPBROADCASTQ_GP128RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPBROADCASTQ_GP256RR`.
    fn vpbroadcastq_gp256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPBROADCASTQ_GP256RR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPBROADCASTQ_GP256RR_MASK`.
    fn vpbroadcastq_gp256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPBROADCASTQ_GP256RR_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPBROADCASTQ_GP256RR_MASKZ`.
    fn vpbroadcastq_gp256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPBROADCASTQ_GP256RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPBROADCASTQ_GP512RR`.
    fn vpbroadcastq_gp512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPBROADCASTQ_GP512RR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPBROADCASTQ_GP512RR_MASK`.
    fn vpbroadcastq_gp512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPBROADCASTQ_GP512RR_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPBROADCASTQ_GP512RR_MASKZ`.
    fn vpbroadcastq_gp512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPBROADCASTQ_GP512RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPCMPD128K`.
    fn vpcmpd128k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPD128KRRI, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPD128KRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPD128K");
        }
    }
    /// Emits `VPCMPD128K_MASK`.
    fn vpcmpd128k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPD128KRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPD128KRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPD128K_MASK");
        }
    }
    /// Emits `VPCMPD128KRBI`.
    fn vpcmpd128kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPD128KRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPD128KRBI_MASK`.
    fn vpcmpd128kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPD128KRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPD256K`.
    fn vpcmpd256k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPD256KRRI, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPD256KRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPD256K");
        }
    }
    /// Emits `VPCMPD256K_MASK`.
    fn vpcmpd256k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPD256KRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPD256KRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPD256K_MASK");
        }
    }
    /// Emits `VPCMPD256KRBI`.
    fn vpcmpd256kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPD256KRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPD256KRBI_MASK`.
    fn vpcmpd256kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPD256KRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPD512K`.
    fn vpcmpd512k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPD512KRRI, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPD512KRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPD512K");
        }
    }
    /// Emits `VPCMPD512K_MASK`.
    fn vpcmpd512k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPD512KRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPD512KRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPD512K_MASK");
        }
    }
    /// Emits `VPCMPD512KRBI`.
    fn vpcmpd512kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPD512KRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPD512KRBI_MASK`.
    fn vpcmpd512kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPD512KRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPEQB128K`.
    fn vpcmpeqb128k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQB128KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQB128KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQB128K");
        }
    }
    /// Emits `VPCMPEQB128K_MASK`.
    fn vpcmpeqb128k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQB128KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQB128KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQB128K_MASK");
        }
    }
    /// Emits `VPCMPEQB256K`.
    fn vpcmpeqb256k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQB256KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQB256KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQB256K");
        }
    }
    /// Emits `VPCMPEQB256K_MASK`.
    fn vpcmpeqb256k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQB256KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQB256KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQB256K_MASK");
        }
    }
    /// Emits `VPCMPEQB512K`.
    fn vpcmpeqb512k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQB512KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQB512KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQB512K");
        }
    }
    /// Emits `VPCMPEQB512K_MASK`.
    fn vpcmpeqb512k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQB512KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQB512KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQB512K_MASK");
        }
    }
    /// Emits `VPCMPEQD128K`.
    fn vpcmpeqd128k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQD128KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQD128KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQD128K");
        }
    }
    /// Emits `VPCMPEQD128K_MASK`.
    fn vpcmpeqd128k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQD128KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQD128KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQD128K_MASK");
        }
    }
    /// Emits `VPCMPEQD128KRB`.
    fn vpcmpeqd128kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPEQD128KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPEQD128KRB_MASK`.
    fn vpcmpeqd128kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPEQD128KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPEQD256K`.
    fn vpcmpeqd256k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQD256KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQD256KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQD256K");
        }
    }
    /// Emits `VPCMPEQD256K_MASK`.
    fn vpcmpeqd256k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQD256KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQD256KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQD256K_MASK");
        }
    }
    /// Emits `VPCMPEQD256KRB`.
    fn vpcmpeqd256kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPEQD256KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPEQD256KRB_MASK`.
    fn vpcmpeqd256kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPEQD256KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPEQD512K`.
    fn vpcmpeqd512k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQD512KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQD512KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQD512K");
        }
    }
    /// Emits `VPCMPEQD512K_MASK`.
    fn vpcmpeqd512k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQD512KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQD512KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQD512K_MASK");
        }
    }
    /// Emits `VPCMPEQD512KRB`.
    fn vpcmpeqd512kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPEQD512KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPEQD512KRB_MASK`.
    fn vpcmpeqd512kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPEQD512KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPEQQ128K`.
    fn vpcmpeqq128k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQQ128KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQQ128KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQQ128K");
        }
    }
    /// Emits `VPCMPEQQ128K_MASK`.
    fn vpcmpeqq128k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQQ128KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQQ128KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQQ128K_MASK");
        }
    }
    /// Emits `VPCMPEQQ128KRB`.
    fn vpcmpeqq128kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPEQQ128KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPEQQ128KRB_MASK`.
    fn vpcmpeqq128kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPEQQ128KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPEQQ256K`.
    fn vpcmpeqq256k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQQ256KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQQ256KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQQ256K");
        }
    }
    /// Emits `VPCMPEQQ256K_MASK`.
    fn vpcmpeqq256k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQQ256KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQQ256KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQQ256K_MASK");
        }
    }
    /// Emits `VPCMPEQQ256KRB`.
    fn vpcmpeqq256kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPEQQ256KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPEQQ256KRB_MASK`.
    fn vpcmpeqq256kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPEQQ256KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPEQQ512K`.
    fn vpcmpeqq512k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQQ512KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQQ512KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQQ512K");
        }
    }
    /// Emits `VPCMPEQQ512K_MASK`.
    fn vpcmpeqq512k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQQ512KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQQ512KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQQ512K_MASK");
        }
    }
    /// Emits `VPCMPEQQ512KRB`.
    fn vpcmpeqq512kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPEQQ512KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPEQQ512KRB_MASK`.
    fn vpcmpeqq512kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPEQQ512KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPEQW128K`.
    fn vpcmpeqw128k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQW128KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQW128KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQW128K");
        }
    }
    /// Emits `VPCMPEQW128K_MASK`.
    fn vpcmpeqw128k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQW128KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQW128KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQW128K_MASK");
        }
    }
    /// Emits `VPCMPEQW256K`.
    fn vpcmpeqw256k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQW256KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQW256KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQW256K");
        }
    }
    /// Emits `VPCMPEQW256K_MASK`.
    fn vpcmpeqw256k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQW256KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQW256KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQW256K_MASK");
        }
    }
    /// Emits `VPCMPEQW512K`.
    fn vpcmpeqw512k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQW512KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQW512KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQW512K");
        }
    }
    /// Emits `VPCMPEQW512K_MASK`.
    fn vpcmpeqw512k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPEQW512KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPEQW512KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPEQW512K_MASK");
        }
    }
    /// Emits `VPCMPGTB128K`.
    fn vpcmpgtb128k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTB128KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTB128KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTB128K");
        }
    }
    /// Emits `VPCMPGTB128K_MASK`.
    fn vpcmpgtb128k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTB128KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTB128KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTB128K_MASK");
        }
    }
    /// Emits `VPCMPGTB256K`.
    fn vpcmpgtb256k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTB256KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTB256KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTB256K");
        }
    }
    /// Emits `VPCMPGTB256K_MASK`.
    fn vpcmpgtb256k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTB256KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTB256KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTB256K_MASK");
        }
    }
    /// Emits `VPCMPGTB512K`.
    fn vpcmpgtb512k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTB512KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTB512KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTB512K");
        }
    }
    /// Emits `VPCMPGTB512K_MASK`.
    fn vpcmpgtb512k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTB512KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTB512KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTB512K_MASK");
        }
    }
    /// Emits `VPCMPGTD128K`.
    fn vpcmpgtd128k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTD128KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTD128KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTD128K");
        }
    }
    /// Emits `VPCMPGTD128K_MASK`.
    fn vpcmpgtd128k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTD128KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTD128KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTD128K_MASK");
        }
    }
    /// Emits `VPCMPGTD128KRB`.
    fn vpcmpgtd128kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPGTD128KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPGTD128KRB_MASK`.
    fn vpcmpgtd128kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPGTD128KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPGTD256K`.
    fn vpcmpgtd256k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTD256KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTD256KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTD256K");
        }
    }
    /// Emits `VPCMPGTD256K_MASK`.
    fn vpcmpgtd256k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTD256KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTD256KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTD256K_MASK");
        }
    }
    /// Emits `VPCMPGTD256KRB`.
    fn vpcmpgtd256kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPGTD256KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPGTD256KRB_MASK`.
    fn vpcmpgtd256kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPGTD256KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPGTD512K`.
    fn vpcmpgtd512k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTD512KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTD512KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTD512K");
        }
    }
    /// Emits `VPCMPGTD512K_MASK`.
    fn vpcmpgtd512k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTD512KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTD512KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTD512K_MASK");
        }
    }
    /// Emits `VPCMPGTD512KRB`.
    fn vpcmpgtd512kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPGTD512KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPGTD512KRB_MASK`.
    fn vpcmpgtd512kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPGTD512KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPGTQ128K`.
    fn vpcmpgtq128k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTQ128KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTQ128KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTQ128K");
        }
    }
    /// Emits `VPCMPGTQ128K_MASK`.
    fn vpcmpgtq128k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTQ128KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTQ128KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTQ128K_MASK");
        }
    }
    /// Emits `VPCMPGTQ128KRB`.
    fn vpcmpgtq128kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPGTQ128KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPGTQ128KRB_MASK`.
    fn vpcmpgtq128kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPGTQ128KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPGTQ256K`.
    fn vpcmpgtq256k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTQ256KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTQ256KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTQ256K");
        }
    }
    /// Emits `VPCMPGTQ256K_MASK`.
    fn vpcmpgtq256k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTQ256KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTQ256KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTQ256K_MASK");
        }
    }
    /// Emits `VPCMPGTQ256KRB`.
    fn vpcmpgtq256kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPGTQ256KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPGTQ256KRB_MASK`.
    fn vpcmpgtq256kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPGTQ256KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPGTQ512K`.
    fn vpcmpgtq512k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTQ512KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTQ512KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTQ512K");
        }
    }
    /// Emits `VPCMPGTQ512K_MASK`.
    fn vpcmpgtq512k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTQ512KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTQ512KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTQ512K_MASK");
        }
    }
    /// Emits `VPCMPGTQ512KRB`.
    fn vpcmpgtq512kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPGTQ512KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPGTQ512KRB_MASK`.
    fn vpcmpgtq512kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPCMPGTQ512KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPCMPGTW128K`.
    fn vpcmpgtw128k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTW128KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTW128KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTW128K");
        }
    }
    /// Emits `VPCMPGTW128K_MASK`.
    fn vpcmpgtw128k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTW128KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTW128KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTW128K_MASK");
        }
    }
    /// Emits `VPCMPGTW256K`.
    fn vpcmpgtw256k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTW256KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTW256KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTW256K");
        }
    }
    /// Emits `VPCMPGTW256K_MASK`.
    fn vpcmpgtw256k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTW256KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTW256KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTW256K_MASK");
        }
    }
    /// Emits `VPCMPGTW512K`.
    fn vpcmpgtw512k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTW512KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTW512KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTW512K");
        }
    }
    /// Emits `VPCMPGTW512K_MASK`.
    fn vpcmpgtw512k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPCMPGTW512KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPCMPGTW512KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCMPGTW512K_MASK");
        }
    }
    /// Emits `VPCMPQ128K`.
    fn vpcmpq128k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPQ128KRRI, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPQ128KRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPQ128K");
        }
    }
    /// Emits `VPCMPQ128K_MASK`.
    fn vpcmpq128k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPQ128KRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPQ128KRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPQ128K_MASK");
        }
    }
    /// Emits `VPCMPQ128KRBI`.
    fn vpcmpq128kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPQ128KRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPQ128KRBI_MASK`.
    fn vpcmpq128kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPQ128KRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPQ256K`.
    fn vpcmpq256k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPQ256KRRI, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPQ256KRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPQ256K");
        }
    }
    /// Emits `VPCMPQ256K_MASK`.
    fn vpcmpq256k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPQ256KRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPQ256KRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPQ256K_MASK");
        }
    }
    /// Emits `VPCMPQ256KRBI`.
    fn vpcmpq256kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPQ256KRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPQ256KRBI_MASK`.
    fn vpcmpq256kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPQ256KRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPQ512K`.
    fn vpcmpq512k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPQ512KRRI, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPQ512KRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPQ512K");
        }
    }
    /// Emits `VPCMPQ512K_MASK`.
    fn vpcmpq512k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPQ512KRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPQ512KRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPQ512K_MASK");
        }
    }
    /// Emits `VPCMPQ512KRBI`.
    fn vpcmpq512kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPQ512KRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPQ512KRBI_MASK`.
    fn vpcmpq512kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPQ512KRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPUD128K`.
    fn vpcmpud128k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPUD128KRRI, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPUD128KRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPUD128K");
        }
    }
    /// Emits `VPCMPUD128K_MASK`.
    fn vpcmpud128k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPUD128KRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPUD128KRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPUD128K_MASK");
        }
    }
    /// Emits `VPCMPUD128KRBI`.
    fn vpcmpud128kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPUD128KRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPUD128KRBI_MASK`.
    fn vpcmpud128kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPUD128KRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPUD256K`.
    fn vpcmpud256k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPUD256KRRI, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPUD256KRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPUD256K");
        }
    }
    /// Emits `VPCMPUD256K_MASK`.
    fn vpcmpud256k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPUD256KRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPUD256KRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPUD256K_MASK");
        }
    }
    /// Emits `VPCMPUD256KRBI`.
    fn vpcmpud256kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPUD256KRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPUD256KRBI_MASK`.
    fn vpcmpud256kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPUD256KRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPUD512K`.
    fn vpcmpud512k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPUD512KRRI, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPUD512KRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPUD512K");
        }
    }
    /// Emits `VPCMPUD512K_MASK`.
    fn vpcmpud512k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPUD512KRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPUD512KRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPUD512K_MASK");
        }
    }
    /// Emits `VPCMPUD512KRBI`.
    fn vpcmpud512kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPUD512KRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPUD512KRBI_MASK`.
    fn vpcmpud512kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPUD512KRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPUQ128K`.
    fn vpcmpuq128k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPUQ128KRRI, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPUQ128KRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPUQ128K");
        }
    }
    /// Emits `VPCMPUQ128K_MASK`.
    fn vpcmpuq128k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPUQ128KRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPUQ128KRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPUQ128K_MASK");
        }
    }
    /// Emits `VPCMPUQ128KRBI`.
    fn vpcmpuq128kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPUQ128KRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPUQ128KRBI_MASK`.
    fn vpcmpuq128kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPUQ128KRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPUQ256K`.
    fn vpcmpuq256k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPUQ256KRRI, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPUQ256KRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPUQ256K");
        }
    }
    /// Emits `VPCMPUQ256K_MASK`.
    fn vpcmpuq256k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPUQ256KRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPUQ256KRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPUQ256K_MASK");
        }
    }
    /// Emits `VPCMPUQ256KRBI`.
    fn vpcmpuq256kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPUQ256KRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPUQ256KRBI_MASK`.
    fn vpcmpuq256kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPUQ256KRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPUQ512K`.
    fn vpcmpuq512k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPUQ512KRRI, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPUQ512KRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPUQ512K");
        }
    }
    /// Emits `VPCMPUQ512K_MASK`.
    fn vpcmpuq512k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPCMPUQ512KRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPCMPUQ512KRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPCMPUQ512K_MASK");
        }
    }
    /// Emits `VPCMPUQ512KRBI`.
    fn vpcmpuq512kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPUQ512KRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCMPUQ512KRBI_MASK`.
    fn vpcmpuq512kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPCMPUQ512KRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPCOMPRESSD128`.
    fn vpcompressd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VPCOMPRESSD128MR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPCOMPRESSD128RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCOMPRESSD128");
        }
    }
    /// Emits `VPCOMPRESSD128_MASK`.
    fn vpcompressd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VPCOMPRESSD128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPCOMPRESSD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCOMPRESSD128_MASK");
        }
    }
    /// Emits `VPCOMPRESSD128RR_MASKZ`.
    fn vpcompressd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPCOMPRESSD128RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPCOMPRESSD256`.
    fn vpcompressd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VPCOMPRESSD256MR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPCOMPRESSD256RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCOMPRESSD256");
        }
    }
    /// Emits `VPCOMPRESSD256_MASK`.
    fn vpcompressd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VPCOMPRESSD256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPCOMPRESSD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCOMPRESSD256_MASK");
        }
    }
    /// Emits `VPCOMPRESSD256RR_MASKZ`.
    fn vpcompressd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPCOMPRESSD256RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPCOMPRESSD512`.
    fn vpcompressd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VPCOMPRESSD512MR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPCOMPRESSD512RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCOMPRESSD512");
        }
    }
    /// Emits `VPCOMPRESSD512_MASK`.
    fn vpcompressd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VPCOMPRESSD512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPCOMPRESSD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCOMPRESSD512_MASK");
        }
    }
    /// Emits `VPCOMPRESSD512RR_MASKZ`.
    fn vpcompressd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPCOMPRESSD512RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPCOMPRESSQ128`.
    fn vpcompressq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VPCOMPRESSQ128MR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPCOMPRESSQ128RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCOMPRESSQ128");
        }
    }
    /// Emits `VPCOMPRESSQ128_MASK`.
    fn vpcompressq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VPCOMPRESSQ128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPCOMPRESSQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCOMPRESSQ128_MASK");
        }
    }
    /// Emits `VPCOMPRESSQ128RR_MASKZ`.
    fn vpcompressq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPCOMPRESSQ128RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPCOMPRESSQ256`.
    fn vpcompressq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VPCOMPRESSQ256MR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPCOMPRESSQ256RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCOMPRESSQ256");
        }
    }
    /// Emits `VPCOMPRESSQ256_MASK`.
    fn vpcompressq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VPCOMPRESSQ256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPCOMPRESSQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCOMPRESSQ256_MASK");
        }
    }
    /// Emits `VPCOMPRESSQ256RR_MASKZ`.
    fn vpcompressq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPCOMPRESSQ256RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPCOMPRESSQ512`.
    fn vpcompressq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VPCOMPRESSQ512MR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPCOMPRESSQ512RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCOMPRESSQ512");
        }
    }
    /// Emits `VPCOMPRESSQ512_MASK`.
    fn vpcompressq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_mem() && op1.is_vec() {
            self.emit(VPCOMPRESSQ512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPCOMPRESSQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPCOMPRESSQ512_MASK");
        }
    }
    /// Emits `VPCOMPRESSQ512RR_MASKZ`.
    fn vpcompressq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPCOMPRESSQ512RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPERMD256`.
    fn vpermd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMD256");
        }
    }
    /// Emits `VPERMD256_MASK`.
    fn vpermd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMD256_MASK");
        }
    }
    /// Emits `VPERMD256_MASKZ`.
    fn vpermd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMD256_MASKZ");
        }
    }
    /// Emits `VPERMD256RRB`.
    fn vpermd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMD256RRB_MASK`.
    fn vpermd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMD256RRB_MASKZ`.
    fn vpermd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMD512`.
    fn vpermd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMD512");
        }
    }
    /// Emits `VPERMD512_MASK`.
    fn vpermd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMD512_MASK");
        }
    }
    /// Emits `VPERMD512_MASKZ`.
    fn vpermd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMD512_MASKZ");
        }
    }
    /// Emits `VPERMD512RRB`.
    fn vpermd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMD512RRB_MASK`.
    fn vpermd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMD512RRB_MASKZ`.
    fn vpermd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2D128`.
    fn vpermi2d128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2D128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2D128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2D128");
        }
    }
    /// Emits `VPERMI2D128_MASK`.
    fn vpermi2d128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2D128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2D128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2D128_MASK");
        }
    }
    /// Emits `VPERMI2D128_MASKZ`.
    fn vpermi2d128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2D128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2D128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2D128_MASKZ");
        }
    }
    /// Emits `VPERMI2D128RRB`.
    fn vpermi2d128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2D128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2D128RRB_MASK`.
    fn vpermi2d128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2D128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2D128RRB_MASKZ`.
    fn vpermi2d128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2D128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2D256`.
    fn vpermi2d256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2D256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2D256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2D256");
        }
    }
    /// Emits `VPERMI2D256_MASK`.
    fn vpermi2d256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2D256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2D256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2D256_MASK");
        }
    }
    /// Emits `VPERMI2D256_MASKZ`.
    fn vpermi2d256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2D256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2D256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2D256_MASKZ");
        }
    }
    /// Emits `VPERMI2D256RRB`.
    fn vpermi2d256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2D256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2D256RRB_MASK`.
    fn vpermi2d256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2D256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2D256RRB_MASKZ`.
    fn vpermi2d256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2D256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2D512`.
    fn vpermi2d512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2D512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2D512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2D512");
        }
    }
    /// Emits `VPERMI2D512_MASK`.
    fn vpermi2d512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2D512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2D512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2D512_MASK");
        }
    }
    /// Emits `VPERMI2D512_MASKZ`.
    fn vpermi2d512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2D512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2D512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2D512_MASKZ");
        }
    }
    /// Emits `VPERMI2D512RRB`.
    fn vpermi2d512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2D512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2D512RRB_MASK`.
    fn vpermi2d512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2D512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2D512RRB_MASKZ`.
    fn vpermi2d512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2D512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2PD128`.
    fn vpermi2pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2PD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2PD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2PD128");
        }
    }
    /// Emits `VPERMI2PD128_MASK`.
    fn vpermi2pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2PD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2PD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2PD128_MASK");
        }
    }
    /// Emits `VPERMI2PD128_MASKZ`.
    fn vpermi2pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2PD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2PD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2PD128_MASKZ");
        }
    }
    /// Emits `VPERMI2PD128RRB`.
    fn vpermi2pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2PD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2PD128RRB_MASK`.
    fn vpermi2pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2PD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2PD128RRB_MASKZ`.
    fn vpermi2pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2PD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2PD256`.
    fn vpermi2pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2PD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2PD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2PD256");
        }
    }
    /// Emits `VPERMI2PD256_MASK`.
    fn vpermi2pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2PD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2PD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2PD256_MASK");
        }
    }
    /// Emits `VPERMI2PD256_MASKZ`.
    fn vpermi2pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2PD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2PD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2PD256_MASKZ");
        }
    }
    /// Emits `VPERMI2PD256RRB`.
    fn vpermi2pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2PD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2PD256RRB_MASK`.
    fn vpermi2pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2PD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2PD256RRB_MASKZ`.
    fn vpermi2pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2PD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2PD512`.
    fn vpermi2pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2PD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2PD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2PD512");
        }
    }
    /// Emits `VPERMI2PD512_MASK`.
    fn vpermi2pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2PD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2PD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2PD512_MASK");
        }
    }
    /// Emits `VPERMI2PD512_MASKZ`.
    fn vpermi2pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2PD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2PD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2PD512_MASKZ");
        }
    }
    /// Emits `VPERMI2PD512RRB`.
    fn vpermi2pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2PD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2PD512RRB_MASK`.
    fn vpermi2pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2PD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2PD512RRB_MASKZ`.
    fn vpermi2pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2PD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2PS128`.
    fn vpermi2ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2PS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2PS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2PS128");
        }
    }
    /// Emits `VPERMI2PS128_MASK`.
    fn vpermi2ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2PS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2PS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2PS128_MASK");
        }
    }
    /// Emits `VPERMI2PS128_MASKZ`.
    fn vpermi2ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2PS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2PS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2PS128_MASKZ");
        }
    }
    /// Emits `VPERMI2PS128RRB`.
    fn vpermi2ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2PS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2PS128RRB_MASK`.
    fn vpermi2ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2PS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2PS128RRB_MASKZ`.
    fn vpermi2ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2PS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2PS256`.
    fn vpermi2ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2PS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2PS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2PS256");
        }
    }
    /// Emits `VPERMI2PS256_MASK`.
    fn vpermi2ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2PS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2PS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2PS256_MASK");
        }
    }
    /// Emits `VPERMI2PS256_MASKZ`.
    fn vpermi2ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2PS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2PS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2PS256_MASKZ");
        }
    }
    /// Emits `VPERMI2PS256RRB`.
    fn vpermi2ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2PS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2PS256RRB_MASK`.
    fn vpermi2ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2PS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2PS256RRB_MASKZ`.
    fn vpermi2ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2PS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2PS512`.
    fn vpermi2ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2PS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2PS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2PS512");
        }
    }
    /// Emits `VPERMI2PS512_MASK`.
    fn vpermi2ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2PS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2PS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2PS512_MASK");
        }
    }
    /// Emits `VPERMI2PS512_MASKZ`.
    fn vpermi2ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2PS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2PS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2PS512_MASKZ");
        }
    }
    /// Emits `VPERMI2PS512RRB`.
    fn vpermi2ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2PS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2PS512RRB_MASK`.
    fn vpermi2ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2PS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2PS512RRB_MASKZ`.
    fn vpermi2ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2PS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2Q128`.
    fn vpermi2q128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2Q128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2Q128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2Q128");
        }
    }
    /// Emits `VPERMI2Q128_MASK`.
    fn vpermi2q128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2Q128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2Q128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2Q128_MASK");
        }
    }
    /// Emits `VPERMI2Q128_MASKZ`.
    fn vpermi2q128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2Q128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2Q128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2Q128_MASKZ");
        }
    }
    /// Emits `VPERMI2Q128RRB`.
    fn vpermi2q128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2Q128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2Q128RRB_MASK`.
    fn vpermi2q128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2Q128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2Q128RRB_MASKZ`.
    fn vpermi2q128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2Q128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2Q256`.
    fn vpermi2q256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2Q256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2Q256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2Q256");
        }
    }
    /// Emits `VPERMI2Q256_MASK`.
    fn vpermi2q256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2Q256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2Q256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2Q256_MASK");
        }
    }
    /// Emits `VPERMI2Q256_MASKZ`.
    fn vpermi2q256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2Q256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2Q256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2Q256_MASKZ");
        }
    }
    /// Emits `VPERMI2Q256RRB`.
    fn vpermi2q256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2Q256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2Q256RRB_MASK`.
    fn vpermi2q256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2Q256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2Q256RRB_MASKZ`.
    fn vpermi2q256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2Q256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2Q512`.
    fn vpermi2q512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2Q512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2Q512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2Q512");
        }
    }
    /// Emits `VPERMI2Q512_MASK`.
    fn vpermi2q512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2Q512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2Q512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2Q512_MASK");
        }
    }
    /// Emits `VPERMI2Q512_MASKZ`.
    fn vpermi2q512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMI2Q512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMI2Q512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMI2Q512_MASKZ");
        }
    }
    /// Emits `VPERMI2Q512RRB`.
    fn vpermi2q512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2Q512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2Q512RRB_MASK`.
    fn vpermi2q512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2Q512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMI2Q512RRB_MASKZ`.
    fn vpermi2q512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMI2Q512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMILPD128`.
    fn vpermilpd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMILPD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPD128RRM, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMILPD128RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPD128RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPD128");
        }
    }
    /// Emits `VPERMILPD128_MASK`.
    fn vpermilpd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMILPD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPD128RRM_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMILPD128RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPD128RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPD128_MASK");
        }
    }
    /// Emits `VPERMILPD128_MASKZ`.
    fn vpermilpd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMILPD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMILPD128RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPD128RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPD128_MASKZ");
        }
    }
    /// Emits `VPERMILPD128B`.
    fn vpermilpd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPD128RRB, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPD128RBI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPD128B");
        }
    }
    /// Emits `VPERMILPD128B_MASK`.
    fn vpermilpd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPD128RRB_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPD128RBI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPD128B_MASK");
        }
    }
    /// Emits `VPERMILPD128B_MASKZ`.
    fn vpermilpd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPD128RRB_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPD128RBI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPD128B_MASKZ");
        }
    }
    /// Emits `VPERMILPD256`.
    fn vpermilpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMILPD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPD256RRM, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMILPD256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPD256RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPD256");
        }
    }
    /// Emits `VPERMILPD256_MASK`.
    fn vpermilpd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMILPD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPD256RRM_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMILPD256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPD256RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPD256_MASK");
        }
    }
    /// Emits `VPERMILPD256_MASKZ`.
    fn vpermilpd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMILPD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMILPD256RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPD256RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPD256_MASKZ");
        }
    }
    /// Emits `VPERMILPD256B`.
    fn vpermilpd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPD256RRB, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPD256RBI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPD256B");
        }
    }
    /// Emits `VPERMILPD256B_MASK`.
    fn vpermilpd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPD256RRB_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPD256RBI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPD256B_MASK");
        }
    }
    /// Emits `VPERMILPD256B_MASKZ`.
    fn vpermilpd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPD256RRB_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPD256RBI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPD256B_MASKZ");
        }
    }
    /// Emits `VPERMILPD512`.
    fn vpermilpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMILPD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPD512RRM, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMILPD512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPD512RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPD512");
        }
    }
    /// Emits `VPERMILPD512_MASK`.
    fn vpermilpd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMILPD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPD512RRM_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMILPD512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPD512RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPD512_MASK");
        }
    }
    /// Emits `VPERMILPD512_MASKZ`.
    fn vpermilpd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMILPD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMILPD512RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPD512RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPD512_MASKZ");
        }
    }
    /// Emits `VPERMILPD512B`.
    fn vpermilpd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPD512RRB, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPD512RBI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPD512B");
        }
    }
    /// Emits `VPERMILPD512B_MASK`.
    fn vpermilpd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPD512RRB_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPD512RBI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPD512B_MASK");
        }
    }
    /// Emits `VPERMILPD512B_MASKZ`.
    fn vpermilpd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPD512RRB_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPD512RBI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPD512B_MASKZ");
        }
    }
    /// Emits `VPERMILPS128`.
    fn vpermilps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMILPS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPS128RRM, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMILPS128RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPS128RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPS128");
        }
    }
    /// Emits `VPERMILPS128_MASK`.
    fn vpermilps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMILPS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPS128RRM_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMILPS128RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPS128RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPS128_MASK");
        }
    }
    /// Emits `VPERMILPS128_MASKZ`.
    fn vpermilps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMILPS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMILPS128RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPS128RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPS128_MASKZ");
        }
    }
    /// Emits `VPERMILPS128B`.
    fn vpermilps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPS128RRB, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPS128RBI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPS128B");
        }
    }
    /// Emits `VPERMILPS128B_MASK`.
    fn vpermilps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPS128RRB_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPS128RBI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPS128B_MASK");
        }
    }
    /// Emits `VPERMILPS128B_MASKZ`.
    fn vpermilps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPS128RRB_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPS128RBI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPS128B_MASKZ");
        }
    }
    /// Emits `VPERMILPS256`.
    fn vpermilps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMILPS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPS256RRM, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMILPS256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPS256RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPS256");
        }
    }
    /// Emits `VPERMILPS256_MASK`.
    fn vpermilps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMILPS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPS256RRM_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMILPS256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPS256RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPS256_MASK");
        }
    }
    /// Emits `VPERMILPS256_MASKZ`.
    fn vpermilps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMILPS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMILPS256RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPS256RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPS256_MASKZ");
        }
    }
    /// Emits `VPERMILPS256B`.
    fn vpermilps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPS256RRB, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPS256RBI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPS256B");
        }
    }
    /// Emits `VPERMILPS256B_MASK`.
    fn vpermilps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPS256RRB_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPS256RBI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPS256B_MASK");
        }
    }
    /// Emits `VPERMILPS256B_MASKZ`.
    fn vpermilps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPS256RRB_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPS256RBI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPS256B_MASKZ");
        }
    }
    /// Emits `VPERMILPS512`.
    fn vpermilps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMILPS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPS512RRM, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMILPS512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPS512RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPS512");
        }
    }
    /// Emits `VPERMILPS512_MASK`.
    fn vpermilps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMILPS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPS512RRM_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMILPS512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPS512RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPS512_MASK");
        }
    }
    /// Emits `VPERMILPS512_MASKZ`.
    fn vpermilps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMILPS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMILPS512RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPS512RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPS512_MASKZ");
        }
    }
    /// Emits `VPERMILPS512B`.
    fn vpermilps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPS512RRB, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPS512RBI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPS512B");
        }
    }
    /// Emits `VPERMILPS512B_MASK`.
    fn vpermilps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPS512RRB_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPS512RBI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPS512B_MASK");
        }
    }
    /// Emits `VPERMILPS512B_MASKZ`.
    fn vpermilps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMILPS512RRB_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMILPS512RBI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMILPS512B_MASKZ");
        }
    }
    /// Emits `VPERMPD256`.
    fn vpermpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMPD256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMPD256RMI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMPD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMPD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMPD256");
        }
    }
    /// Emits `VPERMPD256_MASK`.
    fn vpermpd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMPD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMPD256RRM_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMPD256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMPD256RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMPD256_MASK");
        }
    }
    /// Emits `VPERMPD256_MASKZ`.
    fn vpermpd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMPD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMPD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMPD256RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMPD256RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMPD256_MASKZ");
        }
    }
    /// Emits `VPERMPD256B`.
    fn vpermpd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMPD256RRB, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMPD256RBI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMPD256B");
        }
    }
    /// Emits `VPERMPD256B_MASK`.
    fn vpermpd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMPD256RRB_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMPD256RBI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMPD256B_MASK");
        }
    }
    /// Emits `VPERMPD256B_MASKZ`.
    fn vpermpd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMPD256RRB_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMPD256RBI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMPD256B_MASKZ");
        }
    }
    /// Emits `VPERMPD512`.
    fn vpermpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMPD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMPD512RRM, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMPD512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMPD512RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMPD512");
        }
    }
    /// Emits `VPERMPD512_MASK`.
    fn vpermpd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMPD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMPD512RRM_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMPD512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMPD512RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMPD512_MASK");
        }
    }
    /// Emits `VPERMPD512_MASKZ`.
    fn vpermpd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMPD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMPD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMPD512RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMPD512RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMPD512_MASKZ");
        }
    }
    /// Emits `VPERMPD512B`.
    fn vpermpd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMPD512RRB, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMPD512RBI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMPD512B");
        }
    }
    /// Emits `VPERMPD512B_MASK`.
    fn vpermpd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMPD512RRB_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMPD512RBI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMPD512B_MASK");
        }
    }
    /// Emits `VPERMPD512B_MASKZ`.
    fn vpermpd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMPD512RRB_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMPD512RBI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMPD512B_MASKZ");
        }
    }
    /// Emits `VPERMPS256`.
    fn vpermps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMPS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMPS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMPS256");
        }
    }
    /// Emits `VPERMPS256_MASK`.
    fn vpermps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMPS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMPS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMPS256_MASK");
        }
    }
    /// Emits `VPERMPS256_MASKZ`.
    fn vpermps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMPS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMPS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMPS256_MASKZ");
        }
    }
    /// Emits `VPERMPS256RRB`.
    fn vpermps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMPS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMPS256RRB_MASK`.
    fn vpermps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMPS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMPS256RRB_MASKZ`.
    fn vpermps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMPS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMPS512`.
    fn vpermps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMPS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMPS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMPS512");
        }
    }
    /// Emits `VPERMPS512_MASK`.
    fn vpermps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMPS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMPS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMPS512_MASK");
        }
    }
    /// Emits `VPERMPS512_MASKZ`.
    fn vpermps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMPS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMPS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMPS512_MASKZ");
        }
    }
    /// Emits `VPERMPS512RRB`.
    fn vpermps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMPS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMPS512RRB_MASK`.
    fn vpermps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMPS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMPS512RRB_MASKZ`.
    fn vpermps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMPS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMQ256`.
    fn vpermq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMQ256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMQ256RMI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMQ256");
        }
    }
    /// Emits `VPERMQ256_MASK`.
    fn vpermq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMQ256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMQ256RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMQ256_MASK");
        }
    }
    /// Emits `VPERMQ256_MASKZ`.
    fn vpermq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMQ256RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMQ256RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMQ256_MASKZ");
        }
    }
    /// Emits `VPERMQ256B`.
    fn vpermq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMQ256RRB, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMQ256RBI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMQ256B");
        }
    }
    /// Emits `VPERMQ256B_MASK`.
    fn vpermq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMQ256RRB_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMQ256RBI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMQ256B_MASK");
        }
    }
    /// Emits `VPERMQ256B_MASKZ`.
    fn vpermq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMQ256RRB_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMQ256RBI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMQ256B_MASKZ");
        }
    }
    /// Emits `VPERMQ512`.
    fn vpermq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMQ512RRM, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMQ512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMQ512RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMQ512");
        }
    }
    /// Emits `VPERMQ512_MASK`.
    fn vpermq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMQ512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMQ512RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMQ512_MASK");
        }
    }
    /// Emits `VPERMQ512_MASKZ`.
    fn vpermq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPERMQ512RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMQ512RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMQ512_MASKZ");
        }
    }
    /// Emits `VPERMQ512B`.
    fn vpermq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMQ512RRB, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMQ512RBI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMQ512B");
        }
    }
    /// Emits `VPERMQ512B_MASK`.
    fn vpermq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMQ512RRB_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMQ512RBI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMQ512B_MASK");
        }
    }
    /// Emits `VPERMQ512B_MASKZ`.
    fn vpermq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMQ512RRB_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPERMQ512RBI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMQ512B_MASKZ");
        }
    }
    /// Emits `VPERMT2D128`.
    fn vpermt2d128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2D128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2D128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2D128");
        }
    }
    /// Emits `VPERMT2D128_MASK`.
    fn vpermt2d128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2D128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2D128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2D128_MASK");
        }
    }
    /// Emits `VPERMT2D128_MASKZ`.
    fn vpermt2d128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2D128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2D128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2D128_MASKZ");
        }
    }
    /// Emits `VPERMT2D128RRB`.
    fn vpermt2d128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2D128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2D128RRB_MASK`.
    fn vpermt2d128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2D128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2D128RRB_MASKZ`.
    fn vpermt2d128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2D128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2D256`.
    fn vpermt2d256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2D256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2D256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2D256");
        }
    }
    /// Emits `VPERMT2D256_MASK`.
    fn vpermt2d256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2D256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2D256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2D256_MASK");
        }
    }
    /// Emits `VPERMT2D256_MASKZ`.
    fn vpermt2d256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2D256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2D256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2D256_MASKZ");
        }
    }
    /// Emits `VPERMT2D256RRB`.
    fn vpermt2d256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2D256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2D256RRB_MASK`.
    fn vpermt2d256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2D256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2D256RRB_MASKZ`.
    fn vpermt2d256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2D256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2D512`.
    fn vpermt2d512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2D512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2D512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2D512");
        }
    }
    /// Emits `VPERMT2D512_MASK`.
    fn vpermt2d512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2D512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2D512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2D512_MASK");
        }
    }
    /// Emits `VPERMT2D512_MASKZ`.
    fn vpermt2d512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2D512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2D512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2D512_MASKZ");
        }
    }
    /// Emits `VPERMT2D512RRB`.
    fn vpermt2d512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2D512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2D512RRB_MASK`.
    fn vpermt2d512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2D512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2D512RRB_MASKZ`.
    fn vpermt2d512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2D512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2PD128`.
    fn vpermt2pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2PD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2PD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2PD128");
        }
    }
    /// Emits `VPERMT2PD128_MASK`.
    fn vpermt2pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2PD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2PD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2PD128_MASK");
        }
    }
    /// Emits `VPERMT2PD128_MASKZ`.
    fn vpermt2pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2PD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2PD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2PD128_MASKZ");
        }
    }
    /// Emits `VPERMT2PD128RRB`.
    fn vpermt2pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2PD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2PD128RRB_MASK`.
    fn vpermt2pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2PD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2PD128RRB_MASKZ`.
    fn vpermt2pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2PD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2PD256`.
    fn vpermt2pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2PD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2PD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2PD256");
        }
    }
    /// Emits `VPERMT2PD256_MASK`.
    fn vpermt2pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2PD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2PD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2PD256_MASK");
        }
    }
    /// Emits `VPERMT2PD256_MASKZ`.
    fn vpermt2pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2PD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2PD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2PD256_MASKZ");
        }
    }
    /// Emits `VPERMT2PD256RRB`.
    fn vpermt2pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2PD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2PD256RRB_MASK`.
    fn vpermt2pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2PD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2PD256RRB_MASKZ`.
    fn vpermt2pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2PD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2PD512`.
    fn vpermt2pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2PD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2PD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2PD512");
        }
    }
    /// Emits `VPERMT2PD512_MASK`.
    fn vpermt2pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2PD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2PD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2PD512_MASK");
        }
    }
    /// Emits `VPERMT2PD512_MASKZ`.
    fn vpermt2pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2PD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2PD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2PD512_MASKZ");
        }
    }
    /// Emits `VPERMT2PD512RRB`.
    fn vpermt2pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2PD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2PD512RRB_MASK`.
    fn vpermt2pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2PD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2PD512RRB_MASKZ`.
    fn vpermt2pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2PD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2PS128`.
    fn vpermt2ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2PS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2PS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2PS128");
        }
    }
    /// Emits `VPERMT2PS128_MASK`.
    fn vpermt2ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2PS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2PS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2PS128_MASK");
        }
    }
    /// Emits `VPERMT2PS128_MASKZ`.
    fn vpermt2ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2PS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2PS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2PS128_MASKZ");
        }
    }
    /// Emits `VPERMT2PS128RRB`.
    fn vpermt2ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2PS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2PS128RRB_MASK`.
    fn vpermt2ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2PS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2PS128RRB_MASKZ`.
    fn vpermt2ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2PS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2PS256`.
    fn vpermt2ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2PS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2PS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2PS256");
        }
    }
    /// Emits `VPERMT2PS256_MASK`.
    fn vpermt2ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2PS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2PS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2PS256_MASK");
        }
    }
    /// Emits `VPERMT2PS256_MASKZ`.
    fn vpermt2ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2PS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2PS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2PS256_MASKZ");
        }
    }
    /// Emits `VPERMT2PS256RRB`.
    fn vpermt2ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2PS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2PS256RRB_MASK`.
    fn vpermt2ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2PS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2PS256RRB_MASKZ`.
    fn vpermt2ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2PS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2PS512`.
    fn vpermt2ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2PS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2PS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2PS512");
        }
    }
    /// Emits `VPERMT2PS512_MASK`.
    fn vpermt2ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2PS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2PS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2PS512_MASK");
        }
    }
    /// Emits `VPERMT2PS512_MASKZ`.
    fn vpermt2ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2PS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2PS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2PS512_MASKZ");
        }
    }
    /// Emits `VPERMT2PS512RRB`.
    fn vpermt2ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2PS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2PS512RRB_MASK`.
    fn vpermt2ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2PS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2PS512RRB_MASKZ`.
    fn vpermt2ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2PS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2Q128`.
    fn vpermt2q128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2Q128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2Q128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2Q128");
        }
    }
    /// Emits `VPERMT2Q128_MASK`.
    fn vpermt2q128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2Q128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2Q128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2Q128_MASK");
        }
    }
    /// Emits `VPERMT2Q128_MASKZ`.
    fn vpermt2q128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2Q128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2Q128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2Q128_MASKZ");
        }
    }
    /// Emits `VPERMT2Q128RRB`.
    fn vpermt2q128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2Q128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2Q128RRB_MASK`.
    fn vpermt2q128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2Q128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2Q128RRB_MASKZ`.
    fn vpermt2q128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2Q128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2Q256`.
    fn vpermt2q256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2Q256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2Q256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2Q256");
        }
    }
    /// Emits `VPERMT2Q256_MASK`.
    fn vpermt2q256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2Q256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2Q256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2Q256_MASK");
        }
    }
    /// Emits `VPERMT2Q256_MASKZ`.
    fn vpermt2q256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2Q256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2Q256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2Q256_MASKZ");
        }
    }
    /// Emits `VPERMT2Q256RRB`.
    fn vpermt2q256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2Q256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2Q256RRB_MASK`.
    fn vpermt2q256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2Q256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2Q256RRB_MASKZ`.
    fn vpermt2q256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2Q256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2Q512`.
    fn vpermt2q512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2Q512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2Q512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2Q512");
        }
    }
    /// Emits `VPERMT2Q512_MASK`.
    fn vpermt2q512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2Q512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2Q512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2Q512_MASK");
        }
    }
    /// Emits `VPERMT2Q512_MASKZ`.
    fn vpermt2q512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPERMT2Q512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPERMT2Q512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPERMT2Q512_MASKZ");
        }
    }
    /// Emits `VPERMT2Q512RRB`.
    fn vpermt2q512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2Q512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2Q512RRB_MASK`.
    fn vpermt2q512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2Q512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPERMT2Q512RRB_MASKZ`.
    fn vpermt2q512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPERMT2Q512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPEXPANDD128`.
    fn vpexpandd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VPEXPANDD128RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPEXPANDD128RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPEXPANDD128");
        }
    }
    /// Emits `VPEXPANDD128_MASK`.
    fn vpexpandd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VPEXPANDD128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPEXPANDD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPEXPANDD128_MASK");
        }
    }
    /// Emits `VPEXPANDD128_MASKZ`.
    fn vpexpandd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VPEXPANDD128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPEXPANDD128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPEXPANDD128_MASKZ");
        }
    }
    /// Emits `VPEXPANDD256`.
    fn vpexpandd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VPEXPANDD256RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPEXPANDD256RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPEXPANDD256");
        }
    }
    /// Emits `VPEXPANDD256_MASK`.
    fn vpexpandd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VPEXPANDD256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPEXPANDD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPEXPANDD256_MASK");
        }
    }
    /// Emits `VPEXPANDD256_MASKZ`.
    fn vpexpandd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VPEXPANDD256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPEXPANDD256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPEXPANDD256_MASKZ");
        }
    }
    /// Emits `VPEXPANDD512`.
    fn vpexpandd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VPEXPANDD512RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPEXPANDD512RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPEXPANDD512");
        }
    }
    /// Emits `VPEXPANDD512_MASK`.
    fn vpexpandd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VPEXPANDD512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPEXPANDD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPEXPANDD512_MASK");
        }
    }
    /// Emits `VPEXPANDD512_MASKZ`.
    fn vpexpandd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VPEXPANDD512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPEXPANDD512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPEXPANDD512_MASKZ");
        }
    }
    /// Emits `VPEXPANDQ128`.
    fn vpexpandq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VPEXPANDQ128RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPEXPANDQ128RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPEXPANDQ128");
        }
    }
    /// Emits `VPEXPANDQ128_MASK`.
    fn vpexpandq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VPEXPANDQ128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPEXPANDQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPEXPANDQ128_MASK");
        }
    }
    /// Emits `VPEXPANDQ128_MASKZ`.
    fn vpexpandq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VPEXPANDQ128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPEXPANDQ128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPEXPANDQ128_MASKZ");
        }
    }
    /// Emits `VPEXPANDQ256`.
    fn vpexpandq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VPEXPANDQ256RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPEXPANDQ256RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPEXPANDQ256");
        }
    }
    /// Emits `VPEXPANDQ256_MASK`.
    fn vpexpandq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VPEXPANDQ256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPEXPANDQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPEXPANDQ256_MASK");
        }
    }
    /// Emits `VPEXPANDQ256_MASKZ`.
    fn vpexpandq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VPEXPANDQ256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPEXPANDQ256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPEXPANDQ256_MASKZ");
        }
    }
    /// Emits `VPEXPANDQ512`.
    fn vpexpandq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VPEXPANDQ512RM, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPEXPANDQ512RR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPEXPANDQ512");
        }
    }
    /// Emits `VPEXPANDQ512_MASK`.
    fn vpexpandq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VPEXPANDQ512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPEXPANDQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPEXPANDQ512_MASK");
        }
    }
    /// Emits `VPEXPANDQ512_MASKZ`.
    fn vpexpandq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_mem() {
            self.emit(VPEXPANDQ512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_vec() {
            self.emit(VPEXPANDQ512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPEXPANDQ512_MASKZ");
        }
    }
    /// Emits `VPGATHERDD128RM`.
    fn vpgatherdd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPGATHERDD128RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPGATHERDD256RM`.
    fn vpgatherdd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPGATHERDD256RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPGATHERDD512RM`.
    fn vpgatherdd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPGATHERDD512RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPGATHERDQ128RM`.
    fn vpgatherdq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPGATHERDQ128RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPGATHERDQ256RM`.
    fn vpgatherdq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPGATHERDQ256RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPGATHERDQ512RM`.
    fn vpgatherdq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPGATHERDQ512RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPGATHERQD128RM`.
    fn vpgatherqd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPGATHERQD128RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPGATHERQD256RM`.
    fn vpgatherqd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPGATHERQD256RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPGATHERQD512RM`.
    fn vpgatherqd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPGATHERQD512RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPGATHERQQ128RM`.
    fn vpgatherqq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPGATHERQQ128RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPGATHERQQ256RM`.
    fn vpgatherqq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPGATHERQQ256RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPGATHERQQ512RM`.
    fn vpgatherqq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPGATHERQQ512RM, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMAXSD128`.
    fn vpmaxsd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXSD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXSD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXSD128");
        }
    }
    /// Emits `VPMAXSD128_MASK`.
    fn vpmaxsd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXSD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXSD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXSD128_MASK");
        }
    }
    /// Emits `VPMAXSD128_MASKZ`.
    fn vpmaxsd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXSD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXSD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXSD128_MASKZ");
        }
    }
    /// Emits `VPMAXSD128RRB`.
    fn vpmaxsd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXSD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXSD128RRB_MASK`.
    fn vpmaxsd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXSD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXSD128RRB_MASKZ`.
    fn vpmaxsd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXSD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXSD256`.
    fn vpmaxsd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXSD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXSD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXSD256");
        }
    }
    /// Emits `VPMAXSD256_MASK`.
    fn vpmaxsd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXSD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXSD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXSD256_MASK");
        }
    }
    /// Emits `VPMAXSD256_MASKZ`.
    fn vpmaxsd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXSD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXSD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXSD256_MASKZ");
        }
    }
    /// Emits `VPMAXSD256RRB`.
    fn vpmaxsd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXSD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXSD256RRB_MASK`.
    fn vpmaxsd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXSD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXSD256RRB_MASKZ`.
    fn vpmaxsd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXSD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXSD512`.
    fn vpmaxsd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXSD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXSD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXSD512");
        }
    }
    /// Emits `VPMAXSD512_MASK`.
    fn vpmaxsd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXSD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXSD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXSD512_MASK");
        }
    }
    /// Emits `VPMAXSD512_MASKZ`.
    fn vpmaxsd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXSD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXSD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXSD512_MASKZ");
        }
    }
    /// Emits `VPMAXSD512RRB`.
    fn vpmaxsd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXSD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXSD512RRB_MASK`.
    fn vpmaxsd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXSD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXSD512RRB_MASKZ`.
    fn vpmaxsd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXSD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXSQ128`.
    fn vpmaxsq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXSQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXSQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXSQ128");
        }
    }
    /// Emits `VPMAXSQ128_MASK`.
    fn vpmaxsq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXSQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXSQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXSQ128_MASK");
        }
    }
    /// Emits `VPMAXSQ128_MASKZ`.
    fn vpmaxsq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXSQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXSQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXSQ128_MASKZ");
        }
    }
    /// Emits `VPMAXSQ128RRB`.
    fn vpmaxsq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXSQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXSQ128RRB_MASK`.
    fn vpmaxsq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXSQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXSQ128RRB_MASKZ`.
    fn vpmaxsq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXSQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXSQ256`.
    fn vpmaxsq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXSQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXSQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXSQ256");
        }
    }
    /// Emits `VPMAXSQ256_MASK`.
    fn vpmaxsq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXSQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXSQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXSQ256_MASK");
        }
    }
    /// Emits `VPMAXSQ256_MASKZ`.
    fn vpmaxsq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXSQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXSQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXSQ256_MASKZ");
        }
    }
    /// Emits `VPMAXSQ256RRB`.
    fn vpmaxsq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXSQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXSQ256RRB_MASK`.
    fn vpmaxsq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXSQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXSQ256RRB_MASKZ`.
    fn vpmaxsq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXSQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXSQ512`.
    fn vpmaxsq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXSQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXSQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXSQ512");
        }
    }
    /// Emits `VPMAXSQ512_MASK`.
    fn vpmaxsq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXSQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXSQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXSQ512_MASK");
        }
    }
    /// Emits `VPMAXSQ512_MASKZ`.
    fn vpmaxsq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXSQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXSQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXSQ512_MASKZ");
        }
    }
    /// Emits `VPMAXSQ512RRB`.
    fn vpmaxsq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXSQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXSQ512RRB_MASK`.
    fn vpmaxsq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXSQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXSQ512RRB_MASKZ`.
    fn vpmaxsq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXSQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXUD128`.
    fn vpmaxud128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXUD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXUD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXUD128");
        }
    }
    /// Emits `VPMAXUD128_MASK`.
    fn vpmaxud128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXUD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXUD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXUD128_MASK");
        }
    }
    /// Emits `VPMAXUD128_MASKZ`.
    fn vpmaxud128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXUD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXUD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXUD128_MASKZ");
        }
    }
    /// Emits `VPMAXUD128RRB`.
    fn vpmaxud128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXUD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXUD128RRB_MASK`.
    fn vpmaxud128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXUD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXUD128RRB_MASKZ`.
    fn vpmaxud128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXUD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXUD256`.
    fn vpmaxud256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXUD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXUD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXUD256");
        }
    }
    /// Emits `VPMAXUD256_MASK`.
    fn vpmaxud256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXUD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXUD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXUD256_MASK");
        }
    }
    /// Emits `VPMAXUD256_MASKZ`.
    fn vpmaxud256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXUD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXUD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXUD256_MASKZ");
        }
    }
    /// Emits `VPMAXUD256RRB`.
    fn vpmaxud256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXUD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXUD256RRB_MASK`.
    fn vpmaxud256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXUD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXUD256RRB_MASKZ`.
    fn vpmaxud256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXUD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXUD512`.
    fn vpmaxud512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXUD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXUD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXUD512");
        }
    }
    /// Emits `VPMAXUD512_MASK`.
    fn vpmaxud512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXUD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXUD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXUD512_MASK");
        }
    }
    /// Emits `VPMAXUD512_MASKZ`.
    fn vpmaxud512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXUD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXUD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXUD512_MASKZ");
        }
    }
    /// Emits `VPMAXUD512RRB`.
    fn vpmaxud512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXUD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXUD512RRB_MASK`.
    fn vpmaxud512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXUD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXUD512RRB_MASKZ`.
    fn vpmaxud512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXUD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXUQ128`.
    fn vpmaxuq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXUQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXUQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXUQ128");
        }
    }
    /// Emits `VPMAXUQ128_MASK`.
    fn vpmaxuq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXUQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXUQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXUQ128_MASK");
        }
    }
    /// Emits `VPMAXUQ128_MASKZ`.
    fn vpmaxuq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXUQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXUQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXUQ128_MASKZ");
        }
    }
    /// Emits `VPMAXUQ128RRB`.
    fn vpmaxuq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXUQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXUQ128RRB_MASK`.
    fn vpmaxuq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXUQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXUQ128RRB_MASKZ`.
    fn vpmaxuq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXUQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXUQ256`.
    fn vpmaxuq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXUQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXUQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXUQ256");
        }
    }
    /// Emits `VPMAXUQ256_MASK`.
    fn vpmaxuq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXUQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXUQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXUQ256_MASK");
        }
    }
    /// Emits `VPMAXUQ256_MASKZ`.
    fn vpmaxuq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXUQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXUQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXUQ256_MASKZ");
        }
    }
    /// Emits `VPMAXUQ256RRB`.
    fn vpmaxuq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXUQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXUQ256RRB_MASK`.
    fn vpmaxuq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXUQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXUQ256RRB_MASKZ`.
    fn vpmaxuq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXUQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXUQ512`.
    fn vpmaxuq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXUQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXUQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXUQ512");
        }
    }
    /// Emits `VPMAXUQ512_MASK`.
    fn vpmaxuq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXUQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXUQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXUQ512_MASK");
        }
    }
    /// Emits `VPMAXUQ512_MASKZ`.
    fn vpmaxuq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMAXUQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMAXUQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMAXUQ512_MASKZ");
        }
    }
    /// Emits `VPMAXUQ512RRB`.
    fn vpmaxuq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXUQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXUQ512RRB_MASK`.
    fn vpmaxuq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXUQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMAXUQ512RRB_MASKZ`.
    fn vpmaxuq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMAXUQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINSD128`.
    fn vpminsd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINSD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINSD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINSD128");
        }
    }
    /// Emits `VPMINSD128_MASK`.
    fn vpminsd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINSD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINSD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINSD128_MASK");
        }
    }
    /// Emits `VPMINSD128_MASKZ`.
    fn vpminsd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINSD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINSD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINSD128_MASKZ");
        }
    }
    /// Emits `VPMINSD128RRB`.
    fn vpminsd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINSD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINSD128RRB_MASK`.
    fn vpminsd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINSD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINSD128RRB_MASKZ`.
    fn vpminsd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINSD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINSD256`.
    fn vpminsd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINSD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINSD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINSD256");
        }
    }
    /// Emits `VPMINSD256_MASK`.
    fn vpminsd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINSD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINSD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINSD256_MASK");
        }
    }
    /// Emits `VPMINSD256_MASKZ`.
    fn vpminsd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINSD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINSD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINSD256_MASKZ");
        }
    }
    /// Emits `VPMINSD256RRB`.
    fn vpminsd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINSD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINSD256RRB_MASK`.
    fn vpminsd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINSD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINSD256RRB_MASKZ`.
    fn vpminsd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINSD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINSD512`.
    fn vpminsd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINSD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINSD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINSD512");
        }
    }
    /// Emits `VPMINSD512_MASK`.
    fn vpminsd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINSD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINSD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINSD512_MASK");
        }
    }
    /// Emits `VPMINSD512_MASKZ`.
    fn vpminsd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINSD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINSD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINSD512_MASKZ");
        }
    }
    /// Emits `VPMINSD512RRB`.
    fn vpminsd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINSD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINSD512RRB_MASK`.
    fn vpminsd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINSD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINSD512RRB_MASKZ`.
    fn vpminsd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINSD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINSQ128`.
    fn vpminsq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINSQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINSQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINSQ128");
        }
    }
    /// Emits `VPMINSQ128_MASK`.
    fn vpminsq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINSQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINSQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINSQ128_MASK");
        }
    }
    /// Emits `VPMINSQ128_MASKZ`.
    fn vpminsq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINSQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINSQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINSQ128_MASKZ");
        }
    }
    /// Emits `VPMINSQ128RRB`.
    fn vpminsq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINSQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINSQ128RRB_MASK`.
    fn vpminsq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINSQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINSQ128RRB_MASKZ`.
    fn vpminsq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINSQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINSQ256`.
    fn vpminsq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINSQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINSQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINSQ256");
        }
    }
    /// Emits `VPMINSQ256_MASK`.
    fn vpminsq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINSQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINSQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINSQ256_MASK");
        }
    }
    /// Emits `VPMINSQ256_MASKZ`.
    fn vpminsq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINSQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINSQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINSQ256_MASKZ");
        }
    }
    /// Emits `VPMINSQ256RRB`.
    fn vpminsq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINSQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINSQ256RRB_MASK`.
    fn vpminsq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINSQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINSQ256RRB_MASKZ`.
    fn vpminsq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINSQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINSQ512`.
    fn vpminsq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINSQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINSQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINSQ512");
        }
    }
    /// Emits `VPMINSQ512_MASK`.
    fn vpminsq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINSQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINSQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINSQ512_MASK");
        }
    }
    /// Emits `VPMINSQ512_MASKZ`.
    fn vpminsq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINSQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINSQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINSQ512_MASKZ");
        }
    }
    /// Emits `VPMINSQ512RRB`.
    fn vpminsq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINSQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINSQ512RRB_MASK`.
    fn vpminsq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINSQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINSQ512RRB_MASKZ`.
    fn vpminsq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINSQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINUD128`.
    fn vpminud128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINUD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINUD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINUD128");
        }
    }
    /// Emits `VPMINUD128_MASK`.
    fn vpminud128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINUD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINUD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINUD128_MASK");
        }
    }
    /// Emits `VPMINUD128_MASKZ`.
    fn vpminud128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINUD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINUD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINUD128_MASKZ");
        }
    }
    /// Emits `VPMINUD128RRB`.
    fn vpminud128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINUD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINUD128RRB_MASK`.
    fn vpminud128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINUD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINUD128RRB_MASKZ`.
    fn vpminud128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINUD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINUD256`.
    fn vpminud256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINUD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINUD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINUD256");
        }
    }
    /// Emits `VPMINUD256_MASK`.
    fn vpminud256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINUD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINUD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINUD256_MASK");
        }
    }
    /// Emits `VPMINUD256_MASKZ`.
    fn vpminud256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINUD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINUD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINUD256_MASKZ");
        }
    }
    /// Emits `VPMINUD256RRB`.
    fn vpminud256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINUD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINUD256RRB_MASK`.
    fn vpminud256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINUD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINUD256RRB_MASKZ`.
    fn vpminud256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINUD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINUD512`.
    fn vpminud512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINUD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINUD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINUD512");
        }
    }
    /// Emits `VPMINUD512_MASK`.
    fn vpminud512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINUD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINUD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINUD512_MASK");
        }
    }
    /// Emits `VPMINUD512_MASKZ`.
    fn vpminud512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINUD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINUD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINUD512_MASKZ");
        }
    }
    /// Emits `VPMINUD512RRB`.
    fn vpminud512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINUD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINUD512RRB_MASK`.
    fn vpminud512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINUD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINUD512RRB_MASKZ`.
    fn vpminud512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINUD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINUQ128`.
    fn vpminuq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINUQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINUQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINUQ128");
        }
    }
    /// Emits `VPMINUQ128_MASK`.
    fn vpminuq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINUQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINUQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINUQ128_MASK");
        }
    }
    /// Emits `VPMINUQ128_MASKZ`.
    fn vpminuq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINUQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINUQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINUQ128_MASKZ");
        }
    }
    /// Emits `VPMINUQ128RRB`.
    fn vpminuq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINUQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINUQ128RRB_MASK`.
    fn vpminuq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINUQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINUQ128RRB_MASKZ`.
    fn vpminuq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINUQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINUQ256`.
    fn vpminuq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINUQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINUQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINUQ256");
        }
    }
    /// Emits `VPMINUQ256_MASK`.
    fn vpminuq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINUQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINUQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINUQ256_MASK");
        }
    }
    /// Emits `VPMINUQ256_MASKZ`.
    fn vpminuq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINUQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINUQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINUQ256_MASKZ");
        }
    }
    /// Emits `VPMINUQ256RRB`.
    fn vpminuq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINUQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINUQ256RRB_MASK`.
    fn vpminuq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINUQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINUQ256RRB_MASKZ`.
    fn vpminuq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINUQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINUQ512`.
    fn vpminuq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINUQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINUQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINUQ512");
        }
    }
    /// Emits `VPMINUQ512_MASK`.
    fn vpminuq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINUQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINUQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINUQ512_MASK");
        }
    }
    /// Emits `VPMINUQ512_MASKZ`.
    fn vpminuq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMINUQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMINUQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMINUQ512_MASKZ");
        }
    }
    /// Emits `VPMINUQ512RRB`.
    fn vpminuq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINUQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINUQ512RRB_MASK`.
    fn vpminuq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINUQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMINUQ512RRB_MASKZ`.
    fn vpminuq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMINUQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMOVDB128`.
    fn vpmovdb128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVDB128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVDB128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVDB128");
        }
    }
    /// Emits `VPMOVDB128_MASK`.
    fn vpmovdb128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVDB128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVDB128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVDB128_MASK");
        }
    }
    /// Emits `VPMOVDB128RR_MASKZ`.
    fn vpmovdb128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVDB128RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVDB256`.
    fn vpmovdb256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVDB256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVDB256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVDB256");
        }
    }
    /// Emits `VPMOVDB256_MASK`.
    fn vpmovdb256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVDB256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVDB256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVDB256_MASK");
        }
    }
    /// Emits `VPMOVDB256RR_MASKZ`.
    fn vpmovdb256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVDB256RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVDB512`.
    fn vpmovdb512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVDB512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVDB512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVDB512");
        }
    }
    /// Emits `VPMOVDB512_MASK`.
    fn vpmovdb512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVDB512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVDB512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVDB512_MASK");
        }
    }
    /// Emits `VPMOVDB512RR_MASKZ`.
    fn vpmovdb512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVDB512RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVDW128`.
    fn vpmovdw128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVDW128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVDW128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVDW128");
        }
    }
    /// Emits `VPMOVDW128_MASK`.
    fn vpmovdw128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVDW128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVDW128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVDW128_MASK");
        }
    }
    /// Emits `VPMOVDW128RR_MASKZ`.
    fn vpmovdw128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVDW128RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVDW256`.
    fn vpmovdw256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVDW256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVDW256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVDW256");
        }
    }
    /// Emits `VPMOVDW256_MASK`.
    fn vpmovdw256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVDW256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVDW256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVDW256_MASK");
        }
    }
    /// Emits `VPMOVDW256RR_MASKZ`.
    fn vpmovdw256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVDW256RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVDW512`.
    fn vpmovdw512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVDW512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVDW512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVDW512");
        }
    }
    /// Emits `VPMOVDW512_MASK`.
    fn vpmovdw512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVDW512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVDW512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVDW512_MASK");
        }
    }
    /// Emits `VPMOVDW512RR_MASKZ`.
    fn vpmovdw512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVDW512RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVQB128`.
    fn vpmovqb128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVQB128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVQB128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVQB128");
        }
    }
    /// Emits `VPMOVQB128_MASK`.
    fn vpmovqb128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVQB128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVQB128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVQB128_MASK");
        }
    }
    /// Emits `VPMOVQB128RR_MASKZ`.
    fn vpmovqb128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVQB128RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVQB256`.
    fn vpmovqb256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVQB256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVQB256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVQB256");
        }
    }
    /// Emits `VPMOVQB256_MASK`.
    fn vpmovqb256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVQB256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVQB256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVQB256_MASK");
        }
    }
    /// Emits `VPMOVQB256RR_MASKZ`.
    fn vpmovqb256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVQB256RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVQB512`.
    fn vpmovqb512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVQB512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVQB512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVQB512");
        }
    }
    /// Emits `VPMOVQB512_MASK`.
    fn vpmovqb512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVQB512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVQB512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVQB512_MASK");
        }
    }
    /// Emits `VPMOVQB512RR_MASKZ`.
    fn vpmovqb512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVQB512RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVQD128`.
    fn vpmovqd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVQD128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVQD128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVQD128");
        }
    }
    /// Emits `VPMOVQD128_MASK`.
    fn vpmovqd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVQD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVQD128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVQD128_MASK");
        }
    }
    /// Emits `VPMOVQD128RR_MASKZ`.
    fn vpmovqd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVQD128RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVQD256`.
    fn vpmovqd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVQD256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVQD256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVQD256");
        }
    }
    /// Emits `VPMOVQD256_MASK`.
    fn vpmovqd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVQD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVQD256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVQD256_MASK");
        }
    }
    /// Emits `VPMOVQD256RR_MASKZ`.
    fn vpmovqd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVQD256RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVQD512`.
    fn vpmovqd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVQD512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVQD512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVQD512");
        }
    }
    /// Emits `VPMOVQD512_MASK`.
    fn vpmovqd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVQD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVQD512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVQD512_MASK");
        }
    }
    /// Emits `VPMOVQD512RR_MASKZ`.
    fn vpmovqd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVQD512RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVQW128`.
    fn vpmovqw128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVQW128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVQW128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVQW128");
        }
    }
    /// Emits `VPMOVQW128_MASK`.
    fn vpmovqw128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVQW128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVQW128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVQW128_MASK");
        }
    }
    /// Emits `VPMOVQW128RR_MASKZ`.
    fn vpmovqw128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVQW128RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVQW256`.
    fn vpmovqw256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVQW256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVQW256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVQW256");
        }
    }
    /// Emits `VPMOVQW256_MASK`.
    fn vpmovqw256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVQW256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVQW256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVQW256_MASK");
        }
    }
    /// Emits `VPMOVQW256RR_MASKZ`.
    fn vpmovqw256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVQW256RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVQW512`.
    fn vpmovqw512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVQW512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVQW512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVQW512");
        }
    }
    /// Emits `VPMOVQW512_MASK`.
    fn vpmovqw512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVQW512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVQW512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVQW512_MASK");
        }
    }
    /// Emits `VPMOVQW512RR_MASKZ`.
    fn vpmovqw512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVQW512RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVSDB128`.
    fn vpmovsdb128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSDB128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSDB128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSDB128");
        }
    }
    /// Emits `VPMOVSDB128_MASK`.
    fn vpmovsdb128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSDB128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSDB128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSDB128_MASK");
        }
    }
    /// Emits `VPMOVSDB128RR_MASKZ`.
    fn vpmovsdb128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVSDB128RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVSDB256`.
    fn vpmovsdb256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSDB256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSDB256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSDB256");
        }
    }
    /// Emits `VPMOVSDB256_MASK`.
    fn vpmovsdb256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSDB256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSDB256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSDB256_MASK");
        }
    }
    /// Emits `VPMOVSDB256RR_MASKZ`.
    fn vpmovsdb256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVSDB256RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVSDB512`.
    fn vpmovsdb512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSDB512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSDB512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSDB512");
        }
    }
    /// Emits `VPMOVSDB512_MASK`.
    fn vpmovsdb512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSDB512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSDB512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSDB512_MASK");
        }
    }
    /// Emits `VPMOVSDB512RR_MASKZ`.
    fn vpmovsdb512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVSDB512RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVSDW128`.
    fn vpmovsdw128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSDW128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSDW128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSDW128");
        }
    }
    /// Emits `VPMOVSDW128_MASK`.
    fn vpmovsdw128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSDW128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSDW128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSDW128_MASK");
        }
    }
    /// Emits `VPMOVSDW128RR_MASKZ`.
    fn vpmovsdw128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVSDW128RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVSDW256`.
    fn vpmovsdw256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSDW256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSDW256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSDW256");
        }
    }
    /// Emits `VPMOVSDW256_MASK`.
    fn vpmovsdw256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSDW256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSDW256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSDW256_MASK");
        }
    }
    /// Emits `VPMOVSDW256RR_MASKZ`.
    fn vpmovsdw256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVSDW256RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVSDW512`.
    fn vpmovsdw512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSDW512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSDW512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSDW512");
        }
    }
    /// Emits `VPMOVSDW512_MASK`.
    fn vpmovsdw512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSDW512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSDW512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSDW512_MASK");
        }
    }
    /// Emits `VPMOVSDW512RR_MASKZ`.
    fn vpmovsdw512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVSDW512RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVSQB128`.
    fn vpmovsqb128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSQB128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSQB128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSQB128");
        }
    }
    /// Emits `VPMOVSQB128_MASK`.
    fn vpmovsqb128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSQB128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSQB128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSQB128_MASK");
        }
    }
    /// Emits `VPMOVSQB128RR_MASKZ`.
    fn vpmovsqb128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVSQB128RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVSQB256`.
    fn vpmovsqb256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSQB256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSQB256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSQB256");
        }
    }
    /// Emits `VPMOVSQB256_MASK`.
    fn vpmovsqb256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSQB256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSQB256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSQB256_MASK");
        }
    }
    /// Emits `VPMOVSQB256RR_MASKZ`.
    fn vpmovsqb256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVSQB256RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVSQB512`.
    fn vpmovsqb512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSQB512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSQB512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSQB512");
        }
    }
    /// Emits `VPMOVSQB512_MASK`.
    fn vpmovsqb512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSQB512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSQB512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSQB512_MASK");
        }
    }
    /// Emits `VPMOVSQB512RR_MASKZ`.
    fn vpmovsqb512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVSQB512RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVSQD128`.
    fn vpmovsqd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSQD128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSQD128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSQD128");
        }
    }
    /// Emits `VPMOVSQD128_MASK`.
    fn vpmovsqd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSQD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSQD128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSQD128_MASK");
        }
    }
    /// Emits `VPMOVSQD128RR_MASKZ`.
    fn vpmovsqd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVSQD128RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVSQD256`.
    fn vpmovsqd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSQD256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSQD256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSQD256");
        }
    }
    /// Emits `VPMOVSQD256_MASK`.
    fn vpmovsqd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSQD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSQD256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSQD256_MASK");
        }
    }
    /// Emits `VPMOVSQD256RR_MASKZ`.
    fn vpmovsqd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVSQD256RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVSQD512`.
    fn vpmovsqd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSQD512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSQD512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSQD512");
        }
    }
    /// Emits `VPMOVSQD512_MASK`.
    fn vpmovsqd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSQD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSQD512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSQD512_MASK");
        }
    }
    /// Emits `VPMOVSQD512RR_MASKZ`.
    fn vpmovsqd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVSQD512RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVSQW128`.
    fn vpmovsqw128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSQW128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSQW128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSQW128");
        }
    }
    /// Emits `VPMOVSQW128_MASK`.
    fn vpmovsqw128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSQW128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSQW128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSQW128_MASK");
        }
    }
    /// Emits `VPMOVSQW128RR_MASKZ`.
    fn vpmovsqw128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVSQW128RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVSQW256`.
    fn vpmovsqw256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSQW256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSQW256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSQW256");
        }
    }
    /// Emits `VPMOVSQW256_MASK`.
    fn vpmovsqw256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSQW256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSQW256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSQW256_MASK");
        }
    }
    /// Emits `VPMOVSQW256RR_MASKZ`.
    fn vpmovsqw256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVSQW256RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVSQW512`.
    fn vpmovsqw512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSQW512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSQW512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSQW512");
        }
    }
    /// Emits `VPMOVSQW512_MASK`.
    fn vpmovsqw512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSQW512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVSQW512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSQW512_MASK");
        }
    }
    /// Emits `VPMOVSQW512RR_MASKZ`.
    fn vpmovsqw512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVSQW512RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVSXBD128`.
    fn vpmovsxbd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBD128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBD128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBD128");
        }
    }
    /// Emits `VPMOVSXBD128_MASK`.
    fn vpmovsxbd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBD128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBD128_MASK");
        }
    }
    /// Emits `VPMOVSXBD128_MASKZ`.
    fn vpmovsxbd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBD128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBD128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBD128_MASKZ");
        }
    }
    /// Emits `VPMOVSXBD256`.
    fn vpmovsxbd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBD256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBD256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBD256");
        }
    }
    /// Emits `VPMOVSXBD256_MASK`.
    fn vpmovsxbd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBD256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBD256_MASK");
        }
    }
    /// Emits `VPMOVSXBD256_MASKZ`.
    fn vpmovsxbd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBD256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBD256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBD256_MASKZ");
        }
    }
    /// Emits `VPMOVSXBD512`.
    fn vpmovsxbd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBD512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBD512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBD512");
        }
    }
    /// Emits `VPMOVSXBD512_MASK`.
    fn vpmovsxbd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBD512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBD512_MASK");
        }
    }
    /// Emits `VPMOVSXBD512_MASKZ`.
    fn vpmovsxbd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBD512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBD512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBD512_MASKZ");
        }
    }
    /// Emits `VPMOVSXBQ128`.
    fn vpmovsxbq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBQ128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBQ128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBQ128");
        }
    }
    /// Emits `VPMOVSXBQ128_MASK`.
    fn vpmovsxbq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBQ128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBQ128_MASK");
        }
    }
    /// Emits `VPMOVSXBQ128_MASKZ`.
    fn vpmovsxbq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBQ128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBQ128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBQ128_MASKZ");
        }
    }
    /// Emits `VPMOVSXBQ256`.
    fn vpmovsxbq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBQ256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBQ256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBQ256");
        }
    }
    /// Emits `VPMOVSXBQ256_MASK`.
    fn vpmovsxbq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBQ256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBQ256_MASK");
        }
    }
    /// Emits `VPMOVSXBQ256_MASKZ`.
    fn vpmovsxbq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBQ256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBQ256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBQ256_MASKZ");
        }
    }
    /// Emits `VPMOVSXBQ512`.
    fn vpmovsxbq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBQ512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBQ512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBQ512");
        }
    }
    /// Emits `VPMOVSXBQ512_MASK`.
    fn vpmovsxbq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBQ512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBQ512_MASK");
        }
    }
    /// Emits `VPMOVSXBQ512_MASKZ`.
    fn vpmovsxbq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBQ512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBQ512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBQ512_MASKZ");
        }
    }
    /// Emits `VPMOVSXBW128`.
    fn vpmovsxbw128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBW128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBW128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBW128");
        }
    }
    /// Emits `VPMOVSXBW128_MASK`.
    fn vpmovsxbw128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBW128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBW128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBW128_MASK");
        }
    }
    /// Emits `VPMOVSXBW128_MASKZ`.
    fn vpmovsxbw128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBW128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBW128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBW128_MASKZ");
        }
    }
    /// Emits `VPMOVSXBW256`.
    fn vpmovsxbw256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBW256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBW256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBW256");
        }
    }
    /// Emits `VPMOVSXBW256_MASK`.
    fn vpmovsxbw256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBW256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBW256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBW256_MASK");
        }
    }
    /// Emits `VPMOVSXBW256_MASKZ`.
    fn vpmovsxbw256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBW256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBW256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBW256_MASKZ");
        }
    }
    /// Emits `VPMOVSXBW512`.
    fn vpmovsxbw512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBW512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBW512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBW512");
        }
    }
    /// Emits `VPMOVSXBW512_MASK`.
    fn vpmovsxbw512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBW512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBW512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBW512_MASK");
        }
    }
    /// Emits `VPMOVSXBW512_MASKZ`.
    fn vpmovsxbw512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXBW512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXBW512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXBW512_MASKZ");
        }
    }
    /// Emits `VPMOVSXDQ128`.
    fn vpmovsxdq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXDQ128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXDQ128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXDQ128");
        }
    }
    /// Emits `VPMOVSXDQ128_MASK`.
    fn vpmovsxdq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXDQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXDQ128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXDQ128_MASK");
        }
    }
    /// Emits `VPMOVSXDQ128_MASKZ`.
    fn vpmovsxdq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXDQ128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXDQ128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXDQ128_MASKZ");
        }
    }
    /// Emits `VPMOVSXDQ256`.
    fn vpmovsxdq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXDQ256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXDQ256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXDQ256");
        }
    }
    /// Emits `VPMOVSXDQ256_MASK`.
    fn vpmovsxdq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXDQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXDQ256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXDQ256_MASK");
        }
    }
    /// Emits `VPMOVSXDQ256_MASKZ`.
    fn vpmovsxdq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXDQ256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXDQ256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXDQ256_MASKZ");
        }
    }
    /// Emits `VPMOVSXDQ512`.
    fn vpmovsxdq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXDQ512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXDQ512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXDQ512");
        }
    }
    /// Emits `VPMOVSXDQ512_MASK`.
    fn vpmovsxdq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXDQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXDQ512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXDQ512_MASK");
        }
    }
    /// Emits `VPMOVSXDQ512_MASKZ`.
    fn vpmovsxdq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXDQ512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXDQ512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXDQ512_MASKZ");
        }
    }
    /// Emits `VPMOVSXWD128`.
    fn vpmovsxwd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXWD128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXWD128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXWD128");
        }
    }
    /// Emits `VPMOVSXWD128_MASK`.
    fn vpmovsxwd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXWD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXWD128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXWD128_MASK");
        }
    }
    /// Emits `VPMOVSXWD128_MASKZ`.
    fn vpmovsxwd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXWD128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXWD128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXWD128_MASKZ");
        }
    }
    /// Emits `VPMOVSXWD256`.
    fn vpmovsxwd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXWD256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXWD256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXWD256");
        }
    }
    /// Emits `VPMOVSXWD256_MASK`.
    fn vpmovsxwd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXWD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXWD256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXWD256_MASK");
        }
    }
    /// Emits `VPMOVSXWD256_MASKZ`.
    fn vpmovsxwd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXWD256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXWD256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXWD256_MASKZ");
        }
    }
    /// Emits `VPMOVSXWD512`.
    fn vpmovsxwd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXWD512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXWD512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXWD512");
        }
    }
    /// Emits `VPMOVSXWD512_MASK`.
    fn vpmovsxwd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXWD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXWD512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXWD512_MASK");
        }
    }
    /// Emits `VPMOVSXWD512_MASKZ`.
    fn vpmovsxwd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXWD512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXWD512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXWD512_MASKZ");
        }
    }
    /// Emits `VPMOVSXWQ128`.
    fn vpmovsxwq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXWQ128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXWQ128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXWQ128");
        }
    }
    /// Emits `VPMOVSXWQ128_MASK`.
    fn vpmovsxwq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXWQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXWQ128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXWQ128_MASK");
        }
    }
    /// Emits `VPMOVSXWQ128_MASKZ`.
    fn vpmovsxwq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXWQ128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXWQ128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXWQ128_MASKZ");
        }
    }
    /// Emits `VPMOVSXWQ256`.
    fn vpmovsxwq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXWQ256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXWQ256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXWQ256");
        }
    }
    /// Emits `VPMOVSXWQ256_MASK`.
    fn vpmovsxwq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXWQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXWQ256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXWQ256_MASK");
        }
    }
    /// Emits `VPMOVSXWQ256_MASKZ`.
    fn vpmovsxwq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXWQ256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXWQ256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXWQ256_MASKZ");
        }
    }
    /// Emits `VPMOVSXWQ512`.
    fn vpmovsxwq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXWQ512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXWQ512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXWQ512");
        }
    }
    /// Emits `VPMOVSXWQ512_MASK`.
    fn vpmovsxwq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXWQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXWQ512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXWQ512_MASK");
        }
    }
    /// Emits `VPMOVSXWQ512_MASKZ`.
    fn vpmovsxwq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVSXWQ512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVSXWQ512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVSXWQ512_MASKZ");
        }
    }
    /// Emits `VPMOVUSDB128`.
    fn vpmovusdb128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSDB128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSDB128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSDB128");
        }
    }
    /// Emits `VPMOVUSDB128_MASK`.
    fn vpmovusdb128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSDB128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSDB128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSDB128_MASK");
        }
    }
    /// Emits `VPMOVUSDB128RR_MASKZ`.
    fn vpmovusdb128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVUSDB128RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVUSDB256`.
    fn vpmovusdb256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSDB256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSDB256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSDB256");
        }
    }
    /// Emits `VPMOVUSDB256_MASK`.
    fn vpmovusdb256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSDB256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSDB256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSDB256_MASK");
        }
    }
    /// Emits `VPMOVUSDB256RR_MASKZ`.
    fn vpmovusdb256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVUSDB256RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVUSDB512`.
    fn vpmovusdb512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSDB512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSDB512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSDB512");
        }
    }
    /// Emits `VPMOVUSDB512_MASK`.
    fn vpmovusdb512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSDB512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSDB512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSDB512_MASK");
        }
    }
    /// Emits `VPMOVUSDB512RR_MASKZ`.
    fn vpmovusdb512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVUSDB512RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVUSDW128`.
    fn vpmovusdw128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSDW128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSDW128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSDW128");
        }
    }
    /// Emits `VPMOVUSDW128_MASK`.
    fn vpmovusdw128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSDW128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSDW128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSDW128_MASK");
        }
    }
    /// Emits `VPMOVUSDW128RR_MASKZ`.
    fn vpmovusdw128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVUSDW128RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVUSDW256`.
    fn vpmovusdw256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSDW256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSDW256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSDW256");
        }
    }
    /// Emits `VPMOVUSDW256_MASK`.
    fn vpmovusdw256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSDW256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSDW256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSDW256_MASK");
        }
    }
    /// Emits `VPMOVUSDW256RR_MASKZ`.
    fn vpmovusdw256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVUSDW256RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVUSDW512`.
    fn vpmovusdw512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSDW512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSDW512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSDW512");
        }
    }
    /// Emits `VPMOVUSDW512_MASK`.
    fn vpmovusdw512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSDW512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSDW512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSDW512_MASK");
        }
    }
    /// Emits `VPMOVUSDW512RR_MASKZ`.
    fn vpmovusdw512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVUSDW512RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVUSQB128`.
    fn vpmovusqb128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSQB128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSQB128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSQB128");
        }
    }
    /// Emits `VPMOVUSQB128_MASK`.
    fn vpmovusqb128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSQB128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSQB128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSQB128_MASK");
        }
    }
    /// Emits `VPMOVUSQB128RR_MASKZ`.
    fn vpmovusqb128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVUSQB128RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVUSQB256`.
    fn vpmovusqb256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSQB256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSQB256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSQB256");
        }
    }
    /// Emits `VPMOVUSQB256_MASK`.
    fn vpmovusqb256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSQB256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSQB256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSQB256_MASK");
        }
    }
    /// Emits `VPMOVUSQB256RR_MASKZ`.
    fn vpmovusqb256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVUSQB256RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVUSQB512`.
    fn vpmovusqb512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSQB512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSQB512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSQB512");
        }
    }
    /// Emits `VPMOVUSQB512_MASK`.
    fn vpmovusqb512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSQB512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSQB512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSQB512_MASK");
        }
    }
    /// Emits `VPMOVUSQB512RR_MASKZ`.
    fn vpmovusqb512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVUSQB512RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVUSQD128`.
    fn vpmovusqd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSQD128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSQD128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSQD128");
        }
    }
    /// Emits `VPMOVUSQD128_MASK`.
    fn vpmovusqd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSQD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSQD128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSQD128_MASK");
        }
    }
    /// Emits `VPMOVUSQD128RR_MASKZ`.
    fn vpmovusqd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVUSQD128RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVUSQD256`.
    fn vpmovusqd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSQD256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSQD256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSQD256");
        }
    }
    /// Emits `VPMOVUSQD256_MASK`.
    fn vpmovusqd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSQD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSQD256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSQD256_MASK");
        }
    }
    /// Emits `VPMOVUSQD256RR_MASKZ`.
    fn vpmovusqd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVUSQD256RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVUSQD512`.
    fn vpmovusqd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSQD512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSQD512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSQD512");
        }
    }
    /// Emits `VPMOVUSQD512_MASK`.
    fn vpmovusqd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSQD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSQD512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSQD512_MASK");
        }
    }
    /// Emits `VPMOVUSQD512RR_MASKZ`.
    fn vpmovusqd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVUSQD512RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVUSQW128`.
    fn vpmovusqw128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSQW128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSQW128MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSQW128");
        }
    }
    /// Emits `VPMOVUSQW128_MASK`.
    fn vpmovusqw128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSQW128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSQW128MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSQW128_MASK");
        }
    }
    /// Emits `VPMOVUSQW128RR_MASKZ`.
    fn vpmovusqw128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVUSQW128RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVUSQW256`.
    fn vpmovusqw256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSQW256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSQW256MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSQW256");
        }
    }
    /// Emits `VPMOVUSQW256_MASK`.
    fn vpmovusqw256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSQW256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSQW256MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSQW256_MASK");
        }
    }
    /// Emits `VPMOVUSQW256RR_MASKZ`.
    fn vpmovusqw256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVUSQW256RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVUSQW512`.
    fn vpmovusqw512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSQW512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSQW512MR, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSQW512");
        }
    }
    /// Emits `VPMOVUSQW512_MASK`.
    fn vpmovusqw512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVUSQW512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_mem() && op1.is_vec() {
            self.emit(VPMOVUSQW512MR_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVUSQW512_MASK");
        }
    }
    /// Emits `VPMOVUSQW512RR_MASKZ`.
    fn vpmovusqw512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPMOVUSQW512RR_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPMOVZXBD128`.
    fn vpmovzxbd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBD128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBD128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBD128");
        }
    }
    /// Emits `VPMOVZXBD128_MASK`.
    fn vpmovzxbd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBD128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBD128_MASK");
        }
    }
    /// Emits `VPMOVZXBD128_MASKZ`.
    fn vpmovzxbd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBD128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBD128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBD128_MASKZ");
        }
    }
    /// Emits `VPMOVZXBD256`.
    fn vpmovzxbd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBD256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBD256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBD256");
        }
    }
    /// Emits `VPMOVZXBD256_MASK`.
    fn vpmovzxbd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBD256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBD256_MASK");
        }
    }
    /// Emits `VPMOVZXBD256_MASKZ`.
    fn vpmovzxbd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBD256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBD256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBD256_MASKZ");
        }
    }
    /// Emits `VPMOVZXBD512`.
    fn vpmovzxbd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBD512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBD512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBD512");
        }
    }
    /// Emits `VPMOVZXBD512_MASK`.
    fn vpmovzxbd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBD512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBD512_MASK");
        }
    }
    /// Emits `VPMOVZXBD512_MASKZ`.
    fn vpmovzxbd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBD512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBD512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBD512_MASKZ");
        }
    }
    /// Emits `VPMOVZXBQ128`.
    fn vpmovzxbq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBQ128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBQ128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBQ128");
        }
    }
    /// Emits `VPMOVZXBQ128_MASK`.
    fn vpmovzxbq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBQ128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBQ128_MASK");
        }
    }
    /// Emits `VPMOVZXBQ128_MASKZ`.
    fn vpmovzxbq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBQ128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBQ128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBQ128_MASKZ");
        }
    }
    /// Emits `VPMOVZXBQ256`.
    fn vpmovzxbq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBQ256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBQ256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBQ256");
        }
    }
    /// Emits `VPMOVZXBQ256_MASK`.
    fn vpmovzxbq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBQ256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBQ256_MASK");
        }
    }
    /// Emits `VPMOVZXBQ256_MASKZ`.
    fn vpmovzxbq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBQ256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBQ256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBQ256_MASKZ");
        }
    }
    /// Emits `VPMOVZXBQ512`.
    fn vpmovzxbq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBQ512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBQ512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBQ512");
        }
    }
    /// Emits `VPMOVZXBQ512_MASK`.
    fn vpmovzxbq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBQ512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBQ512_MASK");
        }
    }
    /// Emits `VPMOVZXBQ512_MASKZ`.
    fn vpmovzxbq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBQ512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBQ512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBQ512_MASKZ");
        }
    }
    /// Emits `VPMOVZXBW128`.
    fn vpmovzxbw128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBW128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBW128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBW128");
        }
    }
    /// Emits `VPMOVZXBW128_MASK`.
    fn vpmovzxbw128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBW128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBW128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBW128_MASK");
        }
    }
    /// Emits `VPMOVZXBW128_MASKZ`.
    fn vpmovzxbw128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBW128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBW128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBW128_MASKZ");
        }
    }
    /// Emits `VPMOVZXBW256`.
    fn vpmovzxbw256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBW256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBW256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBW256");
        }
    }
    /// Emits `VPMOVZXBW256_MASK`.
    fn vpmovzxbw256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBW256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBW256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBW256_MASK");
        }
    }
    /// Emits `VPMOVZXBW256_MASKZ`.
    fn vpmovzxbw256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBW256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBW256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBW256_MASKZ");
        }
    }
    /// Emits `VPMOVZXBW512`.
    fn vpmovzxbw512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBW512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBW512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBW512");
        }
    }
    /// Emits `VPMOVZXBW512_MASK`.
    fn vpmovzxbw512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBW512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBW512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBW512_MASK");
        }
    }
    /// Emits `VPMOVZXBW512_MASKZ`.
    fn vpmovzxbw512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXBW512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXBW512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXBW512_MASKZ");
        }
    }
    /// Emits `VPMOVZXDQ128`.
    fn vpmovzxdq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXDQ128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXDQ128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXDQ128");
        }
    }
    /// Emits `VPMOVZXDQ128_MASK`.
    fn vpmovzxdq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXDQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXDQ128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXDQ128_MASK");
        }
    }
    /// Emits `VPMOVZXDQ128_MASKZ`.
    fn vpmovzxdq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXDQ128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXDQ128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXDQ128_MASKZ");
        }
    }
    /// Emits `VPMOVZXDQ256`.
    fn vpmovzxdq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXDQ256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXDQ256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXDQ256");
        }
    }
    /// Emits `VPMOVZXDQ256_MASK`.
    fn vpmovzxdq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXDQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXDQ256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXDQ256_MASK");
        }
    }
    /// Emits `VPMOVZXDQ256_MASKZ`.
    fn vpmovzxdq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXDQ256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXDQ256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXDQ256_MASKZ");
        }
    }
    /// Emits `VPMOVZXDQ512`.
    fn vpmovzxdq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXDQ512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXDQ512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXDQ512");
        }
    }
    /// Emits `VPMOVZXDQ512_MASK`.
    fn vpmovzxdq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXDQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXDQ512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXDQ512_MASK");
        }
    }
    /// Emits `VPMOVZXDQ512_MASKZ`.
    fn vpmovzxdq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXDQ512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXDQ512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXDQ512_MASKZ");
        }
    }
    /// Emits `VPMOVZXWD128`.
    fn vpmovzxwd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXWD128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXWD128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXWD128");
        }
    }
    /// Emits `VPMOVZXWD128_MASK`.
    fn vpmovzxwd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXWD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXWD128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXWD128_MASK");
        }
    }
    /// Emits `VPMOVZXWD128_MASKZ`.
    fn vpmovzxwd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXWD128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXWD128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXWD128_MASKZ");
        }
    }
    /// Emits `VPMOVZXWD256`.
    fn vpmovzxwd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXWD256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXWD256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXWD256");
        }
    }
    /// Emits `VPMOVZXWD256_MASK`.
    fn vpmovzxwd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXWD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXWD256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXWD256_MASK");
        }
    }
    /// Emits `VPMOVZXWD256_MASKZ`.
    fn vpmovzxwd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXWD256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXWD256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXWD256_MASKZ");
        }
    }
    /// Emits `VPMOVZXWD512`.
    fn vpmovzxwd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXWD512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXWD512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXWD512");
        }
    }
    /// Emits `VPMOVZXWD512_MASK`.
    fn vpmovzxwd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXWD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXWD512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXWD512_MASK");
        }
    }
    /// Emits `VPMOVZXWD512_MASKZ`.
    fn vpmovzxwd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXWD512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXWD512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXWD512_MASKZ");
        }
    }
    /// Emits `VPMOVZXWQ128`.
    fn vpmovzxwq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXWQ128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXWQ128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXWQ128");
        }
    }
    /// Emits `VPMOVZXWQ128_MASK`.
    fn vpmovzxwq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXWQ128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXWQ128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXWQ128_MASK");
        }
    }
    /// Emits `VPMOVZXWQ128_MASKZ`.
    fn vpmovzxwq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXWQ128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXWQ128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXWQ128_MASKZ");
        }
    }
    /// Emits `VPMOVZXWQ256`.
    fn vpmovzxwq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXWQ256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXWQ256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXWQ256");
        }
    }
    /// Emits `VPMOVZXWQ256_MASK`.
    fn vpmovzxwq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXWQ256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXWQ256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXWQ256_MASK");
        }
    }
    /// Emits `VPMOVZXWQ256_MASKZ`.
    fn vpmovzxwq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXWQ256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXWQ256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXWQ256_MASKZ");
        }
    }
    /// Emits `VPMOVZXWQ512`.
    fn vpmovzxwq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXWQ512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXWQ512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXWQ512");
        }
    }
    /// Emits `VPMOVZXWQ512_MASK`.
    fn vpmovzxwq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXWQ512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXWQ512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXWQ512_MASK");
        }
    }
    /// Emits `VPMOVZXWQ512_MASKZ`.
    fn vpmovzxwq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VPMOVZXWQ512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VPMOVZXWQ512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMOVZXWQ512_MASKZ");
        }
    }
    /// Emits `VPMULDQ128`.
    fn vpmuldq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMULDQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMULDQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMULDQ128");
        }
    }
    /// Emits `VPMULDQ128_MASK`.
    fn vpmuldq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMULDQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMULDQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMULDQ128_MASK");
        }
    }
    /// Emits `VPMULDQ128_MASKZ`.
    fn vpmuldq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMULDQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMULDQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMULDQ128_MASKZ");
        }
    }
    /// Emits `VPMULDQ128RRB`.
    fn vpmuldq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMULDQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMULDQ128RRB_MASK`.
    fn vpmuldq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMULDQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMULDQ128RRB_MASKZ`.
    fn vpmuldq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMULDQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMULDQ256`.
    fn vpmuldq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMULDQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMULDQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMULDQ256");
        }
    }
    /// Emits `VPMULDQ256_MASK`.
    fn vpmuldq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMULDQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMULDQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMULDQ256_MASK");
        }
    }
    /// Emits `VPMULDQ256_MASKZ`.
    fn vpmuldq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMULDQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMULDQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMULDQ256_MASKZ");
        }
    }
    /// Emits `VPMULDQ256RRB`.
    fn vpmuldq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMULDQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMULDQ256RRB_MASK`.
    fn vpmuldq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMULDQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMULDQ256RRB_MASKZ`.
    fn vpmuldq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMULDQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMULDQ512`.
    fn vpmuldq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMULDQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMULDQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMULDQ512");
        }
    }
    /// Emits `VPMULDQ512_MASK`.
    fn vpmuldq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMULDQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMULDQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMULDQ512_MASK");
        }
    }
    /// Emits `VPMULDQ512_MASKZ`.
    fn vpmuldq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMULDQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMULDQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMULDQ512_MASKZ");
        }
    }
    /// Emits `VPMULDQ512RRB`.
    fn vpmuldq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMULDQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMULDQ512RRB_MASK`.
    fn vpmuldq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMULDQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMULDQ512RRB_MASKZ`.
    fn vpmuldq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMULDQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMULUDQ128`.
    fn vpmuludq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMULUDQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMULUDQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMULUDQ128");
        }
    }
    /// Emits `VPMULUDQ128_MASK`.
    fn vpmuludq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMULUDQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMULUDQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMULUDQ128_MASK");
        }
    }
    /// Emits `VPMULUDQ128_MASKZ`.
    fn vpmuludq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMULUDQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMULUDQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMULUDQ128_MASKZ");
        }
    }
    /// Emits `VPMULUDQ128RRB`.
    fn vpmuludq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMULUDQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMULUDQ128RRB_MASK`.
    fn vpmuludq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMULUDQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMULUDQ128RRB_MASKZ`.
    fn vpmuludq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMULUDQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMULUDQ256`.
    fn vpmuludq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMULUDQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMULUDQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMULUDQ256");
        }
    }
    /// Emits `VPMULUDQ256_MASK`.
    fn vpmuludq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMULUDQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMULUDQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMULUDQ256_MASK");
        }
    }
    /// Emits `VPMULUDQ256_MASKZ`.
    fn vpmuludq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMULUDQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMULUDQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMULUDQ256_MASKZ");
        }
    }
    /// Emits `VPMULUDQ256RRB`.
    fn vpmuludq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMULUDQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMULUDQ256RRB_MASK`.
    fn vpmuludq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMULUDQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMULUDQ256RRB_MASKZ`.
    fn vpmuludq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMULUDQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMULUDQ512`.
    fn vpmuludq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMULUDQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMULUDQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMULUDQ512");
        }
    }
    /// Emits `VPMULUDQ512_MASK`.
    fn vpmuludq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMULUDQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMULUDQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMULUDQ512_MASK");
        }
    }
    /// Emits `VPMULUDQ512_MASKZ`.
    fn vpmuludq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPMULUDQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPMULUDQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPMULUDQ512_MASKZ");
        }
    }
    /// Emits `VPMULUDQ512RRB`.
    fn vpmuludq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMULUDQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMULUDQ512RRB_MASK`.
    fn vpmuludq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMULUDQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPMULUDQ512RRB_MASKZ`.
    fn vpmuludq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPMULUDQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPORD128`.
    fn vpord128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPORD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPORD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPORD128");
        }
    }
    /// Emits `VPORD128_MASK`.
    fn vpord128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPORD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPORD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPORD128_MASK");
        }
    }
    /// Emits `VPORD128_MASKZ`.
    fn vpord128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPORD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPORD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPORD128_MASKZ");
        }
    }
    /// Emits `VPORD128RRB`.
    fn vpord128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPORD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPORD128RRB_MASK`.
    fn vpord128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPORD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPORD128RRB_MASKZ`.
    fn vpord128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPORD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPORD256`.
    fn vpord256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPORD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPORD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPORD256");
        }
    }
    /// Emits `VPORD256_MASK`.
    fn vpord256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPORD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPORD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPORD256_MASK");
        }
    }
    /// Emits `VPORD256_MASKZ`.
    fn vpord256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPORD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPORD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPORD256_MASKZ");
        }
    }
    /// Emits `VPORD256RRB`.
    fn vpord256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPORD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPORD256RRB_MASK`.
    fn vpord256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPORD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPORD256RRB_MASKZ`.
    fn vpord256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPORD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPORD512`.
    fn vpord512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPORD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPORD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPORD512");
        }
    }
    /// Emits `VPORD512_MASK`.
    fn vpord512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPORD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPORD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPORD512_MASK");
        }
    }
    /// Emits `VPORD512_MASKZ`.
    fn vpord512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPORD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPORD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPORD512_MASKZ");
        }
    }
    /// Emits `VPORD512RRB`.
    fn vpord512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPORD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPORD512RRB_MASK`.
    fn vpord512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPORD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPORD512RRB_MASKZ`.
    fn vpord512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPORD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPORQ128`.
    fn vporq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPORQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPORQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPORQ128");
        }
    }
    /// Emits `VPORQ128_MASK`.
    fn vporq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPORQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPORQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPORQ128_MASK");
        }
    }
    /// Emits `VPORQ128_MASKZ`.
    fn vporq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPORQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPORQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPORQ128_MASKZ");
        }
    }
    /// Emits `VPORQ128RRB`.
    fn vporq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPORQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPORQ128RRB_MASK`.
    fn vporq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPORQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPORQ128RRB_MASKZ`.
    fn vporq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPORQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPORQ256`.
    fn vporq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPORQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPORQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPORQ256");
        }
    }
    /// Emits `VPORQ256_MASK`.
    fn vporq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPORQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPORQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPORQ256_MASK");
        }
    }
    /// Emits `VPORQ256_MASKZ`.
    fn vporq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPORQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPORQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPORQ256_MASKZ");
        }
    }
    /// Emits `VPORQ256RRB`.
    fn vporq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPORQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPORQ256RRB_MASK`.
    fn vporq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPORQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPORQ256RRB_MASKZ`.
    fn vporq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPORQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPORQ512`.
    fn vporq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPORQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPORQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPORQ512");
        }
    }
    /// Emits `VPORQ512_MASK`.
    fn vporq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPORQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPORQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPORQ512_MASK");
        }
    }
    /// Emits `VPORQ512_MASKZ`.
    fn vporq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPORQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPORQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPORQ512_MASKZ");
        }
    }
    /// Emits `VPORQ512RRB`.
    fn vporq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPORQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPORQ512RRB_MASK`.
    fn vporq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPORQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPORQ512RRB_MASKZ`.
    fn vporq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPORQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLD128`.
    fn vprold128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPROLD128RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPROLD128RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLD128");
        }
    }
    /// Emits `VPROLD128_MASK`.
    fn vprold128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPROLD128RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPROLD128RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLD128_MASK");
        }
    }
    /// Emits `VPROLD128_MASKZ`.
    fn vprold128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPROLD128RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPROLD128RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLD128_MASKZ");
        }
    }
    /// Emits `VPROLD128RBI`.
    fn vprold128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLD128RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLD128RBI_MASK`.
    fn vprold128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLD128RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLD128RBI_MASKZ`.
    fn vprold128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLD128RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLD256`.
    fn vprold256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPROLD256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPROLD256RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLD256");
        }
    }
    /// Emits `VPROLD256_MASK`.
    fn vprold256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPROLD256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPROLD256RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLD256_MASK");
        }
    }
    /// Emits `VPROLD256_MASKZ`.
    fn vprold256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPROLD256RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPROLD256RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLD256_MASKZ");
        }
    }
    /// Emits `VPROLD256RBI`.
    fn vprold256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLD256RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLD256RBI_MASK`.
    fn vprold256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLD256RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLD256RBI_MASKZ`.
    fn vprold256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLD256RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLD512`.
    fn vprold512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPROLD512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPROLD512RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLD512");
        }
    }
    /// Emits `VPROLD512_MASK`.
    fn vprold512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPROLD512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPROLD512RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLD512_MASK");
        }
    }
    /// Emits `VPROLD512_MASKZ`.
    fn vprold512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPROLD512RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPROLD512RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLD512_MASKZ");
        }
    }
    /// Emits `VPROLD512RBI`.
    fn vprold512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLD512RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLD512RBI_MASK`.
    fn vprold512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLD512RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLD512RBI_MASKZ`.
    fn vprold512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLD512RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLQ128`.
    fn vprolq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPROLQ128RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPROLQ128RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLQ128");
        }
    }
    /// Emits `VPROLQ128_MASK`.
    fn vprolq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPROLQ128RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPROLQ128RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLQ128_MASK");
        }
    }
    /// Emits `VPROLQ128_MASKZ`.
    fn vprolq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPROLQ128RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPROLQ128RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLQ128_MASKZ");
        }
    }
    /// Emits `VPROLQ128RBI`.
    fn vprolq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLQ128RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLQ128RBI_MASK`.
    fn vprolq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLQ128RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLQ128RBI_MASKZ`.
    fn vprolq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLQ128RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLQ256`.
    fn vprolq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPROLQ256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPROLQ256RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLQ256");
        }
    }
    /// Emits `VPROLQ256_MASK`.
    fn vprolq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPROLQ256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPROLQ256RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLQ256_MASK");
        }
    }
    /// Emits `VPROLQ256_MASKZ`.
    fn vprolq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPROLQ256RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPROLQ256RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLQ256_MASKZ");
        }
    }
    /// Emits `VPROLQ256RBI`.
    fn vprolq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLQ256RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLQ256RBI_MASK`.
    fn vprolq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLQ256RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLQ256RBI_MASKZ`.
    fn vprolq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLQ256RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLQ512`.
    fn vprolq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPROLQ512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPROLQ512RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLQ512");
        }
    }
    /// Emits `VPROLQ512_MASK`.
    fn vprolq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPROLQ512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPROLQ512RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLQ512_MASK");
        }
    }
    /// Emits `VPROLQ512_MASKZ`.
    fn vprolq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPROLQ512RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPROLQ512RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLQ512_MASKZ");
        }
    }
    /// Emits `VPROLQ512RBI`.
    fn vprolq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLQ512RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLQ512RBI_MASK`.
    fn vprolq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLQ512RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLQ512RBI_MASKZ`.
    fn vprolq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLQ512RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLVD128`.
    fn vprolvd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPROLVD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPROLVD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLVD128");
        }
    }
    /// Emits `VPROLVD128_MASK`.
    fn vprolvd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPROLVD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPROLVD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLVD128_MASK");
        }
    }
    /// Emits `VPROLVD128_MASKZ`.
    fn vprolvd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPROLVD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPROLVD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLVD128_MASKZ");
        }
    }
    /// Emits `VPROLVD128RRB`.
    fn vprolvd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLVD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLVD128RRB_MASK`.
    fn vprolvd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLVD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLVD128RRB_MASKZ`.
    fn vprolvd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLVD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLVD256`.
    fn vprolvd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPROLVD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPROLVD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLVD256");
        }
    }
    /// Emits `VPROLVD256_MASK`.
    fn vprolvd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPROLVD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPROLVD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLVD256_MASK");
        }
    }
    /// Emits `VPROLVD256_MASKZ`.
    fn vprolvd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPROLVD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPROLVD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLVD256_MASKZ");
        }
    }
    /// Emits `VPROLVD256RRB`.
    fn vprolvd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLVD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLVD256RRB_MASK`.
    fn vprolvd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLVD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLVD256RRB_MASKZ`.
    fn vprolvd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLVD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLVD512`.
    fn vprolvd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPROLVD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPROLVD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLVD512");
        }
    }
    /// Emits `VPROLVD512_MASK`.
    fn vprolvd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPROLVD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPROLVD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLVD512_MASK");
        }
    }
    /// Emits `VPROLVD512_MASKZ`.
    fn vprolvd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPROLVD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPROLVD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLVD512_MASKZ");
        }
    }
    /// Emits `VPROLVD512RRB`.
    fn vprolvd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLVD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLVD512RRB_MASK`.
    fn vprolvd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLVD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLVD512RRB_MASKZ`.
    fn vprolvd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLVD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLVQ128`.
    fn vprolvq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPROLVQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPROLVQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLVQ128");
        }
    }
    /// Emits `VPROLVQ128_MASK`.
    fn vprolvq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPROLVQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPROLVQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLVQ128_MASK");
        }
    }
    /// Emits `VPROLVQ128_MASKZ`.
    fn vprolvq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPROLVQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPROLVQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLVQ128_MASKZ");
        }
    }
    /// Emits `VPROLVQ128RRB`.
    fn vprolvq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLVQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLVQ128RRB_MASK`.
    fn vprolvq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLVQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLVQ128RRB_MASKZ`.
    fn vprolvq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLVQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLVQ256`.
    fn vprolvq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPROLVQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPROLVQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLVQ256");
        }
    }
    /// Emits `VPROLVQ256_MASK`.
    fn vprolvq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPROLVQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPROLVQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLVQ256_MASK");
        }
    }
    /// Emits `VPROLVQ256_MASKZ`.
    fn vprolvq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPROLVQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPROLVQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLVQ256_MASKZ");
        }
    }
    /// Emits `VPROLVQ256RRB`.
    fn vprolvq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLVQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLVQ256RRB_MASK`.
    fn vprolvq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLVQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLVQ256RRB_MASKZ`.
    fn vprolvq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLVQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLVQ512`.
    fn vprolvq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPROLVQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPROLVQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLVQ512");
        }
    }
    /// Emits `VPROLVQ512_MASK`.
    fn vprolvq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPROLVQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPROLVQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLVQ512_MASK");
        }
    }
    /// Emits `VPROLVQ512_MASKZ`.
    fn vprolvq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPROLVQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPROLVQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPROLVQ512_MASKZ");
        }
    }
    /// Emits `VPROLVQ512RRB`.
    fn vprolvq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLVQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLVQ512RRB_MASK`.
    fn vprolvq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLVQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPROLVQ512RRB_MASKZ`.
    fn vprolvq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPROLVQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORD128`.
    fn vprord128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPRORD128RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPRORD128RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORD128");
        }
    }
    /// Emits `VPRORD128_MASK`.
    fn vprord128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPRORD128RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPRORD128RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORD128_MASK");
        }
    }
    /// Emits `VPRORD128_MASKZ`.
    fn vprord128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPRORD128RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPRORD128RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORD128_MASKZ");
        }
    }
    /// Emits `VPRORD128RBI`.
    fn vprord128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORD128RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORD128RBI_MASK`.
    fn vprord128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORD128RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORD128RBI_MASKZ`.
    fn vprord128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORD128RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORD256`.
    fn vprord256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPRORD256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPRORD256RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORD256");
        }
    }
    /// Emits `VPRORD256_MASK`.
    fn vprord256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPRORD256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPRORD256RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORD256_MASK");
        }
    }
    /// Emits `VPRORD256_MASKZ`.
    fn vprord256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPRORD256RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPRORD256RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORD256_MASKZ");
        }
    }
    /// Emits `VPRORD256RBI`.
    fn vprord256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORD256RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORD256RBI_MASK`.
    fn vprord256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORD256RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORD256RBI_MASKZ`.
    fn vprord256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORD256RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORD512`.
    fn vprord512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPRORD512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPRORD512RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORD512");
        }
    }
    /// Emits `VPRORD512_MASK`.
    fn vprord512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPRORD512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPRORD512RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORD512_MASK");
        }
    }
    /// Emits `VPRORD512_MASKZ`.
    fn vprord512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPRORD512RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPRORD512RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORD512_MASKZ");
        }
    }
    /// Emits `VPRORD512RBI`.
    fn vprord512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORD512RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORD512RBI_MASK`.
    fn vprord512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORD512RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORD512RBI_MASKZ`.
    fn vprord512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORD512RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORQ128`.
    fn vprorq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPRORQ128RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPRORQ128RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORQ128");
        }
    }
    /// Emits `VPRORQ128_MASK`.
    fn vprorq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPRORQ128RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPRORQ128RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORQ128_MASK");
        }
    }
    /// Emits `VPRORQ128_MASKZ`.
    fn vprorq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPRORQ128RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPRORQ128RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORQ128_MASKZ");
        }
    }
    /// Emits `VPRORQ128RBI`.
    fn vprorq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORQ128RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORQ128RBI_MASK`.
    fn vprorq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORQ128RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORQ128RBI_MASKZ`.
    fn vprorq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORQ128RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORQ256`.
    fn vprorq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPRORQ256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPRORQ256RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORQ256");
        }
    }
    /// Emits `VPRORQ256_MASK`.
    fn vprorq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPRORQ256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPRORQ256RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORQ256_MASK");
        }
    }
    /// Emits `VPRORQ256_MASKZ`.
    fn vprorq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPRORQ256RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPRORQ256RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORQ256_MASKZ");
        }
    }
    /// Emits `VPRORQ256RBI`.
    fn vprorq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORQ256RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORQ256RBI_MASK`.
    fn vprorq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORQ256RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORQ256RBI_MASKZ`.
    fn vprorq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORQ256RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORQ512`.
    fn vprorq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPRORQ512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPRORQ512RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORQ512");
        }
    }
    /// Emits `VPRORQ512_MASK`.
    fn vprorq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPRORQ512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPRORQ512RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORQ512_MASK");
        }
    }
    /// Emits `VPRORQ512_MASKZ`.
    fn vprorq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPRORQ512RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPRORQ512RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORQ512_MASKZ");
        }
    }
    /// Emits `VPRORQ512RBI`.
    fn vprorq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORQ512RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORQ512RBI_MASK`.
    fn vprorq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORQ512RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORQ512RBI_MASKZ`.
    fn vprorq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORQ512RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORVD128`.
    fn vprorvd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPRORVD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPRORVD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORVD128");
        }
    }
    /// Emits `VPRORVD128_MASK`.
    fn vprorvd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPRORVD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPRORVD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORVD128_MASK");
        }
    }
    /// Emits `VPRORVD128_MASKZ`.
    fn vprorvd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPRORVD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPRORVD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORVD128_MASKZ");
        }
    }
    /// Emits `VPRORVD128RRB`.
    fn vprorvd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORVD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORVD128RRB_MASK`.
    fn vprorvd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORVD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORVD128RRB_MASKZ`.
    fn vprorvd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORVD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORVD256`.
    fn vprorvd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPRORVD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPRORVD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORVD256");
        }
    }
    /// Emits `VPRORVD256_MASK`.
    fn vprorvd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPRORVD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPRORVD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORVD256_MASK");
        }
    }
    /// Emits `VPRORVD256_MASKZ`.
    fn vprorvd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPRORVD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPRORVD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORVD256_MASKZ");
        }
    }
    /// Emits `VPRORVD256RRB`.
    fn vprorvd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORVD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORVD256RRB_MASK`.
    fn vprorvd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORVD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORVD256RRB_MASKZ`.
    fn vprorvd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORVD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORVD512`.
    fn vprorvd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPRORVD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPRORVD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORVD512");
        }
    }
    /// Emits `VPRORVD512_MASK`.
    fn vprorvd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPRORVD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPRORVD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORVD512_MASK");
        }
    }
    /// Emits `VPRORVD512_MASKZ`.
    fn vprorvd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPRORVD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPRORVD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORVD512_MASKZ");
        }
    }
    /// Emits `VPRORVD512RRB`.
    fn vprorvd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORVD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORVD512RRB_MASK`.
    fn vprorvd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORVD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORVD512RRB_MASKZ`.
    fn vprorvd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORVD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORVQ128`.
    fn vprorvq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPRORVQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPRORVQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORVQ128");
        }
    }
    /// Emits `VPRORVQ128_MASK`.
    fn vprorvq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPRORVQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPRORVQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORVQ128_MASK");
        }
    }
    /// Emits `VPRORVQ128_MASKZ`.
    fn vprorvq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPRORVQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPRORVQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORVQ128_MASKZ");
        }
    }
    /// Emits `VPRORVQ128RRB`.
    fn vprorvq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORVQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORVQ128RRB_MASK`.
    fn vprorvq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORVQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORVQ128RRB_MASKZ`.
    fn vprorvq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORVQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORVQ256`.
    fn vprorvq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPRORVQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPRORVQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORVQ256");
        }
    }
    /// Emits `VPRORVQ256_MASK`.
    fn vprorvq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPRORVQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPRORVQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORVQ256_MASK");
        }
    }
    /// Emits `VPRORVQ256_MASKZ`.
    fn vprorvq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPRORVQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPRORVQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORVQ256_MASKZ");
        }
    }
    /// Emits `VPRORVQ256RRB`.
    fn vprorvq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORVQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORVQ256RRB_MASK`.
    fn vprorvq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORVQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORVQ256RRB_MASKZ`.
    fn vprorvq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORVQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORVQ512`.
    fn vprorvq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPRORVQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPRORVQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORVQ512");
        }
    }
    /// Emits `VPRORVQ512_MASK`.
    fn vprorvq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPRORVQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPRORVQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORVQ512_MASK");
        }
    }
    /// Emits `VPRORVQ512_MASKZ`.
    fn vprorvq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPRORVQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPRORVQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPRORVQ512_MASKZ");
        }
    }
    /// Emits `VPRORVQ512RRB`.
    fn vprorvq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORVQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORVQ512RRB_MASK`.
    fn vprorvq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORVQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPRORVQ512RRB_MASKZ`.
    fn vprorvq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPRORVQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSCATTERDD128MR`.
    fn vpscatterdd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPSCATTERDD128MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPSCATTERDD256MR`.
    fn vpscatterdd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPSCATTERDD256MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPSCATTERDD512MR`.
    fn vpscatterdd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPSCATTERDD512MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPSCATTERDQ128MR`.
    fn vpscatterdq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPSCATTERDQ128MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPSCATTERDQ256MR`.
    fn vpscatterdq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPSCATTERDQ256MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPSCATTERDQ512MR`.
    fn vpscatterdq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPSCATTERDQ512MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPSCATTERQD128MR`.
    fn vpscatterqd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPSCATTERQD128MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPSCATTERQD256MR`.
    fn vpscatterqd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPSCATTERQD256MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPSCATTERQD512MR`.
    fn vpscatterqd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPSCATTERQD512MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPSCATTERQQ128MR`.
    fn vpscatterqq128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPSCATTERQQ128MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPSCATTERQQ256MR`.
    fn vpscatterqq256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPSCATTERQQ256MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPSCATTERQQ512MR`.
    fn vpscatterqq512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VPSCATTERQQ512MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VPSHUFD128`.
    fn vpshufd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSHUFD128RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSHUFD128RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSHUFD128");
        }
    }
    /// Emits `VPSHUFD128_MASK`.
    fn vpshufd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSHUFD128RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSHUFD128RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSHUFD128_MASK");
        }
    }
    /// Emits `VPSHUFD128_MASKZ`.
    fn vpshufd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSHUFD128RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSHUFD128RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSHUFD128_MASKZ");
        }
    }
    /// Emits `VPSHUFD128RBI`.
    fn vpshufd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSHUFD128RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSHUFD128RBI_MASK`.
    fn vpshufd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSHUFD128RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSHUFD128RBI_MASKZ`.
    fn vpshufd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSHUFD128RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSHUFD256`.
    fn vpshufd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSHUFD256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSHUFD256RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSHUFD256");
        }
    }
    /// Emits `VPSHUFD256_MASK`.
    fn vpshufd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSHUFD256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSHUFD256RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSHUFD256_MASK");
        }
    }
    /// Emits `VPSHUFD256_MASKZ`.
    fn vpshufd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSHUFD256RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSHUFD256RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSHUFD256_MASKZ");
        }
    }
    /// Emits `VPSHUFD256RBI`.
    fn vpshufd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSHUFD256RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSHUFD256RBI_MASK`.
    fn vpshufd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSHUFD256RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSHUFD256RBI_MASKZ`.
    fn vpshufd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSHUFD256RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSHUFD512`.
    fn vpshufd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSHUFD512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSHUFD512RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSHUFD512");
        }
    }
    /// Emits `VPSHUFD512_MASK`.
    fn vpshufd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSHUFD512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSHUFD512RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSHUFD512_MASK");
        }
    }
    /// Emits `VPSHUFD512_MASKZ`.
    fn vpshufd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSHUFD512RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSHUFD512RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSHUFD512_MASKZ");
        }
    }
    /// Emits `VPSHUFD512RBI`.
    fn vpshufd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSHUFD512RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSHUFD512RBI_MASK`.
    fn vpshufd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSHUFD512RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSHUFD512RBI_MASKZ`.
    fn vpshufd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSHUFD512RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLD128`.
    fn vpslld128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSLLD128RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLD128RRM, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSLLD128RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLD128");
        }
    }
    /// Emits `VPSLLD128_MASK`.
    fn vpslld128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSLLD128RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSLLD128RMI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLD128_MASK");
        }
    }
    /// Emits `VPSLLD128_MASKZ`.
    fn vpslld128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSLLD128RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSLLD128RMI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLD128_MASKZ");
        }
    }
    /// Emits `VPSLLD128RBI`.
    fn vpslld128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLD128RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLD128RBI_MASK`.
    fn vpslld128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLD128RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLD128RBI_MASKZ`.
    fn vpslld128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLD128RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLD256`.
    fn vpslld256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSLLD256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLD256RRM, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSLLD256RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLD256");
        }
    }
    /// Emits `VPSLLD256_MASK`.
    fn vpslld256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSLLD256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSLLD256RMI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLD256_MASK");
        }
    }
    /// Emits `VPSLLD256_MASKZ`.
    fn vpslld256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSLLD256RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSLLD256RMI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLD256_MASKZ");
        }
    }
    /// Emits `VPSLLD256RBI`.
    fn vpslld256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLD256RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLD256RBI_MASK`.
    fn vpslld256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLD256RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLD256RBI_MASKZ`.
    fn vpslld256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLD256RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLD512`.
    fn vpslld512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSLLD512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSLLD512RMI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLD512");
        }
    }
    /// Emits `VPSLLD512_MASK`.
    fn vpslld512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSLLD512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSLLD512RMI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLD512_MASK");
        }
    }
    /// Emits `VPSLLD512_MASKZ`.
    fn vpslld512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSLLD512RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSLLD512RMI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLD512_MASKZ");
        }
    }
    /// Emits `VPSLLD512RBI`.
    fn vpslld512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLD512RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLD512RBI_MASK`.
    fn vpslld512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLD512RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLD512RBI_MASKZ`.
    fn vpslld512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLD512RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLQ128`.
    fn vpsllq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSLLQ128RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLQ128RRM, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSLLQ128RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLQ128");
        }
    }
    /// Emits `VPSLLQ128_MASK`.
    fn vpsllq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSLLQ128RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSLLQ128RMI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLQ128_MASK");
        }
    }
    /// Emits `VPSLLQ128_MASKZ`.
    fn vpsllq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSLLQ128RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSLLQ128RMI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLQ128_MASKZ");
        }
    }
    /// Emits `VPSLLQ128RBI`.
    fn vpsllq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLQ128RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLQ128RBI_MASK`.
    fn vpsllq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLQ128RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLQ128RBI_MASKZ`.
    fn vpsllq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLQ128RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLQ256`.
    fn vpsllq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSLLQ256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLQ256RRM, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSLLQ256RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLQ256");
        }
    }
    /// Emits `VPSLLQ256_MASK`.
    fn vpsllq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSLLQ256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSLLQ256RMI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLQ256_MASK");
        }
    }
    /// Emits `VPSLLQ256_MASKZ`.
    fn vpsllq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSLLQ256RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSLLQ256RMI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLQ256_MASKZ");
        }
    }
    /// Emits `VPSLLQ256RBI`.
    fn vpsllq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLQ256RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLQ256RBI_MASK`.
    fn vpsllq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLQ256RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLQ256RBI_MASKZ`.
    fn vpsllq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLQ256RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLQ512`.
    fn vpsllq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSLLQ512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSLLQ512RMI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLQ512");
        }
    }
    /// Emits `VPSLLQ512_MASK`.
    fn vpsllq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSLLQ512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSLLQ512RMI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLQ512_MASK");
        }
    }
    /// Emits `VPSLLQ512_MASKZ`.
    fn vpsllq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSLLQ512RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSLLQ512RMI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLQ512_MASKZ");
        }
    }
    /// Emits `VPSLLQ512RBI`.
    fn vpsllq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLQ512RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLQ512RBI_MASK`.
    fn vpsllq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLQ512RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLQ512RBI_MASKZ`.
    fn vpsllq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLQ512RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLVD128`.
    fn vpsllvd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLVD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLVD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLVD128");
        }
    }
    /// Emits `VPSLLVD128_MASK`.
    fn vpsllvd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLVD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLVD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLVD128_MASK");
        }
    }
    /// Emits `VPSLLVD128_MASKZ`.
    fn vpsllvd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLVD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLVD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLVD128_MASKZ");
        }
    }
    /// Emits `VPSLLVD128RRB`.
    fn vpsllvd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLVD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLVD128RRB_MASK`.
    fn vpsllvd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLVD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLVD128RRB_MASKZ`.
    fn vpsllvd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLVD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLVD256`.
    fn vpsllvd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLVD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLVD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLVD256");
        }
    }
    /// Emits `VPSLLVD256_MASK`.
    fn vpsllvd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLVD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLVD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLVD256_MASK");
        }
    }
    /// Emits `VPSLLVD256_MASKZ`.
    fn vpsllvd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLVD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLVD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLVD256_MASKZ");
        }
    }
    /// Emits `VPSLLVD256RRB`.
    fn vpsllvd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLVD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLVD256RRB_MASK`.
    fn vpsllvd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLVD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLVD256RRB_MASKZ`.
    fn vpsllvd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLVD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLVD512`.
    fn vpsllvd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLVD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLVD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLVD512");
        }
    }
    /// Emits `VPSLLVD512_MASK`.
    fn vpsllvd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLVD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLVD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLVD512_MASK");
        }
    }
    /// Emits `VPSLLVD512_MASKZ`.
    fn vpsllvd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLVD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLVD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLVD512_MASKZ");
        }
    }
    /// Emits `VPSLLVD512RRB`.
    fn vpsllvd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLVD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLVD512RRB_MASK`.
    fn vpsllvd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLVD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLVD512RRB_MASKZ`.
    fn vpsllvd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLVD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLVQ128`.
    fn vpsllvq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLVQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLVQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLVQ128");
        }
    }
    /// Emits `VPSLLVQ128_MASK`.
    fn vpsllvq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLVQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLVQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLVQ128_MASK");
        }
    }
    /// Emits `VPSLLVQ128_MASKZ`.
    fn vpsllvq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLVQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLVQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLVQ128_MASKZ");
        }
    }
    /// Emits `VPSLLVQ128RRB`.
    fn vpsllvq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLVQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLVQ128RRB_MASK`.
    fn vpsllvq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLVQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLVQ128RRB_MASKZ`.
    fn vpsllvq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLVQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLVQ256`.
    fn vpsllvq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLVQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLVQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLVQ256");
        }
    }
    /// Emits `VPSLLVQ256_MASK`.
    fn vpsllvq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLVQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLVQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLVQ256_MASK");
        }
    }
    /// Emits `VPSLLVQ256_MASKZ`.
    fn vpsllvq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLVQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLVQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLVQ256_MASKZ");
        }
    }
    /// Emits `VPSLLVQ256RRB`.
    fn vpsllvq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLVQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLVQ256RRB_MASK`.
    fn vpsllvq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLVQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLVQ256RRB_MASKZ`.
    fn vpsllvq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLVQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLVQ512`.
    fn vpsllvq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLVQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLVQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLVQ512");
        }
    }
    /// Emits `VPSLLVQ512_MASK`.
    fn vpsllvq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLVQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLVQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLVQ512_MASK");
        }
    }
    /// Emits `VPSLLVQ512_MASKZ`.
    fn vpsllvq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSLLVQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSLLVQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSLLVQ512_MASKZ");
        }
    }
    /// Emits `VPSLLVQ512RRB`.
    fn vpsllvq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLVQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLVQ512RRB_MASK`.
    fn vpsllvq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLVQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSLLVQ512RRB_MASKZ`.
    fn vpsllvq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSLLVQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAD128`.
    fn vpsrad128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRAD128RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAD128RRM, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRAD128RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAD128");
        }
    }
    /// Emits `VPSRAD128_MASK`.
    fn vpsrad128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRAD128RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRAD128RMI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAD128_MASK");
        }
    }
    /// Emits `VPSRAD128_MASKZ`.
    fn vpsrad128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRAD128RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRAD128RMI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAD128_MASKZ");
        }
    }
    /// Emits `VPSRAD128RBI`.
    fn vpsrad128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAD128RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAD128RBI_MASK`.
    fn vpsrad128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAD128RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAD128RBI_MASKZ`.
    fn vpsrad128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAD128RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAD256`.
    fn vpsrad256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRAD256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAD256RRM, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRAD256RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAD256");
        }
    }
    /// Emits `VPSRAD256_MASK`.
    fn vpsrad256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRAD256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRAD256RMI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAD256_MASK");
        }
    }
    /// Emits `VPSRAD256_MASKZ`.
    fn vpsrad256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRAD256RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRAD256RMI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAD256_MASKZ");
        }
    }
    /// Emits `VPSRAD256RBI`.
    fn vpsrad256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAD256RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAD256RBI_MASK`.
    fn vpsrad256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAD256RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAD256RBI_MASKZ`.
    fn vpsrad256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAD256RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAD512`.
    fn vpsrad512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRAD512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRAD512RMI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAD512");
        }
    }
    /// Emits `VPSRAD512_MASK`.
    fn vpsrad512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRAD512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRAD512RMI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAD512_MASK");
        }
    }
    /// Emits `VPSRAD512_MASKZ`.
    fn vpsrad512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRAD512RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRAD512RMI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAD512_MASKZ");
        }
    }
    /// Emits `VPSRAD512RBI`.
    fn vpsrad512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAD512RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAD512RBI_MASK`.
    fn vpsrad512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAD512RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAD512RBI_MASKZ`.
    fn vpsrad512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAD512RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAQ128`.
    fn vpsraq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRAQ128RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRAQ128RMI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAQ128");
        }
    }
    /// Emits `VPSRAQ128_MASK`.
    fn vpsraq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRAQ128RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRAQ128RMI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAQ128_MASK");
        }
    }
    /// Emits `VPSRAQ128_MASKZ`.
    fn vpsraq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRAQ128RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRAQ128RMI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAQ128_MASKZ");
        }
    }
    /// Emits `VPSRAQ128RBI`.
    fn vpsraq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAQ128RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAQ128RBI_MASK`.
    fn vpsraq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAQ128RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAQ128RBI_MASKZ`.
    fn vpsraq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAQ128RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAQ256`.
    fn vpsraq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRAQ256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRAQ256RMI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAQ256");
        }
    }
    /// Emits `VPSRAQ256_MASK`.
    fn vpsraq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRAQ256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRAQ256RMI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAQ256_MASK");
        }
    }
    /// Emits `VPSRAQ256_MASKZ`.
    fn vpsraq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRAQ256RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRAQ256RMI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAQ256_MASKZ");
        }
    }
    /// Emits `VPSRAQ256RBI`.
    fn vpsraq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAQ256RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAQ256RBI_MASK`.
    fn vpsraq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAQ256RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAQ256RBI_MASKZ`.
    fn vpsraq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAQ256RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAQ512`.
    fn vpsraq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRAQ512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRAQ512RMI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAQ512");
        }
    }
    /// Emits `VPSRAQ512_MASK`.
    fn vpsraq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRAQ512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRAQ512RMI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAQ512_MASK");
        }
    }
    /// Emits `VPSRAQ512_MASKZ`.
    fn vpsraq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRAQ512RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRAQ512RMI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAQ512_MASKZ");
        }
    }
    /// Emits `VPSRAQ512RBI`.
    fn vpsraq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAQ512RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAQ512RBI_MASK`.
    fn vpsraq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAQ512RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAQ512RBI_MASKZ`.
    fn vpsraq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAQ512RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAVD128`.
    fn vpsravd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAVD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAVD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAVD128");
        }
    }
    /// Emits `VPSRAVD128_MASK`.
    fn vpsravd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAVD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAVD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAVD128_MASK");
        }
    }
    /// Emits `VPSRAVD128_MASKZ`.
    fn vpsravd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAVD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAVD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAVD128_MASKZ");
        }
    }
    /// Emits `VPSRAVD128RRB`.
    fn vpsravd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAVD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAVD128RRB_MASK`.
    fn vpsravd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAVD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAVD128RRB_MASKZ`.
    fn vpsravd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAVD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAVD256`.
    fn vpsravd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAVD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAVD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAVD256");
        }
    }
    /// Emits `VPSRAVD256_MASK`.
    fn vpsravd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAVD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAVD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAVD256_MASK");
        }
    }
    /// Emits `VPSRAVD256_MASKZ`.
    fn vpsravd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAVD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAVD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAVD256_MASKZ");
        }
    }
    /// Emits `VPSRAVD256RRB`.
    fn vpsravd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAVD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAVD256RRB_MASK`.
    fn vpsravd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAVD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAVD256RRB_MASKZ`.
    fn vpsravd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAVD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAVD512`.
    fn vpsravd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAVD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAVD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAVD512");
        }
    }
    /// Emits `VPSRAVD512_MASK`.
    fn vpsravd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAVD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAVD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAVD512_MASK");
        }
    }
    /// Emits `VPSRAVD512_MASKZ`.
    fn vpsravd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAVD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAVD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAVD512_MASKZ");
        }
    }
    /// Emits `VPSRAVD512RRB`.
    fn vpsravd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAVD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAVD512RRB_MASK`.
    fn vpsravd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAVD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAVD512RRB_MASKZ`.
    fn vpsravd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAVD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAVQ128`.
    fn vpsravq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAVQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAVQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAVQ128");
        }
    }
    /// Emits `VPSRAVQ128_MASK`.
    fn vpsravq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAVQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAVQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAVQ128_MASK");
        }
    }
    /// Emits `VPSRAVQ128_MASKZ`.
    fn vpsravq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAVQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAVQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAVQ128_MASKZ");
        }
    }
    /// Emits `VPSRAVQ128RRB`.
    fn vpsravq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAVQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAVQ128RRB_MASK`.
    fn vpsravq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAVQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAVQ128RRB_MASKZ`.
    fn vpsravq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAVQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAVQ256`.
    fn vpsravq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAVQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAVQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAVQ256");
        }
    }
    /// Emits `VPSRAVQ256_MASK`.
    fn vpsravq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAVQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAVQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAVQ256_MASK");
        }
    }
    /// Emits `VPSRAVQ256_MASKZ`.
    fn vpsravq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAVQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAVQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAVQ256_MASKZ");
        }
    }
    /// Emits `VPSRAVQ256RRB`.
    fn vpsravq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAVQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAVQ256RRB_MASK`.
    fn vpsravq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAVQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAVQ256RRB_MASKZ`.
    fn vpsravq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAVQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAVQ512`.
    fn vpsravq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAVQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAVQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAVQ512");
        }
    }
    /// Emits `VPSRAVQ512_MASK`.
    fn vpsravq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAVQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAVQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAVQ512_MASK");
        }
    }
    /// Emits `VPSRAVQ512_MASKZ`.
    fn vpsravq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRAVQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRAVQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRAVQ512_MASKZ");
        }
    }
    /// Emits `VPSRAVQ512RRB`.
    fn vpsravq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAVQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAVQ512RRB_MASK`.
    fn vpsravq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAVQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRAVQ512RRB_MASKZ`.
    fn vpsravq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRAVQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLD128`.
    fn vpsrld128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRLD128RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLD128RRM, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRLD128RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLD128");
        }
    }
    /// Emits `VPSRLD128_MASK`.
    fn vpsrld128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRLD128RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRLD128RMI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLD128_MASK");
        }
    }
    /// Emits `VPSRLD128_MASKZ`.
    fn vpsrld128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRLD128RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRLD128RMI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLD128_MASKZ");
        }
    }
    /// Emits `VPSRLD128RBI`.
    fn vpsrld128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLD128RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLD128RBI_MASK`.
    fn vpsrld128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLD128RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLD128RBI_MASKZ`.
    fn vpsrld128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLD128RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLD256`.
    fn vpsrld256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRLD256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLD256RRM, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRLD256RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLD256");
        }
    }
    /// Emits `VPSRLD256_MASK`.
    fn vpsrld256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRLD256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRLD256RMI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLD256_MASK");
        }
    }
    /// Emits `VPSRLD256_MASKZ`.
    fn vpsrld256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRLD256RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRLD256RMI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLD256_MASKZ");
        }
    }
    /// Emits `VPSRLD256RBI`.
    fn vpsrld256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLD256RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLD256RBI_MASK`.
    fn vpsrld256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLD256RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLD256RBI_MASKZ`.
    fn vpsrld256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLD256RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLD512`.
    fn vpsrld512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRLD512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRLD512RMI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLD512");
        }
    }
    /// Emits `VPSRLD512_MASK`.
    fn vpsrld512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRLD512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRLD512RMI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLD512_MASK");
        }
    }
    /// Emits `VPSRLD512_MASKZ`.
    fn vpsrld512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRLD512RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRLD512RMI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLD512_MASKZ");
        }
    }
    /// Emits `VPSRLD512RBI`.
    fn vpsrld512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLD512RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLD512RBI_MASK`.
    fn vpsrld512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLD512RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLD512RBI_MASKZ`.
    fn vpsrld512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLD512RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLQ128`.
    fn vpsrlq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRLQ128RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLQ128RRM, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRLQ128RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLQ128");
        }
    }
    /// Emits `VPSRLQ128_MASK`.
    fn vpsrlq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRLQ128RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRLQ128RMI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLQ128_MASK");
        }
    }
    /// Emits `VPSRLQ128_MASKZ`.
    fn vpsrlq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRLQ128RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRLQ128RMI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLQ128_MASKZ");
        }
    }
    /// Emits `VPSRLQ128RBI`.
    fn vpsrlq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLQ128RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLQ128RBI_MASK`.
    fn vpsrlq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLQ128RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLQ128RBI_MASKZ`.
    fn vpsrlq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLQ128RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLQ256`.
    fn vpsrlq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRLQ256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLQ256RRM, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRLQ256RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLQ256");
        }
    }
    /// Emits `VPSRLQ256_MASK`.
    fn vpsrlq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRLQ256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRLQ256RMI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLQ256_MASK");
        }
    }
    /// Emits `VPSRLQ256_MASKZ`.
    fn vpsrlq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRLQ256RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRLQ256RMI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLQ256_MASKZ");
        }
    }
    /// Emits `VPSRLQ256RBI`.
    fn vpsrlq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLQ256RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLQ256RBI_MASK`.
    fn vpsrlq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLQ256RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLQ256RBI_MASKZ`.
    fn vpsrlq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLQ256RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLQ512`.
    fn vpsrlq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRLQ512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRLQ512RMI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLQ512");
        }
    }
    /// Emits `VPSRLQ512_MASK`.
    fn vpsrlq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRLQ512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRLQ512RMI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLQ512_MASK");
        }
    }
    /// Emits `VPSRLQ512_MASKZ`.
    fn vpsrlq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VPSRLQ512RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VPSRLQ512RMI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLQ512_MASKZ");
        }
    }
    /// Emits `VPSRLQ512RBI`.
    fn vpsrlq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLQ512RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLQ512RBI_MASK`.
    fn vpsrlq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLQ512RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLQ512RBI_MASKZ`.
    fn vpsrlq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLQ512RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLVD128`.
    fn vpsrlvd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLVD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLVD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLVD128");
        }
    }
    /// Emits `VPSRLVD128_MASK`.
    fn vpsrlvd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLVD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLVD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLVD128_MASK");
        }
    }
    /// Emits `VPSRLVD128_MASKZ`.
    fn vpsrlvd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLVD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLVD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLVD128_MASKZ");
        }
    }
    /// Emits `VPSRLVD128RRB`.
    fn vpsrlvd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLVD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLVD128RRB_MASK`.
    fn vpsrlvd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLVD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLVD128RRB_MASKZ`.
    fn vpsrlvd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLVD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLVD256`.
    fn vpsrlvd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLVD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLVD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLVD256");
        }
    }
    /// Emits `VPSRLVD256_MASK`.
    fn vpsrlvd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLVD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLVD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLVD256_MASK");
        }
    }
    /// Emits `VPSRLVD256_MASKZ`.
    fn vpsrlvd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLVD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLVD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLVD256_MASKZ");
        }
    }
    /// Emits `VPSRLVD256RRB`.
    fn vpsrlvd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLVD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLVD256RRB_MASK`.
    fn vpsrlvd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLVD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLVD256RRB_MASKZ`.
    fn vpsrlvd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLVD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLVD512`.
    fn vpsrlvd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLVD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLVD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLVD512");
        }
    }
    /// Emits `VPSRLVD512_MASK`.
    fn vpsrlvd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLVD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLVD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLVD512_MASK");
        }
    }
    /// Emits `VPSRLVD512_MASKZ`.
    fn vpsrlvd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLVD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLVD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLVD512_MASKZ");
        }
    }
    /// Emits `VPSRLVD512RRB`.
    fn vpsrlvd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLVD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLVD512RRB_MASK`.
    fn vpsrlvd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLVD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLVD512RRB_MASKZ`.
    fn vpsrlvd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLVD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLVQ128`.
    fn vpsrlvq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLVQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLVQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLVQ128");
        }
    }
    /// Emits `VPSRLVQ128_MASK`.
    fn vpsrlvq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLVQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLVQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLVQ128_MASK");
        }
    }
    /// Emits `VPSRLVQ128_MASKZ`.
    fn vpsrlvq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLVQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLVQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLVQ128_MASKZ");
        }
    }
    /// Emits `VPSRLVQ128RRB`.
    fn vpsrlvq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLVQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLVQ128RRB_MASK`.
    fn vpsrlvq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLVQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLVQ128RRB_MASKZ`.
    fn vpsrlvq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLVQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLVQ256`.
    fn vpsrlvq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLVQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLVQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLVQ256");
        }
    }
    /// Emits `VPSRLVQ256_MASK`.
    fn vpsrlvq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLVQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLVQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLVQ256_MASK");
        }
    }
    /// Emits `VPSRLVQ256_MASKZ`.
    fn vpsrlvq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLVQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLVQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLVQ256_MASKZ");
        }
    }
    /// Emits `VPSRLVQ256RRB`.
    fn vpsrlvq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLVQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLVQ256RRB_MASK`.
    fn vpsrlvq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLVQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLVQ256RRB_MASKZ`.
    fn vpsrlvq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLVQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLVQ512`.
    fn vpsrlvq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLVQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLVQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLVQ512");
        }
    }
    /// Emits `VPSRLVQ512_MASK`.
    fn vpsrlvq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLVQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLVQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLVQ512_MASK");
        }
    }
    /// Emits `VPSRLVQ512_MASKZ`.
    fn vpsrlvq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSRLVQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSRLVQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSRLVQ512_MASKZ");
        }
    }
    /// Emits `VPSRLVQ512RRB`.
    fn vpsrlvq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLVQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLVQ512RRB_MASK`.
    fn vpsrlvq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLVQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSRLVQ512RRB_MASKZ`.
    fn vpsrlvq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSRLVQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSUBD128`.
    fn vpsubd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSUBD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSUBD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSUBD128");
        }
    }
    /// Emits `VPSUBD128_MASK`.
    fn vpsubd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSUBD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSUBD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSUBD128_MASK");
        }
    }
    /// Emits `VPSUBD128_MASKZ`.
    fn vpsubd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSUBD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSUBD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSUBD128_MASKZ");
        }
    }
    /// Emits `VPSUBD128RRB`.
    fn vpsubd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSUBD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSUBD128RRB_MASK`.
    fn vpsubd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSUBD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSUBD128RRB_MASKZ`.
    fn vpsubd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSUBD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSUBD256`.
    fn vpsubd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSUBD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSUBD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSUBD256");
        }
    }
    /// Emits `VPSUBD256_MASK`.
    fn vpsubd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSUBD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSUBD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSUBD256_MASK");
        }
    }
    /// Emits `VPSUBD256_MASKZ`.
    fn vpsubd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSUBD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSUBD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSUBD256_MASKZ");
        }
    }
    /// Emits `VPSUBD256RRB`.
    fn vpsubd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSUBD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSUBD256RRB_MASK`.
    fn vpsubd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSUBD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSUBD256RRB_MASKZ`.
    fn vpsubd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSUBD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSUBD512`.
    fn vpsubd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSUBD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSUBD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSUBD512");
        }
    }
    /// Emits `VPSUBD512_MASK`.
    fn vpsubd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSUBD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSUBD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSUBD512_MASK");
        }
    }
    /// Emits `VPSUBD512_MASKZ`.
    fn vpsubd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSUBD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSUBD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSUBD512_MASKZ");
        }
    }
    /// Emits `VPSUBD512RRB`.
    fn vpsubd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSUBD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSUBD512RRB_MASK`.
    fn vpsubd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSUBD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSUBD512RRB_MASKZ`.
    fn vpsubd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSUBD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSUBQ128`.
    fn vpsubq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSUBQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSUBQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSUBQ128");
        }
    }
    /// Emits `VPSUBQ128_MASK`.
    fn vpsubq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSUBQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSUBQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSUBQ128_MASK");
        }
    }
    /// Emits `VPSUBQ128_MASKZ`.
    fn vpsubq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSUBQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSUBQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSUBQ128_MASKZ");
        }
    }
    /// Emits `VPSUBQ128RRB`.
    fn vpsubq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSUBQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSUBQ128RRB_MASK`.
    fn vpsubq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSUBQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSUBQ128RRB_MASKZ`.
    fn vpsubq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSUBQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSUBQ256`.
    fn vpsubq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSUBQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSUBQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSUBQ256");
        }
    }
    /// Emits `VPSUBQ256_MASK`.
    fn vpsubq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSUBQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSUBQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSUBQ256_MASK");
        }
    }
    /// Emits `VPSUBQ256_MASKZ`.
    fn vpsubq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSUBQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSUBQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSUBQ256_MASKZ");
        }
    }
    /// Emits `VPSUBQ256RRB`.
    fn vpsubq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSUBQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSUBQ256RRB_MASK`.
    fn vpsubq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSUBQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSUBQ256RRB_MASKZ`.
    fn vpsubq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSUBQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSUBQ512`.
    fn vpsubq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSUBQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSUBQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSUBQ512");
        }
    }
    /// Emits `VPSUBQ512_MASK`.
    fn vpsubq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSUBQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSUBQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSUBQ512_MASK");
        }
    }
    /// Emits `VPSUBQ512_MASKZ`.
    fn vpsubq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPSUBQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPSUBQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPSUBQ512_MASKZ");
        }
    }
    /// Emits `VPSUBQ512RRB`.
    fn vpsubq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSUBQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSUBQ512RRB_MASK`.
    fn vpsubq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSUBQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPSUBQ512RRB_MASKZ`.
    fn vpsubq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPSUBQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTERNLOGD128`.
    fn vpternlogd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPTERNLOGD128RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPTERNLOGD128RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPTERNLOGD128");
        }
    }
    /// Emits `VPTERNLOGD128_MASK`.
    fn vpternlogd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPTERNLOGD128RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPTERNLOGD128RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPTERNLOGD128_MASK");
        }
    }
    /// Emits `VPTERNLOGD128_MASKZ`.
    fn vpternlogd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPTERNLOGD128RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPTERNLOGD128RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPTERNLOGD128_MASKZ");
        }
    }
    /// Emits `VPTERNLOGD128RRBI`.
    fn vpternlogd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPTERNLOGD128RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPTERNLOGD128RRBI_MASK`.
    fn vpternlogd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPTERNLOGD128RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPTERNLOGD128RRBI_MASKZ`.
    fn vpternlogd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPTERNLOGD128RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPTERNLOGD256`.
    fn vpternlogd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPTERNLOGD256RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPTERNLOGD256RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPTERNLOGD256");
        }
    }
    /// Emits `VPTERNLOGD256_MASK`.
    fn vpternlogd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPTERNLOGD256RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPTERNLOGD256RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPTERNLOGD256_MASK");
        }
    }
    /// Emits `VPTERNLOGD256_MASKZ`.
    fn vpternlogd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPTERNLOGD256RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPTERNLOGD256RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPTERNLOGD256_MASKZ");
        }
    }
    /// Emits `VPTERNLOGD256RRBI`.
    fn vpternlogd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPTERNLOGD256RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPTERNLOGD256RRBI_MASK`.
    fn vpternlogd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPTERNLOGD256RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPTERNLOGD256RRBI_MASKZ`.
    fn vpternlogd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPTERNLOGD256RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPTERNLOGD512`.
    fn vpternlogd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPTERNLOGD512RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPTERNLOGD512RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPTERNLOGD512");
        }
    }
    /// Emits `VPTERNLOGD512_MASK`.
    fn vpternlogd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPTERNLOGD512RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPTERNLOGD512RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPTERNLOGD512_MASK");
        }
    }
    /// Emits `VPTERNLOGD512_MASKZ`.
    fn vpternlogd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPTERNLOGD512RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPTERNLOGD512RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPTERNLOGD512_MASKZ");
        }
    }
    /// Emits `VPTERNLOGD512RRBI`.
    fn vpternlogd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPTERNLOGD512RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPTERNLOGD512RRBI_MASK`.
    fn vpternlogd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPTERNLOGD512RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPTERNLOGD512RRBI_MASKZ`.
    fn vpternlogd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPTERNLOGD512RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPTERNLOGQ128`.
    fn vpternlogq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPTERNLOGQ128RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPTERNLOGQ128RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPTERNLOGQ128");
        }
    }
    /// Emits `VPTERNLOGQ128_MASK`.
    fn vpternlogq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPTERNLOGQ128RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPTERNLOGQ128RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPTERNLOGQ128_MASK");
        }
    }
    /// Emits `VPTERNLOGQ128_MASKZ`.
    fn vpternlogq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPTERNLOGQ128RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPTERNLOGQ128RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPTERNLOGQ128_MASKZ");
        }
    }
    /// Emits `VPTERNLOGQ128RRBI`.
    fn vpternlogq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPTERNLOGQ128RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPTERNLOGQ128RRBI_MASK`.
    fn vpternlogq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPTERNLOGQ128RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPTERNLOGQ128RRBI_MASKZ`.
    fn vpternlogq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPTERNLOGQ128RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPTERNLOGQ256`.
    fn vpternlogq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPTERNLOGQ256RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPTERNLOGQ256RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPTERNLOGQ256");
        }
    }
    /// Emits `VPTERNLOGQ256_MASK`.
    fn vpternlogq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPTERNLOGQ256RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPTERNLOGQ256RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPTERNLOGQ256_MASK");
        }
    }
    /// Emits `VPTERNLOGQ256_MASKZ`.
    fn vpternlogq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPTERNLOGQ256RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPTERNLOGQ256RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPTERNLOGQ256_MASKZ");
        }
    }
    /// Emits `VPTERNLOGQ256RRBI`.
    fn vpternlogq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPTERNLOGQ256RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPTERNLOGQ256RRBI_MASK`.
    fn vpternlogq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPTERNLOGQ256RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPTERNLOGQ256RRBI_MASKZ`.
    fn vpternlogq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPTERNLOGQ256RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPTERNLOGQ512`.
    fn vpternlogq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPTERNLOGQ512RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPTERNLOGQ512RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPTERNLOGQ512");
        }
    }
    /// Emits `VPTERNLOGQ512_MASK`.
    fn vpternlogq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPTERNLOGQ512RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPTERNLOGQ512RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPTERNLOGQ512_MASK");
        }
    }
    /// Emits `VPTERNLOGQ512_MASKZ`.
    fn vpternlogq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VPTERNLOGQ512RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VPTERNLOGQ512RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VPTERNLOGQ512_MASKZ");
        }
    }
    /// Emits `VPTERNLOGQ512RRBI`.
    fn vpternlogq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPTERNLOGQ512RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPTERNLOGQ512RRBI_MASK`.
    fn vpternlogq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPTERNLOGQ512RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPTERNLOGQ512RRBI_MASKZ`.
    fn vpternlogq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VPTERNLOGQ512RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VPTESTMD128K`.
    fn vptestmd128k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTMD128KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTMD128KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTMD128K");
        }
    }
    /// Emits `VPTESTMD128K_MASK`.
    fn vptestmd128k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTMD128KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTMD128KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTMD128K_MASK");
        }
    }
    /// Emits `VPTESTMD128KRB`.
    fn vptestmd128kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTMD128KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTMD128KRB_MASK`.
    fn vptestmd128kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTMD128KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTMD256K`.
    fn vptestmd256k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTMD256KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTMD256KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTMD256K");
        }
    }
    /// Emits `VPTESTMD256K_MASK`.
    fn vptestmd256k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTMD256KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTMD256KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTMD256K_MASK");
        }
    }
    /// Emits `VPTESTMD256KRB`.
    fn vptestmd256kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTMD256KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTMD256KRB_MASK`.
    fn vptestmd256kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTMD256KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTMD512K`.
    fn vptestmd512k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTMD512KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTMD512KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTMD512K");
        }
    }
    /// Emits `VPTESTMD512K_MASK`.
    fn vptestmd512k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTMD512KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTMD512KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTMD512K_MASK");
        }
    }
    /// Emits `VPTESTMD512KRB`.
    fn vptestmd512kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTMD512KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTMD512KRB_MASK`.
    fn vptestmd512kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTMD512KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTMQ128K`.
    fn vptestmq128k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTMQ128KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTMQ128KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTMQ128K");
        }
    }
    /// Emits `VPTESTMQ128K_MASK`.
    fn vptestmq128k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTMQ128KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTMQ128KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTMQ128K_MASK");
        }
    }
    /// Emits `VPTESTMQ128KRB`.
    fn vptestmq128kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTMQ128KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTMQ128KRB_MASK`.
    fn vptestmq128kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTMQ128KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTMQ256K`.
    fn vptestmq256k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTMQ256KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTMQ256KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTMQ256K");
        }
    }
    /// Emits `VPTESTMQ256K_MASK`.
    fn vptestmq256k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTMQ256KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTMQ256KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTMQ256K_MASK");
        }
    }
    /// Emits `VPTESTMQ256KRB`.
    fn vptestmq256kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTMQ256KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTMQ256KRB_MASK`.
    fn vptestmq256kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTMQ256KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTMQ512K`.
    fn vptestmq512k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTMQ512KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTMQ512KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTMQ512K");
        }
    }
    /// Emits `VPTESTMQ512K_MASK`.
    fn vptestmq512k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTMQ512KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTMQ512KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTMQ512K_MASK");
        }
    }
    /// Emits `VPTESTMQ512KRB`.
    fn vptestmq512kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTMQ512KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTMQ512KRB_MASK`.
    fn vptestmq512kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTMQ512KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTNMD128K`.
    fn vptestnmd128k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTNMD128KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTNMD128KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTNMD128K");
        }
    }
    /// Emits `VPTESTNMD128K_MASK`.
    fn vptestnmd128k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTNMD128KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTNMD128KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTNMD128K_MASK");
        }
    }
    /// Emits `VPTESTNMD128KRB`.
    fn vptestnmd128kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTNMD128KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTNMD128KRB_MASK`.
    fn vptestnmd128kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTNMD128KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTNMD256K`.
    fn vptestnmd256k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTNMD256KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTNMD256KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTNMD256K");
        }
    }
    /// Emits `VPTESTNMD256K_MASK`.
    fn vptestnmd256k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTNMD256KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTNMD256KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTNMD256K_MASK");
        }
    }
    /// Emits `VPTESTNMD256KRB`.
    fn vptestnmd256kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTNMD256KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTNMD256KRB_MASK`.
    fn vptestnmd256kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTNMD256KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTNMD512K`.
    fn vptestnmd512k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTNMD512KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTNMD512KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTNMD512K");
        }
    }
    /// Emits `VPTESTNMD512K_MASK`.
    fn vptestnmd512k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTNMD512KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTNMD512KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTNMD512K_MASK");
        }
    }
    /// Emits `VPTESTNMD512KRB`.
    fn vptestnmd512kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTNMD512KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTNMD512KRB_MASK`.
    fn vptestnmd512kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTNMD512KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTNMQ128K`.
    fn vptestnmq128k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTNMQ128KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTNMQ128KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTNMQ128K");
        }
    }
    /// Emits `VPTESTNMQ128K_MASK`.
    fn vptestnmq128k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTNMQ128KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTNMQ128KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTNMQ128K_MASK");
        }
    }
    /// Emits `VPTESTNMQ128KRB`.
    fn vptestnmq128kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTNMQ128KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTNMQ128KRB_MASK`.
    fn vptestnmq128kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTNMQ128KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTNMQ256K`.
    fn vptestnmq256k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTNMQ256KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTNMQ256KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTNMQ256K");
        }
    }
    /// Emits `VPTESTNMQ256K_MASK`.
    fn vptestnmq256k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTNMQ256KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTNMQ256KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTNMQ256K_MASK");
        }
    }
    /// Emits `VPTESTNMQ256KRB`.
    fn vptestnmq256kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTNMQ256KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTNMQ256KRB_MASK`.
    fn vptestnmq256kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTNMQ256KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTNMQ512K`.
    fn vptestnmq512k(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTNMQ512KRR, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTNMQ512KRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTNMQ512K");
        }
    }
    /// Emits `VPTESTNMQ512K_MASK`.
    fn vptestnmq512k_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_mask() && op1.is_vec() && op2.is_vec() {
            self.emit(VPTESTNMQ512KRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_mask() && op1.is_vec() && op2.is_mem() {
            self.emit(VPTESTNMQ512KRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPTESTNMQ512K_MASK");
        }
    }
    /// Emits `VPTESTNMQ512KRB`.
    fn vptestnmq512kb(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTNMQ512KRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPTESTNMQ512KRB_MASK`.
    fn vptestnmq512kb_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPTESTNMQ512KRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKHDQ128`.
    fn vpunpckhdq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKHDQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKHDQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKHDQ128");
        }
    }
    /// Emits `VPUNPCKHDQ128_MASK`.
    fn vpunpckhdq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKHDQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKHDQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKHDQ128_MASK");
        }
    }
    /// Emits `VPUNPCKHDQ128_MASKZ`.
    fn vpunpckhdq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKHDQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKHDQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKHDQ128_MASKZ");
        }
    }
    /// Emits `VPUNPCKHDQ128RRB`.
    fn vpunpckhdq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKHDQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKHDQ128RRB_MASK`.
    fn vpunpckhdq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKHDQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKHDQ128RRB_MASKZ`.
    fn vpunpckhdq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKHDQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKHDQ256`.
    fn vpunpckhdq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKHDQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKHDQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKHDQ256");
        }
    }
    /// Emits `VPUNPCKHDQ256_MASK`.
    fn vpunpckhdq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKHDQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKHDQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKHDQ256_MASK");
        }
    }
    /// Emits `VPUNPCKHDQ256_MASKZ`.
    fn vpunpckhdq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKHDQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKHDQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKHDQ256_MASKZ");
        }
    }
    /// Emits `VPUNPCKHDQ256RRB`.
    fn vpunpckhdq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKHDQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKHDQ256RRB_MASK`.
    fn vpunpckhdq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKHDQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKHDQ256RRB_MASKZ`.
    fn vpunpckhdq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKHDQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKHDQ512`.
    fn vpunpckhdq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKHDQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKHDQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKHDQ512");
        }
    }
    /// Emits `VPUNPCKHDQ512_MASK`.
    fn vpunpckhdq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKHDQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKHDQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKHDQ512_MASK");
        }
    }
    /// Emits `VPUNPCKHDQ512_MASKZ`.
    fn vpunpckhdq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKHDQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKHDQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKHDQ512_MASKZ");
        }
    }
    /// Emits `VPUNPCKHDQ512RRB`.
    fn vpunpckhdq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKHDQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKHDQ512RRB_MASK`.
    fn vpunpckhdq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKHDQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKHDQ512RRB_MASKZ`.
    fn vpunpckhdq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKHDQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKHQDQ128`.
    fn vpunpckhqdq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKHQDQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKHQDQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKHQDQ128");
        }
    }
    /// Emits `VPUNPCKHQDQ128_MASK`.
    fn vpunpckhqdq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKHQDQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKHQDQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKHQDQ128_MASK");
        }
    }
    /// Emits `VPUNPCKHQDQ128_MASKZ`.
    fn vpunpckhqdq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKHQDQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKHQDQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKHQDQ128_MASKZ");
        }
    }
    /// Emits `VPUNPCKHQDQ128RRB`.
    fn vpunpckhqdq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKHQDQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKHQDQ128RRB_MASK`.
    fn vpunpckhqdq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKHQDQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKHQDQ128RRB_MASKZ`.
    fn vpunpckhqdq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKHQDQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKHQDQ256`.
    fn vpunpckhqdq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKHQDQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKHQDQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKHQDQ256");
        }
    }
    /// Emits `VPUNPCKHQDQ256_MASK`.
    fn vpunpckhqdq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKHQDQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKHQDQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKHQDQ256_MASK");
        }
    }
    /// Emits `VPUNPCKHQDQ256_MASKZ`.
    fn vpunpckhqdq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKHQDQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKHQDQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKHQDQ256_MASKZ");
        }
    }
    /// Emits `VPUNPCKHQDQ256RRB`.
    fn vpunpckhqdq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKHQDQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKHQDQ256RRB_MASK`.
    fn vpunpckhqdq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKHQDQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKHQDQ256RRB_MASKZ`.
    fn vpunpckhqdq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKHQDQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKHQDQ512`.
    fn vpunpckhqdq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKHQDQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKHQDQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKHQDQ512");
        }
    }
    /// Emits `VPUNPCKHQDQ512_MASK`.
    fn vpunpckhqdq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKHQDQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKHQDQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKHQDQ512_MASK");
        }
    }
    /// Emits `VPUNPCKHQDQ512_MASKZ`.
    fn vpunpckhqdq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKHQDQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKHQDQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKHQDQ512_MASKZ");
        }
    }
    /// Emits `VPUNPCKHQDQ512RRB`.
    fn vpunpckhqdq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKHQDQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKHQDQ512RRB_MASK`.
    fn vpunpckhqdq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKHQDQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKHQDQ512RRB_MASKZ`.
    fn vpunpckhqdq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKHQDQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKLDQ128`.
    fn vpunpckldq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKLDQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKLDQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKLDQ128");
        }
    }
    /// Emits `VPUNPCKLDQ128_MASK`.
    fn vpunpckldq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKLDQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKLDQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKLDQ128_MASK");
        }
    }
    /// Emits `VPUNPCKLDQ128_MASKZ`.
    fn vpunpckldq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKLDQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKLDQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKLDQ128_MASKZ");
        }
    }
    /// Emits `VPUNPCKLDQ128RRB`.
    fn vpunpckldq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKLDQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKLDQ128RRB_MASK`.
    fn vpunpckldq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKLDQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKLDQ128RRB_MASKZ`.
    fn vpunpckldq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKLDQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKLDQ256`.
    fn vpunpckldq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKLDQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKLDQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKLDQ256");
        }
    }
    /// Emits `VPUNPCKLDQ256_MASK`.
    fn vpunpckldq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKLDQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKLDQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKLDQ256_MASK");
        }
    }
    /// Emits `VPUNPCKLDQ256_MASKZ`.
    fn vpunpckldq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKLDQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKLDQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKLDQ256_MASKZ");
        }
    }
    /// Emits `VPUNPCKLDQ256RRB`.
    fn vpunpckldq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKLDQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKLDQ256RRB_MASK`.
    fn vpunpckldq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKLDQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKLDQ256RRB_MASKZ`.
    fn vpunpckldq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKLDQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKLDQ512`.
    fn vpunpckldq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKLDQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKLDQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKLDQ512");
        }
    }
    /// Emits `VPUNPCKLDQ512_MASK`.
    fn vpunpckldq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKLDQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKLDQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKLDQ512_MASK");
        }
    }
    /// Emits `VPUNPCKLDQ512_MASKZ`.
    fn vpunpckldq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKLDQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKLDQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKLDQ512_MASKZ");
        }
    }
    /// Emits `VPUNPCKLDQ512RRB`.
    fn vpunpckldq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKLDQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKLDQ512RRB_MASK`.
    fn vpunpckldq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKLDQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKLDQ512RRB_MASKZ`.
    fn vpunpckldq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKLDQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKLQDQ128`.
    fn vpunpcklqdq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKLQDQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKLQDQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKLQDQ128");
        }
    }
    /// Emits `VPUNPCKLQDQ128_MASK`.
    fn vpunpcklqdq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKLQDQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKLQDQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKLQDQ128_MASK");
        }
    }
    /// Emits `VPUNPCKLQDQ128_MASKZ`.
    fn vpunpcklqdq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKLQDQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKLQDQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKLQDQ128_MASKZ");
        }
    }
    /// Emits `VPUNPCKLQDQ128RRB`.
    fn vpunpcklqdq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKLQDQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKLQDQ128RRB_MASK`.
    fn vpunpcklqdq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKLQDQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKLQDQ128RRB_MASKZ`.
    fn vpunpcklqdq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKLQDQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKLQDQ256`.
    fn vpunpcklqdq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKLQDQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKLQDQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKLQDQ256");
        }
    }
    /// Emits `VPUNPCKLQDQ256_MASK`.
    fn vpunpcklqdq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKLQDQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKLQDQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKLQDQ256_MASK");
        }
    }
    /// Emits `VPUNPCKLQDQ256_MASKZ`.
    fn vpunpcklqdq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKLQDQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKLQDQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKLQDQ256_MASKZ");
        }
    }
    /// Emits `VPUNPCKLQDQ256RRB`.
    fn vpunpcklqdq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKLQDQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKLQDQ256RRB_MASK`.
    fn vpunpcklqdq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKLQDQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKLQDQ256RRB_MASKZ`.
    fn vpunpcklqdq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKLQDQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKLQDQ512`.
    fn vpunpcklqdq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKLQDQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKLQDQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKLQDQ512");
        }
    }
    /// Emits `VPUNPCKLQDQ512_MASK`.
    fn vpunpcklqdq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKLQDQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKLQDQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKLQDQ512_MASK");
        }
    }
    /// Emits `VPUNPCKLQDQ512_MASKZ`.
    fn vpunpcklqdq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPUNPCKLQDQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPUNPCKLQDQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPUNPCKLQDQ512_MASKZ");
        }
    }
    /// Emits `VPUNPCKLQDQ512RRB`.
    fn vpunpcklqdq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKLQDQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKLQDQ512RRB_MASK`.
    fn vpunpcklqdq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKLQDQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPUNPCKLQDQ512RRB_MASKZ`.
    fn vpunpcklqdq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPUNPCKLQDQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPXORD128`.
    fn vpxord128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPXORD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPXORD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPXORD128");
        }
    }
    /// Emits `VPXORD128_MASK`.
    fn vpxord128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPXORD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPXORD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPXORD128_MASK");
        }
    }
    /// Emits `VPXORD128_MASKZ`.
    fn vpxord128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPXORD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPXORD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPXORD128_MASKZ");
        }
    }
    /// Emits `VPXORD128RRB`.
    fn vpxord128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPXORD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPXORD128RRB_MASK`.
    fn vpxord128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPXORD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPXORD128RRB_MASKZ`.
    fn vpxord128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPXORD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPXORD256`.
    fn vpxord256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPXORD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPXORD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPXORD256");
        }
    }
    /// Emits `VPXORD256_MASK`.
    fn vpxord256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPXORD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPXORD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPXORD256_MASK");
        }
    }
    /// Emits `VPXORD256_MASKZ`.
    fn vpxord256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPXORD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPXORD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPXORD256_MASKZ");
        }
    }
    /// Emits `VPXORD256RRB`.
    fn vpxord256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPXORD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPXORD256RRB_MASK`.
    fn vpxord256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPXORD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPXORD256RRB_MASKZ`.
    fn vpxord256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPXORD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPXORD512`.
    fn vpxord512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPXORD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPXORD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPXORD512");
        }
    }
    /// Emits `VPXORD512_MASK`.
    fn vpxord512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPXORD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPXORD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPXORD512_MASK");
        }
    }
    /// Emits `VPXORD512_MASKZ`.
    fn vpxord512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPXORD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPXORD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPXORD512_MASKZ");
        }
    }
    /// Emits `VPXORD512RRB`.
    fn vpxord512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPXORD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPXORD512RRB_MASK`.
    fn vpxord512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPXORD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPXORD512RRB_MASKZ`.
    fn vpxord512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPXORD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPXORQ128`.
    fn vpxorq128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPXORQ128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPXORQ128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPXORQ128");
        }
    }
    /// Emits `VPXORQ128_MASK`.
    fn vpxorq128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPXORQ128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPXORQ128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPXORQ128_MASK");
        }
    }
    /// Emits `VPXORQ128_MASKZ`.
    fn vpxorq128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPXORQ128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPXORQ128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPXORQ128_MASKZ");
        }
    }
    /// Emits `VPXORQ128RRB`.
    fn vpxorq128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPXORQ128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPXORQ128RRB_MASK`.
    fn vpxorq128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPXORQ128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPXORQ128RRB_MASKZ`.
    fn vpxorq128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPXORQ128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPXORQ256`.
    fn vpxorq256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPXORQ256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPXORQ256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPXORQ256");
        }
    }
    /// Emits `VPXORQ256_MASK`.
    fn vpxorq256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPXORQ256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPXORQ256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPXORQ256_MASK");
        }
    }
    /// Emits `VPXORQ256_MASKZ`.
    fn vpxorq256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPXORQ256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPXORQ256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPXORQ256_MASKZ");
        }
    }
    /// Emits `VPXORQ256RRB`.
    fn vpxorq256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPXORQ256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPXORQ256RRB_MASK`.
    fn vpxorq256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPXORQ256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPXORQ256RRB_MASKZ`.
    fn vpxorq256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPXORQ256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPXORQ512`.
    fn vpxorq512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPXORQ512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPXORQ512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPXORQ512");
        }
    }
    /// Emits `VPXORQ512_MASK`.
    fn vpxorq512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPXORQ512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPXORQ512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPXORQ512_MASK");
        }
    }
    /// Emits `VPXORQ512_MASKZ`.
    fn vpxorq512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VPXORQ512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VPXORQ512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VPXORQ512_MASKZ");
        }
    }
    /// Emits `VPXORQ512RRB`.
    fn vpxorq512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPXORQ512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPXORQ512RRB_MASK`.
    fn vpxorq512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPXORQ512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VPXORQ512RRB_MASKZ`.
    fn vpxorq512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VPXORQ512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRCP14PD128`.
    fn vrcp14pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRCP14PD128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRCP14PD128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14PD128");
        }
    }
    /// Emits `VRCP14PD128_MASK`.
    fn vrcp14pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRCP14PD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRCP14PD128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14PD128_MASK");
        }
    }
    /// Emits `VRCP14PD128_MASKZ`.
    fn vrcp14pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRCP14PD128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRCP14PD128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14PD128_MASKZ");
        }
    }
    /// Emits `VRCP14PD128RB`.
    fn vrcp14pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRCP14PD128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRCP14PD128RB_MASK`.
    fn vrcp14pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRCP14PD128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRCP14PD128RB_MASKZ`.
    fn vrcp14pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRCP14PD128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRCP14PD256`.
    fn vrcp14pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRCP14PD256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRCP14PD256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14PD256");
        }
    }
    /// Emits `VRCP14PD256_MASK`.
    fn vrcp14pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRCP14PD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRCP14PD256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14PD256_MASK");
        }
    }
    /// Emits `VRCP14PD256_MASKZ`.
    fn vrcp14pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRCP14PD256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRCP14PD256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14PD256_MASKZ");
        }
    }
    /// Emits `VRCP14PD256RB`.
    fn vrcp14pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRCP14PD256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRCP14PD256RB_MASK`.
    fn vrcp14pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRCP14PD256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRCP14PD256RB_MASKZ`.
    fn vrcp14pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRCP14PD256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRCP14PD512`.
    fn vrcp14pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRCP14PD512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRCP14PD512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14PD512");
        }
    }
    /// Emits `VRCP14PD512_MASK`.
    fn vrcp14pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRCP14PD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRCP14PD512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14PD512_MASK");
        }
    }
    /// Emits `VRCP14PD512_MASKZ`.
    fn vrcp14pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRCP14PD512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRCP14PD512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14PD512_MASKZ");
        }
    }
    /// Emits `VRCP14PD512RB`.
    fn vrcp14pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRCP14PD512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRCP14PD512RB_MASK`.
    fn vrcp14pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRCP14PD512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRCP14PD512RB_MASKZ`.
    fn vrcp14pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRCP14PD512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRCP14PS128`.
    fn vrcp14ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRCP14PS128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRCP14PS128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14PS128");
        }
    }
    /// Emits `VRCP14PS128_MASK`.
    fn vrcp14ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRCP14PS128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRCP14PS128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14PS128_MASK");
        }
    }
    /// Emits `VRCP14PS128_MASKZ`.
    fn vrcp14ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRCP14PS128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRCP14PS128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14PS128_MASKZ");
        }
    }
    /// Emits `VRCP14PS128RB`.
    fn vrcp14ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRCP14PS128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRCP14PS128RB_MASK`.
    fn vrcp14ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRCP14PS128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRCP14PS128RB_MASKZ`.
    fn vrcp14ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRCP14PS128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRCP14PS256`.
    fn vrcp14ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRCP14PS256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRCP14PS256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14PS256");
        }
    }
    /// Emits `VRCP14PS256_MASK`.
    fn vrcp14ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRCP14PS256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRCP14PS256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14PS256_MASK");
        }
    }
    /// Emits `VRCP14PS256_MASKZ`.
    fn vrcp14ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRCP14PS256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRCP14PS256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14PS256_MASKZ");
        }
    }
    /// Emits `VRCP14PS256RB`.
    fn vrcp14ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRCP14PS256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRCP14PS256RB_MASK`.
    fn vrcp14ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRCP14PS256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRCP14PS256RB_MASKZ`.
    fn vrcp14ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRCP14PS256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRCP14PS512`.
    fn vrcp14ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRCP14PS512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRCP14PS512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14PS512");
        }
    }
    /// Emits `VRCP14PS512_MASK`.
    fn vrcp14ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRCP14PS512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRCP14PS512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14PS512_MASK");
        }
    }
    /// Emits `VRCP14PS512_MASKZ`.
    fn vrcp14ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRCP14PS512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRCP14PS512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14PS512_MASKZ");
        }
    }
    /// Emits `VRCP14PS512RB`.
    fn vrcp14ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRCP14PS512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRCP14PS512RB_MASK`.
    fn vrcp14ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRCP14PS512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRCP14PS512RB_MASKZ`.
    fn vrcp14ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRCP14PS512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRCP14SD`.
    fn vrcp14sd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VRCP14SDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VRCP14SDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14SD");
        }
    }
    /// Emits `VRCP14SD_MASK`.
    fn vrcp14sd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VRCP14SDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VRCP14SDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14SD_MASK");
        }
    }
    /// Emits `VRCP14SD_MASKZ`.
    fn vrcp14sd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VRCP14SDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VRCP14SDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14SD_MASKZ");
        }
    }
    /// Emits `VRCP14SS`.
    fn vrcp14ss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VRCP14SSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VRCP14SSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14SS");
        }
    }
    /// Emits `VRCP14SS_MASK`.
    fn vrcp14ss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VRCP14SSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VRCP14SSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14SS_MASK");
        }
    }
    /// Emits `VRCP14SS_MASKZ`.
    fn vrcp14ss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VRCP14SSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VRCP14SSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRCP14SS_MASKZ");
        }
    }
    /// Emits `VRNDSCALEPD128`.
    fn vrndscalepd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VRNDSCALEPD128RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VRNDSCALEPD128RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRNDSCALEPD128");
        }
    }
    /// Emits `VRNDSCALEPD128_MASK`.
    fn vrndscalepd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VRNDSCALEPD128RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VRNDSCALEPD128RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRNDSCALEPD128_MASK");
        }
    }
    /// Emits `VRNDSCALEPD128_MASKZ`.
    fn vrndscalepd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VRNDSCALEPD128RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VRNDSCALEPD128RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRNDSCALEPD128_MASKZ");
        }
    }
    /// Emits `VRNDSCALEPD128RBI`.
    fn vrndscalepd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPD128RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPD128RBI_MASK`.
    fn vrndscalepd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPD128RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPD128RBI_MASKZ`.
    fn vrndscalepd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPD128RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPD256`.
    fn vrndscalepd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VRNDSCALEPD256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VRNDSCALEPD256RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRNDSCALEPD256");
        }
    }
    /// Emits `VRNDSCALEPD256_MASK`.
    fn vrndscalepd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VRNDSCALEPD256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VRNDSCALEPD256RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRNDSCALEPD256_MASK");
        }
    }
    /// Emits `VRNDSCALEPD256_MASKZ`.
    fn vrndscalepd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VRNDSCALEPD256RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VRNDSCALEPD256RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRNDSCALEPD256_MASKZ");
        }
    }
    /// Emits `VRNDSCALEPD256RBI`.
    fn vrndscalepd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPD256RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPD256RBI_MASK`.
    fn vrndscalepd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPD256RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPD256RBI_MASKZ`.
    fn vrndscalepd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPD256RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPD512`.
    fn vrndscalepd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VRNDSCALEPD512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VRNDSCALEPD512RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRNDSCALEPD512");
        }
    }
    /// Emits `VRNDSCALEPD512_MASK`.
    fn vrndscalepd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VRNDSCALEPD512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VRNDSCALEPD512RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRNDSCALEPD512_MASK");
        }
    }
    /// Emits `VRNDSCALEPD512RRI_MASK_SAE`.
    fn vrndscalepd512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPD512RRI_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPD512_MASKZ`.
    fn vrndscalepd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VRNDSCALEPD512RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VRNDSCALEPD512RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRNDSCALEPD512_MASKZ");
        }
    }
    /// Emits `VRNDSCALEPD512RRI_MASKZ_SAE`.
    fn vrndscalepd512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPD512RRI_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPD512RRI_SAE`.
    fn vrndscalepd512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPD512RRI_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPD512RBI`.
    fn vrndscalepd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPD512RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPD512RBI_MASK`.
    fn vrndscalepd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPD512RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPD512RBI_MASKZ`.
    fn vrndscalepd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPD512RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPS128`.
    fn vrndscaleps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VRNDSCALEPS128RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VRNDSCALEPS128RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRNDSCALEPS128");
        }
    }
    /// Emits `VRNDSCALEPS128_MASK`.
    fn vrndscaleps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VRNDSCALEPS128RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VRNDSCALEPS128RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRNDSCALEPS128_MASK");
        }
    }
    /// Emits `VRNDSCALEPS128_MASKZ`.
    fn vrndscaleps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VRNDSCALEPS128RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VRNDSCALEPS128RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRNDSCALEPS128_MASKZ");
        }
    }
    /// Emits `VRNDSCALEPS128RBI`.
    fn vrndscaleps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPS128RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPS128RBI_MASK`.
    fn vrndscaleps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPS128RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPS128RBI_MASKZ`.
    fn vrndscaleps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPS128RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPS256`.
    fn vrndscaleps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VRNDSCALEPS256RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VRNDSCALEPS256RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRNDSCALEPS256");
        }
    }
    /// Emits `VRNDSCALEPS256_MASK`.
    fn vrndscaleps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VRNDSCALEPS256RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VRNDSCALEPS256RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRNDSCALEPS256_MASK");
        }
    }
    /// Emits `VRNDSCALEPS256_MASKZ`.
    fn vrndscaleps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VRNDSCALEPS256RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VRNDSCALEPS256RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRNDSCALEPS256_MASKZ");
        }
    }
    /// Emits `VRNDSCALEPS256RBI`.
    fn vrndscaleps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPS256RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPS256RBI_MASK`.
    fn vrndscaleps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPS256RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPS256RBI_MASKZ`.
    fn vrndscaleps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPS256RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPS512`.
    fn vrndscaleps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VRNDSCALEPS512RRI, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VRNDSCALEPS512RMI, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRNDSCALEPS512");
        }
    }
    /// Emits `VRNDSCALEPS512_MASK`.
    fn vrndscaleps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VRNDSCALEPS512RRI_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VRNDSCALEPS512RMI_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRNDSCALEPS512_MASK");
        }
    }
    /// Emits `VRNDSCALEPS512RRI_MASK_SAE`.
    fn vrndscaleps512_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPS512RRI_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPS512_MASKZ`.
    fn vrndscaleps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_imm() {
            self.emit(VRNDSCALEPS512RRI_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_mem() && op2.is_imm() {
            self.emit(VRNDSCALEPS512RMI_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRNDSCALEPS512_MASKZ");
        }
    }
    /// Emits `VRNDSCALEPS512RRI_MASKZ_SAE`.
    fn vrndscaleps512_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPS512RRI_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPS512RRI_SAE`.
    fn vrndscaleps512_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPS512RRI_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPS512RBI`.
    fn vrndscaleps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPS512RBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPS512RBI_MASK`.
    fn vrndscaleps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPS512RBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALEPS512RBI_MASKZ`.
    fn vrndscaleps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VRNDSCALEPS512RBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VRNDSCALESD`.
    fn vrndscalesd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VRNDSCALESDRRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VRNDSCALESDRRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VRNDSCALESD");
        }
    }
    /// Emits `VRNDSCALESD_MASK`.
    fn vrndscalesd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VRNDSCALESDRRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VRNDSCALESDRRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VRNDSCALESD_MASK");
        }
    }
    /// Emits `VRNDSCALESDRRRI_MASK_SAE`.
    fn vrndscalesd_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VRNDSCALESDRRRI_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VRNDSCALESD_MASKZ`.
    fn vrndscalesd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VRNDSCALESDRRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VRNDSCALESDRRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VRNDSCALESD_MASKZ");
        }
    }
    /// Emits `VRNDSCALESDRRRI_MASKZ_SAE`.
    fn vrndscalesd_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VRNDSCALESDRRRI_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VRNDSCALESDRRRI_SAE`.
    fn vrndscalesd_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VRNDSCALESDRRRI_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VRNDSCALESS`.
    fn vrndscaless(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VRNDSCALESSRRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VRNDSCALESSRRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VRNDSCALESS");
        }
    }
    /// Emits `VRNDSCALESS_MASK`.
    fn vrndscaless_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VRNDSCALESSRRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VRNDSCALESSRRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VRNDSCALESS_MASK");
        }
    }
    /// Emits `VRNDSCALESSRRRI_MASK_SAE`.
    fn vrndscaless_mask_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VRNDSCALESSRRRI_MASK_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VRNDSCALESS_MASKZ`.
    fn vrndscaless_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VRNDSCALESSRRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VRNDSCALESSRRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VRNDSCALESS_MASKZ");
        }
    }
    /// Emits `VRNDSCALESSRRRI_MASKZ_SAE`.
    fn vrndscaless_maskz_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VRNDSCALESSRRRI_MASKZ_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VRNDSCALESSRRRI_SAE`.
    fn vrndscaless_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VRNDSCALESSRRRI_SAE, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VRSQRT14PD128`.
    fn vrsqrt14pd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRSQRT14PD128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRSQRT14PD128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14PD128");
        }
    }
    /// Emits `VRSQRT14PD128_MASK`.
    fn vrsqrt14pd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRSQRT14PD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRSQRT14PD128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14PD128_MASK");
        }
    }
    /// Emits `VRSQRT14PD128_MASKZ`.
    fn vrsqrt14pd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRSQRT14PD128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRSQRT14PD128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14PD128_MASKZ");
        }
    }
    /// Emits `VRSQRT14PD128RB`.
    fn vrsqrt14pd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRSQRT14PD128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRSQRT14PD128RB_MASK`.
    fn vrsqrt14pd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRSQRT14PD128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRSQRT14PD128RB_MASKZ`.
    fn vrsqrt14pd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRSQRT14PD128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRSQRT14PD256`.
    fn vrsqrt14pd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRSQRT14PD256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRSQRT14PD256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14PD256");
        }
    }
    /// Emits `VRSQRT14PD256_MASK`.
    fn vrsqrt14pd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRSQRT14PD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRSQRT14PD256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14PD256_MASK");
        }
    }
    /// Emits `VRSQRT14PD256_MASKZ`.
    fn vrsqrt14pd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRSQRT14PD256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRSQRT14PD256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14PD256_MASKZ");
        }
    }
    /// Emits `VRSQRT14PD256RB`.
    fn vrsqrt14pd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRSQRT14PD256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRSQRT14PD256RB_MASK`.
    fn vrsqrt14pd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRSQRT14PD256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRSQRT14PD256RB_MASKZ`.
    fn vrsqrt14pd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRSQRT14PD256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRSQRT14PD512`.
    fn vrsqrt14pd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRSQRT14PD512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRSQRT14PD512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14PD512");
        }
    }
    /// Emits `VRSQRT14PD512_MASK`.
    fn vrsqrt14pd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRSQRT14PD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRSQRT14PD512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14PD512_MASK");
        }
    }
    /// Emits `VRSQRT14PD512_MASKZ`.
    fn vrsqrt14pd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRSQRT14PD512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRSQRT14PD512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14PD512_MASKZ");
        }
    }
    /// Emits `VRSQRT14PD512RB`.
    fn vrsqrt14pd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRSQRT14PD512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRSQRT14PD512RB_MASK`.
    fn vrsqrt14pd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRSQRT14PD512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRSQRT14PD512RB_MASKZ`.
    fn vrsqrt14pd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRSQRT14PD512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRSQRT14PS128`.
    fn vrsqrt14ps128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRSQRT14PS128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRSQRT14PS128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14PS128");
        }
    }
    /// Emits `VRSQRT14PS128_MASK`.
    fn vrsqrt14ps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRSQRT14PS128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRSQRT14PS128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14PS128_MASK");
        }
    }
    /// Emits `VRSQRT14PS128_MASKZ`.
    fn vrsqrt14ps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRSQRT14PS128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRSQRT14PS128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14PS128_MASKZ");
        }
    }
    /// Emits `VRSQRT14PS128RB`.
    fn vrsqrt14ps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRSQRT14PS128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRSQRT14PS128RB_MASK`.
    fn vrsqrt14ps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRSQRT14PS128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRSQRT14PS128RB_MASKZ`.
    fn vrsqrt14ps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRSQRT14PS128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRSQRT14PS256`.
    fn vrsqrt14ps256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRSQRT14PS256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRSQRT14PS256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14PS256");
        }
    }
    /// Emits `VRSQRT14PS256_MASK`.
    fn vrsqrt14ps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRSQRT14PS256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRSQRT14PS256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14PS256_MASK");
        }
    }
    /// Emits `VRSQRT14PS256_MASKZ`.
    fn vrsqrt14ps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRSQRT14PS256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRSQRT14PS256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14PS256_MASKZ");
        }
    }
    /// Emits `VRSQRT14PS256RB`.
    fn vrsqrt14ps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRSQRT14PS256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRSQRT14PS256RB_MASK`.
    fn vrsqrt14ps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRSQRT14PS256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRSQRT14PS256RB_MASKZ`.
    fn vrsqrt14ps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRSQRT14PS256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRSQRT14PS512`.
    fn vrsqrt14ps512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRSQRT14PS512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRSQRT14PS512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14PS512");
        }
    }
    /// Emits `VRSQRT14PS512_MASK`.
    fn vrsqrt14ps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRSQRT14PS512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRSQRT14PS512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14PS512_MASK");
        }
    }
    /// Emits `VRSQRT14PS512_MASKZ`.
    fn vrsqrt14ps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VRSQRT14PS512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VRSQRT14PS512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14PS512_MASKZ");
        }
    }
    /// Emits `VRSQRT14PS512RB`.
    fn vrsqrt14ps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRSQRT14PS512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRSQRT14PS512RB_MASK`.
    fn vrsqrt14ps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRSQRT14PS512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRSQRT14PS512RB_MASKZ`.
    fn vrsqrt14ps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VRSQRT14PS512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VRSQRT14SD`.
    fn vrsqrt14sd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VRSQRT14SDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VRSQRT14SDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14SD");
        }
    }
    /// Emits `VRSQRT14SD_MASK`.
    fn vrsqrt14sd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VRSQRT14SDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VRSQRT14SDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14SD_MASK");
        }
    }
    /// Emits `VRSQRT14SD_MASKZ`.
    fn vrsqrt14sd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VRSQRT14SDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VRSQRT14SDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14SD_MASKZ");
        }
    }
    /// Emits `VRSQRT14SS`.
    fn vrsqrt14ss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VRSQRT14SSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VRSQRT14SSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14SS");
        }
    }
    /// Emits `VRSQRT14SS_MASK`.
    fn vrsqrt14ss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VRSQRT14SSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VRSQRT14SSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14SS_MASK");
        }
    }
    /// Emits `VRSQRT14SS_MASKZ`.
    fn vrsqrt14ss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VRSQRT14SSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VRSQRT14SSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VRSQRT14SS_MASKZ");
        }
    }
    /// Emits `VSCALEFPD128`.
    fn vscalefpd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFPD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFPD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFPD128");
        }
    }
    /// Emits `VSCALEFPD128_MASK`.
    fn vscalefpd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFPD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFPD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFPD128_MASK");
        }
    }
    /// Emits `VSCALEFPD128_MASKZ`.
    fn vscalefpd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFPD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFPD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFPD128_MASKZ");
        }
    }
    /// Emits `VSCALEFPD128RRB`.
    fn vscalefpd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPD128RRB_MASK`.
    fn vscalefpd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPD128RRB_MASKZ`.
    fn vscalefpd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPD256`.
    fn vscalefpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFPD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFPD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFPD256");
        }
    }
    /// Emits `VSCALEFPD256_MASK`.
    fn vscalefpd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFPD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFPD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFPD256_MASK");
        }
    }
    /// Emits `VSCALEFPD256_MASKZ`.
    fn vscalefpd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFPD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFPD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFPD256_MASKZ");
        }
    }
    /// Emits `VSCALEFPD256RRB`.
    fn vscalefpd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPD256RRB_MASK`.
    fn vscalefpd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPD256RRB_MASKZ`.
    fn vscalefpd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPD512`.
    fn vscalefpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFPD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFPD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFPD512");
        }
    }
    /// Emits `VSCALEFPD512RRR_ER`.
    fn vscalefpd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPD512_MASK`.
    fn vscalefpd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFPD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFPD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFPD512_MASK");
        }
    }
    /// Emits `VSCALEFPD512RRR_MASK_ER`.
    fn vscalefpd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPD512_MASKZ`.
    fn vscalefpd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFPD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFPD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFPD512_MASKZ");
        }
    }
    /// Emits `VSCALEFPD512RRR_MASKZ_ER`.
    fn vscalefpd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPD512RRB`.
    fn vscalefpd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPD512RRB_MASK`.
    fn vscalefpd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPD512RRB_MASKZ`.
    fn vscalefpd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPS128`.
    fn vscalefps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFPS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFPS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFPS128");
        }
    }
    /// Emits `VSCALEFPS128_MASK`.
    fn vscalefps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFPS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFPS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFPS128_MASK");
        }
    }
    /// Emits `VSCALEFPS128_MASKZ`.
    fn vscalefps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFPS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFPS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFPS128_MASKZ");
        }
    }
    /// Emits `VSCALEFPS128RRB`.
    fn vscalefps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPS128RRB_MASK`.
    fn vscalefps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPS128RRB_MASKZ`.
    fn vscalefps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPS256`.
    fn vscalefps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFPS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFPS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFPS256");
        }
    }
    /// Emits `VSCALEFPS256_MASK`.
    fn vscalefps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFPS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFPS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFPS256_MASK");
        }
    }
    /// Emits `VSCALEFPS256_MASKZ`.
    fn vscalefps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFPS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFPS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFPS256_MASKZ");
        }
    }
    /// Emits `VSCALEFPS256RRB`.
    fn vscalefps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPS256RRB_MASK`.
    fn vscalefps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPS256RRB_MASKZ`.
    fn vscalefps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPS512`.
    fn vscalefps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFPS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFPS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFPS512");
        }
    }
    /// Emits `VSCALEFPS512RRR_ER`.
    fn vscalefps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPS512_MASK`.
    fn vscalefps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFPS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFPS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFPS512_MASK");
        }
    }
    /// Emits `VSCALEFPS512RRR_MASK_ER`.
    fn vscalefps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPS512_MASKZ`.
    fn vscalefps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFPS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFPS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFPS512_MASKZ");
        }
    }
    /// Emits `VSCALEFPS512RRR_MASKZ_ER`.
    fn vscalefps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPS512RRB`.
    fn vscalefps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPS512RRB_MASK`.
    fn vscalefps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFPS512RRB_MASKZ`.
    fn vscalefps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFPS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFSD`.
    fn vscalefsd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFSDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFSDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFSD");
        }
    }
    /// Emits `VSCALEFSDRRR_ER`.
    fn vscalefsd_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFSDRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFSD_MASK`.
    fn vscalefsd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFSDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFSDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFSD_MASK");
        }
    }
    /// Emits `VSCALEFSDRRR_MASK_ER`.
    fn vscalefsd_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFSDRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFSD_MASKZ`.
    fn vscalefsd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFSDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFSDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFSD_MASKZ");
        }
    }
    /// Emits `VSCALEFSDRRR_MASKZ_ER`.
    fn vscalefsd_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFSDRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFSS`.
    fn vscalefss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFSSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFSSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFSS");
        }
    }
    /// Emits `VSCALEFSSRRR_ER`.
    fn vscalefss_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFSSRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFSS_MASK`.
    fn vscalefss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFSSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFSSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFSS_MASK");
        }
    }
    /// Emits `VSCALEFSSRRR_MASK_ER`.
    fn vscalefss_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFSSRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCALEFSS_MASKZ`.
    fn vscalefss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSCALEFSSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSCALEFSSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSCALEFSS_MASKZ");
        }
    }
    /// Emits `VSCALEFSSRRR_MASKZ_ER`.
    fn vscalefss_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSCALEFSSRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSCATTERDPD128MR`.
    fn vscatterdpd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSCATTERDPD128MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSCATTERDPD256MR`.
    fn vscatterdpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSCATTERDPD256MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSCATTERDPD512MR`.
    fn vscatterdpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSCATTERDPD512MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSCATTERDPS128MR`.
    fn vscatterdps128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSCATTERDPS128MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSCATTERDPS256MR`.
    fn vscatterdps256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSCATTERDPS256MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSCATTERDPS512MR`.
    fn vscatterdps512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSCATTERDPS512MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSCATTERQPD128MR`.
    fn vscatterqpd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSCATTERQPD128MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSCATTERQPD256MR`.
    fn vscatterqpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSCATTERQPD256MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSCATTERQPD512MR`.
    fn vscatterqpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSCATTERQPD512MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSCATTERQPS128MR`.
    fn vscatterqps128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSCATTERQPS128MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSCATTERQPS256MR`.
    fn vscatterqps256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSCATTERQPS256MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSCATTERQPS512MR`.
    fn vscatterqps512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSCATTERQPS512MR, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSHUFF32X4_256`.
    fn vshuff32x4_256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFF32X4_256RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFF32X4_256RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFF32X4_256");
        }
    }
    /// Emits `VSHUFF32X4_256_MASK`.
    fn vshuff32x4_256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFF32X4_256RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFF32X4_256RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFF32X4_256_MASK");
        }
    }
    /// Emits `VSHUFF32X4_256_MASKZ`.
    fn vshuff32x4_256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFF32X4_256RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFF32X4_256RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFF32X4_256_MASKZ");
        }
    }
    /// Emits `VSHUFF32X4_256RRBI`.
    fn vshuff32x4_256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFF32X4_256RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFF32X4_256RRBI_MASK`.
    fn vshuff32x4_256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFF32X4_256RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFF32X4_256RRBI_MASKZ`.
    fn vshuff32x4_256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFF32X4_256RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFF32X4_512`.
    fn vshuff32x4_512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFF32X4_512RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFF32X4_512RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFF32X4_512");
        }
    }
    /// Emits `VSHUFF32X4_512_MASK`.
    fn vshuff32x4_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFF32X4_512RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFF32X4_512RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFF32X4_512_MASK");
        }
    }
    /// Emits `VSHUFF32X4_512_MASKZ`.
    fn vshuff32x4_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFF32X4_512RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFF32X4_512RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFF32X4_512_MASKZ");
        }
    }
    /// Emits `VSHUFF32X4_512RRBI`.
    fn vshuff32x4_512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFF32X4_512RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFF32X4_512RRBI_MASK`.
    fn vshuff32x4_512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFF32X4_512RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFF32X4_512RRBI_MASKZ`.
    fn vshuff32x4_512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFF32X4_512RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFF64X2_256`.
    fn vshuff64x2_256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFF64X2_256RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFF64X2_256RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFF64X2_256");
        }
    }
    /// Emits `VSHUFF64X2_256_MASK`.
    fn vshuff64x2_256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFF64X2_256RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFF64X2_256RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFF64X2_256_MASK");
        }
    }
    /// Emits `VSHUFF64X2_256_MASKZ`.
    fn vshuff64x2_256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFF64X2_256RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFF64X2_256RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFF64X2_256_MASKZ");
        }
    }
    /// Emits `VSHUFF64X2_256RRBI`.
    fn vshuff64x2_256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFF64X2_256RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFF64X2_256RRBI_MASK`.
    fn vshuff64x2_256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFF64X2_256RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFF64X2_256RRBI_MASKZ`.
    fn vshuff64x2_256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFF64X2_256RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFF64X2_512`.
    fn vshuff64x2_512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFF64X2_512RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFF64X2_512RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFF64X2_512");
        }
    }
    /// Emits `VSHUFF64X2_512_MASK`.
    fn vshuff64x2_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFF64X2_512RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFF64X2_512RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFF64X2_512_MASK");
        }
    }
    /// Emits `VSHUFF64X2_512_MASKZ`.
    fn vshuff64x2_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFF64X2_512RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFF64X2_512RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFF64X2_512_MASKZ");
        }
    }
    /// Emits `VSHUFF64X2_512RRBI`.
    fn vshuff64x2_512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFF64X2_512RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFF64X2_512RRBI_MASK`.
    fn vshuff64x2_512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFF64X2_512RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFF64X2_512RRBI_MASKZ`.
    fn vshuff64x2_512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFF64X2_512RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFI32X4_256`.
    fn vshufi32x4_256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFI32X4_256RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFI32X4_256RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFI32X4_256");
        }
    }
    /// Emits `VSHUFI32X4_256_MASK`.
    fn vshufi32x4_256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFI32X4_256RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFI32X4_256RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFI32X4_256_MASK");
        }
    }
    /// Emits `VSHUFI32X4_256_MASKZ`.
    fn vshufi32x4_256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFI32X4_256RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFI32X4_256RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFI32X4_256_MASKZ");
        }
    }
    /// Emits `VSHUFI32X4_256RRBI`.
    fn vshufi32x4_256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFI32X4_256RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFI32X4_256RRBI_MASK`.
    fn vshufi32x4_256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFI32X4_256RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFI32X4_256RRBI_MASKZ`.
    fn vshufi32x4_256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFI32X4_256RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFI32X4_512`.
    fn vshufi32x4_512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFI32X4_512RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFI32X4_512RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFI32X4_512");
        }
    }
    /// Emits `VSHUFI32X4_512_MASK`.
    fn vshufi32x4_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFI32X4_512RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFI32X4_512RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFI32X4_512_MASK");
        }
    }
    /// Emits `VSHUFI32X4_512_MASKZ`.
    fn vshufi32x4_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFI32X4_512RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFI32X4_512RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFI32X4_512_MASKZ");
        }
    }
    /// Emits `VSHUFI32X4_512RRBI`.
    fn vshufi32x4_512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFI32X4_512RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFI32X4_512RRBI_MASK`.
    fn vshufi32x4_512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFI32X4_512RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFI32X4_512RRBI_MASKZ`.
    fn vshufi32x4_512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFI32X4_512RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFI64X2_256`.
    fn vshufi64x2_256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFI64X2_256RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFI64X2_256RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFI64X2_256");
        }
    }
    /// Emits `VSHUFI64X2_256_MASK`.
    fn vshufi64x2_256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFI64X2_256RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFI64X2_256RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFI64X2_256_MASK");
        }
    }
    /// Emits `VSHUFI64X2_256_MASKZ`.
    fn vshufi64x2_256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFI64X2_256RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFI64X2_256RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFI64X2_256_MASKZ");
        }
    }
    /// Emits `VSHUFI64X2_256RRBI`.
    fn vshufi64x2_256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFI64X2_256RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFI64X2_256RRBI_MASK`.
    fn vshufi64x2_256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFI64X2_256RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFI64X2_256RRBI_MASKZ`.
    fn vshufi64x2_256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFI64X2_256RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFI64X2_512`.
    fn vshufi64x2_512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFI64X2_512RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFI64X2_512RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFI64X2_512");
        }
    }
    /// Emits `VSHUFI64X2_512_MASK`.
    fn vshufi64x2_512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFI64X2_512RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFI64X2_512RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFI64X2_512_MASK");
        }
    }
    /// Emits `VSHUFI64X2_512_MASKZ`.
    fn vshufi64x2_512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFI64X2_512RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFI64X2_512RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFI64X2_512_MASKZ");
        }
    }
    /// Emits `VSHUFI64X2_512RRBI`.
    fn vshufi64x2_512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFI64X2_512RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFI64X2_512RRBI_MASK`.
    fn vshufi64x2_512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFI64X2_512RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFI64X2_512RRBI_MASKZ`.
    fn vshufi64x2_512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFI64X2_512RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFPD128`.
    fn vshufpd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFPD128RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFPD128RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFPD128");
        }
    }
    /// Emits `VSHUFPD128_MASK`.
    fn vshufpd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFPD128RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFPD128RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFPD128_MASK");
        }
    }
    /// Emits `VSHUFPD128_MASKZ`.
    fn vshufpd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFPD128RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFPD128RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFPD128_MASKZ");
        }
    }
    /// Emits `VSHUFPD128RRBI`.
    fn vshufpd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFPD128RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFPD128RRBI_MASK`.
    fn vshufpd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFPD128RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFPD128RRBI_MASKZ`.
    fn vshufpd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFPD128RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFPD256`.
    fn vshufpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFPD256RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFPD256RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFPD256");
        }
    }
    /// Emits `VSHUFPD256_MASK`.
    fn vshufpd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFPD256RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFPD256RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFPD256_MASK");
        }
    }
    /// Emits `VSHUFPD256_MASKZ`.
    fn vshufpd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFPD256RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFPD256RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFPD256_MASKZ");
        }
    }
    /// Emits `VSHUFPD256RRBI`.
    fn vshufpd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFPD256RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFPD256RRBI_MASK`.
    fn vshufpd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFPD256RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFPD256RRBI_MASKZ`.
    fn vshufpd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFPD256RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFPD512`.
    fn vshufpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFPD512RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFPD512RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFPD512");
        }
    }
    /// Emits `VSHUFPD512_MASK`.
    fn vshufpd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFPD512RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFPD512RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFPD512_MASK");
        }
    }
    /// Emits `VSHUFPD512_MASKZ`.
    fn vshufpd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFPD512RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFPD512RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFPD512_MASKZ");
        }
    }
    /// Emits `VSHUFPD512RRBI`.
    fn vshufpd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFPD512RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFPD512RRBI_MASK`.
    fn vshufpd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFPD512RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFPD512RRBI_MASKZ`.
    fn vshufpd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFPD512RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFPS128`.
    fn vshufps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFPS128RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFPS128RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFPS128");
        }
    }
    /// Emits `VSHUFPS128_MASK`.
    fn vshufps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFPS128RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFPS128RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFPS128_MASK");
        }
    }
    /// Emits `VSHUFPS128_MASKZ`.
    fn vshufps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFPS128RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFPS128RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFPS128_MASKZ");
        }
    }
    /// Emits `VSHUFPS128RRBI`.
    fn vshufps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFPS128RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFPS128RRBI_MASK`.
    fn vshufps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFPS128RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFPS128RRBI_MASKZ`.
    fn vshufps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFPS128RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFPS256`.
    fn vshufps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFPS256RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFPS256RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFPS256");
        }
    }
    /// Emits `VSHUFPS256_MASK`.
    fn vshufps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFPS256RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFPS256RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFPS256_MASK");
        }
    }
    /// Emits `VSHUFPS256_MASKZ`.
    fn vshufps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFPS256RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFPS256RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFPS256_MASKZ");
        }
    }
    /// Emits `VSHUFPS256RRBI`.
    fn vshufps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFPS256RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFPS256RRBI_MASK`.
    fn vshufps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFPS256RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFPS256RRBI_MASKZ`.
    fn vshufps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFPS256RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFPS512`.
    fn vshufps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFPS512RRRI, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFPS512RRMI, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFPS512");
        }
    }
    /// Emits `VSHUFPS512_MASK`.
    fn vshufps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFPS512RRRI_MASK, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFPS512RRMI_MASK, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFPS512_MASK");
        }
    }
    /// Emits `VSHUFPS512_MASKZ`.
    fn vshufps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        let op3 = op3.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() && op3.is_imm() {
            self.emit(VSHUFPS512RRRI_MASKZ, op0,op1,op2,op3,);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() && op3.is_imm() {
            self.emit(VSHUFPS512RRMI_MASKZ, op0,op1,op2,op3,);
        } else {
            unreachable!("invalid operand types for VSHUFPS512_MASKZ");
        }
    }
    /// Emits `VSHUFPS512RRBI`.
    fn vshufps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFPS512RRBI, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFPS512RRBI_MASK`.
    fn vshufps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFPS512RRBI_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSHUFPS512RRBI_MASKZ`.
    fn vshufps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast,op3: impl OperandCast) -> () {
        self.emit(VSHUFPS512RRBI_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),op3.as_operand(),);
    }
    /// Emits `VSQRTPD128`.
    fn vsqrtpd128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VSQRTPD128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VSQRTPD128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTPD128");
        }
    }
    /// Emits `VSQRTPD128_MASK`.
    fn vsqrtpd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VSQRTPD128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VSQRTPD128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTPD128_MASK");
        }
    }
    /// Emits `VSQRTPD128_MASKZ`.
    fn vsqrtpd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VSQRTPD128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VSQRTPD128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTPD128_MASKZ");
        }
    }
    /// Emits `VSQRTPD128RB`.
    fn vsqrtpd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPD128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPD128RB_MASK`.
    fn vsqrtpd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPD128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPD128RB_MASKZ`.
    fn vsqrtpd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPD128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPD256`.
    fn vsqrtpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VSQRTPD256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VSQRTPD256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTPD256");
        }
    }
    /// Emits `VSQRTPD256_MASK`.
    fn vsqrtpd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VSQRTPD256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VSQRTPD256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTPD256_MASK");
        }
    }
    /// Emits `VSQRTPD256_MASKZ`.
    fn vsqrtpd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VSQRTPD256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VSQRTPD256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTPD256_MASKZ");
        }
    }
    /// Emits `VSQRTPD256RB`.
    fn vsqrtpd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPD256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPD256RB_MASK`.
    fn vsqrtpd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPD256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPD256RB_MASKZ`.
    fn vsqrtpd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPD256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPD512`.
    fn vsqrtpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VSQRTPD512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VSQRTPD512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTPD512");
        }
    }
    /// Emits `VSQRTPD512RR_ER`.
    fn vsqrtpd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPD512RR_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPD512_MASK`.
    fn vsqrtpd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VSQRTPD512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VSQRTPD512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTPD512_MASK");
        }
    }
    /// Emits `VSQRTPD512RR_MASK_ER`.
    fn vsqrtpd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPD512RR_MASK_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPD512_MASKZ`.
    fn vsqrtpd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VSQRTPD512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VSQRTPD512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTPD512_MASKZ");
        }
    }
    /// Emits `VSQRTPD512RR_MASKZ_ER`.
    fn vsqrtpd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPD512RR_MASKZ_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPD512RB`.
    fn vsqrtpd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPD512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPD512RB_MASK`.
    fn vsqrtpd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPD512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPD512RB_MASKZ`.
    fn vsqrtpd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPD512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPS128`.
    fn vsqrtps128(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VSQRTPS128RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VSQRTPS128RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTPS128");
        }
    }
    /// Emits `VSQRTPS128_MASK`.
    fn vsqrtps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VSQRTPS128RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VSQRTPS128RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTPS128_MASK");
        }
    }
    /// Emits `VSQRTPS128_MASKZ`.
    fn vsqrtps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VSQRTPS128RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VSQRTPS128RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTPS128_MASKZ");
        }
    }
    /// Emits `VSQRTPS128RB`.
    fn vsqrtps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPS128RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPS128RB_MASK`.
    fn vsqrtps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPS128RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPS128RB_MASKZ`.
    fn vsqrtps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPS128RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPS256`.
    fn vsqrtps256(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VSQRTPS256RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VSQRTPS256RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTPS256");
        }
    }
    /// Emits `VSQRTPS256_MASK`.
    fn vsqrtps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VSQRTPS256RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VSQRTPS256RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTPS256_MASK");
        }
    }
    /// Emits `VSQRTPS256_MASKZ`.
    fn vsqrtps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VSQRTPS256RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VSQRTPS256RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTPS256_MASKZ");
        }
    }
    /// Emits `VSQRTPS256RB`.
    fn vsqrtps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPS256RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPS256RB_MASK`.
    fn vsqrtps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPS256RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPS256RB_MASKZ`.
    fn vsqrtps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPS256RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPS512`.
    fn vsqrtps512(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VSQRTPS512RR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VSQRTPS512RM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTPS512");
        }
    }
    /// Emits `VSQRTPS512RR_ER`.
    fn vsqrtps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPS512RR_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPS512_MASK`.
    fn vsqrtps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VSQRTPS512RR_MASK, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VSQRTPS512RM_MASK, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTPS512_MASK");
        }
    }
    /// Emits `VSQRTPS512RR_MASK_ER`.
    fn vsqrtps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPS512RR_MASK_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPS512_MASKZ`.
    fn vsqrtps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VSQRTPS512RR_MASKZ, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VSQRTPS512RM_MASKZ, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTPS512_MASKZ");
        }
    }
    /// Emits `VSQRTPS512RR_MASKZ_ER`.
    fn vsqrtps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPS512RR_MASKZ_ER, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPS512RB`.
    fn vsqrtps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPS512RB, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPS512RB_MASK`.
    fn vsqrtps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPS512RB_MASK, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTPS512RB_MASKZ`.
    fn vsqrtps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VSQRTPS512RB_MASKZ, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VSQRTSD`.
    fn vsqrtsd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSQRTSDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSQRTSDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTSD");
        }
    }
    /// Emits `VSQRTSDRRR_ER`.
    fn vsqrtsd_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSQRTSDRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSQRTSD_MASK`.
    fn vsqrtsd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSQRTSDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSQRTSDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTSD_MASK");
        }
    }
    /// Emits `VSQRTSDRRR_MASK_ER`.
    fn vsqrtsd_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSQRTSDRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSQRTSD_MASKZ`.
    fn vsqrtsd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSQRTSDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSQRTSDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTSD_MASKZ");
        }
    }
    /// Emits `VSQRTSDRRR_MASKZ_ER`.
    fn vsqrtsd_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSQRTSDRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSQRTSS`.
    fn vsqrtss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSQRTSSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSQRTSSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTSS");
        }
    }
    /// Emits `VSQRTSSRRR_ER`.
    fn vsqrtss_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSQRTSSRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSQRTSS_MASK`.
    fn vsqrtss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSQRTSSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSQRTSSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTSS_MASK");
        }
    }
    /// Emits `VSQRTSSRRR_MASK_ER`.
    fn vsqrtss_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSQRTSSRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSQRTSS_MASKZ`.
    fn vsqrtss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSQRTSSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSQRTSSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSQRTSS_MASKZ");
        }
    }
    /// Emits `VSQRTSSRRR_MASKZ_ER`.
    fn vsqrtss_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSQRTSSRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPD128`.
    fn vsubpd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBPD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBPD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBPD128");
        }
    }
    /// Emits `VSUBPD128_MASK`.
    fn vsubpd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBPD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBPD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBPD128_MASK");
        }
    }
    /// Emits `VSUBPD128_MASKZ`.
    fn vsubpd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBPD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBPD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBPD128_MASKZ");
        }
    }
    /// Emits `VSUBPD128RRB`.
    fn vsubpd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPD128RRB_MASK`.
    fn vsubpd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPD128RRB_MASKZ`.
    fn vsubpd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPD256`.
    fn vsubpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBPD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBPD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBPD256");
        }
    }
    /// Emits `VSUBPD256_MASK`.
    fn vsubpd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBPD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBPD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBPD256_MASK");
        }
    }
    /// Emits `VSUBPD256_MASKZ`.
    fn vsubpd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBPD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBPD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBPD256_MASKZ");
        }
    }
    /// Emits `VSUBPD256RRB`.
    fn vsubpd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPD256RRB_MASK`.
    fn vsubpd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPD256RRB_MASKZ`.
    fn vsubpd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPD512`.
    fn vsubpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBPD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBPD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBPD512");
        }
    }
    /// Emits `VSUBPD512RRR_ER`.
    fn vsubpd512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPD512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPD512_MASK`.
    fn vsubpd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBPD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBPD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBPD512_MASK");
        }
    }
    /// Emits `VSUBPD512RRR_MASK_ER`.
    fn vsubpd512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPD512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPD512_MASKZ`.
    fn vsubpd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBPD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBPD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBPD512_MASKZ");
        }
    }
    /// Emits `VSUBPD512RRR_MASKZ_ER`.
    fn vsubpd512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPD512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPD512RRB`.
    fn vsubpd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPD512RRB_MASK`.
    fn vsubpd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPD512RRB_MASKZ`.
    fn vsubpd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPS128`.
    fn vsubps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBPS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBPS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBPS128");
        }
    }
    /// Emits `VSUBPS128_MASK`.
    fn vsubps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBPS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBPS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBPS128_MASK");
        }
    }
    /// Emits `VSUBPS128_MASKZ`.
    fn vsubps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBPS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBPS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBPS128_MASKZ");
        }
    }
    /// Emits `VSUBPS128RRB`.
    fn vsubps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPS128RRB_MASK`.
    fn vsubps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPS128RRB_MASKZ`.
    fn vsubps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPS256`.
    fn vsubps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBPS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBPS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBPS256");
        }
    }
    /// Emits `VSUBPS256_MASK`.
    fn vsubps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBPS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBPS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBPS256_MASK");
        }
    }
    /// Emits `VSUBPS256_MASKZ`.
    fn vsubps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBPS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBPS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBPS256_MASKZ");
        }
    }
    /// Emits `VSUBPS256RRB`.
    fn vsubps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPS256RRB_MASK`.
    fn vsubps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPS256RRB_MASKZ`.
    fn vsubps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPS512`.
    fn vsubps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBPS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBPS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBPS512");
        }
    }
    /// Emits `VSUBPS512RRR_ER`.
    fn vsubps512_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPS512RRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPS512_MASK`.
    fn vsubps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBPS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBPS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBPS512_MASK");
        }
    }
    /// Emits `VSUBPS512RRR_MASK_ER`.
    fn vsubps512_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPS512RRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPS512_MASKZ`.
    fn vsubps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBPS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBPS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBPS512_MASKZ");
        }
    }
    /// Emits `VSUBPS512RRR_MASKZ_ER`.
    fn vsubps512_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPS512RRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPS512RRB`.
    fn vsubps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPS512RRB_MASK`.
    fn vsubps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBPS512RRB_MASKZ`.
    fn vsubps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBPS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBSD`.
    fn vsubsd(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBSDRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBSDRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBSD");
        }
    }
    /// Emits `VSUBSDRRR_ER`.
    fn vsubsd_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBSDRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBSD_MASK`.
    fn vsubsd_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBSDRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBSDRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBSD_MASK");
        }
    }
    /// Emits `VSUBSDRRR_MASK_ER`.
    fn vsubsd_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBSDRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBSD_MASKZ`.
    fn vsubsd_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBSDRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBSDRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBSD_MASKZ");
        }
    }
    /// Emits `VSUBSDRRR_MASKZ_ER`.
    fn vsubsd_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBSDRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBSS`.
    fn vsubss(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBSSRRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBSSRRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBSS");
        }
    }
    /// Emits `VSUBSSRRR_ER`.
    fn vsubss_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBSSRRR_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBSS_MASK`.
    fn vsubss_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBSSRRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBSSRRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBSS_MASK");
        }
    }
    /// Emits `VSUBSSRRR_MASK_ER`.
    fn vsubss_mask_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBSSRRR_MASK_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VSUBSS_MASKZ`.
    fn vsubss_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VSUBSSRRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VSUBSSRRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VSUBSS_MASKZ");
        }
    }
    /// Emits `VSUBSSRRR_MASKZ_ER`.
    fn vsubss_maskz_er(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VSUBSSRRR_MASKZ_ER, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUCOMISD`.
    fn vucomisd(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VUCOMISDRR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VUCOMISDRM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VUCOMISD");
        }
    }
    /// Emits `VUCOMISDRR_SAE`.
    fn vucomisd_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VUCOMISDRR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VUCOMISS`.
    fn vucomiss(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        if op0.is_vec() && op1.is_vec() {
            self.emit(VUCOMISSRR, op0,op1,&NOREG,&NOREG);
        } else if op0.is_vec() && op1.is_mem() {
            self.emit(VUCOMISSRM, op0,op1,&NOREG,&NOREG);
        } else {
            unreachable!("invalid operand types for VUCOMISS");
        }
    }
    /// Emits `VUCOMISSRR_SAE`.
    fn vucomiss_sae(&mut self,op0: impl OperandCast,op1: impl OperandCast) -> () {
        self.emit(VUCOMISSRR_SAE, op0.as_operand(),op1.as_operand(),&NOREG,&NOREG);
    }
    /// Emits `VUNPCKHPD128`.
    fn vunpckhpd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKHPD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKHPD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKHPD128");
        }
    }
    /// Emits `VUNPCKHPD128_MASK`.
    fn vunpckhpd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKHPD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKHPD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKHPD128_MASK");
        }
    }
    /// Emits `VUNPCKHPD128_MASKZ`.
    fn vunpckhpd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKHPD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKHPD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKHPD128_MASKZ");
        }
    }
    /// Emits `VUNPCKHPD128RRB`.
    fn vunpckhpd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKHPD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKHPD128RRB_MASK`.
    fn vunpckhpd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKHPD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKHPD128RRB_MASKZ`.
    fn vunpckhpd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKHPD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKHPD256`.
    fn vunpckhpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKHPD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKHPD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKHPD256");
        }
    }
    /// Emits `VUNPCKHPD256_MASK`.
    fn vunpckhpd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKHPD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKHPD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKHPD256_MASK");
        }
    }
    /// Emits `VUNPCKHPD256_MASKZ`.
    fn vunpckhpd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKHPD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKHPD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKHPD256_MASKZ");
        }
    }
    /// Emits `VUNPCKHPD256RRB`.
    fn vunpckhpd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKHPD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKHPD256RRB_MASK`.
    fn vunpckhpd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKHPD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKHPD256RRB_MASKZ`.
    fn vunpckhpd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKHPD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKHPD512`.
    fn vunpckhpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKHPD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKHPD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKHPD512");
        }
    }
    /// Emits `VUNPCKHPD512_MASK`.
    fn vunpckhpd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKHPD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKHPD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKHPD512_MASK");
        }
    }
    /// Emits `VUNPCKHPD512_MASKZ`.
    fn vunpckhpd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKHPD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKHPD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKHPD512_MASKZ");
        }
    }
    /// Emits `VUNPCKHPD512RRB`.
    fn vunpckhpd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKHPD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKHPD512RRB_MASK`.
    fn vunpckhpd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKHPD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKHPD512RRB_MASKZ`.
    fn vunpckhpd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKHPD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKHPS128`.
    fn vunpckhps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKHPS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKHPS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKHPS128");
        }
    }
    /// Emits `VUNPCKHPS128_MASK`.
    fn vunpckhps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKHPS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKHPS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKHPS128_MASK");
        }
    }
    /// Emits `VUNPCKHPS128_MASKZ`.
    fn vunpckhps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKHPS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKHPS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKHPS128_MASKZ");
        }
    }
    /// Emits `VUNPCKHPS128RRB`.
    fn vunpckhps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKHPS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKHPS128RRB_MASK`.
    fn vunpckhps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKHPS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKHPS128RRB_MASKZ`.
    fn vunpckhps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKHPS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKHPS256`.
    fn vunpckhps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKHPS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKHPS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKHPS256");
        }
    }
    /// Emits `VUNPCKHPS256_MASK`.
    fn vunpckhps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKHPS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKHPS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKHPS256_MASK");
        }
    }
    /// Emits `VUNPCKHPS256_MASKZ`.
    fn vunpckhps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKHPS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKHPS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKHPS256_MASKZ");
        }
    }
    /// Emits `VUNPCKHPS256RRB`.
    fn vunpckhps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKHPS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKHPS256RRB_MASK`.
    fn vunpckhps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKHPS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKHPS256RRB_MASKZ`.
    fn vunpckhps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKHPS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKHPS512`.
    fn vunpckhps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKHPS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKHPS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKHPS512");
        }
    }
    /// Emits `VUNPCKHPS512_MASK`.
    fn vunpckhps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKHPS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKHPS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKHPS512_MASK");
        }
    }
    /// Emits `VUNPCKHPS512_MASKZ`.
    fn vunpckhps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKHPS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKHPS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKHPS512_MASKZ");
        }
    }
    /// Emits `VUNPCKHPS512RRB`.
    fn vunpckhps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKHPS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKHPS512RRB_MASK`.
    fn vunpckhps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKHPS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKHPS512RRB_MASKZ`.
    fn vunpckhps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKHPS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKLPD128`.
    fn vunpcklpd128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKLPD128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKLPD128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKLPD128");
        }
    }
    /// Emits `VUNPCKLPD128_MASK`.
    fn vunpcklpd128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKLPD128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKLPD128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKLPD128_MASK");
        }
    }
    /// Emits `VUNPCKLPD128_MASKZ`.
    fn vunpcklpd128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKLPD128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKLPD128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKLPD128_MASKZ");
        }
    }
    /// Emits `VUNPCKLPD128RRB`.
    fn vunpcklpd128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKLPD128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKLPD128RRB_MASK`.
    fn vunpcklpd128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKLPD128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKLPD128RRB_MASKZ`.
    fn vunpcklpd128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKLPD128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKLPD256`.
    fn vunpcklpd256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKLPD256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKLPD256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKLPD256");
        }
    }
    /// Emits `VUNPCKLPD256_MASK`.
    fn vunpcklpd256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKLPD256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKLPD256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKLPD256_MASK");
        }
    }
    /// Emits `VUNPCKLPD256_MASKZ`.
    fn vunpcklpd256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKLPD256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKLPD256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKLPD256_MASKZ");
        }
    }
    /// Emits `VUNPCKLPD256RRB`.
    fn vunpcklpd256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKLPD256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKLPD256RRB_MASK`.
    fn vunpcklpd256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKLPD256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKLPD256RRB_MASKZ`.
    fn vunpcklpd256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKLPD256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKLPD512`.
    fn vunpcklpd512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKLPD512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKLPD512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKLPD512");
        }
    }
    /// Emits `VUNPCKLPD512_MASK`.
    fn vunpcklpd512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKLPD512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKLPD512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKLPD512_MASK");
        }
    }
    /// Emits `VUNPCKLPD512_MASKZ`.
    fn vunpcklpd512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKLPD512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKLPD512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKLPD512_MASKZ");
        }
    }
    /// Emits `VUNPCKLPD512RRB`.
    fn vunpcklpd512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKLPD512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKLPD512RRB_MASK`.
    fn vunpcklpd512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKLPD512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKLPD512RRB_MASKZ`.
    fn vunpcklpd512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKLPD512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKLPS128`.
    fn vunpcklps128(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKLPS128RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKLPS128RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKLPS128");
        }
    }
    /// Emits `VUNPCKLPS128_MASK`.
    fn vunpcklps128_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKLPS128RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKLPS128RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKLPS128_MASK");
        }
    }
    /// Emits `VUNPCKLPS128_MASKZ`.
    fn vunpcklps128_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKLPS128RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKLPS128RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKLPS128_MASKZ");
        }
    }
    /// Emits `VUNPCKLPS128RRB`.
    fn vunpcklps128b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKLPS128RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKLPS128RRB_MASK`.
    fn vunpcklps128b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKLPS128RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKLPS128RRB_MASKZ`.
    fn vunpcklps128b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKLPS128RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKLPS256`.
    fn vunpcklps256(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKLPS256RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKLPS256RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKLPS256");
        }
    }
    /// Emits `VUNPCKLPS256_MASK`.
    fn vunpcklps256_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKLPS256RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKLPS256RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKLPS256_MASK");
        }
    }
    /// Emits `VUNPCKLPS256_MASKZ`.
    fn vunpcklps256_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKLPS256RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKLPS256RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKLPS256_MASKZ");
        }
    }
    /// Emits `VUNPCKLPS256RRB`.
    fn vunpcklps256b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKLPS256RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKLPS256RRB_MASK`.
    fn vunpcklps256b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKLPS256RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKLPS256RRB_MASKZ`.
    fn vunpcklps256b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKLPS256RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKLPS512`.
    fn vunpcklps512(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKLPS512RRR, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKLPS512RRM, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKLPS512");
        }
    }
    /// Emits `VUNPCKLPS512_MASK`.
    fn vunpcklps512_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKLPS512RRR_MASK, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKLPS512RRM_MASK, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKLPS512_MASK");
        }
    }
    /// Emits `VUNPCKLPS512_MASKZ`.
    fn vunpcklps512_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        let op0 = op0.as_operand();
        let op1 = op1.as_operand();
        let op2 = op2.as_operand();
        if op0.is_vec() && op1.is_vec() && op2.is_vec() {
            self.emit(VUNPCKLPS512RRR_MASKZ, op0,op1,op2,&NOREG);
        } else if op0.is_vec() && op1.is_vec() && op2.is_mem() {
            self.emit(VUNPCKLPS512RRM_MASKZ, op0,op1,op2,&NOREG);
        } else {
            unreachable!("invalid operand types for VUNPCKLPS512_MASKZ");
        }
    }
    /// Emits `VUNPCKLPS512RRB`.
    fn vunpcklps512b(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKLPS512RRB, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKLPS512RRB_MASK`.
    fn vunpcklps512b_mask(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKLPS512RRB_MASK, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
    /// Emits `VUNPCKLPS512RRB_MASKZ`.
    fn vunpcklps512b_maskz(&mut self,op0: impl OperandCast,op1: impl OperandCast,op2: impl OperandCast) -> () {
        self.emit(VUNPCKLPS512RRB_MASKZ, op0.as_operand(),op1.as_operand(),op2.as_operand(),&NOREG);
    }
}
