//! X86 emitter traits, generated by `meta/x86_emitter_gen.py` from
//! `meta/x86_emitter.txt` (AsmJit's `x86emitter.h` declarations). Do not edit by
//! hand; regenerate instead.
//!
//! Conventions:
//! - One `{Name}Emitter<T0..Tn>` trait per (mnemonic, arity) pair. `Assembler<'_>`
//!   implements it once per declared concrete operand tuple, forwarding to
//!   `emit_n(InstId::{Id} as u32, &[...])`. Unlike the legacy `features/*` API there
//!   are no inherent forwarding methods, so both APIs can coexist.
//! - Both `[EXPLICIT]` (fixed-register operands spelled out) and `[IMPLICIT]`
//!   (fixed registers hidden) input declarations are kept. When a mnemonic is
//!   declared with more than one arity, the first-seen arity keeps the plain name
//!   and later arities are suffixed: the 1-operand explicit `cbw` keeps its name,
//!   the 0-operand implicit form becomes `cbw_0`.
//! - AsmJit fixed-register alias operands (`Gp_AX`, `DS_ZSI`, `XMM0`, ...) are
//!   collapsed to their base operand kind (`Gp`, `Mem`, `Vec`); operand tuples
//!   that become duplicates after collapsing are dropped (first one wins).
//! - Besides the abstract-kind impls (`MovEmitter<Gp, Gp>`), impls are generated
//!   for the concrete sized register wrappers (`Gpq`/`Gpd`/`Gpw`/`GpbLo`/`GpbHi`,
//!   `Xmm`/`Ymm`/`Zmm`, ...) so register constants work without dereferencing
//!   (`a.mov(RAX, RBX)`). Only width combinations present in the instruction's
//!   instdb signatures are generated. Immediate positions take `U: Into<Imm>`,
//!   so integer literals work too (`a.mov(RAX, 42)`); when a sized impl would
//!   overlap its abstract `Imm` counterpart, the generic one replaces it.
//! - The conditional families `j`, `set` and `cmov` take an explicit [`CondCode`]
//!   translated through the `JCC_FROM_COND` / `SETCC_FROM_COND` /
//!   `CMOVCC_FROM_COND` tables below, plus one named method per x86 condition
//!   suffix (`jo`, `jno`, ...) that calls a concrete `InstId::{Id}{Suffix}`.
//! - Rust keywords are raw-escaped (`r#loop`, `r#in`); names already C++-escaped
//!   in the input (`and_`, `or_`, `not_`, `xor_`, `int_`) are kept as-is.
#![allow(non_snake_case, non_camel_case_types)]
use super::{assembler::*, instdb::*, operands::*};
use crate::core::globals::CondCode;
use crate::core::operand::*;

/// Translates asmkit's ARM-ordered [`CondCode`] to a `jcc`
/// instruction id, reindexing AsmJit's x86-ordered
/// `_jcc_from_cond_table` (x86globals.h). Conditions with no x86
/// form map to `InstId::None`.
pub(crate) const JCC_FROM_COND: [u32; 16] = [
    InstId::Jmp as u32,  // AL: unconditional jump
    InstId::None as u32, // NA: no x86 equivalent
    InstId::Jz as u32,   // EQ
    InstId::Jnz as u32,  // NE
    InstId::Jnb as u32,  // CS
    InstId::Jb as u32,   // LO
    InstId::Js as u32,   // MI
    InstId::Jns as u32,  // PL
    InstId::Jo as u32,   // VS
    InstId::Jno as u32,  // VC
    InstId::Jnbe as u32, // HI
    InstId::Jbe as u32,  // LS
    InstId::Jnl as u32,  // GE
    InstId::Jl as u32,   // LT
    InstId::Jnle as u32, // GT
    InstId::Jle as u32,  // LE
];

/// Translates asmkit's ARM-ordered [`CondCode`] to a `setcc`
/// instruction id, reindexing AsmJit's x86-ordered
/// `_setcc_from_cond_table` (x86globals.h). Conditions with no x86
/// form map to `InstId::None`.
pub(crate) const SETCC_FROM_COND: [u32; 16] = [
    InstId::None as u32,   // AL: no x86 equivalent
    InstId::None as u32,   // NA: no x86 equivalent
    InstId::Setz as u32,   // EQ
    InstId::Setnz as u32,  // NE
    InstId::Setnb as u32,  // CS
    InstId::Setb as u32,   // LO
    InstId::Sets as u32,   // MI
    InstId::Setns as u32,  // PL
    InstId::Seto as u32,   // VS
    InstId::Setno as u32,  // VC
    InstId::Setnbe as u32, // HI
    InstId::Setbe as u32,  // LS
    InstId::Setnl as u32,  // GE
    InstId::Setl as u32,   // LT
    InstId::Setnle as u32, // GT
    InstId::Setle as u32,  // LE
];

/// Translates asmkit's ARM-ordered [`CondCode`] to a `cmovcc`
/// instruction id, reindexing AsmJit's x86-ordered
/// `_cmovcc_from_cond_table` (x86globals.h). Conditions with no x86
/// form map to `InstId::None`.
pub(crate) const CMOVCC_FROM_COND: [u32; 16] = [
    InstId::None as u32,    // AL: no x86 equivalent
    InstId::None as u32,    // NA: no x86 equivalent
    InstId::Cmovz as u32,   // EQ
    InstId::Cmovnz as u32,  // NE
    InstId::Cmovnb as u32,  // CS
    InstId::Cmovb as u32,   // LO
    InstId::Cmovs as u32,   // MI
    InstId::Cmovns as u32,  // PL
    InstId::Cmovo as u32,   // VS
    InstId::Cmovno as u32,  // VC
    InstId::Cmovnbe as u32, // HI
    InstId::Cmovbe as u32,  // LS
    InstId::Cmovnl as u32,  // GE
    InstId::Cmovl as u32,   // LT
    InstId::Cmovnle as u32, // GT
    InstId::Cmovle as u32,  // LE
];

/// Emits `ADC`.
pub trait AdcEmitter<T0, T1> {
    fn adc(&mut self, op0: T0, op1: T1);
}

/// Emits `ADD`.
pub trait AddEmitter<T0, T1> {
    fn add(&mut self, op0: T0, op1: T1);
}

/// Emits `AND_`.
pub trait And_Emitter<T0, T1> {
    fn and_(&mut self, op0: T0, op1: T1);
}

/// Emits `BOUND`.
pub trait BoundEmitter<T0, T1> {
    fn bound(&mut self, op0: T0, op1: T1);
}

/// Emits `BSF`.
pub trait BsfEmitter<T0, T1> {
    fn bsf(&mut self, op0: T0, op1: T1);
}

/// Emits `BSR`.
pub trait BsrEmitter<T0, T1> {
    fn bsr(&mut self, op0: T0, op1: T1);
}

/// Emits `BSWAP`.
pub trait BswapEmitter<T0> {
    fn bswap(&mut self, op0: T0);
}

/// Emits `BT`.
pub trait BtEmitter<T0, T1> {
    fn bt(&mut self, op0: T0, op1: T1);
}

/// Emits `BTC`.
pub trait BtcEmitter<T0, T1> {
    fn btc(&mut self, op0: T0, op1: T1);
}

/// Emits `BTR`.
pub trait BtrEmitter<T0, T1> {
    fn btr(&mut self, op0: T0, op1: T1);
}

/// Emits `BTS`.
pub trait BtsEmitter<T0, T1> {
    fn bts(&mut self, op0: T0, op1: T1);
}

/// Emits `CBW`.
pub trait CbwEmitter<T0> {
    fn cbw(&mut self, op0: T0);
}

/// Emits `CDQ`.
pub trait CdqEmitter<T0, T1> {
    fn cdq(&mut self, op0: T0, op1: T1);
}

/// Emits `CDQE`.
pub trait CdqeEmitter<T0> {
    fn cdqe(&mut self, op0: T0);
}

/// Emits `CQO`.
pub trait CqoEmitter<T0, T1> {
    fn cqo(&mut self, op0: T0, op1: T1);
}

/// Emits `CWD`.
pub trait CwdEmitter<T0, T1> {
    fn cwd(&mut self, op0: T0, op1: T1);
}

/// Emits `CWDE`.
pub trait CwdeEmitter<T0> {
    fn cwde(&mut self, op0: T0);
}

/// Emits `CALL`.
pub trait CallEmitter<T0> {
    fn call(&mut self, op0: T0);
}

/// Emits `CMOV`.
pub trait CmovEmitter<T0, T1> {
    fn cmov(&mut self, cc: CondCode, op0: T0, op1: T1);
    fn cmovo(&mut self, op0: T0, op1: T1);
    fn cmovno(&mut self, op0: T0, op1: T1);
    fn cmovb(&mut self, op0: T0, op1: T1);
    fn cmovnb(&mut self, op0: T0, op1: T1);
    fn cmovz(&mut self, op0: T0, op1: T1);
    fn cmovnz(&mut self, op0: T0, op1: T1);
    fn cmovbe(&mut self, op0: T0, op1: T1);
    fn cmovnbe(&mut self, op0: T0, op1: T1);
    fn cmovs(&mut self, op0: T0, op1: T1);
    fn cmovns(&mut self, op0: T0, op1: T1);
    fn cmovp(&mut self, op0: T0, op1: T1);
    fn cmovnp(&mut self, op0: T0, op1: T1);
    fn cmovl(&mut self, op0: T0, op1: T1);
    fn cmovnl(&mut self, op0: T0, op1: T1);
    fn cmovle(&mut self, op0: T0, op1: T1);
    fn cmovnle(&mut self, op0: T0, op1: T1);
}

/// Emits `CMP`.
pub trait CmpEmitter<T0, T1> {
    fn cmp(&mut self, op0: T0, op1: T1);
}

/// Emits `CMPS`.
pub trait CmpsEmitter<T0, T1> {
    fn cmps(&mut self, op0: T0, op1: T1);
}

/// Emits `CMPXCHG`.
pub trait CmpxchgEmitter<T0, T1, T2> {
    fn cmpxchg(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CMPXCHG16B`.
pub trait Cmpxchg16bEmitter<T0, T1, T2, T3, T4> {
    fn cmpxchg16b(&mut self, op0: T0, op1: T1, op2: T2, op3: T3, op4: T4);
}

/// Emits `CMPXCHG8B`.
pub trait Cmpxchg8bEmitter<T0, T1, T2, T3, T4> {
    fn cmpxchg8b(&mut self, op0: T0, op1: T1, op2: T2, op3: T3, op4: T4);
}

/// Emits `DEC`.
pub trait DecEmitter<T0> {
    fn dec(&mut self, op0: T0);
}

/// Emits `DIV`.
pub trait DivEmitter<T0, T1> {
    fn div(&mut self, op0: T0, op1: T1);
}

/// Emits `DIV_3`.
pub trait Div3Emitter<T0, T1, T2> {
    fn div_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `IDIV`.
pub trait IdivEmitter<T0, T1> {
    fn idiv(&mut self, op0: T0, op1: T1);
}

/// Emits `IDIV_3`.
pub trait Idiv3Emitter<T0, T1, T2> {
    fn idiv_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `IMUL`.
pub trait ImulEmitter<T0, T1> {
    fn imul(&mut self, op0: T0, op1: T1);
}

/// Emits `IMUL_3`.
pub trait Imul3Emitter<T0, T1, T2> {
    fn imul_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `INC`.
pub trait IncEmitter<T0> {
    fn inc(&mut self, op0: T0);
}

/// Emits `J`.
pub trait JEmitter<T0> {
    fn j(&mut self, cc: CondCode, op0: T0);
    fn jo(&mut self, op0: T0);
    fn jno(&mut self, op0: T0);
    fn jb(&mut self, op0: T0);
    fn jnb(&mut self, op0: T0);
    fn jz(&mut self, op0: T0);
    fn jnz(&mut self, op0: T0);
    fn jbe(&mut self, op0: T0);
    fn jnbe(&mut self, op0: T0);
    fn js(&mut self, op0: T0);
    fn jns(&mut self, op0: T0);
    fn jp(&mut self, op0: T0);
    fn jnp(&mut self, op0: T0);
    fn jl(&mut self, op0: T0);
    fn jnl(&mut self, op0: T0);
    fn jle(&mut self, op0: T0);
    fn jnle(&mut self, op0: T0);
}

/// Emits `JECXZ`.
pub trait JecxzEmitter<T0, T1> {
    fn jecxz(&mut self, op0: T0, op1: T1);
}

/// Emits `JMP`.
pub trait JmpEmitter<T0> {
    fn jmp(&mut self, op0: T0);
}

/// Emits `LCALL`.
pub trait LcallEmitter<T0, T1> {
    fn lcall(&mut self, op0: T0, op1: T1);
}

/// Emits `LCALL_1`.
pub trait Lcall1Emitter<T0> {
    fn lcall_1(&mut self, op0: T0);
}

/// Emits `LEA`.
pub trait LeaEmitter<T0, T1> {
    fn lea(&mut self, op0: T0, op1: T1);
}

/// Emits `LJMP`.
pub trait LjmpEmitter<T0, T1> {
    fn ljmp(&mut self, op0: T0, op1: T1);
}

/// Emits `LJMP_1`.
pub trait Ljmp1Emitter<T0> {
    fn ljmp_1(&mut self, op0: T0);
}

/// Emits `LODS`.
pub trait LodsEmitter<T0, T1> {
    fn lods(&mut self, op0: T0, op1: T1);
}

/// Emits `LOOP`.
pub trait LoopEmitter<T0, T1> {
    fn r#loop(&mut self, op0: T0, op1: T1);
}

/// Emits `LOOPE`.
pub trait LoopeEmitter<T0, T1> {
    fn loope(&mut self, op0: T0, op1: T1);
}

/// Emits `LOOPNE`.
pub trait LoopneEmitter<T0, T1> {
    fn loopne(&mut self, op0: T0, op1: T1);
}

/// Emits `MOV`.
pub trait MovEmitter<T0, T1> {
    fn mov(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVABS`.
pub trait MovabsEmitter<T0, T1> {
    fn movabs(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVNTI`.
pub trait MovntiEmitter<T0, T1> {
    fn movnti(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVS`.
pub trait MovsEmitter<T0, T1> {
    fn movs(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVSX`.
pub trait MovsxEmitter<T0, T1> {
    fn movsx(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVSXD`.
pub trait MovsxdEmitter<T0, T1> {
    fn movsxd(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVZX`.
pub trait MovzxEmitter<T0, T1> {
    fn movzx(&mut self, op0: T0, op1: T1);
}

/// Emits `MUL`.
pub trait MulEmitter<T0, T1> {
    fn mul(&mut self, op0: T0, op1: T1);
}

/// Emits `MUL_3`.
pub trait Mul3Emitter<T0, T1, T2> {
    fn mul_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `NEG`.
pub trait NegEmitter<T0> {
    fn neg(&mut self, op0: T0);
}

/// Emits `NOP`.
pub trait NopEmitter {
    fn nop(&mut self);
}

/// Emits `NOP_1`.
pub trait Nop1Emitter<T0> {
    fn nop_1(&mut self, op0: T0);
}

/// Emits `NOP_2`.
pub trait Nop2Emitter<T0, T1> {
    fn nop_2(&mut self, op0: T0, op1: T1);
}

/// Emits `NOT_`.
pub trait Not_Emitter<T0> {
    fn not_(&mut self, op0: T0);
}

/// Emits `OR_`.
pub trait Or_Emitter<T0, T1> {
    fn or_(&mut self, op0: T0, op1: T1);
}

/// Emits `POP`.
pub trait PopEmitter<T0> {
    fn pop(&mut self, op0: T0);
}

/// Emits `POPA`.
pub trait PopaEmitter {
    fn popa(&mut self);
}

/// Emits `POPAD`.
pub trait PopadEmitter {
    fn popad(&mut self);
}

/// Emits `POPF`.
pub trait PopfEmitter {
    fn popf(&mut self);
}

/// Emits `POPFD`.
pub trait PopfdEmitter {
    fn popfd(&mut self);
}

/// Emits `POPFQ`.
pub trait PopfqEmitter {
    fn popfq(&mut self);
}

/// Emits `PUSH`.
pub trait PushEmitter<T0> {
    fn push(&mut self, op0: T0);
}

/// Emits `PUSHA`.
pub trait PushaEmitter {
    fn pusha(&mut self);
}

/// Emits `PUSHAD`.
pub trait PushadEmitter {
    fn pushad(&mut self);
}

/// Emits `PUSHF`.
pub trait PushfEmitter {
    fn pushf(&mut self);
}

/// Emits `PUSHFD`.
pub trait PushfdEmitter {
    fn pushfd(&mut self);
}

/// Emits `PUSHFQ`.
pub trait PushfqEmitter {
    fn pushfq(&mut self);
}

/// Emits `PUSHW`.
pub trait PushwEmitter<T0> {
    fn pushw(&mut self, op0: T0);
}

/// Emits `RCL`.
pub trait RclEmitter<T0, T1> {
    fn rcl(&mut self, op0: T0, op1: T1);
}

/// Emits `RCR`.
pub trait RcrEmitter<T0, T1> {
    fn rcr(&mut self, op0: T0, op1: T1);
}

/// Emits `ROL`.
pub trait RolEmitter<T0, T1> {
    fn rol(&mut self, op0: T0, op1: T1);
}

/// Emits `ROR`.
pub trait RorEmitter<T0, T1> {
    fn ror(&mut self, op0: T0, op1: T1);
}

/// Emits `SBB`.
pub trait SbbEmitter<T0, T1> {
    fn sbb(&mut self, op0: T0, op1: T1);
}

/// Emits `SAL`.
pub trait SalEmitter<T0, T1> {
    fn sal(&mut self, op0: T0, op1: T1);
}

/// Emits `SAR`.
pub trait SarEmitter<T0, T1> {
    fn sar(&mut self, op0: T0, op1: T1);
}

/// Emits `SCAS`.
pub trait ScasEmitter<T0, T1> {
    fn scas(&mut self, op0: T0, op1: T1);
}

/// Emits `SET`.
pub trait SetEmitter<T0> {
    fn set(&mut self, cc: CondCode, op0: T0);
    fn seto(&mut self, op0: T0);
    fn setno(&mut self, op0: T0);
    fn setb(&mut self, op0: T0);
    fn setnb(&mut self, op0: T0);
    fn setz(&mut self, op0: T0);
    fn setnz(&mut self, op0: T0);
    fn setbe(&mut self, op0: T0);
    fn setnbe(&mut self, op0: T0);
    fn sets(&mut self, op0: T0);
    fn setns(&mut self, op0: T0);
    fn setp(&mut self, op0: T0);
    fn setnp(&mut self, op0: T0);
    fn setl(&mut self, op0: T0);
    fn setnl(&mut self, op0: T0);
    fn setle(&mut self, op0: T0);
    fn setnle(&mut self, op0: T0);
}

/// Emits `SHL`.
pub trait ShlEmitter<T0, T1> {
    fn shl(&mut self, op0: T0, op1: T1);
}

/// Emits `SHR`.
pub trait ShrEmitter<T0, T1> {
    fn shr(&mut self, op0: T0, op1: T1);
}

/// Emits `SHLD`.
pub trait ShldEmitter<T0, T1, T2> {
    fn shld(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `SHRD`.
pub trait ShrdEmitter<T0, T1, T2> {
    fn shrd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `STOS`.
pub trait StosEmitter<T0, T1> {
    fn stos(&mut self, op0: T0, op1: T1);
}

/// Emits `SUB`.
pub trait SubEmitter<T0, T1> {
    fn sub(&mut self, op0: T0, op1: T1);
}

/// Emits `TEST`.
pub trait TestEmitter<T0, T1> {
    fn test(&mut self, op0: T0, op1: T1);
}

/// Emits `UD0`.
pub trait Ud0Emitter<T0, T1> {
    fn ud0(&mut self, op0: T0, op1: T1);
}

/// Emits `UD1`.
pub trait Ud1Emitter<T0, T1> {
    fn ud1(&mut self, op0: T0, op1: T1);
}

/// Emits `UD2`.
pub trait Ud2Emitter {
    fn ud2(&mut self);
}

/// Emits `XADD`.
pub trait XaddEmitter<T0, T1> {
    fn xadd(&mut self, op0: T0, op1: T1);
}

/// Emits `XCHG`.
pub trait XchgEmitter<T0, T1> {
    fn xchg(&mut self, op0: T0, op1: T1);
}

/// Emits `XOR_`.
pub trait Xor_Emitter<T0, T1> {
    fn xor_(&mut self, op0: T0, op1: T1);
}

/// Emits `AAA`.
pub trait AaaEmitter<T0> {
    fn aaa(&mut self, op0: T0);
}

/// Emits `AAD`.
pub trait AadEmitter<T0, T1> {
    fn aad(&mut self, op0: T0, op1: T1);
}

/// Emits `AAM`.
pub trait AamEmitter<T0, T1> {
    fn aam(&mut self, op0: T0, op1: T1);
}

/// Emits `AAS`.
pub trait AasEmitter<T0> {
    fn aas(&mut self, op0: T0);
}

/// Emits `DAA`.
pub trait DaaEmitter<T0> {
    fn daa(&mut self, op0: T0);
}

/// Emits `DAS`.
pub trait DasEmitter<T0> {
    fn das(&mut self, op0: T0);
}

/// Emits `ENTER`.
pub trait EnterEmitter<T0, T1> {
    fn enter(&mut self, op0: T0, op1: T1);
}

/// Emits `LEAVE`.
pub trait LeaveEmitter {
    fn leave(&mut self);
}

/// Emits `IN`.
pub trait InEmitter<T0, T1> {
    fn r#in(&mut self, op0: T0, op1: T1);
}

/// Emits `INS`.
pub trait InsEmitter<T0, T1> {
    fn ins(&mut self, op0: T0, op1: T1);
}

/// Emits `OUT`.
pub trait OutEmitter<T0, T1> {
    fn out(&mut self, op0: T0, op1: T1);
}

/// Emits `OUTS`.
pub trait OutsEmitter<T0, T1> {
    fn outs(&mut self, op0: T0, op1: T1);
}

/// Emits `CLC`.
pub trait ClcEmitter {
    fn clc(&mut self);
}

/// Emits `CLD`.
pub trait CldEmitter {
    fn cld(&mut self);
}

/// Emits `CMC`.
pub trait CmcEmitter {
    fn cmc(&mut self);
}

/// Emits `STC`.
pub trait StcEmitter {
    fn stc(&mut self);
}

/// Emits `STD`.
pub trait StdEmitter {
    fn std(&mut self);
}

/// Emits `ADCX`.
pub trait AdcxEmitter<T0, T1> {
    fn adcx(&mut self, op0: T0, op1: T1);
}

/// Emits `ADOX`.
pub trait AdoxEmitter<T0, T1> {
    fn adox(&mut self, op0: T0, op1: T1);
}

/// Emits `CPUID`.
pub trait CpuidEmitter<T0, T1, T2, T3> {
    fn cpuid(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `LAHF`.
pub trait LahfEmitter<T0> {
    fn lahf(&mut self, op0: T0);
}

/// Emits `SAHF`.
pub trait SahfEmitter<T0> {
    fn sahf(&mut self, op0: T0);
}

/// Emits `ANDN`.
pub trait AndnEmitter<T0, T1, T2> {
    fn andn(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `BEXTR`.
pub trait BextrEmitter<T0, T1, T2> {
    fn bextr(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `BLSI`.
pub trait BlsiEmitter<T0, T1> {
    fn blsi(&mut self, op0: T0, op1: T1);
}

/// Emits `BLSMSK`.
pub trait BlsmskEmitter<T0, T1> {
    fn blsmsk(&mut self, op0: T0, op1: T1);
}

/// Emits `BLSR`.
pub trait BlsrEmitter<T0, T1> {
    fn blsr(&mut self, op0: T0, op1: T1);
}

/// Emits `TZCNT`.
pub trait TzcntEmitter<T0, T1> {
    fn tzcnt(&mut self, op0: T0, op1: T1);
}

/// Emits `BZHI`.
pub trait BzhiEmitter<T0, T1, T2> {
    fn bzhi(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `MULX`.
pub trait MulxEmitter<T0, T1, T2, T3> {
    fn mulx(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `PDEP`.
pub trait PdepEmitter<T0, T1, T2> {
    fn pdep(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `PEXT`.
pub trait PextEmitter<T0, T1, T2> {
    fn pext(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `RORX`.
pub trait RorxEmitter<T0, T1, T2> {
    fn rorx(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `SARX`.
pub trait SarxEmitter<T0, T1, T2> {
    fn sarx(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `SHLX`.
pub trait ShlxEmitter<T0, T1, T2> {
    fn shlx(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `SHRX`.
pub trait ShrxEmitter<T0, T1, T2> {
    fn shrx(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CMPBEXADD`.
pub trait CmpbexaddEmitter<T0, T1, T2> {
    fn cmpbexadd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CMPBXADD`.
pub trait CmpbxaddEmitter<T0, T1, T2> {
    fn cmpbxadd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CMPLEXADD`.
pub trait CmplexaddEmitter<T0, T1, T2> {
    fn cmplexadd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CMPLXADD`.
pub trait CmplxaddEmitter<T0, T1, T2> {
    fn cmplxadd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CMPNBEXADD`.
pub trait CmpnbexaddEmitter<T0, T1, T2> {
    fn cmpnbexadd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CMPNBXADD`.
pub trait CmpnbxaddEmitter<T0, T1, T2> {
    fn cmpnbxadd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CMPNLEXADD`.
pub trait CmpnlexaddEmitter<T0, T1, T2> {
    fn cmpnlexadd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CMPNLXADD`.
pub trait CmpnlxaddEmitter<T0, T1, T2> {
    fn cmpnlxadd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CMPNOXADD`.
pub trait CmpnoxaddEmitter<T0, T1, T2> {
    fn cmpnoxadd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CMPNPXADD`.
pub trait CmpnpxaddEmitter<T0, T1, T2> {
    fn cmpnpxadd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CMPNSXADD`.
pub trait CmpnsxaddEmitter<T0, T1, T2> {
    fn cmpnsxadd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CMPNZXADD`.
pub trait CmpnzxaddEmitter<T0, T1, T2> {
    fn cmpnzxadd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CMPOXADD`.
pub trait CmpoxaddEmitter<T0, T1, T2> {
    fn cmpoxadd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CMPPXADD`.
pub trait CmppxaddEmitter<T0, T1, T2> {
    fn cmppxadd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CMPSXADD`.
pub trait CmpsxaddEmitter<T0, T1, T2> {
    fn cmpsxadd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CMPZXADD`.
pub trait CmpzxaddEmitter<T0, T1, T2> {
    fn cmpzxadd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CLDEMOTE`.
pub trait CldemoteEmitter<T0> {
    fn cldemote(&mut self, op0: T0);
}

/// Emits `CLFLUSH`.
pub trait ClflushEmitter<T0> {
    fn clflush(&mut self, op0: T0);
}

/// Emits `CLFLUSHOPT`.
pub trait ClflushoptEmitter<T0> {
    fn clflushopt(&mut self, op0: T0);
}

/// Emits `CLWB`.
pub trait ClwbEmitter<T0> {
    fn clwb(&mut self, op0: T0);
}

/// Emits `CLZERO`.
pub trait ClzeroEmitter<T0> {
    fn clzero(&mut self, op0: T0);
}

/// Emits `CRC32`.
pub trait Crc32Emitter<T0, T1> {
    fn crc32(&mut self, op0: T0, op1: T1);
}

/// Emits `LFENCE`.
pub trait LfenceEmitter {
    fn lfence(&mut self);
}

/// Emits `MFENCE`.
pub trait MfenceEmitter {
    fn mfence(&mut self);
}

/// Emits `SFENCE`.
pub trait SfenceEmitter {
    fn sfence(&mut self);
}

/// Emits `LZCNT`.
pub trait LzcntEmitter<T0, T1> {
    fn lzcnt(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVBE`.
pub trait MovbeEmitter<T0, T1> {
    fn movbe(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVDIRI`.
pub trait MovdiriEmitter<T0, T1> {
    fn movdiri(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVDIR64B`.
pub trait Movdir64bEmitter<T0, T1> {
    fn movdir64b(&mut self, op0: T0, op1: T1);
}

/// Emits `LDMXCSR`.
pub trait LdmxcsrEmitter<T0> {
    fn ldmxcsr(&mut self, op0: T0);
}

/// Emits `STMXCSR`.
pub trait StmxcsrEmitter<T0> {
    fn stmxcsr(&mut self, op0: T0);
}

/// Emits `POPCNT`.
pub trait PopcntEmitter<T0, T1> {
    fn popcnt(&mut self, op0: T0, op1: T1);
}

/// Emits `PREFETCH`.
pub trait PrefetchEmitter<T0> {
    fn prefetch(&mut self, op0: T0);
}

/// Emits `PREFETCHNTA`.
pub trait PrefetchntaEmitter<T0> {
    fn prefetchnta(&mut self, op0: T0);
}

/// Emits `PREFETCHT0`.
pub trait Prefetcht0Emitter<T0> {
    fn prefetcht0(&mut self, op0: T0);
}

/// Emits `PREFETCHT1`.
pub trait Prefetcht1Emitter<T0> {
    fn prefetcht1(&mut self, op0: T0);
}

/// Emits `PREFETCHT2`.
pub trait Prefetcht2Emitter<T0> {
    fn prefetcht2(&mut self, op0: T0);
}

/// Emits `PREFETCHW`.
pub trait PrefetchwEmitter<T0> {
    fn prefetchw(&mut self, op0: T0);
}

/// Emits `PREFETCHWT1`.
pub trait Prefetchwt1Emitter<T0> {
    fn prefetchwt1(&mut self, op0: T0);
}

/// Emits `PREFETCHIT0`.
pub trait Prefetchit0Emitter<T0> {
    fn prefetchit0(&mut self, op0: T0);
}

/// Emits `PREFETCHIT1`.
pub trait Prefetchit1Emitter<T0> {
    fn prefetchit1(&mut self, op0: T0);
}

/// Emits `AADD`.
pub trait AaddEmitter<T0, T1> {
    fn aadd(&mut self, op0: T0, op1: T1);
}

/// Emits `AAND`.
pub trait AandEmitter<T0, T1> {
    fn aand(&mut self, op0: T0, op1: T1);
}

/// Emits `AOR`.
pub trait AorEmitter<T0, T1> {
    fn aor(&mut self, op0: T0, op1: T1);
}

/// Emits `AXOR`.
pub trait AxorEmitter<T0, T1> {
    fn axor(&mut self, op0: T0, op1: T1);
}

/// Emits `RDPID`.
pub trait RdpidEmitter<T0> {
    fn rdpid(&mut self, op0: T0);
}

/// Emits `RDPRU`.
pub trait RdpruEmitter<T0, T1, T2> {
    fn rdpru(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `RDPKRU`.
pub trait RdpkruEmitter<T0, T1, T2> {
    fn rdpkru(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `RDTSC`.
pub trait RdtscEmitter<T0, T1> {
    fn rdtsc(&mut self, op0: T0, op1: T1);
}

/// Emits `RDTSCP`.
pub trait RdtscpEmitter<T0, T1, T2> {
    fn rdtscp(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `SERIALIZE`.
pub trait SerializeEmitter {
    fn serialize(&mut self);
}

/// Emits `BLCFILL`.
pub trait BlcfillEmitter<T0, T1> {
    fn blcfill(&mut self, op0: T0, op1: T1);
}

/// Emits `BLCI`.
pub trait BlciEmitter<T0, T1> {
    fn blci(&mut self, op0: T0, op1: T1);
}

/// Emits `BLCIC`.
pub trait BlcicEmitter<T0, T1> {
    fn blcic(&mut self, op0: T0, op1: T1);
}

/// Emits `BLCMSK`.
pub trait BlcmskEmitter<T0, T1> {
    fn blcmsk(&mut self, op0: T0, op1: T1);
}

/// Emits `BLCS`.
pub trait BlcsEmitter<T0, T1> {
    fn blcs(&mut self, op0: T0, op1: T1);
}

/// Emits `BLSFILL`.
pub trait BlsfillEmitter<T0, T1> {
    fn blsfill(&mut self, op0: T0, op1: T1);
}

/// Emits `BLSIC`.
pub trait BlsicEmitter<T0, T1> {
    fn blsic(&mut self, op0: T0, op1: T1);
}

/// Emits `T1MSKC`.
pub trait T1mskcEmitter<T0, T1> {
    fn t1mskc(&mut self, op0: T0, op1: T1);
}

/// Emits `TZMSK`.
pub trait TzmskEmitter<T0, T1> {
    fn tzmsk(&mut self, op0: T0, op1: T1);
}

/// Emits `ARPL`.
pub trait ArplEmitter<T0, T1> {
    fn arpl(&mut self, op0: T0, op1: T1);
}

/// Emits `CLI`.
pub trait CliEmitter {
    fn cli(&mut self);
}

/// Emits `GETSEC`.
pub trait GetsecEmitter {
    fn getsec(&mut self);
}

/// Emits `INT_`.
pub trait Int_Emitter<T0> {
    fn int_(&mut self, op0: T0);
}

/// Emits `INT3`.
pub trait Int3Emitter {
    fn int3(&mut self);
}

/// Emits `INTO`.
pub trait IntoEmitter {
    fn into(&mut self);
}

/// Emits `LAR`.
pub trait LarEmitter<T0, T1> {
    fn lar(&mut self, op0: T0, op1: T1);
}

/// Emits `LDS`.
pub trait LdsEmitter<T0, T1> {
    fn lds(&mut self, op0: T0, op1: T1);
}

/// Emits `LES`.
pub trait LesEmitter<T0, T1> {
    fn les(&mut self, op0: T0, op1: T1);
}

/// Emits `LFS`.
pub trait LfsEmitter<T0, T1> {
    fn lfs(&mut self, op0: T0, op1: T1);
}

/// Emits `LGS`.
pub trait LgsEmitter<T0, T1> {
    fn lgs(&mut self, op0: T0, op1: T1);
}

/// Emits `LSL`.
pub trait LslEmitter<T0, T1> {
    fn lsl(&mut self, op0: T0, op1: T1);
}

/// Emits `LSS`.
pub trait LssEmitter<T0, T1> {
    fn lss(&mut self, op0: T0, op1: T1);
}

/// Emits `PAUSE`.
pub trait PauseEmitter {
    fn pause(&mut self);
}

/// Emits `RSM`.
pub trait RsmEmitter {
    fn rsm(&mut self);
}

/// Emits `SGDT`.
pub trait SgdtEmitter<T0> {
    fn sgdt(&mut self, op0: T0);
}

/// Emits `SIDT`.
pub trait SidtEmitter<T0> {
    fn sidt(&mut self, op0: T0);
}

/// Emits `SLDT`.
pub trait SldtEmitter<T0> {
    fn sldt(&mut self, op0: T0);
}

/// Emits `SMSW`.
pub trait SmswEmitter<T0> {
    fn smsw(&mut self, op0: T0);
}

/// Emits `STI`.
pub trait StiEmitter {
    fn sti(&mut self);
}

/// Emits `STR`.
pub trait StrEmitter<T0> {
    fn str(&mut self, op0: T0);
}

/// Emits `VERR`.
pub trait VerrEmitter<T0> {
    fn verr(&mut self, op0: T0);
}

/// Emits `VERW`.
pub trait VerwEmitter<T0> {
    fn verw(&mut self, op0: T0);
}

/// Emits `RDFSBASE`.
pub trait RdfsbaseEmitter<T0> {
    fn rdfsbase(&mut self, op0: T0);
}

/// Emits `RDGSBASE`.
pub trait RdgsbaseEmitter<T0> {
    fn rdgsbase(&mut self, op0: T0);
}

/// Emits `WRFSBASE`.
pub trait WrfsbaseEmitter<T0> {
    fn wrfsbase(&mut self, op0: T0);
}

/// Emits `WRGSBASE`.
pub trait WrgsbaseEmitter<T0> {
    fn wrgsbase(&mut self, op0: T0);
}

/// Emits `FXRSTOR`.
pub trait FxrstorEmitter<T0> {
    fn fxrstor(&mut self, op0: T0);
}

/// Emits `FXRSTOR64`.
pub trait Fxrstor64Emitter<T0> {
    fn fxrstor64(&mut self, op0: T0);
}

/// Emits `FXSAVE`.
pub trait FxsaveEmitter<T0> {
    fn fxsave(&mut self, op0: T0);
}

/// Emits `FXSAVE64`.
pub trait Fxsave64Emitter<T0> {
    fn fxsave64(&mut self, op0: T0);
}

/// Emits `XGETBV`.
pub trait XgetbvEmitter<T0, T1, T2> {
    fn xgetbv(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `XRSTOR`.
pub trait XrstorEmitter<T0, T1, T2> {
    fn xrstor(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `XRSTOR64`.
pub trait Xrstor64Emitter<T0, T1, T2> {
    fn xrstor64(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `XRSTORS`.
pub trait XrstorsEmitter<T0, T1, T2> {
    fn xrstors(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `XRSTORS64`.
pub trait Xrstors64Emitter<T0, T1, T2> {
    fn xrstors64(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `XSAVE`.
pub trait XsaveEmitter<T0, T1, T2> {
    fn xsave(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `XSAVE64`.
pub trait Xsave64Emitter<T0, T1, T2> {
    fn xsave64(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `XSAVEC`.
pub trait XsavecEmitter<T0, T1, T2> {
    fn xsavec(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `XSAVEC64`.
pub trait Xsavec64Emitter<T0, T1, T2> {
    fn xsavec64(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `XSAVEOPT`.
pub trait XsaveoptEmitter<T0, T1, T2> {
    fn xsaveopt(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `XSAVEOPT64`.
pub trait Xsaveopt64Emitter<T0, T1, T2> {
    fn xsaveopt64(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `XSAVES`.
pub trait XsavesEmitter<T0, T1, T2> {
    fn xsaves(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `XSAVES64`.
pub trait Xsaves64Emitter<T0, T1, T2> {
    fn xsaves64(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `BNDCL`.
pub trait BndclEmitter<T0, T1> {
    fn bndcl(&mut self, op0: T0, op1: T1);
}

/// Emits `BNDCN`.
pub trait BndcnEmitter<T0, T1> {
    fn bndcn(&mut self, op0: T0, op1: T1);
}

/// Emits `BNDCU`.
pub trait BndcuEmitter<T0, T1> {
    fn bndcu(&mut self, op0: T0, op1: T1);
}

/// Emits `BNDLDX`.
pub trait BndldxEmitter<T0, T1> {
    fn bndldx(&mut self, op0: T0, op1: T1);
}

/// Emits `BNDMK`.
pub trait BndmkEmitter<T0, T1> {
    fn bndmk(&mut self, op0: T0, op1: T1);
}

/// Emits `BNDMOV`.
pub trait BndmovEmitter<T0, T1> {
    fn bndmov(&mut self, op0: T0, op1: T1);
}

/// Emits `BNDSTX`.
pub trait BndstxEmitter<T0, T1> {
    fn bndstx(&mut self, op0: T0, op1: T1);
}

/// Emits `MONITORX`.
pub trait MonitorxEmitter<T0, T1, T2> {
    fn monitorx(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `MWAITX`.
pub trait MwaitxEmitter<T0, T1, T2> {
    fn mwaitx(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `MCOMMIT`.
pub trait McommitEmitter {
    fn mcommit(&mut self);
}

/// Emits `PTWRITE`.
pub trait PtwriteEmitter<T0> {
    fn ptwrite(&mut self, op0: T0);
}

/// Emits `ENQCMD`.
pub trait EnqcmdEmitter<T0, T1> {
    fn enqcmd(&mut self, op0: T0, op1: T1);
}

/// Emits `ENQCMDS`.
pub trait EnqcmdsEmitter<T0, T1> {
    fn enqcmds(&mut self, op0: T0, op1: T1);
}

/// Emits `TPAUSE`.
pub trait TpauseEmitter<T0, T1, T2> {
    fn tpause(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `UMONITOR`.
pub trait UmonitorEmitter<T0> {
    fn umonitor(&mut self, op0: T0);
}

/// Emits `UMWAIT`.
pub trait UmwaitEmitter<T0, T1, T2> {
    fn umwait(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `RDRAND`.
pub trait RdrandEmitter<T0> {
    fn rdrand(&mut self, op0: T0);
}

/// Emits `RDSEED`.
pub trait RdseedEmitter<T0> {
    fn rdseed(&mut self, op0: T0);
}

/// Emits `LLWPCB`.
pub trait LlwpcbEmitter<T0> {
    fn llwpcb(&mut self, op0: T0);
}

/// Emits `LWPINS`.
pub trait LwpinsEmitter<T0, T1, T2> {
    fn lwpins(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `LWPVAL`.
pub trait LwpvalEmitter<T0, T1, T2> {
    fn lwpval(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `SLWPCB`.
pub trait SlwpcbEmitter<T0> {
    fn slwpcb(&mut self, op0: T0);
}

/// Emits `XABORT`.
pub trait XabortEmitter<T0> {
    fn xabort(&mut self, op0: T0);
}

/// Emits `XBEGIN`.
pub trait XbeginEmitter<T0> {
    fn xbegin(&mut self, op0: T0);
}

/// Emits `XEND`.
pub trait XendEmitter {
    fn xend(&mut self);
}

/// Emits `XTEST`.
pub trait XtestEmitter {
    fn xtest(&mut self);
}

/// Emits `XRESLDTRK`.
pub trait XresldtrkEmitter {
    fn xresldtrk(&mut self);
}

/// Emits `XSUSLDTRK`.
pub trait XsusldtrkEmitter {
    fn xsusldtrk(&mut self);
}

/// Emits `ENDBR32`.
pub trait Endbr32Emitter {
    fn endbr32(&mut self);
}

/// Emits `ENDBR64`.
pub trait Endbr64Emitter {
    fn endbr64(&mut self);
}

/// Emits `CLRSSBSY`.
pub trait ClrssbsyEmitter<T0> {
    fn clrssbsy(&mut self, op0: T0);
}

/// Emits `SETSSBSY`.
pub trait SetssbsyEmitter {
    fn setssbsy(&mut self);
}

/// Emits `RSTORSSP`.
pub trait RstorsspEmitter<T0> {
    fn rstorssp(&mut self, op0: T0);
}

/// Emits `SAVEPREVSSP`.
pub trait SaveprevsspEmitter {
    fn saveprevssp(&mut self);
}

/// Emits `INCSSPD`.
pub trait IncsspdEmitter<T0> {
    fn incsspd(&mut self, op0: T0);
}

/// Emits `INCSSPQ`.
pub trait IncsspqEmitter<T0> {
    fn incsspq(&mut self, op0: T0);
}

/// Emits `RDSSPD`.
pub trait RdsspdEmitter<T0> {
    fn rdsspd(&mut self, op0: T0);
}

/// Emits `RDSSPQ`.
pub trait RdsspqEmitter<T0> {
    fn rdsspq(&mut self, op0: T0);
}

/// Emits `WRSSD`.
pub trait WrssdEmitter<T0, T1> {
    fn wrssd(&mut self, op0: T0, op1: T1);
}

/// Emits `WRSSQ`.
pub trait WrssqEmitter<T0, T1> {
    fn wrssq(&mut self, op0: T0, op1: T1);
}

/// Emits `WRUSSD`.
pub trait WrussdEmitter<T0, T1> {
    fn wrussd(&mut self, op0: T0, op1: T1);
}

/// Emits `WRUSSQ`.
pub trait WrussqEmitter<T0, T1> {
    fn wrussq(&mut self, op0: T0, op1: T1);
}

/// Emits `HRESET`.
pub trait HresetEmitter<T0, T1> {
    fn hreset(&mut self, op0: T0, op1: T1);
}

/// Emits `CLUI`.
pub trait CluiEmitter {
    fn clui(&mut self);
}

/// Emits `SENDUIPI`.
pub trait SenduipiEmitter<T0> {
    fn senduipi(&mut self, op0: T0);
}

/// Emits `TESTUI`.
pub trait TestuiEmitter {
    fn testui(&mut self);
}

/// Emits `STUI`.
pub trait StuiEmitter {
    fn stui(&mut self);
}

/// Emits `UIRET`.
pub trait UiretEmitter {
    fn uiret(&mut self);
}

/// Emits `CLTS`.
pub trait CltsEmitter {
    fn clts(&mut self);
}

/// Emits `HLT`.
pub trait HltEmitter {
    fn hlt(&mut self);
}

/// Emits `INVD`.
pub trait InvdEmitter {
    fn invd(&mut self);
}

/// Emits `INVLPG`.
pub trait InvlpgEmitter<T0> {
    fn invlpg(&mut self, op0: T0);
}

/// Emits `INVPCID`.
pub trait InvpcidEmitter<T0, T1> {
    fn invpcid(&mut self, op0: T0, op1: T1);
}

/// Emits `LGDT`.
pub trait LgdtEmitter<T0> {
    fn lgdt(&mut self, op0: T0);
}

/// Emits `LIDT`.
pub trait LidtEmitter<T0> {
    fn lidt(&mut self, op0: T0);
}

/// Emits `LLDT`.
pub trait LldtEmitter<T0> {
    fn lldt(&mut self, op0: T0);
}

/// Emits `LMSW`.
pub trait LmswEmitter<T0> {
    fn lmsw(&mut self, op0: T0);
}

/// Emits `LTR`.
pub trait LtrEmitter<T0> {
    fn ltr(&mut self, op0: T0);
}

/// Emits `RDMSR`.
pub trait RdmsrEmitter<T0, T1, T2> {
    fn rdmsr(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `RDPMC`.
pub trait RdpmcEmitter<T0, T1, T2> {
    fn rdpmc(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `SWAPGS`.
pub trait SwapgsEmitter {
    fn swapgs(&mut self);
}

/// Emits `WBINVD`.
pub trait WbinvdEmitter {
    fn wbinvd(&mut self);
}

/// Emits `WBNOINVD`.
pub trait WbnoinvdEmitter {
    fn wbnoinvd(&mut self);
}

/// Emits `WRMSR`.
pub trait WrmsrEmitter<T0, T1, T2> {
    fn wrmsr(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `XSETBV`.
pub trait XsetbvEmitter<T0, T1, T2> {
    fn xsetbv(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `INVLPGB`.
pub trait InvlpgbEmitter<T0, T1, T2> {
    fn invlpgb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `TLBSYNC`.
pub trait TlbsyncEmitter {
    fn tlbsync(&mut self);
}

/// Emits `MONITOR`.
pub trait MonitorEmitter<T0, T1, T2> {
    fn monitor(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `MWAIT`.
pub trait MwaitEmitter<T0, T1> {
    fn mwait(&mut self, op0: T0, op1: T1);
}

/// Emits `CLAC`.
pub trait ClacEmitter {
    fn clac(&mut self);
}

/// Emits `STAC`.
pub trait StacEmitter {
    fn stac(&mut self);
}

/// Emits `SKINIT`.
pub trait SkinitEmitter<T0> {
    fn skinit(&mut self, op0: T0);
}

/// Emits `STGI`.
pub trait StgiEmitter {
    fn stgi(&mut self);
}

/// Emits `PSMASH`.
pub trait PsmashEmitter {
    fn psmash(&mut self);
}

/// Emits `PVALIDATE`.
pub trait PvalidateEmitter {
    fn pvalidate(&mut self);
}

/// Emits `RMPADJUST`.
pub trait RmpadjustEmitter {
    fn rmpadjust(&mut self);
}

/// Emits `RMPUPDATE`.
pub trait RmpupdateEmitter {
    fn rmpupdate(&mut self);
}

/// Emits `INVEPT`.
pub trait InveptEmitter<T0, T1> {
    fn invept(&mut self, op0: T0, op1: T1);
}

/// Emits `INVVPID`.
pub trait InvvpidEmitter<T0, T1> {
    fn invvpid(&mut self, op0: T0, op1: T1);
}

/// Emits `VMCALL`.
pub trait VmcallEmitter {
    fn vmcall(&mut self);
}

/// Emits `VMCLEAR`.
pub trait VmclearEmitter<T0> {
    fn vmclear(&mut self, op0: T0);
}

/// Emits `VMFUNC`.
pub trait VmfuncEmitter {
    fn vmfunc(&mut self);
}

/// Emits `VMLAUNCH`.
pub trait VmlaunchEmitter {
    fn vmlaunch(&mut self);
}

/// Emits `VMPTRLD`.
pub trait VmptrldEmitter<T0> {
    fn vmptrld(&mut self, op0: T0);
}

/// Emits `VMPTRST`.
pub trait VmptrstEmitter<T0> {
    fn vmptrst(&mut self, op0: T0);
}

/// Emits `VMREAD`.
pub trait VmreadEmitter<T0, T1> {
    fn vmread(&mut self, op0: T0, op1: T1);
}

/// Emits `VMRESUME`.
pub trait VmresumeEmitter {
    fn vmresume(&mut self);
}

/// Emits `VMWRITE`.
pub trait VmwriteEmitter<T0, T1> {
    fn vmwrite(&mut self, op0: T0, op1: T1);
}

/// Emits `VMXOFF`.
pub trait VmxoffEmitter {
    fn vmxoff(&mut self);
}

/// Emits `VMXON`.
pub trait VmxonEmitter<T0> {
    fn vmxon(&mut self, op0: T0);
}

/// Emits `CLGI`.
pub trait ClgiEmitter {
    fn clgi(&mut self);
}

/// Emits `INVLPGA`.
pub trait InvlpgaEmitter<T0, T1> {
    fn invlpga(&mut self, op0: T0, op1: T1);
}

/// Emits `VMLOAD`.
pub trait VmloadEmitter<T0> {
    fn vmload(&mut self, op0: T0);
}

/// Emits `VMMCALL`.
pub trait VmmcallEmitter {
    fn vmmcall(&mut self);
}

/// Emits `VMRUN`.
pub trait VmrunEmitter<T0> {
    fn vmrun(&mut self, op0: T0);
}

/// Emits `VMSAVE`.
pub trait VmsaveEmitter<T0> {
    fn vmsave(&mut self, op0: T0);
}

/// Emits `VMGEXIT`.
pub trait VmgexitEmitter {
    fn vmgexit(&mut self);
}

/// Emits `F2XM1`.
pub trait F2xm1Emitter {
    fn f2xm1(&mut self);
}

/// Emits `FABS`.
pub trait FabsEmitter {
    fn fabs(&mut self);
}

/// Emits `FADD`.
pub trait FaddEmitter<T0, T1> {
    fn fadd(&mut self, op0: T0, op1: T1);
}

/// Emits `FADD_1`.
pub trait Fadd1Emitter<T0> {
    fn fadd_1(&mut self, op0: T0);
}

/// Emits `FADDP`.
pub trait FaddpEmitter<T0> {
    fn faddp(&mut self, op0: T0);
}

/// Emits `FADDP_0`.
pub trait Faddp0Emitter {
    fn faddp_0(&mut self);
}

/// Emits `FBLD`.
pub trait FbldEmitter<T0> {
    fn fbld(&mut self, op0: T0);
}

/// Emits `FBSTP`.
pub trait FbstpEmitter<T0> {
    fn fbstp(&mut self, op0: T0);
}

/// Emits `FCHS`.
pub trait FchsEmitter {
    fn fchs(&mut self);
}

/// Emits `FCLEX`.
pub trait FclexEmitter {
    fn fclex(&mut self);
}

/// Emits `FCMOVB`.
pub trait FcmovbEmitter<T0> {
    fn fcmovb(&mut self, op0: T0);
}

/// Emits `FCMOVBE`.
pub trait FcmovbeEmitter<T0> {
    fn fcmovbe(&mut self, op0: T0);
}

/// Emits `FCMOVE`.
pub trait FcmoveEmitter<T0> {
    fn fcmove(&mut self, op0: T0);
}

/// Emits `FCMOVNB`.
pub trait FcmovnbEmitter<T0> {
    fn fcmovnb(&mut self, op0: T0);
}

/// Emits `FCMOVNBE`.
pub trait FcmovnbeEmitter<T0> {
    fn fcmovnbe(&mut self, op0: T0);
}

/// Emits `FCMOVNE`.
pub trait FcmovneEmitter<T0> {
    fn fcmovne(&mut self, op0: T0);
}

/// Emits `FCMOVNU`.
pub trait FcmovnuEmitter<T0> {
    fn fcmovnu(&mut self, op0: T0);
}

/// Emits `FCMOVU`.
pub trait FcmovuEmitter<T0> {
    fn fcmovu(&mut self, op0: T0);
}

/// Emits `FCOM`.
pub trait FcomEmitter<T0> {
    fn fcom(&mut self, op0: T0);
}

/// Emits `FCOM_0`.
pub trait Fcom0Emitter {
    fn fcom_0(&mut self);
}

/// Emits `FCOMP`.
pub trait FcompEmitter<T0> {
    fn fcomp(&mut self, op0: T0);
}

/// Emits `FCOMP_0`.
pub trait Fcomp0Emitter {
    fn fcomp_0(&mut self);
}

/// Emits `FCOMPP`.
pub trait FcomppEmitter {
    fn fcompp(&mut self);
}

/// Emits `FCOMI`.
pub trait FcomiEmitter<T0> {
    fn fcomi(&mut self, op0: T0);
}

/// Emits `FCOMIP`.
pub trait FcomipEmitter<T0> {
    fn fcomip(&mut self, op0: T0);
}

/// Emits `FCOS`.
pub trait FcosEmitter {
    fn fcos(&mut self);
}

/// Emits `FDECSTP`.
pub trait FdecstpEmitter {
    fn fdecstp(&mut self);
}

/// Emits `FDIV`.
pub trait FdivEmitter<T0, T1> {
    fn fdiv(&mut self, op0: T0, op1: T1);
}

/// Emits `FDIV_1`.
pub trait Fdiv1Emitter<T0> {
    fn fdiv_1(&mut self, op0: T0);
}

/// Emits `FDIVP`.
pub trait FdivpEmitter<T0> {
    fn fdivp(&mut self, op0: T0);
}

/// Emits `FDIVP_0`.
pub trait Fdivp0Emitter {
    fn fdivp_0(&mut self);
}

/// Emits `FDIVR`.
pub trait FdivrEmitter<T0, T1> {
    fn fdivr(&mut self, op0: T0, op1: T1);
}

/// Emits `FDIVR_1`.
pub trait Fdivr1Emitter<T0> {
    fn fdivr_1(&mut self, op0: T0);
}

/// Emits `FDIVRP`.
pub trait FdivrpEmitter<T0> {
    fn fdivrp(&mut self, op0: T0);
}

/// Emits `FDIVRP_0`.
pub trait Fdivrp0Emitter {
    fn fdivrp_0(&mut self);
}

/// Emits `FFREE`.
pub trait FfreeEmitter<T0> {
    fn ffree(&mut self, op0: T0);
}

/// Emits `FIADD`.
pub trait FiaddEmitter<T0> {
    fn fiadd(&mut self, op0: T0);
}

/// Emits `FICOM`.
pub trait FicomEmitter<T0> {
    fn ficom(&mut self, op0: T0);
}

/// Emits `FICOMP`.
pub trait FicompEmitter<T0> {
    fn ficomp(&mut self, op0: T0);
}

/// Emits `FIDIV`.
pub trait FidivEmitter<T0> {
    fn fidiv(&mut self, op0: T0);
}

/// Emits `FIDIVR`.
pub trait FidivrEmitter<T0> {
    fn fidivr(&mut self, op0: T0);
}

/// Emits `FILD`.
pub trait FildEmitter<T0> {
    fn fild(&mut self, op0: T0);
}

/// Emits `FIMUL`.
pub trait FimulEmitter<T0> {
    fn fimul(&mut self, op0: T0);
}

/// Emits `FINCSTP`.
pub trait FincstpEmitter {
    fn fincstp(&mut self);
}

/// Emits `FINIT`.
pub trait FinitEmitter {
    fn finit(&mut self);
}

/// Emits `FISUB`.
pub trait FisubEmitter<T0> {
    fn fisub(&mut self, op0: T0);
}

/// Emits `FISUBR`.
pub trait FisubrEmitter<T0> {
    fn fisubr(&mut self, op0: T0);
}

/// Emits `FNINIT`.
pub trait FninitEmitter {
    fn fninit(&mut self);
}

/// Emits `FIST`.
pub trait FistEmitter<T0> {
    fn fist(&mut self, op0: T0);
}

/// Emits `FISTP`.
pub trait FistpEmitter<T0> {
    fn fistp(&mut self, op0: T0);
}

/// Emits `FISTTP`.
pub trait FisttpEmitter<T0> {
    fn fisttp(&mut self, op0: T0);
}

/// Emits `FLD`.
pub trait FldEmitter<T0> {
    fn fld(&mut self, op0: T0);
}

/// Emits `FLD1`.
pub trait Fld1Emitter {
    fn fld1(&mut self);
}

/// Emits `FLDL2T`.
pub trait Fldl2tEmitter {
    fn fldl2t(&mut self);
}

/// Emits `FLDL2E`.
pub trait Fldl2eEmitter {
    fn fldl2e(&mut self);
}

/// Emits `FLDPI`.
pub trait FldpiEmitter {
    fn fldpi(&mut self);
}

/// Emits `FLDLG2`.
pub trait Fldlg2Emitter {
    fn fldlg2(&mut self);
}

/// Emits `FLDLN2`.
pub trait Fldln2Emitter {
    fn fldln2(&mut self);
}

/// Emits `FLDZ`.
pub trait FldzEmitter {
    fn fldz(&mut self);
}

/// Emits `FLDCW`.
pub trait FldcwEmitter<T0> {
    fn fldcw(&mut self, op0: T0);
}

/// Emits `FLDENV`.
pub trait FldenvEmitter<T0> {
    fn fldenv(&mut self, op0: T0);
}

/// Emits `FMUL`.
pub trait FmulEmitter<T0, T1> {
    fn fmul(&mut self, op0: T0, op1: T1);
}

/// Emits `FMUL_1`.
pub trait Fmul1Emitter<T0> {
    fn fmul_1(&mut self, op0: T0);
}

/// Emits `FMULP`.
pub trait FmulpEmitter<T0> {
    fn fmulp(&mut self, op0: T0);
}

/// Emits `FMULP_0`.
pub trait Fmulp0Emitter {
    fn fmulp_0(&mut self);
}

/// Emits `FNCLEX`.
pub trait FnclexEmitter {
    fn fnclex(&mut self);
}

/// Emits `FNOP`.
pub trait FnopEmitter {
    fn fnop(&mut self);
}

/// Emits `FNSAVE`.
pub trait FnsaveEmitter<T0> {
    fn fnsave(&mut self, op0: T0);
}

/// Emits `FNSTENV`.
pub trait FnstenvEmitter<T0> {
    fn fnstenv(&mut self, op0: T0);
}

/// Emits `FNSTCW`.
pub trait FnstcwEmitter<T0> {
    fn fnstcw(&mut self, op0: T0);
}

/// Emits `FPATAN`.
pub trait FpatanEmitter {
    fn fpatan(&mut self);
}

/// Emits `FPREM`.
pub trait FpremEmitter {
    fn fprem(&mut self);
}

/// Emits `FPREM1`.
pub trait Fprem1Emitter {
    fn fprem1(&mut self);
}

/// Emits `FPTAN`.
pub trait FptanEmitter {
    fn fptan(&mut self);
}

/// Emits `FRNDINT`.
pub trait FrndintEmitter {
    fn frndint(&mut self);
}

/// Emits `FRSTOR`.
pub trait FrstorEmitter<T0> {
    fn frstor(&mut self, op0: T0);
}

/// Emits `FSAVE`.
pub trait FsaveEmitter<T0> {
    fn fsave(&mut self, op0: T0);
}

/// Emits `FSCALE`.
pub trait FscaleEmitter {
    fn fscale(&mut self);
}

/// Emits `FSIN`.
pub trait FsinEmitter {
    fn fsin(&mut self);
}

/// Emits `FSINCOS`.
pub trait FsincosEmitter {
    fn fsincos(&mut self);
}

/// Emits `FSQRT`.
pub trait FsqrtEmitter {
    fn fsqrt(&mut self);
}

/// Emits `FST`.
pub trait FstEmitter<T0> {
    fn fst(&mut self, op0: T0);
}

/// Emits `FSTP`.
pub trait FstpEmitter<T0> {
    fn fstp(&mut self, op0: T0);
}

/// Emits `FSTCW`.
pub trait FstcwEmitter<T0> {
    fn fstcw(&mut self, op0: T0);
}

/// Emits `FSTENV`.
pub trait FstenvEmitter<T0> {
    fn fstenv(&mut self, op0: T0);
}

/// Emits `FSUB`.
pub trait FsubEmitter<T0, T1> {
    fn fsub(&mut self, op0: T0, op1: T1);
}

/// Emits `FSUB_1`.
pub trait Fsub1Emitter<T0> {
    fn fsub_1(&mut self, op0: T0);
}

/// Emits `FSUBP`.
pub trait FsubpEmitter<T0> {
    fn fsubp(&mut self, op0: T0);
}

/// Emits `FSUBP_0`.
pub trait Fsubp0Emitter {
    fn fsubp_0(&mut self);
}

/// Emits `FSUBR`.
pub trait FsubrEmitter<T0, T1> {
    fn fsubr(&mut self, op0: T0, op1: T1);
}

/// Emits `FSUBR_1`.
pub trait Fsubr1Emitter<T0> {
    fn fsubr_1(&mut self, op0: T0);
}

/// Emits `FSUBRP`.
pub trait FsubrpEmitter<T0> {
    fn fsubrp(&mut self, op0: T0);
}

/// Emits `FSUBRP_0`.
pub trait Fsubrp0Emitter {
    fn fsubrp_0(&mut self);
}

/// Emits `FTST`.
pub trait FtstEmitter {
    fn ftst(&mut self);
}

/// Emits `FUCOM`.
pub trait FucomEmitter<T0> {
    fn fucom(&mut self, op0: T0);
}

/// Emits `FUCOM_0`.
pub trait Fucom0Emitter {
    fn fucom_0(&mut self);
}

/// Emits `FUCOMI`.
pub trait FucomiEmitter<T0> {
    fn fucomi(&mut self, op0: T0);
}

/// Emits `FUCOMIP`.
pub trait FucomipEmitter<T0> {
    fn fucomip(&mut self, op0: T0);
}

/// Emits `FUCOMP`.
pub trait FucompEmitter<T0> {
    fn fucomp(&mut self, op0: T0);
}

/// Emits `FUCOMP_0`.
pub trait Fucomp0Emitter {
    fn fucomp_0(&mut self);
}

/// Emits `FUCOMPP`.
pub trait FucomppEmitter {
    fn fucompp(&mut self);
}

/// Emits `FWAIT`.
pub trait FwaitEmitter {
    fn fwait(&mut self);
}

/// Emits `FXAM`.
pub trait FxamEmitter {
    fn fxam(&mut self);
}

/// Emits `FXCH`.
pub trait FxchEmitter<T0> {
    fn fxch(&mut self, op0: T0);
}

/// Emits `FXTRACT`.
pub trait FxtractEmitter {
    fn fxtract(&mut self);
}

/// Emits `FYL2X`.
pub trait Fyl2xEmitter {
    fn fyl2x(&mut self);
}

/// Emits `FYL2XP1`.
pub trait Fyl2xp1Emitter {
    fn fyl2xp1(&mut self);
}

/// Emits `FSTSW`.
pub trait FstswEmitter<T0> {
    fn fstsw(&mut self, op0: T0);
}

/// Emits `FNSTSW`.
pub trait FnstswEmitter<T0> {
    fn fnstsw(&mut self, op0: T0);
}

/// Emits `ADDPD`.
pub trait AddpdEmitter<T0, T1> {
    fn addpd(&mut self, op0: T0, op1: T1);
}

/// Emits `ADDPS`.
pub trait AddpsEmitter<T0, T1> {
    fn addps(&mut self, op0: T0, op1: T1);
}

/// Emits `ADDSD`.
pub trait AddsdEmitter<T0, T1> {
    fn addsd(&mut self, op0: T0, op1: T1);
}

/// Emits `ADDSS`.
pub trait AddssEmitter<T0, T1> {
    fn addss(&mut self, op0: T0, op1: T1);
}

/// Emits `ADDSUBPD`.
pub trait AddsubpdEmitter<T0, T1> {
    fn addsubpd(&mut self, op0: T0, op1: T1);
}

/// Emits `ADDSUBPS`.
pub trait AddsubpsEmitter<T0, T1> {
    fn addsubps(&mut self, op0: T0, op1: T1);
}

/// Emits `ANDNPD`.
pub trait AndnpdEmitter<T0, T1> {
    fn andnpd(&mut self, op0: T0, op1: T1);
}

/// Emits `ANDNPS`.
pub trait AndnpsEmitter<T0, T1> {
    fn andnps(&mut self, op0: T0, op1: T1);
}

/// Emits `ANDPD`.
pub trait AndpdEmitter<T0, T1> {
    fn andpd(&mut self, op0: T0, op1: T1);
}

/// Emits `ANDPS`.
pub trait AndpsEmitter<T0, T1> {
    fn andps(&mut self, op0: T0, op1: T1);
}

/// Emits `BLENDPD`.
pub trait BlendpdEmitter<T0, T1, T2> {
    fn blendpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `BLENDPS`.
pub trait BlendpsEmitter<T0, T1, T2> {
    fn blendps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `BLENDVPD`.
pub trait BlendvpdEmitter<T0, T1, T2> {
    fn blendvpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `BLENDVPS`.
pub trait BlendvpsEmitter<T0, T1, T2> {
    fn blendvps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CMPPD`.
pub trait CmppdEmitter<T0, T1, T2> {
    fn cmppd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CMPPS`.
pub trait CmppsEmitter<T0, T1, T2> {
    fn cmpps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CMPSD`.
pub trait CmpsdEmitter<T0, T1, T2> {
    fn cmpsd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CMPSS`.
pub trait CmpssEmitter<T0, T1, T2> {
    fn cmpss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `COMISD`.
pub trait ComisdEmitter<T0, T1> {
    fn comisd(&mut self, op0: T0, op1: T1);
}

/// Emits `COMISS`.
pub trait ComissEmitter<T0, T1> {
    fn comiss(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTDQ2PD`.
pub trait Cvtdq2pdEmitter<T0, T1> {
    fn cvtdq2pd(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTDQ2PS`.
pub trait Cvtdq2psEmitter<T0, T1> {
    fn cvtdq2ps(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTPD2DQ`.
pub trait Cvtpd2dqEmitter<T0, T1> {
    fn cvtpd2dq(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTPD2PI`.
pub trait Cvtpd2piEmitter<T0, T1> {
    fn cvtpd2pi(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTPD2PS`.
pub trait Cvtpd2psEmitter<T0, T1> {
    fn cvtpd2ps(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTPI2PD`.
pub trait Cvtpi2pdEmitter<T0, T1> {
    fn cvtpi2pd(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTPI2PS`.
pub trait Cvtpi2psEmitter<T0, T1> {
    fn cvtpi2ps(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTPS2DQ`.
pub trait Cvtps2dqEmitter<T0, T1> {
    fn cvtps2dq(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTPS2PD`.
pub trait Cvtps2pdEmitter<T0, T1> {
    fn cvtps2pd(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTPS2PI`.
pub trait Cvtps2piEmitter<T0, T1> {
    fn cvtps2pi(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTSD2SI`.
pub trait Cvtsd2siEmitter<T0, T1> {
    fn cvtsd2si(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTSD2SS`.
pub trait Cvtsd2ssEmitter<T0, T1> {
    fn cvtsd2ss(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTSI2SD`.
pub trait Cvtsi2sdEmitter<T0, T1> {
    fn cvtsi2sd(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTSI2SS`.
pub trait Cvtsi2ssEmitter<T0, T1> {
    fn cvtsi2ss(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTSS2SD`.
pub trait Cvtss2sdEmitter<T0, T1> {
    fn cvtss2sd(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTSS2SI`.
pub trait Cvtss2siEmitter<T0, T1> {
    fn cvtss2si(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTTPD2PI`.
pub trait Cvttpd2piEmitter<T0, T1> {
    fn cvttpd2pi(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTTPD2DQ`.
pub trait Cvttpd2dqEmitter<T0, T1> {
    fn cvttpd2dq(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTTPS2DQ`.
pub trait Cvttps2dqEmitter<T0, T1> {
    fn cvttps2dq(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTTPS2PI`.
pub trait Cvttps2piEmitter<T0, T1> {
    fn cvttps2pi(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTTSD2SI`.
pub trait Cvttsd2siEmitter<T0, T1> {
    fn cvttsd2si(&mut self, op0: T0, op1: T1);
}

/// Emits `CVTTSS2SI`.
pub trait Cvttss2siEmitter<T0, T1> {
    fn cvttss2si(&mut self, op0: T0, op1: T1);
}

/// Emits `DIVPD`.
pub trait DivpdEmitter<T0, T1> {
    fn divpd(&mut self, op0: T0, op1: T1);
}

/// Emits `DIVPS`.
pub trait DivpsEmitter<T0, T1> {
    fn divps(&mut self, op0: T0, op1: T1);
}

/// Emits `DIVSD`.
pub trait DivsdEmitter<T0, T1> {
    fn divsd(&mut self, op0: T0, op1: T1);
}

/// Emits `DIVSS`.
pub trait DivssEmitter<T0, T1> {
    fn divss(&mut self, op0: T0, op1: T1);
}

/// Emits `DPPD`.
pub trait DppdEmitter<T0, T1, T2> {
    fn dppd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `DPPS`.
pub trait DppsEmitter<T0, T1, T2> {
    fn dpps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `EXTRACTPS`.
pub trait ExtractpsEmitter<T0, T1, T2> {
    fn extractps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `EXTRQ`.
pub trait ExtrqEmitter<T0, T1> {
    fn extrq(&mut self, op0: T0, op1: T1);
}

/// Emits `EXTRQ_3`.
pub trait Extrq3Emitter<T0, T1, T2> {
    fn extrq_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `HADDPD`.
pub trait HaddpdEmitter<T0, T1> {
    fn haddpd(&mut self, op0: T0, op1: T1);
}

/// Emits `HADDPS`.
pub trait HaddpsEmitter<T0, T1> {
    fn haddps(&mut self, op0: T0, op1: T1);
}

/// Emits `HSUBPD`.
pub trait HsubpdEmitter<T0, T1> {
    fn hsubpd(&mut self, op0: T0, op1: T1);
}

/// Emits `HSUBPS`.
pub trait HsubpsEmitter<T0, T1> {
    fn hsubps(&mut self, op0: T0, op1: T1);
}

/// Emits `INSERTPS`.
pub trait InsertpsEmitter<T0, T1, T2> {
    fn insertps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `INSERTQ`.
pub trait InsertqEmitter<T0, T1> {
    fn insertq(&mut self, op0: T0, op1: T1);
}

/// Emits `INSERTQ_4`.
pub trait Insertq4Emitter<T0, T1, T2, T3> {
    fn insertq_4(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `LDDQU`.
pub trait LddquEmitter<T0, T1> {
    fn lddqu(&mut self, op0: T0, op1: T1);
}

/// Emits `MASKMOVQ`.
pub trait MaskmovqEmitter<T0, T1, T2> {
    fn maskmovq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `MASKMOVDQU`.
pub trait MaskmovdquEmitter<T0, T1, T2> {
    fn maskmovdqu(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `MAXPD`.
pub trait MaxpdEmitter<T0, T1> {
    fn maxpd(&mut self, op0: T0, op1: T1);
}

/// Emits `MAXPS`.
pub trait MaxpsEmitter<T0, T1> {
    fn maxps(&mut self, op0: T0, op1: T1);
}

/// Emits `MAXSD`.
pub trait MaxsdEmitter<T0, T1> {
    fn maxsd(&mut self, op0: T0, op1: T1);
}

/// Emits `MAXSS`.
pub trait MaxssEmitter<T0, T1> {
    fn maxss(&mut self, op0: T0, op1: T1);
}

/// Emits `MINPD`.
pub trait MinpdEmitter<T0, T1> {
    fn minpd(&mut self, op0: T0, op1: T1);
}

/// Emits `MINPS`.
pub trait MinpsEmitter<T0, T1> {
    fn minps(&mut self, op0: T0, op1: T1);
}

/// Emits `MINSD`.
pub trait MinsdEmitter<T0, T1> {
    fn minsd(&mut self, op0: T0, op1: T1);
}

/// Emits `MINSS`.
pub trait MinssEmitter<T0, T1> {
    fn minss(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVAPD`.
pub trait MovapdEmitter<T0, T1> {
    fn movapd(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVAPS`.
pub trait MovapsEmitter<T0, T1> {
    fn movaps(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVD`.
pub trait MovdEmitter<T0, T1> {
    fn movd(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVDDUP`.
pub trait MovddupEmitter<T0, T1> {
    fn movddup(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVDQ2Q`.
pub trait Movdq2qEmitter<T0, T1> {
    fn movdq2q(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVDQA`.
pub trait MovdqaEmitter<T0, T1> {
    fn movdqa(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVDQU`.
pub trait MovdquEmitter<T0, T1> {
    fn movdqu(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVHLPS`.
pub trait MovhlpsEmitter<T0, T1> {
    fn movhlps(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVHPD`.
pub trait MovhpdEmitter<T0, T1> {
    fn movhpd(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVHPS`.
pub trait MovhpsEmitter<T0, T1> {
    fn movhps(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVLHPS`.
pub trait MovlhpsEmitter<T0, T1> {
    fn movlhps(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVLPD`.
pub trait MovlpdEmitter<T0, T1> {
    fn movlpd(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVLPS`.
pub trait MovlpsEmitter<T0, T1> {
    fn movlps(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVMSKPS`.
pub trait MovmskpsEmitter<T0, T1> {
    fn movmskps(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVMSKPD`.
pub trait MovmskpdEmitter<T0, T1> {
    fn movmskpd(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVNTDQ`.
pub trait MovntdqEmitter<T0, T1> {
    fn movntdq(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVNTDQA`.
pub trait MovntdqaEmitter<T0, T1> {
    fn movntdqa(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVNTPD`.
pub trait MovntpdEmitter<T0, T1> {
    fn movntpd(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVNTPS`.
pub trait MovntpsEmitter<T0, T1> {
    fn movntps(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVNTSD`.
pub trait MovntsdEmitter<T0, T1> {
    fn movntsd(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVNTSS`.
pub trait MovntssEmitter<T0, T1> {
    fn movntss(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVNTQ`.
pub trait MovntqEmitter<T0, T1> {
    fn movntq(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVQ`.
pub trait MovqEmitter<T0, T1> {
    fn movq(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVQ2DQ`.
pub trait Movq2dqEmitter<T0, T1> {
    fn movq2dq(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVSD`.
pub trait MovsdEmitter<T0, T1> {
    fn movsd(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVSHDUP`.
pub trait MovshdupEmitter<T0, T1> {
    fn movshdup(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVSLDUP`.
pub trait MovsldupEmitter<T0, T1> {
    fn movsldup(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVSS`.
pub trait MovssEmitter<T0, T1> {
    fn movss(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVUPD`.
pub trait MovupdEmitter<T0, T1> {
    fn movupd(&mut self, op0: T0, op1: T1);
}

/// Emits `MOVUPS`.
pub trait MovupsEmitter<T0, T1> {
    fn movups(&mut self, op0: T0, op1: T1);
}

/// Emits `MPSADBW`.
pub trait MpsadbwEmitter<T0, T1, T2> {
    fn mpsadbw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `MULPD`.
pub trait MulpdEmitter<T0, T1> {
    fn mulpd(&mut self, op0: T0, op1: T1);
}

/// Emits `MULPS`.
pub trait MulpsEmitter<T0, T1> {
    fn mulps(&mut self, op0: T0, op1: T1);
}

/// Emits `MULSD`.
pub trait MulsdEmitter<T0, T1> {
    fn mulsd(&mut self, op0: T0, op1: T1);
}

/// Emits `MULSS`.
pub trait MulssEmitter<T0, T1> {
    fn mulss(&mut self, op0: T0, op1: T1);
}

/// Emits `ORPD`.
pub trait OrpdEmitter<T0, T1> {
    fn orpd(&mut self, op0: T0, op1: T1);
}

/// Emits `ORPS`.
pub trait OrpsEmitter<T0, T1> {
    fn orps(&mut self, op0: T0, op1: T1);
}

/// Emits `PACKSSDW`.
pub trait PackssdwEmitter<T0, T1> {
    fn packssdw(&mut self, op0: T0, op1: T1);
}

/// Emits `PACKSSWB`.
pub trait PacksswbEmitter<T0, T1> {
    fn packsswb(&mut self, op0: T0, op1: T1);
}

/// Emits `PACKUSDW`.
pub trait PackusdwEmitter<T0, T1> {
    fn packusdw(&mut self, op0: T0, op1: T1);
}

/// Emits `PACKUSWB`.
pub trait PackuswbEmitter<T0, T1> {
    fn packuswb(&mut self, op0: T0, op1: T1);
}

/// Emits `PABSB`.
pub trait PabsbEmitter<T0, T1> {
    fn pabsb(&mut self, op0: T0, op1: T1);
}

/// Emits `PABSD`.
pub trait PabsdEmitter<T0, T1> {
    fn pabsd(&mut self, op0: T0, op1: T1);
}

/// Emits `PABSW`.
pub trait PabswEmitter<T0, T1> {
    fn pabsw(&mut self, op0: T0, op1: T1);
}

/// Emits `PADDB`.
pub trait PaddbEmitter<T0, T1> {
    fn paddb(&mut self, op0: T0, op1: T1);
}

/// Emits `PADDD`.
pub trait PadddEmitter<T0, T1> {
    fn paddd(&mut self, op0: T0, op1: T1);
}

/// Emits `PADDQ`.
pub trait PaddqEmitter<T0, T1> {
    fn paddq(&mut self, op0: T0, op1: T1);
}

/// Emits `PADDSB`.
pub trait PaddsbEmitter<T0, T1> {
    fn paddsb(&mut self, op0: T0, op1: T1);
}

/// Emits `PADDSW`.
pub trait PaddswEmitter<T0, T1> {
    fn paddsw(&mut self, op0: T0, op1: T1);
}

/// Emits `PADDUSB`.
pub trait PaddusbEmitter<T0, T1> {
    fn paddusb(&mut self, op0: T0, op1: T1);
}

/// Emits `PADDUSW`.
pub trait PadduswEmitter<T0, T1> {
    fn paddusw(&mut self, op0: T0, op1: T1);
}

/// Emits `PADDW`.
pub trait PaddwEmitter<T0, T1> {
    fn paddw(&mut self, op0: T0, op1: T1);
}

/// Emits `PALIGNR`.
pub trait PalignrEmitter<T0, T1, T2> {
    fn palignr(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `PAND`.
pub trait PandEmitter<T0, T1> {
    fn pand(&mut self, op0: T0, op1: T1);
}

/// Emits `PANDN`.
pub trait PandnEmitter<T0, T1> {
    fn pandn(&mut self, op0: T0, op1: T1);
}

/// Emits `PAVGB`.
pub trait PavgbEmitter<T0, T1> {
    fn pavgb(&mut self, op0: T0, op1: T1);
}

/// Emits `PAVGW`.
pub trait PavgwEmitter<T0, T1> {
    fn pavgw(&mut self, op0: T0, op1: T1);
}

/// Emits `PBLENDVB`.
pub trait PblendvbEmitter<T0, T1, T2> {
    fn pblendvb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `PBLENDW`.
pub trait PblendwEmitter<T0, T1, T2> {
    fn pblendw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `PCLMULQDQ`.
pub trait PclmulqdqEmitter<T0, T1, T2> {
    fn pclmulqdq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `PCMPESTRI`.
pub trait PcmpestriEmitter<T0, T1, T2, T3, T4, T5> {
    fn pcmpestri(&mut self, op0: T0, op1: T1, op2: T2, op3: T3, op4: T4, op5: T5);
}

/// Emits `PCMPESTRM`.
pub trait PcmpestrmEmitter<T0, T1, T2, T3, T4, T5> {
    fn pcmpestrm(&mut self, op0: T0, op1: T1, op2: T2, op3: T3, op4: T4, op5: T5);
}

/// Emits `PCMPEQB`.
pub trait PcmpeqbEmitter<T0, T1> {
    fn pcmpeqb(&mut self, op0: T0, op1: T1);
}

/// Emits `PCMPEQD`.
pub trait PcmpeqdEmitter<T0, T1> {
    fn pcmpeqd(&mut self, op0: T0, op1: T1);
}

/// Emits `PCMPEQQ`.
pub trait PcmpeqqEmitter<T0, T1> {
    fn pcmpeqq(&mut self, op0: T0, op1: T1);
}

/// Emits `PCMPEQW`.
pub trait PcmpeqwEmitter<T0, T1> {
    fn pcmpeqw(&mut self, op0: T0, op1: T1);
}

/// Emits `PCMPGTB`.
pub trait PcmpgtbEmitter<T0, T1> {
    fn pcmpgtb(&mut self, op0: T0, op1: T1);
}

/// Emits `PCMPGTD`.
pub trait PcmpgtdEmitter<T0, T1> {
    fn pcmpgtd(&mut self, op0: T0, op1: T1);
}

/// Emits `PCMPGTQ`.
pub trait PcmpgtqEmitter<T0, T1> {
    fn pcmpgtq(&mut self, op0: T0, op1: T1);
}

/// Emits `PCMPGTW`.
pub trait PcmpgtwEmitter<T0, T1> {
    fn pcmpgtw(&mut self, op0: T0, op1: T1);
}

/// Emits `PCMPISTRI`.
pub trait PcmpistriEmitter<T0, T1, T2, T3> {
    fn pcmpistri(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `PCMPISTRM`.
pub trait PcmpistrmEmitter<T0, T1, T2, T3> {
    fn pcmpistrm(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `PEXTRB`.
pub trait PextrbEmitter<T0, T1, T2> {
    fn pextrb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `PEXTRD`.
pub trait PextrdEmitter<T0, T1, T2> {
    fn pextrd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `PEXTRQ`.
pub trait PextrqEmitter<T0, T1, T2> {
    fn pextrq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `PEXTRW`.
pub trait PextrwEmitter<T0, T1, T2> {
    fn pextrw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `PHADDD`.
pub trait PhadddEmitter<T0, T1> {
    fn phaddd(&mut self, op0: T0, op1: T1);
}

/// Emits `PHADDSW`.
pub trait PhaddswEmitter<T0, T1> {
    fn phaddsw(&mut self, op0: T0, op1: T1);
}

/// Emits `PHADDW`.
pub trait PhaddwEmitter<T0, T1> {
    fn phaddw(&mut self, op0: T0, op1: T1);
}

/// Emits `PHMINPOSUW`.
pub trait PhminposuwEmitter<T0, T1> {
    fn phminposuw(&mut self, op0: T0, op1: T1);
}

/// Emits `PHSUBD`.
pub trait PhsubdEmitter<T0, T1> {
    fn phsubd(&mut self, op0: T0, op1: T1);
}

/// Emits `PHSUBSW`.
pub trait PhsubswEmitter<T0, T1> {
    fn phsubsw(&mut self, op0: T0, op1: T1);
}

/// Emits `PHSUBW`.
pub trait PhsubwEmitter<T0, T1> {
    fn phsubw(&mut self, op0: T0, op1: T1);
}

/// Emits `PINSRB`.
pub trait PinsrbEmitter<T0, T1, T2> {
    fn pinsrb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `PINSRD`.
pub trait PinsrdEmitter<T0, T1, T2> {
    fn pinsrd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `PINSRQ`.
pub trait PinsrqEmitter<T0, T1, T2> {
    fn pinsrq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `PINSRW`.
pub trait PinsrwEmitter<T0, T1, T2> {
    fn pinsrw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `PMADDUBSW`.
pub trait PmaddubswEmitter<T0, T1> {
    fn pmaddubsw(&mut self, op0: T0, op1: T1);
}

/// Emits `PMADDWD`.
pub trait PmaddwdEmitter<T0, T1> {
    fn pmaddwd(&mut self, op0: T0, op1: T1);
}

/// Emits `PMAXSB`.
pub trait PmaxsbEmitter<T0, T1> {
    fn pmaxsb(&mut self, op0: T0, op1: T1);
}

/// Emits `PMAXSD`.
pub trait PmaxsdEmitter<T0, T1> {
    fn pmaxsd(&mut self, op0: T0, op1: T1);
}

/// Emits `PMAXSW`.
pub trait PmaxswEmitter<T0, T1> {
    fn pmaxsw(&mut self, op0: T0, op1: T1);
}

/// Emits `PMAXUB`.
pub trait PmaxubEmitter<T0, T1> {
    fn pmaxub(&mut self, op0: T0, op1: T1);
}

/// Emits `PMAXUD`.
pub trait PmaxudEmitter<T0, T1> {
    fn pmaxud(&mut self, op0: T0, op1: T1);
}

/// Emits `PMAXUW`.
pub trait PmaxuwEmitter<T0, T1> {
    fn pmaxuw(&mut self, op0: T0, op1: T1);
}

/// Emits `PMINSB`.
pub trait PminsbEmitter<T0, T1> {
    fn pminsb(&mut self, op0: T0, op1: T1);
}

/// Emits `PMINSD`.
pub trait PminsdEmitter<T0, T1> {
    fn pminsd(&mut self, op0: T0, op1: T1);
}

/// Emits `PMINSW`.
pub trait PminswEmitter<T0, T1> {
    fn pminsw(&mut self, op0: T0, op1: T1);
}

/// Emits `PMINUB`.
pub trait PminubEmitter<T0, T1> {
    fn pminub(&mut self, op0: T0, op1: T1);
}

/// Emits `PMINUD`.
pub trait PminudEmitter<T0, T1> {
    fn pminud(&mut self, op0: T0, op1: T1);
}

/// Emits `PMINUW`.
pub trait PminuwEmitter<T0, T1> {
    fn pminuw(&mut self, op0: T0, op1: T1);
}

/// Emits `PMOVMSKB`.
pub trait PmovmskbEmitter<T0, T1> {
    fn pmovmskb(&mut self, op0: T0, op1: T1);
}

/// Emits `PMOVSXBD`.
pub trait PmovsxbdEmitter<T0, T1> {
    fn pmovsxbd(&mut self, op0: T0, op1: T1);
}

/// Emits `PMOVSXBQ`.
pub trait PmovsxbqEmitter<T0, T1> {
    fn pmovsxbq(&mut self, op0: T0, op1: T1);
}

/// Emits `PMOVSXBW`.
pub trait PmovsxbwEmitter<T0, T1> {
    fn pmovsxbw(&mut self, op0: T0, op1: T1);
}

/// Emits `PMOVSXDQ`.
pub trait PmovsxdqEmitter<T0, T1> {
    fn pmovsxdq(&mut self, op0: T0, op1: T1);
}

/// Emits `PMOVSXWD`.
pub trait PmovsxwdEmitter<T0, T1> {
    fn pmovsxwd(&mut self, op0: T0, op1: T1);
}

/// Emits `PMOVSXWQ`.
pub trait PmovsxwqEmitter<T0, T1> {
    fn pmovsxwq(&mut self, op0: T0, op1: T1);
}

/// Emits `PMOVZXBD`.
pub trait PmovzxbdEmitter<T0, T1> {
    fn pmovzxbd(&mut self, op0: T0, op1: T1);
}

/// Emits `PMOVZXBQ`.
pub trait PmovzxbqEmitter<T0, T1> {
    fn pmovzxbq(&mut self, op0: T0, op1: T1);
}

/// Emits `PMOVZXBW`.
pub trait PmovzxbwEmitter<T0, T1> {
    fn pmovzxbw(&mut self, op0: T0, op1: T1);
}

/// Emits `PMOVZXDQ`.
pub trait PmovzxdqEmitter<T0, T1> {
    fn pmovzxdq(&mut self, op0: T0, op1: T1);
}

/// Emits `PMOVZXWD`.
pub trait PmovzxwdEmitter<T0, T1> {
    fn pmovzxwd(&mut self, op0: T0, op1: T1);
}

/// Emits `PMOVZXWQ`.
pub trait PmovzxwqEmitter<T0, T1> {
    fn pmovzxwq(&mut self, op0: T0, op1: T1);
}

/// Emits `PMULDQ`.
pub trait PmuldqEmitter<T0, T1> {
    fn pmuldq(&mut self, op0: T0, op1: T1);
}

/// Emits `PMULHRSW`.
pub trait PmulhrswEmitter<T0, T1> {
    fn pmulhrsw(&mut self, op0: T0, op1: T1);
}

/// Emits `PMULHW`.
pub trait PmulhwEmitter<T0, T1> {
    fn pmulhw(&mut self, op0: T0, op1: T1);
}

/// Emits `PMULHUW`.
pub trait PmulhuwEmitter<T0, T1> {
    fn pmulhuw(&mut self, op0: T0, op1: T1);
}

/// Emits `PMULLD`.
pub trait PmulldEmitter<T0, T1> {
    fn pmulld(&mut self, op0: T0, op1: T1);
}

/// Emits `PMULLW`.
pub trait PmullwEmitter<T0, T1> {
    fn pmullw(&mut self, op0: T0, op1: T1);
}

/// Emits `PMULUDQ`.
pub trait PmuludqEmitter<T0, T1> {
    fn pmuludq(&mut self, op0: T0, op1: T1);
}

/// Emits `POR`.
pub trait PorEmitter<T0, T1> {
    fn por(&mut self, op0: T0, op1: T1);
}

/// Emits `PSADBW`.
pub trait PsadbwEmitter<T0, T1> {
    fn psadbw(&mut self, op0: T0, op1: T1);
}

/// Emits `PSLLD`.
pub trait PslldEmitter<T0, T1> {
    fn pslld(&mut self, op0: T0, op1: T1);
}

/// Emits `PSLLDQ`.
pub trait PslldqEmitter<T0, T1> {
    fn pslldq(&mut self, op0: T0, op1: T1);
}

/// Emits `PSLLQ`.
pub trait PsllqEmitter<T0, T1> {
    fn psllq(&mut self, op0: T0, op1: T1);
}

/// Emits `PSLLW`.
pub trait PsllwEmitter<T0, T1> {
    fn psllw(&mut self, op0: T0, op1: T1);
}

/// Emits `PSRAD`.
pub trait PsradEmitter<T0, T1> {
    fn psrad(&mut self, op0: T0, op1: T1);
}

/// Emits `PSRAW`.
pub trait PsrawEmitter<T0, T1> {
    fn psraw(&mut self, op0: T0, op1: T1);
}

/// Emits `PSHUFB`.
pub trait PshufbEmitter<T0, T1> {
    fn pshufb(&mut self, op0: T0, op1: T1);
}

/// Emits `PSHUFD`.
pub trait PshufdEmitter<T0, T1, T2> {
    fn pshufd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `PSHUFHW`.
pub trait PshufhwEmitter<T0, T1, T2> {
    fn pshufhw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `PSHUFLW`.
pub trait PshuflwEmitter<T0, T1, T2> {
    fn pshuflw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `PSHUFW`.
pub trait PshufwEmitter<T0, T1, T2> {
    fn pshufw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `PSIGNB`.
pub trait PsignbEmitter<T0, T1> {
    fn psignb(&mut self, op0: T0, op1: T1);
}

/// Emits `PSIGND`.
pub trait PsigndEmitter<T0, T1> {
    fn psignd(&mut self, op0: T0, op1: T1);
}

/// Emits `PSIGNW`.
pub trait PsignwEmitter<T0, T1> {
    fn psignw(&mut self, op0: T0, op1: T1);
}

/// Emits `PSRLD`.
pub trait PsrldEmitter<T0, T1> {
    fn psrld(&mut self, op0: T0, op1: T1);
}

/// Emits `PSRLDQ`.
pub trait PsrldqEmitter<T0, T1> {
    fn psrldq(&mut self, op0: T0, op1: T1);
}

/// Emits `PSRLQ`.
pub trait PsrlqEmitter<T0, T1> {
    fn psrlq(&mut self, op0: T0, op1: T1);
}

/// Emits `PSRLW`.
pub trait PsrlwEmitter<T0, T1> {
    fn psrlw(&mut self, op0: T0, op1: T1);
}

/// Emits `PSUBB`.
pub trait PsubbEmitter<T0, T1> {
    fn psubb(&mut self, op0: T0, op1: T1);
}

/// Emits `PSUBD`.
pub trait PsubdEmitter<T0, T1> {
    fn psubd(&mut self, op0: T0, op1: T1);
}

/// Emits `PSUBQ`.
pub trait PsubqEmitter<T0, T1> {
    fn psubq(&mut self, op0: T0, op1: T1);
}

/// Emits `PSUBSB`.
pub trait PsubsbEmitter<T0, T1> {
    fn psubsb(&mut self, op0: T0, op1: T1);
}

/// Emits `PSUBSW`.
pub trait PsubswEmitter<T0, T1> {
    fn psubsw(&mut self, op0: T0, op1: T1);
}

/// Emits `PSUBUSB`.
pub trait PsubusbEmitter<T0, T1> {
    fn psubusb(&mut self, op0: T0, op1: T1);
}

/// Emits `PSUBUSW`.
pub trait PsubuswEmitter<T0, T1> {
    fn psubusw(&mut self, op0: T0, op1: T1);
}

/// Emits `PSUBW`.
pub trait PsubwEmitter<T0, T1> {
    fn psubw(&mut self, op0: T0, op1: T1);
}

/// Emits `PTEST`.
pub trait PtestEmitter<T0, T1> {
    fn ptest(&mut self, op0: T0, op1: T1);
}

/// Emits `PUNPCKHBW`.
pub trait PunpckhbwEmitter<T0, T1> {
    fn punpckhbw(&mut self, op0: T0, op1: T1);
}

/// Emits `PUNPCKHDQ`.
pub trait PunpckhdqEmitter<T0, T1> {
    fn punpckhdq(&mut self, op0: T0, op1: T1);
}

/// Emits `PUNPCKHQDQ`.
pub trait PunpckhqdqEmitter<T0, T1> {
    fn punpckhqdq(&mut self, op0: T0, op1: T1);
}

/// Emits `PUNPCKHWD`.
pub trait PunpckhwdEmitter<T0, T1> {
    fn punpckhwd(&mut self, op0: T0, op1: T1);
}

/// Emits `PUNPCKLBW`.
pub trait PunpcklbwEmitter<T0, T1> {
    fn punpcklbw(&mut self, op0: T0, op1: T1);
}

/// Emits `PUNPCKLDQ`.
pub trait PunpckldqEmitter<T0, T1> {
    fn punpckldq(&mut self, op0: T0, op1: T1);
}

/// Emits `PUNPCKLQDQ`.
pub trait PunpcklqdqEmitter<T0, T1> {
    fn punpcklqdq(&mut self, op0: T0, op1: T1);
}

/// Emits `PUNPCKLWD`.
pub trait PunpcklwdEmitter<T0, T1> {
    fn punpcklwd(&mut self, op0: T0, op1: T1);
}

/// Emits `PXOR`.
pub trait PxorEmitter<T0, T1> {
    fn pxor(&mut self, op0: T0, op1: T1);
}

/// Emits `RCPPS`.
pub trait RcppsEmitter<T0, T1> {
    fn rcpps(&mut self, op0: T0, op1: T1);
}

/// Emits `RCPSS`.
pub trait RcpssEmitter<T0, T1> {
    fn rcpss(&mut self, op0: T0, op1: T1);
}

/// Emits `ROUNDPD`.
pub trait RoundpdEmitter<T0, T1, T2> {
    fn roundpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `ROUNDPS`.
pub trait RoundpsEmitter<T0, T1, T2> {
    fn roundps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `ROUNDSD`.
pub trait RoundsdEmitter<T0, T1, T2> {
    fn roundsd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `ROUNDSS`.
pub trait RoundssEmitter<T0, T1, T2> {
    fn roundss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `RSQRTPS`.
pub trait RsqrtpsEmitter<T0, T1> {
    fn rsqrtps(&mut self, op0: T0, op1: T1);
}

/// Emits `RSQRTSS`.
pub trait RsqrtssEmitter<T0, T1> {
    fn rsqrtss(&mut self, op0: T0, op1: T1);
}

/// Emits `SHUFPD`.
pub trait ShufpdEmitter<T0, T1, T2> {
    fn shufpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `SHUFPS`.
pub trait ShufpsEmitter<T0, T1, T2> {
    fn shufps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `SQRTPD`.
pub trait SqrtpdEmitter<T0, T1> {
    fn sqrtpd(&mut self, op0: T0, op1: T1);
}

/// Emits `SQRTPS`.
pub trait SqrtpsEmitter<T0, T1> {
    fn sqrtps(&mut self, op0: T0, op1: T1);
}

/// Emits `SQRTSD`.
pub trait SqrtsdEmitter<T0, T1> {
    fn sqrtsd(&mut self, op0: T0, op1: T1);
}

/// Emits `SQRTSS`.
pub trait SqrtssEmitter<T0, T1> {
    fn sqrtss(&mut self, op0: T0, op1: T1);
}

/// Emits `SUBPD`.
pub trait SubpdEmitter<T0, T1> {
    fn subpd(&mut self, op0: T0, op1: T1);
}

/// Emits `SUBPS`.
pub trait SubpsEmitter<T0, T1> {
    fn subps(&mut self, op0: T0, op1: T1);
}

/// Emits `SUBSD`.
pub trait SubsdEmitter<T0, T1> {
    fn subsd(&mut self, op0: T0, op1: T1);
}

/// Emits `SUBSS`.
pub trait SubssEmitter<T0, T1> {
    fn subss(&mut self, op0: T0, op1: T1);
}

/// Emits `UCOMISD`.
pub trait UcomisdEmitter<T0, T1> {
    fn ucomisd(&mut self, op0: T0, op1: T1);
}

/// Emits `UCOMISS`.
pub trait UcomissEmitter<T0, T1> {
    fn ucomiss(&mut self, op0: T0, op1: T1);
}

/// Emits `UNPCKHPD`.
pub trait UnpckhpdEmitter<T0, T1> {
    fn unpckhpd(&mut self, op0: T0, op1: T1);
}

/// Emits `UNPCKHPS`.
pub trait UnpckhpsEmitter<T0, T1> {
    fn unpckhps(&mut self, op0: T0, op1: T1);
}

/// Emits `UNPCKLPD`.
pub trait UnpcklpdEmitter<T0, T1> {
    fn unpcklpd(&mut self, op0: T0, op1: T1);
}

/// Emits `UNPCKLPS`.
pub trait UnpcklpsEmitter<T0, T1> {
    fn unpcklps(&mut self, op0: T0, op1: T1);
}

/// Emits `XORPD`.
pub trait XorpdEmitter<T0, T1> {
    fn xorpd(&mut self, op0: T0, op1: T1);
}

/// Emits `XORPS`.
pub trait XorpsEmitter<T0, T1> {
    fn xorps(&mut self, op0: T0, op1: T1);
}

/// Emits `PAVGUSB`.
pub trait PavgusbEmitter<T0, T1> {
    fn pavgusb(&mut self, op0: T0, op1: T1);
}

/// Emits `PF2ID`.
pub trait Pf2idEmitter<T0, T1> {
    fn pf2id(&mut self, op0: T0, op1: T1);
}

/// Emits `PF2IW`.
pub trait Pf2iwEmitter<T0, T1> {
    fn pf2iw(&mut self, op0: T0, op1: T1);
}

/// Emits `PFACC`.
pub trait PfaccEmitter<T0, T1> {
    fn pfacc(&mut self, op0: T0, op1: T1);
}

/// Emits `PFADD`.
pub trait PfaddEmitter<T0, T1> {
    fn pfadd(&mut self, op0: T0, op1: T1);
}

/// Emits `PFCMPEQ`.
pub trait PfcmpeqEmitter<T0, T1> {
    fn pfcmpeq(&mut self, op0: T0, op1: T1);
}

/// Emits `PFCMPGE`.
pub trait PfcmpgeEmitter<T0, T1> {
    fn pfcmpge(&mut self, op0: T0, op1: T1);
}

/// Emits `PFCMPGT`.
pub trait PfcmpgtEmitter<T0, T1> {
    fn pfcmpgt(&mut self, op0: T0, op1: T1);
}

/// Emits `PFMAX`.
pub trait PfmaxEmitter<T0, T1> {
    fn pfmax(&mut self, op0: T0, op1: T1);
}

/// Emits `PFMIN`.
pub trait PfminEmitter<T0, T1> {
    fn pfmin(&mut self, op0: T0, op1: T1);
}

/// Emits `PFMUL`.
pub trait PfmulEmitter<T0, T1> {
    fn pfmul(&mut self, op0: T0, op1: T1);
}

/// Emits `PFNACC`.
pub trait PfnaccEmitter<T0, T1> {
    fn pfnacc(&mut self, op0: T0, op1: T1);
}

/// Emits `PFPNACC`.
pub trait PfpnaccEmitter<T0, T1> {
    fn pfpnacc(&mut self, op0: T0, op1: T1);
}

/// Emits `PFRCP`.
pub trait PfrcpEmitter<T0, T1> {
    fn pfrcp(&mut self, op0: T0, op1: T1);
}

/// Emits `PFRCPIT1`.
pub trait Pfrcpit1Emitter<T0, T1> {
    fn pfrcpit1(&mut self, op0: T0, op1: T1);
}

/// Emits `PFRCPIT2`.
pub trait Pfrcpit2Emitter<T0, T1> {
    fn pfrcpit2(&mut self, op0: T0, op1: T1);
}

/// Emits `PFRCPV`.
pub trait PfrcpvEmitter<T0, T1> {
    fn pfrcpv(&mut self, op0: T0, op1: T1);
}

/// Emits `PFRSQIT1`.
pub trait Pfrsqit1Emitter<T0, T1> {
    fn pfrsqit1(&mut self, op0: T0, op1: T1);
}

/// Emits `PFRSQRT`.
pub trait PfrsqrtEmitter<T0, T1> {
    fn pfrsqrt(&mut self, op0: T0, op1: T1);
}

/// Emits `PFRSQRTV`.
pub trait PfrsqrtvEmitter<T0, T1> {
    fn pfrsqrtv(&mut self, op0: T0, op1: T1);
}

/// Emits `PFSUB`.
pub trait PfsubEmitter<T0, T1> {
    fn pfsub(&mut self, op0: T0, op1: T1);
}

/// Emits `PFSUBR`.
pub trait PfsubrEmitter<T0, T1> {
    fn pfsubr(&mut self, op0: T0, op1: T1);
}

/// Emits `PI2FD`.
pub trait Pi2fdEmitter<T0, T1> {
    fn pi2fd(&mut self, op0: T0, op1: T1);
}

/// Emits `PI2FW`.
pub trait Pi2fwEmitter<T0, T1> {
    fn pi2fw(&mut self, op0: T0, op1: T1);
}

/// Emits `PMULHRW`.
pub trait PmulhrwEmitter<T0, T1> {
    fn pmulhrw(&mut self, op0: T0, op1: T1);
}

/// Emits `PSWAPD`.
pub trait PswapdEmitter<T0, T1> {
    fn pswapd(&mut self, op0: T0, op1: T1);
}

/// Emits `EMMS`.
pub trait EmmsEmitter {
    fn emms(&mut self);
}

/// Emits `FEMMS`.
pub trait FemmsEmitter {
    fn femms(&mut self);
}

/// Emits `AESDEC`.
pub trait AesdecEmitter<T0, T1> {
    fn aesdec(&mut self, op0: T0, op1: T1);
}

/// Emits `AESDECLAST`.
pub trait AesdeclastEmitter<T0, T1> {
    fn aesdeclast(&mut self, op0: T0, op1: T1);
}

/// Emits `AESENC`.
pub trait AesencEmitter<T0, T1> {
    fn aesenc(&mut self, op0: T0, op1: T1);
}

/// Emits `AESENCLAST`.
pub trait AesenclastEmitter<T0, T1> {
    fn aesenclast(&mut self, op0: T0, op1: T1);
}

/// Emits `AESIMC`.
pub trait AesimcEmitter<T0, T1> {
    fn aesimc(&mut self, op0: T0, op1: T1);
}

/// Emits `AESKEYGENASSIST`.
pub trait AeskeygenassistEmitter<T0, T1, T2> {
    fn aeskeygenassist(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `SHA1MSG1`.
pub trait Sha1msg1Emitter<T0, T1> {
    fn sha1msg1(&mut self, op0: T0, op1: T1);
}

/// Emits `SHA1MSG2`.
pub trait Sha1msg2Emitter<T0, T1> {
    fn sha1msg2(&mut self, op0: T0, op1: T1);
}

/// Emits `SHA1NEXTE`.
pub trait Sha1nexteEmitter<T0, T1> {
    fn sha1nexte(&mut self, op0: T0, op1: T1);
}

/// Emits `SHA1RNDS4`.
pub trait Sha1rnds4Emitter<T0, T1, T2> {
    fn sha1rnds4(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `SHA256MSG1`.
pub trait Sha256msg1Emitter<T0, T1> {
    fn sha256msg1(&mut self, op0: T0, op1: T1);
}

/// Emits `SHA256MSG2`.
pub trait Sha256msg2Emitter<T0, T1> {
    fn sha256msg2(&mut self, op0: T0, op1: T1);
}

/// Emits `SHA256RNDS2`.
pub trait Sha256rnds2Emitter<T0, T1, T2> {
    fn sha256rnds2(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `GF2P8AFFINEINVQB`.
pub trait Gf2p8affineinvqbEmitter<T0, T1, T2> {
    fn gf2p8affineinvqb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `GF2P8AFFINEQB`.
pub trait Gf2p8affineqbEmitter<T0, T1, T2> {
    fn gf2p8affineqb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `GF2P8MULB`.
pub trait Gf2p8mulbEmitter<T0, T1> {
    fn gf2p8mulb(&mut self, op0: T0, op1: T1);
}

/// Emits `KADDB`.
pub trait KaddbEmitter<T0, T1, T2> {
    fn kaddb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KADDD`.
pub trait KadddEmitter<T0, T1, T2> {
    fn kaddd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KADDQ`.
pub trait KaddqEmitter<T0, T1, T2> {
    fn kaddq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KADDW`.
pub trait KaddwEmitter<T0, T1, T2> {
    fn kaddw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KANDB`.
pub trait KandbEmitter<T0, T1, T2> {
    fn kandb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KANDD`.
pub trait KanddEmitter<T0, T1, T2> {
    fn kandd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KANDNB`.
pub trait KandnbEmitter<T0, T1, T2> {
    fn kandnb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KANDND`.
pub trait KandndEmitter<T0, T1, T2> {
    fn kandnd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KANDNQ`.
pub trait KandnqEmitter<T0, T1, T2> {
    fn kandnq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KANDNW`.
pub trait KandnwEmitter<T0, T1, T2> {
    fn kandnw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KANDQ`.
pub trait KandqEmitter<T0, T1, T2> {
    fn kandq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KANDW`.
pub trait KandwEmitter<T0, T1, T2> {
    fn kandw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KMOVB`.
pub trait KmovbEmitter<T0, T1> {
    fn kmovb(&mut self, op0: T0, op1: T1);
}

/// Emits `KMOVD`.
pub trait KmovdEmitter<T0, T1> {
    fn kmovd(&mut self, op0: T0, op1: T1);
}

/// Emits `KMOVQ`.
pub trait KmovqEmitter<T0, T1> {
    fn kmovq(&mut self, op0: T0, op1: T1);
}

/// Emits `KMOVW`.
pub trait KmovwEmitter<T0, T1> {
    fn kmovw(&mut self, op0: T0, op1: T1);
}

/// Emits `KNOTB`.
pub trait KnotbEmitter<T0, T1> {
    fn knotb(&mut self, op0: T0, op1: T1);
}

/// Emits `KNOTD`.
pub trait KnotdEmitter<T0, T1> {
    fn knotd(&mut self, op0: T0, op1: T1);
}

/// Emits `KNOTQ`.
pub trait KnotqEmitter<T0, T1> {
    fn knotq(&mut self, op0: T0, op1: T1);
}

/// Emits `KNOTW`.
pub trait KnotwEmitter<T0, T1> {
    fn knotw(&mut self, op0: T0, op1: T1);
}

/// Emits `KORB`.
pub trait KorbEmitter<T0, T1, T2> {
    fn korb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KORD`.
pub trait KordEmitter<T0, T1, T2> {
    fn kord(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KORQ`.
pub trait KorqEmitter<T0, T1, T2> {
    fn korq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KORTESTB`.
pub trait KortestbEmitter<T0, T1> {
    fn kortestb(&mut self, op0: T0, op1: T1);
}

/// Emits `KORTESTD`.
pub trait KortestdEmitter<T0, T1> {
    fn kortestd(&mut self, op0: T0, op1: T1);
}

/// Emits `KORTESTQ`.
pub trait KortestqEmitter<T0, T1> {
    fn kortestq(&mut self, op0: T0, op1: T1);
}

/// Emits `KORTESTW`.
pub trait KortestwEmitter<T0, T1> {
    fn kortestw(&mut self, op0: T0, op1: T1);
}

/// Emits `KORW`.
pub trait KorwEmitter<T0, T1, T2> {
    fn korw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KSHIFTLB`.
pub trait KshiftlbEmitter<T0, T1, T2> {
    fn kshiftlb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KSHIFTLD`.
pub trait KshiftldEmitter<T0, T1, T2> {
    fn kshiftld(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KSHIFTLQ`.
pub trait KshiftlqEmitter<T0, T1, T2> {
    fn kshiftlq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KSHIFTLW`.
pub trait KshiftlwEmitter<T0, T1, T2> {
    fn kshiftlw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KSHIFTRB`.
pub trait KshiftrbEmitter<T0, T1, T2> {
    fn kshiftrb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KSHIFTRD`.
pub trait KshiftrdEmitter<T0, T1, T2> {
    fn kshiftrd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KSHIFTRQ`.
pub trait KshiftrqEmitter<T0, T1, T2> {
    fn kshiftrq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KSHIFTRW`.
pub trait KshiftrwEmitter<T0, T1, T2> {
    fn kshiftrw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KTESTB`.
pub trait KtestbEmitter<T0, T1> {
    fn ktestb(&mut self, op0: T0, op1: T1);
}

/// Emits `KTESTD`.
pub trait KtestdEmitter<T0, T1> {
    fn ktestd(&mut self, op0: T0, op1: T1);
}

/// Emits `KTESTQ`.
pub trait KtestqEmitter<T0, T1> {
    fn ktestq(&mut self, op0: T0, op1: T1);
}

/// Emits `KTESTW`.
pub trait KtestwEmitter<T0, T1> {
    fn ktestw(&mut self, op0: T0, op1: T1);
}

/// Emits `KUNPCKBW`.
pub trait KunpckbwEmitter<T0, T1, T2> {
    fn kunpckbw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KUNPCKDQ`.
pub trait KunpckdqEmitter<T0, T1, T2> {
    fn kunpckdq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KUNPCKWD`.
pub trait KunpckwdEmitter<T0, T1, T2> {
    fn kunpckwd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KXNORB`.
pub trait KxnorbEmitter<T0, T1, T2> {
    fn kxnorb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KXNORD`.
pub trait KxnordEmitter<T0, T1, T2> {
    fn kxnord(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KXNORQ`.
pub trait KxnorqEmitter<T0, T1, T2> {
    fn kxnorq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KXNORW`.
pub trait KxnorwEmitter<T0, T1, T2> {
    fn kxnorw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KXORB`.
pub trait KxorbEmitter<T0, T1, T2> {
    fn kxorb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KXORD`.
pub trait KxordEmitter<T0, T1, T2> {
    fn kxord(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KXORQ`.
pub trait KxorqEmitter<T0, T1, T2> {
    fn kxorq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `KXORW`.
pub trait KxorwEmitter<T0, T1, T2> {
    fn kxorw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VADDPD`.
pub trait VaddpdEmitter<T0, T1, T2> {
    fn vaddpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VADDPS`.
pub trait VaddpsEmitter<T0, T1, T2> {
    fn vaddps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VADDSD`.
pub trait VaddsdEmitter<T0, T1, T2> {
    fn vaddsd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VADDSS`.
pub trait VaddssEmitter<T0, T1, T2> {
    fn vaddss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VADDSUBPD`.
pub trait VaddsubpdEmitter<T0, T1, T2> {
    fn vaddsubpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VADDSUBPS`.
pub trait VaddsubpsEmitter<T0, T1, T2> {
    fn vaddsubps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VAESDEC`.
pub trait VaesdecEmitter<T0, T1, T2> {
    fn vaesdec(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VAESDECLAST`.
pub trait VaesdeclastEmitter<T0, T1, T2> {
    fn vaesdeclast(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VAESENC`.
pub trait VaesencEmitter<T0, T1, T2> {
    fn vaesenc(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VAESENCLAST`.
pub trait VaesenclastEmitter<T0, T1, T2> {
    fn vaesenclast(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VAESIMC`.
pub trait VaesimcEmitter<T0, T1> {
    fn vaesimc(&mut self, op0: T0, op1: T1);
}

/// Emits `VAESKEYGENASSIST`.
pub trait VaeskeygenassistEmitter<T0, T1, T2> {
    fn vaeskeygenassist(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VALIGND`.
pub trait ValigndEmitter<T0, T1, T2, T3> {
    fn valignd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VALIGNQ`.
pub trait ValignqEmitter<T0, T1, T2, T3> {
    fn valignq(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VANDNPD`.
pub trait VandnpdEmitter<T0, T1, T2> {
    fn vandnpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VANDNPS`.
pub trait VandnpsEmitter<T0, T1, T2> {
    fn vandnps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VANDPD`.
pub trait VandpdEmitter<T0, T1, T2> {
    fn vandpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VANDPS`.
pub trait VandpsEmitter<T0, T1, T2> {
    fn vandps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VBLENDMPD`.
pub trait VblendmpdEmitter<T0, T1, T2> {
    fn vblendmpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VBLENDMPS`.
pub trait VblendmpsEmitter<T0, T1, T2> {
    fn vblendmps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VBLENDPD`.
pub trait VblendpdEmitter<T0, T1, T2, T3> {
    fn vblendpd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VBLENDPS`.
pub trait VblendpsEmitter<T0, T1, T2, T3> {
    fn vblendps(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VBLENDVPD`.
pub trait VblendvpdEmitter<T0, T1, T2, T3> {
    fn vblendvpd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VBLENDVPS`.
pub trait VblendvpsEmitter<T0, T1, T2, T3> {
    fn vblendvps(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VBROADCASTF128`.
pub trait Vbroadcastf128Emitter<T0, T1> {
    fn vbroadcastf128(&mut self, op0: T0, op1: T1);
}

/// Emits `VBROADCASTF32X2`.
pub trait Vbroadcastf32x2Emitter<T0, T1> {
    fn vbroadcastf32x2(&mut self, op0: T0, op1: T1);
}

/// Emits `VBROADCASTF32X4`.
pub trait Vbroadcastf32x4Emitter<T0, T1> {
    fn vbroadcastf32x4(&mut self, op0: T0, op1: T1);
}

/// Emits `VBROADCASTF32X8`.
pub trait Vbroadcastf32x8Emitter<T0, T1> {
    fn vbroadcastf32x8(&mut self, op0: T0, op1: T1);
}

/// Emits `VBROADCASTF64X2`.
pub trait Vbroadcastf64x2Emitter<T0, T1> {
    fn vbroadcastf64x2(&mut self, op0: T0, op1: T1);
}

/// Emits `VBROADCASTF64X4`.
pub trait Vbroadcastf64x4Emitter<T0, T1> {
    fn vbroadcastf64x4(&mut self, op0: T0, op1: T1);
}

/// Emits `VBROADCASTI128`.
pub trait Vbroadcasti128Emitter<T0, T1> {
    fn vbroadcasti128(&mut self, op0: T0, op1: T1);
}

/// Emits `VBROADCASTI32X2`.
pub trait Vbroadcasti32x2Emitter<T0, T1> {
    fn vbroadcasti32x2(&mut self, op0: T0, op1: T1);
}

/// Emits `VBROADCASTI32X4`.
pub trait Vbroadcasti32x4Emitter<T0, T1> {
    fn vbroadcasti32x4(&mut self, op0: T0, op1: T1);
}

/// Emits `VBROADCASTI32X8`.
pub trait Vbroadcasti32x8Emitter<T0, T1> {
    fn vbroadcasti32x8(&mut self, op0: T0, op1: T1);
}

/// Emits `VBROADCASTI64X2`.
pub trait Vbroadcasti64x2Emitter<T0, T1> {
    fn vbroadcasti64x2(&mut self, op0: T0, op1: T1);
}

/// Emits `VBROADCASTI64X4`.
pub trait Vbroadcasti64x4Emitter<T0, T1> {
    fn vbroadcasti64x4(&mut self, op0: T0, op1: T1);
}

/// Emits `VBROADCASTSD`.
pub trait VbroadcastsdEmitter<T0, T1> {
    fn vbroadcastsd(&mut self, op0: T0, op1: T1);
}

/// Emits `VBROADCASTSS`.
pub trait VbroadcastssEmitter<T0, T1> {
    fn vbroadcastss(&mut self, op0: T0, op1: T1);
}

/// Emits `VCMPPD`.
pub trait VcmppdEmitter<T0, T1, T2, T3> {
    fn vcmppd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VCMPPS`.
pub trait VcmppsEmitter<T0, T1, T2, T3> {
    fn vcmpps(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VCMPSD`.
pub trait VcmpsdEmitter<T0, T1, T2, T3> {
    fn vcmpsd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VCMPSS`.
pub trait VcmpssEmitter<T0, T1, T2, T3> {
    fn vcmpss(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VCOMISD`.
pub trait VcomisdEmitter<T0, T1> {
    fn vcomisd(&mut self, op0: T0, op1: T1);
}

/// Emits `VCOMISS`.
pub trait VcomissEmitter<T0, T1> {
    fn vcomiss(&mut self, op0: T0, op1: T1);
}

/// Emits `VCOMPRESSPD`.
pub trait VcompresspdEmitter<T0, T1> {
    fn vcompresspd(&mut self, op0: T0, op1: T1);
}

/// Emits `VCOMPRESSPS`.
pub trait VcompresspsEmitter<T0, T1> {
    fn vcompressps(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTDQ2PD`.
pub trait Vcvtdq2pdEmitter<T0, T1> {
    fn vcvtdq2pd(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTDQ2PS`.
pub trait Vcvtdq2psEmitter<T0, T1> {
    fn vcvtdq2ps(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTNE2PS2BF16`.
pub trait Vcvtne2ps2bf16Emitter<T0, T1, T2> {
    fn vcvtne2ps2bf16(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VCVTNEPS2BF16`.
pub trait Vcvtneps2bf16Emitter<T0, T1> {
    fn vcvtneps2bf16(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTPD2DQ`.
pub trait Vcvtpd2dqEmitter<T0, T1> {
    fn vcvtpd2dq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTPD2PS`.
pub trait Vcvtpd2psEmitter<T0, T1> {
    fn vcvtpd2ps(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTPD2QQ`.
pub trait Vcvtpd2qqEmitter<T0, T1> {
    fn vcvtpd2qq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTPD2UDQ`.
pub trait Vcvtpd2udqEmitter<T0, T1> {
    fn vcvtpd2udq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTPD2UQQ`.
pub trait Vcvtpd2uqqEmitter<T0, T1> {
    fn vcvtpd2uqq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTPH2PS`.
pub trait Vcvtph2psEmitter<T0, T1> {
    fn vcvtph2ps(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTPS2DQ`.
pub trait Vcvtps2dqEmitter<T0, T1> {
    fn vcvtps2dq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTPS2PD`.
pub trait Vcvtps2pdEmitter<T0, T1> {
    fn vcvtps2pd(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTPS2PH`.
pub trait Vcvtps2phEmitter<T0, T1, T2> {
    fn vcvtps2ph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VCVTPS2QQ`.
pub trait Vcvtps2qqEmitter<T0, T1> {
    fn vcvtps2qq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTPS2UDQ`.
pub trait Vcvtps2udqEmitter<T0, T1> {
    fn vcvtps2udq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTPS2UQQ`.
pub trait Vcvtps2uqqEmitter<T0, T1> {
    fn vcvtps2uqq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTQQ2PD`.
pub trait Vcvtqq2pdEmitter<T0, T1> {
    fn vcvtqq2pd(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTQQ2PS`.
pub trait Vcvtqq2psEmitter<T0, T1> {
    fn vcvtqq2ps(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTSD2SI`.
pub trait Vcvtsd2siEmitter<T0, T1> {
    fn vcvtsd2si(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTSD2SS`.
pub trait Vcvtsd2ssEmitter<T0, T1, T2> {
    fn vcvtsd2ss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VCVTSD2USI`.
pub trait Vcvtsd2usiEmitter<T0, T1> {
    fn vcvtsd2usi(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTSI2SD`.
pub trait Vcvtsi2sdEmitter<T0, T1, T2> {
    fn vcvtsi2sd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VCVTSI2SS`.
pub trait Vcvtsi2ssEmitter<T0, T1, T2> {
    fn vcvtsi2ss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VCVTSS2SD`.
pub trait Vcvtss2sdEmitter<T0, T1, T2> {
    fn vcvtss2sd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VCVTSS2SI`.
pub trait Vcvtss2siEmitter<T0, T1> {
    fn vcvtss2si(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTSS2USI`.
pub trait Vcvtss2usiEmitter<T0, T1> {
    fn vcvtss2usi(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTTPD2DQ`.
pub trait Vcvttpd2dqEmitter<T0, T1> {
    fn vcvttpd2dq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTTPD2QQ`.
pub trait Vcvttpd2qqEmitter<T0, T1> {
    fn vcvttpd2qq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTTPD2UDQ`.
pub trait Vcvttpd2udqEmitter<T0, T1> {
    fn vcvttpd2udq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTTPD2UQQ`.
pub trait Vcvttpd2uqqEmitter<T0, T1> {
    fn vcvttpd2uqq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTTPS2DQ`.
pub trait Vcvttps2dqEmitter<T0, T1> {
    fn vcvttps2dq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTTPS2QQ`.
pub trait Vcvttps2qqEmitter<T0, T1> {
    fn vcvttps2qq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTTPS2UDQ`.
pub trait Vcvttps2udqEmitter<T0, T1> {
    fn vcvttps2udq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTTPS2UQQ`.
pub trait Vcvttps2uqqEmitter<T0, T1> {
    fn vcvttps2uqq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTTSD2SI`.
pub trait Vcvttsd2siEmitter<T0, T1> {
    fn vcvttsd2si(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTTSD2USI`.
pub trait Vcvttsd2usiEmitter<T0, T1> {
    fn vcvttsd2usi(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTTSS2SI`.
pub trait Vcvttss2siEmitter<T0, T1> {
    fn vcvttss2si(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTTSS2USI`.
pub trait Vcvttss2usiEmitter<T0, T1> {
    fn vcvttss2usi(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTUDQ2PD`.
pub trait Vcvtudq2pdEmitter<T0, T1> {
    fn vcvtudq2pd(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTUDQ2PS`.
pub trait Vcvtudq2psEmitter<T0, T1> {
    fn vcvtudq2ps(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTUQQ2PD`.
pub trait Vcvtuqq2pdEmitter<T0, T1> {
    fn vcvtuqq2pd(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTUQQ2PS`.
pub trait Vcvtuqq2psEmitter<T0, T1> {
    fn vcvtuqq2ps(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTUSI2SD`.
pub trait Vcvtusi2sdEmitter<T0, T1, T2> {
    fn vcvtusi2sd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VCVTUSI2SS`.
pub trait Vcvtusi2ssEmitter<T0, T1, T2> {
    fn vcvtusi2ss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VDBPSADBW`.
pub trait VdbpsadbwEmitter<T0, T1, T2, T3> {
    fn vdbpsadbw(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VDIVPD`.
pub trait VdivpdEmitter<T0, T1, T2> {
    fn vdivpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VDIVPS`.
pub trait VdivpsEmitter<T0, T1, T2> {
    fn vdivps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VDIVSD`.
pub trait VdivsdEmitter<T0, T1, T2> {
    fn vdivsd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VDIVSS`.
pub trait VdivssEmitter<T0, T1, T2> {
    fn vdivss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VDPBF16PS`.
pub trait Vdpbf16psEmitter<T0, T1, T2> {
    fn vdpbf16ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VDPPD`.
pub trait VdppdEmitter<T0, T1, T2, T3> {
    fn vdppd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VDPPS`.
pub trait VdppsEmitter<T0, T1, T2, T3> {
    fn vdpps(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VEXPANDPD`.
pub trait VexpandpdEmitter<T0, T1> {
    fn vexpandpd(&mut self, op0: T0, op1: T1);
}

/// Emits `VEXPANDPS`.
pub trait VexpandpsEmitter<T0, T1> {
    fn vexpandps(&mut self, op0: T0, op1: T1);
}

/// Emits `VEXTRACTF128`.
pub trait Vextractf128Emitter<T0, T1, T2> {
    fn vextractf128(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VEXTRACTF32X4`.
pub trait Vextractf32x4Emitter<T0, T1, T2> {
    fn vextractf32x4(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VEXTRACTF32X8`.
pub trait Vextractf32x8Emitter<T0, T1, T2> {
    fn vextractf32x8(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VEXTRACTF64X2`.
pub trait Vextractf64x2Emitter<T0, T1, T2> {
    fn vextractf64x2(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VEXTRACTF64X4`.
pub trait Vextractf64x4Emitter<T0, T1, T2> {
    fn vextractf64x4(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VEXTRACTI128`.
pub trait Vextracti128Emitter<T0, T1, T2> {
    fn vextracti128(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VEXTRACTI32X4`.
pub trait Vextracti32x4Emitter<T0, T1, T2> {
    fn vextracti32x4(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VEXTRACTI32X8`.
pub trait Vextracti32x8Emitter<T0, T1, T2> {
    fn vextracti32x8(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VEXTRACTI64X2`.
pub trait Vextracti64x2Emitter<T0, T1, T2> {
    fn vextracti64x2(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VEXTRACTI64X4`.
pub trait Vextracti64x4Emitter<T0, T1, T2> {
    fn vextracti64x4(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VEXTRACTPS`.
pub trait VextractpsEmitter<T0, T1, T2> {
    fn vextractps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFIXUPIMMPD`.
pub trait VfixupimmpdEmitter<T0, T1, T2, T3> {
    fn vfixupimmpd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFIXUPIMMPS`.
pub trait VfixupimmpsEmitter<T0, T1, T2, T3> {
    fn vfixupimmps(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFIXUPIMMSD`.
pub trait VfixupimmsdEmitter<T0, T1, T2, T3> {
    fn vfixupimmsd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFIXUPIMMSS`.
pub trait VfixupimmssEmitter<T0, T1, T2, T3> {
    fn vfixupimmss(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFMADD132PD`.
pub trait Vfmadd132pdEmitter<T0, T1, T2> {
    fn vfmadd132pd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADD132PS`.
pub trait Vfmadd132psEmitter<T0, T1, T2> {
    fn vfmadd132ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADD132SD`.
pub trait Vfmadd132sdEmitter<T0, T1, T2> {
    fn vfmadd132sd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADD132SS`.
pub trait Vfmadd132ssEmitter<T0, T1, T2> {
    fn vfmadd132ss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADD213PD`.
pub trait Vfmadd213pdEmitter<T0, T1, T2> {
    fn vfmadd213pd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADD213PS`.
pub trait Vfmadd213psEmitter<T0, T1, T2> {
    fn vfmadd213ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADD213SD`.
pub trait Vfmadd213sdEmitter<T0, T1, T2> {
    fn vfmadd213sd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADD213SS`.
pub trait Vfmadd213ssEmitter<T0, T1, T2> {
    fn vfmadd213ss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADD231PD`.
pub trait Vfmadd231pdEmitter<T0, T1, T2> {
    fn vfmadd231pd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADD231PS`.
pub trait Vfmadd231psEmitter<T0, T1, T2> {
    fn vfmadd231ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADD231SD`.
pub trait Vfmadd231sdEmitter<T0, T1, T2> {
    fn vfmadd231sd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADD231SS`.
pub trait Vfmadd231ssEmitter<T0, T1, T2> {
    fn vfmadd231ss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADDSUB132PD`.
pub trait Vfmaddsub132pdEmitter<T0, T1, T2> {
    fn vfmaddsub132pd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADDSUB132PS`.
pub trait Vfmaddsub132psEmitter<T0, T1, T2> {
    fn vfmaddsub132ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADDSUB213PD`.
pub trait Vfmaddsub213pdEmitter<T0, T1, T2> {
    fn vfmaddsub213pd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADDSUB213PS`.
pub trait Vfmaddsub213psEmitter<T0, T1, T2> {
    fn vfmaddsub213ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADDSUB231PD`.
pub trait Vfmaddsub231pdEmitter<T0, T1, T2> {
    fn vfmaddsub231pd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADDSUB231PS`.
pub trait Vfmaddsub231psEmitter<T0, T1, T2> {
    fn vfmaddsub231ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUB132PD`.
pub trait Vfmsub132pdEmitter<T0, T1, T2> {
    fn vfmsub132pd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUB132PS`.
pub trait Vfmsub132psEmitter<T0, T1, T2> {
    fn vfmsub132ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUB132SD`.
pub trait Vfmsub132sdEmitter<T0, T1, T2> {
    fn vfmsub132sd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUB132SS`.
pub trait Vfmsub132ssEmitter<T0, T1, T2> {
    fn vfmsub132ss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUB213PD`.
pub trait Vfmsub213pdEmitter<T0, T1, T2> {
    fn vfmsub213pd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUB213PS`.
pub trait Vfmsub213psEmitter<T0, T1, T2> {
    fn vfmsub213ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUB213SD`.
pub trait Vfmsub213sdEmitter<T0, T1, T2> {
    fn vfmsub213sd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUB213SS`.
pub trait Vfmsub213ssEmitter<T0, T1, T2> {
    fn vfmsub213ss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUB231PD`.
pub trait Vfmsub231pdEmitter<T0, T1, T2> {
    fn vfmsub231pd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUB231PS`.
pub trait Vfmsub231psEmitter<T0, T1, T2> {
    fn vfmsub231ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUB231SD`.
pub trait Vfmsub231sdEmitter<T0, T1, T2> {
    fn vfmsub231sd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUB231SS`.
pub trait Vfmsub231ssEmitter<T0, T1, T2> {
    fn vfmsub231ss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUBADD132PD`.
pub trait Vfmsubadd132pdEmitter<T0, T1, T2> {
    fn vfmsubadd132pd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUBADD132PS`.
pub trait Vfmsubadd132psEmitter<T0, T1, T2> {
    fn vfmsubadd132ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUBADD213PD`.
pub trait Vfmsubadd213pdEmitter<T0, T1, T2> {
    fn vfmsubadd213pd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUBADD213PS`.
pub trait Vfmsubadd213psEmitter<T0, T1, T2> {
    fn vfmsubadd213ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUBADD231PD`.
pub trait Vfmsubadd231pdEmitter<T0, T1, T2> {
    fn vfmsubadd231pd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUBADD231PS`.
pub trait Vfmsubadd231psEmitter<T0, T1, T2> {
    fn vfmsubadd231ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMADD132PD`.
pub trait Vfnmadd132pdEmitter<T0, T1, T2> {
    fn vfnmadd132pd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMADD132PS`.
pub trait Vfnmadd132psEmitter<T0, T1, T2> {
    fn vfnmadd132ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMADD132SD`.
pub trait Vfnmadd132sdEmitter<T0, T1, T2> {
    fn vfnmadd132sd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMADD132SS`.
pub trait Vfnmadd132ssEmitter<T0, T1, T2> {
    fn vfnmadd132ss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMADD213PD`.
pub trait Vfnmadd213pdEmitter<T0, T1, T2> {
    fn vfnmadd213pd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMADD213PS`.
pub trait Vfnmadd213psEmitter<T0, T1, T2> {
    fn vfnmadd213ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMADD213SD`.
pub trait Vfnmadd213sdEmitter<T0, T1, T2> {
    fn vfnmadd213sd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMADD213SS`.
pub trait Vfnmadd213ssEmitter<T0, T1, T2> {
    fn vfnmadd213ss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMADD231PD`.
pub trait Vfnmadd231pdEmitter<T0, T1, T2> {
    fn vfnmadd231pd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMADD231PS`.
pub trait Vfnmadd231psEmitter<T0, T1, T2> {
    fn vfnmadd231ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMADD231SD`.
pub trait Vfnmadd231sdEmitter<T0, T1, T2> {
    fn vfnmadd231sd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMADD231SS`.
pub trait Vfnmadd231ssEmitter<T0, T1, T2> {
    fn vfnmadd231ss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMSUB132PD`.
pub trait Vfnmsub132pdEmitter<T0, T1, T2> {
    fn vfnmsub132pd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMSUB132PS`.
pub trait Vfnmsub132psEmitter<T0, T1, T2> {
    fn vfnmsub132ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMSUB132SD`.
pub trait Vfnmsub132sdEmitter<T0, T1, T2> {
    fn vfnmsub132sd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMSUB132SS`.
pub trait Vfnmsub132ssEmitter<T0, T1, T2> {
    fn vfnmsub132ss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMSUB213PD`.
pub trait Vfnmsub213pdEmitter<T0, T1, T2> {
    fn vfnmsub213pd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMSUB213PS`.
pub trait Vfnmsub213psEmitter<T0, T1, T2> {
    fn vfnmsub213ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMSUB213SD`.
pub trait Vfnmsub213sdEmitter<T0, T1, T2> {
    fn vfnmsub213sd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMSUB213SS`.
pub trait Vfnmsub213ssEmitter<T0, T1, T2> {
    fn vfnmsub213ss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMSUB231PD`.
pub trait Vfnmsub231pdEmitter<T0, T1, T2> {
    fn vfnmsub231pd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMSUB231PS`.
pub trait Vfnmsub231psEmitter<T0, T1, T2> {
    fn vfnmsub231ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMSUB231SD`.
pub trait Vfnmsub231sdEmitter<T0, T1, T2> {
    fn vfnmsub231sd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMSUB231SS`.
pub trait Vfnmsub231ssEmitter<T0, T1, T2> {
    fn vfnmsub231ss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFPCLASSPD`.
pub trait VfpclasspdEmitter<T0, T1, T2> {
    fn vfpclasspd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFPCLASSPS`.
pub trait VfpclasspsEmitter<T0, T1, T2> {
    fn vfpclassps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFPCLASSSD`.
pub trait VfpclasssdEmitter<T0, T1, T2> {
    fn vfpclasssd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFPCLASSSS`.
pub trait VfpclassssEmitter<T0, T1, T2> {
    fn vfpclassss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VGATHERDPD`.
pub trait VgatherdpdEmitter<T0, T1> {
    fn vgatherdpd(&mut self, op0: T0, op1: T1);
}

/// Emits `VGATHERDPD_3`.
pub trait Vgatherdpd3Emitter<T0, T1, T2> {
    fn vgatherdpd_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VGATHERDPS`.
pub trait VgatherdpsEmitter<T0, T1> {
    fn vgatherdps(&mut self, op0: T0, op1: T1);
}

/// Emits `VGATHERDPS_3`.
pub trait Vgatherdps3Emitter<T0, T1, T2> {
    fn vgatherdps_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VGATHERQPD`.
pub trait VgatherqpdEmitter<T0, T1> {
    fn vgatherqpd(&mut self, op0: T0, op1: T1);
}

/// Emits `VGATHERQPD_3`.
pub trait Vgatherqpd3Emitter<T0, T1, T2> {
    fn vgatherqpd_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VGATHERQPS`.
pub trait VgatherqpsEmitter<T0, T1> {
    fn vgatherqps(&mut self, op0: T0, op1: T1);
}

/// Emits `VGATHERQPS_3`.
pub trait Vgatherqps3Emitter<T0, T1, T2> {
    fn vgatherqps_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VGETEXPPD`.
pub trait VgetexppdEmitter<T0, T1> {
    fn vgetexppd(&mut self, op0: T0, op1: T1);
}

/// Emits `VGETEXPPS`.
pub trait VgetexppsEmitter<T0, T1> {
    fn vgetexpps(&mut self, op0: T0, op1: T1);
}

/// Emits `VGETEXPSD`.
pub trait VgetexpsdEmitter<T0, T1, T2> {
    fn vgetexpsd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VGETEXPSS`.
pub trait VgetexpssEmitter<T0, T1, T2> {
    fn vgetexpss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VGETMANTPD`.
pub trait VgetmantpdEmitter<T0, T1, T2> {
    fn vgetmantpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VGETMANTPS`.
pub trait VgetmantpsEmitter<T0, T1, T2> {
    fn vgetmantps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VGETMANTSD`.
pub trait VgetmantsdEmitter<T0, T1, T2, T3> {
    fn vgetmantsd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VGETMANTSS`.
pub trait VgetmantssEmitter<T0, T1, T2, T3> {
    fn vgetmantss(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VGF2P8AFFINEINVQB`.
pub trait Vgf2p8affineinvqbEmitter<T0, T1, T2, T3> {
    fn vgf2p8affineinvqb(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VGF2P8AFFINEQB`.
pub trait Vgf2p8affineqbEmitter<T0, T1, T2, T3> {
    fn vgf2p8affineqb(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VGF2P8MULB`.
pub trait Vgf2p8mulbEmitter<T0, T1, T2> {
    fn vgf2p8mulb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VHADDPD`.
pub trait VhaddpdEmitter<T0, T1, T2> {
    fn vhaddpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VHADDPS`.
pub trait VhaddpsEmitter<T0, T1, T2> {
    fn vhaddps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VHSUBPD`.
pub trait VhsubpdEmitter<T0, T1, T2> {
    fn vhsubpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VHSUBPS`.
pub trait VhsubpsEmitter<T0, T1, T2> {
    fn vhsubps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VINSERTF128`.
pub trait Vinsertf128Emitter<T0, T1, T2, T3> {
    fn vinsertf128(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VINSERTF32X4`.
pub trait Vinsertf32x4Emitter<T0, T1, T2, T3> {
    fn vinsertf32x4(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VINSERTF32X8`.
pub trait Vinsertf32x8Emitter<T0, T1, T2, T3> {
    fn vinsertf32x8(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VINSERTF64X2`.
pub trait Vinsertf64x2Emitter<T0, T1, T2, T3> {
    fn vinsertf64x2(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VINSERTF64X4`.
pub trait Vinsertf64x4Emitter<T0, T1, T2, T3> {
    fn vinsertf64x4(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VINSERTI128`.
pub trait Vinserti128Emitter<T0, T1, T2, T3> {
    fn vinserti128(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VINSERTI32X4`.
pub trait Vinserti32x4Emitter<T0, T1, T2, T3> {
    fn vinserti32x4(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VINSERTI32X8`.
pub trait Vinserti32x8Emitter<T0, T1, T2, T3> {
    fn vinserti32x8(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VINSERTI64X2`.
pub trait Vinserti64x2Emitter<T0, T1, T2, T3> {
    fn vinserti64x2(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VINSERTI64X4`.
pub trait Vinserti64x4Emitter<T0, T1, T2, T3> {
    fn vinserti64x4(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VINSERTPS`.
pub trait VinsertpsEmitter<T0, T1, T2, T3> {
    fn vinsertps(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VLDDQU`.
pub trait VlddquEmitter<T0, T1> {
    fn vlddqu(&mut self, op0: T0, op1: T1);
}

/// Emits `VLDMXCSR`.
pub trait VldmxcsrEmitter<T0> {
    fn vldmxcsr(&mut self, op0: T0);
}

/// Emits `VMASKMOVDQU`.
pub trait VmaskmovdquEmitter<T0, T1, T2> {
    fn vmaskmovdqu(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMASKMOVPD`.
pub trait VmaskmovpdEmitter<T0, T1, T2> {
    fn vmaskmovpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMASKMOVPS`.
pub trait VmaskmovpsEmitter<T0, T1, T2> {
    fn vmaskmovps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMAXPD`.
pub trait VmaxpdEmitter<T0, T1, T2> {
    fn vmaxpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMAXPS`.
pub trait VmaxpsEmitter<T0, T1, T2> {
    fn vmaxps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMAXSD`.
pub trait VmaxsdEmitter<T0, T1, T2> {
    fn vmaxsd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMAXSS`.
pub trait VmaxssEmitter<T0, T1, T2> {
    fn vmaxss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMINPD`.
pub trait VminpdEmitter<T0, T1, T2> {
    fn vminpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMINPS`.
pub trait VminpsEmitter<T0, T1, T2> {
    fn vminps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMINSD`.
pub trait VminsdEmitter<T0, T1, T2> {
    fn vminsd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMINSS`.
pub trait VminssEmitter<T0, T1, T2> {
    fn vminss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMOVAPD`.
pub trait VmovapdEmitter<T0, T1> {
    fn vmovapd(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVAPS`.
pub trait VmovapsEmitter<T0, T1> {
    fn vmovaps(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVD`.
pub trait VmovdEmitter<T0, T1> {
    fn vmovd(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVDDUP`.
pub trait VmovddupEmitter<T0, T1> {
    fn vmovddup(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVDQA`.
pub trait VmovdqaEmitter<T0, T1> {
    fn vmovdqa(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVDQA32`.
pub trait Vmovdqa32Emitter<T0, T1> {
    fn vmovdqa32(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVDQA64`.
pub trait Vmovdqa64Emitter<T0, T1> {
    fn vmovdqa64(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVDQU`.
pub trait VmovdquEmitter<T0, T1> {
    fn vmovdqu(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVDQU16`.
pub trait Vmovdqu16Emitter<T0, T1> {
    fn vmovdqu16(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVDQU32`.
pub trait Vmovdqu32Emitter<T0, T1> {
    fn vmovdqu32(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVDQU64`.
pub trait Vmovdqu64Emitter<T0, T1> {
    fn vmovdqu64(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVDQU8`.
pub trait Vmovdqu8Emitter<T0, T1> {
    fn vmovdqu8(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVHLPS`.
pub trait VmovhlpsEmitter<T0, T1, T2> {
    fn vmovhlps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMOVHPD`.
pub trait VmovhpdEmitter<T0, T1> {
    fn vmovhpd(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVHPD_3`.
pub trait Vmovhpd3Emitter<T0, T1, T2> {
    fn vmovhpd_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMOVHPS`.
pub trait VmovhpsEmitter<T0, T1> {
    fn vmovhps(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVHPS_3`.
pub trait Vmovhps3Emitter<T0, T1, T2> {
    fn vmovhps_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMOVLHPS`.
pub trait VmovlhpsEmitter<T0, T1, T2> {
    fn vmovlhps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMOVLPD`.
pub trait VmovlpdEmitter<T0, T1> {
    fn vmovlpd(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVLPD_3`.
pub trait Vmovlpd3Emitter<T0, T1, T2> {
    fn vmovlpd_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMOVLPS`.
pub trait VmovlpsEmitter<T0, T1> {
    fn vmovlps(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVLPS_3`.
pub trait Vmovlps3Emitter<T0, T1, T2> {
    fn vmovlps_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMOVMSKPD`.
pub trait VmovmskpdEmitter<T0, T1> {
    fn vmovmskpd(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVMSKPS`.
pub trait VmovmskpsEmitter<T0, T1> {
    fn vmovmskps(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVNTDQ`.
pub trait VmovntdqEmitter<T0, T1> {
    fn vmovntdq(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVNTDQA`.
pub trait VmovntdqaEmitter<T0, T1> {
    fn vmovntdqa(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVNTPD`.
pub trait VmovntpdEmitter<T0, T1> {
    fn vmovntpd(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVNTPS`.
pub trait VmovntpsEmitter<T0, T1> {
    fn vmovntps(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVQ`.
pub trait VmovqEmitter<T0, T1> {
    fn vmovq(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVSD`.
pub trait VmovsdEmitter<T0, T1> {
    fn vmovsd(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVSD_3`.
pub trait Vmovsd3Emitter<T0, T1, T2> {
    fn vmovsd_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMOVSHDUP`.
pub trait VmovshdupEmitter<T0, T1> {
    fn vmovshdup(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVSLDUP`.
pub trait VmovsldupEmitter<T0, T1> {
    fn vmovsldup(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVSS`.
pub trait VmovssEmitter<T0, T1> {
    fn vmovss(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVSS_3`.
pub trait Vmovss3Emitter<T0, T1, T2> {
    fn vmovss_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMOVUPD`.
pub trait VmovupdEmitter<T0, T1> {
    fn vmovupd(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVUPS`.
pub trait VmovupsEmitter<T0, T1> {
    fn vmovups(&mut self, op0: T0, op1: T1);
}

/// Emits `VMPSADBW`.
pub trait VmpsadbwEmitter<T0, T1, T2, T3> {
    fn vmpsadbw(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VMULPD`.
pub trait VmulpdEmitter<T0, T1, T2> {
    fn vmulpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMULPS`.
pub trait VmulpsEmitter<T0, T1, T2> {
    fn vmulps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMULSD`.
pub trait VmulsdEmitter<T0, T1, T2> {
    fn vmulsd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMULSS`.
pub trait VmulssEmitter<T0, T1, T2> {
    fn vmulss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VORPD`.
pub trait VorpdEmitter<T0, T1, T2> {
    fn vorpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VORPS`.
pub trait VorpsEmitter<T0, T1, T2> {
    fn vorps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VP2INTERSECTD`.
pub trait Vp2intersectdEmitter<T0, T1, T2, T3> {
    fn vp2intersectd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VP2INTERSECTQ`.
pub trait Vp2intersectqEmitter<T0, T1, T2, T3> {
    fn vp2intersectq(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPABSB`.
pub trait VpabsbEmitter<T0, T1> {
    fn vpabsb(&mut self, op0: T0, op1: T1);
}

/// Emits `VPABSD`.
pub trait VpabsdEmitter<T0, T1> {
    fn vpabsd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPABSQ`.
pub trait VpabsqEmitter<T0, T1> {
    fn vpabsq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPABSW`.
pub trait VpabswEmitter<T0, T1> {
    fn vpabsw(&mut self, op0: T0, op1: T1);
}

/// Emits `VPACKSSDW`.
pub trait VpackssdwEmitter<T0, T1, T2> {
    fn vpackssdw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPACKSSWB`.
pub trait VpacksswbEmitter<T0, T1, T2> {
    fn vpacksswb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPACKUSDW`.
pub trait VpackusdwEmitter<T0, T1, T2> {
    fn vpackusdw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPACKUSWB`.
pub trait VpackuswbEmitter<T0, T1, T2> {
    fn vpackuswb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPADDB`.
pub trait VpaddbEmitter<T0, T1, T2> {
    fn vpaddb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPADDD`.
pub trait VpadddEmitter<T0, T1, T2> {
    fn vpaddd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPADDQ`.
pub trait VpaddqEmitter<T0, T1, T2> {
    fn vpaddq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPADDSB`.
pub trait VpaddsbEmitter<T0, T1, T2> {
    fn vpaddsb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPADDSW`.
pub trait VpaddswEmitter<T0, T1, T2> {
    fn vpaddsw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPADDUSB`.
pub trait VpaddusbEmitter<T0, T1, T2> {
    fn vpaddusb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPADDUSW`.
pub trait VpadduswEmitter<T0, T1, T2> {
    fn vpaddusw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPADDW`.
pub trait VpaddwEmitter<T0, T1, T2> {
    fn vpaddw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPALIGNR`.
pub trait VpalignrEmitter<T0, T1, T2, T3> {
    fn vpalignr(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPAND`.
pub trait VpandEmitter<T0, T1, T2> {
    fn vpand(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPANDD`.
pub trait VpanddEmitter<T0, T1, T2> {
    fn vpandd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPANDN`.
pub trait VpandnEmitter<T0, T1, T2> {
    fn vpandn(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPANDND`.
pub trait VpandndEmitter<T0, T1, T2> {
    fn vpandnd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPANDNQ`.
pub trait VpandnqEmitter<T0, T1, T2> {
    fn vpandnq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPANDQ`.
pub trait VpandqEmitter<T0, T1, T2> {
    fn vpandq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPAVGB`.
pub trait VpavgbEmitter<T0, T1, T2> {
    fn vpavgb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPAVGW`.
pub trait VpavgwEmitter<T0, T1, T2> {
    fn vpavgw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPBLENDD`.
pub trait VpblenddEmitter<T0, T1, T2, T3> {
    fn vpblendd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPBLENDMB`.
pub trait VpblendmbEmitter<T0, T1, T2> {
    fn vpblendmb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPBLENDMD`.
pub trait VpblendmdEmitter<T0, T1, T2> {
    fn vpblendmd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPBLENDMQ`.
pub trait VpblendmqEmitter<T0, T1, T2> {
    fn vpblendmq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPBLENDMW`.
pub trait VpblendmwEmitter<T0, T1, T2> {
    fn vpblendmw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPBLENDVB`.
pub trait VpblendvbEmitter<T0, T1, T2, T3> {
    fn vpblendvb(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPBLENDW`.
pub trait VpblendwEmitter<T0, T1, T2, T3> {
    fn vpblendw(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPBROADCASTB`.
pub trait VpbroadcastbEmitter<T0, T1> {
    fn vpbroadcastb(&mut self, op0: T0, op1: T1);
}

/// Emits `VPBROADCASTD`.
pub trait VpbroadcastdEmitter<T0, T1> {
    fn vpbroadcastd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPBROADCASTMB2Q`.
pub trait Vpbroadcastmb2qEmitter<T0, T1> {
    fn vpbroadcastmb2q(&mut self, op0: T0, op1: T1);
}

/// Emits `VPBROADCASTMW2D`.
pub trait Vpbroadcastmw2dEmitter<T0, T1> {
    fn vpbroadcastmw2d(&mut self, op0: T0, op1: T1);
}

/// Emits `VPBROADCASTQ`.
pub trait VpbroadcastqEmitter<T0, T1> {
    fn vpbroadcastq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPBROADCASTW`.
pub trait VpbroadcastwEmitter<T0, T1> {
    fn vpbroadcastw(&mut self, op0: T0, op1: T1);
}

/// Emits `VPCLMULQDQ`.
pub trait VpclmulqdqEmitter<T0, T1, T2, T3> {
    fn vpclmulqdq(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPCMPB`.
pub trait VpcmpbEmitter<T0, T1, T2, T3> {
    fn vpcmpb(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPCMPD`.
pub trait VpcmpdEmitter<T0, T1, T2, T3> {
    fn vpcmpd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPCMPEQB`.
pub trait VpcmpeqbEmitter<T0, T1, T2> {
    fn vpcmpeqb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPCMPEQD`.
pub trait VpcmpeqdEmitter<T0, T1, T2> {
    fn vpcmpeqd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPCMPEQQ`.
pub trait VpcmpeqqEmitter<T0, T1, T2> {
    fn vpcmpeqq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPCMPEQW`.
pub trait VpcmpeqwEmitter<T0, T1, T2> {
    fn vpcmpeqw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPCMPESTRI`.
pub trait VpcmpestriEmitter<T0, T1, T2, T3, T4, T5> {
    fn vpcmpestri(&mut self, op0: T0, op1: T1, op2: T2, op3: T3, op4: T4, op5: T5);
}

/// Emits `VPCMPESTRM`.
pub trait VpcmpestrmEmitter<T0, T1, T2, T3, T4, T5> {
    fn vpcmpestrm(&mut self, op0: T0, op1: T1, op2: T2, op3: T3, op4: T4, op5: T5);
}

/// Emits `VPCMPGTB`.
pub trait VpcmpgtbEmitter<T0, T1, T2> {
    fn vpcmpgtb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPCMPGTD`.
pub trait VpcmpgtdEmitter<T0, T1, T2> {
    fn vpcmpgtd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPCMPGTQ`.
pub trait VpcmpgtqEmitter<T0, T1, T2> {
    fn vpcmpgtq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPCMPGTW`.
pub trait VpcmpgtwEmitter<T0, T1, T2> {
    fn vpcmpgtw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPCMPISTRI`.
pub trait VpcmpistriEmitter<T0, T1, T2, T3> {
    fn vpcmpistri(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPCMPISTRM`.
pub trait VpcmpistrmEmitter<T0, T1, T2, T3> {
    fn vpcmpistrm(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPCMPQ`.
pub trait VpcmpqEmitter<T0, T1, T2, T3> {
    fn vpcmpq(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPCMPUB`.
pub trait VpcmpubEmitter<T0, T1, T2, T3> {
    fn vpcmpub(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPCMPUD`.
pub trait VpcmpudEmitter<T0, T1, T2, T3> {
    fn vpcmpud(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPCMPUQ`.
pub trait VpcmpuqEmitter<T0, T1, T2, T3> {
    fn vpcmpuq(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPCMPUW`.
pub trait VpcmpuwEmitter<T0, T1, T2, T3> {
    fn vpcmpuw(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPCMPW`.
pub trait VpcmpwEmitter<T0, T1, T2, T3> {
    fn vpcmpw(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPCOMPRESSB`.
pub trait VpcompressbEmitter<T0, T1> {
    fn vpcompressb(&mut self, op0: T0, op1: T1);
}

/// Emits `VPCOMPRESSD`.
pub trait VpcompressdEmitter<T0, T1> {
    fn vpcompressd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPCOMPRESSQ`.
pub trait VpcompressqEmitter<T0, T1> {
    fn vpcompressq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPCOMPRESSW`.
pub trait VpcompresswEmitter<T0, T1> {
    fn vpcompressw(&mut self, op0: T0, op1: T1);
}

/// Emits `VPCONFLICTD`.
pub trait VpconflictdEmitter<T0, T1> {
    fn vpconflictd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPCONFLICTQ`.
pub trait VpconflictqEmitter<T0, T1> {
    fn vpconflictq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPDPBUSD`.
pub trait VpdpbusdEmitter<T0, T1, T2> {
    fn vpdpbusd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPDPBUSDS`.
pub trait VpdpbusdsEmitter<T0, T1, T2> {
    fn vpdpbusds(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPDPWSSD`.
pub trait VpdpwssdEmitter<T0, T1, T2> {
    fn vpdpwssd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPDPWSSDS`.
pub trait VpdpwssdsEmitter<T0, T1, T2> {
    fn vpdpwssds(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPERM2F128`.
pub trait Vperm2f128Emitter<T0, T1, T2, T3> {
    fn vperm2f128(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPERM2I128`.
pub trait Vperm2i128Emitter<T0, T1, T2, T3> {
    fn vperm2i128(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPERMB`.
pub trait VpermbEmitter<T0, T1, T2> {
    fn vpermb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPERMD`.
pub trait VpermdEmitter<T0, T1, T2> {
    fn vpermd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPERMI2B`.
pub trait Vpermi2bEmitter<T0, T1, T2> {
    fn vpermi2b(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPERMI2D`.
pub trait Vpermi2dEmitter<T0, T1, T2> {
    fn vpermi2d(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPERMI2PD`.
pub trait Vpermi2pdEmitter<T0, T1, T2> {
    fn vpermi2pd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPERMI2PS`.
pub trait Vpermi2psEmitter<T0, T1, T2> {
    fn vpermi2ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPERMI2Q`.
pub trait Vpermi2qEmitter<T0, T1, T2> {
    fn vpermi2q(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPERMI2W`.
pub trait Vpermi2wEmitter<T0, T1, T2> {
    fn vpermi2w(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPERMILPD`.
pub trait VpermilpdEmitter<T0, T1, T2> {
    fn vpermilpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPERMILPS`.
pub trait VpermilpsEmitter<T0, T1, T2> {
    fn vpermilps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPERMPD`.
pub trait VpermpdEmitter<T0, T1, T2> {
    fn vpermpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPERMPS`.
pub trait VpermpsEmitter<T0, T1, T2> {
    fn vpermps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPERMQ`.
pub trait VpermqEmitter<T0, T1, T2> {
    fn vpermq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPERMT2B`.
pub trait Vpermt2bEmitter<T0, T1, T2> {
    fn vpermt2b(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPERMT2D`.
pub trait Vpermt2dEmitter<T0, T1, T2> {
    fn vpermt2d(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPERMT2PD`.
pub trait Vpermt2pdEmitter<T0, T1, T2> {
    fn vpermt2pd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPERMT2PS`.
pub trait Vpermt2psEmitter<T0, T1, T2> {
    fn vpermt2ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPERMT2Q`.
pub trait Vpermt2qEmitter<T0, T1, T2> {
    fn vpermt2q(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPERMT2W`.
pub trait Vpermt2wEmitter<T0, T1, T2> {
    fn vpermt2w(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPERMW`.
pub trait VpermwEmitter<T0, T1, T2> {
    fn vpermw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPEXPANDB`.
pub trait VpexpandbEmitter<T0, T1> {
    fn vpexpandb(&mut self, op0: T0, op1: T1);
}

/// Emits `VPEXPANDD`.
pub trait VpexpanddEmitter<T0, T1> {
    fn vpexpandd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPEXPANDQ`.
pub trait VpexpandqEmitter<T0, T1> {
    fn vpexpandq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPEXPANDW`.
pub trait VpexpandwEmitter<T0, T1> {
    fn vpexpandw(&mut self, op0: T0, op1: T1);
}

/// Emits `VPEXTRB`.
pub trait VpextrbEmitter<T0, T1, T2> {
    fn vpextrb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPEXTRD`.
pub trait VpextrdEmitter<T0, T1, T2> {
    fn vpextrd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPEXTRQ`.
pub trait VpextrqEmitter<T0, T1, T2> {
    fn vpextrq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPEXTRW`.
pub trait VpextrwEmitter<T0, T1, T2> {
    fn vpextrw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPGATHERDD`.
pub trait VpgatherddEmitter<T0, T1> {
    fn vpgatherdd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPGATHERDD_3`.
pub trait Vpgatherdd3Emitter<T0, T1, T2> {
    fn vpgatherdd_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPGATHERDQ`.
pub trait VpgatherdqEmitter<T0, T1> {
    fn vpgatherdq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPGATHERDQ_3`.
pub trait Vpgatherdq3Emitter<T0, T1, T2> {
    fn vpgatherdq_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPGATHERQD`.
pub trait VpgatherqdEmitter<T0, T1> {
    fn vpgatherqd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPGATHERQD_3`.
pub trait Vpgatherqd3Emitter<T0, T1, T2> {
    fn vpgatherqd_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPGATHERQQ`.
pub trait VpgatherqqEmitter<T0, T1> {
    fn vpgatherqq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPGATHERQQ_3`.
pub trait Vpgatherqq3Emitter<T0, T1, T2> {
    fn vpgatherqq_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPHADDD`.
pub trait VphadddEmitter<T0, T1, T2> {
    fn vphaddd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPHADDSW`.
pub trait VphaddswEmitter<T0, T1, T2> {
    fn vphaddsw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPHADDW`.
pub trait VphaddwEmitter<T0, T1, T2> {
    fn vphaddw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPHMINPOSUW`.
pub trait VphminposuwEmitter<T0, T1> {
    fn vphminposuw(&mut self, op0: T0, op1: T1);
}

/// Emits `VPHSUBD`.
pub trait VphsubdEmitter<T0, T1, T2> {
    fn vphsubd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPHSUBSW`.
pub trait VphsubswEmitter<T0, T1, T2> {
    fn vphsubsw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPHSUBW`.
pub trait VphsubwEmitter<T0, T1, T2> {
    fn vphsubw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPINSRB`.
pub trait VpinsrbEmitter<T0, T1, T2, T3> {
    fn vpinsrb(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPINSRD`.
pub trait VpinsrdEmitter<T0, T1, T2, T3> {
    fn vpinsrd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPINSRQ`.
pub trait VpinsrqEmitter<T0, T1, T2, T3> {
    fn vpinsrq(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPINSRW`.
pub trait VpinsrwEmitter<T0, T1, T2, T3> {
    fn vpinsrw(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPLZCNTD`.
pub trait VplzcntdEmitter<T0, T1> {
    fn vplzcntd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPLZCNTQ`.
pub trait VplzcntqEmitter<T0, T1> {
    fn vplzcntq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMADD52HUQ`.
pub trait Vpmadd52huqEmitter<T0, T1, T2> {
    fn vpmadd52huq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMADD52LUQ`.
pub trait Vpmadd52luqEmitter<T0, T1, T2> {
    fn vpmadd52luq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMADDUBSW`.
pub trait VpmaddubswEmitter<T0, T1, T2> {
    fn vpmaddubsw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMADDWD`.
pub trait VpmaddwdEmitter<T0, T1, T2> {
    fn vpmaddwd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMASKMOVD`.
pub trait VpmaskmovdEmitter<T0, T1, T2> {
    fn vpmaskmovd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMASKMOVQ`.
pub trait VpmaskmovqEmitter<T0, T1, T2> {
    fn vpmaskmovq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMAXSB`.
pub trait VpmaxsbEmitter<T0, T1, T2> {
    fn vpmaxsb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMAXSD`.
pub trait VpmaxsdEmitter<T0, T1, T2> {
    fn vpmaxsd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMAXSQ`.
pub trait VpmaxsqEmitter<T0, T1, T2> {
    fn vpmaxsq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMAXSW`.
pub trait VpmaxswEmitter<T0, T1, T2> {
    fn vpmaxsw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMAXUB`.
pub trait VpmaxubEmitter<T0, T1, T2> {
    fn vpmaxub(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMAXUD`.
pub trait VpmaxudEmitter<T0, T1, T2> {
    fn vpmaxud(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMAXUQ`.
pub trait VpmaxuqEmitter<T0, T1, T2> {
    fn vpmaxuq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMAXUW`.
pub trait VpmaxuwEmitter<T0, T1, T2> {
    fn vpmaxuw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMINSB`.
pub trait VpminsbEmitter<T0, T1, T2> {
    fn vpminsb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMINSD`.
pub trait VpminsdEmitter<T0, T1, T2> {
    fn vpminsd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMINSQ`.
pub trait VpminsqEmitter<T0, T1, T2> {
    fn vpminsq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMINSW`.
pub trait VpminswEmitter<T0, T1, T2> {
    fn vpminsw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMINUB`.
pub trait VpminubEmitter<T0, T1, T2> {
    fn vpminub(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMINUD`.
pub trait VpminudEmitter<T0, T1, T2> {
    fn vpminud(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMINUQ`.
pub trait VpminuqEmitter<T0, T1, T2> {
    fn vpminuq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMINUW`.
pub trait VpminuwEmitter<T0, T1, T2> {
    fn vpminuw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMOVB2M`.
pub trait Vpmovb2mEmitter<T0, T1> {
    fn vpmovb2m(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVD2M`.
pub trait Vpmovd2mEmitter<T0, T1> {
    fn vpmovd2m(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVDB`.
pub trait VpmovdbEmitter<T0, T1> {
    fn vpmovdb(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVDW`.
pub trait VpmovdwEmitter<T0, T1> {
    fn vpmovdw(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVM2B`.
pub trait Vpmovm2bEmitter<T0, T1> {
    fn vpmovm2b(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVM2D`.
pub trait Vpmovm2dEmitter<T0, T1> {
    fn vpmovm2d(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVM2Q`.
pub trait Vpmovm2qEmitter<T0, T1> {
    fn vpmovm2q(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVM2W`.
pub trait Vpmovm2wEmitter<T0, T1> {
    fn vpmovm2w(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVMSKB`.
pub trait VpmovmskbEmitter<T0, T1> {
    fn vpmovmskb(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVQ2M`.
pub trait Vpmovq2mEmitter<T0, T1> {
    fn vpmovq2m(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVQB`.
pub trait VpmovqbEmitter<T0, T1> {
    fn vpmovqb(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVQD`.
pub trait VpmovqdEmitter<T0, T1> {
    fn vpmovqd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVQW`.
pub trait VpmovqwEmitter<T0, T1> {
    fn vpmovqw(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVSDB`.
pub trait VpmovsdbEmitter<T0, T1> {
    fn vpmovsdb(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVSDW`.
pub trait VpmovsdwEmitter<T0, T1> {
    fn vpmovsdw(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVSQB`.
pub trait VpmovsqbEmitter<T0, T1> {
    fn vpmovsqb(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVSQD`.
pub trait VpmovsqdEmitter<T0, T1> {
    fn vpmovsqd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVSQW`.
pub trait VpmovsqwEmitter<T0, T1> {
    fn vpmovsqw(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVSWB`.
pub trait VpmovswbEmitter<T0, T1> {
    fn vpmovswb(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVSXBD`.
pub trait VpmovsxbdEmitter<T0, T1> {
    fn vpmovsxbd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVSXBQ`.
pub trait VpmovsxbqEmitter<T0, T1> {
    fn vpmovsxbq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVSXBW`.
pub trait VpmovsxbwEmitter<T0, T1> {
    fn vpmovsxbw(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVSXDQ`.
pub trait VpmovsxdqEmitter<T0, T1> {
    fn vpmovsxdq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVSXWD`.
pub trait VpmovsxwdEmitter<T0, T1> {
    fn vpmovsxwd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVSXWQ`.
pub trait VpmovsxwqEmitter<T0, T1> {
    fn vpmovsxwq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVUSDB`.
pub trait VpmovusdbEmitter<T0, T1> {
    fn vpmovusdb(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVUSDW`.
pub trait VpmovusdwEmitter<T0, T1> {
    fn vpmovusdw(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVUSQB`.
pub trait VpmovusqbEmitter<T0, T1> {
    fn vpmovusqb(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVUSQD`.
pub trait VpmovusqdEmitter<T0, T1> {
    fn vpmovusqd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVUSQW`.
pub trait VpmovusqwEmitter<T0, T1> {
    fn vpmovusqw(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVUSWB`.
pub trait VpmovuswbEmitter<T0, T1> {
    fn vpmovuswb(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVW2M`.
pub trait Vpmovw2mEmitter<T0, T1> {
    fn vpmovw2m(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVWB`.
pub trait VpmovwbEmitter<T0, T1> {
    fn vpmovwb(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVZXBD`.
pub trait VpmovzxbdEmitter<T0, T1> {
    fn vpmovzxbd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVZXBQ`.
pub trait VpmovzxbqEmitter<T0, T1> {
    fn vpmovzxbq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVZXBW`.
pub trait VpmovzxbwEmitter<T0, T1> {
    fn vpmovzxbw(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVZXDQ`.
pub trait VpmovzxdqEmitter<T0, T1> {
    fn vpmovzxdq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVZXWD`.
pub trait VpmovzxwdEmitter<T0, T1> {
    fn vpmovzxwd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMOVZXWQ`.
pub trait VpmovzxwqEmitter<T0, T1> {
    fn vpmovzxwq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMULDQ`.
pub trait VpmuldqEmitter<T0, T1, T2> {
    fn vpmuldq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMULHRSW`.
pub trait VpmulhrswEmitter<T0, T1, T2> {
    fn vpmulhrsw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMULHUW`.
pub trait VpmulhuwEmitter<T0, T1, T2> {
    fn vpmulhuw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMULHW`.
pub trait VpmulhwEmitter<T0, T1, T2> {
    fn vpmulhw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMULLD`.
pub trait VpmulldEmitter<T0, T1, T2> {
    fn vpmulld(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMULLQ`.
pub trait VpmullqEmitter<T0, T1, T2> {
    fn vpmullq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMULLW`.
pub trait VpmullwEmitter<T0, T1, T2> {
    fn vpmullw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMULTISHIFTQB`.
pub trait VpmultishiftqbEmitter<T0, T1, T2> {
    fn vpmultishiftqb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPMULUDQ`.
pub trait VpmuludqEmitter<T0, T1, T2> {
    fn vpmuludq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPOPCNTB`.
pub trait VpopcntbEmitter<T0, T1> {
    fn vpopcntb(&mut self, op0: T0, op1: T1);
}

/// Emits `VPOPCNTD`.
pub trait VpopcntdEmitter<T0, T1> {
    fn vpopcntd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPOPCNTQ`.
pub trait VpopcntqEmitter<T0, T1> {
    fn vpopcntq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPOPCNTW`.
pub trait VpopcntwEmitter<T0, T1> {
    fn vpopcntw(&mut self, op0: T0, op1: T1);
}

/// Emits `VPOR`.
pub trait VporEmitter<T0, T1, T2> {
    fn vpor(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPORD`.
pub trait VpordEmitter<T0, T1, T2> {
    fn vpord(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPORQ`.
pub trait VporqEmitter<T0, T1, T2> {
    fn vporq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPROLD`.
pub trait VproldEmitter<T0, T1, T2> {
    fn vprold(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPROLQ`.
pub trait VprolqEmitter<T0, T1, T2> {
    fn vprolq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPROLVD`.
pub trait VprolvdEmitter<T0, T1, T2> {
    fn vprolvd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPROLVQ`.
pub trait VprolvqEmitter<T0, T1, T2> {
    fn vprolvq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPRORD`.
pub trait VprordEmitter<T0, T1, T2> {
    fn vprord(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPRORQ`.
pub trait VprorqEmitter<T0, T1, T2> {
    fn vprorq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPRORVD`.
pub trait VprorvdEmitter<T0, T1, T2> {
    fn vprorvd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPRORVQ`.
pub trait VprorvqEmitter<T0, T1, T2> {
    fn vprorvq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSADBW`.
pub trait VpsadbwEmitter<T0, T1, T2> {
    fn vpsadbw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSCATTERDD`.
pub trait VpscatterddEmitter<T0, T1> {
    fn vpscatterdd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPSCATTERDQ`.
pub trait VpscatterdqEmitter<T0, T1> {
    fn vpscatterdq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPSCATTERQD`.
pub trait VpscatterqdEmitter<T0, T1> {
    fn vpscatterqd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPSCATTERQQ`.
pub trait VpscatterqqEmitter<T0, T1> {
    fn vpscatterqq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPSHLDD`.
pub trait VpshlddEmitter<T0, T1, T2, T3> {
    fn vpshldd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPSHLDQ`.
pub trait VpshldqEmitter<T0, T1, T2, T3> {
    fn vpshldq(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPSHLDVD`.
pub trait VpshldvdEmitter<T0, T1, T2> {
    fn vpshldvd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSHLDVQ`.
pub trait VpshldvqEmitter<T0, T1, T2> {
    fn vpshldvq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSHLDVW`.
pub trait VpshldvwEmitter<T0, T1, T2> {
    fn vpshldvw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSHLDW`.
pub trait VpshldwEmitter<T0, T1, T2, T3> {
    fn vpshldw(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPSHRDD`.
pub trait VpshrddEmitter<T0, T1, T2, T3> {
    fn vpshrdd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPSHRDQ`.
pub trait VpshrdqEmitter<T0, T1, T2, T3> {
    fn vpshrdq(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPSHRDVD`.
pub trait VpshrdvdEmitter<T0, T1, T2> {
    fn vpshrdvd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSHRDVQ`.
pub trait VpshrdvqEmitter<T0, T1, T2> {
    fn vpshrdvq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSHRDVW`.
pub trait VpshrdvwEmitter<T0, T1, T2> {
    fn vpshrdvw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSHRDW`.
pub trait VpshrdwEmitter<T0, T1, T2, T3> {
    fn vpshrdw(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPSHUFB`.
pub trait VpshufbEmitter<T0, T1, T2> {
    fn vpshufb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSHUFBITQMB`.
pub trait VpshufbitqmbEmitter<T0, T1, T2> {
    fn vpshufbitqmb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSHUFD`.
pub trait VpshufdEmitter<T0, T1, T2> {
    fn vpshufd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSHUFHW`.
pub trait VpshufhwEmitter<T0, T1, T2> {
    fn vpshufhw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSHUFLW`.
pub trait VpshuflwEmitter<T0, T1, T2> {
    fn vpshuflw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSIGNB`.
pub trait VpsignbEmitter<T0, T1, T2> {
    fn vpsignb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSIGND`.
pub trait VpsigndEmitter<T0, T1, T2> {
    fn vpsignd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSIGNW`.
pub trait VpsignwEmitter<T0, T1, T2> {
    fn vpsignw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSLLD`.
pub trait VpslldEmitter<T0, T1, T2> {
    fn vpslld(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSLLDQ`.
pub trait VpslldqEmitter<T0, T1, T2> {
    fn vpslldq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSLLQ`.
pub trait VpsllqEmitter<T0, T1, T2> {
    fn vpsllq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSLLVD`.
pub trait VpsllvdEmitter<T0, T1, T2> {
    fn vpsllvd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSLLVQ`.
pub trait VpsllvqEmitter<T0, T1, T2> {
    fn vpsllvq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSLLVW`.
pub trait VpsllvwEmitter<T0, T1, T2> {
    fn vpsllvw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSLLW`.
pub trait VpsllwEmitter<T0, T1, T2> {
    fn vpsllw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSRAD`.
pub trait VpsradEmitter<T0, T1, T2> {
    fn vpsrad(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSRAQ`.
pub trait VpsraqEmitter<T0, T1, T2> {
    fn vpsraq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSRAVD`.
pub trait VpsravdEmitter<T0, T1, T2> {
    fn vpsravd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSRAVQ`.
pub trait VpsravqEmitter<T0, T1, T2> {
    fn vpsravq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSRAVW`.
pub trait VpsravwEmitter<T0, T1, T2> {
    fn vpsravw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSRAW`.
pub trait VpsrawEmitter<T0, T1, T2> {
    fn vpsraw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSRLD`.
pub trait VpsrldEmitter<T0, T1, T2> {
    fn vpsrld(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSRLDQ`.
pub trait VpsrldqEmitter<T0, T1, T2> {
    fn vpsrldq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSRLQ`.
pub trait VpsrlqEmitter<T0, T1, T2> {
    fn vpsrlq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSRLVD`.
pub trait VpsrlvdEmitter<T0, T1, T2> {
    fn vpsrlvd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSRLVQ`.
pub trait VpsrlvqEmitter<T0, T1, T2> {
    fn vpsrlvq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSRLVW`.
pub trait VpsrlvwEmitter<T0, T1, T2> {
    fn vpsrlvw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSRLW`.
pub trait VpsrlwEmitter<T0, T1, T2> {
    fn vpsrlw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSUBB`.
pub trait VpsubbEmitter<T0, T1, T2> {
    fn vpsubb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSUBD`.
pub trait VpsubdEmitter<T0, T1, T2> {
    fn vpsubd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSUBQ`.
pub trait VpsubqEmitter<T0, T1, T2> {
    fn vpsubq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSUBSB`.
pub trait VpsubsbEmitter<T0, T1, T2> {
    fn vpsubsb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSUBSW`.
pub trait VpsubswEmitter<T0, T1, T2> {
    fn vpsubsw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSUBUSB`.
pub trait VpsubusbEmitter<T0, T1, T2> {
    fn vpsubusb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSUBUSW`.
pub trait VpsubuswEmitter<T0, T1, T2> {
    fn vpsubusw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSUBW`.
pub trait VpsubwEmitter<T0, T1, T2> {
    fn vpsubw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPTERNLOGD`.
pub trait VpternlogdEmitter<T0, T1, T2, T3> {
    fn vpternlogd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPTERNLOGQ`.
pub trait VpternlogqEmitter<T0, T1, T2, T3> {
    fn vpternlogq(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPTEST`.
pub trait VptestEmitter<T0, T1> {
    fn vptest(&mut self, op0: T0, op1: T1);
}

/// Emits `VPTESTMB`.
pub trait VptestmbEmitter<T0, T1, T2> {
    fn vptestmb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPTESTMD`.
pub trait VptestmdEmitter<T0, T1, T2> {
    fn vptestmd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPTESTMQ`.
pub trait VptestmqEmitter<T0, T1, T2> {
    fn vptestmq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPTESTMW`.
pub trait VptestmwEmitter<T0, T1, T2> {
    fn vptestmw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPTESTNMB`.
pub trait VptestnmbEmitter<T0, T1, T2> {
    fn vptestnmb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPTESTNMD`.
pub trait VptestnmdEmitter<T0, T1, T2> {
    fn vptestnmd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPTESTNMQ`.
pub trait VptestnmqEmitter<T0, T1, T2> {
    fn vptestnmq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPTESTNMW`.
pub trait VptestnmwEmitter<T0, T1, T2> {
    fn vptestnmw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPUNPCKHBW`.
pub trait VpunpckhbwEmitter<T0, T1, T2> {
    fn vpunpckhbw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPUNPCKHDQ`.
pub trait VpunpckhdqEmitter<T0, T1, T2> {
    fn vpunpckhdq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPUNPCKHQDQ`.
pub trait VpunpckhqdqEmitter<T0, T1, T2> {
    fn vpunpckhqdq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPUNPCKHWD`.
pub trait VpunpckhwdEmitter<T0, T1, T2> {
    fn vpunpckhwd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPUNPCKLBW`.
pub trait VpunpcklbwEmitter<T0, T1, T2> {
    fn vpunpcklbw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPUNPCKLDQ`.
pub trait VpunpckldqEmitter<T0, T1, T2> {
    fn vpunpckldq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPUNPCKLQDQ`.
pub trait VpunpcklqdqEmitter<T0, T1, T2> {
    fn vpunpcklqdq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPUNPCKLWD`.
pub trait VpunpcklwdEmitter<T0, T1, T2> {
    fn vpunpcklwd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPXOR`.
pub trait VpxorEmitter<T0, T1, T2> {
    fn vpxor(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPXORD`.
pub trait VpxordEmitter<T0, T1, T2> {
    fn vpxord(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPXORQ`.
pub trait VpxorqEmitter<T0, T1, T2> {
    fn vpxorq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VRANGEPD`.
pub trait VrangepdEmitter<T0, T1, T2, T3> {
    fn vrangepd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VRANGEPS`.
pub trait VrangepsEmitter<T0, T1, T2, T3> {
    fn vrangeps(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VRANGESD`.
pub trait VrangesdEmitter<T0, T1, T2, T3> {
    fn vrangesd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VRANGESS`.
pub trait VrangessEmitter<T0, T1, T2, T3> {
    fn vrangess(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VRCP14PD`.
pub trait Vrcp14pdEmitter<T0, T1> {
    fn vrcp14pd(&mut self, op0: T0, op1: T1);
}

/// Emits `VRCP14PS`.
pub trait Vrcp14psEmitter<T0, T1> {
    fn vrcp14ps(&mut self, op0: T0, op1: T1);
}

/// Emits `VRCP14SD`.
pub trait Vrcp14sdEmitter<T0, T1, T2> {
    fn vrcp14sd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VRCP14SS`.
pub trait Vrcp14ssEmitter<T0, T1, T2> {
    fn vrcp14ss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VRCPPS`.
pub trait VrcppsEmitter<T0, T1> {
    fn vrcpps(&mut self, op0: T0, op1: T1);
}

/// Emits `VRCPSS`.
pub trait VrcpssEmitter<T0, T1, T2> {
    fn vrcpss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VREDUCEPD`.
pub trait VreducepdEmitter<T0, T1, T2> {
    fn vreducepd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VREDUCEPS`.
pub trait VreducepsEmitter<T0, T1, T2> {
    fn vreduceps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VREDUCESD`.
pub trait VreducesdEmitter<T0, T1, T2, T3> {
    fn vreducesd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VREDUCESS`.
pub trait VreducessEmitter<T0, T1, T2, T3> {
    fn vreducess(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VRNDSCALEPD`.
pub trait VrndscalepdEmitter<T0, T1, T2> {
    fn vrndscalepd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VRNDSCALEPS`.
pub trait VrndscalepsEmitter<T0, T1, T2> {
    fn vrndscaleps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VRNDSCALESD`.
pub trait VrndscalesdEmitter<T0, T1, T2, T3> {
    fn vrndscalesd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VRNDSCALESS`.
pub trait VrndscalessEmitter<T0, T1, T2, T3> {
    fn vrndscaless(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VROUNDPD`.
pub trait VroundpdEmitter<T0, T1, T2> {
    fn vroundpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VROUNDPS`.
pub trait VroundpsEmitter<T0, T1, T2> {
    fn vroundps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VROUNDSD`.
pub trait VroundsdEmitter<T0, T1, T2, T3> {
    fn vroundsd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VROUNDSS`.
pub trait VroundssEmitter<T0, T1, T2, T3> {
    fn vroundss(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VRSQRT14PD`.
pub trait Vrsqrt14pdEmitter<T0, T1> {
    fn vrsqrt14pd(&mut self, op0: T0, op1: T1);
}

/// Emits `VRSQRT14PS`.
pub trait Vrsqrt14psEmitter<T0, T1> {
    fn vrsqrt14ps(&mut self, op0: T0, op1: T1);
}

/// Emits `VRSQRT14SD`.
pub trait Vrsqrt14sdEmitter<T0, T1, T2> {
    fn vrsqrt14sd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VRSQRT14SS`.
pub trait Vrsqrt14ssEmitter<T0, T1, T2> {
    fn vrsqrt14ss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VRSQRTPS`.
pub trait VrsqrtpsEmitter<T0, T1> {
    fn vrsqrtps(&mut self, op0: T0, op1: T1);
}

/// Emits `VRSQRTSS`.
pub trait VrsqrtssEmitter<T0, T1, T2> {
    fn vrsqrtss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VSCALEFPD`.
pub trait VscalefpdEmitter<T0, T1, T2> {
    fn vscalefpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VSCALEFPS`.
pub trait VscalefpsEmitter<T0, T1, T2> {
    fn vscalefps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VSCALEFSD`.
pub trait VscalefsdEmitter<T0, T1, T2> {
    fn vscalefsd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VSCALEFSS`.
pub trait VscalefssEmitter<T0, T1, T2> {
    fn vscalefss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VSCATTERDPD`.
pub trait VscatterdpdEmitter<T0, T1> {
    fn vscatterdpd(&mut self, op0: T0, op1: T1);
}

/// Emits `VSCATTERDPS`.
pub trait VscatterdpsEmitter<T0, T1> {
    fn vscatterdps(&mut self, op0: T0, op1: T1);
}

/// Emits `VSCATTERQPD`.
pub trait VscatterqpdEmitter<T0, T1> {
    fn vscatterqpd(&mut self, op0: T0, op1: T1);
}

/// Emits `VSCATTERQPS`.
pub trait VscatterqpsEmitter<T0, T1> {
    fn vscatterqps(&mut self, op0: T0, op1: T1);
}

/// Emits `VSHUFF32X4`.
pub trait Vshuff32x4Emitter<T0, T1, T2, T3> {
    fn vshuff32x4(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VSHUFF64X2`.
pub trait Vshuff64x2Emitter<T0, T1, T2, T3> {
    fn vshuff64x2(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VSHUFI32X4`.
pub trait Vshufi32x4Emitter<T0, T1, T2, T3> {
    fn vshufi32x4(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VSHUFI64X2`.
pub trait Vshufi64x2Emitter<T0, T1, T2, T3> {
    fn vshufi64x2(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VSHUFPD`.
pub trait VshufpdEmitter<T0, T1, T2, T3> {
    fn vshufpd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VSHUFPS`.
pub trait VshufpsEmitter<T0, T1, T2, T3> {
    fn vshufps(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VSQRTPD`.
pub trait VsqrtpdEmitter<T0, T1> {
    fn vsqrtpd(&mut self, op0: T0, op1: T1);
}

/// Emits `VSQRTPS`.
pub trait VsqrtpsEmitter<T0, T1> {
    fn vsqrtps(&mut self, op0: T0, op1: T1);
}

/// Emits `VSQRTSD`.
pub trait VsqrtsdEmitter<T0, T1, T2> {
    fn vsqrtsd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VSQRTSS`.
pub trait VsqrtssEmitter<T0, T1, T2> {
    fn vsqrtss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VSTMXCSR`.
pub trait VstmxcsrEmitter<T0> {
    fn vstmxcsr(&mut self, op0: T0);
}

/// Emits `VSUBPD`.
pub trait VsubpdEmitter<T0, T1, T2> {
    fn vsubpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VSUBPS`.
pub trait VsubpsEmitter<T0, T1, T2> {
    fn vsubps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VSUBSD`.
pub trait VsubsdEmitter<T0, T1, T2> {
    fn vsubsd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VSUBSS`.
pub trait VsubssEmitter<T0, T1, T2> {
    fn vsubss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VTESTPD`.
pub trait VtestpdEmitter<T0, T1> {
    fn vtestpd(&mut self, op0: T0, op1: T1);
}

/// Emits `VTESTPS`.
pub trait VtestpsEmitter<T0, T1> {
    fn vtestps(&mut self, op0: T0, op1: T1);
}

/// Emits `VUCOMISD`.
pub trait VucomisdEmitter<T0, T1> {
    fn vucomisd(&mut self, op0: T0, op1: T1);
}

/// Emits `VUCOMISS`.
pub trait VucomissEmitter<T0, T1> {
    fn vucomiss(&mut self, op0: T0, op1: T1);
}

/// Emits `VUNPCKHPD`.
pub trait VunpckhpdEmitter<T0, T1, T2> {
    fn vunpckhpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VUNPCKHPS`.
pub trait VunpckhpsEmitter<T0, T1, T2> {
    fn vunpckhps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VUNPCKLPD`.
pub trait VunpcklpdEmitter<T0, T1, T2> {
    fn vunpcklpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VUNPCKLPS`.
pub trait VunpcklpsEmitter<T0, T1, T2> {
    fn vunpcklps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VXORPD`.
pub trait VxorpdEmitter<T0, T1, T2> {
    fn vxorpd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VXORPS`.
pub trait VxorpsEmitter<T0, T1, T2> {
    fn vxorps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VZEROALL`.
pub trait VzeroallEmitter {
    fn vzeroall(&mut self);
}

/// Emits `VZEROUPPER`.
pub trait VzeroupperEmitter {
    fn vzeroupper(&mut self);
}

/// Emits `VFMADDPD`.
pub trait VfmaddpdEmitter<T0, T1, T2, T3> {
    fn vfmaddpd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFMADDPS`.
pub trait VfmaddpsEmitter<T0, T1, T2, T3> {
    fn vfmaddps(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFMADDSD`.
pub trait VfmaddsdEmitter<T0, T1, T2, T3> {
    fn vfmaddsd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFMADDSS`.
pub trait VfmaddssEmitter<T0, T1, T2, T3> {
    fn vfmaddss(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFMADDSUBPD`.
pub trait VfmaddsubpdEmitter<T0, T1, T2, T3> {
    fn vfmaddsubpd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFMADDSUBPS`.
pub trait VfmaddsubpsEmitter<T0, T1, T2, T3> {
    fn vfmaddsubps(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFMSUBADDPD`.
pub trait VfmsubaddpdEmitter<T0, T1, T2, T3> {
    fn vfmsubaddpd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFMSUBADDPS`.
pub trait VfmsubaddpsEmitter<T0, T1, T2, T3> {
    fn vfmsubaddps(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFMSUBPD`.
pub trait VfmsubpdEmitter<T0, T1, T2, T3> {
    fn vfmsubpd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFMSUBPS`.
pub trait VfmsubpsEmitter<T0, T1, T2, T3> {
    fn vfmsubps(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFMSUBSD`.
pub trait VfmsubsdEmitter<T0, T1, T2, T3> {
    fn vfmsubsd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFMSUBSS`.
pub trait VfmsubssEmitter<T0, T1, T2, T3> {
    fn vfmsubss(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFNMADDPD`.
pub trait VfnmaddpdEmitter<T0, T1, T2, T3> {
    fn vfnmaddpd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFNMADDPS`.
pub trait VfnmaddpsEmitter<T0, T1, T2, T3> {
    fn vfnmaddps(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFNMADDSD`.
pub trait VfnmaddsdEmitter<T0, T1, T2, T3> {
    fn vfnmaddsd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFNMADDSS`.
pub trait VfnmaddssEmitter<T0, T1, T2, T3> {
    fn vfnmaddss(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFNMSUBPD`.
pub trait VfnmsubpdEmitter<T0, T1, T2, T3> {
    fn vfnmsubpd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFNMSUBPS`.
pub trait VfnmsubpsEmitter<T0, T1, T2, T3> {
    fn vfnmsubps(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFNMSUBSD`.
pub trait VfnmsubsdEmitter<T0, T1, T2, T3> {
    fn vfnmsubsd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFNMSUBSS`.
pub trait VfnmsubssEmitter<T0, T1, T2, T3> {
    fn vfnmsubss(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VFRCZPD`.
pub trait VfrczpdEmitter<T0, T1> {
    fn vfrczpd(&mut self, op0: T0, op1: T1);
}

/// Emits `VFRCZPS`.
pub trait VfrczpsEmitter<T0, T1> {
    fn vfrczps(&mut self, op0: T0, op1: T1);
}

/// Emits `VFRCZSD`.
pub trait VfrczsdEmitter<T0, T1> {
    fn vfrczsd(&mut self, op0: T0, op1: T1);
}

/// Emits `VFRCZSS`.
pub trait VfrczssEmitter<T0, T1> {
    fn vfrczss(&mut self, op0: T0, op1: T1);
}

/// Emits `VPCMOV`.
pub trait VpcmovEmitter<T0, T1, T2, T3> {
    fn vpcmov(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPCOMB`.
pub trait VpcombEmitter<T0, T1, T2, T3> {
    fn vpcomb(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPCOMD`.
pub trait VpcomdEmitter<T0, T1, T2, T3> {
    fn vpcomd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPCOMQ`.
pub trait VpcomqEmitter<T0, T1, T2, T3> {
    fn vpcomq(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPCOMW`.
pub trait VpcomwEmitter<T0, T1, T2, T3> {
    fn vpcomw(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPCOMUB`.
pub trait VpcomubEmitter<T0, T1, T2, T3> {
    fn vpcomub(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPCOMUD`.
pub trait VpcomudEmitter<T0, T1, T2, T3> {
    fn vpcomud(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPCOMUQ`.
pub trait VpcomuqEmitter<T0, T1, T2, T3> {
    fn vpcomuq(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPCOMUW`.
pub trait VpcomuwEmitter<T0, T1, T2, T3> {
    fn vpcomuw(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPERMIL2PD`.
pub trait Vpermil2pdEmitter<T0, T1, T2, T3, T4> {
    fn vpermil2pd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3, op4: T4);
}

/// Emits `VPERMIL2PS`.
pub trait Vpermil2psEmitter<T0, T1, T2, T3, T4> {
    fn vpermil2ps(&mut self, op0: T0, op1: T1, op2: T2, op3: T3, op4: T4);
}

/// Emits `VPHADDBD`.
pub trait VphaddbdEmitter<T0, T1> {
    fn vphaddbd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPHADDBQ`.
pub trait VphaddbqEmitter<T0, T1> {
    fn vphaddbq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPHADDBW`.
pub trait VphaddbwEmitter<T0, T1> {
    fn vphaddbw(&mut self, op0: T0, op1: T1);
}

/// Emits `VPHADDDQ`.
pub trait VphadddqEmitter<T0, T1> {
    fn vphadddq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPHADDWD`.
pub trait VphaddwdEmitter<T0, T1> {
    fn vphaddwd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPHADDWQ`.
pub trait VphaddwqEmitter<T0, T1> {
    fn vphaddwq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPHADDUBD`.
pub trait VphaddubdEmitter<T0, T1> {
    fn vphaddubd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPHADDUBQ`.
pub trait VphaddubqEmitter<T0, T1> {
    fn vphaddubq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPHADDUBW`.
pub trait VphaddubwEmitter<T0, T1> {
    fn vphaddubw(&mut self, op0: T0, op1: T1);
}

/// Emits `VPHADDUDQ`.
pub trait VphaddudqEmitter<T0, T1> {
    fn vphaddudq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPHADDUWD`.
pub trait VphadduwdEmitter<T0, T1> {
    fn vphadduwd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPHADDUWQ`.
pub trait VphadduwqEmitter<T0, T1> {
    fn vphadduwq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPHSUBBW`.
pub trait VphsubbwEmitter<T0, T1> {
    fn vphsubbw(&mut self, op0: T0, op1: T1);
}

/// Emits `VPHSUBDQ`.
pub trait VphsubdqEmitter<T0, T1> {
    fn vphsubdq(&mut self, op0: T0, op1: T1);
}

/// Emits `VPHSUBWD`.
pub trait VphsubwdEmitter<T0, T1> {
    fn vphsubwd(&mut self, op0: T0, op1: T1);
}

/// Emits `VPMACSDD`.
pub trait VpmacsddEmitter<T0, T1, T2, T3> {
    fn vpmacsdd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPMACSDQH`.
pub trait VpmacsdqhEmitter<T0, T1, T2, T3> {
    fn vpmacsdqh(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPMACSDQL`.
pub trait VpmacsdqlEmitter<T0, T1, T2, T3> {
    fn vpmacsdql(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPMACSWD`.
pub trait VpmacswdEmitter<T0, T1, T2, T3> {
    fn vpmacswd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPMACSWW`.
pub trait VpmacswwEmitter<T0, T1, T2, T3> {
    fn vpmacsww(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPMACSSDD`.
pub trait VpmacssddEmitter<T0, T1, T2, T3> {
    fn vpmacssdd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPMACSSDQH`.
pub trait VpmacssdqhEmitter<T0, T1, T2, T3> {
    fn vpmacssdqh(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPMACSSDQL`.
pub trait VpmacssdqlEmitter<T0, T1, T2, T3> {
    fn vpmacssdql(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPMACSSWD`.
pub trait VpmacsswdEmitter<T0, T1, T2, T3> {
    fn vpmacsswd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPMACSSWW`.
pub trait VpmacsswwEmitter<T0, T1, T2, T3> {
    fn vpmacssww(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPMADCSSWD`.
pub trait VpmadcsswdEmitter<T0, T1, T2, T3> {
    fn vpmadcsswd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPMADCSWD`.
pub trait VpmadcswdEmitter<T0, T1, T2, T3> {
    fn vpmadcswd(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPPERM`.
pub trait VppermEmitter<T0, T1, T2, T3> {
    fn vpperm(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VPROTB`.
pub trait VprotbEmitter<T0, T1, T2> {
    fn vprotb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPROTD`.
pub trait VprotdEmitter<T0, T1, T2> {
    fn vprotd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPROTQ`.
pub trait VprotqEmitter<T0, T1, T2> {
    fn vprotq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPROTW`.
pub trait VprotwEmitter<T0, T1, T2> {
    fn vprotw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSHAB`.
pub trait VpshabEmitter<T0, T1, T2> {
    fn vpshab(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSHAD`.
pub trait VpshadEmitter<T0, T1, T2> {
    fn vpshad(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSHAQ`.
pub trait VpshaqEmitter<T0, T1, T2> {
    fn vpshaq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSHAW`.
pub trait VpshawEmitter<T0, T1, T2> {
    fn vpshaw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSHLB`.
pub trait VpshlbEmitter<T0, T1, T2> {
    fn vpshlb(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSHLD`.
pub trait VpshldEmitter<T0, T1, T2> {
    fn vpshld(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSHLQ`.
pub trait VpshlqEmitter<T0, T1, T2> {
    fn vpshlq(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPSHLW`.
pub trait VpshlwEmitter<T0, T1, T2> {
    fn vpshlw(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VBCSTNEBF162PS`.
pub trait Vbcstnebf162psEmitter<T0, T1> {
    fn vbcstnebf162ps(&mut self, op0: T0, op1: T1);
}

/// Emits `VBCSTNESH2PS`.
pub trait Vbcstnesh2psEmitter<T0, T1> {
    fn vbcstnesh2ps(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTNEEBF162PS`.
pub trait Vcvtneebf162psEmitter<T0, T1> {
    fn vcvtneebf162ps(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTNEEPH2PS`.
pub trait Vcvtneeph2psEmitter<T0, T1> {
    fn vcvtneeph2ps(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTNEOBF162PS`.
pub trait Vcvtneobf162psEmitter<T0, T1> {
    fn vcvtneobf162ps(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTNEOPH2PS`.
pub trait Vcvtneoph2psEmitter<T0, T1> {
    fn vcvtneoph2ps(&mut self, op0: T0, op1: T1);
}

/// Emits `VPDPBSSD`.
pub trait VpdpbssdEmitter<T0, T1, T2> {
    fn vpdpbssd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPDPBSSDS`.
pub trait VpdpbssdsEmitter<T0, T1, T2> {
    fn vpdpbssds(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPDPBSUD`.
pub trait VpdpbsudEmitter<T0, T1, T2> {
    fn vpdpbsud(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPDPBSUDS`.
pub trait VpdpbsudsEmitter<T0, T1, T2> {
    fn vpdpbsuds(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPDPBUUD`.
pub trait VpdpbuudEmitter<T0, T1, T2> {
    fn vpdpbuud(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPDPBUUDS`.
pub trait VpdpbuudsEmitter<T0, T1, T2> {
    fn vpdpbuuds(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPDPWSUD`.
pub trait VpdpwsudEmitter<T0, T1, T2> {
    fn vpdpwsud(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPDPWSUDS`.
pub trait VpdpwsudsEmitter<T0, T1, T2> {
    fn vpdpwsuds(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPDPWUSD`.
pub trait VpdpwusdEmitter<T0, T1, T2> {
    fn vpdpwusd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPDPWUSDS`.
pub trait VpdpwusdsEmitter<T0, T1, T2> {
    fn vpdpwusds(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPDPWUUD`.
pub trait VpdpwuudEmitter<T0, T1, T2> {
    fn vpdpwuud(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPDPWUUDS`.
pub trait VpdpwuudsEmitter<T0, T1, T2> {
    fn vpdpwuuds(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VSHA512MSG1`.
pub trait Vsha512msg1Emitter<T0, T1> {
    fn vsha512msg1(&mut self, op0: T0, op1: T1);
}

/// Emits `VSHA512MSG2`.
pub trait Vsha512msg2Emitter<T0, T1> {
    fn vsha512msg2(&mut self, op0: T0, op1: T1);
}

/// Emits `VSHA512RNDS2`.
pub trait Vsha512rnds2Emitter<T0, T1, T2> {
    fn vsha512rnds2(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VSM3MSG1`.
pub trait Vsm3msg1Emitter<T0, T1, T2> {
    fn vsm3msg1(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VSM3MSG2`.
pub trait Vsm3msg2Emitter<T0, T1, T2> {
    fn vsm3msg2(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VSM3RNDS2`.
pub trait Vsm3rnds2Emitter<T0, T1, T2, T3> {
    fn vsm3rnds2(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VSM4KEY4`.
pub trait Vsm4key4Emitter<T0, T1, T2> {
    fn vsm4key4(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VSM4RNDS4`.
pub trait Vsm4rnds4Emitter<T0, T1, T2> {
    fn vsm4rnds4(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VADDPH`.
pub trait VaddphEmitter<T0, T1, T2> {
    fn vaddph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VADDSH`.
pub trait VaddshEmitter<T0, T1, T2> {
    fn vaddsh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VCMPPH`.
pub trait VcmpphEmitter<T0, T1, T2, T3> {
    fn vcmpph(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VCMPSH`.
pub trait VcmpshEmitter<T0, T1, T2, T3> {
    fn vcmpsh(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VCOMISH`.
pub trait VcomishEmitter<T0, T1> {
    fn vcomish(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTDQ2PH`.
pub trait Vcvtdq2phEmitter<T0, T1> {
    fn vcvtdq2ph(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTPD2PH`.
pub trait Vcvtpd2phEmitter<T0, T1> {
    fn vcvtpd2ph(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTPH2DQ`.
pub trait Vcvtph2dqEmitter<T0, T1> {
    fn vcvtph2dq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTPH2PD`.
pub trait Vcvtph2pdEmitter<T0, T1> {
    fn vcvtph2pd(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTPH2PSX`.
pub trait Vcvtph2psxEmitter<T0, T1> {
    fn vcvtph2psx(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTPH2QQ`.
pub trait Vcvtph2qqEmitter<T0, T1> {
    fn vcvtph2qq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTPH2UDQ`.
pub trait Vcvtph2udqEmitter<T0, T1> {
    fn vcvtph2udq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTPH2UQQ`.
pub trait Vcvtph2uqqEmitter<T0, T1> {
    fn vcvtph2uqq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTPH2UW`.
pub trait Vcvtph2uwEmitter<T0, T1> {
    fn vcvtph2uw(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTPH2W`.
pub trait Vcvtph2wEmitter<T0, T1> {
    fn vcvtph2w(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTPS2PHX`.
pub trait Vcvtps2phxEmitter<T0, T1> {
    fn vcvtps2phx(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTQQ2PH`.
pub trait Vcvtqq2phEmitter<T0, T1> {
    fn vcvtqq2ph(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTSD2SH`.
pub trait Vcvtsd2shEmitter<T0, T1, T2> {
    fn vcvtsd2sh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VCVTSH2SD`.
pub trait Vcvtsh2sdEmitter<T0, T1, T2> {
    fn vcvtsh2sd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VCVTSH2SI`.
pub trait Vcvtsh2siEmitter<T0, T1> {
    fn vcvtsh2si(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTSH2SS`.
pub trait Vcvtsh2ssEmitter<T0, T1, T2> {
    fn vcvtsh2ss(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VCVTSH2USI`.
pub trait Vcvtsh2usiEmitter<T0, T1> {
    fn vcvtsh2usi(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTSI2SH`.
pub trait Vcvtsi2shEmitter<T0, T1, T2> {
    fn vcvtsi2sh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VCVTSS2SH`.
pub trait Vcvtss2shEmitter<T0, T1, T2> {
    fn vcvtss2sh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VCVTTPH2DQ`.
pub trait Vcvttph2dqEmitter<T0, T1> {
    fn vcvttph2dq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTTPH2QQ`.
pub trait Vcvttph2qqEmitter<T0, T1> {
    fn vcvttph2qq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTTPH2UDQ`.
pub trait Vcvttph2udqEmitter<T0, T1> {
    fn vcvttph2udq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTTPH2UQQ`.
pub trait Vcvttph2uqqEmitter<T0, T1> {
    fn vcvttph2uqq(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTTPH2UW`.
pub trait Vcvttph2uwEmitter<T0, T1> {
    fn vcvttph2uw(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTTPH2W`.
pub trait Vcvttph2wEmitter<T0, T1> {
    fn vcvttph2w(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTTSH2SI`.
pub trait Vcvttsh2siEmitter<T0, T1> {
    fn vcvttsh2si(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTTSH2USI`.
pub trait Vcvttsh2usiEmitter<T0, T1> {
    fn vcvttsh2usi(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTUDQ2PH`.
pub trait Vcvtudq2phEmitter<T0, T1> {
    fn vcvtudq2ph(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTUQQ2PH`.
pub trait Vcvtuqq2phEmitter<T0, T1> {
    fn vcvtuqq2ph(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTUSI2SH`.
pub trait Vcvtusi2shEmitter<T0, T1, T2> {
    fn vcvtusi2sh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VCVTUW2PH`.
pub trait Vcvtuw2phEmitter<T0, T1> {
    fn vcvtuw2ph(&mut self, op0: T0, op1: T1);
}

/// Emits `VCVTW2PH`.
pub trait Vcvtw2phEmitter<T0, T1> {
    fn vcvtw2ph(&mut self, op0: T0, op1: T1);
}

/// Emits `VDIVPH`.
pub trait VdivphEmitter<T0, T1, T2> {
    fn vdivph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VDIVSH`.
pub trait VdivshEmitter<T0, T1, T2> {
    fn vdivsh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFCMADDCPH`.
pub trait VfcmaddcphEmitter<T0, T1, T2> {
    fn vfcmaddcph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFCMADDCSH`.
pub trait VfcmaddcshEmitter<T0, T1, T2> {
    fn vfcmaddcsh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFCMULCPH`.
pub trait VfcmulcphEmitter<T0, T1, T2> {
    fn vfcmulcph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFCMULCSH`.
pub trait VfcmulcshEmitter<T0, T1, T2> {
    fn vfcmulcsh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADD132PH`.
pub trait Vfmadd132phEmitter<T0, T1, T2> {
    fn vfmadd132ph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADD132SH`.
pub trait Vfmadd132shEmitter<T0, T1, T2> {
    fn vfmadd132sh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADD213PH`.
pub trait Vfmadd213phEmitter<T0, T1, T2> {
    fn vfmadd213ph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADD213SH`.
pub trait Vfmadd213shEmitter<T0, T1, T2> {
    fn vfmadd213sh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADD231PH`.
pub trait Vfmadd231phEmitter<T0, T1, T2> {
    fn vfmadd231ph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADD231SH`.
pub trait Vfmadd231shEmitter<T0, T1, T2> {
    fn vfmadd231sh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADDCPH`.
pub trait VfmaddcphEmitter<T0, T1, T2> {
    fn vfmaddcph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADDCSH`.
pub trait VfmaddcshEmitter<T0, T1, T2> {
    fn vfmaddcsh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADDSUB132PH`.
pub trait Vfmaddsub132phEmitter<T0, T1, T2> {
    fn vfmaddsub132ph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADDSUB213PH`.
pub trait Vfmaddsub213phEmitter<T0, T1, T2> {
    fn vfmaddsub213ph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMADDSUB231PH`.
pub trait Vfmaddsub231phEmitter<T0, T1, T2> {
    fn vfmaddsub231ph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUB132PH`.
pub trait Vfmsub132phEmitter<T0, T1, T2> {
    fn vfmsub132ph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUB132SH`.
pub trait Vfmsub132shEmitter<T0, T1, T2> {
    fn vfmsub132sh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUB213PH`.
pub trait Vfmsub213phEmitter<T0, T1, T2> {
    fn vfmsub213ph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUB213SH`.
pub trait Vfmsub213shEmitter<T0, T1, T2> {
    fn vfmsub213sh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUB231PH`.
pub trait Vfmsub231phEmitter<T0, T1, T2> {
    fn vfmsub231ph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUB231SH`.
pub trait Vfmsub231shEmitter<T0, T1, T2> {
    fn vfmsub231sh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUBADD132PH`.
pub trait Vfmsubadd132phEmitter<T0, T1, T2> {
    fn vfmsubadd132ph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUBADD213PH`.
pub trait Vfmsubadd213phEmitter<T0, T1, T2> {
    fn vfmsubadd213ph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMSUBADD231PH`.
pub trait Vfmsubadd231phEmitter<T0, T1, T2> {
    fn vfmsubadd231ph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMULCPH`.
pub trait VfmulcphEmitter<T0, T1, T2> {
    fn vfmulcph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFMULCSH`.
pub trait VfmulcshEmitter<T0, T1, T2> {
    fn vfmulcsh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMADD132PH`.
pub trait Vfnmadd132phEmitter<T0, T1, T2> {
    fn vfnmadd132ph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMADD132SH`.
pub trait Vfnmadd132shEmitter<T0, T1, T2> {
    fn vfnmadd132sh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMADD213PH`.
pub trait Vfnmadd213phEmitter<T0, T1, T2> {
    fn vfnmadd213ph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMADD213SH`.
pub trait Vfnmadd213shEmitter<T0, T1, T2> {
    fn vfnmadd213sh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMADD231PH`.
pub trait Vfnmadd231phEmitter<T0, T1, T2> {
    fn vfnmadd231ph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMADD231SH`.
pub trait Vfnmadd231shEmitter<T0, T1, T2> {
    fn vfnmadd231sh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMSUB132PH`.
pub trait Vfnmsub132phEmitter<T0, T1, T2> {
    fn vfnmsub132ph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMSUB132SH`.
pub trait Vfnmsub132shEmitter<T0, T1, T2> {
    fn vfnmsub132sh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMSUB213PH`.
pub trait Vfnmsub213phEmitter<T0, T1, T2> {
    fn vfnmsub213ph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMSUB213SH`.
pub trait Vfnmsub213shEmitter<T0, T1, T2> {
    fn vfnmsub213sh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMSUB231PH`.
pub trait Vfnmsub231phEmitter<T0, T1, T2> {
    fn vfnmsub231ph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFNMSUB231SH`.
pub trait Vfnmsub231shEmitter<T0, T1, T2> {
    fn vfnmsub231sh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFPCLASSPH`.
pub trait VfpclassphEmitter<T0, T1, T2> {
    fn vfpclassph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VFPCLASSSH`.
pub trait VfpclassshEmitter<T0, T1, T2> {
    fn vfpclasssh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VGETEXPPH`.
pub trait VgetexpphEmitter<T0, T1> {
    fn vgetexpph(&mut self, op0: T0, op1: T1);
}

/// Emits `VGETEXPSH`.
pub trait VgetexpshEmitter<T0, T1, T2> {
    fn vgetexpsh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VGETMANTPH`.
pub trait VgetmantphEmitter<T0, T1, T2> {
    fn vgetmantph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VGETMANTSH`.
pub trait VgetmantshEmitter<T0, T1, T2, T3> {
    fn vgetmantsh(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VMAXPH`.
pub trait VmaxphEmitter<T0, T1, T2> {
    fn vmaxph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMAXSH`.
pub trait VmaxshEmitter<T0, T1, T2> {
    fn vmaxsh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMINPH`.
pub trait VminphEmitter<T0, T1, T2> {
    fn vminph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMINSH`.
pub trait VminshEmitter<T0, T1, T2> {
    fn vminsh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMOVSH`.
pub trait VmovshEmitter<T0, T1> {
    fn vmovsh(&mut self, op0: T0, op1: T1);
}

/// Emits `VMOVSH_3`.
pub trait Vmovsh3Emitter<T0, T1, T2> {
    fn vmovsh_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMOVW`.
pub trait VmovwEmitter<T0, T1> {
    fn vmovw(&mut self, op0: T0, op1: T1);
}

/// Emits `VMULPH`.
pub trait VmulphEmitter<T0, T1, T2> {
    fn vmulph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VMULSH`.
pub trait VmulshEmitter<T0, T1, T2> {
    fn vmulsh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VRCPPH`.
pub trait VrcpphEmitter<T0, T1> {
    fn vrcpph(&mut self, op0: T0, op1: T1);
}

/// Emits `VRCPSH`.
pub trait VrcpshEmitter<T0, T1, T2> {
    fn vrcpsh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VREDUCEPH`.
pub trait VreducephEmitter<T0, T1, T2> {
    fn vreduceph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VREDUCESH`.
pub trait VreduceshEmitter<T0, T1, T2, T3> {
    fn vreducesh(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VRNDSCALEPH`.
pub trait VrndscalephEmitter<T0, T1, T2> {
    fn vrndscaleph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VRNDSCALESH`.
pub trait VrndscaleshEmitter<T0, T1, T2, T3> {
    fn vrndscalesh(&mut self, op0: T0, op1: T1, op2: T2, op3: T3);
}

/// Emits `VRSQRTPH`.
pub trait VrsqrtphEmitter<T0, T1> {
    fn vrsqrtph(&mut self, op0: T0, op1: T1);
}

/// Emits `VRSQRTSH`.
pub trait VrsqrtshEmitter<T0, T1, T2> {
    fn vrsqrtsh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VSCALEFPH`.
pub trait VscalefphEmitter<T0, T1, T2> {
    fn vscalefph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VSCALEFSH`.
pub trait VscalefshEmitter<T0, T1, T2> {
    fn vscalefsh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VSQRTPH`.
pub trait VsqrtphEmitter<T0, T1> {
    fn vsqrtph(&mut self, op0: T0, op1: T1);
}

/// Emits `VSQRTSH`.
pub trait VsqrtshEmitter<T0, T1, T2> {
    fn vsqrtsh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VSUBPH`.
pub trait VsubphEmitter<T0, T1, T2> {
    fn vsubph(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VSUBSH`.
pub trait VsubshEmitter<T0, T1, T2> {
    fn vsubsh(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VUCOMISH`.
pub trait VucomishEmitter<T0, T1> {
    fn vucomish(&mut self, op0: T0, op1: T1);
}

/// Emits `LDTILECFG`.
pub trait LdtilecfgEmitter<T0> {
    fn ldtilecfg(&mut self, op0: T0);
}

/// Emits `STTILECFG`.
pub trait SttilecfgEmitter<T0> {
    fn sttilecfg(&mut self, op0: T0);
}

/// Emits `TILELOADD`.
pub trait TileloaddEmitter<T0, T1> {
    fn tileloadd(&mut self, op0: T0, op1: T1);
}

/// Emits `TILELOADDT1`.
pub trait Tileloaddt1Emitter<T0, T1> {
    fn tileloaddt1(&mut self, op0: T0, op1: T1);
}

/// Emits `TILERELEASE`.
pub trait TilereleaseEmitter {
    fn tilerelease(&mut self);
}

/// Emits `TILESTORED`.
pub trait TilestoredEmitter<T0, T1> {
    fn tilestored(&mut self, op0: T0, op1: T1);
}

/// Emits `TILEZERO`.
pub trait TilezeroEmitter<T0> {
    fn tilezero(&mut self, op0: T0);
}

/// Emits `TDPBF16PS`.
pub trait Tdpbf16psEmitter<T0, T1, T2> {
    fn tdpbf16ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `TCMMIMFP16PS`.
pub trait Tcmmimfp16psEmitter<T0, T1, T2> {
    fn tcmmimfp16ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `TCMMRLFP16PS`.
pub trait Tcmmrlfp16psEmitter<T0, T1, T2> {
    fn tcmmrlfp16ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `TDPFP16PS`.
pub trait Tdpfp16psEmitter<T0, T1, T2> {
    fn tdpfp16ps(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `TDPBSSD`.
pub trait TdpbssdEmitter<T0, T1, T2> {
    fn tdpbssd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `TDPBSUD`.
pub trait TdpbsudEmitter<T0, T1, T2> {
    fn tdpbsud(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `TDPBUSD`.
pub trait TdpbusdEmitter<T0, T1, T2> {
    fn tdpbusd(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `TDPBUUD`.
pub trait TdpbuudEmitter<T0, T1, T2> {
    fn tdpbuud(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `CBW_0`.
pub trait Cbw0Emitter {
    fn cbw_0(&mut self);
}

/// Emits `CDQ_0`.
pub trait Cdq0Emitter {
    fn cdq_0(&mut self);
}

/// Emits `CDQE_0`.
pub trait Cdqe0Emitter {
    fn cdqe_0(&mut self);
}

/// Emits `CMPXCHG_2`.
pub trait Cmpxchg2Emitter<T0, T1> {
    fn cmpxchg_2(&mut self, op0: T0, op1: T1);
}

/// Emits `CMPXCHG16B_1`.
pub trait Cmpxchg16b1Emitter<T0> {
    fn cmpxchg16b_1(&mut self, op0: T0);
}

/// Emits `CMPXCHG8B_1`.
pub trait Cmpxchg8b1Emitter<T0> {
    fn cmpxchg8b_1(&mut self, op0: T0);
}

/// Emits `CQO_0`.
pub trait Cqo0Emitter {
    fn cqo_0(&mut self);
}

/// Emits `CWD_0`.
pub trait Cwd0Emitter {
    fn cwd_0(&mut self);
}

/// Emits `CWDE_0`.
pub trait Cwde0Emitter {
    fn cwde_0(&mut self);
}

/// Emits `DIV_1`.
pub trait Div1Emitter<T0> {
    fn div_1(&mut self, op0: T0);
}

/// Emits `IDIV_1`.
pub trait Idiv1Emitter<T0> {
    fn idiv_1(&mut self, op0: T0);
}

/// Emits `IMUL_1`.
pub trait Imul1Emitter<T0> {
    fn imul_1(&mut self, op0: T0);
}

/// Emits `IRET`.
pub trait IretEmitter {
    fn iret(&mut self);
}

/// Emits `IRETD`.
pub trait IretdEmitter {
    fn iretd(&mut self);
}

/// Emits `IRETQ`.
pub trait IretqEmitter {
    fn iretq(&mut self);
}

/// Emits `JECXZ_1`.
pub trait Jecxz1Emitter<T0> {
    fn jecxz_1(&mut self, op0: T0);
}

/// Emits `LOOP_1`.
pub trait Loop1Emitter<T0> {
    fn loop_1(&mut self, op0: T0);
}

/// Emits `LOOPE_1`.
pub trait Loope1Emitter<T0> {
    fn loope_1(&mut self, op0: T0);
}

/// Emits `LOOPNE_1`.
pub trait Loopne1Emitter<T0> {
    fn loopne_1(&mut self, op0: T0);
}

/// Emits `MUL_1`.
pub trait Mul1Emitter<T0> {
    fn mul_1(&mut self, op0: T0);
}

/// Emits `RET`.
pub trait RetEmitter {
    fn ret(&mut self);
}

/// Emits `RET_1`.
pub trait Ret1Emitter<T0> {
    fn ret_1(&mut self, op0: T0);
}

/// Emits `RETF`.
pub trait RetfEmitter {
    fn retf(&mut self);
}

/// Emits `RETF_1`.
pub trait Retf1Emitter<T0> {
    fn retf_1(&mut self, op0: T0);
}

/// Emits `XLATB`.
pub trait XlatbEmitter {
    fn xlatb(&mut self);
}

/// Emits `AAA_0`.
pub trait Aaa0Emitter {
    fn aaa_0(&mut self);
}

/// Emits `AAD_1`.
pub trait Aad1Emitter<T0> {
    fn aad_1(&mut self, op0: T0);
}

/// Emits `AAM_1`.
pub trait Aam1Emitter<T0> {
    fn aam_1(&mut self, op0: T0);
}

/// Emits `AAS_0`.
pub trait Aas0Emitter {
    fn aas_0(&mut self);
}

/// Emits `DAA_0`.
pub trait Daa0Emitter {
    fn daa_0(&mut self);
}

/// Emits `DAS_0`.
pub trait Das0Emitter {
    fn das_0(&mut self);
}

/// Emits `LAHF_0`.
pub trait Lahf0Emitter {
    fn lahf_0(&mut self);
}

/// Emits `SAHF_0`.
pub trait Sahf0Emitter {
    fn sahf_0(&mut self);
}

/// Emits `CPUID_0`.
pub trait Cpuid0Emitter {
    fn cpuid_0(&mut self);
}

/// Emits `CLZERO_0`.
pub trait Clzero0Emitter {
    fn clzero_0(&mut self);
}

/// Emits `RDPRU_0`.
pub trait Rdpru0Emitter {
    fn rdpru_0(&mut self);
}

/// Emits `RDPKRU_0`.
pub trait Rdpkru0Emitter {
    fn rdpkru_0(&mut self);
}

/// Emits `RDTSC_0`.
pub trait Rdtsc0Emitter {
    fn rdtsc_0(&mut self);
}

/// Emits `RDTSCP_0`.
pub trait Rdtscp0Emitter {
    fn rdtscp_0(&mut self);
}

/// Emits `MULX_3`.
pub trait Mulx3Emitter<T0, T1, T2> {
    fn mulx_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `XGETBV_0`.
pub trait Xgetbv0Emitter {
    fn xgetbv_0(&mut self);
}

/// Emits `XRSTOR_1`.
pub trait Xrstor1Emitter<T0> {
    fn xrstor_1(&mut self, op0: T0);
}

/// Emits `XRSTOR64_1`.
pub trait Xrstor641Emitter<T0> {
    fn xrstor64_1(&mut self, op0: T0);
}

/// Emits `XRSTORS_1`.
pub trait Xrstors1Emitter<T0> {
    fn xrstors_1(&mut self, op0: T0);
}

/// Emits `XRSTORS64_1`.
pub trait Xrstors641Emitter<T0> {
    fn xrstors64_1(&mut self, op0: T0);
}

/// Emits `XSAVE_1`.
pub trait Xsave1Emitter<T0> {
    fn xsave_1(&mut self, op0: T0);
}

/// Emits `XSAVE64_1`.
pub trait Xsave641Emitter<T0> {
    fn xsave64_1(&mut self, op0: T0);
}

/// Emits `XSAVEC_1`.
pub trait Xsavec1Emitter<T0> {
    fn xsavec_1(&mut self, op0: T0);
}

/// Emits `XSAVEC64_1`.
pub trait Xsavec641Emitter<T0> {
    fn xsavec64_1(&mut self, op0: T0);
}

/// Emits `XSAVEOPT_1`.
pub trait Xsaveopt1Emitter<T0> {
    fn xsaveopt_1(&mut self, op0: T0);
}

/// Emits `XSAVEOPT64_1`.
pub trait Xsaveopt641Emitter<T0> {
    fn xsaveopt64_1(&mut self, op0: T0);
}

/// Emits `XSAVES_1`.
pub trait Xsaves1Emitter<T0> {
    fn xsaves_1(&mut self, op0: T0);
}

/// Emits `XSAVES64_1`.
pub trait Xsaves641Emitter<T0> {
    fn xsaves64_1(&mut self, op0: T0);
}

/// Emits `SYSCALL`.
pub trait SyscallEmitter {
    fn syscall(&mut self);
}

/// Emits `SYSENTER`.
pub trait SysenterEmitter {
    fn sysenter(&mut self);
}

/// Emits `HRESET_1`.
pub trait Hreset1Emitter<T0> {
    fn hreset_1(&mut self, op0: T0);
}

/// Emits `SEAMCALL`.
pub trait SeamcallEmitter {
    fn seamcall(&mut self);
}

/// Emits `SEAMOPS`.
pub trait SeamopsEmitter {
    fn seamops(&mut self);
}

/// Emits `SEAMRET`.
pub trait SeamretEmitter {
    fn seamret(&mut self);
}

/// Emits `TDCALL`.
pub trait TdcallEmitter {
    fn tdcall(&mut self);
}

/// Emits `PCONFIG`.
pub trait PconfigEmitter {
    fn pconfig(&mut self);
}

/// Emits `RDMSR_0`.
pub trait Rdmsr0Emitter {
    fn rdmsr_0(&mut self);
}

/// Emits `RDPMC_0`.
pub trait Rdpmc0Emitter {
    fn rdpmc_0(&mut self);
}

/// Emits `SYSEXIT`.
pub trait SysexitEmitter {
    fn sysexit(&mut self);
}

/// Emits `SYSEXITQ`.
pub trait SysexitqEmitter {
    fn sysexitq(&mut self);
}

/// Emits `SYSRET`.
pub trait SysretEmitter {
    fn sysret(&mut self);
}

/// Emits `SYSRETQ`.
pub trait SysretqEmitter {
    fn sysretq(&mut self);
}

/// Emits `WRMSR_0`.
pub trait Wrmsr0Emitter {
    fn wrmsr_0(&mut self);
}

/// Emits `XSETBV_0`.
pub trait Xsetbv0Emitter {
    fn xsetbv_0(&mut self);
}

/// Emits `MONITOR_0`.
pub trait Monitor0Emitter {
    fn monitor_0(&mut self);
}

/// Emits `MONITORX_0`.
pub trait Monitorx0Emitter {
    fn monitorx_0(&mut self);
}

/// Emits `MWAIT_0`.
pub trait Mwait0Emitter {
    fn mwait_0(&mut self);
}

/// Emits `MWAITX_0`.
pub trait Mwaitx0Emitter {
    fn mwaitx_0(&mut self);
}

/// Emits `TPAUSE_1`.
pub trait Tpause1Emitter<T0> {
    fn tpause_1(&mut self, op0: T0);
}

/// Emits `UMWAIT_1`.
pub trait Umwait1Emitter<T0> {
    fn umwait_1(&mut self, op0: T0);
}

/// Emits `BLENDVPD_2`.
pub trait Blendvpd2Emitter<T0, T1> {
    fn blendvpd_2(&mut self, op0: T0, op1: T1);
}

/// Emits `BLENDVPS_2`.
pub trait Blendvps2Emitter<T0, T1> {
    fn blendvps_2(&mut self, op0: T0, op1: T1);
}

/// Emits `PBLENDVB_2`.
pub trait Pblendvb2Emitter<T0, T1> {
    fn pblendvb_2(&mut self, op0: T0, op1: T1);
}

/// Emits `MASKMOVQ_2`.
pub trait Maskmovq2Emitter<T0, T1> {
    fn maskmovq_2(&mut self, op0: T0, op1: T1);
}

/// Emits `MASKMOVDQU_2`.
pub trait Maskmovdqu2Emitter<T0, T1> {
    fn maskmovdqu_2(&mut self, op0: T0, op1: T1);
}

/// Emits `PCMPESTRI_3`.
pub trait Pcmpestri3Emitter<T0, T1, T2> {
    fn pcmpestri_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `PCMPESTRM_3`.
pub trait Pcmpestrm3Emitter<T0, T1, T2> {
    fn pcmpestrm_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `PCMPISTRI_3`.
pub trait Pcmpistri3Emitter<T0, T1, T2> {
    fn pcmpistri_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `PCMPISTRM_3`.
pub trait Pcmpistrm3Emitter<T0, T1, T2> {
    fn pcmpistrm_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `SHA256RNDS2_2`.
pub trait Sha256rnds22Emitter<T0, T1> {
    fn sha256rnds2_2(&mut self, op0: T0, op1: T1);
}

/// Emits `VMASKMOVDQU_2`.
pub trait Vmaskmovdqu2Emitter<T0, T1> {
    fn vmaskmovdqu_2(&mut self, op0: T0, op1: T1);
}

/// Emits `VPCMPESTRI_3`.
pub trait Vpcmpestri3Emitter<T0, T1, T2> {
    fn vpcmpestri_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPCMPESTRM_3`.
pub trait Vpcmpestrm3Emitter<T0, T1, T2> {
    fn vpcmpestrm_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPCMPISTRI_3`.
pub trait Vpcmpistri3Emitter<T0, T1, T2> {
    fn vpcmpistri_3(&mut self, op0: T0, op1: T1, op2: T2);
}

/// Emits `VPCMPISTRM_3`.
pub trait Vpcmpistrm3Emitter<T0, T1, T2> {
    fn vpcmpistrm_3(&mut self, op0: T0, op1: T1, op2: T2);
}
impl AdcEmitter<Gp, Gp> for Assembler<'_> {
    fn adc(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Adc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AdcEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn adc(&mut self, op0: GpbLo, op1: GpbLo) {
        self.emit_n(InstId::Adc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AdcEmitter<GpbLo, GpbHi> for Assembler<'_> {
    fn adc(&mut self, op0: GpbLo, op1: GpbHi) {
        self.emit_n(InstId::Adc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AdcEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn adc(&mut self, op0: GpbHi, op1: GpbLo) {
        self.emit_n(InstId::Adc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AdcEmitter<GpbHi, GpbHi> for Assembler<'_> {
    fn adc(&mut self, op0: GpbHi, op1: GpbHi) {
        self.emit_n(InstId::Adc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AdcEmitter<Gpw, Gpw> for Assembler<'_> {
    fn adc(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Adc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AdcEmitter<Gpd, Gpd> for Assembler<'_> {
    fn adc(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Adc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AdcEmitter<Gpq, Gpq> for Assembler<'_> {
    fn adc(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Adc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AdcEmitter<Gp, Mem> for Assembler<'_> {
    fn adc(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Adc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AdcEmitter<GpbLo, Mem> for Assembler<'_> {
    fn adc(&mut self, op0: GpbLo, op1: Mem) {
        self.emit_n(InstId::Adc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AdcEmitter<GpbHi, Mem> for Assembler<'_> {
    fn adc(&mut self, op0: GpbHi, op1: Mem) {
        self.emit_n(InstId::Adc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AdcEmitter<Gpw, Mem> for Assembler<'_> {
    fn adc(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Adc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AdcEmitter<Gpd, Mem> for Assembler<'_> {
    fn adc(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Adc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AdcEmitter<Gpq, Mem> for Assembler<'_> {
    fn adc(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Adc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AdcEmitter<Gp, Imm> for Assembler<'_> {
    fn adc(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Adc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> AdcEmitter<GpbLo, U1> for Assembler<'_> {
    fn adc(&mut self, op0: GpbLo, op1: U1) {
        self.emit_n(
            InstId::Adc as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> AdcEmitter<GpbHi, U1> for Assembler<'_> {
    fn adc(&mut self, op0: GpbHi, op1: U1) {
        self.emit_n(
            InstId::Adc as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> AdcEmitter<Gpw, U1> for Assembler<'_> {
    fn adc(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Adc as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> AdcEmitter<Gpd, U1> for Assembler<'_> {
    fn adc(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Adc as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> AdcEmitter<Gpq, U1> for Assembler<'_> {
    fn adc(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Adc as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl AdcEmitter<Mem, Gp> for Assembler<'_> {
    fn adc(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Adc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AdcEmitter<Mem, GpbLo> for Assembler<'_> {
    fn adc(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::Adc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AdcEmitter<Mem, GpbHi> for Assembler<'_> {
    fn adc(&mut self, op0: Mem, op1: GpbHi) {
        self.emit_n(InstId::Adc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AdcEmitter<Mem, Gpw> for Assembler<'_> {
    fn adc(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::Adc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AdcEmitter<Mem, Gpd> for Assembler<'_> {
    fn adc(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Adc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AdcEmitter<Mem, Gpq> for Assembler<'_> {
    fn adc(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Adc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> AdcEmitter<Mem, U1> for Assembler<'_> {
    fn adc(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::Adc as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl AddEmitter<Gp, Gp> for Assembler<'_> {
    fn add(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Add as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AddEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn add(&mut self, op0: GpbLo, op1: GpbLo) {
        self.emit_n(InstId::Add as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AddEmitter<GpbLo, GpbHi> for Assembler<'_> {
    fn add(&mut self, op0: GpbLo, op1: GpbHi) {
        self.emit_n(InstId::Add as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AddEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn add(&mut self, op0: GpbHi, op1: GpbLo) {
        self.emit_n(InstId::Add as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AddEmitter<GpbHi, GpbHi> for Assembler<'_> {
    fn add(&mut self, op0: GpbHi, op1: GpbHi) {
        self.emit_n(InstId::Add as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AddEmitter<Gpw, Gpw> for Assembler<'_> {
    fn add(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Add as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AddEmitter<Gpd, Gpd> for Assembler<'_> {
    fn add(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Add as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AddEmitter<Gpq, Gpq> for Assembler<'_> {
    fn add(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Add as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AddEmitter<Gp, Mem> for Assembler<'_> {
    fn add(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Add as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AddEmitter<GpbLo, Mem> for Assembler<'_> {
    fn add(&mut self, op0: GpbLo, op1: Mem) {
        self.emit_n(InstId::Add as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AddEmitter<GpbHi, Mem> for Assembler<'_> {
    fn add(&mut self, op0: GpbHi, op1: Mem) {
        self.emit_n(InstId::Add as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AddEmitter<Gpw, Mem> for Assembler<'_> {
    fn add(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Add as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AddEmitter<Gpd, Mem> for Assembler<'_> {
    fn add(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Add as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AddEmitter<Gpq, Mem> for Assembler<'_> {
    fn add(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Add as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AddEmitter<Gp, Imm> for Assembler<'_> {
    fn add(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Add as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> AddEmitter<GpbLo, U1> for Assembler<'_> {
    fn add(&mut self, op0: GpbLo, op1: U1) {
        self.emit_n(
            InstId::Add as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> AddEmitter<GpbHi, U1> for Assembler<'_> {
    fn add(&mut self, op0: GpbHi, op1: U1) {
        self.emit_n(
            InstId::Add as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> AddEmitter<Gpw, U1> for Assembler<'_> {
    fn add(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Add as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> AddEmitter<Gpd, U1> for Assembler<'_> {
    fn add(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Add as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> AddEmitter<Gpq, U1> for Assembler<'_> {
    fn add(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Add as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl AddEmitter<Mem, Gp> for Assembler<'_> {
    fn add(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Add as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AddEmitter<Mem, GpbLo> for Assembler<'_> {
    fn add(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::Add as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AddEmitter<Mem, GpbHi> for Assembler<'_> {
    fn add(&mut self, op0: Mem, op1: GpbHi) {
        self.emit_n(InstId::Add as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AddEmitter<Mem, Gpw> for Assembler<'_> {
    fn add(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::Add as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AddEmitter<Mem, Gpd> for Assembler<'_> {
    fn add(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Add as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl AddEmitter<Mem, Gpq> for Assembler<'_> {
    fn add(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Add as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> AddEmitter<Mem, U1> for Assembler<'_> {
    fn add(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::Add as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl And_Emitter<Gp, Gp> for Assembler<'_> {
    fn and_(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::And as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl And_Emitter<Gpq, Gpq> for Assembler<'_> {
    fn and_(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::And as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl And_Emitter<GpbLo, GpbLo> for Assembler<'_> {
    fn and_(&mut self, op0: GpbLo, op1: GpbLo) {
        self.emit_n(InstId::And as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl And_Emitter<GpbLo, GpbHi> for Assembler<'_> {
    fn and_(&mut self, op0: GpbLo, op1: GpbHi) {
        self.emit_n(InstId::And as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl And_Emitter<GpbHi, GpbLo> for Assembler<'_> {
    fn and_(&mut self, op0: GpbHi, op1: GpbLo) {
        self.emit_n(InstId::And as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl And_Emitter<GpbHi, GpbHi> for Assembler<'_> {
    fn and_(&mut self, op0: GpbHi, op1: GpbHi) {
        self.emit_n(InstId::And as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl And_Emitter<Gpw, Gpw> for Assembler<'_> {
    fn and_(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::And as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl And_Emitter<Gpd, Gpd> for Assembler<'_> {
    fn and_(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::And as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl And_Emitter<Gp, Mem> for Assembler<'_> {
    fn and_(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::And as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl And_Emitter<Gpq, Mem> for Assembler<'_> {
    fn and_(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::And as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl And_Emitter<GpbLo, Mem> for Assembler<'_> {
    fn and_(&mut self, op0: GpbLo, op1: Mem) {
        self.emit_n(InstId::And as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl And_Emitter<GpbHi, Mem> for Assembler<'_> {
    fn and_(&mut self, op0: GpbHi, op1: Mem) {
        self.emit_n(InstId::And as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl And_Emitter<Gpw, Mem> for Assembler<'_> {
    fn and_(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::And as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl And_Emitter<Gpd, Mem> for Assembler<'_> {
    fn and_(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::And as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl And_Emitter<Gp, Imm> for Assembler<'_> {
    fn and_(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::And as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> And_Emitter<GpbLo, U1> for Assembler<'_> {
    fn and_(&mut self, op0: GpbLo, op1: U1) {
        self.emit_n(
            InstId::And as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> And_Emitter<GpbHi, U1> for Assembler<'_> {
    fn and_(&mut self, op0: GpbHi, op1: U1) {
        self.emit_n(
            InstId::And as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> And_Emitter<Gpw, U1> for Assembler<'_> {
    fn and_(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::And as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> And_Emitter<Gpd, U1> for Assembler<'_> {
    fn and_(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::And as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> And_Emitter<Gpq, U1> for Assembler<'_> {
    fn and_(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::And as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl And_Emitter<Mem, Gp> for Assembler<'_> {
    fn and_(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::And as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl And_Emitter<Mem, GpbLo> for Assembler<'_> {
    fn and_(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::And as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl And_Emitter<Mem, GpbHi> for Assembler<'_> {
    fn and_(&mut self, op0: Mem, op1: GpbHi) {
        self.emit_n(InstId::And as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl And_Emitter<Mem, Gpw> for Assembler<'_> {
    fn and_(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::And as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl And_Emitter<Mem, Gpd> for Assembler<'_> {
    fn and_(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::And as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl And_Emitter<Mem, Gpq> for Assembler<'_> {
    fn and_(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::And as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> And_Emitter<Mem, U1> for Assembler<'_> {
    fn and_(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::And as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl BoundEmitter<Gp, Mem> for Assembler<'_> {
    fn bound(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Bound as u32, &[op0.as_operand(), op1.as_operand()]); // X86
    }
}

impl BoundEmitter<Gpw, Mem> for Assembler<'_> {
    fn bound(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Bound as u32, &[op0.as_operand(), op1.as_operand()]); // X86
    }
}

impl BoundEmitter<Gpd, Mem> for Assembler<'_> {
    fn bound(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Bound as u32, &[op0.as_operand(), op1.as_operand()]); // X86
    }
}

impl BsfEmitter<Gp, Gp> for Assembler<'_> {
    fn bsf(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Bsf as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BsfEmitter<Gpw, Gpw> for Assembler<'_> {
    fn bsf(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Bsf as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BsfEmitter<Gpd, Gpd> for Assembler<'_> {
    fn bsf(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Bsf as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BsfEmitter<Gpq, Gpq> for Assembler<'_> {
    fn bsf(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Bsf as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BsfEmitter<Gp, Mem> for Assembler<'_> {
    fn bsf(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Bsf as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BsfEmitter<Gpw, Mem> for Assembler<'_> {
    fn bsf(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Bsf as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BsfEmitter<Gpd, Mem> for Assembler<'_> {
    fn bsf(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Bsf as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BsfEmitter<Gpq, Mem> for Assembler<'_> {
    fn bsf(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Bsf as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BsrEmitter<Gp, Gp> for Assembler<'_> {
    fn bsr(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Bsr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BsrEmitter<Gpw, Gpw> for Assembler<'_> {
    fn bsr(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Bsr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BsrEmitter<Gpd, Gpd> for Assembler<'_> {
    fn bsr(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Bsr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BsrEmitter<Gpq, Gpq> for Assembler<'_> {
    fn bsr(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Bsr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BsrEmitter<Gp, Mem> for Assembler<'_> {
    fn bsr(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Bsr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BsrEmitter<Gpw, Mem> for Assembler<'_> {
    fn bsr(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Bsr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BsrEmitter<Gpd, Mem> for Assembler<'_> {
    fn bsr(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Bsr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BsrEmitter<Gpq, Mem> for Assembler<'_> {
    fn bsr(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Bsr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BswapEmitter<Gp> for Assembler<'_> {
    fn bswap(&mut self, op0: Gp) {
        self.emit_n(InstId::Bswap as u32, &[op0.as_operand()]); // ANY
    }
}

impl BswapEmitter<Gpw> for Assembler<'_> {
    fn bswap(&mut self, op0: Gpw) {
        self.emit_n(InstId::Bswap as u32, &[op0.as_operand()]); // ANY
    }
}

impl BswapEmitter<Gpd> for Assembler<'_> {
    fn bswap(&mut self, op0: Gpd) {
        self.emit_n(InstId::Bswap as u32, &[op0.as_operand()]); // ANY
    }
}

impl BswapEmitter<Gpq> for Assembler<'_> {
    fn bswap(&mut self, op0: Gpq) {
        self.emit_n(InstId::Bswap as u32, &[op0.as_operand()]); // ANY
    }
}

impl BtEmitter<Gp, Gp> for Assembler<'_> {
    fn bt(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Bt as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtEmitter<Gpw, Gpw> for Assembler<'_> {
    fn bt(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Bt as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtEmitter<Gpd, Gpd> for Assembler<'_> {
    fn bt(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Bt as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtEmitter<Gpq, Gpq> for Assembler<'_> {
    fn bt(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Bt as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtEmitter<Gp, Imm> for Assembler<'_> {
    fn bt(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Bt as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> BtEmitter<Gpw, U1> for Assembler<'_> {
    fn bt(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Bt as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> BtEmitter<Gpd, U1> for Assembler<'_> {
    fn bt(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Bt as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> BtEmitter<Gpq, U1> for Assembler<'_> {
    fn bt(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Bt as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl BtEmitter<Mem, Gp> for Assembler<'_> {
    fn bt(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Bt as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtEmitter<Mem, Gpw> for Assembler<'_> {
    fn bt(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::Bt as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtEmitter<Mem, Gpd> for Assembler<'_> {
    fn bt(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Bt as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtEmitter<Mem, Gpq> for Assembler<'_> {
    fn bt(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Bt as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> BtEmitter<Mem, U1> for Assembler<'_> {
    fn bt(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::Bt as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl BtcEmitter<Gp, Gp> for Assembler<'_> {
    fn btc(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Btc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtcEmitter<Gpw, Gpw> for Assembler<'_> {
    fn btc(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Btc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtcEmitter<Gpd, Gpd> for Assembler<'_> {
    fn btc(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Btc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtcEmitter<Gpq, Gpq> for Assembler<'_> {
    fn btc(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Btc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtcEmitter<Gp, Imm> for Assembler<'_> {
    fn btc(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Btc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> BtcEmitter<Gpw, U1> for Assembler<'_> {
    fn btc(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Btc as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> BtcEmitter<Gpd, U1> for Assembler<'_> {
    fn btc(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Btc as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> BtcEmitter<Gpq, U1> for Assembler<'_> {
    fn btc(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Btc as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl BtcEmitter<Mem, Gp> for Assembler<'_> {
    fn btc(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Btc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtcEmitter<Mem, Gpw> for Assembler<'_> {
    fn btc(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::Btc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtcEmitter<Mem, Gpd> for Assembler<'_> {
    fn btc(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Btc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtcEmitter<Mem, Gpq> for Assembler<'_> {
    fn btc(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Btc as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> BtcEmitter<Mem, U1> for Assembler<'_> {
    fn btc(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::Btc as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl BtrEmitter<Gp, Gp> for Assembler<'_> {
    fn btr(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Btr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtrEmitter<Gpw, Gpw> for Assembler<'_> {
    fn btr(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Btr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtrEmitter<Gpd, Gpd> for Assembler<'_> {
    fn btr(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Btr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtrEmitter<Gpq, Gpq> for Assembler<'_> {
    fn btr(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Btr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtrEmitter<Gp, Imm> for Assembler<'_> {
    fn btr(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Btr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> BtrEmitter<Gpw, U1> for Assembler<'_> {
    fn btr(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Btr as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> BtrEmitter<Gpd, U1> for Assembler<'_> {
    fn btr(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Btr as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> BtrEmitter<Gpq, U1> for Assembler<'_> {
    fn btr(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Btr as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl BtrEmitter<Mem, Gp> for Assembler<'_> {
    fn btr(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Btr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtrEmitter<Mem, Gpw> for Assembler<'_> {
    fn btr(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::Btr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtrEmitter<Mem, Gpd> for Assembler<'_> {
    fn btr(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Btr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtrEmitter<Mem, Gpq> for Assembler<'_> {
    fn btr(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Btr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> BtrEmitter<Mem, U1> for Assembler<'_> {
    fn btr(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::Btr as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl BtsEmitter<Gp, Gp> for Assembler<'_> {
    fn bts(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Bts as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtsEmitter<Gpw, Gpw> for Assembler<'_> {
    fn bts(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Bts as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtsEmitter<Gpd, Gpd> for Assembler<'_> {
    fn bts(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Bts as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtsEmitter<Gpq, Gpq> for Assembler<'_> {
    fn bts(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Bts as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtsEmitter<Gp, Imm> for Assembler<'_> {
    fn bts(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Bts as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> BtsEmitter<Gpw, U1> for Assembler<'_> {
    fn bts(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Bts as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> BtsEmitter<Gpd, U1> for Assembler<'_> {
    fn bts(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Bts as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> BtsEmitter<Gpq, U1> for Assembler<'_> {
    fn bts(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Bts as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl BtsEmitter<Mem, Gp> for Assembler<'_> {
    fn bts(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Bts as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtsEmitter<Mem, Gpw> for Assembler<'_> {
    fn bts(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::Bts as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtsEmitter<Mem, Gpd> for Assembler<'_> {
    fn bts(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Bts as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl BtsEmitter<Mem, Gpq> for Assembler<'_> {
    fn bts(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Bts as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> BtsEmitter<Mem, U1> for Assembler<'_> {
    fn bts(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::Bts as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl CbwEmitter<Gp> for Assembler<'_> {
    fn cbw(&mut self, op0: Gp) {
        self.emit_n(InstId::Cbw as u32, &[op0.as_operand()]); // ANY [EXPLICIT] AX <- Sign Extend AL
    }
}

impl CbwEmitter<Gpw> for Assembler<'_> {
    fn cbw(&mut self, op0: Gpw) {
        self.emit_n(InstId::Cbw as u32, &[op0.as_operand()]); // ANY [EXPLICIT] AX <- Sign Extend AL
    }
}

impl CdqEmitter<Gp, Gp> for Assembler<'_> {
    fn cdq(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Cdq as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] EDX:EAX <- Sign Extend EAX
    }
}

impl CdqEmitter<Gpd, Gpd> for Assembler<'_> {
    fn cdq(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Cdq as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] EDX:EAX <- Sign Extend EAX
    }
}

impl CdqeEmitter<Gp> for Assembler<'_> {
    fn cdqe(&mut self, op0: Gp) {
        self.emit_n(InstId::Cdqe as u32, &[op0.as_operand()]); // X64 [EXPLICIT] RAX <- Sign Extend EAX
    }
}

impl CdqeEmitter<Gpq> for Assembler<'_> {
    fn cdqe(&mut self, op0: Gpq) {
        self.emit_n(InstId::Cdqe as u32, &[op0.as_operand()]); // X64 [EXPLICIT] RAX <- Sign Extend EAX
    }
}

impl CqoEmitter<Gp, Gp> for Assembler<'_> {
    fn cqo(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Cqo as u32, &[op0.as_operand(), op1.as_operand()]); // X64 [EXPLICIT] RDX:RAX <- Sign Extend RAX
    }
}

impl CqoEmitter<Gpq, Gpq> for Assembler<'_> {
    fn cqo(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Cqo as u32, &[op0.as_operand(), op1.as_operand()]); // X64 [EXPLICIT] RDX:RAX <- Sign Extend RAX
    }
}

impl CwdEmitter<Gp, Gp> for Assembler<'_> {
    fn cwd(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Cwd as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] DX:AX <- Sign Extend AX
    }
}

impl CwdEmitter<Gpw, Gpw> for Assembler<'_> {
    fn cwd(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Cwd as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] DX:AX <- Sign Extend AX
    }
}

impl CwdeEmitter<Gp> for Assembler<'_> {
    fn cwde(&mut self, op0: Gp) {
        self.emit_n(InstId::Cwde as u32, &[op0.as_operand()]); // ANY [EXPLICIT] EAX <- Sign Extend AX
    }
}

impl CwdeEmitter<Gpd> for Assembler<'_> {
    fn cwde(&mut self, op0: Gpd) {
        self.emit_n(InstId::Cwde as u32, &[op0.as_operand()]); // ANY [EXPLICIT] EAX <- Sign Extend AX
    }
}

impl CallEmitter<Gp> for Assembler<'_> {
    fn call(&mut self, op0: Gp) {
        self.emit_n(InstId::Call as u32, &[op0.as_operand()]); // ANY
    }
}

impl CallEmitter<Gpw> for Assembler<'_> {
    fn call(&mut self, op0: Gpw) {
        self.emit_n(InstId::Call as u32, &[op0.as_operand()]); // ANY
    }
}

impl CallEmitter<Gpd> for Assembler<'_> {
    fn call(&mut self, op0: Gpd) {
        self.emit_n(InstId::Call as u32, &[op0.as_operand()]); // ANY
    }
}

impl CallEmitter<Gpq> for Assembler<'_> {
    fn call(&mut self, op0: Gpq) {
        self.emit_n(InstId::Call as u32, &[op0.as_operand()]); // ANY
    }
}

impl CallEmitter<Mem> for Assembler<'_> {
    fn call(&mut self, op0: Mem) {
        self.emit_n(InstId::Call as u32, &[op0.as_operand()]); // ANY
    }
}

impl CallEmitter<Label> for Assembler<'_> {
    fn call(&mut self, op0: Label) {
        self.emit_n(InstId::Call as u32, &[op0.as_operand()]); // ANY
    }
}

impl<U0: Into<Imm>> CallEmitter<U0> for Assembler<'_> {
    fn call(&mut self, op0: U0) {
        self.emit_n(InstId::Call as u32, &[Into::<Imm>::into(op0).as_operand()]); // ANY
    }
}

impl CmovEmitter<Gp, Gp> for Assembler<'_> {
    fn cmov(&mut self, cc: CondCode, op0: Gp, op1: Gp) {
        self.emit_n(
            CMOVCC_FROM_COND[cc as usize],
            &[op0.as_operand(), op1.as_operand()],
        ); // CMOV
    }
    fn cmovo(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Cmovo as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovno(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Cmovno as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovb(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Cmovb as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnb(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Cmovnb as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovz(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Cmovz as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnz(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Cmovnz as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovbe(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Cmovbe as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnbe(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(
            InstId::Cmovnbe as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
    fn cmovs(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Cmovs as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovns(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Cmovns as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovp(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Cmovp as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnp(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Cmovnp as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovl(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Cmovl as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnl(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Cmovnl as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovle(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Cmovle as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnle(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(
            InstId::Cmovnle as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl CmovEmitter<Gpw, Gpw> for Assembler<'_> {
    fn cmov(&mut self, cc: CondCode, op0: Gpw, op1: Gpw) {
        self.emit_n(
            CMOVCC_FROM_COND[cc as usize],
            &[op0.as_operand(), op1.as_operand()],
        ); // CMOV
    }
    fn cmovo(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Cmovo as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovno(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Cmovno as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovb(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Cmovb as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnb(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Cmovnb as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovz(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Cmovz as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnz(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Cmovnz as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovbe(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Cmovbe as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnbe(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(
            InstId::Cmovnbe as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
    fn cmovs(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Cmovs as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovns(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Cmovns as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovp(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Cmovp as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnp(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Cmovnp as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovl(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Cmovl as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnl(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Cmovnl as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovle(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Cmovle as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnle(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(
            InstId::Cmovnle as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl CmovEmitter<Gpd, Gpd> for Assembler<'_> {
    fn cmov(&mut self, cc: CondCode, op0: Gpd, op1: Gpd) {
        self.emit_n(
            CMOVCC_FROM_COND[cc as usize],
            &[op0.as_operand(), op1.as_operand()],
        ); // CMOV
    }
    fn cmovo(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Cmovo as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovno(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Cmovno as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovb(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Cmovb as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnb(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Cmovnb as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovz(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Cmovz as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnz(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Cmovnz as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovbe(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Cmovbe as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnbe(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(
            InstId::Cmovnbe as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
    fn cmovs(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Cmovs as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovns(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Cmovns as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovp(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Cmovp as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnp(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Cmovnp as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovl(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Cmovl as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnl(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Cmovnl as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovle(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Cmovle as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnle(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(
            InstId::Cmovnle as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl CmovEmitter<Gpq, Gpq> for Assembler<'_> {
    fn cmov(&mut self, cc: CondCode, op0: Gpq, op1: Gpq) {
        self.emit_n(
            CMOVCC_FROM_COND[cc as usize],
            &[op0.as_operand(), op1.as_operand()],
        ); // CMOV
    }
    fn cmovo(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Cmovo as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovno(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Cmovno as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovb(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Cmovb as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnb(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Cmovnb as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovz(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Cmovz as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnz(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Cmovnz as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovbe(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Cmovbe as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnbe(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(
            InstId::Cmovnbe as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
    fn cmovs(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Cmovs as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovns(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Cmovns as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovp(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Cmovp as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnp(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Cmovnp as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovl(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Cmovl as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnl(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Cmovnl as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovle(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Cmovle as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnle(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(
            InstId::Cmovnle as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl CmovEmitter<Gp, Mem> for Assembler<'_> {
    fn cmov(&mut self, cc: CondCode, op0: Gp, op1: Mem) {
        self.emit_n(
            CMOVCC_FROM_COND[cc as usize],
            &[op0.as_operand(), op1.as_operand()],
        ); // CMOV
    }
    fn cmovo(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Cmovo as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovno(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Cmovno as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovb(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Cmovb as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnb(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Cmovnb as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovz(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Cmovz as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnz(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Cmovnz as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovbe(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Cmovbe as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnbe(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Cmovnbe as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
    fn cmovs(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Cmovs as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovns(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Cmovns as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovp(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Cmovp as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnp(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Cmovnp as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovl(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Cmovl as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnl(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Cmovnl as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovle(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Cmovle as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnle(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Cmovnle as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl CmovEmitter<Gpw, Mem> for Assembler<'_> {
    fn cmov(&mut self, cc: CondCode, op0: Gpw, op1: Mem) {
        self.emit_n(
            CMOVCC_FROM_COND[cc as usize],
            &[op0.as_operand(), op1.as_operand()],
        ); // CMOV
    }
    fn cmovo(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Cmovo as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovno(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Cmovno as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovb(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Cmovb as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnb(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Cmovnb as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovz(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Cmovz as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnz(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Cmovnz as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovbe(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Cmovbe as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnbe(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(
            InstId::Cmovnbe as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
    fn cmovs(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Cmovs as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovns(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Cmovns as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovp(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Cmovp as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnp(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Cmovnp as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovl(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Cmovl as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnl(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Cmovnl as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovle(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Cmovle as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnle(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(
            InstId::Cmovnle as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl CmovEmitter<Gpd, Mem> for Assembler<'_> {
    fn cmov(&mut self, cc: CondCode, op0: Gpd, op1: Mem) {
        self.emit_n(
            CMOVCC_FROM_COND[cc as usize],
            &[op0.as_operand(), op1.as_operand()],
        ); // CMOV
    }
    fn cmovo(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Cmovo as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovno(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Cmovno as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovb(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Cmovb as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnb(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Cmovnb as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovz(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Cmovz as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnz(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Cmovnz as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovbe(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Cmovbe as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnbe(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Cmovnbe as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
    fn cmovs(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Cmovs as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovns(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Cmovns as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovp(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Cmovp as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnp(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Cmovnp as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovl(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Cmovl as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnl(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Cmovnl as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovle(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Cmovle as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnle(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Cmovnle as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl CmovEmitter<Gpq, Mem> for Assembler<'_> {
    fn cmov(&mut self, cc: CondCode, op0: Gpq, op1: Mem) {
        self.emit_n(
            CMOVCC_FROM_COND[cc as usize],
            &[op0.as_operand(), op1.as_operand()],
        ); // CMOV
    }
    fn cmovo(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Cmovo as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovno(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Cmovno as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovb(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Cmovb as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnb(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Cmovnb as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovz(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Cmovz as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnz(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Cmovnz as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovbe(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Cmovbe as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnbe(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Cmovnbe as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
    fn cmovs(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Cmovs as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovns(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Cmovns as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovp(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Cmovp as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnp(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Cmovnp as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovl(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Cmovl as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnl(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Cmovnl as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovle(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Cmovle as u32, &[op0.as_operand(), op1.as_operand()]);
    }
    fn cmovnle(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Cmovnle as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl CmpEmitter<Gp, Gp> for Assembler<'_> {
    fn cmp(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Cmp as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl CmpEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn cmp(&mut self, op0: GpbLo, op1: GpbLo) {
        self.emit_n(InstId::Cmp as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl CmpEmitter<GpbLo, GpbHi> for Assembler<'_> {
    fn cmp(&mut self, op0: GpbLo, op1: GpbHi) {
        self.emit_n(InstId::Cmp as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl CmpEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn cmp(&mut self, op0: GpbHi, op1: GpbLo) {
        self.emit_n(InstId::Cmp as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl CmpEmitter<GpbHi, GpbHi> for Assembler<'_> {
    fn cmp(&mut self, op0: GpbHi, op1: GpbHi) {
        self.emit_n(InstId::Cmp as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl CmpEmitter<Gpw, Gpw> for Assembler<'_> {
    fn cmp(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Cmp as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl CmpEmitter<Gpd, Gpd> for Assembler<'_> {
    fn cmp(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Cmp as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl CmpEmitter<Gpq, Gpq> for Assembler<'_> {
    fn cmp(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Cmp as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl CmpEmitter<Gp, Mem> for Assembler<'_> {
    fn cmp(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Cmp as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl CmpEmitter<GpbLo, Mem> for Assembler<'_> {
    fn cmp(&mut self, op0: GpbLo, op1: Mem) {
        self.emit_n(InstId::Cmp as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl CmpEmitter<GpbHi, Mem> for Assembler<'_> {
    fn cmp(&mut self, op0: GpbHi, op1: Mem) {
        self.emit_n(InstId::Cmp as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl CmpEmitter<Gpw, Mem> for Assembler<'_> {
    fn cmp(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Cmp as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl CmpEmitter<Gpd, Mem> for Assembler<'_> {
    fn cmp(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Cmp as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl CmpEmitter<Gpq, Mem> for Assembler<'_> {
    fn cmp(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Cmp as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl CmpEmitter<Gp, Imm> for Assembler<'_> {
    fn cmp(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Cmp as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> CmpEmitter<GpbLo, U1> for Assembler<'_> {
    fn cmp(&mut self, op0: GpbLo, op1: U1) {
        self.emit_n(
            InstId::Cmp as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> CmpEmitter<GpbHi, U1> for Assembler<'_> {
    fn cmp(&mut self, op0: GpbHi, op1: U1) {
        self.emit_n(
            InstId::Cmp as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> CmpEmitter<Gpw, U1> for Assembler<'_> {
    fn cmp(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Cmp as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> CmpEmitter<Gpd, U1> for Assembler<'_> {
    fn cmp(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Cmp as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> CmpEmitter<Gpq, U1> for Assembler<'_> {
    fn cmp(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Cmp as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl CmpEmitter<Mem, Gp> for Assembler<'_> {
    fn cmp(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Cmp as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl CmpEmitter<Mem, GpbLo> for Assembler<'_> {
    fn cmp(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::Cmp as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl CmpEmitter<Mem, GpbHi> for Assembler<'_> {
    fn cmp(&mut self, op0: Mem, op1: GpbHi) {
        self.emit_n(InstId::Cmp as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl CmpEmitter<Mem, Gpw> for Assembler<'_> {
    fn cmp(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::Cmp as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl CmpEmitter<Mem, Gpd> for Assembler<'_> {
    fn cmp(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Cmp as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl CmpEmitter<Mem, Gpq> for Assembler<'_> {
    fn cmp(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Cmp as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> CmpEmitter<Mem, U1> for Assembler<'_> {
    fn cmp(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::Cmp as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl CmpsEmitter<Mem, Mem> for Assembler<'_> {
    fn cmps(&mut self, op0: Mem, op1: Mem) {
        self.emit_n(InstId::Cmps as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT]
    }
}

impl CmpxchgEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn cmpxchg(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<GpbLo, GpbLo, GpbLo> for Assembler<'_> {
    fn cmpxchg(&mut self, op0: GpbLo, op1: GpbLo, op2: GpbLo) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<GpbLo, GpbHi, GpbLo> for Assembler<'_> {
    fn cmpxchg(&mut self, op0: GpbLo, op1: GpbHi, op2: GpbLo) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<GpbHi, GpbLo, GpbLo> for Assembler<'_> {
    fn cmpxchg(&mut self, op0: GpbHi, op1: GpbLo, op2: GpbLo) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<GpbHi, GpbHi, GpbLo> for Assembler<'_> {
    fn cmpxchg(&mut self, op0: GpbHi, op1: GpbHi, op2: GpbLo) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<Gpw, Gpw, Gpw> for Assembler<'_> {
    fn cmpxchg(&mut self, op0: Gpw, op1: Gpw, op2: Gpw) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn cmpxchg(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn cmpxchg(&mut self, op0: Gpq, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpxchg(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<Mem, GpbLo, GpbLo> for Assembler<'_> {
    fn cmpxchg(&mut self, op0: Mem, op1: GpbLo, op2: GpbLo) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<Mem, GpbHi, GpbLo> for Assembler<'_> {
    fn cmpxchg(&mut self, op0: Mem, op1: GpbHi, op2: GpbLo) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<Mem, Gpw, Gpw> for Assembler<'_> {
    fn cmpxchg(&mut self, op0: Mem, op1: Gpw, op2: Gpw) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpxchg(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl CmpxchgEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpxchg(&mut self, op0: Mem, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // I486 [EXPLICIT]
    }
}

impl Cmpxchg16bEmitter<Mem, Gp, Gp, Gp, Gp> for Assembler<'_> {
    fn cmpxchg16b(&mut self, op0: Mem, op1: Gp, op2: Gp, op3: Gp, op4: Gp) {
        self.emit_n(
            InstId::Cmpxchg16b as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                op4.as_operand(),
            ],
        ); // CMPXCHG16B [EXPLICIT] m == EDX:EAX ? m <- ECX:EBX
    }
}

impl Cmpxchg16bEmitter<Mem, Gpq, Gpq, Gpq, Gpq> for Assembler<'_> {
    fn cmpxchg16b(&mut self, op0: Mem, op1: Gpq, op2: Gpq, op3: Gpq, op4: Gpq) {
        self.emit_n(
            InstId::Cmpxchg16b as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                op4.as_operand(),
            ],
        ); // CMPXCHG16B [EXPLICIT] m == EDX:EAX ? m <- ECX:EBX
    }
}

impl Cmpxchg8bEmitter<Mem, Gp, Gp, Gp, Gp> for Assembler<'_> {
    fn cmpxchg8b(&mut self, op0: Mem, op1: Gp, op2: Gp, op3: Gp, op4: Gp) {
        self.emit_n(
            InstId::Cmpxchg8b as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                op4.as_operand(),
            ],
        ); // CMPXCHG8B [EXPLICIT] m == RDX:RAX ? m <- RCX:RBX
    }
}

impl Cmpxchg8bEmitter<Mem, Gpd, Gpd, Gpd, Gpd> for Assembler<'_> {
    fn cmpxchg8b(&mut self, op0: Mem, op1: Gpd, op2: Gpd, op3: Gpd, op4: Gpd) {
        self.emit_n(
            InstId::Cmpxchg8b as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                op4.as_operand(),
            ],
        ); // CMPXCHG8B [EXPLICIT] m == RDX:RAX ? m <- RCX:RBX
    }
}

impl DecEmitter<Gp> for Assembler<'_> {
    fn dec(&mut self, op0: Gp) {
        self.emit_n(InstId::Dec as u32, &[op0.as_operand()]); // ANY
    }
}

impl DecEmitter<Gpw> for Assembler<'_> {
    fn dec(&mut self, op0: Gpw) {
        self.emit_n(InstId::Dec as u32, &[op0.as_operand()]); // ANY
    }
}

impl DecEmitter<Gpd> for Assembler<'_> {
    fn dec(&mut self, op0: Gpd) {
        self.emit_n(InstId::Dec as u32, &[op0.as_operand()]); // ANY
    }
}

impl DecEmitter<GpbLo> for Assembler<'_> {
    fn dec(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Dec as u32, &[op0.as_operand()]); // ANY
    }
}

impl DecEmitter<GpbHi> for Assembler<'_> {
    fn dec(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Dec as u32, &[op0.as_operand()]); // ANY
    }
}

impl DecEmitter<Gpq> for Assembler<'_> {
    fn dec(&mut self, op0: Gpq) {
        self.emit_n(InstId::Dec as u32, &[op0.as_operand()]); // ANY
    }
}

impl DecEmitter<Mem> for Assembler<'_> {
    fn dec(&mut self, op0: Mem) {
        self.emit_n(InstId::Dec as u32, &[op0.as_operand()]); // ANY
    }
}

impl DivEmitter<Gp, Gp> for Assembler<'_> {
    fn div(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Div as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AH[Rem]: AL[Quot] <- AX / r8
    }
}

impl DivEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn div(&mut self, op0: Gpw, op1: GpbLo) {
        self.emit_n(InstId::Div as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AH[Rem]: AL[Quot] <- AX / r8
    }
}

impl DivEmitter<Gpw, GpbHi> for Assembler<'_> {
    fn div(&mut self, op0: Gpw, op1: GpbHi) {
        self.emit_n(InstId::Div as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AH[Rem]: AL[Quot] <- AX / r8
    }
}

impl DivEmitter<Gp, Mem> for Assembler<'_> {
    fn div(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Div as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AH[Rem]: AL[Quot] <- AX / m8
    }
}

impl DivEmitter<Gpw, Mem> for Assembler<'_> {
    fn div(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Div as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AH[Rem]: AL[Quot] <- AX / m8
    }
}

impl Div3Emitter<Gp, Gp, Gp> for Assembler<'_> {
    fn div_3(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Div as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / r16|r32|r64
    }
}

impl Div3Emitter<Gpw, Gpw, Gpw> for Assembler<'_> {
    fn div_3(&mut self, op0: Gpw, op1: Gpw, op2: Gpw) {
        self.emit_n(
            InstId::Div as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / r16|r32|r64
    }
}

impl Div3Emitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn div_3(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Div as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / r16|r32|r64
    }
}

impl Div3Emitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn div_3(&mut self, op0: Gpq, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Div as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / r16|r32|r64
    }
}

impl Div3Emitter<Gp, Gp, Mem> for Assembler<'_> {
    fn div_3(&mut self, op0: Gp, op1: Gp, op2: Mem) {
        self.emit_n(
            InstId::Div as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / m16|m32|m64
    }
}

impl Div3Emitter<Gpw, Gpw, Mem> for Assembler<'_> {
    fn div_3(&mut self, op0: Gpw, op1: Gpw, op2: Mem) {
        self.emit_n(
            InstId::Div as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / m16|m32|m64
    }
}

impl Div3Emitter<Gpd, Gpd, Mem> for Assembler<'_> {
    fn div_3(&mut self, op0: Gpd, op1: Gpd, op2: Mem) {
        self.emit_n(
            InstId::Div as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / m16|m32|m64
    }
}

impl Div3Emitter<Gpq, Gpq, Mem> for Assembler<'_> {
    fn div_3(&mut self, op0: Gpq, op1: Gpq, op2: Mem) {
        self.emit_n(
            InstId::Div as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / m16|m32|m64
    }
}

impl IdivEmitter<Gp, Gp> for Assembler<'_> {
    fn idiv(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Idiv as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AH[Rem]: AL[Quot] <- AX / r8
    }
}

impl IdivEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn idiv(&mut self, op0: Gpw, op1: GpbLo) {
        self.emit_n(InstId::Idiv as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AH[Rem]: AL[Quot] <- AX / r8
    }
}

impl IdivEmitter<Gpw, GpbHi> for Assembler<'_> {
    fn idiv(&mut self, op0: Gpw, op1: GpbHi) {
        self.emit_n(InstId::Idiv as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AH[Rem]: AL[Quot] <- AX / r8
    }
}

impl IdivEmitter<Gp, Mem> for Assembler<'_> {
    fn idiv(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Idiv as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AH[Rem]: AL[Quot] <- AX / m8
    }
}

impl IdivEmitter<Gpw, Mem> for Assembler<'_> {
    fn idiv(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Idiv as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AH[Rem]: AL[Quot] <- AX / m8
    }
}

impl Idiv3Emitter<Gp, Gp, Gp> for Assembler<'_> {
    fn idiv_3(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Idiv as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / r16|r32|r64
    }
}

impl Idiv3Emitter<Gpw, Gpw, Gpw> for Assembler<'_> {
    fn idiv_3(&mut self, op0: Gpw, op1: Gpw, op2: Gpw) {
        self.emit_n(
            InstId::Idiv as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / r16|r32|r64
    }
}

impl Idiv3Emitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn idiv_3(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Idiv as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / r16|r32|r64
    }
}

impl Idiv3Emitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn idiv_3(&mut self, op0: Gpq, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Idiv as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / r16|r32|r64
    }
}

impl Idiv3Emitter<Gp, Gp, Mem> for Assembler<'_> {
    fn idiv_3(&mut self, op0: Gp, op1: Gp, op2: Mem) {
        self.emit_n(
            InstId::Idiv as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / m16|m32|m64
    }
}

impl Idiv3Emitter<Gpw, Gpw, Mem> for Assembler<'_> {
    fn idiv_3(&mut self, op0: Gpw, op1: Gpw, op2: Mem) {
        self.emit_n(
            InstId::Idiv as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / m16|m32|m64
    }
}

impl Idiv3Emitter<Gpd, Gpd, Mem> for Assembler<'_> {
    fn idiv_3(&mut self, op0: Gpd, op1: Gpd, op2: Mem) {
        self.emit_n(
            InstId::Idiv as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / m16|m32|m64
    }
}

impl Idiv3Emitter<Gpq, Gpq, Mem> for Assembler<'_> {
    fn idiv_3(&mut self, op0: Gpq, op1: Gpq, op2: Mem) {
        self.emit_n(
            InstId::Idiv as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX[Rem]:xAX[Quot] <- xDX:xAX / m16|m32|m64
    }
}

impl ImulEmitter<Gp, Gp> for Assembler<'_> {
    fn imul(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Imul as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AX <- AL * r8 | ra <- ra * rb
    }
}

impl ImulEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn imul(&mut self, op0: Gpw, op1: GpbLo) {
        self.emit_n(InstId::Imul as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AX <- AL * r8 | ra <- ra * rb
    }
}

impl ImulEmitter<Gpw, GpbHi> for Assembler<'_> {
    fn imul(&mut self, op0: Gpw, op1: GpbHi) {
        self.emit_n(InstId::Imul as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AX <- AL * r8 | ra <- ra * rb
    }
}

impl ImulEmitter<Gpw, Gpw> for Assembler<'_> {
    fn imul(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Imul as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AX <- AL * r8 | ra <- ra * rb
    }
}

impl ImulEmitter<Gpd, Gpd> for Assembler<'_> {
    fn imul(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Imul as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AX <- AL * r8 | ra <- ra * rb
    }
}

impl ImulEmitter<Gpq, Gpq> for Assembler<'_> {
    fn imul(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Imul as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AX <- AL * r8 | ra <- ra * rb
    }
}

impl ImulEmitter<Gp, Mem> for Assembler<'_> {
    fn imul(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Imul as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AX <- AL * m8 | ra <- ra * m16|m32|m64
    }
}

impl ImulEmitter<Gpw, Mem> for Assembler<'_> {
    fn imul(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Imul as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AX <- AL * m8 | ra <- ra * m16|m32|m64
    }
}

impl ImulEmitter<Gpd, Mem> for Assembler<'_> {
    fn imul(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Imul as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AX <- AL * m8 | ra <- ra * m16|m32|m64
    }
}

impl ImulEmitter<Gpq, Mem> for Assembler<'_> {
    fn imul(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Imul as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AX <- AL * m8 | ra <- ra * m16|m32|m64
    }
}

impl Imul3Emitter<Gp, Gp, Imm> for Assembler<'_> {
    fn imul_3(&mut self, op0: Gp, op1: Gp, op2: Imm) {
        self.emit_n(
            InstId::Imul as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl<U2: Into<Imm>> Imul3Emitter<Gpw, Gpw, U2> for Assembler<'_> {
    fn imul_3(&mut self, op0: Gpw, op1: Gpw, op2: U2) {
        self.emit_n(
            InstId::Imul as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> Imul3Emitter<Gpd, Gpd, U2> for Assembler<'_> {
    fn imul_3(&mut self, op0: Gpd, op1: Gpd, op2: U2) {
        self.emit_n(
            InstId::Imul as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> Imul3Emitter<Gpq, Gpq, U2> for Assembler<'_> {
    fn imul_3(&mut self, op0: Gpq, op1: Gpq, op2: U2) {
        self.emit_n(
            InstId::Imul as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // ANY
    }
}

impl Imul3Emitter<Gp, Mem, Imm> for Assembler<'_> {
    fn imul_3(&mut self, op0: Gp, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Imul as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl<U2: Into<Imm>> Imul3Emitter<Gpw, Mem, U2> for Assembler<'_> {
    fn imul_3(&mut self, op0: Gpw, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Imul as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> Imul3Emitter<Gpd, Mem, U2> for Assembler<'_> {
    fn imul_3(&mut self, op0: Gpd, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Imul as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> Imul3Emitter<Gpq, Mem, U2> for Assembler<'_> {
    fn imul_3(&mut self, op0: Gpq, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Imul as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // ANY
    }
}

impl Imul3Emitter<Gp, Gp, Gp> for Assembler<'_> {
    fn imul_3(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Imul as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * r16|r32|r64
    }
}

impl Imul3Emitter<Gpw, Gpw, Gpw> for Assembler<'_> {
    fn imul_3(&mut self, op0: Gpw, op1: Gpw, op2: Gpw) {
        self.emit_n(
            InstId::Imul as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * r16|r32|r64
    }
}

impl Imul3Emitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn imul_3(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Imul as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * r16|r32|r64
    }
}

impl Imul3Emitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn imul_3(&mut self, op0: Gpq, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Imul as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * r16|r32|r64
    }
}

impl Imul3Emitter<Gp, Gp, Mem> for Assembler<'_> {
    fn imul_3(&mut self, op0: Gp, op1: Gp, op2: Mem) {
        self.emit_n(
            InstId::Imul as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * m16|m32|m64
    }
}

impl Imul3Emitter<Gpw, Gpw, Mem> for Assembler<'_> {
    fn imul_3(&mut self, op0: Gpw, op1: Gpw, op2: Mem) {
        self.emit_n(
            InstId::Imul as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * m16|m32|m64
    }
}

impl Imul3Emitter<Gpd, Gpd, Mem> for Assembler<'_> {
    fn imul_3(&mut self, op0: Gpd, op1: Gpd, op2: Mem) {
        self.emit_n(
            InstId::Imul as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * m16|m32|m64
    }
}

impl Imul3Emitter<Gpq, Gpq, Mem> for Assembler<'_> {
    fn imul_3(&mut self, op0: Gpq, op1: Gpq, op2: Mem) {
        self.emit_n(
            InstId::Imul as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * m16|m32|m64
    }
}

impl IncEmitter<Gp> for Assembler<'_> {
    fn inc(&mut self, op0: Gp) {
        self.emit_n(InstId::Inc as u32, &[op0.as_operand()]); // ANY
    }
}

impl IncEmitter<Gpw> for Assembler<'_> {
    fn inc(&mut self, op0: Gpw) {
        self.emit_n(InstId::Inc as u32, &[op0.as_operand()]); // ANY
    }
}

impl IncEmitter<Gpd> for Assembler<'_> {
    fn inc(&mut self, op0: Gpd) {
        self.emit_n(InstId::Inc as u32, &[op0.as_operand()]); // ANY
    }
}

impl IncEmitter<GpbLo> for Assembler<'_> {
    fn inc(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Inc as u32, &[op0.as_operand()]); // ANY
    }
}

impl IncEmitter<GpbHi> for Assembler<'_> {
    fn inc(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Inc as u32, &[op0.as_operand()]); // ANY
    }
}

impl IncEmitter<Gpq> for Assembler<'_> {
    fn inc(&mut self, op0: Gpq) {
        self.emit_n(InstId::Inc as u32, &[op0.as_operand()]); // ANY
    }
}

impl IncEmitter<Mem> for Assembler<'_> {
    fn inc(&mut self, op0: Mem) {
        self.emit_n(InstId::Inc as u32, &[op0.as_operand()]); // ANY
    }
}

impl JEmitter<Label> for Assembler<'_> {
    fn j(&mut self, cc: CondCode, op0: Label) {
        self.emit_n(JCC_FROM_COND[cc as usize], &[op0.as_operand()]); // ANY
    }
    fn jo(&mut self, op0: Label) {
        self.emit_n(InstId::Jo as u32, &[op0.as_operand()]);
    }
    fn jno(&mut self, op0: Label) {
        self.emit_n(InstId::Jno as u32, &[op0.as_operand()]);
    }
    fn jb(&mut self, op0: Label) {
        self.emit_n(InstId::Jb as u32, &[op0.as_operand()]);
    }
    fn jnb(&mut self, op0: Label) {
        self.emit_n(InstId::Jnb as u32, &[op0.as_operand()]);
    }
    fn jz(&mut self, op0: Label) {
        self.emit_n(InstId::Jz as u32, &[op0.as_operand()]);
    }
    fn jnz(&mut self, op0: Label) {
        self.emit_n(InstId::Jnz as u32, &[op0.as_operand()]);
    }
    fn jbe(&mut self, op0: Label) {
        self.emit_n(InstId::Jbe as u32, &[op0.as_operand()]);
    }
    fn jnbe(&mut self, op0: Label) {
        self.emit_n(InstId::Jnbe as u32, &[op0.as_operand()]);
    }
    fn js(&mut self, op0: Label) {
        self.emit_n(InstId::Js as u32, &[op0.as_operand()]);
    }
    fn jns(&mut self, op0: Label) {
        self.emit_n(InstId::Jns as u32, &[op0.as_operand()]);
    }
    fn jp(&mut self, op0: Label) {
        self.emit_n(InstId::Jp as u32, &[op0.as_operand()]);
    }
    fn jnp(&mut self, op0: Label) {
        self.emit_n(InstId::Jnp as u32, &[op0.as_operand()]);
    }
    fn jl(&mut self, op0: Label) {
        self.emit_n(InstId::Jl as u32, &[op0.as_operand()]);
    }
    fn jnl(&mut self, op0: Label) {
        self.emit_n(InstId::Jnl as u32, &[op0.as_operand()]);
    }
    fn jle(&mut self, op0: Label) {
        self.emit_n(InstId::Jle as u32, &[op0.as_operand()]);
    }
    fn jnle(&mut self, op0: Label) {
        self.emit_n(InstId::Jnle as u32, &[op0.as_operand()]);
    }
}

impl<U0: Into<Imm>> JEmitter<U0> for Assembler<'_> {
    fn j(&mut self, cc: CondCode, op0: U0) {
        self.emit_n(
            JCC_FROM_COND[cc as usize],
            &[Into::<Imm>::into(op0).as_operand()],
        ); // ANY
    }
    fn jo(&mut self, op0: U0) {
        self.emit_n(InstId::Jo as u32, &[Into::<Imm>::into(op0).as_operand()]);
    }
    fn jno(&mut self, op0: U0) {
        self.emit_n(InstId::Jno as u32, &[Into::<Imm>::into(op0).as_operand()]);
    }
    fn jb(&mut self, op0: U0) {
        self.emit_n(InstId::Jb as u32, &[Into::<Imm>::into(op0).as_operand()]);
    }
    fn jnb(&mut self, op0: U0) {
        self.emit_n(InstId::Jnb as u32, &[Into::<Imm>::into(op0).as_operand()]);
    }
    fn jz(&mut self, op0: U0) {
        self.emit_n(InstId::Jz as u32, &[Into::<Imm>::into(op0).as_operand()]);
    }
    fn jnz(&mut self, op0: U0) {
        self.emit_n(InstId::Jnz as u32, &[Into::<Imm>::into(op0).as_operand()]);
    }
    fn jbe(&mut self, op0: U0) {
        self.emit_n(InstId::Jbe as u32, &[Into::<Imm>::into(op0).as_operand()]);
    }
    fn jnbe(&mut self, op0: U0) {
        self.emit_n(InstId::Jnbe as u32, &[Into::<Imm>::into(op0).as_operand()]);
    }
    fn js(&mut self, op0: U0) {
        self.emit_n(InstId::Js as u32, &[Into::<Imm>::into(op0).as_operand()]);
    }
    fn jns(&mut self, op0: U0) {
        self.emit_n(InstId::Jns as u32, &[Into::<Imm>::into(op0).as_operand()]);
    }
    fn jp(&mut self, op0: U0) {
        self.emit_n(InstId::Jp as u32, &[Into::<Imm>::into(op0).as_operand()]);
    }
    fn jnp(&mut self, op0: U0) {
        self.emit_n(InstId::Jnp as u32, &[Into::<Imm>::into(op0).as_operand()]);
    }
    fn jl(&mut self, op0: U0) {
        self.emit_n(InstId::Jl as u32, &[Into::<Imm>::into(op0).as_operand()]);
    }
    fn jnl(&mut self, op0: U0) {
        self.emit_n(InstId::Jnl as u32, &[Into::<Imm>::into(op0).as_operand()]);
    }
    fn jle(&mut self, op0: U0) {
        self.emit_n(InstId::Jle as u32, &[Into::<Imm>::into(op0).as_operand()]);
    }
    fn jnle(&mut self, op0: U0) {
        self.emit_n(InstId::Jnle as u32, &[Into::<Imm>::into(op0).as_operand()]);
    }
}

impl JecxzEmitter<Gp, Label> for Assembler<'_> {
    fn jecxz(&mut self, op0: Gp, op1: Label) {
        self.emit_n(InstId::Jecxz as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] Short jump if CX/ECX/RCX is zero.
    }
}

impl JecxzEmitter<Gpw, Label> for Assembler<'_> {
    fn jecxz(&mut self, op0: Gpw, op1: Label) {
        self.emit_n(InstId::Jecxz as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] Short jump if CX/ECX/RCX is zero.
    }
}

impl JecxzEmitter<Gpd, Label> for Assembler<'_> {
    fn jecxz(&mut self, op0: Gpd, op1: Label) {
        self.emit_n(InstId::Jecxz as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] Short jump if CX/ECX/RCX is zero.
    }
}

impl JecxzEmitter<Gpq, Label> for Assembler<'_> {
    fn jecxz(&mut self, op0: Gpq, op1: Label) {
        self.emit_n(InstId::Jecxz as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] Short jump if CX/ECX/RCX is zero.
    }
}

impl JecxzEmitter<Gp, Imm> for Assembler<'_> {
    fn jecxz(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Jecxz as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] Short jump if CX/ECX/RCX is zero.
    }
}

impl<U1: Into<Imm>> JecxzEmitter<Gpw, U1> for Assembler<'_> {
    fn jecxz(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Jecxz as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY [EXPLICIT] Short jump if CX/ECX/RCX is zero.
    }
}

impl<U1: Into<Imm>> JecxzEmitter<Gpd, U1> for Assembler<'_> {
    fn jecxz(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Jecxz as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY [EXPLICIT] Short jump if CX/ECX/RCX is zero.
    }
}

impl<U1: Into<Imm>> JecxzEmitter<Gpq, U1> for Assembler<'_> {
    fn jecxz(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Jecxz as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY [EXPLICIT] Short jump if CX/ECX/RCX is zero.
    }
}

impl JmpEmitter<Gp> for Assembler<'_> {
    fn jmp(&mut self, op0: Gp) {
        self.emit_n(InstId::Jmp as u32, &[op0.as_operand()]); // ANY
    }
}

impl JmpEmitter<Gpd> for Assembler<'_> {
    fn jmp(&mut self, op0: Gpd) {
        self.emit_n(InstId::Jmp as u32, &[op0.as_operand()]); // ANY
    }
}

impl JmpEmitter<Gpq> for Assembler<'_> {
    fn jmp(&mut self, op0: Gpq) {
        self.emit_n(InstId::Jmp as u32, &[op0.as_operand()]); // ANY
    }
}

impl JmpEmitter<Mem> for Assembler<'_> {
    fn jmp(&mut self, op0: Mem) {
        self.emit_n(InstId::Jmp as u32, &[op0.as_operand()]); // ANY
    }
}

impl JmpEmitter<Label> for Assembler<'_> {
    fn jmp(&mut self, op0: Label) {
        self.emit_n(InstId::Jmp as u32, &[op0.as_operand()]); // ANY
    }
}

impl<U0: Into<Imm>> JmpEmitter<U0> for Assembler<'_> {
    fn jmp(&mut self, op0: U0) {
        self.emit_n(InstId::Jmp as u32, &[Into::<Imm>::into(op0).as_operand()]); // ANY
    }
}

impl<U0: Into<Imm>, U1: Into<Imm>> LcallEmitter<U0, U1> for Assembler<'_> {
    fn lcall(&mut self, op0: U0, op1: U1) {
        self.emit_n(
            InstId::Lcall as u32,
            &[
                Into::<Imm>::into(op0).as_operand(),
                Into::<Imm>::into(op1).as_operand(),
            ],
        ); // ANY
    }
}

impl Lcall1Emitter<Mem> for Assembler<'_> {
    fn lcall_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Lcall as u32, &[op0.as_operand()]); // ANY
    }
}

impl LeaEmitter<Gp, Mem> for Assembler<'_> {
    fn lea(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Lea as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LeaEmitter<Gpw, Mem> for Assembler<'_> {
    fn lea(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Lea as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LeaEmitter<Gpd, Mem> for Assembler<'_> {
    fn lea(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Lea as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LeaEmitter<Gpq, Mem> for Assembler<'_> {
    fn lea(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Lea as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U0: Into<Imm>, U1: Into<Imm>> LjmpEmitter<U0, U1> for Assembler<'_> {
    fn ljmp(&mut self, op0: U0, op1: U1) {
        self.emit_n(
            InstId::Ljmp as u32,
            &[
                Into::<Imm>::into(op0).as_operand(),
                Into::<Imm>::into(op1).as_operand(),
            ],
        ); // ANY
    }
}

impl Ljmp1Emitter<Mem> for Assembler<'_> {
    fn ljmp_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Ljmp as u32, &[op0.as_operand()]); // ANY
    }
}

impl LodsEmitter<Gp, Mem> for Assembler<'_> {
    fn lods(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Lods as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT]
    }
}

impl LodsEmitter<GpbLo, Mem> for Assembler<'_> {
    fn lods(&mut self, op0: GpbLo, op1: Mem) {
        self.emit_n(InstId::Lods as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT]
    }
}

impl LodsEmitter<Gpw, Mem> for Assembler<'_> {
    fn lods(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Lods as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT]
    }
}

impl LodsEmitter<Gpd, Mem> for Assembler<'_> {
    fn lods(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Lods as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT]
    }
}

impl LodsEmitter<Gpq, Mem> for Assembler<'_> {
    fn lods(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Lods as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT]
    }
}

impl LoopEmitter<Gp, Label> for Assembler<'_> {
    fn r#loop(&mut self, op0: Gp, op1: Label) {
        self.emit_n(InstId::Loop as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0.
    }
}

impl LoopEmitter<Gpw, Label> for Assembler<'_> {
    fn r#loop(&mut self, op0: Gpw, op1: Label) {
        self.emit_n(InstId::Loop as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0.
    }
}

impl LoopEmitter<Gpd, Label> for Assembler<'_> {
    fn r#loop(&mut self, op0: Gpd, op1: Label) {
        self.emit_n(InstId::Loop as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0.
    }
}

impl LoopEmitter<Gpq, Label> for Assembler<'_> {
    fn r#loop(&mut self, op0: Gpq, op1: Label) {
        self.emit_n(InstId::Loop as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0.
    }
}

impl LoopEmitter<Gp, Imm> for Assembler<'_> {
    fn r#loop(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Loop as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0.
    }
}

impl<U1: Into<Imm>> LoopEmitter<Gpw, U1> for Assembler<'_> {
    fn r#loop(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Loop as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0.
    }
}

impl<U1: Into<Imm>> LoopEmitter<Gpd, U1> for Assembler<'_> {
    fn r#loop(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Loop as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0.
    }
}

impl<U1: Into<Imm>> LoopEmitter<Gpq, U1> for Assembler<'_> {
    fn r#loop(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Loop as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0.
    }
}

impl LoopeEmitter<Gp, Label> for Assembler<'_> {
    fn loope(&mut self, op0: Gp, op1: Label) {
        self.emit_n(InstId::Loope as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 1.
    }
}

impl LoopeEmitter<Gpw, Label> for Assembler<'_> {
    fn loope(&mut self, op0: Gpw, op1: Label) {
        self.emit_n(InstId::Loope as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 1.
    }
}

impl LoopeEmitter<Gpd, Label> for Assembler<'_> {
    fn loope(&mut self, op0: Gpd, op1: Label) {
        self.emit_n(InstId::Loope as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 1.
    }
}

impl LoopeEmitter<Gpq, Label> for Assembler<'_> {
    fn loope(&mut self, op0: Gpq, op1: Label) {
        self.emit_n(InstId::Loope as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 1.
    }
}

impl LoopeEmitter<Gp, Imm> for Assembler<'_> {
    fn loope(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Loope as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 1.
    }
}

impl<U1: Into<Imm>> LoopeEmitter<Gpw, U1> for Assembler<'_> {
    fn loope(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Loope as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 1.
    }
}

impl<U1: Into<Imm>> LoopeEmitter<Gpd, U1> for Assembler<'_> {
    fn loope(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Loope as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 1.
    }
}

impl<U1: Into<Imm>> LoopeEmitter<Gpq, U1> for Assembler<'_> {
    fn loope(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Loope as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 1.
    }
}

impl LoopneEmitter<Gp, Label> for Assembler<'_> {
    fn loopne(&mut self, op0: Gp, op1: Label) {
        self.emit_n(InstId::Loopne as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 0.
    }
}

impl LoopneEmitter<Gpw, Label> for Assembler<'_> {
    fn loopne(&mut self, op0: Gpw, op1: Label) {
        self.emit_n(InstId::Loopne as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 0.
    }
}

impl LoopneEmitter<Gpd, Label> for Assembler<'_> {
    fn loopne(&mut self, op0: Gpd, op1: Label) {
        self.emit_n(InstId::Loopne as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 0.
    }
}

impl LoopneEmitter<Gpq, Label> for Assembler<'_> {
    fn loopne(&mut self, op0: Gpq, op1: Label) {
        self.emit_n(InstId::Loopne as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 0.
    }
}

impl LoopneEmitter<Gp, Imm> for Assembler<'_> {
    fn loopne(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Loopne as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 0.
    }
}

impl<U1: Into<Imm>> LoopneEmitter<Gpw, U1> for Assembler<'_> {
    fn loopne(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Loopne as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 0.
    }
}

impl<U1: Into<Imm>> LoopneEmitter<Gpd, U1> for Assembler<'_> {
    fn loopne(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Loopne as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 0.
    }
}

impl<U1: Into<Imm>> LoopneEmitter<Gpq, U1> for Assembler<'_> {
    fn loopne(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Loopne as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY [EXPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 0.
    }
}

impl MovEmitter<Gp, Gp> for Assembler<'_> {
    fn mov(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn mov(&mut self, op0: GpbLo, op1: GpbLo) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<GpbLo, GpbHi> for Assembler<'_> {
    fn mov(&mut self, op0: GpbLo, op1: GpbHi) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn mov(&mut self, op0: GpbHi, op1: GpbLo) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<GpbHi, GpbHi> for Assembler<'_> {
    fn mov(&mut self, op0: GpbHi, op1: GpbHi) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpw, Gpw> for Assembler<'_> {
    fn mov(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpd, Gpd> for Assembler<'_> {
    fn mov(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpq, Gpq> for Assembler<'_> {
    fn mov(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Gp, Mem> for Assembler<'_> {
    fn mov(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<GpbLo, Mem> for Assembler<'_> {
    fn mov(&mut self, op0: GpbLo, op1: Mem) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<GpbHi, Mem> for Assembler<'_> {
    fn mov(&mut self, op0: GpbHi, op1: Mem) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpw, Mem> for Assembler<'_> {
    fn mov(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpd, Mem> for Assembler<'_> {
    fn mov(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpq, Mem> for Assembler<'_> {
    fn mov(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Gp, Imm> for Assembler<'_> {
    fn mov(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> MovEmitter<GpbLo, U1> for Assembler<'_> {
    fn mov(&mut self, op0: GpbLo, op1: U1) {
        self.emit_n(
            InstId::Mov as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> MovEmitter<GpbHi, U1> for Assembler<'_> {
    fn mov(&mut self, op0: GpbHi, op1: U1) {
        self.emit_n(
            InstId::Mov as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> MovEmitter<Gpw, U1> for Assembler<'_> {
    fn mov(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Mov as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> MovEmitter<Gpd, U1> for Assembler<'_> {
    fn mov(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Mov as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> MovEmitter<Gpq, U1> for Assembler<'_> {
    fn mov(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Mov as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl MovEmitter<Mem, Gp> for Assembler<'_> {
    fn mov(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Mem, GpbLo> for Assembler<'_> {
    fn mov(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Mem, GpbHi> for Assembler<'_> {
    fn mov(&mut self, op0: Mem, op1: GpbHi) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Mem, Gpw> for Assembler<'_> {
    fn mov(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Mem, Gpd> for Assembler<'_> {
    fn mov(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Mem, Gpq> for Assembler<'_> {
    fn mov(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> MovEmitter<Mem, U1> for Assembler<'_> {
    fn mov(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::Mov as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl MovEmitter<Gp, CReg> for Assembler<'_> {
    fn mov(&mut self, op0: Gp, op1: CReg) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpq, CReg> for Assembler<'_> {
    fn mov(&mut self, op0: Gpq, op1: CReg) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpd, CReg> for Assembler<'_> {
    fn mov(&mut self, op0: Gpd, op1: CReg) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<CReg, Gp> for Assembler<'_> {
    fn mov(&mut self, op0: CReg, op1: Gp) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<CReg, Gpq> for Assembler<'_> {
    fn mov(&mut self, op0: CReg, op1: Gpq) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<CReg, Gpd> for Assembler<'_> {
    fn mov(&mut self, op0: CReg, op1: Gpd) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Gp, DReg> for Assembler<'_> {
    fn mov(&mut self, op0: Gp, op1: DReg) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpq, DReg> for Assembler<'_> {
    fn mov(&mut self, op0: Gpq, op1: DReg) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpd, DReg> for Assembler<'_> {
    fn mov(&mut self, op0: Gpd, op1: DReg) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<DReg, Gp> for Assembler<'_> {
    fn mov(&mut self, op0: DReg, op1: Gp) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<DReg, Gpq> for Assembler<'_> {
    fn mov(&mut self, op0: DReg, op1: Gpq) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<DReg, Gpd> for Assembler<'_> {
    fn mov(&mut self, op0: DReg, op1: Gpd) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Gp, SReg> for Assembler<'_> {
    fn mov(&mut self, op0: Gp, op1: SReg) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpw, SReg> for Assembler<'_> {
    fn mov(&mut self, op0: Gpw, op1: SReg) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpd, SReg> for Assembler<'_> {
    fn mov(&mut self, op0: Gpd, op1: SReg) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Gpq, SReg> for Assembler<'_> {
    fn mov(&mut self, op0: Gpq, op1: SReg) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<Mem, SReg> for Assembler<'_> {
    fn mov(&mut self, op0: Mem, op1: SReg) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<SReg, Gp> for Assembler<'_> {
    fn mov(&mut self, op0: SReg, op1: Gp) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<SReg, Gpw> for Assembler<'_> {
    fn mov(&mut self, op0: SReg, op1: Gpw) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<SReg, Gpd> for Assembler<'_> {
    fn mov(&mut self, op0: SReg, op1: Gpd) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<SReg, Gpq> for Assembler<'_> {
    fn mov(&mut self, op0: SReg, op1: Gpq) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovEmitter<SReg, Mem> for Assembler<'_> {
    fn mov(&mut self, op0: SReg, op1: Mem) {
        self.emit_n(InstId::Mov as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovabsEmitter<Gp, Mem> for Assembler<'_> {
    fn movabs(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Movabs as u32, &[op0.as_operand(), op1.as_operand()]); // X64
    }
}

impl MovabsEmitter<GpbLo, Mem> for Assembler<'_> {
    fn movabs(&mut self, op0: GpbLo, op1: Mem) {
        self.emit_n(InstId::Movabs as u32, &[op0.as_operand(), op1.as_operand()]); // X64
    }
}

impl MovabsEmitter<Gpw, Mem> for Assembler<'_> {
    fn movabs(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Movabs as u32, &[op0.as_operand(), op1.as_operand()]); // X64
    }
}

impl MovabsEmitter<Gpd, Mem> for Assembler<'_> {
    fn movabs(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Movabs as u32, &[op0.as_operand(), op1.as_operand()]); // X64
    }
}

impl MovabsEmitter<Gpq, Mem> for Assembler<'_> {
    fn movabs(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Movabs as u32, &[op0.as_operand(), op1.as_operand()]); // X64
    }
}

impl MovabsEmitter<Gp, Imm> for Assembler<'_> {
    fn movabs(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Movabs as u32, &[op0.as_operand(), op1.as_operand()]); // X64
    }
}

impl<U1: Into<Imm>> MovabsEmitter<Gpq, U1> for Assembler<'_> {
    fn movabs(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Movabs as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // X64
    }
}

impl MovabsEmitter<Mem, Gp> for Assembler<'_> {
    fn movabs(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Movabs as u32, &[op0.as_operand(), op1.as_operand()]); // X64
    }
}

impl MovabsEmitter<Mem, GpbLo> for Assembler<'_> {
    fn movabs(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::Movabs as u32, &[op0.as_operand(), op1.as_operand()]); // X64
    }
}

impl MovabsEmitter<Mem, Gpw> for Assembler<'_> {
    fn movabs(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::Movabs as u32, &[op0.as_operand(), op1.as_operand()]); // X64
    }
}

impl MovabsEmitter<Mem, Gpd> for Assembler<'_> {
    fn movabs(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Movabs as u32, &[op0.as_operand(), op1.as_operand()]); // X64
    }
}

impl MovabsEmitter<Mem, Gpq> for Assembler<'_> {
    fn movabs(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Movabs as u32, &[op0.as_operand(), op1.as_operand()]); // X64
    }
}

impl MovntiEmitter<Mem, Gp> for Assembler<'_> {
    fn movnti(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Movnti as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovntiEmitter<Mem, Gpd> for Assembler<'_> {
    fn movnti(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Movnti as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovntiEmitter<Mem, Gpq> for Assembler<'_> {
    fn movnti(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Movnti as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovsEmitter<Mem, Mem> for Assembler<'_> {
    fn movs(&mut self, op0: Mem, op1: Mem) {
        self.emit_n(InstId::Movs as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT]
    }
}

impl MovsxEmitter<Gp, Gp> for Assembler<'_> {
    fn movsx(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Movsx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn movsx(&mut self, op0: Gpw, op1: GpbLo) {
        self.emit_n(InstId::Movsx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpw, GpbHi> for Assembler<'_> {
    fn movsx(&mut self, op0: Gpw, op1: GpbHi) {
        self.emit_n(InstId::Movsx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpd, GpbLo> for Assembler<'_> {
    fn movsx(&mut self, op0: Gpd, op1: GpbLo) {
        self.emit_n(InstId::Movsx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpd, GpbHi> for Assembler<'_> {
    fn movsx(&mut self, op0: Gpd, op1: GpbHi) {
        self.emit_n(InstId::Movsx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpd, Gpw> for Assembler<'_> {
    fn movsx(&mut self, op0: Gpd, op1: Gpw) {
        self.emit_n(InstId::Movsx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpq, GpbLo> for Assembler<'_> {
    fn movsx(&mut self, op0: Gpq, op1: GpbLo) {
        self.emit_n(InstId::Movsx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpq, GpbHi> for Assembler<'_> {
    fn movsx(&mut self, op0: Gpq, op1: GpbHi) {
        self.emit_n(InstId::Movsx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpq, Gpw> for Assembler<'_> {
    fn movsx(&mut self, op0: Gpq, op1: Gpw) {
        self.emit_n(InstId::Movsx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gp, Mem> for Assembler<'_> {
    fn movsx(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Movsx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpw, Mem> for Assembler<'_> {
    fn movsx(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Movsx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpd, Mem> for Assembler<'_> {
    fn movsx(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Movsx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovsxEmitter<Gpq, Mem> for Assembler<'_> {
    fn movsx(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Movsx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovsxdEmitter<Gp, Gp> for Assembler<'_> {
    fn movsxd(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Movsxd as u32, &[op0.as_operand(), op1.as_operand()]); // X64
    }
}

impl MovsxdEmitter<Gpw, Gpw> for Assembler<'_> {
    fn movsxd(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Movsxd as u32, &[op0.as_operand(), op1.as_operand()]); // X64
    }
}

impl MovsxdEmitter<Gpd, Gpd> for Assembler<'_> {
    fn movsxd(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Movsxd as u32, &[op0.as_operand(), op1.as_operand()]); // X64
    }
}

impl MovsxdEmitter<Gpq, Gpd> for Assembler<'_> {
    fn movsxd(&mut self, op0: Gpq, op1: Gpd) {
        self.emit_n(InstId::Movsxd as u32, &[op0.as_operand(), op1.as_operand()]); // X64
    }
}

impl MovsxdEmitter<Gp, Mem> for Assembler<'_> {
    fn movsxd(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Movsxd as u32, &[op0.as_operand(), op1.as_operand()]); // X64
    }
}

impl MovsxdEmitter<Gpw, Mem> for Assembler<'_> {
    fn movsxd(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Movsxd as u32, &[op0.as_operand(), op1.as_operand()]); // X64
    }
}

impl MovsxdEmitter<Gpd, Mem> for Assembler<'_> {
    fn movsxd(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Movsxd as u32, &[op0.as_operand(), op1.as_operand()]); // X64
    }
}

impl MovsxdEmitter<Gpq, Mem> for Assembler<'_> {
    fn movsxd(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Movsxd as u32, &[op0.as_operand(), op1.as_operand()]); // X64
    }
}

impl MovzxEmitter<Gp, Gp> for Assembler<'_> {
    fn movzx(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Movzx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn movzx(&mut self, op0: Gpw, op1: GpbLo) {
        self.emit_n(InstId::Movzx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpw, GpbHi> for Assembler<'_> {
    fn movzx(&mut self, op0: Gpw, op1: GpbHi) {
        self.emit_n(InstId::Movzx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpd, GpbLo> for Assembler<'_> {
    fn movzx(&mut self, op0: Gpd, op1: GpbLo) {
        self.emit_n(InstId::Movzx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpd, GpbHi> for Assembler<'_> {
    fn movzx(&mut self, op0: Gpd, op1: GpbHi) {
        self.emit_n(InstId::Movzx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpd, Gpw> for Assembler<'_> {
    fn movzx(&mut self, op0: Gpd, op1: Gpw) {
        self.emit_n(InstId::Movzx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpq, GpbLo> for Assembler<'_> {
    fn movzx(&mut self, op0: Gpq, op1: GpbLo) {
        self.emit_n(InstId::Movzx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpq, GpbHi> for Assembler<'_> {
    fn movzx(&mut self, op0: Gpq, op1: GpbHi) {
        self.emit_n(InstId::Movzx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpq, Gpw> for Assembler<'_> {
    fn movzx(&mut self, op0: Gpq, op1: Gpw) {
        self.emit_n(InstId::Movzx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gp, Mem> for Assembler<'_> {
    fn movzx(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Movzx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpw, Mem> for Assembler<'_> {
    fn movzx(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Movzx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpd, Mem> for Assembler<'_> {
    fn movzx(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Movzx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MovzxEmitter<Gpq, Mem> for Assembler<'_> {
    fn movzx(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Movzx as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl MulEmitter<Gp, Gp> for Assembler<'_> {
    fn mul(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Mul as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AX <- AL * r8
    }
}

impl MulEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn mul(&mut self, op0: Gpw, op1: GpbLo) {
        self.emit_n(InstId::Mul as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AX <- AL * r8
    }
}

impl MulEmitter<Gpw, GpbHi> for Assembler<'_> {
    fn mul(&mut self, op0: Gpw, op1: GpbHi) {
        self.emit_n(InstId::Mul as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AX <- AL * r8
    }
}

impl MulEmitter<Gp, Mem> for Assembler<'_> {
    fn mul(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Mul as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AX <- AL * m8
    }
}

impl MulEmitter<Gpw, Mem> for Assembler<'_> {
    fn mul(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Mul as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT] AX <- AL * m8
    }
}

impl Mul3Emitter<Gp, Gp, Gp> for Assembler<'_> {
    fn mul_3(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Mul as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * r16|r32|r64
    }
}

impl Mul3Emitter<Gpw, Gpw, Gpw> for Assembler<'_> {
    fn mul_3(&mut self, op0: Gpw, op1: Gpw, op2: Gpw) {
        self.emit_n(
            InstId::Mul as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * r16|r32|r64
    }
}

impl Mul3Emitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn mul_3(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Mul as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * r16|r32|r64
    }
}

impl Mul3Emitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn mul_3(&mut self, op0: Gpq, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Mul as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * r16|r32|r64
    }
}

impl Mul3Emitter<Gp, Gp, Mem> for Assembler<'_> {
    fn mul_3(&mut self, op0: Gp, op1: Gp, op2: Mem) {
        self.emit_n(
            InstId::Mul as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * m16|m32|m64
    }
}

impl Mul3Emitter<Gpw, Gpw, Mem> for Assembler<'_> {
    fn mul_3(&mut self, op0: Gpw, op1: Gpw, op2: Mem) {
        self.emit_n(
            InstId::Mul as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * m16|m32|m64
    }
}

impl Mul3Emitter<Gpd, Gpd, Mem> for Assembler<'_> {
    fn mul_3(&mut self, op0: Gpd, op1: Gpd, op2: Mem) {
        self.emit_n(
            InstId::Mul as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * m16|m32|m64
    }
}

impl Mul3Emitter<Gpq, Gpq, Mem> for Assembler<'_> {
    fn mul_3(&mut self, op0: Gpq, op1: Gpq, op2: Mem) {
        self.emit_n(
            InstId::Mul as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] xDX:xAX <- xAX * m16|m32|m64
    }
}

impl NegEmitter<Gp> for Assembler<'_> {
    fn neg(&mut self, op0: Gp) {
        self.emit_n(InstId::Neg as u32, &[op0.as_operand()]); // ANY
    }
}

impl NegEmitter<GpbLo> for Assembler<'_> {
    fn neg(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Neg as u32, &[op0.as_operand()]); // ANY
    }
}

impl NegEmitter<GpbHi> for Assembler<'_> {
    fn neg(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Neg as u32, &[op0.as_operand()]); // ANY
    }
}

impl NegEmitter<Gpw> for Assembler<'_> {
    fn neg(&mut self, op0: Gpw) {
        self.emit_n(InstId::Neg as u32, &[op0.as_operand()]); // ANY
    }
}

impl NegEmitter<Gpd> for Assembler<'_> {
    fn neg(&mut self, op0: Gpd) {
        self.emit_n(InstId::Neg as u32, &[op0.as_operand()]); // ANY
    }
}

impl NegEmitter<Gpq> for Assembler<'_> {
    fn neg(&mut self, op0: Gpq) {
        self.emit_n(InstId::Neg as u32, &[op0.as_operand()]); // ANY
    }
}

impl NegEmitter<Mem> for Assembler<'_> {
    fn neg(&mut self, op0: Mem) {
        self.emit_n(InstId::Neg as u32, &[op0.as_operand()]); // ANY
    }
}

impl NopEmitter for Assembler<'_> {
    fn nop(&mut self) {
        self.emit_n(InstId::Nop as u32, &[]); // ANY
    }
}

impl Nop1Emitter<Gp> for Assembler<'_> {
    fn nop_1(&mut self, op0: Gp) {
        self.emit_n(InstId::Nop as u32, &[op0.as_operand()]); // ANY
    }
}

impl Nop1Emitter<Gpw> for Assembler<'_> {
    fn nop_1(&mut self, op0: Gpw) {
        self.emit_n(InstId::Nop as u32, &[op0.as_operand()]); // ANY
    }
}

impl Nop1Emitter<Gpd> for Assembler<'_> {
    fn nop_1(&mut self, op0: Gpd) {
        self.emit_n(InstId::Nop as u32, &[op0.as_operand()]); // ANY
    }
}

impl Nop1Emitter<Gpq> for Assembler<'_> {
    fn nop_1(&mut self, op0: Gpq) {
        self.emit_n(InstId::Nop as u32, &[op0.as_operand()]); // ANY
    }
}

impl Nop1Emitter<Mem> for Assembler<'_> {
    fn nop_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Nop as u32, &[op0.as_operand()]); // ANY
    }
}

impl Nop2Emitter<Gp, Gp> for Assembler<'_> {
    fn nop_2(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Nop as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Nop2Emitter<Gpw, Gpw> for Assembler<'_> {
    fn nop_2(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Nop as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Nop2Emitter<Gpd, Gpd> for Assembler<'_> {
    fn nop_2(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Nop as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Nop2Emitter<Gpq, Gpq> for Assembler<'_> {
    fn nop_2(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Nop as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Nop2Emitter<Mem, Gp> for Assembler<'_> {
    fn nop_2(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Nop as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Nop2Emitter<Mem, Gpw> for Assembler<'_> {
    fn nop_2(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::Nop as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Nop2Emitter<Mem, Gpd> for Assembler<'_> {
    fn nop_2(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Nop as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Nop2Emitter<Mem, Gpq> for Assembler<'_> {
    fn nop_2(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Nop as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Not_Emitter<Gp> for Assembler<'_> {
    fn not_(&mut self, op0: Gp) {
        self.emit_n(InstId::Not as u32, &[op0.as_operand()]); // ANY
    }
}

impl Not_Emitter<GpbLo> for Assembler<'_> {
    fn not_(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Not as u32, &[op0.as_operand()]); // ANY
    }
}

impl Not_Emitter<GpbHi> for Assembler<'_> {
    fn not_(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Not as u32, &[op0.as_operand()]); // ANY
    }
}

impl Not_Emitter<Gpw> for Assembler<'_> {
    fn not_(&mut self, op0: Gpw) {
        self.emit_n(InstId::Not as u32, &[op0.as_operand()]); // ANY
    }
}

impl Not_Emitter<Gpd> for Assembler<'_> {
    fn not_(&mut self, op0: Gpd) {
        self.emit_n(InstId::Not as u32, &[op0.as_operand()]); // ANY
    }
}

impl Not_Emitter<Gpq> for Assembler<'_> {
    fn not_(&mut self, op0: Gpq) {
        self.emit_n(InstId::Not as u32, &[op0.as_operand()]); // ANY
    }
}

impl Not_Emitter<Mem> for Assembler<'_> {
    fn not_(&mut self, op0: Mem) {
        self.emit_n(InstId::Not as u32, &[op0.as_operand()]); // ANY
    }
}

impl Or_Emitter<Gp, Gp> for Assembler<'_> {
    fn or_(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Or as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Or_Emitter<GpbLo, GpbLo> for Assembler<'_> {
    fn or_(&mut self, op0: GpbLo, op1: GpbLo) {
        self.emit_n(InstId::Or as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Or_Emitter<GpbLo, GpbHi> for Assembler<'_> {
    fn or_(&mut self, op0: GpbLo, op1: GpbHi) {
        self.emit_n(InstId::Or as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Or_Emitter<GpbHi, GpbLo> for Assembler<'_> {
    fn or_(&mut self, op0: GpbHi, op1: GpbLo) {
        self.emit_n(InstId::Or as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Or_Emitter<GpbHi, GpbHi> for Assembler<'_> {
    fn or_(&mut self, op0: GpbHi, op1: GpbHi) {
        self.emit_n(InstId::Or as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Or_Emitter<Gpw, Gpw> for Assembler<'_> {
    fn or_(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Or as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Or_Emitter<Gpd, Gpd> for Assembler<'_> {
    fn or_(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Or as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Or_Emitter<Gpq, Gpq> for Assembler<'_> {
    fn or_(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Or as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Or_Emitter<Gp, Mem> for Assembler<'_> {
    fn or_(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Or as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Or_Emitter<GpbLo, Mem> for Assembler<'_> {
    fn or_(&mut self, op0: GpbLo, op1: Mem) {
        self.emit_n(InstId::Or as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Or_Emitter<GpbHi, Mem> for Assembler<'_> {
    fn or_(&mut self, op0: GpbHi, op1: Mem) {
        self.emit_n(InstId::Or as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Or_Emitter<Gpw, Mem> for Assembler<'_> {
    fn or_(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Or as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Or_Emitter<Gpd, Mem> for Assembler<'_> {
    fn or_(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Or as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Or_Emitter<Gpq, Mem> for Assembler<'_> {
    fn or_(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Or as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Or_Emitter<Gp, Imm> for Assembler<'_> {
    fn or_(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Or as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> Or_Emitter<GpbLo, U1> for Assembler<'_> {
    fn or_(&mut self, op0: GpbLo, op1: U1) {
        self.emit_n(
            InstId::Or as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> Or_Emitter<GpbHi, U1> for Assembler<'_> {
    fn or_(&mut self, op0: GpbHi, op1: U1) {
        self.emit_n(
            InstId::Or as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> Or_Emitter<Gpw, U1> for Assembler<'_> {
    fn or_(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Or as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> Or_Emitter<Gpd, U1> for Assembler<'_> {
    fn or_(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Or as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> Or_Emitter<Gpq, U1> for Assembler<'_> {
    fn or_(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Or as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl Or_Emitter<Mem, Gp> for Assembler<'_> {
    fn or_(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Or as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Or_Emitter<Mem, GpbLo> for Assembler<'_> {
    fn or_(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::Or as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Or_Emitter<Mem, GpbHi> for Assembler<'_> {
    fn or_(&mut self, op0: Mem, op1: GpbHi) {
        self.emit_n(InstId::Or as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Or_Emitter<Mem, Gpw> for Assembler<'_> {
    fn or_(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::Or as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Or_Emitter<Mem, Gpd> for Assembler<'_> {
    fn or_(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Or as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Or_Emitter<Mem, Gpq> for Assembler<'_> {
    fn or_(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Or as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> Or_Emitter<Mem, U1> for Assembler<'_> {
    fn or_(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::Or as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl PopEmitter<Gp> for Assembler<'_> {
    fn pop(&mut self, op0: Gp) {
        self.emit_n(InstId::Pop as u32, &[op0.as_operand()]); // ANY
    }
}

impl PopEmitter<Gpw> for Assembler<'_> {
    fn pop(&mut self, op0: Gpw) {
        self.emit_n(InstId::Pop as u32, &[op0.as_operand()]); // ANY
    }
}

impl PopEmitter<Gpd> for Assembler<'_> {
    fn pop(&mut self, op0: Gpd) {
        self.emit_n(InstId::Pop as u32, &[op0.as_operand()]); // ANY
    }
}

impl PopEmitter<Gpq> for Assembler<'_> {
    fn pop(&mut self, op0: Gpq) {
        self.emit_n(InstId::Pop as u32, &[op0.as_operand()]); // ANY
    }
}

impl PopEmitter<Mem> for Assembler<'_> {
    fn pop(&mut self, op0: Mem) {
        self.emit_n(InstId::Pop as u32, &[op0.as_operand()]); // ANY
    }
}

impl PopEmitter<SReg> for Assembler<'_> {
    fn pop(&mut self, op0: SReg) {
        self.emit_n(InstId::Pop as u32, &[op0.as_operand()]); // ANY
    }
}

impl PopaEmitter for Assembler<'_> {
    fn popa(&mut self) {
        self.emit_n(InstId::Popa as u32, &[]); // X86
    }
}

impl PopadEmitter for Assembler<'_> {
    fn popad(&mut self) {
        self.emit_n(InstId::Popad as u32, &[]); // X86
    }
}

impl PopfEmitter for Assembler<'_> {
    fn popf(&mut self) {
        self.emit_n(InstId::Popf as u32, &[]); // ANY
    }
}

impl PopfdEmitter for Assembler<'_> {
    fn popfd(&mut self) {
        self.emit_n(InstId::Popfd as u32, &[]); // X86
    }
}

impl PopfqEmitter for Assembler<'_> {
    fn popfq(&mut self) {
        self.emit_n(InstId::Popfq as u32, &[]); // X64
    }
}

impl PushEmitter<Gp> for Assembler<'_> {
    fn push(&mut self, op0: Gp) {
        self.emit_n(InstId::Push as u32, &[op0.as_operand()]); // ANY
    }
}

impl PushEmitter<Gpw> for Assembler<'_> {
    fn push(&mut self, op0: Gpw) {
        self.emit_n(InstId::Push as u32, &[op0.as_operand()]); // ANY
    }
}

impl PushEmitter<Gpd> for Assembler<'_> {
    fn push(&mut self, op0: Gpd) {
        self.emit_n(InstId::Push as u32, &[op0.as_operand()]); // ANY
    }
}

impl PushEmitter<Gpq> for Assembler<'_> {
    fn push(&mut self, op0: Gpq) {
        self.emit_n(InstId::Push as u32, &[op0.as_operand()]); // ANY
    }
}

impl PushEmitter<Mem> for Assembler<'_> {
    fn push(&mut self, op0: Mem) {
        self.emit_n(InstId::Push as u32, &[op0.as_operand()]); // ANY
    }
}

impl PushEmitter<SReg> for Assembler<'_> {
    fn push(&mut self, op0: SReg) {
        self.emit_n(InstId::Push as u32, &[op0.as_operand()]); // ANY
    }
}

impl<U0: Into<Imm>> PushEmitter<U0> for Assembler<'_> {
    fn push(&mut self, op0: U0) {
        self.emit_n(InstId::Push as u32, &[Into::<Imm>::into(op0).as_operand()]); // ANY
    }
}

impl PushaEmitter for Assembler<'_> {
    fn pusha(&mut self) {
        self.emit_n(InstId::Pusha as u32, &[]); // X86
    }
}

impl PushadEmitter for Assembler<'_> {
    fn pushad(&mut self) {
        self.emit_n(InstId::Pushad as u32, &[]); // X86
    }
}

impl PushfEmitter for Assembler<'_> {
    fn pushf(&mut self) {
        self.emit_n(InstId::Pushf as u32, &[]); // ANY
    }
}

impl PushfdEmitter for Assembler<'_> {
    fn pushfd(&mut self) {
        self.emit_n(InstId::Pushfd as u32, &[]); // X86
    }
}

impl PushfqEmitter for Assembler<'_> {
    fn pushfq(&mut self) {
        self.emit_n(InstId::Pushfq as u32, &[]); // X64
    }
}

impl<U0: Into<Imm>> PushwEmitter<U0> for Assembler<'_> {
    fn pushw(&mut self, op0: U0) {
        self.emit_n(InstId::Pushw as u32, &[Into::<Imm>::into(op0).as_operand()]); // ANY
    }
}

impl RclEmitter<Gp, Gp> for Assembler<'_> {
    fn rcl(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Rcl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RclEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn rcl(&mut self, op0: GpbLo, op1: GpbLo) {
        self.emit_n(InstId::Rcl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RclEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn rcl(&mut self, op0: GpbHi, op1: GpbLo) {
        self.emit_n(InstId::Rcl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RclEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn rcl(&mut self, op0: Gpw, op1: GpbLo) {
        self.emit_n(InstId::Rcl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RclEmitter<Gpd, GpbLo> for Assembler<'_> {
    fn rcl(&mut self, op0: Gpd, op1: GpbLo) {
        self.emit_n(InstId::Rcl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RclEmitter<Gpq, GpbLo> for Assembler<'_> {
    fn rcl(&mut self, op0: Gpq, op1: GpbLo) {
        self.emit_n(InstId::Rcl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RclEmitter<Mem, Gp> for Assembler<'_> {
    fn rcl(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Rcl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RclEmitter<Mem, GpbLo> for Assembler<'_> {
    fn rcl(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::Rcl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RclEmitter<Gp, Imm> for Assembler<'_> {
    fn rcl(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Rcl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> RclEmitter<GpbLo, U1> for Assembler<'_> {
    fn rcl(&mut self, op0: GpbLo, op1: U1) {
        self.emit_n(
            InstId::Rcl as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RclEmitter<GpbHi, U1> for Assembler<'_> {
    fn rcl(&mut self, op0: GpbHi, op1: U1) {
        self.emit_n(
            InstId::Rcl as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RclEmitter<Gpw, U1> for Assembler<'_> {
    fn rcl(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Rcl as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RclEmitter<Gpd, U1> for Assembler<'_> {
    fn rcl(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Rcl as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RclEmitter<Gpq, U1> for Assembler<'_> {
    fn rcl(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Rcl as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RclEmitter<Mem, U1> for Assembler<'_> {
    fn rcl(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::Rcl as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl RcrEmitter<Gp, Gp> for Assembler<'_> {
    fn rcr(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Rcr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RcrEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn rcr(&mut self, op0: GpbLo, op1: GpbLo) {
        self.emit_n(InstId::Rcr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RcrEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn rcr(&mut self, op0: GpbHi, op1: GpbLo) {
        self.emit_n(InstId::Rcr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RcrEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn rcr(&mut self, op0: Gpw, op1: GpbLo) {
        self.emit_n(InstId::Rcr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RcrEmitter<Gpd, GpbLo> for Assembler<'_> {
    fn rcr(&mut self, op0: Gpd, op1: GpbLo) {
        self.emit_n(InstId::Rcr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RcrEmitter<Gpq, GpbLo> for Assembler<'_> {
    fn rcr(&mut self, op0: Gpq, op1: GpbLo) {
        self.emit_n(InstId::Rcr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RcrEmitter<Mem, Gp> for Assembler<'_> {
    fn rcr(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Rcr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RcrEmitter<Mem, GpbLo> for Assembler<'_> {
    fn rcr(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::Rcr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RcrEmitter<Gp, Imm> for Assembler<'_> {
    fn rcr(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Rcr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> RcrEmitter<GpbLo, U1> for Assembler<'_> {
    fn rcr(&mut self, op0: GpbLo, op1: U1) {
        self.emit_n(
            InstId::Rcr as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RcrEmitter<GpbHi, U1> for Assembler<'_> {
    fn rcr(&mut self, op0: GpbHi, op1: U1) {
        self.emit_n(
            InstId::Rcr as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RcrEmitter<Gpw, U1> for Assembler<'_> {
    fn rcr(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Rcr as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RcrEmitter<Gpd, U1> for Assembler<'_> {
    fn rcr(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Rcr as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RcrEmitter<Gpq, U1> for Assembler<'_> {
    fn rcr(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Rcr as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RcrEmitter<Mem, U1> for Assembler<'_> {
    fn rcr(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::Rcr as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl RolEmitter<Gp, Gp> for Assembler<'_> {
    fn rol(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Rol as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RolEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn rol(&mut self, op0: GpbLo, op1: GpbLo) {
        self.emit_n(InstId::Rol as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RolEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn rol(&mut self, op0: GpbHi, op1: GpbLo) {
        self.emit_n(InstId::Rol as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RolEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn rol(&mut self, op0: Gpw, op1: GpbLo) {
        self.emit_n(InstId::Rol as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RolEmitter<Gpd, GpbLo> for Assembler<'_> {
    fn rol(&mut self, op0: Gpd, op1: GpbLo) {
        self.emit_n(InstId::Rol as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RolEmitter<Gpq, GpbLo> for Assembler<'_> {
    fn rol(&mut self, op0: Gpq, op1: GpbLo) {
        self.emit_n(InstId::Rol as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RolEmitter<Mem, Gp> for Assembler<'_> {
    fn rol(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Rol as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RolEmitter<Mem, GpbLo> for Assembler<'_> {
    fn rol(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::Rol as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RolEmitter<Gp, Imm> for Assembler<'_> {
    fn rol(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Rol as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> RolEmitter<GpbLo, U1> for Assembler<'_> {
    fn rol(&mut self, op0: GpbLo, op1: U1) {
        self.emit_n(
            InstId::Rol as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RolEmitter<GpbHi, U1> for Assembler<'_> {
    fn rol(&mut self, op0: GpbHi, op1: U1) {
        self.emit_n(
            InstId::Rol as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RolEmitter<Gpw, U1> for Assembler<'_> {
    fn rol(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Rol as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RolEmitter<Gpd, U1> for Assembler<'_> {
    fn rol(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Rol as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RolEmitter<Gpq, U1> for Assembler<'_> {
    fn rol(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Rol as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RolEmitter<Mem, U1> for Assembler<'_> {
    fn rol(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::Rol as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl RorEmitter<Gp, Gp> for Assembler<'_> {
    fn ror(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Ror as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RorEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn ror(&mut self, op0: GpbLo, op1: GpbLo) {
        self.emit_n(InstId::Ror as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RorEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn ror(&mut self, op0: GpbHi, op1: GpbLo) {
        self.emit_n(InstId::Ror as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RorEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn ror(&mut self, op0: Gpw, op1: GpbLo) {
        self.emit_n(InstId::Ror as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RorEmitter<Gpd, GpbLo> for Assembler<'_> {
    fn ror(&mut self, op0: Gpd, op1: GpbLo) {
        self.emit_n(InstId::Ror as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RorEmitter<Gpq, GpbLo> for Assembler<'_> {
    fn ror(&mut self, op0: Gpq, op1: GpbLo) {
        self.emit_n(InstId::Ror as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RorEmitter<Mem, Gp> for Assembler<'_> {
    fn ror(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Ror as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RorEmitter<Mem, GpbLo> for Assembler<'_> {
    fn ror(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::Ror as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl RorEmitter<Gp, Imm> for Assembler<'_> {
    fn ror(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Ror as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> RorEmitter<GpbLo, U1> for Assembler<'_> {
    fn ror(&mut self, op0: GpbLo, op1: U1) {
        self.emit_n(
            InstId::Ror as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RorEmitter<GpbHi, U1> for Assembler<'_> {
    fn ror(&mut self, op0: GpbHi, op1: U1) {
        self.emit_n(
            InstId::Ror as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RorEmitter<Gpw, U1> for Assembler<'_> {
    fn ror(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Ror as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RorEmitter<Gpd, U1> for Assembler<'_> {
    fn ror(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Ror as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RorEmitter<Gpq, U1> for Assembler<'_> {
    fn ror(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Ror as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> RorEmitter<Mem, U1> for Assembler<'_> {
    fn ror(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::Ror as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl SbbEmitter<Gp, Gp> for Assembler<'_> {
    fn sbb(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Sbb as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SbbEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn sbb(&mut self, op0: GpbLo, op1: GpbLo) {
        self.emit_n(InstId::Sbb as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SbbEmitter<GpbLo, GpbHi> for Assembler<'_> {
    fn sbb(&mut self, op0: GpbLo, op1: GpbHi) {
        self.emit_n(InstId::Sbb as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SbbEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn sbb(&mut self, op0: GpbHi, op1: GpbLo) {
        self.emit_n(InstId::Sbb as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SbbEmitter<GpbHi, GpbHi> for Assembler<'_> {
    fn sbb(&mut self, op0: GpbHi, op1: GpbHi) {
        self.emit_n(InstId::Sbb as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SbbEmitter<Gpw, Gpw> for Assembler<'_> {
    fn sbb(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Sbb as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SbbEmitter<Gpd, Gpd> for Assembler<'_> {
    fn sbb(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Sbb as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SbbEmitter<Gpq, Gpq> for Assembler<'_> {
    fn sbb(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Sbb as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SbbEmitter<Gp, Mem> for Assembler<'_> {
    fn sbb(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Sbb as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SbbEmitter<GpbLo, Mem> for Assembler<'_> {
    fn sbb(&mut self, op0: GpbLo, op1: Mem) {
        self.emit_n(InstId::Sbb as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SbbEmitter<GpbHi, Mem> for Assembler<'_> {
    fn sbb(&mut self, op0: GpbHi, op1: Mem) {
        self.emit_n(InstId::Sbb as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SbbEmitter<Gpw, Mem> for Assembler<'_> {
    fn sbb(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Sbb as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SbbEmitter<Gpd, Mem> for Assembler<'_> {
    fn sbb(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Sbb as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SbbEmitter<Gpq, Mem> for Assembler<'_> {
    fn sbb(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Sbb as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SbbEmitter<Gp, Imm> for Assembler<'_> {
    fn sbb(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Sbb as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> SbbEmitter<GpbLo, U1> for Assembler<'_> {
    fn sbb(&mut self, op0: GpbLo, op1: U1) {
        self.emit_n(
            InstId::Sbb as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SbbEmitter<GpbHi, U1> for Assembler<'_> {
    fn sbb(&mut self, op0: GpbHi, op1: U1) {
        self.emit_n(
            InstId::Sbb as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SbbEmitter<Gpw, U1> for Assembler<'_> {
    fn sbb(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Sbb as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SbbEmitter<Gpd, U1> for Assembler<'_> {
    fn sbb(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Sbb as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SbbEmitter<Gpq, U1> for Assembler<'_> {
    fn sbb(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Sbb as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl SbbEmitter<Mem, Gp> for Assembler<'_> {
    fn sbb(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Sbb as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SbbEmitter<Mem, GpbLo> for Assembler<'_> {
    fn sbb(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::Sbb as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SbbEmitter<Mem, GpbHi> for Assembler<'_> {
    fn sbb(&mut self, op0: Mem, op1: GpbHi) {
        self.emit_n(InstId::Sbb as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SbbEmitter<Mem, Gpw> for Assembler<'_> {
    fn sbb(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::Sbb as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SbbEmitter<Mem, Gpd> for Assembler<'_> {
    fn sbb(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Sbb as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SbbEmitter<Mem, Gpq> for Assembler<'_> {
    fn sbb(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Sbb as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> SbbEmitter<Mem, U1> for Assembler<'_> {
    fn sbb(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::Sbb as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl SalEmitter<Gp, Gp> for Assembler<'_> {
    fn sal(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Shl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SalEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn sal(&mut self, op0: GpbLo, op1: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SalEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn sal(&mut self, op0: GpbHi, op1: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SalEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn sal(&mut self, op0: Gpw, op1: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SalEmitter<Gpd, GpbLo> for Assembler<'_> {
    fn sal(&mut self, op0: Gpd, op1: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SalEmitter<Gpq, GpbLo> for Assembler<'_> {
    fn sal(&mut self, op0: Gpq, op1: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SalEmitter<Mem, Gp> for Assembler<'_> {
    fn sal(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Shl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SalEmitter<Mem, GpbLo> for Assembler<'_> {
    fn sal(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SalEmitter<Gp, Imm> for Assembler<'_> {
    fn sal(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Shl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> SalEmitter<GpbLo, U1> for Assembler<'_> {
    fn sal(&mut self, op0: GpbLo, op1: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SalEmitter<GpbHi, U1> for Assembler<'_> {
    fn sal(&mut self, op0: GpbHi, op1: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SalEmitter<Gpw, U1> for Assembler<'_> {
    fn sal(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SalEmitter<Gpd, U1> for Assembler<'_> {
    fn sal(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SalEmitter<Gpq, U1> for Assembler<'_> {
    fn sal(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SalEmitter<Mem, U1> for Assembler<'_> {
    fn sal(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl SarEmitter<Gp, Gp> for Assembler<'_> {
    fn sar(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Sar as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SarEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn sar(&mut self, op0: GpbLo, op1: GpbLo) {
        self.emit_n(InstId::Sar as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SarEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn sar(&mut self, op0: GpbHi, op1: GpbLo) {
        self.emit_n(InstId::Sar as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SarEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn sar(&mut self, op0: Gpw, op1: GpbLo) {
        self.emit_n(InstId::Sar as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SarEmitter<Gpd, GpbLo> for Assembler<'_> {
    fn sar(&mut self, op0: Gpd, op1: GpbLo) {
        self.emit_n(InstId::Sar as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SarEmitter<Gpq, GpbLo> for Assembler<'_> {
    fn sar(&mut self, op0: Gpq, op1: GpbLo) {
        self.emit_n(InstId::Sar as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SarEmitter<Mem, Gp> for Assembler<'_> {
    fn sar(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Sar as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SarEmitter<Mem, GpbLo> for Assembler<'_> {
    fn sar(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::Sar as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SarEmitter<Gp, Imm> for Assembler<'_> {
    fn sar(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Sar as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> SarEmitter<GpbLo, U1> for Assembler<'_> {
    fn sar(&mut self, op0: GpbLo, op1: U1) {
        self.emit_n(
            InstId::Sar as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SarEmitter<GpbHi, U1> for Assembler<'_> {
    fn sar(&mut self, op0: GpbHi, op1: U1) {
        self.emit_n(
            InstId::Sar as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SarEmitter<Gpw, U1> for Assembler<'_> {
    fn sar(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Sar as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SarEmitter<Gpd, U1> for Assembler<'_> {
    fn sar(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Sar as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SarEmitter<Gpq, U1> for Assembler<'_> {
    fn sar(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Sar as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SarEmitter<Mem, U1> for Assembler<'_> {
    fn sar(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::Sar as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl ScasEmitter<Gp, Mem> for Assembler<'_> {
    fn scas(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Scas as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT]
    }
}

impl ScasEmitter<GpbLo, Mem> for Assembler<'_> {
    fn scas(&mut self, op0: GpbLo, op1: Mem) {
        self.emit_n(InstId::Scas as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT]
    }
}

impl ScasEmitter<Gpw, Mem> for Assembler<'_> {
    fn scas(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Scas as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT]
    }
}

impl ScasEmitter<Gpd, Mem> for Assembler<'_> {
    fn scas(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Scas as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT]
    }
}

impl ScasEmitter<Gpq, Mem> for Assembler<'_> {
    fn scas(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Scas as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT]
    }
}

impl SetEmitter<Gp> for Assembler<'_> {
    fn set(&mut self, cc: CondCode, op0: Gp) {
        self.emit_n(SETCC_FROM_COND[cc as usize], &[op0.as_operand()]); // ANY
    }
    fn seto(&mut self, op0: Gp) {
        self.emit_n(InstId::Seto as u32, &[op0.as_operand()]);
    }
    fn setno(&mut self, op0: Gp) {
        self.emit_n(InstId::Setno as u32, &[op0.as_operand()]);
    }
    fn setb(&mut self, op0: Gp) {
        self.emit_n(InstId::Setb as u32, &[op0.as_operand()]);
    }
    fn setnb(&mut self, op0: Gp) {
        self.emit_n(InstId::Setnb as u32, &[op0.as_operand()]);
    }
    fn setz(&mut self, op0: Gp) {
        self.emit_n(InstId::Setz as u32, &[op0.as_operand()]);
    }
    fn setnz(&mut self, op0: Gp) {
        self.emit_n(InstId::Setnz as u32, &[op0.as_operand()]);
    }
    fn setbe(&mut self, op0: Gp) {
        self.emit_n(InstId::Setbe as u32, &[op0.as_operand()]);
    }
    fn setnbe(&mut self, op0: Gp) {
        self.emit_n(InstId::Setnbe as u32, &[op0.as_operand()]);
    }
    fn sets(&mut self, op0: Gp) {
        self.emit_n(InstId::Sets as u32, &[op0.as_operand()]);
    }
    fn setns(&mut self, op0: Gp) {
        self.emit_n(InstId::Setns as u32, &[op0.as_operand()]);
    }
    fn setp(&mut self, op0: Gp) {
        self.emit_n(InstId::Setp as u32, &[op0.as_operand()]);
    }
    fn setnp(&mut self, op0: Gp) {
        self.emit_n(InstId::Setnp as u32, &[op0.as_operand()]);
    }
    fn setl(&mut self, op0: Gp) {
        self.emit_n(InstId::Setl as u32, &[op0.as_operand()]);
    }
    fn setnl(&mut self, op0: Gp) {
        self.emit_n(InstId::Setnl as u32, &[op0.as_operand()]);
    }
    fn setle(&mut self, op0: Gp) {
        self.emit_n(InstId::Setle as u32, &[op0.as_operand()]);
    }
    fn setnle(&mut self, op0: Gp) {
        self.emit_n(InstId::Setnle as u32, &[op0.as_operand()]);
    }
}

impl SetEmitter<GpbLo> for Assembler<'_> {
    fn set(&mut self, cc: CondCode, op0: GpbLo) {
        self.emit_n(SETCC_FROM_COND[cc as usize], &[op0.as_operand()]); // ANY
    }
    fn seto(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Seto as u32, &[op0.as_operand()]);
    }
    fn setno(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Setno as u32, &[op0.as_operand()]);
    }
    fn setb(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Setb as u32, &[op0.as_operand()]);
    }
    fn setnb(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Setnb as u32, &[op0.as_operand()]);
    }
    fn setz(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Setz as u32, &[op0.as_operand()]);
    }
    fn setnz(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Setnz as u32, &[op0.as_operand()]);
    }
    fn setbe(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Setbe as u32, &[op0.as_operand()]);
    }
    fn setnbe(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Setnbe as u32, &[op0.as_operand()]);
    }
    fn sets(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Sets as u32, &[op0.as_operand()]);
    }
    fn setns(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Setns as u32, &[op0.as_operand()]);
    }
    fn setp(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Setp as u32, &[op0.as_operand()]);
    }
    fn setnp(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Setnp as u32, &[op0.as_operand()]);
    }
    fn setl(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Setl as u32, &[op0.as_operand()]);
    }
    fn setnl(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Setnl as u32, &[op0.as_operand()]);
    }
    fn setle(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Setle as u32, &[op0.as_operand()]);
    }
    fn setnle(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Setnle as u32, &[op0.as_operand()]);
    }
}

impl SetEmitter<GpbHi> for Assembler<'_> {
    fn set(&mut self, cc: CondCode, op0: GpbHi) {
        self.emit_n(SETCC_FROM_COND[cc as usize], &[op0.as_operand()]); // ANY
    }
    fn seto(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Seto as u32, &[op0.as_operand()]);
    }
    fn setno(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Setno as u32, &[op0.as_operand()]);
    }
    fn setb(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Setb as u32, &[op0.as_operand()]);
    }
    fn setnb(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Setnb as u32, &[op0.as_operand()]);
    }
    fn setz(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Setz as u32, &[op0.as_operand()]);
    }
    fn setnz(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Setnz as u32, &[op0.as_operand()]);
    }
    fn setbe(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Setbe as u32, &[op0.as_operand()]);
    }
    fn setnbe(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Setnbe as u32, &[op0.as_operand()]);
    }
    fn sets(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Sets as u32, &[op0.as_operand()]);
    }
    fn setns(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Setns as u32, &[op0.as_operand()]);
    }
    fn setp(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Setp as u32, &[op0.as_operand()]);
    }
    fn setnp(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Setnp as u32, &[op0.as_operand()]);
    }
    fn setl(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Setl as u32, &[op0.as_operand()]);
    }
    fn setnl(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Setnl as u32, &[op0.as_operand()]);
    }
    fn setle(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Setle as u32, &[op0.as_operand()]);
    }
    fn setnle(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Setnle as u32, &[op0.as_operand()]);
    }
}

impl SetEmitter<Mem> for Assembler<'_> {
    fn set(&mut self, cc: CondCode, op0: Mem) {
        self.emit_n(SETCC_FROM_COND[cc as usize], &[op0.as_operand()]); // ANY
    }
    fn seto(&mut self, op0: Mem) {
        self.emit_n(InstId::Seto as u32, &[op0.as_operand()]);
    }
    fn setno(&mut self, op0: Mem) {
        self.emit_n(InstId::Setno as u32, &[op0.as_operand()]);
    }
    fn setb(&mut self, op0: Mem) {
        self.emit_n(InstId::Setb as u32, &[op0.as_operand()]);
    }
    fn setnb(&mut self, op0: Mem) {
        self.emit_n(InstId::Setnb as u32, &[op0.as_operand()]);
    }
    fn setz(&mut self, op0: Mem) {
        self.emit_n(InstId::Setz as u32, &[op0.as_operand()]);
    }
    fn setnz(&mut self, op0: Mem) {
        self.emit_n(InstId::Setnz as u32, &[op0.as_operand()]);
    }
    fn setbe(&mut self, op0: Mem) {
        self.emit_n(InstId::Setbe as u32, &[op0.as_operand()]);
    }
    fn setnbe(&mut self, op0: Mem) {
        self.emit_n(InstId::Setnbe as u32, &[op0.as_operand()]);
    }
    fn sets(&mut self, op0: Mem) {
        self.emit_n(InstId::Sets as u32, &[op0.as_operand()]);
    }
    fn setns(&mut self, op0: Mem) {
        self.emit_n(InstId::Setns as u32, &[op0.as_operand()]);
    }
    fn setp(&mut self, op0: Mem) {
        self.emit_n(InstId::Setp as u32, &[op0.as_operand()]);
    }
    fn setnp(&mut self, op0: Mem) {
        self.emit_n(InstId::Setnp as u32, &[op0.as_operand()]);
    }
    fn setl(&mut self, op0: Mem) {
        self.emit_n(InstId::Setl as u32, &[op0.as_operand()]);
    }
    fn setnl(&mut self, op0: Mem) {
        self.emit_n(InstId::Setnl as u32, &[op0.as_operand()]);
    }
    fn setle(&mut self, op0: Mem) {
        self.emit_n(InstId::Setle as u32, &[op0.as_operand()]);
    }
    fn setnle(&mut self, op0: Mem) {
        self.emit_n(InstId::Setnle as u32, &[op0.as_operand()]);
    }
}

impl ShlEmitter<Gp, Gp> for Assembler<'_> {
    fn shl(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Shl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl ShlEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn shl(&mut self, op0: GpbLo, op1: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl ShlEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn shl(&mut self, op0: GpbHi, op1: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl ShlEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn shl(&mut self, op0: Gpw, op1: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl ShlEmitter<Gpd, GpbLo> for Assembler<'_> {
    fn shl(&mut self, op0: Gpd, op1: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl ShlEmitter<Gpq, GpbLo> for Assembler<'_> {
    fn shl(&mut self, op0: Gpq, op1: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl ShlEmitter<Mem, Gp> for Assembler<'_> {
    fn shl(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Shl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl ShlEmitter<Mem, GpbLo> for Assembler<'_> {
    fn shl(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::Shl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl ShlEmitter<Gp, Imm> for Assembler<'_> {
    fn shl(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Shl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> ShlEmitter<GpbLo, U1> for Assembler<'_> {
    fn shl(&mut self, op0: GpbLo, op1: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> ShlEmitter<GpbHi, U1> for Assembler<'_> {
    fn shl(&mut self, op0: GpbHi, op1: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> ShlEmitter<Gpw, U1> for Assembler<'_> {
    fn shl(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> ShlEmitter<Gpd, U1> for Assembler<'_> {
    fn shl(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> ShlEmitter<Gpq, U1> for Assembler<'_> {
    fn shl(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> ShlEmitter<Mem, U1> for Assembler<'_> {
    fn shl(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::Shl as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl ShrEmitter<Gp, Gp> for Assembler<'_> {
    fn shr(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Shr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl ShrEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn shr(&mut self, op0: GpbLo, op1: GpbLo) {
        self.emit_n(InstId::Shr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl ShrEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn shr(&mut self, op0: GpbHi, op1: GpbLo) {
        self.emit_n(InstId::Shr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl ShrEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn shr(&mut self, op0: Gpw, op1: GpbLo) {
        self.emit_n(InstId::Shr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl ShrEmitter<Gpd, GpbLo> for Assembler<'_> {
    fn shr(&mut self, op0: Gpd, op1: GpbLo) {
        self.emit_n(InstId::Shr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl ShrEmitter<Gpq, GpbLo> for Assembler<'_> {
    fn shr(&mut self, op0: Gpq, op1: GpbLo) {
        self.emit_n(InstId::Shr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl ShrEmitter<Mem, Gp> for Assembler<'_> {
    fn shr(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Shr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl ShrEmitter<Mem, GpbLo> for Assembler<'_> {
    fn shr(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::Shr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl ShrEmitter<Gp, Imm> for Assembler<'_> {
    fn shr(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Shr as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> ShrEmitter<GpbLo, U1> for Assembler<'_> {
    fn shr(&mut self, op0: GpbLo, op1: U1) {
        self.emit_n(
            InstId::Shr as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> ShrEmitter<GpbHi, U1> for Assembler<'_> {
    fn shr(&mut self, op0: GpbHi, op1: U1) {
        self.emit_n(
            InstId::Shr as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> ShrEmitter<Gpw, U1> for Assembler<'_> {
    fn shr(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Shr as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> ShrEmitter<Gpd, U1> for Assembler<'_> {
    fn shr(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Shr as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> ShrEmitter<Gpq, U1> for Assembler<'_> {
    fn shr(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Shr as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> ShrEmitter<Mem, U1> for Assembler<'_> {
    fn shr(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::Shr as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl ShldEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn shld(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Shld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl ShldEmitter<Gpw, Gpw, GpbLo> for Assembler<'_> {
    fn shld(&mut self, op0: Gpw, op1: Gpw, op2: GpbLo) {
        self.emit_n(
            InstId::Shld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl ShldEmitter<Gpd, Gpd, GpbLo> for Assembler<'_> {
    fn shld(&mut self, op0: Gpd, op1: Gpd, op2: GpbLo) {
        self.emit_n(
            InstId::Shld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl ShldEmitter<Gpq, Gpq, GpbLo> for Assembler<'_> {
    fn shld(&mut self, op0: Gpq, op1: Gpq, op2: GpbLo) {
        self.emit_n(
            InstId::Shld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl ShldEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn shld(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Shld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl ShldEmitter<Mem, Gpw, GpbLo> for Assembler<'_> {
    fn shld(&mut self, op0: Mem, op1: Gpw, op2: GpbLo) {
        self.emit_n(
            InstId::Shld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl ShldEmitter<Mem, Gpd, GpbLo> for Assembler<'_> {
    fn shld(&mut self, op0: Mem, op1: Gpd, op2: GpbLo) {
        self.emit_n(
            InstId::Shld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl ShldEmitter<Mem, Gpq, GpbLo> for Assembler<'_> {
    fn shld(&mut self, op0: Mem, op1: Gpq, op2: GpbLo) {
        self.emit_n(
            InstId::Shld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl ShldEmitter<Gp, Gp, Imm> for Assembler<'_> {
    fn shld(&mut self, op0: Gp, op1: Gp, op2: Imm) {
        self.emit_n(
            InstId::Shld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShldEmitter<Gpw, Gpw, U2> for Assembler<'_> {
    fn shld(&mut self, op0: Gpw, op1: Gpw, op2: U2) {
        self.emit_n(
            InstId::Shld as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShldEmitter<Gpd, Gpd, U2> for Assembler<'_> {
    fn shld(&mut self, op0: Gpd, op1: Gpd, op2: U2) {
        self.emit_n(
            InstId::Shld as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShldEmitter<Gpq, Gpq, U2> for Assembler<'_> {
    fn shld(&mut self, op0: Gpq, op1: Gpq, op2: U2) {
        self.emit_n(
            InstId::Shld as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // ANY
    }
}

impl ShldEmitter<Mem, Gp, Imm> for Assembler<'_> {
    fn shld(&mut self, op0: Mem, op1: Gp, op2: Imm) {
        self.emit_n(
            InstId::Shld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShldEmitter<Mem, Gpw, U2> for Assembler<'_> {
    fn shld(&mut self, op0: Mem, op1: Gpw, op2: U2) {
        self.emit_n(
            InstId::Shld as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShldEmitter<Mem, Gpd, U2> for Assembler<'_> {
    fn shld(&mut self, op0: Mem, op1: Gpd, op2: U2) {
        self.emit_n(
            InstId::Shld as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShldEmitter<Mem, Gpq, U2> for Assembler<'_> {
    fn shld(&mut self, op0: Mem, op1: Gpq, op2: U2) {
        self.emit_n(
            InstId::Shld as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // ANY
    }
}

impl ShrdEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn shrd(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Shrd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl ShrdEmitter<Gpw, Gpw, GpbLo> for Assembler<'_> {
    fn shrd(&mut self, op0: Gpw, op1: Gpw, op2: GpbLo) {
        self.emit_n(
            InstId::Shrd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl ShrdEmitter<Gpd, Gpd, GpbLo> for Assembler<'_> {
    fn shrd(&mut self, op0: Gpd, op1: Gpd, op2: GpbLo) {
        self.emit_n(
            InstId::Shrd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl ShrdEmitter<Gpq, Gpq, GpbLo> for Assembler<'_> {
    fn shrd(&mut self, op0: Gpq, op1: Gpq, op2: GpbLo) {
        self.emit_n(
            InstId::Shrd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl ShrdEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn shrd(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Shrd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl ShrdEmitter<Mem, Gpw, GpbLo> for Assembler<'_> {
    fn shrd(&mut self, op0: Mem, op1: Gpw, op2: GpbLo) {
        self.emit_n(
            InstId::Shrd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl ShrdEmitter<Mem, Gpd, GpbLo> for Assembler<'_> {
    fn shrd(&mut self, op0: Mem, op1: Gpd, op2: GpbLo) {
        self.emit_n(
            InstId::Shrd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl ShrdEmitter<Mem, Gpq, GpbLo> for Assembler<'_> {
    fn shrd(&mut self, op0: Mem, op1: Gpq, op2: GpbLo) {
        self.emit_n(
            InstId::Shrd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl ShrdEmitter<Gp, Gp, Imm> for Assembler<'_> {
    fn shrd(&mut self, op0: Gp, op1: Gp, op2: Imm) {
        self.emit_n(
            InstId::Shrd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShrdEmitter<Gpw, Gpw, U2> for Assembler<'_> {
    fn shrd(&mut self, op0: Gpw, op1: Gpw, op2: U2) {
        self.emit_n(
            InstId::Shrd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShrdEmitter<Gpd, Gpd, U2> for Assembler<'_> {
    fn shrd(&mut self, op0: Gpd, op1: Gpd, op2: U2) {
        self.emit_n(
            InstId::Shrd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShrdEmitter<Gpq, Gpq, U2> for Assembler<'_> {
    fn shrd(&mut self, op0: Gpq, op1: Gpq, op2: U2) {
        self.emit_n(
            InstId::Shrd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // ANY
    }
}

impl ShrdEmitter<Mem, Gp, Imm> for Assembler<'_> {
    fn shrd(&mut self, op0: Mem, op1: Gp, op2: Imm) {
        self.emit_n(
            InstId::Shrd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShrdEmitter<Mem, Gpw, U2> for Assembler<'_> {
    fn shrd(&mut self, op0: Mem, op1: Gpw, op2: U2) {
        self.emit_n(
            InstId::Shrd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShrdEmitter<Mem, Gpd, U2> for Assembler<'_> {
    fn shrd(&mut self, op0: Mem, op1: Gpd, op2: U2) {
        self.emit_n(
            InstId::Shrd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // ANY
    }
}

impl<U2: Into<Imm>> ShrdEmitter<Mem, Gpq, U2> for Assembler<'_> {
    fn shrd(&mut self, op0: Mem, op1: Gpq, op2: U2) {
        self.emit_n(
            InstId::Shrd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // ANY
    }
}

impl StosEmitter<Mem, Gp> for Assembler<'_> {
    fn stos(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Stos as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT]
    }
}

impl StosEmitter<Mem, GpbLo> for Assembler<'_> {
    fn stos(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::Stos as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT]
    }
}

impl StosEmitter<Mem, Gpw> for Assembler<'_> {
    fn stos(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::Stos as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT]
    }
}

impl StosEmitter<Mem, Gpd> for Assembler<'_> {
    fn stos(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Stos as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT]
    }
}

impl StosEmitter<Mem, Gpq> for Assembler<'_> {
    fn stos(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Stos as u32, &[op0.as_operand(), op1.as_operand()]); // ANY [EXPLICIT]
    }
}

impl SubEmitter<Gp, Gp> for Assembler<'_> {
    fn sub(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Sub as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SubEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn sub(&mut self, op0: GpbLo, op1: GpbLo) {
        self.emit_n(InstId::Sub as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SubEmitter<GpbLo, GpbHi> for Assembler<'_> {
    fn sub(&mut self, op0: GpbLo, op1: GpbHi) {
        self.emit_n(InstId::Sub as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SubEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn sub(&mut self, op0: GpbHi, op1: GpbLo) {
        self.emit_n(InstId::Sub as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SubEmitter<GpbHi, GpbHi> for Assembler<'_> {
    fn sub(&mut self, op0: GpbHi, op1: GpbHi) {
        self.emit_n(InstId::Sub as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SubEmitter<Gpw, Gpw> for Assembler<'_> {
    fn sub(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Sub as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SubEmitter<Gpd, Gpd> for Assembler<'_> {
    fn sub(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Sub as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SubEmitter<Gpq, Gpq> for Assembler<'_> {
    fn sub(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Sub as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SubEmitter<Gp, Mem> for Assembler<'_> {
    fn sub(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Sub as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SubEmitter<GpbLo, Mem> for Assembler<'_> {
    fn sub(&mut self, op0: GpbLo, op1: Mem) {
        self.emit_n(InstId::Sub as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SubEmitter<GpbHi, Mem> for Assembler<'_> {
    fn sub(&mut self, op0: GpbHi, op1: Mem) {
        self.emit_n(InstId::Sub as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SubEmitter<Gpw, Mem> for Assembler<'_> {
    fn sub(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Sub as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SubEmitter<Gpd, Mem> for Assembler<'_> {
    fn sub(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Sub as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SubEmitter<Gpq, Mem> for Assembler<'_> {
    fn sub(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Sub as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SubEmitter<Gp, Imm> for Assembler<'_> {
    fn sub(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Sub as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> SubEmitter<GpbLo, U1> for Assembler<'_> {
    fn sub(&mut self, op0: GpbLo, op1: U1) {
        self.emit_n(
            InstId::Sub as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SubEmitter<GpbHi, U1> for Assembler<'_> {
    fn sub(&mut self, op0: GpbHi, op1: U1) {
        self.emit_n(
            InstId::Sub as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SubEmitter<Gpw, U1> for Assembler<'_> {
    fn sub(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Sub as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SubEmitter<Gpd, U1> for Assembler<'_> {
    fn sub(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Sub as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> SubEmitter<Gpq, U1> for Assembler<'_> {
    fn sub(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Sub as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl SubEmitter<Mem, Gp> for Assembler<'_> {
    fn sub(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Sub as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SubEmitter<Mem, GpbLo> for Assembler<'_> {
    fn sub(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::Sub as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SubEmitter<Mem, GpbHi> for Assembler<'_> {
    fn sub(&mut self, op0: Mem, op1: GpbHi) {
        self.emit_n(InstId::Sub as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SubEmitter<Mem, Gpw> for Assembler<'_> {
    fn sub(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::Sub as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SubEmitter<Mem, Gpd> for Assembler<'_> {
    fn sub(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Sub as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl SubEmitter<Mem, Gpq> for Assembler<'_> {
    fn sub(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Sub as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> SubEmitter<Mem, U1> for Assembler<'_> {
    fn sub(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::Sub as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl TestEmitter<Gp, Gp> for Assembler<'_> {
    fn test(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Test as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl TestEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn test(&mut self, op0: GpbLo, op1: GpbLo) {
        self.emit_n(InstId::Test as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl TestEmitter<GpbLo, GpbHi> for Assembler<'_> {
    fn test(&mut self, op0: GpbLo, op1: GpbHi) {
        self.emit_n(InstId::Test as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl TestEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn test(&mut self, op0: GpbHi, op1: GpbLo) {
        self.emit_n(InstId::Test as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl TestEmitter<GpbHi, GpbHi> for Assembler<'_> {
    fn test(&mut self, op0: GpbHi, op1: GpbHi) {
        self.emit_n(InstId::Test as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl TestEmitter<Gpw, Gpw> for Assembler<'_> {
    fn test(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Test as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl TestEmitter<Gpd, Gpd> for Assembler<'_> {
    fn test(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Test as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl TestEmitter<Gpq, Gpq> for Assembler<'_> {
    fn test(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Test as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl TestEmitter<Gp, Imm> for Assembler<'_> {
    fn test(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Test as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> TestEmitter<GpbLo, U1> for Assembler<'_> {
    fn test(&mut self, op0: GpbLo, op1: U1) {
        self.emit_n(
            InstId::Test as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> TestEmitter<GpbHi, U1> for Assembler<'_> {
    fn test(&mut self, op0: GpbHi, op1: U1) {
        self.emit_n(
            InstId::Test as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> TestEmitter<Gpw, U1> for Assembler<'_> {
    fn test(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Test as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> TestEmitter<Gpd, U1> for Assembler<'_> {
    fn test(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Test as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> TestEmitter<Gpq, U1> for Assembler<'_> {
    fn test(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Test as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl TestEmitter<Mem, Gp> for Assembler<'_> {
    fn test(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Test as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl TestEmitter<Mem, GpbLo> for Assembler<'_> {
    fn test(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::Test as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl TestEmitter<Mem, GpbHi> for Assembler<'_> {
    fn test(&mut self, op0: Mem, op1: GpbHi) {
        self.emit_n(InstId::Test as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl TestEmitter<Mem, Gpw> for Assembler<'_> {
    fn test(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::Test as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl TestEmitter<Mem, Gpd> for Assembler<'_> {
    fn test(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Test as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl TestEmitter<Mem, Gpq> for Assembler<'_> {
    fn test(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Test as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> TestEmitter<Mem, U1> for Assembler<'_> {
    fn test(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::Test as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl Ud0Emitter<Gp, Gp> for Assembler<'_> {
    fn ud0(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Ud0 as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Ud0Emitter<Gpd, Gpd> for Assembler<'_> {
    fn ud0(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Ud0 as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Ud0Emitter<Gp, Mem> for Assembler<'_> {
    fn ud0(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Ud0 as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Ud0Emitter<Gpd, Mem> for Assembler<'_> {
    fn ud0(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Ud0 as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Ud1Emitter<Gp, Gp> for Assembler<'_> {
    fn ud1(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Ud1 as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Ud1Emitter<Gpd, Gpd> for Assembler<'_> {
    fn ud1(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Ud1 as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Ud1Emitter<Gp, Mem> for Assembler<'_> {
    fn ud1(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Ud1 as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Ud1Emitter<Gpd, Mem> for Assembler<'_> {
    fn ud1(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Ud1 as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Ud2Emitter for Assembler<'_> {
    fn ud2(&mut self) {
        self.emit_n(InstId::Ud2 as u32, &[]); // ANY
    }
}

impl XaddEmitter<Gp, Gp> for Assembler<'_> {
    fn xadd(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Xadd as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XaddEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn xadd(&mut self, op0: GpbLo, op1: GpbLo) {
        self.emit_n(InstId::Xadd as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XaddEmitter<GpbLo, GpbHi> for Assembler<'_> {
    fn xadd(&mut self, op0: GpbLo, op1: GpbHi) {
        self.emit_n(InstId::Xadd as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XaddEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn xadd(&mut self, op0: GpbHi, op1: GpbLo) {
        self.emit_n(InstId::Xadd as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XaddEmitter<GpbHi, GpbHi> for Assembler<'_> {
    fn xadd(&mut self, op0: GpbHi, op1: GpbHi) {
        self.emit_n(InstId::Xadd as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XaddEmitter<Gpw, Gpw> for Assembler<'_> {
    fn xadd(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Xadd as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XaddEmitter<Gpd, Gpd> for Assembler<'_> {
    fn xadd(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Xadd as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XaddEmitter<Gpq, Gpq> for Assembler<'_> {
    fn xadd(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Xadd as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XaddEmitter<Mem, Gp> for Assembler<'_> {
    fn xadd(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Xadd as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XaddEmitter<Mem, GpbLo> for Assembler<'_> {
    fn xadd(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::Xadd as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XaddEmitter<Mem, GpbHi> for Assembler<'_> {
    fn xadd(&mut self, op0: Mem, op1: GpbHi) {
        self.emit_n(InstId::Xadd as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XaddEmitter<Mem, Gpw> for Assembler<'_> {
    fn xadd(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::Xadd as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XaddEmitter<Mem, Gpd> for Assembler<'_> {
    fn xadd(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Xadd as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XaddEmitter<Mem, Gpq> for Assembler<'_> {
    fn xadd(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Xadd as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XchgEmitter<Gp, Gp> for Assembler<'_> {
    fn xchg(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Xchg as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XchgEmitter<GpbLo, GpbLo> for Assembler<'_> {
    fn xchg(&mut self, op0: GpbLo, op1: GpbLo) {
        self.emit_n(InstId::Xchg as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XchgEmitter<GpbLo, GpbHi> for Assembler<'_> {
    fn xchg(&mut self, op0: GpbLo, op1: GpbHi) {
        self.emit_n(InstId::Xchg as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XchgEmitter<GpbHi, GpbLo> for Assembler<'_> {
    fn xchg(&mut self, op0: GpbHi, op1: GpbLo) {
        self.emit_n(InstId::Xchg as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XchgEmitter<GpbHi, GpbHi> for Assembler<'_> {
    fn xchg(&mut self, op0: GpbHi, op1: GpbHi) {
        self.emit_n(InstId::Xchg as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XchgEmitter<Gpw, Gpw> for Assembler<'_> {
    fn xchg(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Xchg as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XchgEmitter<Gpd, Gpd> for Assembler<'_> {
    fn xchg(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Xchg as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XchgEmitter<Gpq, Gpq> for Assembler<'_> {
    fn xchg(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Xchg as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XchgEmitter<Mem, Gp> for Assembler<'_> {
    fn xchg(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Xchg as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XchgEmitter<Mem, GpbLo> for Assembler<'_> {
    fn xchg(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::Xchg as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XchgEmitter<Mem, GpbHi> for Assembler<'_> {
    fn xchg(&mut self, op0: Mem, op1: GpbHi) {
        self.emit_n(InstId::Xchg as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XchgEmitter<Mem, Gpw> for Assembler<'_> {
    fn xchg(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::Xchg as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XchgEmitter<Mem, Gpd> for Assembler<'_> {
    fn xchg(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Xchg as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XchgEmitter<Mem, Gpq> for Assembler<'_> {
    fn xchg(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Xchg as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XchgEmitter<Gp, Mem> for Assembler<'_> {
    fn xchg(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Xchg as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XchgEmitter<GpbLo, Mem> for Assembler<'_> {
    fn xchg(&mut self, op0: GpbLo, op1: Mem) {
        self.emit_n(InstId::Xchg as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XchgEmitter<GpbHi, Mem> for Assembler<'_> {
    fn xchg(&mut self, op0: GpbHi, op1: Mem) {
        self.emit_n(InstId::Xchg as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XchgEmitter<Gpw, Mem> for Assembler<'_> {
    fn xchg(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Xchg as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XchgEmitter<Gpd, Mem> for Assembler<'_> {
    fn xchg(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Xchg as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl XchgEmitter<Gpq, Mem> for Assembler<'_> {
    fn xchg(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Xchg as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Gp, Gp> for Assembler<'_> {
    fn xor_(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Xor as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Xor_Emitter<GpbLo, GpbLo> for Assembler<'_> {
    fn xor_(&mut self, op0: GpbLo, op1: GpbLo) {
        self.emit_n(InstId::Xor as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Xor_Emitter<GpbLo, GpbHi> for Assembler<'_> {
    fn xor_(&mut self, op0: GpbLo, op1: GpbHi) {
        self.emit_n(InstId::Xor as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Xor_Emitter<GpbHi, GpbLo> for Assembler<'_> {
    fn xor_(&mut self, op0: GpbHi, op1: GpbLo) {
        self.emit_n(InstId::Xor as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Xor_Emitter<GpbHi, GpbHi> for Assembler<'_> {
    fn xor_(&mut self, op0: GpbHi, op1: GpbHi) {
        self.emit_n(InstId::Xor as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Gpw, Gpw> for Assembler<'_> {
    fn xor_(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Xor as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Gpd, Gpd> for Assembler<'_> {
    fn xor_(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Xor as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Gpq, Gpq> for Assembler<'_> {
    fn xor_(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Xor as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Gp, Mem> for Assembler<'_> {
    fn xor_(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Xor as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Xor_Emitter<GpbLo, Mem> for Assembler<'_> {
    fn xor_(&mut self, op0: GpbLo, op1: Mem) {
        self.emit_n(InstId::Xor as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Xor_Emitter<GpbHi, Mem> for Assembler<'_> {
    fn xor_(&mut self, op0: GpbHi, op1: Mem) {
        self.emit_n(InstId::Xor as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Gpw, Mem> for Assembler<'_> {
    fn xor_(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Xor as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Gpd, Mem> for Assembler<'_> {
    fn xor_(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Xor as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Gpq, Mem> for Assembler<'_> {
    fn xor_(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Xor as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Gp, Imm> for Assembler<'_> {
    fn xor_(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Xor as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> Xor_Emitter<GpbLo, U1> for Assembler<'_> {
    fn xor_(&mut self, op0: GpbLo, op1: U1) {
        self.emit_n(
            InstId::Xor as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> Xor_Emitter<GpbHi, U1> for Assembler<'_> {
    fn xor_(&mut self, op0: GpbHi, op1: U1) {
        self.emit_n(
            InstId::Xor as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> Xor_Emitter<Gpw, U1> for Assembler<'_> {
    fn xor_(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Xor as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> Xor_Emitter<Gpd, U1> for Assembler<'_> {
    fn xor_(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::Xor as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> Xor_Emitter<Gpq, U1> for Assembler<'_> {
    fn xor_(&mut self, op0: Gpq, op1: U1) {
        self.emit_n(
            InstId::Xor as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl Xor_Emitter<Mem, Gp> for Assembler<'_> {
    fn xor_(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Xor as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Mem, GpbLo> for Assembler<'_> {
    fn xor_(&mut self, op0: Mem, op1: GpbLo) {
        self.emit_n(InstId::Xor as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Mem, GpbHi> for Assembler<'_> {
    fn xor_(&mut self, op0: Mem, op1: GpbHi) {
        self.emit_n(InstId::Xor as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Mem, Gpw> for Assembler<'_> {
    fn xor_(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::Xor as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Mem, Gpd> for Assembler<'_> {
    fn xor_(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Xor as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl Xor_Emitter<Mem, Gpq> for Assembler<'_> {
    fn xor_(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Xor as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> Xor_Emitter<Mem, U1> for Assembler<'_> {
    fn xor_(&mut self, op0: Mem, op1: U1) {
        self.emit_n(
            InstId::Xor as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl AaaEmitter<Gp> for Assembler<'_> {
    fn aaa(&mut self, op0: Gp) {
        self.emit_n(InstId::Aaa as u32, &[op0.as_operand()]); // X86 [EXPLICIT]
    }
}

impl AaaEmitter<Gpw> for Assembler<'_> {
    fn aaa(&mut self, op0: Gpw) {
        self.emit_n(InstId::Aaa as u32, &[op0.as_operand()]); // X86 [EXPLICIT]
    }
}

impl AadEmitter<Gp, Imm> for Assembler<'_> {
    fn aad(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Aad as u32, &[op0.as_operand(), op1.as_operand()]); // X86 [EXPLICIT]
    }
}

impl<U1: Into<Imm>> AadEmitter<Gpw, U1> for Assembler<'_> {
    fn aad(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Aad as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // X86 [EXPLICIT]
    }
}

impl AamEmitter<Gp, Imm> for Assembler<'_> {
    fn aam(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::Aam as u32, &[op0.as_operand(), op1.as_operand()]); // X86 [EXPLICIT]
    }
}

impl<U1: Into<Imm>> AamEmitter<Gpw, U1> for Assembler<'_> {
    fn aam(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::Aam as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // X86 [EXPLICIT]
    }
}

impl AasEmitter<Gp> for Assembler<'_> {
    fn aas(&mut self, op0: Gp) {
        self.emit_n(InstId::Aas as u32, &[op0.as_operand()]); // X86 [EXPLICIT]
    }
}

impl AasEmitter<Gpw> for Assembler<'_> {
    fn aas(&mut self, op0: Gpw) {
        self.emit_n(InstId::Aas as u32, &[op0.as_operand()]); // X86 [EXPLICIT]
    }
}

impl DaaEmitter<Gp> for Assembler<'_> {
    fn daa(&mut self, op0: Gp) {
        self.emit_n(InstId::Daa as u32, &[op0.as_operand()]); // X86 [EXPLICIT]
    }
}

impl DaaEmitter<Gpw> for Assembler<'_> {
    fn daa(&mut self, op0: Gpw) {
        self.emit_n(InstId::Daa as u32, &[op0.as_operand()]); // X86 [EXPLICIT]
    }
}

impl DasEmitter<Gp> for Assembler<'_> {
    fn das(&mut self, op0: Gp) {
        self.emit_n(InstId::Das as u32, &[op0.as_operand()]); // X86 [EXPLICIT]
    }
}

impl DasEmitter<Gpw> for Assembler<'_> {
    fn das(&mut self, op0: Gpw) {
        self.emit_n(InstId::Das as u32, &[op0.as_operand()]); // X86 [EXPLICIT]
    }
}

impl<U0: Into<Imm>, U1: Into<Imm>> EnterEmitter<U0, U1> for Assembler<'_> {
    fn enter(&mut self, op0: U0, op1: U1) {
        self.emit_n(
            InstId::Enter as u32,
            &[
                Into::<Imm>::into(op0).as_operand(),
                Into::<Imm>::into(op1).as_operand(),
            ],
        ); // ANY
    }
}

impl LeaveEmitter for Assembler<'_> {
    fn leave(&mut self) {
        self.emit_n(InstId::Leave as u32, &[]); // ANY
    }
}

impl InEmitter<Gp, Imm> for Assembler<'_> {
    fn r#in(&mut self, op0: Gp, op1: Imm) {
        self.emit_n(InstId::In as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U1: Into<Imm>> InEmitter<GpbLo, U1> for Assembler<'_> {
    fn r#in(&mut self, op0: GpbLo, op1: U1) {
        self.emit_n(
            InstId::In as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> InEmitter<Gpw, U1> for Assembler<'_> {
    fn r#in(&mut self, op0: Gpw, op1: U1) {
        self.emit_n(
            InstId::In as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl<U1: Into<Imm>> InEmitter<Gpd, U1> for Assembler<'_> {
    fn r#in(&mut self, op0: Gpd, op1: U1) {
        self.emit_n(
            InstId::In as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // ANY
    }
}

impl InEmitter<Gp, Gp> for Assembler<'_> {
    fn r#in(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::In as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl InEmitter<GpbLo, Gpw> for Assembler<'_> {
    fn r#in(&mut self, op0: GpbLo, op1: Gpw) {
        self.emit_n(InstId::In as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl InEmitter<Gpw, Gpw> for Assembler<'_> {
    fn r#in(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::In as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl InEmitter<Gpd, Gpw> for Assembler<'_> {
    fn r#in(&mut self, op0: Gpd, op1: Gpw) {
        self.emit_n(InstId::In as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl InsEmitter<Mem, Gp> for Assembler<'_> {
    fn ins(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Ins as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl InsEmitter<Mem, Gpw> for Assembler<'_> {
    fn ins(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::Ins as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl OutEmitter<Imm, Gp> for Assembler<'_> {
    fn out(&mut self, op0: Imm, op1: Gp) {
        self.emit_n(InstId::Out as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl<U0: Into<Imm>> OutEmitter<U0, GpbLo> for Assembler<'_> {
    fn out(&mut self, op0: U0, op1: GpbLo) {
        self.emit_n(
            InstId::Out as u32,
            &[Into::<Imm>::into(op0).as_operand(), op1.as_operand()],
        ); // ANY
    }
}

impl<U0: Into<Imm>> OutEmitter<U0, Gpw> for Assembler<'_> {
    fn out(&mut self, op0: U0, op1: Gpw) {
        self.emit_n(
            InstId::Out as u32,
            &[Into::<Imm>::into(op0).as_operand(), op1.as_operand()],
        ); // ANY
    }
}

impl<U0: Into<Imm>> OutEmitter<U0, Gpd> for Assembler<'_> {
    fn out(&mut self, op0: U0, op1: Gpd) {
        self.emit_n(
            InstId::Out as u32,
            &[Into::<Imm>::into(op0).as_operand(), op1.as_operand()],
        ); // ANY
    }
}

impl OutEmitter<Gp, Gp> for Assembler<'_> {
    fn out(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Out as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl OutEmitter<Gpw, GpbLo> for Assembler<'_> {
    fn out(&mut self, op0: Gpw, op1: GpbLo) {
        self.emit_n(InstId::Out as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl OutEmitter<Gpw, Gpw> for Assembler<'_> {
    fn out(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Out as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl OutEmitter<Gpw, Gpd> for Assembler<'_> {
    fn out(&mut self, op0: Gpw, op1: Gpd) {
        self.emit_n(InstId::Out as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl OutsEmitter<Gp, Mem> for Assembler<'_> {
    fn outs(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Outs as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl OutsEmitter<Gpw, Mem> for Assembler<'_> {
    fn outs(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Outs as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl ClcEmitter for Assembler<'_> {
    fn clc(&mut self) {
        self.emit_n(InstId::Clc as u32, &[]); // ANY
    }
}

impl CldEmitter for Assembler<'_> {
    fn cld(&mut self) {
        self.emit_n(InstId::Cld as u32, &[]); // ANY
    }
}

impl CmcEmitter for Assembler<'_> {
    fn cmc(&mut self) {
        self.emit_n(InstId::Cmc as u32, &[]); // ANY
    }
}

impl StcEmitter for Assembler<'_> {
    fn stc(&mut self) {
        self.emit_n(InstId::Stc as u32, &[]); // ANY
    }
}

impl StdEmitter for Assembler<'_> {
    fn std(&mut self) {
        self.emit_n(InstId::Std as u32, &[]); // ANY
    }
}

impl AdcxEmitter<Gp, Gp> for Assembler<'_> {
    fn adcx(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Adcx as u32, &[op0.as_operand(), op1.as_operand()]); // ADX
    }
}

impl AdcxEmitter<Gpd, Gpd> for Assembler<'_> {
    fn adcx(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Adcx as u32, &[op0.as_operand(), op1.as_operand()]); // ADX
    }
}

impl AdcxEmitter<Gpq, Gpq> for Assembler<'_> {
    fn adcx(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Adcx as u32, &[op0.as_operand(), op1.as_operand()]); // ADX
    }
}

impl AdcxEmitter<Gp, Mem> for Assembler<'_> {
    fn adcx(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Adcx as u32, &[op0.as_operand(), op1.as_operand()]); // ADX
    }
}

impl AdcxEmitter<Gpd, Mem> for Assembler<'_> {
    fn adcx(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Adcx as u32, &[op0.as_operand(), op1.as_operand()]); // ADX
    }
}

impl AdcxEmitter<Gpq, Mem> for Assembler<'_> {
    fn adcx(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Adcx as u32, &[op0.as_operand(), op1.as_operand()]); // ADX
    }
}

impl AdoxEmitter<Gp, Gp> for Assembler<'_> {
    fn adox(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Adox as u32, &[op0.as_operand(), op1.as_operand()]); // ADX
    }
}

impl AdoxEmitter<Gpd, Gpd> for Assembler<'_> {
    fn adox(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Adox as u32, &[op0.as_operand(), op1.as_operand()]); // ADX
    }
}

impl AdoxEmitter<Gpq, Gpq> for Assembler<'_> {
    fn adox(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Adox as u32, &[op0.as_operand(), op1.as_operand()]); // ADX
    }
}

impl AdoxEmitter<Gp, Mem> for Assembler<'_> {
    fn adox(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Adox as u32, &[op0.as_operand(), op1.as_operand()]); // ADX
    }
}

impl AdoxEmitter<Gpd, Mem> for Assembler<'_> {
    fn adox(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Adox as u32, &[op0.as_operand(), op1.as_operand()]); // ADX
    }
}

impl AdoxEmitter<Gpq, Mem> for Assembler<'_> {
    fn adox(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Adox as u32, &[op0.as_operand(), op1.as_operand()]); // ADX
    }
}

impl CpuidEmitter<Gp, Gp, Gp, Gp> for Assembler<'_> {
    fn cpuid(&mut self, op0: Gp, op1: Gp, op2: Gp, op3: Gp) {
        self.emit_n(
            InstId::Cpuid as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // I486 [EXPLICIT] EAX:EBX:ECX:EDX <- CPUID[EAX:ECX]
    }
}

impl CpuidEmitter<Gpd, Gpd, Gpd, Gpd> for Assembler<'_> {
    fn cpuid(&mut self, op0: Gpd, op1: Gpd, op2: Gpd, op3: Gpd) {
        self.emit_n(
            InstId::Cpuid as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // I486 [EXPLICIT] EAX:EBX:ECX:EDX <- CPUID[EAX:ECX]
    }
}

impl LahfEmitter<Gp> for Assembler<'_> {
    fn lahf(&mut self, op0: Gp) {
        self.emit_n(InstId::Lahf as u32, &[op0.as_operand()]); // LAHFSAHF [EXPLICIT] AH <- EFL
    }
}

impl LahfEmitter<GpbHi> for Assembler<'_> {
    fn lahf(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Lahf as u32, &[op0.as_operand()]); // LAHFSAHF [EXPLICIT] AH <- EFL
    }
}

impl SahfEmitter<Gp> for Assembler<'_> {
    fn sahf(&mut self, op0: Gp) {
        self.emit_n(InstId::Sahf as u32, &[op0.as_operand()]); // LAHFSAHF [EXPLICIT] EFL <- AH
    }
}

impl SahfEmitter<GpbHi> for Assembler<'_> {
    fn sahf(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Sahf as u32, &[op0.as_operand()]); // LAHFSAHF [EXPLICIT] EFL <- AH
    }
}

impl AndnEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn andn(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Andn as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI
    }
}

impl AndnEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn andn(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Andn as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI
    }
}

impl AndnEmitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn andn(&mut self, op0: Gpq, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Andn as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI
    }
}

impl AndnEmitter<Gp, Gp, Mem> for Assembler<'_> {
    fn andn(&mut self, op0: Gp, op1: Gp, op2: Mem) {
        self.emit_n(
            InstId::Andn as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI
    }
}

impl AndnEmitter<Gpd, Gpd, Mem> for Assembler<'_> {
    fn andn(&mut self, op0: Gpd, op1: Gpd, op2: Mem) {
        self.emit_n(
            InstId::Andn as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI
    }
}

impl AndnEmitter<Gpq, Gpq, Mem> for Assembler<'_> {
    fn andn(&mut self, op0: Gpq, op1: Gpq, op2: Mem) {
        self.emit_n(
            InstId::Andn as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI
    }
}

impl BextrEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn bextr(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Bextr as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI
    }
}

impl BextrEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn bextr(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Bextr as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI
    }
}

impl BextrEmitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn bextr(&mut self, op0: Gpq, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Bextr as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI
    }
}

impl BextrEmitter<Gp, Mem, Gp> for Assembler<'_> {
    fn bextr(&mut self, op0: Gp, op1: Mem, op2: Gp) {
        self.emit_n(
            InstId::Bextr as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI
    }
}

impl BextrEmitter<Gpd, Mem, Gpd> for Assembler<'_> {
    fn bextr(&mut self, op0: Gpd, op1: Mem, op2: Gpd) {
        self.emit_n(
            InstId::Bextr as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI
    }
}

impl BextrEmitter<Gpq, Mem, Gpq> for Assembler<'_> {
    fn bextr(&mut self, op0: Gpq, op1: Mem, op2: Gpq) {
        self.emit_n(
            InstId::Bextr as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI
    }
}

impl BlsiEmitter<Gp, Gp> for Assembler<'_> {
    fn blsi(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Blsi as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl BlsiEmitter<Gpd, Gpd> for Assembler<'_> {
    fn blsi(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Blsi as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl BlsiEmitter<Gpq, Gpq> for Assembler<'_> {
    fn blsi(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Blsi as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl BlsiEmitter<Gp, Mem> for Assembler<'_> {
    fn blsi(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Blsi as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl BlsiEmitter<Gpd, Mem> for Assembler<'_> {
    fn blsi(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Blsi as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl BlsiEmitter<Gpq, Mem> for Assembler<'_> {
    fn blsi(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Blsi as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl BlsmskEmitter<Gp, Gp> for Assembler<'_> {
    fn blsmsk(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Blsmsk as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl BlsmskEmitter<Gpd, Gpd> for Assembler<'_> {
    fn blsmsk(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Blsmsk as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl BlsmskEmitter<Gpq, Gpq> for Assembler<'_> {
    fn blsmsk(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Blsmsk as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl BlsmskEmitter<Gp, Mem> for Assembler<'_> {
    fn blsmsk(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Blsmsk as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl BlsmskEmitter<Gpd, Mem> for Assembler<'_> {
    fn blsmsk(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Blsmsk as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl BlsmskEmitter<Gpq, Mem> for Assembler<'_> {
    fn blsmsk(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Blsmsk as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl BlsrEmitter<Gp, Gp> for Assembler<'_> {
    fn blsr(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Blsr as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl BlsrEmitter<Gpd, Gpd> for Assembler<'_> {
    fn blsr(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Blsr as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl BlsrEmitter<Gpq, Gpq> for Assembler<'_> {
    fn blsr(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Blsr as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl BlsrEmitter<Gp, Mem> for Assembler<'_> {
    fn blsr(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Blsr as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl BlsrEmitter<Gpd, Mem> for Assembler<'_> {
    fn blsr(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Blsr as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl BlsrEmitter<Gpq, Mem> for Assembler<'_> {
    fn blsr(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Blsr as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl TzcntEmitter<Gp, Gp> for Assembler<'_> {
    fn tzcnt(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Tzcnt as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl TzcntEmitter<Gpw, Gpw> for Assembler<'_> {
    fn tzcnt(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Tzcnt as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl TzcntEmitter<Gpd, Gpd> for Assembler<'_> {
    fn tzcnt(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Tzcnt as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl TzcntEmitter<Gpq, Gpq> for Assembler<'_> {
    fn tzcnt(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Tzcnt as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl TzcntEmitter<Gp, Mem> for Assembler<'_> {
    fn tzcnt(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Tzcnt as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl TzcntEmitter<Gpw, Mem> for Assembler<'_> {
    fn tzcnt(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Tzcnt as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl TzcntEmitter<Gpd, Mem> for Assembler<'_> {
    fn tzcnt(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Tzcnt as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl TzcntEmitter<Gpq, Mem> for Assembler<'_> {
    fn tzcnt(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Tzcnt as u32, &[op0.as_operand(), op1.as_operand()]); // BMI
    }
}

impl BzhiEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn bzhi(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Bzhi as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl BzhiEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn bzhi(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Bzhi as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl BzhiEmitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn bzhi(&mut self, op0: Gpq, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Bzhi as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl BzhiEmitter<Gp, Mem, Gp> for Assembler<'_> {
    fn bzhi(&mut self, op0: Gp, op1: Mem, op2: Gp) {
        self.emit_n(
            InstId::Bzhi as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl BzhiEmitter<Gpd, Mem, Gpd> for Assembler<'_> {
    fn bzhi(&mut self, op0: Gpd, op1: Mem, op2: Gpd) {
        self.emit_n(
            InstId::Bzhi as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl BzhiEmitter<Gpq, Mem, Gpq> for Assembler<'_> {
    fn bzhi(&mut self, op0: Gpq, op1: Mem, op2: Gpq) {
        self.emit_n(
            InstId::Bzhi as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl MulxEmitter<Gp, Gp, Gp, Gp> for Assembler<'_> {
    fn mulx(&mut self, op0: Gp, op1: Gp, op2: Gp, op3: Gp) {
        self.emit_n(
            InstId::Mulx as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // BMI2 [EXPLICIT]
    }
}

impl MulxEmitter<Gpd, Gpd, Gpd, Gpd> for Assembler<'_> {
    fn mulx(&mut self, op0: Gpd, op1: Gpd, op2: Gpd, op3: Gpd) {
        self.emit_n(
            InstId::Mulx as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // BMI2 [EXPLICIT]
    }
}

impl MulxEmitter<Gpq, Gpq, Gpq, Gpq> for Assembler<'_> {
    fn mulx(&mut self, op0: Gpq, op1: Gpq, op2: Gpq, op3: Gpq) {
        self.emit_n(
            InstId::Mulx as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // BMI2 [EXPLICIT]
    }
}

impl MulxEmitter<Gp, Gp, Mem, Gp> for Assembler<'_> {
    fn mulx(&mut self, op0: Gp, op1: Gp, op2: Mem, op3: Gp) {
        self.emit_n(
            InstId::Mulx as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // BMI2 [EXPLICIT]
    }
}

impl MulxEmitter<Gpd, Gpd, Mem, Gpd> for Assembler<'_> {
    fn mulx(&mut self, op0: Gpd, op1: Gpd, op2: Mem, op3: Gpd) {
        self.emit_n(
            InstId::Mulx as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // BMI2 [EXPLICIT]
    }
}

impl MulxEmitter<Gpq, Gpq, Mem, Gpq> for Assembler<'_> {
    fn mulx(&mut self, op0: Gpq, op1: Gpq, op2: Mem, op3: Gpq) {
        self.emit_n(
            InstId::Mulx as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // BMI2 [EXPLICIT]
    }
}

impl PdepEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn pdep(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Pdep as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl PdepEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn pdep(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Pdep as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl PdepEmitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn pdep(&mut self, op0: Gpq, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Pdep as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl PdepEmitter<Gp, Gp, Mem> for Assembler<'_> {
    fn pdep(&mut self, op0: Gp, op1: Gp, op2: Mem) {
        self.emit_n(
            InstId::Pdep as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl PdepEmitter<Gpd, Gpd, Mem> for Assembler<'_> {
    fn pdep(&mut self, op0: Gpd, op1: Gpd, op2: Mem) {
        self.emit_n(
            InstId::Pdep as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl PdepEmitter<Gpq, Gpq, Mem> for Assembler<'_> {
    fn pdep(&mut self, op0: Gpq, op1: Gpq, op2: Mem) {
        self.emit_n(
            InstId::Pdep as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl PextEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn pext(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Pext as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl PextEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn pext(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Pext as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl PextEmitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn pext(&mut self, op0: Gpq, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Pext as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl PextEmitter<Gp, Gp, Mem> for Assembler<'_> {
    fn pext(&mut self, op0: Gp, op1: Gp, op2: Mem) {
        self.emit_n(
            InstId::Pext as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl PextEmitter<Gpd, Gpd, Mem> for Assembler<'_> {
    fn pext(&mut self, op0: Gpd, op1: Gpd, op2: Mem) {
        self.emit_n(
            InstId::Pext as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl PextEmitter<Gpq, Gpq, Mem> for Assembler<'_> {
    fn pext(&mut self, op0: Gpq, op1: Gpq, op2: Mem) {
        self.emit_n(
            InstId::Pext as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl RorxEmitter<Gp, Gp, Imm> for Assembler<'_> {
    fn rorx(&mut self, op0: Gp, op1: Gp, op2: Imm) {
        self.emit_n(
            InstId::Rorx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl<U2: Into<Imm>> RorxEmitter<Gpd, Gpd, U2> for Assembler<'_> {
    fn rorx(&mut self, op0: Gpd, op1: Gpd, op2: U2) {
        self.emit_n(
            InstId::Rorx as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // BMI2
    }
}

impl<U2: Into<Imm>> RorxEmitter<Gpq, Gpq, U2> for Assembler<'_> {
    fn rorx(&mut self, op0: Gpq, op1: Gpq, op2: U2) {
        self.emit_n(
            InstId::Rorx as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // BMI2
    }
}

impl RorxEmitter<Gp, Mem, Imm> for Assembler<'_> {
    fn rorx(&mut self, op0: Gp, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Rorx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl<U2: Into<Imm>> RorxEmitter<Gpd, Mem, U2> for Assembler<'_> {
    fn rorx(&mut self, op0: Gpd, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Rorx as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // BMI2
    }
}

impl<U2: Into<Imm>> RorxEmitter<Gpq, Mem, U2> for Assembler<'_> {
    fn rorx(&mut self, op0: Gpq, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Rorx as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // BMI2
    }
}

impl SarxEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn sarx(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Sarx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl SarxEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn sarx(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Sarx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl SarxEmitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn sarx(&mut self, op0: Gpq, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Sarx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl SarxEmitter<Gp, Mem, Gp> for Assembler<'_> {
    fn sarx(&mut self, op0: Gp, op1: Mem, op2: Gp) {
        self.emit_n(
            InstId::Sarx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl SarxEmitter<Gpd, Mem, Gpd> for Assembler<'_> {
    fn sarx(&mut self, op0: Gpd, op1: Mem, op2: Gpd) {
        self.emit_n(
            InstId::Sarx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl SarxEmitter<Gpq, Mem, Gpq> for Assembler<'_> {
    fn sarx(&mut self, op0: Gpq, op1: Mem, op2: Gpq) {
        self.emit_n(
            InstId::Sarx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl ShlxEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn shlx(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Shlx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl ShlxEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn shlx(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Shlx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl ShlxEmitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn shlx(&mut self, op0: Gpq, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Shlx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl ShlxEmitter<Gp, Mem, Gp> for Assembler<'_> {
    fn shlx(&mut self, op0: Gp, op1: Mem, op2: Gp) {
        self.emit_n(
            InstId::Shlx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl ShlxEmitter<Gpd, Mem, Gpd> for Assembler<'_> {
    fn shlx(&mut self, op0: Gpd, op1: Mem, op2: Gpd) {
        self.emit_n(
            InstId::Shlx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl ShlxEmitter<Gpq, Mem, Gpq> for Assembler<'_> {
    fn shlx(&mut self, op0: Gpq, op1: Mem, op2: Gpq) {
        self.emit_n(
            InstId::Shlx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl ShrxEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn shrx(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Shrx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl ShrxEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn shrx(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Shrx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl ShrxEmitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn shrx(&mut self, op0: Gpq, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Shrx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl ShrxEmitter<Gp, Mem, Gp> for Assembler<'_> {
    fn shrx(&mut self, op0: Gp, op1: Mem, op2: Gp) {
        self.emit_n(
            InstId::Shrx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl ShrxEmitter<Gpd, Mem, Gpd> for Assembler<'_> {
    fn shrx(&mut self, op0: Gpd, op1: Mem, op2: Gpd) {
        self.emit_n(
            InstId::Shrx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl ShrxEmitter<Gpq, Mem, Gpq> for Assembler<'_> {
    fn shrx(&mut self, op0: Gpq, op1: Mem, op2: Gpq) {
        self.emit_n(
            InstId::Shrx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2
    }
}

impl CmpbexaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpbexadd(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Cmpbexadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpbexaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpbexadd(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Cmpbexadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpbexaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpbexadd(&mut self, op0: Mem, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Cmpbexadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpbxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpbxadd(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Cmpbxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpbxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpbxadd(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Cmpbxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpbxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpbxadd(&mut self, op0: Mem, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Cmpbxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmplexaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmplexadd(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Cmplexadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmplexaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmplexadd(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Cmplexadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmplexaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmplexadd(&mut self, op0: Mem, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Cmplexadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmplxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmplxadd(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Cmplxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmplxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmplxadd(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Cmplxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmplxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmplxadd(&mut self, op0: Mem, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Cmplxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnbexaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpnbexadd(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Cmpnbexadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnbexaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpnbexadd(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Cmpnbexadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnbexaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpnbexadd(&mut self, op0: Mem, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Cmpnbexadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnbxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpnbxadd(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Cmpnbxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnbxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpnbxadd(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Cmpnbxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnbxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpnbxadd(&mut self, op0: Mem, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Cmpnbxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnlexaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpnlexadd(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Cmpnlexadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnlexaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpnlexadd(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Cmpnlexadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnlexaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpnlexadd(&mut self, op0: Mem, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Cmpnlexadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnlxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpnlxadd(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Cmpnlxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnlxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpnlxadd(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Cmpnlxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnlxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpnlxadd(&mut self, op0: Mem, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Cmpnlxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnoxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpnoxadd(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Cmpnoxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnoxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpnoxadd(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Cmpnoxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnoxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpnoxadd(&mut self, op0: Mem, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Cmpnoxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnpxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpnpxadd(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Cmpnpxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnpxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpnpxadd(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Cmpnpxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnpxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpnpxadd(&mut self, op0: Mem, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Cmpnpxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnsxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpnsxadd(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Cmpnsxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnsxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpnsxadd(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Cmpnsxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnsxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpnsxadd(&mut self, op0: Mem, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Cmpnsxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnzxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpnzxadd(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Cmpnzxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnzxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpnzxadd(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Cmpnzxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpnzxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpnzxadd(&mut self, op0: Mem, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Cmpnzxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpoxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpoxadd(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Cmpoxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpoxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpoxadd(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Cmpoxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpoxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpoxadd(&mut self, op0: Mem, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Cmpoxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmppxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmppxadd(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Cmppxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmppxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmppxadd(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Cmppxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmppxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmppxadd(&mut self, op0: Mem, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Cmppxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpsxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpsxadd(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Cmpsxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpsxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpsxadd(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Cmpsxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpsxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpsxadd(&mut self, op0: Mem, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Cmpsxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpzxaddEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn cmpzxadd(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Cmpzxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpzxaddEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn cmpzxadd(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Cmpzxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CmpzxaddEmitter<Mem, Gpq, Gpq> for Assembler<'_> {
    fn cmpzxadd(&mut self, op0: Mem, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Cmpzxadd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl CldemoteEmitter<Mem> for Assembler<'_> {
    fn cldemote(&mut self, op0: Mem) {
        self.emit_n(InstId::Cldemote as u32, &[op0.as_operand()]); // CLDEMOTE
    }
}

impl ClflushEmitter<Mem> for Assembler<'_> {
    fn clflush(&mut self, op0: Mem) {
        self.emit_n(InstId::Clflush as u32, &[op0.as_operand()]); // CLFLUSH
    }
}

impl ClflushoptEmitter<Mem> for Assembler<'_> {
    fn clflushopt(&mut self, op0: Mem) {
        self.emit_n(InstId::Clflushopt as u32, &[op0.as_operand()]); // CLFLUSH_OPT
    }
}

impl ClwbEmitter<Mem> for Assembler<'_> {
    fn clwb(&mut self, op0: Mem) {
        self.emit_n(InstId::Clwb as u32, &[op0.as_operand()]); // CLWB
    }
}

impl ClzeroEmitter<Mem> for Assembler<'_> {
    fn clzero(&mut self, op0: Mem) {
        self.emit_n(InstId::Clzero as u32, &[op0.as_operand()]); // CLZERO [EXPLICIT]
    }
}

impl Crc32Emitter<Gp, Gp> for Assembler<'_> {
    fn crc32(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Crc32 as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_2
    }
}

impl Crc32Emitter<Gpd, GpbLo> for Assembler<'_> {
    fn crc32(&mut self, op0: Gpd, op1: GpbLo) {
        self.emit_n(InstId::Crc32 as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_2
    }
}

impl Crc32Emitter<Gpd, GpbHi> for Assembler<'_> {
    fn crc32(&mut self, op0: Gpd, op1: GpbHi) {
        self.emit_n(InstId::Crc32 as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_2
    }
}

impl Crc32Emitter<Gpd, Gpw> for Assembler<'_> {
    fn crc32(&mut self, op0: Gpd, op1: Gpw) {
        self.emit_n(InstId::Crc32 as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_2
    }
}

impl Crc32Emitter<Gpd, Gpd> for Assembler<'_> {
    fn crc32(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Crc32 as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_2
    }
}

impl Crc32Emitter<Gpq, GpbLo> for Assembler<'_> {
    fn crc32(&mut self, op0: Gpq, op1: GpbLo) {
        self.emit_n(InstId::Crc32 as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_2
    }
}

impl Crc32Emitter<Gpq, GpbHi> for Assembler<'_> {
    fn crc32(&mut self, op0: Gpq, op1: GpbHi) {
        self.emit_n(InstId::Crc32 as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_2
    }
}

impl Crc32Emitter<Gpq, Gpq> for Assembler<'_> {
    fn crc32(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Crc32 as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_2
    }
}

impl Crc32Emitter<Gp, Mem> for Assembler<'_> {
    fn crc32(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Crc32 as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_2
    }
}

impl Crc32Emitter<Gpd, Mem> for Assembler<'_> {
    fn crc32(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Crc32 as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_2
    }
}

impl Crc32Emitter<Gpq, Mem> for Assembler<'_> {
    fn crc32(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Crc32 as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_2
    }
}

impl LfenceEmitter for Assembler<'_> {
    fn lfence(&mut self) {
        self.emit_n(InstId::Lfence as u32, &[]); // SSE2
    }
}

impl MfenceEmitter for Assembler<'_> {
    fn mfence(&mut self) {
        self.emit_n(InstId::Mfence as u32, &[]); // SSE2
    }
}

impl SfenceEmitter for Assembler<'_> {
    fn sfence(&mut self) {
        self.emit_n(InstId::Sfence as u32, &[]); // SSE
    }
}

impl LzcntEmitter<Gp, Gp> for Assembler<'_> {
    fn lzcnt(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Lzcnt as u32, &[op0.as_operand(), op1.as_operand()]); // LZCNT
    }
}

impl LzcntEmitter<Gpw, Gpw> for Assembler<'_> {
    fn lzcnt(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Lzcnt as u32, &[op0.as_operand(), op1.as_operand()]); // LZCNT
    }
}

impl LzcntEmitter<Gpd, Gpd> for Assembler<'_> {
    fn lzcnt(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Lzcnt as u32, &[op0.as_operand(), op1.as_operand()]); // LZCNT
    }
}

impl LzcntEmitter<Gpq, Gpq> for Assembler<'_> {
    fn lzcnt(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Lzcnt as u32, &[op0.as_operand(), op1.as_operand()]); // LZCNT
    }
}

impl LzcntEmitter<Gp, Mem> for Assembler<'_> {
    fn lzcnt(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Lzcnt as u32, &[op0.as_operand(), op1.as_operand()]); // LZCNT
    }
}

impl LzcntEmitter<Gpw, Mem> for Assembler<'_> {
    fn lzcnt(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Lzcnt as u32, &[op0.as_operand(), op1.as_operand()]); // LZCNT
    }
}

impl LzcntEmitter<Gpd, Mem> for Assembler<'_> {
    fn lzcnt(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Lzcnt as u32, &[op0.as_operand(), op1.as_operand()]); // LZCNT
    }
}

impl LzcntEmitter<Gpq, Mem> for Assembler<'_> {
    fn lzcnt(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Lzcnt as u32, &[op0.as_operand(), op1.as_operand()]); // LZCNT
    }
}

impl MovbeEmitter<Gp, Mem> for Assembler<'_> {
    fn movbe(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Movbe as u32, &[op0.as_operand(), op1.as_operand()]); // MOVBE
    }
}

impl MovbeEmitter<Gpw, Mem> for Assembler<'_> {
    fn movbe(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Movbe as u32, &[op0.as_operand(), op1.as_operand()]); // MOVBE
    }
}

impl MovbeEmitter<Gpd, Mem> for Assembler<'_> {
    fn movbe(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Movbe as u32, &[op0.as_operand(), op1.as_operand()]); // MOVBE
    }
}

impl MovbeEmitter<Gpq, Mem> for Assembler<'_> {
    fn movbe(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Movbe as u32, &[op0.as_operand(), op1.as_operand()]); // MOVBE
    }
}

impl MovbeEmitter<Mem, Gp> for Assembler<'_> {
    fn movbe(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Movbe as u32, &[op0.as_operand(), op1.as_operand()]); // MOVBE
    }
}

impl MovbeEmitter<Mem, Gpw> for Assembler<'_> {
    fn movbe(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::Movbe as u32, &[op0.as_operand(), op1.as_operand()]); // MOVBE
    }
}

impl MovbeEmitter<Mem, Gpd> for Assembler<'_> {
    fn movbe(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Movbe as u32, &[op0.as_operand(), op1.as_operand()]); // MOVBE
    }
}

impl MovbeEmitter<Mem, Gpq> for Assembler<'_> {
    fn movbe(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Movbe as u32, &[op0.as_operand(), op1.as_operand()]); // MOVBE
    }
}

impl MovdiriEmitter<Mem, Gp> for Assembler<'_> {
    fn movdiri(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(
            InstId::Movdiri as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MOVDIRI
    }
}

impl MovdiriEmitter<Mem, Gpd> for Assembler<'_> {
    fn movdiri(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(
            InstId::Movdiri as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MOVDIRI
    }
}

impl MovdiriEmitter<Mem, Gpq> for Assembler<'_> {
    fn movdiri(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(
            InstId::Movdiri as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MOVDIRI
    }
}

impl Movdir64bEmitter<Mem, Mem> for Assembler<'_> {
    fn movdir64b(&mut self, op0: Mem, op1: Mem) {
        self.emit_n(
            InstId::Movdir64b as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MOVDIR64B
    }
}

impl LdmxcsrEmitter<Mem> for Assembler<'_> {
    fn ldmxcsr(&mut self, op0: Mem) {
        self.emit_n(InstId::Ldmxcsr as u32, &[op0.as_operand()]); // SSE
    }
}

impl StmxcsrEmitter<Mem> for Assembler<'_> {
    fn stmxcsr(&mut self, op0: Mem) {
        self.emit_n(InstId::Stmxcsr as u32, &[op0.as_operand()]); // SSE
    }
}

impl PopcntEmitter<Gp, Gp> for Assembler<'_> {
    fn popcnt(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Popcnt as u32, &[op0.as_operand(), op1.as_operand()]); // POPCNT
    }
}

impl PopcntEmitter<Gpw, Gpw> for Assembler<'_> {
    fn popcnt(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Popcnt as u32, &[op0.as_operand(), op1.as_operand()]); // POPCNT
    }
}

impl PopcntEmitter<Gpd, Gpd> for Assembler<'_> {
    fn popcnt(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Popcnt as u32, &[op0.as_operand(), op1.as_operand()]); // POPCNT
    }
}

impl PopcntEmitter<Gpq, Gpq> for Assembler<'_> {
    fn popcnt(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Popcnt as u32, &[op0.as_operand(), op1.as_operand()]); // POPCNT
    }
}

impl PopcntEmitter<Gp, Mem> for Assembler<'_> {
    fn popcnt(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Popcnt as u32, &[op0.as_operand(), op1.as_operand()]); // POPCNT
    }
}

impl PopcntEmitter<Gpw, Mem> for Assembler<'_> {
    fn popcnt(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Popcnt as u32, &[op0.as_operand(), op1.as_operand()]); // POPCNT
    }
}

impl PopcntEmitter<Gpd, Mem> for Assembler<'_> {
    fn popcnt(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Popcnt as u32, &[op0.as_operand(), op1.as_operand()]); // POPCNT
    }
}

impl PopcntEmitter<Gpq, Mem> for Assembler<'_> {
    fn popcnt(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Popcnt as u32, &[op0.as_operand(), op1.as_operand()]); // POPCNT
    }
}

impl PrefetchEmitter<Mem> for Assembler<'_> {
    fn prefetch(&mut self, op0: Mem) {
        self.emit_n(InstId::Prefetch as u32, &[op0.as_operand()]); // 3DNOW
    }
}

impl PrefetchntaEmitter<Mem> for Assembler<'_> {
    fn prefetchnta(&mut self, op0: Mem) {
        self.emit_n(InstId::Prefetchnta as u32, &[op0.as_operand()]); // SSE
    }
}

impl Prefetcht0Emitter<Mem> for Assembler<'_> {
    fn prefetcht0(&mut self, op0: Mem) {
        self.emit_n(InstId::Prefetcht0 as u32, &[op0.as_operand()]); // SSE
    }
}

impl Prefetcht1Emitter<Mem> for Assembler<'_> {
    fn prefetcht1(&mut self, op0: Mem) {
        self.emit_n(InstId::Prefetcht1 as u32, &[op0.as_operand()]); // SSE
    }
}

impl Prefetcht2Emitter<Mem> for Assembler<'_> {
    fn prefetcht2(&mut self, op0: Mem) {
        self.emit_n(InstId::Prefetcht2 as u32, &[op0.as_operand()]); // SSE
    }
}

impl PrefetchwEmitter<Mem> for Assembler<'_> {
    fn prefetchw(&mut self, op0: Mem) {
        self.emit_n(InstId::Prefetchw as u32, &[op0.as_operand()]); // PREFETCHW
    }
}

impl Prefetchwt1Emitter<Mem> for Assembler<'_> {
    fn prefetchwt1(&mut self, op0: Mem) {
        self.emit_n(InstId::Prefetchwt1 as u32, &[op0.as_operand()]); // PREFETCHW1
    }
}

impl Prefetchit0Emitter<Mem> for Assembler<'_> {
    fn prefetchit0(&mut self, op0: Mem) {
        self.emit_n(InstId::Prefetchit0 as u32, &[op0.as_operand()]);
    }
}

impl Prefetchit1Emitter<Mem> for Assembler<'_> {
    fn prefetchit1(&mut self, op0: Mem) {
        self.emit_n(InstId::Prefetchit1 as u32, &[op0.as_operand()]);
    }
}

impl AaddEmitter<Mem, Gp> for Assembler<'_> {
    fn aadd(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Aadd as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl AaddEmitter<Mem, Gpd> for Assembler<'_> {
    fn aadd(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Aadd as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl AaddEmitter<Mem, Gpq> for Assembler<'_> {
    fn aadd(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Aadd as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl AandEmitter<Mem, Gp> for Assembler<'_> {
    fn aand(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Aand as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl AandEmitter<Mem, Gpd> for Assembler<'_> {
    fn aand(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Aand as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl AandEmitter<Mem, Gpq> for Assembler<'_> {
    fn aand(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Aand as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl AorEmitter<Mem, Gp> for Assembler<'_> {
    fn aor(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Aor as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl AorEmitter<Mem, Gpd> for Assembler<'_> {
    fn aor(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Aor as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl AorEmitter<Mem, Gpq> for Assembler<'_> {
    fn aor(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Aor as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl AxorEmitter<Mem, Gp> for Assembler<'_> {
    fn axor(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Axor as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl AxorEmitter<Mem, Gpd> for Assembler<'_> {
    fn axor(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Axor as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl AxorEmitter<Mem, Gpq> for Assembler<'_> {
    fn axor(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Axor as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl RdpidEmitter<Gp> for Assembler<'_> {
    fn rdpid(&mut self, op0: Gp) {
        self.emit_n(InstId::Rdpid as u32, &[op0.as_operand()]); // RDPID
    }
}

impl RdpidEmitter<Gpd> for Assembler<'_> {
    fn rdpid(&mut self, op0: Gpd) {
        self.emit_n(InstId::Rdpid as u32, &[op0.as_operand()]); // RDPID
    }
}

impl RdpidEmitter<Gpq> for Assembler<'_> {
    fn rdpid(&mut self, op0: Gpq) {
        self.emit_n(InstId::Rdpid as u32, &[op0.as_operand()]); // RDPID
    }
}

impl RdpruEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn rdpru(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Rdpru as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // RDPRU [EXPLICIT] EDX:EAX <- PRU[ECX]
    }
}

impl RdpruEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn rdpru(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Rdpru as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // RDPRU [EXPLICIT] EDX:EAX <- PRU[ECX]
    }
}

impl RdpkruEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn rdpkru(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Rdpkru as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // RDPKRU [EXPLICIT] EDX:EAX <- PKRU[ECX]
    }
}

impl RdpkruEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn rdpkru(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Rdpkru as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // RDPKRU [EXPLICIT] EDX:EAX <- PKRU[ECX]
    }
}

impl RdtscEmitter<Gp, Gp> for Assembler<'_> {
    fn rdtsc(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Rdtsc as u32, &[op0.as_operand(), op1.as_operand()]); // RDTSC [EXPLICIT] EDX:EAX <- Counter
    }
}

impl RdtscEmitter<Gpd, Gpd> for Assembler<'_> {
    fn rdtsc(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Rdtsc as u32, &[op0.as_operand(), op1.as_operand()]); // RDTSC [EXPLICIT] EDX:EAX <- Counter
    }
}

impl RdtscpEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn rdtscp(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Rdtscp as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // RDTSCP [EXPLICIT] EDX:EAX:EXC <- Counter
    }
}

impl RdtscpEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn rdtscp(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Rdtscp as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // RDTSCP [EXPLICIT] EDX:EAX:EXC <- Counter
    }
}

impl SerializeEmitter for Assembler<'_> {
    fn serialize(&mut self) {
        self.emit_n(InstId::Serialize as u32, &[]); // SERIALIZE
    }
}

impl BlcfillEmitter<Gp, Gp> for Assembler<'_> {
    fn blcfill(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(
            InstId::Blcfill as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // TBM
    }
}

impl BlcfillEmitter<Gpd, Gpd> for Assembler<'_> {
    fn blcfill(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(
            InstId::Blcfill as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // TBM
    }
}

impl BlcfillEmitter<Gpq, Gpq> for Assembler<'_> {
    fn blcfill(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(
            InstId::Blcfill as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // TBM
    }
}

impl BlcfillEmitter<Gp, Mem> for Assembler<'_> {
    fn blcfill(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Blcfill as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // TBM
    }
}

impl BlcfillEmitter<Gpd, Mem> for Assembler<'_> {
    fn blcfill(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Blcfill as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // TBM
    }
}

impl BlcfillEmitter<Gpq, Mem> for Assembler<'_> {
    fn blcfill(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Blcfill as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // TBM
    }
}

impl BlciEmitter<Gp, Gp> for Assembler<'_> {
    fn blci(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Blci as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlciEmitter<Gpd, Gpd> for Assembler<'_> {
    fn blci(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Blci as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlciEmitter<Gpq, Gpq> for Assembler<'_> {
    fn blci(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Blci as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlciEmitter<Gp, Mem> for Assembler<'_> {
    fn blci(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Blci as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlciEmitter<Gpd, Mem> for Assembler<'_> {
    fn blci(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Blci as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlciEmitter<Gpq, Mem> for Assembler<'_> {
    fn blci(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Blci as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlcicEmitter<Gp, Gp> for Assembler<'_> {
    fn blcic(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Blcic as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlcicEmitter<Gpd, Gpd> for Assembler<'_> {
    fn blcic(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Blcic as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlcicEmitter<Gpq, Gpq> for Assembler<'_> {
    fn blcic(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Blcic as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlcicEmitter<Gp, Mem> for Assembler<'_> {
    fn blcic(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Blcic as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlcicEmitter<Gpd, Mem> for Assembler<'_> {
    fn blcic(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Blcic as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlcicEmitter<Gpq, Mem> for Assembler<'_> {
    fn blcic(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Blcic as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlcmskEmitter<Gp, Gp> for Assembler<'_> {
    fn blcmsk(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Blcmsk as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlcmskEmitter<Gpd, Gpd> for Assembler<'_> {
    fn blcmsk(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Blcmsk as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlcmskEmitter<Gpq, Gpq> for Assembler<'_> {
    fn blcmsk(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Blcmsk as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlcmskEmitter<Gp, Mem> for Assembler<'_> {
    fn blcmsk(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Blcmsk as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlcmskEmitter<Gpd, Mem> for Assembler<'_> {
    fn blcmsk(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Blcmsk as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlcmskEmitter<Gpq, Mem> for Assembler<'_> {
    fn blcmsk(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Blcmsk as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlcsEmitter<Gp, Gp> for Assembler<'_> {
    fn blcs(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Blcs as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlcsEmitter<Gpd, Gpd> for Assembler<'_> {
    fn blcs(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Blcs as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlcsEmitter<Gpq, Gpq> for Assembler<'_> {
    fn blcs(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Blcs as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlcsEmitter<Gp, Mem> for Assembler<'_> {
    fn blcs(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Blcs as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlcsEmitter<Gpd, Mem> for Assembler<'_> {
    fn blcs(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Blcs as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlcsEmitter<Gpq, Mem> for Assembler<'_> {
    fn blcs(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Blcs as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlsfillEmitter<Gp, Gp> for Assembler<'_> {
    fn blsfill(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(
            InstId::Blsfill as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // TBM
    }
}

impl BlsfillEmitter<Gpd, Gpd> for Assembler<'_> {
    fn blsfill(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(
            InstId::Blsfill as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // TBM
    }
}

impl BlsfillEmitter<Gpq, Gpq> for Assembler<'_> {
    fn blsfill(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(
            InstId::Blsfill as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // TBM
    }
}

impl BlsfillEmitter<Gp, Mem> for Assembler<'_> {
    fn blsfill(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Blsfill as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // TBM
    }
}

impl BlsfillEmitter<Gpd, Mem> for Assembler<'_> {
    fn blsfill(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Blsfill as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // TBM
    }
}

impl BlsfillEmitter<Gpq, Mem> for Assembler<'_> {
    fn blsfill(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Blsfill as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // TBM
    }
}

impl BlsicEmitter<Gp, Gp> for Assembler<'_> {
    fn blsic(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Blsic as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlsicEmitter<Gpd, Gpd> for Assembler<'_> {
    fn blsic(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Blsic as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlsicEmitter<Gpq, Gpq> for Assembler<'_> {
    fn blsic(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Blsic as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlsicEmitter<Gp, Mem> for Assembler<'_> {
    fn blsic(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Blsic as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlsicEmitter<Gpd, Mem> for Assembler<'_> {
    fn blsic(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Blsic as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl BlsicEmitter<Gpq, Mem> for Assembler<'_> {
    fn blsic(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Blsic as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl T1mskcEmitter<Gp, Gp> for Assembler<'_> {
    fn t1mskc(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::T1mskc as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl T1mskcEmitter<Gpd, Gpd> for Assembler<'_> {
    fn t1mskc(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::T1mskc as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl T1mskcEmitter<Gpq, Gpq> for Assembler<'_> {
    fn t1mskc(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::T1mskc as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl T1mskcEmitter<Gp, Mem> for Assembler<'_> {
    fn t1mskc(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::T1mskc as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl T1mskcEmitter<Gpd, Mem> for Assembler<'_> {
    fn t1mskc(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::T1mskc as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl T1mskcEmitter<Gpq, Mem> for Assembler<'_> {
    fn t1mskc(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::T1mskc as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl TzmskEmitter<Gp, Gp> for Assembler<'_> {
    fn tzmsk(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Tzmsk as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl TzmskEmitter<Gpd, Gpd> for Assembler<'_> {
    fn tzmsk(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Tzmsk as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl TzmskEmitter<Gpq, Gpq> for Assembler<'_> {
    fn tzmsk(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Tzmsk as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl TzmskEmitter<Gp, Mem> for Assembler<'_> {
    fn tzmsk(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Tzmsk as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl TzmskEmitter<Gpd, Mem> for Assembler<'_> {
    fn tzmsk(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Tzmsk as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl TzmskEmitter<Gpq, Mem> for Assembler<'_> {
    fn tzmsk(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Tzmsk as u32, &[op0.as_operand(), op1.as_operand()]); // TBM
    }
}

impl ArplEmitter<Gp, Gp> for Assembler<'_> {
    fn arpl(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Arpl as u32, &[op0.as_operand(), op1.as_operand()]); // X86
    }
}

impl ArplEmitter<Gpw, Gpw> for Assembler<'_> {
    fn arpl(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Arpl as u32, &[op0.as_operand(), op1.as_operand()]); // X86
    }
}

impl ArplEmitter<Mem, Gp> for Assembler<'_> {
    fn arpl(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Arpl as u32, &[op0.as_operand(), op1.as_operand()]); // X86
    }
}

impl ArplEmitter<Mem, Gpw> for Assembler<'_> {
    fn arpl(&mut self, op0: Mem, op1: Gpw) {
        self.emit_n(InstId::Arpl as u32, &[op0.as_operand(), op1.as_operand()]); // X86
    }
}

impl CliEmitter for Assembler<'_> {
    fn cli(&mut self) {
        self.emit_n(InstId::Cli as u32, &[]); // ANY
    }
}

impl GetsecEmitter for Assembler<'_> {
    fn getsec(&mut self) {
        self.emit_n(InstId::Getsec as u32, &[]); // SMX
    }
}

impl<U0: Into<Imm>> Int_Emitter<U0> for Assembler<'_> {
    fn int_(&mut self, op0: U0) {
        self.emit_n(InstId::Int as u32, &[Into::<Imm>::into(op0).as_operand()]); // ANY
    }
}

impl Int3Emitter for Assembler<'_> {
    fn int3(&mut self) {
        self.emit_n(InstId::Int3 as u32, &[]); // ANY
    }
}

impl IntoEmitter for Assembler<'_> {
    fn into(&mut self) {
        self.emit_n(InstId::Into as u32, &[]); // ANY
    }
}

impl LarEmitter<Gp, Gp> for Assembler<'_> {
    fn lar(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Lar as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LarEmitter<Gpw, Gpw> for Assembler<'_> {
    fn lar(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Lar as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LarEmitter<Gpd, Gpd> for Assembler<'_> {
    fn lar(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Lar as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LarEmitter<Gp, Mem> for Assembler<'_> {
    fn lar(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Lar as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LarEmitter<Gpw, Mem> for Assembler<'_> {
    fn lar(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Lar as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LarEmitter<Gpd, Mem> for Assembler<'_> {
    fn lar(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Lar as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LdsEmitter<Gp, Mem> for Assembler<'_> {
    fn lds(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Lds as u32, &[op0.as_operand(), op1.as_operand()]); // X86
    }
}

impl LdsEmitter<Gpw, Mem> for Assembler<'_> {
    fn lds(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Lds as u32, &[op0.as_operand(), op1.as_operand()]); // X86
    }
}

impl LdsEmitter<Gpd, Mem> for Assembler<'_> {
    fn lds(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Lds as u32, &[op0.as_operand(), op1.as_operand()]); // X86
    }
}

impl LesEmitter<Gp, Mem> for Assembler<'_> {
    fn les(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Les as u32, &[op0.as_operand(), op1.as_operand()]); // X86
    }
}

impl LesEmitter<Gpw, Mem> for Assembler<'_> {
    fn les(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Les as u32, &[op0.as_operand(), op1.as_operand()]); // X86
    }
}

impl LesEmitter<Gpd, Mem> for Assembler<'_> {
    fn les(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Les as u32, &[op0.as_operand(), op1.as_operand()]); // X86
    }
}

impl LfsEmitter<Gp, Mem> for Assembler<'_> {
    fn lfs(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Lfs as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LfsEmitter<Gpw, Mem> for Assembler<'_> {
    fn lfs(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Lfs as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LfsEmitter<Gpd, Mem> for Assembler<'_> {
    fn lfs(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Lfs as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LfsEmitter<Gpq, Mem> for Assembler<'_> {
    fn lfs(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Lfs as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LgsEmitter<Gp, Mem> for Assembler<'_> {
    fn lgs(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Lgs as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LgsEmitter<Gpw, Mem> for Assembler<'_> {
    fn lgs(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Lgs as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LgsEmitter<Gpd, Mem> for Assembler<'_> {
    fn lgs(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Lgs as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LgsEmitter<Gpq, Mem> for Assembler<'_> {
    fn lgs(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Lgs as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LslEmitter<Gp, Gp> for Assembler<'_> {
    fn lsl(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Lsl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LslEmitter<Gpw, Gpw> for Assembler<'_> {
    fn lsl(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(InstId::Lsl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LslEmitter<Gpd, Gpd> for Assembler<'_> {
    fn lsl(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Lsl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LslEmitter<Gpq, Gpd> for Assembler<'_> {
    fn lsl(&mut self, op0: Gpq, op1: Gpd) {
        self.emit_n(InstId::Lsl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LslEmitter<Gp, Mem> for Assembler<'_> {
    fn lsl(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Lsl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LslEmitter<Gpw, Mem> for Assembler<'_> {
    fn lsl(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Lsl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LslEmitter<Gpd, Mem> for Assembler<'_> {
    fn lsl(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Lsl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LslEmitter<Gpq, Mem> for Assembler<'_> {
    fn lsl(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Lsl as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LssEmitter<Gp, Mem> for Assembler<'_> {
    fn lss(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Lss as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LssEmitter<Gpw, Mem> for Assembler<'_> {
    fn lss(&mut self, op0: Gpw, op1: Mem) {
        self.emit_n(InstId::Lss as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LssEmitter<Gpd, Mem> for Assembler<'_> {
    fn lss(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Lss as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl LssEmitter<Gpq, Mem> for Assembler<'_> {
    fn lss(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Lss as u32, &[op0.as_operand(), op1.as_operand()]); // ANY
    }
}

impl PauseEmitter for Assembler<'_> {
    fn pause(&mut self) {
        self.emit_n(InstId::Pause as u32, &[]); // SSE2
    }
}

impl RsmEmitter for Assembler<'_> {
    fn rsm(&mut self) {
        self.emit_n(InstId::Rsm as u32, &[]); // X86
    }
}

impl SgdtEmitter<Mem> for Assembler<'_> {
    fn sgdt(&mut self, op0: Mem) {
        self.emit_n(InstId::Sgdt as u32, &[op0.as_operand()]); // ANY
    }
}

impl SidtEmitter<Mem> for Assembler<'_> {
    fn sidt(&mut self, op0: Mem) {
        self.emit_n(InstId::Sidt as u32, &[op0.as_operand()]); // ANY
    }
}

impl SldtEmitter<Gp> for Assembler<'_> {
    fn sldt(&mut self, op0: Gp) {
        self.emit_n(InstId::Sldt as u32, &[op0.as_operand()]); // ANY
    }
}

impl SldtEmitter<Gpw> for Assembler<'_> {
    fn sldt(&mut self, op0: Gpw) {
        self.emit_n(InstId::Sldt as u32, &[op0.as_operand()]); // ANY
    }
}

impl SldtEmitter<Gpd> for Assembler<'_> {
    fn sldt(&mut self, op0: Gpd) {
        self.emit_n(InstId::Sldt as u32, &[op0.as_operand()]); // ANY
    }
}

impl SldtEmitter<Gpq> for Assembler<'_> {
    fn sldt(&mut self, op0: Gpq) {
        self.emit_n(InstId::Sldt as u32, &[op0.as_operand()]); // ANY
    }
}

impl SldtEmitter<Mem> for Assembler<'_> {
    fn sldt(&mut self, op0: Mem) {
        self.emit_n(InstId::Sldt as u32, &[op0.as_operand()]); // ANY
    }
}

impl SmswEmitter<Gp> for Assembler<'_> {
    fn smsw(&mut self, op0: Gp) {
        self.emit_n(InstId::Smsw as u32, &[op0.as_operand()]); // ANY
    }
}

impl SmswEmitter<Gpw> for Assembler<'_> {
    fn smsw(&mut self, op0: Gpw) {
        self.emit_n(InstId::Smsw as u32, &[op0.as_operand()]); // ANY
    }
}

impl SmswEmitter<Gpd> for Assembler<'_> {
    fn smsw(&mut self, op0: Gpd) {
        self.emit_n(InstId::Smsw as u32, &[op0.as_operand()]); // ANY
    }
}

impl SmswEmitter<Gpq> for Assembler<'_> {
    fn smsw(&mut self, op0: Gpq) {
        self.emit_n(InstId::Smsw as u32, &[op0.as_operand()]); // ANY
    }
}

impl SmswEmitter<Mem> for Assembler<'_> {
    fn smsw(&mut self, op0: Mem) {
        self.emit_n(InstId::Smsw as u32, &[op0.as_operand()]); // ANY
    }
}

impl StiEmitter for Assembler<'_> {
    fn sti(&mut self) {
        self.emit_n(InstId::Sti as u32, &[]); // ANY
    }
}

impl StrEmitter<Gp> for Assembler<'_> {
    fn str(&mut self, op0: Gp) {
        self.emit_n(InstId::Str as u32, &[op0.as_operand()]); // ANY
    }
}

impl StrEmitter<Gpw> for Assembler<'_> {
    fn str(&mut self, op0: Gpw) {
        self.emit_n(InstId::Str as u32, &[op0.as_operand()]); // ANY
    }
}

impl StrEmitter<Gpd> for Assembler<'_> {
    fn str(&mut self, op0: Gpd) {
        self.emit_n(InstId::Str as u32, &[op0.as_operand()]); // ANY
    }
}

impl StrEmitter<Gpq> for Assembler<'_> {
    fn str(&mut self, op0: Gpq) {
        self.emit_n(InstId::Str as u32, &[op0.as_operand()]); // ANY
    }
}

impl StrEmitter<Mem> for Assembler<'_> {
    fn str(&mut self, op0: Mem) {
        self.emit_n(InstId::Str as u32, &[op0.as_operand()]); // ANY
    }
}

impl VerrEmitter<Gp> for Assembler<'_> {
    fn verr(&mut self, op0: Gp) {
        self.emit_n(InstId::Verr as u32, &[op0.as_operand()]); // ANY
    }
}

impl VerrEmitter<Gpw> for Assembler<'_> {
    fn verr(&mut self, op0: Gpw) {
        self.emit_n(InstId::Verr as u32, &[op0.as_operand()]); // ANY
    }
}

impl VerrEmitter<Mem> for Assembler<'_> {
    fn verr(&mut self, op0: Mem) {
        self.emit_n(InstId::Verr as u32, &[op0.as_operand()]); // ANY
    }
}

impl VerwEmitter<Gp> for Assembler<'_> {
    fn verw(&mut self, op0: Gp) {
        self.emit_n(InstId::Verw as u32, &[op0.as_operand()]); // ANY
    }
}

impl VerwEmitter<Gpw> for Assembler<'_> {
    fn verw(&mut self, op0: Gpw) {
        self.emit_n(InstId::Verw as u32, &[op0.as_operand()]); // ANY
    }
}

impl VerwEmitter<Mem> for Assembler<'_> {
    fn verw(&mut self, op0: Mem) {
        self.emit_n(InstId::Verw as u32, &[op0.as_operand()]); // ANY
    }
}

impl RdfsbaseEmitter<Gp> for Assembler<'_> {
    fn rdfsbase(&mut self, op0: Gp) {
        self.emit_n(InstId::Rdfsbase as u32, &[op0.as_operand()]); // FSGSBASE
    }
}

impl RdfsbaseEmitter<Gpd> for Assembler<'_> {
    fn rdfsbase(&mut self, op0: Gpd) {
        self.emit_n(InstId::Rdfsbase as u32, &[op0.as_operand()]); // FSGSBASE
    }
}

impl RdfsbaseEmitter<Gpq> for Assembler<'_> {
    fn rdfsbase(&mut self, op0: Gpq) {
        self.emit_n(InstId::Rdfsbase as u32, &[op0.as_operand()]); // FSGSBASE
    }
}

impl RdgsbaseEmitter<Gp> for Assembler<'_> {
    fn rdgsbase(&mut self, op0: Gp) {
        self.emit_n(InstId::Rdgsbase as u32, &[op0.as_operand()]); // FSGSBASE
    }
}

impl RdgsbaseEmitter<Gpd> for Assembler<'_> {
    fn rdgsbase(&mut self, op0: Gpd) {
        self.emit_n(InstId::Rdgsbase as u32, &[op0.as_operand()]); // FSGSBASE
    }
}

impl RdgsbaseEmitter<Gpq> for Assembler<'_> {
    fn rdgsbase(&mut self, op0: Gpq) {
        self.emit_n(InstId::Rdgsbase as u32, &[op0.as_operand()]); // FSGSBASE
    }
}

impl WrfsbaseEmitter<Gp> for Assembler<'_> {
    fn wrfsbase(&mut self, op0: Gp) {
        self.emit_n(InstId::Wrfsbase as u32, &[op0.as_operand()]); // FSGSBASE
    }
}

impl WrfsbaseEmitter<Gpd> for Assembler<'_> {
    fn wrfsbase(&mut self, op0: Gpd) {
        self.emit_n(InstId::Wrfsbase as u32, &[op0.as_operand()]); // FSGSBASE
    }
}

impl WrfsbaseEmitter<Gpq> for Assembler<'_> {
    fn wrfsbase(&mut self, op0: Gpq) {
        self.emit_n(InstId::Wrfsbase as u32, &[op0.as_operand()]); // FSGSBASE
    }
}

impl WrgsbaseEmitter<Gp> for Assembler<'_> {
    fn wrgsbase(&mut self, op0: Gp) {
        self.emit_n(InstId::Wrgsbase as u32, &[op0.as_operand()]); // FSGSBASE
    }
}

impl WrgsbaseEmitter<Gpd> for Assembler<'_> {
    fn wrgsbase(&mut self, op0: Gpd) {
        self.emit_n(InstId::Wrgsbase as u32, &[op0.as_operand()]); // FSGSBASE
    }
}

impl WrgsbaseEmitter<Gpq> for Assembler<'_> {
    fn wrgsbase(&mut self, op0: Gpq) {
        self.emit_n(InstId::Wrgsbase as u32, &[op0.as_operand()]); // FSGSBASE
    }
}

impl FxrstorEmitter<Mem> for Assembler<'_> {
    fn fxrstor(&mut self, op0: Mem) {
        self.emit_n(InstId::Fxrstor as u32, &[op0.as_operand()]); // FXSR
    }
}

impl Fxrstor64Emitter<Mem> for Assembler<'_> {
    fn fxrstor64(&mut self, op0: Mem) {
        self.emit_n(InstId::Fxrstor64 as u32, &[op0.as_operand()]); // FXSR
    }
}

impl FxsaveEmitter<Mem> for Assembler<'_> {
    fn fxsave(&mut self, op0: Mem) {
        self.emit_n(InstId::Fxsave as u32, &[op0.as_operand()]); // FXSR
    }
}

impl Fxsave64Emitter<Mem> for Assembler<'_> {
    fn fxsave64(&mut self, op0: Mem) {
        self.emit_n(InstId::Fxsave64 as u32, &[op0.as_operand()]); // FXSR
    }
}

impl XgetbvEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn xgetbv(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Xgetbv as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE [EXPLICIT] EDX:EAX <- XCR[ECX]
    }
}

impl XgetbvEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn xgetbv(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Xgetbv as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE [EXPLICIT] EDX:EAX <- XCR[ECX]
    }
}

impl XrstorEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xrstor(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Xrstor as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl XrstorEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xrstor(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Xrstor as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl Xrstor64Emitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xrstor64(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Xrstor64 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl Xrstor64Emitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xrstor64(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Xrstor64 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl XrstorsEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xrstors(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Xrstors as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl XrstorsEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xrstors(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Xrstors as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl Xrstors64Emitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xrstors64(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Xrstors64 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl Xrstors64Emitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xrstors64(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Xrstors64 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl XsaveEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xsave(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Xsave as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl XsaveEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xsave(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Xsave as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl Xsave64Emitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xsave64(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Xsave64 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl Xsave64Emitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xsave64(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Xsave64 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl XsavecEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xsavec(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Xsavec as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl XsavecEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xsavec(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Xsavec as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl Xsavec64Emitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xsavec64(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Xsavec64 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl Xsavec64Emitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xsavec64(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Xsavec64 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl XsaveoptEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xsaveopt(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Xsaveopt as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl XsaveoptEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xsaveopt(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Xsaveopt as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl Xsaveopt64Emitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xsaveopt64(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Xsaveopt64 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl Xsaveopt64Emitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xsaveopt64(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Xsaveopt64 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl XsavesEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xsaves(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Xsaves as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl XsavesEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xsaves(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Xsaves as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE [EXPLICIT]
    }
}

impl Xsaves64Emitter<Mem, Gp, Gp> for Assembler<'_> {
    fn xsaves64(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Xsaves64 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl Xsaves64Emitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn xsaves64(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Xsaves64 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE+X64 [EXPLICIT]
    }
}

impl BndclEmitter<Bnd, Gp> for Assembler<'_> {
    fn bndcl(&mut self, op0: Bnd, op1: Gp) {
        self.emit_n(InstId::Bndcl as u32, &[op0.as_operand(), op1.as_operand()]); // MPX
    }
}

impl BndclEmitter<Bnd, Gpd> for Assembler<'_> {
    fn bndcl(&mut self, op0: Bnd, op1: Gpd) {
        self.emit_n(InstId::Bndcl as u32, &[op0.as_operand(), op1.as_operand()]); // MPX
    }
}

impl BndclEmitter<Bnd, Gpq> for Assembler<'_> {
    fn bndcl(&mut self, op0: Bnd, op1: Gpq) {
        self.emit_n(InstId::Bndcl as u32, &[op0.as_operand(), op1.as_operand()]); // MPX
    }
}

impl BndclEmitter<Bnd, Mem> for Assembler<'_> {
    fn bndcl(&mut self, op0: Bnd, op1: Mem) {
        self.emit_n(InstId::Bndcl as u32, &[op0.as_operand(), op1.as_operand()]); // MPX
    }
}

impl BndcnEmitter<Bnd, Gp> for Assembler<'_> {
    fn bndcn(&mut self, op0: Bnd, op1: Gp) {
        self.emit_n(InstId::Bndcn as u32, &[op0.as_operand(), op1.as_operand()]); // MPX
    }
}

impl BndcnEmitter<Bnd, Gpd> for Assembler<'_> {
    fn bndcn(&mut self, op0: Bnd, op1: Gpd) {
        self.emit_n(InstId::Bndcn as u32, &[op0.as_operand(), op1.as_operand()]); // MPX
    }
}

impl BndcnEmitter<Bnd, Gpq> for Assembler<'_> {
    fn bndcn(&mut self, op0: Bnd, op1: Gpq) {
        self.emit_n(InstId::Bndcn as u32, &[op0.as_operand(), op1.as_operand()]); // MPX
    }
}

impl BndcnEmitter<Bnd, Mem> for Assembler<'_> {
    fn bndcn(&mut self, op0: Bnd, op1: Mem) {
        self.emit_n(InstId::Bndcn as u32, &[op0.as_operand(), op1.as_operand()]); // MPX
    }
}

impl BndcuEmitter<Bnd, Gp> for Assembler<'_> {
    fn bndcu(&mut self, op0: Bnd, op1: Gp) {
        self.emit_n(InstId::Bndcu as u32, &[op0.as_operand(), op1.as_operand()]); // MPX
    }
}

impl BndcuEmitter<Bnd, Gpd> for Assembler<'_> {
    fn bndcu(&mut self, op0: Bnd, op1: Gpd) {
        self.emit_n(InstId::Bndcu as u32, &[op0.as_operand(), op1.as_operand()]); // MPX
    }
}

impl BndcuEmitter<Bnd, Gpq> for Assembler<'_> {
    fn bndcu(&mut self, op0: Bnd, op1: Gpq) {
        self.emit_n(InstId::Bndcu as u32, &[op0.as_operand(), op1.as_operand()]); // MPX
    }
}

impl BndcuEmitter<Bnd, Mem> for Assembler<'_> {
    fn bndcu(&mut self, op0: Bnd, op1: Mem) {
        self.emit_n(InstId::Bndcu as u32, &[op0.as_operand(), op1.as_operand()]); // MPX
    }
}

impl BndldxEmitter<Bnd, Mem> for Assembler<'_> {
    fn bndldx(&mut self, op0: Bnd, op1: Mem) {
        self.emit_n(InstId::Bndldx as u32, &[op0.as_operand(), op1.as_operand()]); // MPX
    }
}

impl BndmkEmitter<Bnd, Mem> for Assembler<'_> {
    fn bndmk(&mut self, op0: Bnd, op1: Mem) {
        self.emit_n(InstId::Bndmk as u32, &[op0.as_operand(), op1.as_operand()]); // MPX
    }
}

impl BndmovEmitter<Bnd, Bnd> for Assembler<'_> {
    fn bndmov(&mut self, op0: Bnd, op1: Bnd) {
        self.emit_n(InstId::Bndmov as u32, &[op0.as_operand(), op1.as_operand()]); // MPX
    }
}

impl BndmovEmitter<Bnd, Mem> for Assembler<'_> {
    fn bndmov(&mut self, op0: Bnd, op1: Mem) {
        self.emit_n(InstId::Bndmov as u32, &[op0.as_operand(), op1.as_operand()]); // MPX
    }
}

impl BndmovEmitter<Mem, Bnd> for Assembler<'_> {
    fn bndmov(&mut self, op0: Mem, op1: Bnd) {
        self.emit_n(InstId::Bndmov as u32, &[op0.as_operand(), op1.as_operand()]); // MPX
    }
}

impl BndstxEmitter<Mem, Bnd> for Assembler<'_> {
    fn bndstx(&mut self, op0: Mem, op1: Bnd) {
        self.emit_n(InstId::Bndstx as u32, &[op0.as_operand(), op1.as_operand()]); // MPX
    }
}

impl MonitorxEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn monitorx(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Monitorx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // MONITORX
    }
}

impl MonitorxEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn monitorx(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Monitorx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // MONITORX
    }
}

impl MwaitxEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn mwaitx(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Mwaitx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // MONITORX
    }
}

impl MwaitxEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn mwaitx(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Mwaitx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // MONITORX
    }
}

impl McommitEmitter for Assembler<'_> {
    fn mcommit(&mut self) {
        self.emit_n(InstId::Mcommit as u32, &[]); // MCOMMIT
    }
}

impl PtwriteEmitter<Gp> for Assembler<'_> {
    fn ptwrite(&mut self, op0: Gp) {
        self.emit_n(InstId::Ptwrite as u32, &[op0.as_operand()]); // PTWRITE
    }
}

impl PtwriteEmitter<Gpd> for Assembler<'_> {
    fn ptwrite(&mut self, op0: Gpd) {
        self.emit_n(InstId::Ptwrite as u32, &[op0.as_operand()]); // PTWRITE
    }
}

impl PtwriteEmitter<Gpq> for Assembler<'_> {
    fn ptwrite(&mut self, op0: Gpq) {
        self.emit_n(InstId::Ptwrite as u32, &[op0.as_operand()]); // PTWRITE
    }
}

impl PtwriteEmitter<Mem> for Assembler<'_> {
    fn ptwrite(&mut self, op0: Mem) {
        self.emit_n(InstId::Ptwrite as u32, &[op0.as_operand()]); // PTWRITE
    }
}

impl EnqcmdEmitter<Mem, Mem> for Assembler<'_> {
    fn enqcmd(&mut self, op0: Mem, op1: Mem) {
        self.emit_n(InstId::Enqcmd as u32, &[op0.as_operand(), op1.as_operand()]); // ENQCMD
    }
}

impl EnqcmdsEmitter<Mem, Mem> for Assembler<'_> {
    fn enqcmds(&mut self, op0: Mem, op1: Mem) {
        self.emit_n(
            InstId::Enqcmds as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // ENQCMD
    }
}

impl TpauseEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn tpause(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Tpause as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // WAITPKG
    }
}

impl TpauseEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn tpause(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Tpause as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // WAITPKG
    }
}

impl UmonitorEmitter<Mem> for Assembler<'_> {
    fn umonitor(&mut self, op0: Mem) {
        self.emit_n(InstId::Umonitor as u32, &[op0.as_operand()]); // WAITPKG
    }
}

impl UmwaitEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn umwait(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Umwait as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // WAITPKG
    }
}

impl UmwaitEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn umwait(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Umwait as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // WAITPKG
    }
}

impl RdrandEmitter<Gp> for Assembler<'_> {
    fn rdrand(&mut self, op0: Gp) {
        self.emit_n(InstId::Rdrand as u32, &[op0.as_operand()]); // RDRAND
    }
}

impl RdrandEmitter<Gpw> for Assembler<'_> {
    fn rdrand(&mut self, op0: Gpw) {
        self.emit_n(InstId::Rdrand as u32, &[op0.as_operand()]); // RDRAND
    }
}

impl RdrandEmitter<Gpd> for Assembler<'_> {
    fn rdrand(&mut self, op0: Gpd) {
        self.emit_n(InstId::Rdrand as u32, &[op0.as_operand()]); // RDRAND
    }
}

impl RdrandEmitter<Gpq> for Assembler<'_> {
    fn rdrand(&mut self, op0: Gpq) {
        self.emit_n(InstId::Rdrand as u32, &[op0.as_operand()]); // RDRAND
    }
}

impl RdseedEmitter<Gp> for Assembler<'_> {
    fn rdseed(&mut self, op0: Gp) {
        self.emit_n(InstId::Rdseed as u32, &[op0.as_operand()]); // RDSEED
    }
}

impl RdseedEmitter<Gpw> for Assembler<'_> {
    fn rdseed(&mut self, op0: Gpw) {
        self.emit_n(InstId::Rdseed as u32, &[op0.as_operand()]); // RDSEED
    }
}

impl RdseedEmitter<Gpd> for Assembler<'_> {
    fn rdseed(&mut self, op0: Gpd) {
        self.emit_n(InstId::Rdseed as u32, &[op0.as_operand()]); // RDSEED
    }
}

impl RdseedEmitter<Gpq> for Assembler<'_> {
    fn rdseed(&mut self, op0: Gpq) {
        self.emit_n(InstId::Rdseed as u32, &[op0.as_operand()]); // RDSEED
    }
}

impl LlwpcbEmitter<Gp> for Assembler<'_> {
    fn llwpcb(&mut self, op0: Gp) {
        self.emit_n(InstId::Llwpcb as u32, &[op0.as_operand()]); // LWP
    }
}

impl LlwpcbEmitter<Gpd> for Assembler<'_> {
    fn llwpcb(&mut self, op0: Gpd) {
        self.emit_n(InstId::Llwpcb as u32, &[op0.as_operand()]); // LWP
    }
}

impl LlwpcbEmitter<Gpq> for Assembler<'_> {
    fn llwpcb(&mut self, op0: Gpq) {
        self.emit_n(InstId::Llwpcb as u32, &[op0.as_operand()]); // LWP
    }
}

impl LwpinsEmitter<Gp, Gp, Imm> for Assembler<'_> {
    fn lwpins(&mut self, op0: Gp, op1: Gp, op2: Imm) {
        self.emit_n(
            InstId::Lwpins as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // LWP
    }
}

impl<U2: Into<Imm>> LwpinsEmitter<Gpd, Gpd, U2> for Assembler<'_> {
    fn lwpins(&mut self, op0: Gpd, op1: Gpd, op2: U2) {
        self.emit_n(
            InstId::Lwpins as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // LWP
    }
}

impl<U2: Into<Imm>> LwpinsEmitter<Gpq, Gpd, U2> for Assembler<'_> {
    fn lwpins(&mut self, op0: Gpq, op1: Gpd, op2: U2) {
        self.emit_n(
            InstId::Lwpins as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // LWP
    }
}

impl LwpinsEmitter<Gp, Mem, Imm> for Assembler<'_> {
    fn lwpins(&mut self, op0: Gp, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Lwpins as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // LWP
    }
}

impl<U2: Into<Imm>> LwpinsEmitter<Gpd, Mem, U2> for Assembler<'_> {
    fn lwpins(&mut self, op0: Gpd, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Lwpins as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // LWP
    }
}

impl<U2: Into<Imm>> LwpinsEmitter<Gpq, Mem, U2> for Assembler<'_> {
    fn lwpins(&mut self, op0: Gpq, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Lwpins as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // LWP
    }
}

impl LwpvalEmitter<Gp, Gp, Imm> for Assembler<'_> {
    fn lwpval(&mut self, op0: Gp, op1: Gp, op2: Imm) {
        self.emit_n(
            InstId::Lwpval as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // LWP
    }
}

impl<U2: Into<Imm>> LwpvalEmitter<Gpd, Gpd, U2> for Assembler<'_> {
    fn lwpval(&mut self, op0: Gpd, op1: Gpd, op2: U2) {
        self.emit_n(
            InstId::Lwpval as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // LWP
    }
}

impl<U2: Into<Imm>> LwpvalEmitter<Gpq, Gpd, U2> for Assembler<'_> {
    fn lwpval(&mut self, op0: Gpq, op1: Gpd, op2: U2) {
        self.emit_n(
            InstId::Lwpval as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // LWP
    }
}

impl LwpvalEmitter<Gp, Mem, Imm> for Assembler<'_> {
    fn lwpval(&mut self, op0: Gp, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Lwpval as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // LWP
    }
}

impl<U2: Into<Imm>> LwpvalEmitter<Gpd, Mem, U2> for Assembler<'_> {
    fn lwpval(&mut self, op0: Gpd, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Lwpval as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // LWP
    }
}

impl<U2: Into<Imm>> LwpvalEmitter<Gpq, Mem, U2> for Assembler<'_> {
    fn lwpval(&mut self, op0: Gpq, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Lwpval as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // LWP
    }
}

impl SlwpcbEmitter<Gp> for Assembler<'_> {
    fn slwpcb(&mut self, op0: Gp) {
        self.emit_n(InstId::Slwpcb as u32, &[op0.as_operand()]); // LWP
    }
}

impl SlwpcbEmitter<Gpd> for Assembler<'_> {
    fn slwpcb(&mut self, op0: Gpd) {
        self.emit_n(InstId::Slwpcb as u32, &[op0.as_operand()]); // LWP
    }
}

impl SlwpcbEmitter<Gpq> for Assembler<'_> {
    fn slwpcb(&mut self, op0: Gpq) {
        self.emit_n(InstId::Slwpcb as u32, &[op0.as_operand()]); // LWP
    }
}

impl<U0: Into<Imm>> XabortEmitter<U0> for Assembler<'_> {
    fn xabort(&mut self, op0: U0) {
        self.emit_n(
            InstId::Xabort as u32,
            &[Into::<Imm>::into(op0).as_operand()],
        ); // RTM
    }
}

impl XbeginEmitter<Label> for Assembler<'_> {
    fn xbegin(&mut self, op0: Label) {
        self.emit_n(InstId::Xbegin as u32, &[op0.as_operand()]); // RTM
    }
}

impl<U0: Into<Imm>> XbeginEmitter<U0> for Assembler<'_> {
    fn xbegin(&mut self, op0: U0) {
        self.emit_n(
            InstId::Xbegin as u32,
            &[Into::<Imm>::into(op0).as_operand()],
        ); // RTM
    }
}

impl XendEmitter for Assembler<'_> {
    fn xend(&mut self) {
        self.emit_n(InstId::Xend as u32, &[]); // RTM
    }
}

impl XtestEmitter for Assembler<'_> {
    fn xtest(&mut self) {
        self.emit_n(InstId::Xtest as u32, &[]); // TSX
    }
}

impl XresldtrkEmitter for Assembler<'_> {
    fn xresldtrk(&mut self) {
        self.emit_n(InstId::Xresldtrk as u32, &[]); // TSXLDTRK
    }
}

impl XsusldtrkEmitter for Assembler<'_> {
    fn xsusldtrk(&mut self) {
        self.emit_n(InstId::Xsusldtrk as u32, &[]); // TSXLDTRK
    }
}

impl Endbr32Emitter for Assembler<'_> {
    fn endbr32(&mut self) {
        self.emit_n(InstId::Endbr32 as u32, &[]); // CET_IBT
    }
}

impl Endbr64Emitter for Assembler<'_> {
    fn endbr64(&mut self) {
        self.emit_n(InstId::Endbr64 as u32, &[]); // CET_IBT
    }
}

impl ClrssbsyEmitter<Mem> for Assembler<'_> {
    fn clrssbsy(&mut self, op0: Mem) {
        self.emit_n(InstId::Clrssbsy as u32, &[op0.as_operand()]); // CET_SS
    }
}

impl SetssbsyEmitter for Assembler<'_> {
    fn setssbsy(&mut self) {
        self.emit_n(InstId::Setssbsy as u32, &[]); // CET_SS
    }
}

impl RstorsspEmitter<Mem> for Assembler<'_> {
    fn rstorssp(&mut self, op0: Mem) {
        self.emit_n(InstId::Rstorssp as u32, &[op0.as_operand()]); // CET_SS
    }
}

impl SaveprevsspEmitter for Assembler<'_> {
    fn saveprevssp(&mut self) {
        self.emit_n(InstId::Saveprevssp as u32, &[]); // CET_SS
    }
}

impl IncsspdEmitter<Gp> for Assembler<'_> {
    fn incsspd(&mut self, op0: Gp) {
        self.emit_n(InstId::Incsspd as u32, &[op0.as_operand()]); // CET_SS
    }
}

impl IncsspdEmitter<Gpd> for Assembler<'_> {
    fn incsspd(&mut self, op0: Gpd) {
        self.emit_n(InstId::Incsspd as u32, &[op0.as_operand()]); // CET_SS
    }
}

impl IncsspqEmitter<Gp> for Assembler<'_> {
    fn incsspq(&mut self, op0: Gp) {
        self.emit_n(InstId::Incsspq as u32, &[op0.as_operand()]); // CET_SS
    }
}

impl IncsspqEmitter<Gpq> for Assembler<'_> {
    fn incsspq(&mut self, op0: Gpq) {
        self.emit_n(InstId::Incsspq as u32, &[op0.as_operand()]); // CET_SS
    }
}

impl RdsspdEmitter<Gp> for Assembler<'_> {
    fn rdsspd(&mut self, op0: Gp) {
        self.emit_n(InstId::Rdsspd as u32, &[op0.as_operand()]); // CET_SS
    }
}

impl RdsspdEmitter<Gpd> for Assembler<'_> {
    fn rdsspd(&mut self, op0: Gpd) {
        self.emit_n(InstId::Rdsspd as u32, &[op0.as_operand()]); // CET_SS
    }
}

impl RdsspqEmitter<Gp> for Assembler<'_> {
    fn rdsspq(&mut self, op0: Gp) {
        self.emit_n(InstId::Rdsspq as u32, &[op0.as_operand()]); // CET_SS
    }
}

impl RdsspqEmitter<Gpq> for Assembler<'_> {
    fn rdsspq(&mut self, op0: Gpq) {
        self.emit_n(InstId::Rdsspq as u32, &[op0.as_operand()]); // CET_SS
    }
}

impl WrssdEmitter<Gp, Gp> for Assembler<'_> {
    fn wrssd(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Wrssd as u32, &[op0.as_operand(), op1.as_operand()]); // CET_SS
    }
}

impl WrssdEmitter<Mem, Gp> for Assembler<'_> {
    fn wrssd(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Wrssd as u32, &[op0.as_operand(), op1.as_operand()]); // CET_SS
    }
}

impl WrssdEmitter<Mem, Gpd> for Assembler<'_> {
    fn wrssd(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Wrssd as u32, &[op0.as_operand(), op1.as_operand()]); // CET_SS
    }
}

impl WrssqEmitter<Gp, Gp> for Assembler<'_> {
    fn wrssq(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Wrssq as u32, &[op0.as_operand(), op1.as_operand()]); // CET_SS
    }
}

impl WrssqEmitter<Mem, Gp> for Assembler<'_> {
    fn wrssq(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Wrssq as u32, &[op0.as_operand(), op1.as_operand()]); // CET_SS
    }
}

impl WrssqEmitter<Mem, Gpq> for Assembler<'_> {
    fn wrssq(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Wrssq as u32, &[op0.as_operand(), op1.as_operand()]); // CET_SS
    }
}

impl WrussdEmitter<Gp, Gp> for Assembler<'_> {
    fn wrussd(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Wrussd as u32, &[op0.as_operand(), op1.as_operand()]); // CET_SS
    }
}

impl WrussdEmitter<Mem, Gp> for Assembler<'_> {
    fn wrussd(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Wrussd as u32, &[op0.as_operand(), op1.as_operand()]); // CET_SS
    }
}

impl WrussdEmitter<Mem, Gpd> for Assembler<'_> {
    fn wrussd(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Wrussd as u32, &[op0.as_operand(), op1.as_operand()]); // CET_SS
    }
}

impl WrussqEmitter<Gp, Gp> for Assembler<'_> {
    fn wrussq(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Wrussq as u32, &[op0.as_operand(), op1.as_operand()]); // CET_SS
    }
}

impl WrussqEmitter<Mem, Gp> for Assembler<'_> {
    fn wrussq(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Wrussq as u32, &[op0.as_operand(), op1.as_operand()]); // CET_SS
    }
}

impl WrussqEmitter<Mem, Gpq> for Assembler<'_> {
    fn wrussq(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Wrussq as u32, &[op0.as_operand(), op1.as_operand()]); // CET_SS
    }
}

impl HresetEmitter<Imm, Gp> for Assembler<'_> {
    fn hreset(&mut self, op0: Imm, op1: Gp) {
        self.emit_n(InstId::Hreset as u32, &[op0.as_operand(), op1.as_operand()]); // HRESET
    }
}

impl<U0: Into<Imm>> HresetEmitter<U0, Gpd> for Assembler<'_> {
    fn hreset(&mut self, op0: U0, op1: Gpd) {
        self.emit_n(
            InstId::Hreset as u32,
            &[Into::<Imm>::into(op0).as_operand(), op1.as_operand()],
        ); // HRESET
    }
}

impl CluiEmitter for Assembler<'_> {
    fn clui(&mut self) {
        self.emit_n(InstId::Clui as u32, &[]); // UINTR
    }
}

impl SenduipiEmitter<Gp> for Assembler<'_> {
    fn senduipi(&mut self, op0: Gp) {
        self.emit_n(InstId::Senduipi as u32, &[op0.as_operand()]); // UINTR
    }
}

impl SenduipiEmitter<Gpq> for Assembler<'_> {
    fn senduipi(&mut self, op0: Gpq) {
        self.emit_n(InstId::Senduipi as u32, &[op0.as_operand()]); // UINTR
    }
}

impl TestuiEmitter for Assembler<'_> {
    fn testui(&mut self) {
        self.emit_n(InstId::Testui as u32, &[]); // UINTR
    }
}

impl StuiEmitter for Assembler<'_> {
    fn stui(&mut self) {
        self.emit_n(InstId::Stui as u32, &[]); // UINTR
    }
}

impl UiretEmitter for Assembler<'_> {
    fn uiret(&mut self) {
        self.emit_n(InstId::Uiret as u32, &[]); // UINTR
    }
}

impl CltsEmitter for Assembler<'_> {
    fn clts(&mut self) {
        self.emit_n(InstId::Clts as u32, &[]); // ANY
    }
}

impl HltEmitter for Assembler<'_> {
    fn hlt(&mut self) {
        self.emit_n(InstId::Hlt as u32, &[]); // ANY
    }
}

impl InvdEmitter for Assembler<'_> {
    fn invd(&mut self) {
        self.emit_n(InstId::Invd as u32, &[]); // ANY
    }
}

impl InvlpgEmitter<Mem> for Assembler<'_> {
    fn invlpg(&mut self, op0: Mem) {
        self.emit_n(InstId::Invlpg as u32, &[op0.as_operand()]); // ANY
    }
}

impl InvpcidEmitter<Gp, Mem> for Assembler<'_> {
    fn invpcid(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Invpcid as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // ANY
    }
}

impl InvpcidEmitter<Gpd, Mem> for Assembler<'_> {
    fn invpcid(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Invpcid as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // ANY
    }
}

impl InvpcidEmitter<Gpq, Mem> for Assembler<'_> {
    fn invpcid(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Invpcid as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // ANY
    }
}

impl LgdtEmitter<Mem> for Assembler<'_> {
    fn lgdt(&mut self, op0: Mem) {
        self.emit_n(InstId::Lgdt as u32, &[op0.as_operand()]); // ANY
    }
}

impl LidtEmitter<Mem> for Assembler<'_> {
    fn lidt(&mut self, op0: Mem) {
        self.emit_n(InstId::Lidt as u32, &[op0.as_operand()]); // ANY
    }
}

impl LldtEmitter<Gp> for Assembler<'_> {
    fn lldt(&mut self, op0: Gp) {
        self.emit_n(InstId::Lldt as u32, &[op0.as_operand()]); // ANY
    }
}

impl LldtEmitter<Gpw> for Assembler<'_> {
    fn lldt(&mut self, op0: Gpw) {
        self.emit_n(InstId::Lldt as u32, &[op0.as_operand()]); // ANY
    }
}

impl LldtEmitter<Mem> for Assembler<'_> {
    fn lldt(&mut self, op0: Mem) {
        self.emit_n(InstId::Lldt as u32, &[op0.as_operand()]); // ANY
    }
}

impl LmswEmitter<Gp> for Assembler<'_> {
    fn lmsw(&mut self, op0: Gp) {
        self.emit_n(InstId::Lmsw as u32, &[op0.as_operand()]); // ANY
    }
}

impl LmswEmitter<Gpw> for Assembler<'_> {
    fn lmsw(&mut self, op0: Gpw) {
        self.emit_n(InstId::Lmsw as u32, &[op0.as_operand()]); // ANY
    }
}

impl LmswEmitter<Mem> for Assembler<'_> {
    fn lmsw(&mut self, op0: Mem) {
        self.emit_n(InstId::Lmsw as u32, &[op0.as_operand()]); // ANY
    }
}

impl LtrEmitter<Gp> for Assembler<'_> {
    fn ltr(&mut self, op0: Gp) {
        self.emit_n(InstId::Ltr as u32, &[op0.as_operand()]); // ANY
    }
}

impl LtrEmitter<Gpw> for Assembler<'_> {
    fn ltr(&mut self, op0: Gpw) {
        self.emit_n(InstId::Ltr as u32, &[op0.as_operand()]); // ANY
    }
}

impl LtrEmitter<Mem> for Assembler<'_> {
    fn ltr(&mut self, op0: Mem) {
        self.emit_n(InstId::Ltr as u32, &[op0.as_operand()]); // ANY
    }
}

impl RdmsrEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn rdmsr(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Rdmsr as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // MSR [EXPLICIT] RDX:EAX <- MSR[ECX]
    }
}

impl RdmsrEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn rdmsr(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Rdmsr as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // MSR [EXPLICIT] RDX:EAX <- MSR[ECX]
    }
}

impl RdpmcEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn rdpmc(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Rdpmc as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] RDX:EAX <- PMC[ECX]
    }
}

impl RdpmcEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn rdpmc(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Rdpmc as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // ANY [EXPLICIT] RDX:EAX <- PMC[ECX]
    }
}

impl SwapgsEmitter for Assembler<'_> {
    fn swapgs(&mut self) {
        self.emit_n(InstId::Swapgs as u32, &[]); // X64
    }
}

impl WbinvdEmitter for Assembler<'_> {
    fn wbinvd(&mut self) {
        self.emit_n(InstId::Wbinvd as u32, &[]); // ANY
    }
}

impl WbnoinvdEmitter for Assembler<'_> {
    fn wbnoinvd(&mut self) {
        self.emit_n(InstId::Wbnoinvd as u32, &[]); // WBNOINVD
    }
}

impl WrmsrEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn wrmsr(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Wrmsr as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // MSR [EXPLICIT] RDX:EAX -> MSR[ECX]
    }
}

impl WrmsrEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn wrmsr(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Wrmsr as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // MSR [EXPLICIT] RDX:EAX -> MSR[ECX]
    }
}

impl XsetbvEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn xsetbv(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Xsetbv as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE [EXPLICIT] XCR[ECX] <- EDX:EAX
    }
}

impl XsetbvEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn xsetbv(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Xsetbv as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XSAVE [EXPLICIT] XCR[ECX] <- EDX:EAX
    }
}

impl InvlpgbEmitter<Gp, Gp, Gp> for Assembler<'_> {
    fn invlpgb(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Invlpgb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl InvlpgbEmitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn invlpgb(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Invlpgb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl InvlpgbEmitter<Gpq, Gpd, Gpd> for Assembler<'_> {
    fn invlpgb(&mut self, op0: Gpq, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Invlpgb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl TlbsyncEmitter for Assembler<'_> {
    fn tlbsync(&mut self) {
        self.emit_n(InstId::Tlbsync as u32, &[]);
    }
}

impl MonitorEmitter<Mem, Gp, Gp> for Assembler<'_> {
    fn monitor(&mut self, op0: Mem, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Monitor as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // MONITOR
    }
}

impl MonitorEmitter<Mem, Gpd, Gpd> for Assembler<'_> {
    fn monitor(&mut self, op0: Mem, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Monitor as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // MONITOR
    }
}

impl MwaitEmitter<Gp, Gp> for Assembler<'_> {
    fn mwait(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Mwait as u32, &[op0.as_operand(), op1.as_operand()]); // MONITOR
    }
}

impl MwaitEmitter<Gpd, Gpd> for Assembler<'_> {
    fn mwait(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Mwait as u32, &[op0.as_operand(), op1.as_operand()]); // MONITOR
    }
}

impl ClacEmitter for Assembler<'_> {
    fn clac(&mut self) {
        self.emit_n(InstId::Clac as u32, &[]); // SMAP
    }
}

impl StacEmitter for Assembler<'_> {
    fn stac(&mut self) {
        self.emit_n(InstId::Stac as u32, &[]); // SMAP
    }
}

impl SkinitEmitter<Gp> for Assembler<'_> {
    fn skinit(&mut self, op0: Gp) {
        self.emit_n(InstId::Skinit as u32, &[op0.as_operand()]); // SKINIT [EXPLICIT] <eax>
    }
}

impl SkinitEmitter<Gpd> for Assembler<'_> {
    fn skinit(&mut self, op0: Gpd) {
        self.emit_n(InstId::Skinit as u32, &[op0.as_operand()]); // SKINIT [EXPLICIT] <eax>
    }
}

impl StgiEmitter for Assembler<'_> {
    fn stgi(&mut self) {
        self.emit_n(InstId::Stgi as u32, &[]); // SKINIT
    }
}

impl PsmashEmitter for Assembler<'_> {
    fn psmash(&mut self) {
        self.emit_n(InstId::Psmash as u32, &[]); // SNP
    }
}

impl PvalidateEmitter for Assembler<'_> {
    fn pvalidate(&mut self) {
        self.emit_n(InstId::Pvalidate as u32, &[]); // SNP
    }
}

impl RmpadjustEmitter for Assembler<'_> {
    fn rmpadjust(&mut self) {
        self.emit_n(InstId::Rmpadjust as u32, &[]); // SNP
    }
}

impl RmpupdateEmitter for Assembler<'_> {
    fn rmpupdate(&mut self) {
        self.emit_n(InstId::Rmpupdate as u32, &[]); // SNP
    }
}

impl InveptEmitter<Gp, Mem> for Assembler<'_> {
    fn invept(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(InstId::Invept as u32, &[op0.as_operand(), op1.as_operand()]); // VMX
    }
}

impl InveptEmitter<Gpd, Mem> for Assembler<'_> {
    fn invept(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(InstId::Invept as u32, &[op0.as_operand(), op1.as_operand()]); // VMX
    }
}

impl InveptEmitter<Gpq, Mem> for Assembler<'_> {
    fn invept(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(InstId::Invept as u32, &[op0.as_operand(), op1.as_operand()]); // VMX
    }
}

impl InvvpidEmitter<Gp, Mem> for Assembler<'_> {
    fn invvpid(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Invvpid as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // VMX
    }
}

impl InvvpidEmitter<Gpd, Mem> for Assembler<'_> {
    fn invvpid(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Invvpid as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // VMX
    }
}

impl InvvpidEmitter<Gpq, Mem> for Assembler<'_> {
    fn invvpid(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Invvpid as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // VMX
    }
}

impl VmcallEmitter for Assembler<'_> {
    fn vmcall(&mut self) {
        self.emit_n(InstId::Vmcall as u32, &[]); // VMX
    }
}

impl VmclearEmitter<Mem> for Assembler<'_> {
    fn vmclear(&mut self, op0: Mem) {
        self.emit_n(InstId::Vmclear as u32, &[op0.as_operand()]); // VMX
    }
}

impl VmfuncEmitter for Assembler<'_> {
    fn vmfunc(&mut self) {
        self.emit_n(InstId::Vmfunc as u32, &[]); // VMX
    }
}

impl VmlaunchEmitter for Assembler<'_> {
    fn vmlaunch(&mut self) {
        self.emit_n(InstId::Vmlaunch as u32, &[]); // VMX
    }
}

impl VmptrldEmitter<Mem> for Assembler<'_> {
    fn vmptrld(&mut self, op0: Mem) {
        self.emit_n(InstId::Vmptrld as u32, &[op0.as_operand()]); // VMX
    }
}

impl VmptrstEmitter<Mem> for Assembler<'_> {
    fn vmptrst(&mut self, op0: Mem) {
        self.emit_n(InstId::Vmptrst as u32, &[op0.as_operand()]); // VMX
    }
}

impl VmreadEmitter<Gp, Gp> for Assembler<'_> {
    fn vmread(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(InstId::Vmread as u32, &[op0.as_operand(), op1.as_operand()]); // VMX
    }
}

impl VmreadEmitter<Gpd, Gpd> for Assembler<'_> {
    fn vmread(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(InstId::Vmread as u32, &[op0.as_operand(), op1.as_operand()]); // VMX
    }
}

impl VmreadEmitter<Gpq, Gpq> for Assembler<'_> {
    fn vmread(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(InstId::Vmread as u32, &[op0.as_operand(), op1.as_operand()]); // VMX
    }
}

impl VmreadEmitter<Mem, Gp> for Assembler<'_> {
    fn vmread(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(InstId::Vmread as u32, &[op0.as_operand(), op1.as_operand()]); // VMX
    }
}

impl VmreadEmitter<Mem, Gpd> for Assembler<'_> {
    fn vmread(&mut self, op0: Mem, op1: Gpd) {
        self.emit_n(InstId::Vmread as u32, &[op0.as_operand(), op1.as_operand()]); // VMX
    }
}

impl VmreadEmitter<Mem, Gpq> for Assembler<'_> {
    fn vmread(&mut self, op0: Mem, op1: Gpq) {
        self.emit_n(InstId::Vmread as u32, &[op0.as_operand(), op1.as_operand()]); // VMX
    }
}

impl VmresumeEmitter for Assembler<'_> {
    fn vmresume(&mut self) {
        self.emit_n(InstId::Vmresume as u32, &[]); // VMX
    }
}

impl VmwriteEmitter<Gp, Mem> for Assembler<'_> {
    fn vmwrite(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Vmwrite as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // VMX
    }
}

impl VmwriteEmitter<Gpd, Mem> for Assembler<'_> {
    fn vmwrite(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Vmwrite as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // VMX
    }
}

impl VmwriteEmitter<Gpq, Mem> for Assembler<'_> {
    fn vmwrite(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Vmwrite as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // VMX
    }
}

impl VmwriteEmitter<Gp, Gp> for Assembler<'_> {
    fn vmwrite(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(
            InstId::Vmwrite as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // VMX
    }
}

impl VmwriteEmitter<Gpd, Gpd> for Assembler<'_> {
    fn vmwrite(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(
            InstId::Vmwrite as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // VMX
    }
}

impl VmwriteEmitter<Gpq, Gpq> for Assembler<'_> {
    fn vmwrite(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(
            InstId::Vmwrite as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // VMX
    }
}

impl VmxoffEmitter for Assembler<'_> {
    fn vmxoff(&mut self) {
        self.emit_n(InstId::Vmxoff as u32, &[]); // VMX
    }
}

impl VmxonEmitter<Mem> for Assembler<'_> {
    fn vmxon(&mut self, op0: Mem) {
        self.emit_n(InstId::Vmxon as u32, &[op0.as_operand()]); // VMX
    }
}

impl ClgiEmitter for Assembler<'_> {
    fn clgi(&mut self) {
        self.emit_n(InstId::Clgi as u32, &[]); // SVM
    }
}

impl InvlpgaEmitter<Gp, Gp> for Assembler<'_> {
    fn invlpga(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(
            InstId::Invlpga as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SVM [EXPLICIT] <eax|rax, ecx>
    }
}

impl InvlpgaEmitter<Gpd, Gpd> for Assembler<'_> {
    fn invlpga(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(
            InstId::Invlpga as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SVM [EXPLICIT] <eax|rax, ecx>
    }
}

impl InvlpgaEmitter<Gpq, Gpd> for Assembler<'_> {
    fn invlpga(&mut self, op0: Gpq, op1: Gpd) {
        self.emit_n(
            InstId::Invlpga as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SVM [EXPLICIT] <eax|rax, ecx>
    }
}

impl VmloadEmitter<Gp> for Assembler<'_> {
    fn vmload(&mut self, op0: Gp) {
        self.emit_n(InstId::Vmload as u32, &[op0.as_operand()]); // SVM [EXPLICIT] <zax>
    }
}

impl VmloadEmitter<Gpd> for Assembler<'_> {
    fn vmload(&mut self, op0: Gpd) {
        self.emit_n(InstId::Vmload as u32, &[op0.as_operand()]); // SVM [EXPLICIT] <zax>
    }
}

impl VmloadEmitter<Gpq> for Assembler<'_> {
    fn vmload(&mut self, op0: Gpq) {
        self.emit_n(InstId::Vmload as u32, &[op0.as_operand()]); // SVM [EXPLICIT] <zax>
    }
}

impl VmmcallEmitter for Assembler<'_> {
    fn vmmcall(&mut self) {
        self.emit_n(InstId::Vmmcall as u32, &[]); // SVM
    }
}

impl VmrunEmitter<Gp> for Assembler<'_> {
    fn vmrun(&mut self, op0: Gp) {
        self.emit_n(InstId::Vmrun as u32, &[op0.as_operand()]); // SVM [EXPLICIT] <zax>
    }
}

impl VmrunEmitter<Gpd> for Assembler<'_> {
    fn vmrun(&mut self, op0: Gpd) {
        self.emit_n(InstId::Vmrun as u32, &[op0.as_operand()]); // SVM [EXPLICIT] <zax>
    }
}

impl VmrunEmitter<Gpq> for Assembler<'_> {
    fn vmrun(&mut self, op0: Gpq) {
        self.emit_n(InstId::Vmrun as u32, &[op0.as_operand()]); // SVM [EXPLICIT] <zax>
    }
}

impl VmsaveEmitter<Gp> for Assembler<'_> {
    fn vmsave(&mut self, op0: Gp) {
        self.emit_n(InstId::Vmsave as u32, &[op0.as_operand()]); // SVM [EXPLICIT] <zax>
    }
}

impl VmsaveEmitter<Gpd> for Assembler<'_> {
    fn vmsave(&mut self, op0: Gpd) {
        self.emit_n(InstId::Vmsave as u32, &[op0.as_operand()]); // SVM [EXPLICIT] <zax>
    }
}

impl VmsaveEmitter<Gpq> for Assembler<'_> {
    fn vmsave(&mut self, op0: Gpq) {
        self.emit_n(InstId::Vmsave as u32, &[op0.as_operand()]); // SVM [EXPLICIT] <zax>
    }
}

impl VmgexitEmitter for Assembler<'_> {
    fn vmgexit(&mut self) {
        self.emit_n(InstId::Vmgexit as u32, &[]);
    }
}

impl F2xm1Emitter for Assembler<'_> {
    fn f2xm1(&mut self) {
        self.emit_n(InstId::F2xm1 as u32, &[]); // FPU
    }
}

impl FabsEmitter for Assembler<'_> {
    fn fabs(&mut self) {
        self.emit_n(InstId::Fabs as u32, &[]); // FPU
    }
}

impl FaddEmitter<St, St> for Assembler<'_> {
    fn fadd(&mut self, op0: St, op1: St) {
        self.emit_n(InstId::Fadd as u32, &[op0.as_operand(), op1.as_operand()]); // FPU
    }
}

impl Fadd1Emitter<Mem> for Assembler<'_> {
    fn fadd_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Fadd as u32, &[op0.as_operand()]); // FPU
    }
}

impl FaddpEmitter<St> for Assembler<'_> {
    fn faddp(&mut self, op0: St) {
        self.emit_n(InstId::Faddp as u32, &[op0.as_operand()]); // FPU
    }
}

impl Faddp0Emitter for Assembler<'_> {
    fn faddp_0(&mut self) {
        self.emit_n(InstId::Faddp as u32, &[]); // FPU
    }
}

impl FbldEmitter<Mem> for Assembler<'_> {
    fn fbld(&mut self, op0: Mem) {
        self.emit_n(InstId::Fbld as u32, &[op0.as_operand()]); // FPU
    }
}

impl FbstpEmitter<Mem> for Assembler<'_> {
    fn fbstp(&mut self, op0: Mem) {
        self.emit_n(InstId::Fbstp as u32, &[op0.as_operand()]); // FPU
    }
}

impl FchsEmitter for Assembler<'_> {
    fn fchs(&mut self) {
        self.emit_n(InstId::Fchs as u32, &[]); // FPU
    }
}

impl FclexEmitter for Assembler<'_> {
    fn fclex(&mut self) {
        self.emit_n(InstId::Fclex as u32, &[]); // FPU
    }
}

impl FcmovbEmitter<St> for Assembler<'_> {
    fn fcmovb(&mut self, op0: St) {
        self.emit_n(InstId::Fcmovb as u32, &[op0.as_operand()]); // FPU
    }
}

impl FcmovbeEmitter<St> for Assembler<'_> {
    fn fcmovbe(&mut self, op0: St) {
        self.emit_n(InstId::Fcmovbe as u32, &[op0.as_operand()]); // FPU
    }
}

impl FcmoveEmitter<St> for Assembler<'_> {
    fn fcmove(&mut self, op0: St) {
        self.emit_n(InstId::Fcmove as u32, &[op0.as_operand()]); // FPU
    }
}

impl FcmovnbEmitter<St> for Assembler<'_> {
    fn fcmovnb(&mut self, op0: St) {
        self.emit_n(InstId::Fcmovnb as u32, &[op0.as_operand()]); // FPU
    }
}

impl FcmovnbeEmitter<St> for Assembler<'_> {
    fn fcmovnbe(&mut self, op0: St) {
        self.emit_n(InstId::Fcmovnbe as u32, &[op0.as_operand()]); // FPU
    }
}

impl FcmovneEmitter<St> for Assembler<'_> {
    fn fcmovne(&mut self, op0: St) {
        self.emit_n(InstId::Fcmovne as u32, &[op0.as_operand()]); // FPU
    }
}

impl FcmovnuEmitter<St> for Assembler<'_> {
    fn fcmovnu(&mut self, op0: St) {
        self.emit_n(InstId::Fcmovnu as u32, &[op0.as_operand()]); // FPU
    }
}

impl FcmovuEmitter<St> for Assembler<'_> {
    fn fcmovu(&mut self, op0: St) {
        self.emit_n(InstId::Fcmovu as u32, &[op0.as_operand()]); // FPU
    }
}

impl FcomEmitter<St> for Assembler<'_> {
    fn fcom(&mut self, op0: St) {
        self.emit_n(InstId::Fcom as u32, &[op0.as_operand()]); // FPU
    }
}

impl FcomEmitter<Mem> for Assembler<'_> {
    fn fcom(&mut self, op0: Mem) {
        self.emit_n(InstId::Fcom as u32, &[op0.as_operand()]); // FPU
    }
}

impl Fcom0Emitter for Assembler<'_> {
    fn fcom_0(&mut self) {
        self.emit_n(InstId::Fcom as u32, &[]); // FPU
    }
}

impl FcompEmitter<St> for Assembler<'_> {
    fn fcomp(&mut self, op0: St) {
        self.emit_n(InstId::Fcomp as u32, &[op0.as_operand()]); // FPU
    }
}

impl FcompEmitter<Mem> for Assembler<'_> {
    fn fcomp(&mut self, op0: Mem) {
        self.emit_n(InstId::Fcomp as u32, &[op0.as_operand()]); // FPU
    }
}

impl Fcomp0Emitter for Assembler<'_> {
    fn fcomp_0(&mut self) {
        self.emit_n(InstId::Fcomp as u32, &[]); // FPU
    }
}

impl FcomppEmitter for Assembler<'_> {
    fn fcompp(&mut self) {
        self.emit_n(InstId::Fcompp as u32, &[]); // FPU
    }
}

impl FcomiEmitter<St> for Assembler<'_> {
    fn fcomi(&mut self, op0: St) {
        self.emit_n(InstId::Fcomi as u32, &[op0.as_operand()]); // FPU
    }
}

impl FcomipEmitter<St> for Assembler<'_> {
    fn fcomip(&mut self, op0: St) {
        self.emit_n(InstId::Fcomip as u32, &[op0.as_operand()]); // FPU
    }
}

impl FcosEmitter for Assembler<'_> {
    fn fcos(&mut self) {
        self.emit_n(InstId::Fcos as u32, &[]); // FPU
    }
}

impl FdecstpEmitter for Assembler<'_> {
    fn fdecstp(&mut self) {
        self.emit_n(InstId::Fdecstp as u32, &[]); // FPU
    }
}

impl FdivEmitter<St, St> for Assembler<'_> {
    fn fdiv(&mut self, op0: St, op1: St) {
        self.emit_n(InstId::Fdiv as u32, &[op0.as_operand(), op1.as_operand()]); // FPU
    }
}

impl Fdiv1Emitter<Mem> for Assembler<'_> {
    fn fdiv_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Fdiv as u32, &[op0.as_operand()]); // FPU
    }
}

impl FdivpEmitter<St> for Assembler<'_> {
    fn fdivp(&mut self, op0: St) {
        self.emit_n(InstId::Fdivp as u32, &[op0.as_operand()]); // FPU
    }
}

impl Fdivp0Emitter for Assembler<'_> {
    fn fdivp_0(&mut self) {
        self.emit_n(InstId::Fdivp as u32, &[]); // FPU
    }
}

impl FdivrEmitter<St, St> for Assembler<'_> {
    fn fdivr(&mut self, op0: St, op1: St) {
        self.emit_n(InstId::Fdivr as u32, &[op0.as_operand(), op1.as_operand()]); // FPU
    }
}

impl Fdivr1Emitter<Mem> for Assembler<'_> {
    fn fdivr_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Fdivr as u32, &[op0.as_operand()]); // FPU
    }
}

impl FdivrpEmitter<St> for Assembler<'_> {
    fn fdivrp(&mut self, op0: St) {
        self.emit_n(InstId::Fdivrp as u32, &[op0.as_operand()]); // FPU
    }
}

impl Fdivrp0Emitter for Assembler<'_> {
    fn fdivrp_0(&mut self) {
        self.emit_n(InstId::Fdivrp as u32, &[]); // FPU
    }
}

impl FfreeEmitter<St> for Assembler<'_> {
    fn ffree(&mut self, op0: St) {
        self.emit_n(InstId::Ffree as u32, &[op0.as_operand()]); // FPU
    }
}

impl FiaddEmitter<Mem> for Assembler<'_> {
    fn fiadd(&mut self, op0: Mem) {
        self.emit_n(InstId::Fiadd as u32, &[op0.as_operand()]); // FPU
    }
}

impl FicomEmitter<Mem> for Assembler<'_> {
    fn ficom(&mut self, op0: Mem) {
        self.emit_n(InstId::Ficom as u32, &[op0.as_operand()]); // FPU
    }
}

impl FicompEmitter<Mem> for Assembler<'_> {
    fn ficomp(&mut self, op0: Mem) {
        self.emit_n(InstId::Ficomp as u32, &[op0.as_operand()]); // FPU
    }
}

impl FidivEmitter<Mem> for Assembler<'_> {
    fn fidiv(&mut self, op0: Mem) {
        self.emit_n(InstId::Fidiv as u32, &[op0.as_operand()]); // FPU
    }
}

impl FidivrEmitter<Mem> for Assembler<'_> {
    fn fidivr(&mut self, op0: Mem) {
        self.emit_n(InstId::Fidivr as u32, &[op0.as_operand()]); // FPU
    }
}

impl FildEmitter<Mem> for Assembler<'_> {
    fn fild(&mut self, op0: Mem) {
        self.emit_n(InstId::Fild as u32, &[op0.as_operand()]); // FPU
    }
}

impl FimulEmitter<Mem> for Assembler<'_> {
    fn fimul(&mut self, op0: Mem) {
        self.emit_n(InstId::Fimul as u32, &[op0.as_operand()]); // FPU
    }
}

impl FincstpEmitter for Assembler<'_> {
    fn fincstp(&mut self) {
        self.emit_n(InstId::Fincstp as u32, &[]); // FPU
    }
}

impl FinitEmitter for Assembler<'_> {
    fn finit(&mut self) {
        self.emit_n(InstId::Finit as u32, &[]); // FPU
    }
}

impl FisubEmitter<Mem> for Assembler<'_> {
    fn fisub(&mut self, op0: Mem) {
        self.emit_n(InstId::Fisub as u32, &[op0.as_operand()]); // FPU
    }
}

impl FisubrEmitter<Mem> for Assembler<'_> {
    fn fisubr(&mut self, op0: Mem) {
        self.emit_n(InstId::Fisubr as u32, &[op0.as_operand()]); // FPU
    }
}

impl FninitEmitter for Assembler<'_> {
    fn fninit(&mut self) {
        self.emit_n(InstId::Fninit as u32, &[]); // FPU
    }
}

impl FistEmitter<Mem> for Assembler<'_> {
    fn fist(&mut self, op0: Mem) {
        self.emit_n(InstId::Fist as u32, &[op0.as_operand()]); // FPU
    }
}

impl FistpEmitter<Mem> for Assembler<'_> {
    fn fistp(&mut self, op0: Mem) {
        self.emit_n(InstId::Fistp as u32, &[op0.as_operand()]); // FPU
    }
}

impl FisttpEmitter<Mem> for Assembler<'_> {
    fn fisttp(&mut self, op0: Mem) {
        self.emit_n(InstId::Fisttp as u32, &[op0.as_operand()]); // FPU+SSE3
    }
}

impl FldEmitter<Mem> for Assembler<'_> {
    fn fld(&mut self, op0: Mem) {
        self.emit_n(InstId::Fld as u32, &[op0.as_operand()]); // FPU
    }
}

impl FldEmitter<St> for Assembler<'_> {
    fn fld(&mut self, op0: St) {
        self.emit_n(InstId::Fld as u32, &[op0.as_operand()]); // FPU
    }
}

impl Fld1Emitter for Assembler<'_> {
    fn fld1(&mut self) {
        self.emit_n(InstId::Fld1 as u32, &[]); // FPU
    }
}

impl Fldl2tEmitter for Assembler<'_> {
    fn fldl2t(&mut self) {
        self.emit_n(InstId::Fldl2t as u32, &[]); // FPU
    }
}

impl Fldl2eEmitter for Assembler<'_> {
    fn fldl2e(&mut self) {
        self.emit_n(InstId::Fldl2e as u32, &[]); // FPU
    }
}

impl FldpiEmitter for Assembler<'_> {
    fn fldpi(&mut self) {
        self.emit_n(InstId::Fldpi as u32, &[]); // FPU
    }
}

impl Fldlg2Emitter for Assembler<'_> {
    fn fldlg2(&mut self) {
        self.emit_n(InstId::Fldlg2 as u32, &[]); // FPU
    }
}

impl Fldln2Emitter for Assembler<'_> {
    fn fldln2(&mut self) {
        self.emit_n(InstId::Fldln2 as u32, &[]); // FPU
    }
}

impl FldzEmitter for Assembler<'_> {
    fn fldz(&mut self) {
        self.emit_n(InstId::Fldz as u32, &[]); // FPU
    }
}

impl FldcwEmitter<Mem> for Assembler<'_> {
    fn fldcw(&mut self, op0: Mem) {
        self.emit_n(InstId::Fldcw as u32, &[op0.as_operand()]); // FPU
    }
}

impl FldenvEmitter<Mem> for Assembler<'_> {
    fn fldenv(&mut self, op0: Mem) {
        self.emit_n(InstId::Fldenv as u32, &[op0.as_operand()]); // FPU
    }
}

impl FmulEmitter<St, St> for Assembler<'_> {
    fn fmul(&mut self, op0: St, op1: St) {
        self.emit_n(InstId::Fmul as u32, &[op0.as_operand(), op1.as_operand()]); // FPU
    }
}

impl Fmul1Emitter<Mem> for Assembler<'_> {
    fn fmul_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Fmul as u32, &[op0.as_operand()]); // FPU
    }
}

impl FmulpEmitter<St> for Assembler<'_> {
    fn fmulp(&mut self, op0: St) {
        self.emit_n(InstId::Fmulp as u32, &[op0.as_operand()]); // FPU
    }
}

impl Fmulp0Emitter for Assembler<'_> {
    fn fmulp_0(&mut self) {
        self.emit_n(InstId::Fmulp as u32, &[]); // FPU
    }
}

impl FnclexEmitter for Assembler<'_> {
    fn fnclex(&mut self) {
        self.emit_n(InstId::Fnclex as u32, &[]); // FPU
    }
}

impl FnopEmitter for Assembler<'_> {
    fn fnop(&mut self) {
        self.emit_n(InstId::Fnop as u32, &[]); // FPU
    }
}

impl FnsaveEmitter<Mem> for Assembler<'_> {
    fn fnsave(&mut self, op0: Mem) {
        self.emit_n(InstId::Fnsave as u32, &[op0.as_operand()]); // FPU
    }
}

impl FnstenvEmitter<Mem> for Assembler<'_> {
    fn fnstenv(&mut self, op0: Mem) {
        self.emit_n(InstId::Fnstenv as u32, &[op0.as_operand()]); // FPU
    }
}

impl FnstcwEmitter<Mem> for Assembler<'_> {
    fn fnstcw(&mut self, op0: Mem) {
        self.emit_n(InstId::Fnstcw as u32, &[op0.as_operand()]); // FPU
    }
}

impl FpatanEmitter for Assembler<'_> {
    fn fpatan(&mut self) {
        self.emit_n(InstId::Fpatan as u32, &[]); // FPU
    }
}

impl FpremEmitter for Assembler<'_> {
    fn fprem(&mut self) {
        self.emit_n(InstId::Fprem as u32, &[]); // FPU
    }
}

impl Fprem1Emitter for Assembler<'_> {
    fn fprem1(&mut self) {
        self.emit_n(InstId::Fprem1 as u32, &[]); // FPU
    }
}

impl FptanEmitter for Assembler<'_> {
    fn fptan(&mut self) {
        self.emit_n(InstId::Fptan as u32, &[]); // FPU
    }
}

impl FrndintEmitter for Assembler<'_> {
    fn frndint(&mut self) {
        self.emit_n(InstId::Frndint as u32, &[]); // FPU
    }
}

impl FrstorEmitter<Mem> for Assembler<'_> {
    fn frstor(&mut self, op0: Mem) {
        self.emit_n(InstId::Frstor as u32, &[op0.as_operand()]); // FPU
    }
}

impl FsaveEmitter<Mem> for Assembler<'_> {
    fn fsave(&mut self, op0: Mem) {
        self.emit_n(InstId::Fsave as u32, &[op0.as_operand()]); // FPU
    }
}

impl FscaleEmitter for Assembler<'_> {
    fn fscale(&mut self) {
        self.emit_n(InstId::Fscale as u32, &[]); // FPU
    }
}

impl FsinEmitter for Assembler<'_> {
    fn fsin(&mut self) {
        self.emit_n(InstId::Fsin as u32, &[]); // FPU
    }
}

impl FsincosEmitter for Assembler<'_> {
    fn fsincos(&mut self) {
        self.emit_n(InstId::Fsincos as u32, &[]); // FPU
    }
}

impl FsqrtEmitter for Assembler<'_> {
    fn fsqrt(&mut self) {
        self.emit_n(InstId::Fsqrt as u32, &[]); // FPU
    }
}

impl FstEmitter<Mem> for Assembler<'_> {
    fn fst(&mut self, op0: Mem) {
        self.emit_n(InstId::Fst as u32, &[op0.as_operand()]); // FPU
    }
}

impl FstEmitter<St> for Assembler<'_> {
    fn fst(&mut self, op0: St) {
        self.emit_n(InstId::Fst as u32, &[op0.as_operand()]); // FPU
    }
}

impl FstpEmitter<Mem> for Assembler<'_> {
    fn fstp(&mut self, op0: Mem) {
        self.emit_n(InstId::Fstp as u32, &[op0.as_operand()]); // FPU
    }
}

impl FstpEmitter<St> for Assembler<'_> {
    fn fstp(&mut self, op0: St) {
        self.emit_n(InstId::Fstp as u32, &[op0.as_operand()]); // FPU
    }
}

impl FstcwEmitter<Mem> for Assembler<'_> {
    fn fstcw(&mut self, op0: Mem) {
        self.emit_n(InstId::Fstcw as u32, &[op0.as_operand()]); // FPU
    }
}

impl FstenvEmitter<Mem> for Assembler<'_> {
    fn fstenv(&mut self, op0: Mem) {
        self.emit_n(InstId::Fstenv as u32, &[op0.as_operand()]); // FPU
    }
}

impl FsubEmitter<St, St> for Assembler<'_> {
    fn fsub(&mut self, op0: St, op1: St) {
        self.emit_n(InstId::Fsub as u32, &[op0.as_operand(), op1.as_operand()]); // FPU
    }
}

impl Fsub1Emitter<Mem> for Assembler<'_> {
    fn fsub_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Fsub as u32, &[op0.as_operand()]); // FPU
    }
}

impl FsubpEmitter<St> for Assembler<'_> {
    fn fsubp(&mut self, op0: St) {
        self.emit_n(InstId::Fsubp as u32, &[op0.as_operand()]); // FPU
    }
}

impl Fsubp0Emitter for Assembler<'_> {
    fn fsubp_0(&mut self) {
        self.emit_n(InstId::Fsubp as u32, &[]); // FPU
    }
}

impl FsubrEmitter<St, St> for Assembler<'_> {
    fn fsubr(&mut self, op0: St, op1: St) {
        self.emit_n(InstId::Fsubr as u32, &[op0.as_operand(), op1.as_operand()]); // FPU
    }
}

impl Fsubr1Emitter<Mem> for Assembler<'_> {
    fn fsubr_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Fsubr as u32, &[op0.as_operand()]); // FPU
    }
}

impl FsubrpEmitter<St> for Assembler<'_> {
    fn fsubrp(&mut self, op0: St) {
        self.emit_n(InstId::Fsubrp as u32, &[op0.as_operand()]); // FPU
    }
}

impl Fsubrp0Emitter for Assembler<'_> {
    fn fsubrp_0(&mut self) {
        self.emit_n(InstId::Fsubrp as u32, &[]); // FPU
    }
}

impl FtstEmitter for Assembler<'_> {
    fn ftst(&mut self) {
        self.emit_n(InstId::Ftst as u32, &[]); // FPU
    }
}

impl FucomEmitter<St> for Assembler<'_> {
    fn fucom(&mut self, op0: St) {
        self.emit_n(InstId::Fucom as u32, &[op0.as_operand()]); // FPU
    }
}

impl Fucom0Emitter for Assembler<'_> {
    fn fucom_0(&mut self) {
        self.emit_n(InstId::Fucom as u32, &[]); // FPU
    }
}

impl FucomiEmitter<St> for Assembler<'_> {
    fn fucomi(&mut self, op0: St) {
        self.emit_n(InstId::Fucomi as u32, &[op0.as_operand()]); // FPU
    }
}

impl FucomipEmitter<St> for Assembler<'_> {
    fn fucomip(&mut self, op0: St) {
        self.emit_n(InstId::Fucomip as u32, &[op0.as_operand()]); // FPU
    }
}

impl FucompEmitter<St> for Assembler<'_> {
    fn fucomp(&mut self, op0: St) {
        self.emit_n(InstId::Fucomp as u32, &[op0.as_operand()]); // FPU
    }
}

impl Fucomp0Emitter for Assembler<'_> {
    fn fucomp_0(&mut self) {
        self.emit_n(InstId::Fucomp as u32, &[]); // FPU
    }
}

impl FucomppEmitter for Assembler<'_> {
    fn fucompp(&mut self) {
        self.emit_n(InstId::Fucompp as u32, &[]); // FPU
    }
}

impl FwaitEmitter for Assembler<'_> {
    fn fwait(&mut self) {
        self.emit_n(InstId::Fwait as u32, &[]); // FPU
    }
}

impl FxamEmitter for Assembler<'_> {
    fn fxam(&mut self) {
        self.emit_n(InstId::Fxam as u32, &[]); // FPU
    }
}

impl FxchEmitter<St> for Assembler<'_> {
    fn fxch(&mut self, op0: St) {
        self.emit_n(InstId::Fxch as u32, &[op0.as_operand()]); // FPU
    }
}

impl FxtractEmitter for Assembler<'_> {
    fn fxtract(&mut self) {
        self.emit_n(InstId::Fxtract as u32, &[]); // FPU
    }
}

impl Fyl2xEmitter for Assembler<'_> {
    fn fyl2x(&mut self) {
        self.emit_n(InstId::Fyl2x as u32, &[]); // FPU
    }
}

impl Fyl2xp1Emitter for Assembler<'_> {
    fn fyl2xp1(&mut self) {
        self.emit_n(InstId::Fyl2xp1 as u32, &[]); // FPU
    }
}

impl FstswEmitter<Gp> for Assembler<'_> {
    fn fstsw(&mut self, op0: Gp) {
        self.emit_n(InstId::Fstsw as u32, &[op0.as_operand()]); // FPU
    }
}

impl FstswEmitter<Gpw> for Assembler<'_> {
    fn fstsw(&mut self, op0: Gpw) {
        self.emit_n(InstId::Fstsw as u32, &[op0.as_operand()]); // FPU
    }
}

impl FstswEmitter<Mem> for Assembler<'_> {
    fn fstsw(&mut self, op0: Mem) {
        self.emit_n(InstId::Fstsw as u32, &[op0.as_operand()]); // FPU
    }
}

impl FnstswEmitter<Gp> for Assembler<'_> {
    fn fnstsw(&mut self, op0: Gp) {
        self.emit_n(InstId::Fnstsw as u32, &[op0.as_operand()]); // FPU
    }
}

impl FnstswEmitter<Gpw> for Assembler<'_> {
    fn fnstsw(&mut self, op0: Gpw) {
        self.emit_n(InstId::Fnstsw as u32, &[op0.as_operand()]); // FPU
    }
}

impl FnstswEmitter<Mem> for Assembler<'_> {
    fn fnstsw(&mut self, op0: Mem) {
        self.emit_n(InstId::Fnstsw as u32, &[op0.as_operand()]); // FPU
    }
}

impl AddpdEmitter<Vec, Vec> for Assembler<'_> {
    fn addpd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Addpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl AddpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn addpd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Addpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl AddpdEmitter<Vec, Mem> for Assembler<'_> {
    fn addpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Addpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl AddpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn addpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Addpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl AddpsEmitter<Vec, Vec> for Assembler<'_> {
    fn addps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Addps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl AddpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn addps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Addps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl AddpsEmitter<Vec, Mem> for Assembler<'_> {
    fn addps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Addps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl AddpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn addps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Addps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl AddsdEmitter<Vec, Vec> for Assembler<'_> {
    fn addsd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Addsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl AddsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn addsd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Addsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl AddsdEmitter<Vec, Mem> for Assembler<'_> {
    fn addsd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Addsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl AddsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn addsd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Addsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl AddssEmitter<Vec, Vec> for Assembler<'_> {
    fn addss(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Addss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl AddssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn addss(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Addss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl AddssEmitter<Vec, Mem> for Assembler<'_> {
    fn addss(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Addss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl AddssEmitter<Xmm, Mem> for Assembler<'_> {
    fn addss(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Addss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl AddsubpdEmitter<Vec, Vec> for Assembler<'_> {
    fn addsubpd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Addsubpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE3
    }
}

impl AddsubpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn addsubpd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Addsubpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE3
    }
}

impl AddsubpdEmitter<Vec, Mem> for Assembler<'_> {
    fn addsubpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Addsubpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE3
    }
}

impl AddsubpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn addsubpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Addsubpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE3
    }
}

impl AddsubpsEmitter<Vec, Vec> for Assembler<'_> {
    fn addsubps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Addsubps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE3
    }
}

impl AddsubpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn addsubps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Addsubps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE3
    }
}

impl AddsubpsEmitter<Vec, Mem> for Assembler<'_> {
    fn addsubps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Addsubps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE3
    }
}

impl AddsubpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn addsubps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Addsubps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE3
    }
}

impl AndnpdEmitter<Vec, Vec> for Assembler<'_> {
    fn andnpd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Andnpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl AndnpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn andnpd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Andnpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl AndnpdEmitter<Vec, Mem> for Assembler<'_> {
    fn andnpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Andnpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl AndnpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn andnpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Andnpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl AndnpsEmitter<Vec, Vec> for Assembler<'_> {
    fn andnps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Andnps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl AndnpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn andnps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Andnps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl AndnpsEmitter<Vec, Mem> for Assembler<'_> {
    fn andnps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Andnps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl AndnpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn andnps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Andnps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl AndpdEmitter<Vec, Vec> for Assembler<'_> {
    fn andpd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Andpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl AndpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn andpd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Andpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl AndpdEmitter<Vec, Mem> for Assembler<'_> {
    fn andpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Andpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl AndpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn andpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Andpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl AndpsEmitter<Vec, Vec> for Assembler<'_> {
    fn andps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Andps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl AndpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn andps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Andps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl AndpsEmitter<Vec, Mem> for Assembler<'_> {
    fn andps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Andps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl AndpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn andps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Andps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl BlendpdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn blendpd(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Blendpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> BlendpdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn blendpd(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Blendpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl BlendpdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn blendpd(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Blendpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> BlendpdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn blendpd(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Blendpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl BlendpsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn blendps(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Blendps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> BlendpsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn blendps(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Blendps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl BlendpsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn blendps(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Blendps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> BlendpsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn blendps(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Blendps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl BlendvpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn blendvpd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Blendvpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl BlendvpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn blendvpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Blendvpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl BlendvpdEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn blendvpd(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Blendvpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl BlendvpdEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn blendvpd(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Blendvpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl BlendvpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn blendvps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Blendvps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl BlendvpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn blendvps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Blendvps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl BlendvpsEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn blendvps(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Blendvps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl BlendvpsEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn blendvps(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Blendvps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl CmppdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn cmppd(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Cmppd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> CmppdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn cmppd(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Cmppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE2
    }
}

impl CmppdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn cmppd(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Cmppd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> CmppdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn cmppd(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Cmppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE2
    }
}

impl CmppsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn cmpps(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Cmpps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE
    }
}

impl<U2: Into<Imm>> CmppsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn cmpps(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Cmpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE
    }
}

impl CmppsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn cmpps(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Cmpps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE
    }
}

impl<U2: Into<Imm>> CmppsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn cmpps(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Cmpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE
    }
}

impl CmpsdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn cmpsd(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Cmpsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> CmpsdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn cmpsd(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Cmpsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE2
    }
}

impl CmpsdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn cmpsd(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Cmpsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> CmpsdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn cmpsd(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Cmpsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE2
    }
}

impl CmpssEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn cmpss(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Cmpss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE
    }
}

impl<U2: Into<Imm>> CmpssEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn cmpss(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Cmpss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE
    }
}

impl CmpssEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn cmpss(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Cmpss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE
    }
}

impl<U2: Into<Imm>> CmpssEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn cmpss(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Cmpss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE
    }
}

impl ComisdEmitter<Vec, Vec> for Assembler<'_> {
    fn comisd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Comisd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl ComisdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn comisd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Comisd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl ComisdEmitter<Vec, Mem> for Assembler<'_> {
    fn comisd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Comisd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl ComisdEmitter<Xmm, Mem> for Assembler<'_> {
    fn comisd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Comisd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl ComissEmitter<Vec, Vec> for Assembler<'_> {
    fn comiss(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Comiss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl ComissEmitter<Xmm, Xmm> for Assembler<'_> {
    fn comiss(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Comiss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl ComissEmitter<Vec, Mem> for Assembler<'_> {
    fn comiss(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Comiss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl ComissEmitter<Xmm, Mem> for Assembler<'_> {
    fn comiss(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Comiss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl Cvtdq2pdEmitter<Vec, Vec> for Assembler<'_> {
    fn cvtdq2pd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Cvtdq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtdq2pdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn cvtdq2pd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Cvtdq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtdq2pdEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtdq2pd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Cvtdq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtdq2pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtdq2pd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Cvtdq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtdq2psEmitter<Vec, Vec> for Assembler<'_> {
    fn cvtdq2ps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Cvtdq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtdq2psEmitter<Xmm, Xmm> for Assembler<'_> {
    fn cvtdq2ps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Cvtdq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtdq2psEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtdq2ps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Cvtdq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtdq2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtdq2ps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Cvtdq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2dqEmitter<Vec, Vec> for Assembler<'_> {
    fn cvtpd2dq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Cvtpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2dqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn cvtpd2dq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Cvtpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2dqEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtpd2dq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Cvtpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2dqEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtpd2dq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Cvtpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2piEmitter<Mm, Vec> for Assembler<'_> {
    fn cvtpd2pi(&mut self, op0: Mm, op1: Vec) {
        self.emit_n(
            InstId::Cvtpd2pi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2piEmitter<Mm, Xmm> for Assembler<'_> {
    fn cvtpd2pi(&mut self, op0: Mm, op1: Xmm) {
        self.emit_n(
            InstId::Cvtpd2pi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2piEmitter<Mm, Mem> for Assembler<'_> {
    fn cvtpd2pi(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Cvtpd2pi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2psEmitter<Vec, Vec> for Assembler<'_> {
    fn cvtpd2ps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Cvtpd2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2psEmitter<Xmm, Xmm> for Assembler<'_> {
    fn cvtpd2ps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Cvtpd2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2psEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtpd2ps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Cvtpd2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtpd2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtpd2ps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Cvtpd2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtpi2pdEmitter<Vec, Mm> for Assembler<'_> {
    fn cvtpi2pd(&mut self, op0: Vec, op1: Mm) {
        self.emit_n(
            InstId::Cvtpi2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtpi2pdEmitter<Xmm, Mm> for Assembler<'_> {
    fn cvtpi2pd(&mut self, op0: Xmm, op1: Mm) {
        self.emit_n(
            InstId::Cvtpi2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtpi2pdEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtpi2pd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Cvtpi2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtpi2pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtpi2pd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Cvtpi2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtpi2psEmitter<Vec, Mm> for Assembler<'_> {
    fn cvtpi2ps(&mut self, op0: Vec, op1: Mm) {
        self.emit_n(
            InstId::Cvtpi2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvtpi2psEmitter<Xmm, Mm> for Assembler<'_> {
    fn cvtpi2ps(&mut self, op0: Xmm, op1: Mm) {
        self.emit_n(
            InstId::Cvtpi2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvtpi2psEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtpi2ps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Cvtpi2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvtpi2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtpi2ps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Cvtpi2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvtps2dqEmitter<Vec, Vec> for Assembler<'_> {
    fn cvtps2dq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Cvtps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtps2dqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn cvtps2dq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Cvtps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtps2dqEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtps2dq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Cvtps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtps2dqEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtps2dq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Cvtps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtps2pdEmitter<Vec, Vec> for Assembler<'_> {
    fn cvtps2pd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Cvtps2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtps2pdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn cvtps2pd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Cvtps2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtps2pdEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtps2pd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Cvtps2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtps2pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtps2pd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Cvtps2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtps2piEmitter<Mm, Vec> for Assembler<'_> {
    fn cvtps2pi(&mut self, op0: Mm, op1: Vec) {
        self.emit_n(
            InstId::Cvtps2pi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvtps2piEmitter<Mm, Xmm> for Assembler<'_> {
    fn cvtps2pi(&mut self, op0: Mm, op1: Xmm) {
        self.emit_n(
            InstId::Cvtps2pi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvtps2piEmitter<Mm, Mem> for Assembler<'_> {
    fn cvtps2pi(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Cvtps2pi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvtsd2siEmitter<Gp, Vec> for Assembler<'_> {
    fn cvtsd2si(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Cvtsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtsd2siEmitter<Gpd, Xmm> for Assembler<'_> {
    fn cvtsd2si(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Cvtsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtsd2siEmitter<Gpq, Xmm> for Assembler<'_> {
    fn cvtsd2si(&mut self, op0: Gpq, op1: Xmm) {
        self.emit_n(
            InstId::Cvtsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtsd2siEmitter<Gp, Mem> for Assembler<'_> {
    fn cvtsd2si(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Cvtsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtsd2siEmitter<Gpd, Mem> for Assembler<'_> {
    fn cvtsd2si(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Cvtsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtsd2siEmitter<Gpq, Mem> for Assembler<'_> {
    fn cvtsd2si(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Cvtsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtsd2ssEmitter<Vec, Vec> for Assembler<'_> {
    fn cvtsd2ss(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Cvtsd2ss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtsd2ssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn cvtsd2ss(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Cvtsd2ss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtsd2ssEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtsd2ss(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Cvtsd2ss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtsd2ssEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtsd2ss(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Cvtsd2ss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtsi2sdEmitter<Vec, Gp> for Assembler<'_> {
    fn cvtsi2sd(&mut self, op0: Vec, op1: Gp) {
        self.emit_n(
            InstId::Cvtsi2sd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtsi2sdEmitter<Xmm, Gpd> for Assembler<'_> {
    fn cvtsi2sd(&mut self, op0: Xmm, op1: Gpd) {
        self.emit_n(
            InstId::Cvtsi2sd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtsi2sdEmitter<Xmm, Gpq> for Assembler<'_> {
    fn cvtsi2sd(&mut self, op0: Xmm, op1: Gpq) {
        self.emit_n(
            InstId::Cvtsi2sd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtsi2sdEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtsi2sd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Cvtsi2sd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtsi2sdEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtsi2sd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Cvtsi2sd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtsi2ssEmitter<Vec, Gp> for Assembler<'_> {
    fn cvtsi2ss(&mut self, op0: Vec, op1: Gp) {
        self.emit_n(
            InstId::Cvtsi2ss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvtsi2ssEmitter<Xmm, Gpd> for Assembler<'_> {
    fn cvtsi2ss(&mut self, op0: Xmm, op1: Gpd) {
        self.emit_n(
            InstId::Cvtsi2ss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvtsi2ssEmitter<Xmm, Gpq> for Assembler<'_> {
    fn cvtsi2ss(&mut self, op0: Xmm, op1: Gpq) {
        self.emit_n(
            InstId::Cvtsi2ss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvtsi2ssEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtsi2ss(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Cvtsi2ss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvtsi2ssEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtsi2ss(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Cvtsi2ss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvtss2sdEmitter<Vec, Vec> for Assembler<'_> {
    fn cvtss2sd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Cvtss2sd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtss2sdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn cvtss2sd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Cvtss2sd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtss2sdEmitter<Vec, Mem> for Assembler<'_> {
    fn cvtss2sd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Cvtss2sd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtss2sdEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvtss2sd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Cvtss2sd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvtss2siEmitter<Gp, Vec> for Assembler<'_> {
    fn cvtss2si(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Cvtss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvtss2siEmitter<Gpd, Xmm> for Assembler<'_> {
    fn cvtss2si(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Cvtss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvtss2siEmitter<Gpq, Xmm> for Assembler<'_> {
    fn cvtss2si(&mut self, op0: Gpq, op1: Xmm) {
        self.emit_n(
            InstId::Cvtss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvtss2siEmitter<Gp, Mem> for Assembler<'_> {
    fn cvtss2si(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Cvtss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvtss2siEmitter<Gpd, Mem> for Assembler<'_> {
    fn cvtss2si(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Cvtss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvtss2siEmitter<Gpq, Mem> for Assembler<'_> {
    fn cvtss2si(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Cvtss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvttpd2piEmitter<Mm, Vec> for Assembler<'_> {
    fn cvttpd2pi(&mut self, op0: Mm, op1: Vec) {
        self.emit_n(
            InstId::Cvttpd2pi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvttpd2piEmitter<Mm, Xmm> for Assembler<'_> {
    fn cvttpd2pi(&mut self, op0: Mm, op1: Xmm) {
        self.emit_n(
            InstId::Cvttpd2pi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvttpd2piEmitter<Mm, Mem> for Assembler<'_> {
    fn cvttpd2pi(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Cvttpd2pi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvttpd2dqEmitter<Vec, Vec> for Assembler<'_> {
    fn cvttpd2dq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Cvttpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvttpd2dqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn cvttpd2dq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Cvttpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvttpd2dqEmitter<Vec, Mem> for Assembler<'_> {
    fn cvttpd2dq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Cvttpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvttpd2dqEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvttpd2dq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Cvttpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvttps2dqEmitter<Vec, Vec> for Assembler<'_> {
    fn cvttps2dq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Cvttps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvttps2dqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn cvttps2dq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Cvttps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvttps2dqEmitter<Vec, Mem> for Assembler<'_> {
    fn cvttps2dq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Cvttps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvttps2dqEmitter<Xmm, Mem> for Assembler<'_> {
    fn cvttps2dq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Cvttps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvttps2piEmitter<Mm, Vec> for Assembler<'_> {
    fn cvttps2pi(&mut self, op0: Mm, op1: Vec) {
        self.emit_n(
            InstId::Cvttps2pi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvttps2piEmitter<Mm, Xmm> for Assembler<'_> {
    fn cvttps2pi(&mut self, op0: Mm, op1: Xmm) {
        self.emit_n(
            InstId::Cvttps2pi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvttps2piEmitter<Mm, Mem> for Assembler<'_> {
    fn cvttps2pi(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Cvttps2pi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvttsd2siEmitter<Gp, Vec> for Assembler<'_> {
    fn cvttsd2si(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Cvttsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvttsd2siEmitter<Gpd, Xmm> for Assembler<'_> {
    fn cvttsd2si(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Cvttsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvttsd2siEmitter<Gpq, Xmm> for Assembler<'_> {
    fn cvttsd2si(&mut self, op0: Gpq, op1: Xmm) {
        self.emit_n(
            InstId::Cvttsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvttsd2siEmitter<Gp, Mem> for Assembler<'_> {
    fn cvttsd2si(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Cvttsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvttsd2siEmitter<Gpd, Mem> for Assembler<'_> {
    fn cvttsd2si(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Cvttsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvttsd2siEmitter<Gpq, Mem> for Assembler<'_> {
    fn cvttsd2si(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Cvttsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Cvttss2siEmitter<Gp, Vec> for Assembler<'_> {
    fn cvttss2si(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Cvttss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvttss2siEmitter<Gpd, Xmm> for Assembler<'_> {
    fn cvttss2si(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Cvttss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvttss2siEmitter<Gpq, Xmm> for Assembler<'_> {
    fn cvttss2si(&mut self, op0: Gpq, op1: Xmm) {
        self.emit_n(
            InstId::Cvttss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvttss2siEmitter<Gp, Mem> for Assembler<'_> {
    fn cvttss2si(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Cvttss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvttss2siEmitter<Gpd, Mem> for Assembler<'_> {
    fn cvttss2si(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Cvttss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl Cvttss2siEmitter<Gpq, Mem> for Assembler<'_> {
    fn cvttss2si(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Cvttss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl DivpdEmitter<Vec, Vec> for Assembler<'_> {
    fn divpd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Divpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl DivpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn divpd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Divpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl DivpdEmitter<Vec, Mem> for Assembler<'_> {
    fn divpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Divpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl DivpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn divpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Divpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl DivpsEmitter<Vec, Vec> for Assembler<'_> {
    fn divps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Divps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl DivpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn divps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Divps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl DivpsEmitter<Vec, Mem> for Assembler<'_> {
    fn divps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Divps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl DivpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn divps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Divps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl DivsdEmitter<Vec, Vec> for Assembler<'_> {
    fn divsd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Divsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl DivsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn divsd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Divsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl DivsdEmitter<Vec, Mem> for Assembler<'_> {
    fn divsd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Divsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl DivsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn divsd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Divsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl DivssEmitter<Vec, Vec> for Assembler<'_> {
    fn divss(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Divss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl DivssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn divss(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Divss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl DivssEmitter<Vec, Mem> for Assembler<'_> {
    fn divss(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Divss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl DivssEmitter<Xmm, Mem> for Assembler<'_> {
    fn divss(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Divss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl DppdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn dppd(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Dppd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> DppdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn dppd(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Dppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl DppdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn dppd(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Dppd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> DppdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn dppd(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Dppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl DppsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn dpps(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Dpps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> DppsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn dpps(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Dpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl DppsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn dpps(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Dpps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> DppsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn dpps(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Dpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl ExtractpsEmitter<Gp, Vec, Imm> for Assembler<'_> {
    fn extractps(&mut self, op0: Gp, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Extractps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> ExtractpsEmitter<Gpd, Xmm, U2> for Assembler<'_> {
    fn extractps(&mut self, op0: Gpd, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Extractps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl ExtractpsEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn extractps(&mut self, op0: Mem, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Extractps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> ExtractpsEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn extractps(&mut self, op0: Mem, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Extractps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl ExtrqEmitter<Vec, Vec> for Assembler<'_> {
    fn extrq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Extrq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4A
    }
}

impl ExtrqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn extrq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Extrq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4A
    }
}

impl Extrq3Emitter<Vec, Imm, Imm> for Assembler<'_> {
    fn extrq_3(&mut self, op0: Vec, op1: Imm, op2: Imm) {
        self.emit_n(
            InstId::Extrq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4A
    }
}

impl<U1: Into<Imm>, U2: Into<Imm>> Extrq3Emitter<Xmm, U1, U2> for Assembler<'_> {
    fn extrq_3(&mut self, op0: Xmm, op1: U1, op2: U2) {
        self.emit_n(
            InstId::Extrq as u32,
            &[
                op0.as_operand(),
                Into::<Imm>::into(op1).as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4A
    }
}

impl HaddpdEmitter<Vec, Vec> for Assembler<'_> {
    fn haddpd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Haddpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE3
    }
}

impl HaddpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn haddpd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Haddpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE3
    }
}

impl HaddpdEmitter<Vec, Mem> for Assembler<'_> {
    fn haddpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Haddpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE3
    }
}

impl HaddpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn haddpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Haddpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE3
    }
}

impl HaddpsEmitter<Vec, Vec> for Assembler<'_> {
    fn haddps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Haddps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE3
    }
}

impl HaddpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn haddps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Haddps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE3
    }
}

impl HaddpsEmitter<Vec, Mem> for Assembler<'_> {
    fn haddps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Haddps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE3
    }
}

impl HaddpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn haddps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Haddps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE3
    }
}

impl HsubpdEmitter<Vec, Vec> for Assembler<'_> {
    fn hsubpd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Hsubpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE3
    }
}

impl HsubpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn hsubpd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Hsubpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE3
    }
}

impl HsubpdEmitter<Vec, Mem> for Assembler<'_> {
    fn hsubpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Hsubpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE3
    }
}

impl HsubpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn hsubpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Hsubpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE3
    }
}

impl HsubpsEmitter<Vec, Vec> for Assembler<'_> {
    fn hsubps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Hsubps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE3
    }
}

impl HsubpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn hsubps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Hsubps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE3
    }
}

impl HsubpsEmitter<Vec, Mem> for Assembler<'_> {
    fn hsubps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Hsubps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE3
    }
}

impl HsubpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn hsubps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Hsubps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE3
    }
}

impl InsertpsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn insertps(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Insertps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> InsertpsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn insertps(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Insertps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl InsertpsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn insertps(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Insertps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> InsertpsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn insertps(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Insertps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl InsertqEmitter<Vec, Vec> for Assembler<'_> {
    fn insertq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Insertq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4A
    }
}

impl InsertqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn insertq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Insertq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4A
    }
}

impl Insertq4Emitter<Vec, Vec, Imm, Imm> for Assembler<'_> {
    fn insertq_4(&mut self, op0: Vec, op1: Vec, op2: Imm, op3: Imm) {
        self.emit_n(
            InstId::Insertq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // SSE4A
    }
}

impl<U2: Into<Imm>, U3: Into<Imm>> Insertq4Emitter<Xmm, Xmm, U2, U3> for Assembler<'_> {
    fn insertq_4(&mut self, op0: Xmm, op1: Xmm, op2: U2, op3: U3) {
        self.emit_n(
            InstId::Insertq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // SSE4A
    }
}

impl LddquEmitter<Vec, Mem> for Assembler<'_> {
    fn lddqu(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Lddqu as u32, &[op0.as_operand(), op1.as_operand()]); // SSE3
    }
}

impl LddquEmitter<Xmm, Mem> for Assembler<'_> {
    fn lddqu(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Lddqu as u32, &[op0.as_operand(), op1.as_operand()]); // SSE3
    }
}

impl MaskmovqEmitter<Mm, Mm, Mem> for Assembler<'_> {
    fn maskmovq(&mut self, op0: Mm, op1: Mm, op2: Mem) {
        self.emit_n(
            InstId::Maskmovq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE [EXPLICIT]
    }
}

impl MaskmovdquEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn maskmovdqu(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Maskmovdqu as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE2 [EXPLICIT]
    }
}

impl MaskmovdquEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn maskmovdqu(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Maskmovdqu as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE2 [EXPLICIT]
    }
}

impl MaxpdEmitter<Vec, Vec> for Assembler<'_> {
    fn maxpd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Maxpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MaxpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn maxpd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Maxpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MaxpdEmitter<Vec, Mem> for Assembler<'_> {
    fn maxpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Maxpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MaxpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn maxpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Maxpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MaxpsEmitter<Vec, Vec> for Assembler<'_> {
    fn maxps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Maxps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MaxpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn maxps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Maxps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MaxpsEmitter<Vec, Mem> for Assembler<'_> {
    fn maxps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Maxps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MaxpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn maxps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Maxps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MaxsdEmitter<Vec, Vec> for Assembler<'_> {
    fn maxsd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Maxsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MaxsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn maxsd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Maxsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MaxsdEmitter<Vec, Mem> for Assembler<'_> {
    fn maxsd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Maxsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MaxsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn maxsd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Maxsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MaxssEmitter<Vec, Vec> for Assembler<'_> {
    fn maxss(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Maxss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MaxssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn maxss(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Maxss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MaxssEmitter<Vec, Mem> for Assembler<'_> {
    fn maxss(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Maxss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MaxssEmitter<Xmm, Mem> for Assembler<'_> {
    fn maxss(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Maxss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MinpdEmitter<Vec, Vec> for Assembler<'_> {
    fn minpd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Minpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MinpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn minpd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Minpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MinpdEmitter<Vec, Mem> for Assembler<'_> {
    fn minpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Minpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MinpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn minpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Minpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MinpsEmitter<Vec, Vec> for Assembler<'_> {
    fn minps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Minps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MinpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn minps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Minps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MinpsEmitter<Vec, Mem> for Assembler<'_> {
    fn minps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Minps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MinpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn minps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Minps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MinsdEmitter<Vec, Vec> for Assembler<'_> {
    fn minsd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Minsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MinsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn minsd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Minsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MinsdEmitter<Vec, Mem> for Assembler<'_> {
    fn minsd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Minsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MinsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn minsd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Minsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MinssEmitter<Vec, Vec> for Assembler<'_> {
    fn minss(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Minss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MinssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn minss(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Minss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MinssEmitter<Vec, Mem> for Assembler<'_> {
    fn minss(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Minss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MinssEmitter<Xmm, Mem> for Assembler<'_> {
    fn minss(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Minss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovapdEmitter<Vec, Vec> for Assembler<'_> {
    fn movapd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Movapd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovapdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movapd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Movapd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovapdEmitter<Vec, Mem> for Assembler<'_> {
    fn movapd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Movapd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovapdEmitter<Xmm, Mem> for Assembler<'_> {
    fn movapd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Movapd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovapdEmitter<Mem, Vec> for Assembler<'_> {
    fn movapd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(InstId::Movapd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovapdEmitter<Mem, Xmm> for Assembler<'_> {
    fn movapd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(InstId::Movapd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovapsEmitter<Vec, Vec> for Assembler<'_> {
    fn movaps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Movaps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovapsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movaps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Movaps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovapsEmitter<Vec, Mem> for Assembler<'_> {
    fn movaps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Movaps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovapsEmitter<Xmm, Mem> for Assembler<'_> {
    fn movaps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Movaps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovapsEmitter<Mem, Vec> for Assembler<'_> {
    fn movaps(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(InstId::Movaps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovapsEmitter<Mem, Xmm> for Assembler<'_> {
    fn movaps(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(InstId::Movaps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovdEmitter<Mem, Mm> for Assembler<'_> {
    fn movd(&mut self, op0: Mem, op1: Mm) {
        self.emit_n(InstId::Movd as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl MovdEmitter<Mem, Vec> for Assembler<'_> {
    fn movd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(InstId::Movd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovdEmitter<Mem, Xmm> for Assembler<'_> {
    fn movd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(InstId::Movd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovdEmitter<Gp, Mm> for Assembler<'_> {
    fn movd(&mut self, op0: Gp, op1: Mm) {
        self.emit_n(InstId::Movd as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl MovdEmitter<Gpd, Mm> for Assembler<'_> {
    fn movd(&mut self, op0: Gpd, op1: Mm) {
        self.emit_n(InstId::Movd as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl MovdEmitter<Gp, Vec> for Assembler<'_> {
    fn movd(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(InstId::Movd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovdEmitter<Gpd, Xmm> for Assembler<'_> {
    fn movd(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(InstId::Movd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovdEmitter<Mm, Mem> for Assembler<'_> {
    fn movd(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Movd as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl MovdEmitter<Vec, Mem> for Assembler<'_> {
    fn movd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Movd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovdEmitter<Xmm, Mem> for Assembler<'_> {
    fn movd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Movd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovdEmitter<Mm, Gp> for Assembler<'_> {
    fn movd(&mut self, op0: Mm, op1: Gp) {
        self.emit_n(InstId::Movd as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl MovdEmitter<Mm, Gpd> for Assembler<'_> {
    fn movd(&mut self, op0: Mm, op1: Gpd) {
        self.emit_n(InstId::Movd as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl MovdEmitter<Vec, Gp> for Assembler<'_> {
    fn movd(&mut self, op0: Vec, op1: Gp) {
        self.emit_n(InstId::Movd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovdEmitter<Xmm, Gpd> for Assembler<'_> {
    fn movd(&mut self, op0: Xmm, op1: Gpd) {
        self.emit_n(InstId::Movd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovddupEmitter<Vec, Vec> for Assembler<'_> {
    fn movddup(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Movddup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE3
    }
}

impl MovddupEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movddup(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Movddup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE3
    }
}

impl MovddupEmitter<Vec, Mem> for Assembler<'_> {
    fn movddup(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Movddup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE3
    }
}

impl MovddupEmitter<Xmm, Mem> for Assembler<'_> {
    fn movddup(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Movddup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE3
    }
}

impl Movdq2qEmitter<Mm, Vec> for Assembler<'_> {
    fn movdq2q(&mut self, op0: Mm, op1: Vec) {
        self.emit_n(
            InstId::Movdq2q as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Movdq2qEmitter<Mm, Xmm> for Assembler<'_> {
    fn movdq2q(&mut self, op0: Mm, op1: Xmm) {
        self.emit_n(
            InstId::Movdq2q as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl MovdqaEmitter<Vec, Vec> for Assembler<'_> {
    fn movdqa(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Movdqa as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovdqaEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movdqa(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Movdqa as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovdqaEmitter<Vec, Mem> for Assembler<'_> {
    fn movdqa(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Movdqa as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovdqaEmitter<Xmm, Mem> for Assembler<'_> {
    fn movdqa(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Movdqa as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovdqaEmitter<Mem, Vec> for Assembler<'_> {
    fn movdqa(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(InstId::Movdqa as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovdqaEmitter<Mem, Xmm> for Assembler<'_> {
    fn movdqa(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(InstId::Movdqa as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovdquEmitter<Vec, Vec> for Assembler<'_> {
    fn movdqu(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Movdqu as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovdquEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movdqu(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Movdqu as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovdquEmitter<Vec, Mem> for Assembler<'_> {
    fn movdqu(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Movdqu as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovdquEmitter<Xmm, Mem> for Assembler<'_> {
    fn movdqu(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Movdqu as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovdquEmitter<Mem, Vec> for Assembler<'_> {
    fn movdqu(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(InstId::Movdqu as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovdquEmitter<Mem, Xmm> for Assembler<'_> {
    fn movdqu(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(InstId::Movdqu as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovhlpsEmitter<Vec, Vec> for Assembler<'_> {
    fn movhlps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Movhlps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl MovhlpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movhlps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Movhlps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl MovhpdEmitter<Vec, Mem> for Assembler<'_> {
    fn movhpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Movhpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovhpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn movhpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Movhpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovhpdEmitter<Mem, Vec> for Assembler<'_> {
    fn movhpd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(InstId::Movhpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovhpdEmitter<Mem, Xmm> for Assembler<'_> {
    fn movhpd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(InstId::Movhpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovhpsEmitter<Vec, Mem> for Assembler<'_> {
    fn movhps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Movhps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovhpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn movhps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Movhps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovhpsEmitter<Mem, Vec> for Assembler<'_> {
    fn movhps(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(InstId::Movhps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovhpsEmitter<Mem, Xmm> for Assembler<'_> {
    fn movhps(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(InstId::Movhps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovlhpsEmitter<Vec, Vec> for Assembler<'_> {
    fn movlhps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Movlhps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl MovlhpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movlhps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Movlhps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl MovlpdEmitter<Vec, Mem> for Assembler<'_> {
    fn movlpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Movlpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovlpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn movlpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Movlpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovlpdEmitter<Mem, Vec> for Assembler<'_> {
    fn movlpd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(InstId::Movlpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovlpdEmitter<Mem, Xmm> for Assembler<'_> {
    fn movlpd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(InstId::Movlpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovlpsEmitter<Vec, Mem> for Assembler<'_> {
    fn movlps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Movlps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovlpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn movlps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Movlps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovlpsEmitter<Mem, Vec> for Assembler<'_> {
    fn movlps(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(InstId::Movlps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovlpsEmitter<Mem, Xmm> for Assembler<'_> {
    fn movlps(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(InstId::Movlps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovmskpsEmitter<Gp, Vec> for Assembler<'_> {
    fn movmskps(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Movmskps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl MovmskpsEmitter<Gpd, Xmm> for Assembler<'_> {
    fn movmskps(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Movmskps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl MovmskpdEmitter<Gp, Vec> for Assembler<'_> {
    fn movmskpd(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Movmskpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl MovmskpdEmitter<Gpd, Xmm> for Assembler<'_> {
    fn movmskpd(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Movmskpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl MovntdqEmitter<Mem, Vec> for Assembler<'_> {
    fn movntdq(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Movntdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl MovntdqEmitter<Mem, Xmm> for Assembler<'_> {
    fn movntdq(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Movntdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl MovntdqaEmitter<Vec, Mem> for Assembler<'_> {
    fn movntdqa(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Movntdqa as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl MovntdqaEmitter<Xmm, Mem> for Assembler<'_> {
    fn movntdqa(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Movntdqa as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl MovntpdEmitter<Mem, Vec> for Assembler<'_> {
    fn movntpd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Movntpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl MovntpdEmitter<Mem, Xmm> for Assembler<'_> {
    fn movntpd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Movntpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl MovntpsEmitter<Mem, Vec> for Assembler<'_> {
    fn movntps(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Movntps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl MovntpsEmitter<Mem, Xmm> for Assembler<'_> {
    fn movntps(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Movntps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl MovntsdEmitter<Mem, Vec> for Assembler<'_> {
    fn movntsd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Movntsd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4A
    }
}

impl MovntsdEmitter<Mem, Xmm> for Assembler<'_> {
    fn movntsd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Movntsd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4A
    }
}

impl MovntssEmitter<Mem, Vec> for Assembler<'_> {
    fn movntss(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Movntss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4A
    }
}

impl MovntssEmitter<Mem, Xmm> for Assembler<'_> {
    fn movntss(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Movntss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4A
    }
}

impl MovntqEmitter<Mem, Mm> for Assembler<'_> {
    fn movntq(&mut self, op0: Mem, op1: Mm) {
        self.emit_n(InstId::Movntq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovqEmitter<Mm, Mm> for Assembler<'_> {
    fn movq(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Movq as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl MovqEmitter<Vec, Vec> for Assembler<'_> {
    fn movq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Movq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Movq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovqEmitter<Mem, Mm> for Assembler<'_> {
    fn movq(&mut self, op0: Mem, op1: Mm) {
        self.emit_n(InstId::Movq as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl MovqEmitter<Mem, Vec> for Assembler<'_> {
    fn movq(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(InstId::Movq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovqEmitter<Mem, Xmm> for Assembler<'_> {
    fn movq(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(InstId::Movq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovqEmitter<Mm, Mem> for Assembler<'_> {
    fn movq(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Movq as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl MovqEmitter<Vec, Mem> for Assembler<'_> {
    fn movq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Movq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovqEmitter<Xmm, Mem> for Assembler<'_> {
    fn movq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Movq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovqEmitter<Gp, Mm> for Assembler<'_> {
    fn movq(&mut self, op0: Gp, op1: Mm) {
        self.emit_n(InstId::Movq as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl MovqEmitter<Gpq, Mm> for Assembler<'_> {
    fn movq(&mut self, op0: Gpq, op1: Mm) {
        self.emit_n(InstId::Movq as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl MovqEmitter<Gp, Vec> for Assembler<'_> {
    fn movq(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(InstId::Movq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE+X64.
    }
}

impl MovqEmitter<Gpq, Xmm> for Assembler<'_> {
    fn movq(&mut self, op0: Gpq, op1: Xmm) {
        self.emit_n(InstId::Movq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE+X64.
    }
}

impl MovqEmitter<Mm, Gp> for Assembler<'_> {
    fn movq(&mut self, op0: Mm, op1: Gp) {
        self.emit_n(InstId::Movq as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl MovqEmitter<Mm, Gpq> for Assembler<'_> {
    fn movq(&mut self, op0: Mm, op1: Gpq) {
        self.emit_n(InstId::Movq as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl MovqEmitter<Vec, Gp> for Assembler<'_> {
    fn movq(&mut self, op0: Vec, op1: Gp) {
        self.emit_n(InstId::Movq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE+X64.
    }
}

impl MovqEmitter<Xmm, Gpq> for Assembler<'_> {
    fn movq(&mut self, op0: Xmm, op1: Gpq) {
        self.emit_n(InstId::Movq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE+X64.
    }
}

impl Movq2dqEmitter<Vec, Mm> for Assembler<'_> {
    fn movq2dq(&mut self, op0: Vec, op1: Mm) {
        self.emit_n(
            InstId::Movq2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl Movq2dqEmitter<Xmm, Mm> for Assembler<'_> {
    fn movq2dq(&mut self, op0: Xmm, op1: Mm) {
        self.emit_n(
            InstId::Movq2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl MovsdEmitter<Vec, Vec> for Assembler<'_> {
    fn movsd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Movsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movsd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Movsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovsdEmitter<Vec, Mem> for Assembler<'_> {
    fn movsd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Movsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn movsd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Movsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovsdEmitter<Mem, Vec> for Assembler<'_> {
    fn movsd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(InstId::Movsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovsdEmitter<Mem, Xmm> for Assembler<'_> {
    fn movsd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(InstId::Movsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovshdupEmitter<Vec, Vec> for Assembler<'_> {
    fn movshdup(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Movshdup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE3
    }
}

impl MovshdupEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movshdup(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Movshdup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE3
    }
}

impl MovshdupEmitter<Vec, Mem> for Assembler<'_> {
    fn movshdup(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Movshdup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE3
    }
}

impl MovshdupEmitter<Xmm, Mem> for Assembler<'_> {
    fn movshdup(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Movshdup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE3
    }
}

impl MovsldupEmitter<Vec, Vec> for Assembler<'_> {
    fn movsldup(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Movsldup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE3
    }
}

impl MovsldupEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movsldup(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Movsldup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE3
    }
}

impl MovsldupEmitter<Vec, Mem> for Assembler<'_> {
    fn movsldup(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Movsldup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE3
    }
}

impl MovsldupEmitter<Xmm, Mem> for Assembler<'_> {
    fn movsldup(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Movsldup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE3
    }
}

impl MovssEmitter<Vec, Vec> for Assembler<'_> {
    fn movss(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Movss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movss(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Movss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovssEmitter<Vec, Mem> for Assembler<'_> {
    fn movss(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Movss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovssEmitter<Xmm, Mem> for Assembler<'_> {
    fn movss(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Movss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovssEmitter<Mem, Vec> for Assembler<'_> {
    fn movss(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(InstId::Movss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovssEmitter<Mem, Xmm> for Assembler<'_> {
    fn movss(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(InstId::Movss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovupdEmitter<Vec, Vec> for Assembler<'_> {
    fn movupd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Movupd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovupdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movupd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Movupd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovupdEmitter<Vec, Mem> for Assembler<'_> {
    fn movupd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Movupd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovupdEmitter<Xmm, Mem> for Assembler<'_> {
    fn movupd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Movupd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovupdEmitter<Mem, Vec> for Assembler<'_> {
    fn movupd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(InstId::Movupd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovupdEmitter<Mem, Xmm> for Assembler<'_> {
    fn movupd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(InstId::Movupd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MovupsEmitter<Vec, Vec> for Assembler<'_> {
    fn movups(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Movups as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovupsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn movups(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Movups as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovupsEmitter<Vec, Mem> for Assembler<'_> {
    fn movups(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Movups as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovupsEmitter<Xmm, Mem> for Assembler<'_> {
    fn movups(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Movups as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovupsEmitter<Mem, Vec> for Assembler<'_> {
    fn movups(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(InstId::Movups as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MovupsEmitter<Mem, Xmm> for Assembler<'_> {
    fn movups(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(InstId::Movups as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MpsadbwEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn mpsadbw(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Mpsadbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> MpsadbwEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn mpsadbw(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Mpsadbw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl MpsadbwEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn mpsadbw(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Mpsadbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> MpsadbwEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn mpsadbw(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Mpsadbw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl MulpdEmitter<Vec, Vec> for Assembler<'_> {
    fn mulpd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Mulpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MulpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn mulpd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Mulpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MulpdEmitter<Vec, Mem> for Assembler<'_> {
    fn mulpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Mulpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MulpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn mulpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Mulpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MulpsEmitter<Vec, Vec> for Assembler<'_> {
    fn mulps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Mulps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MulpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn mulps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Mulps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MulpsEmitter<Vec, Mem> for Assembler<'_> {
    fn mulps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Mulps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MulpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn mulps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Mulps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MulsdEmitter<Vec, Vec> for Assembler<'_> {
    fn mulsd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Mulsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MulsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn mulsd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Mulsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MulsdEmitter<Vec, Mem> for Assembler<'_> {
    fn mulsd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Mulsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MulsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn mulsd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Mulsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl MulssEmitter<Vec, Vec> for Assembler<'_> {
    fn mulss(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Mulss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MulssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn mulss(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Mulss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MulssEmitter<Vec, Mem> for Assembler<'_> {
    fn mulss(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Mulss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl MulssEmitter<Xmm, Mem> for Assembler<'_> {
    fn mulss(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Mulss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl OrpdEmitter<Vec, Vec> for Assembler<'_> {
    fn orpd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Orpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl OrpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn orpd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Orpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl OrpdEmitter<Vec, Mem> for Assembler<'_> {
    fn orpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Orpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl OrpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn orpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Orpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl OrpsEmitter<Vec, Vec> for Assembler<'_> {
    fn orps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Orps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl OrpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn orps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Orps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl OrpsEmitter<Vec, Mem> for Assembler<'_> {
    fn orps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Orps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl OrpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn orps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Orps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl PackssdwEmitter<Mm, Mm> for Assembler<'_> {
    fn packssdw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Packssdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PackssdwEmitter<Mm, Mem> for Assembler<'_> {
    fn packssdw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Packssdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PackssdwEmitter<Vec, Vec> for Assembler<'_> {
    fn packssdw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Packssdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PackssdwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn packssdw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Packssdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PackssdwEmitter<Vec, Mem> for Assembler<'_> {
    fn packssdw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Packssdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PackssdwEmitter<Xmm, Mem> for Assembler<'_> {
    fn packssdw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Packssdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PacksswbEmitter<Mm, Mm> for Assembler<'_> {
    fn packsswb(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Packsswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PacksswbEmitter<Mm, Mem> for Assembler<'_> {
    fn packsswb(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Packsswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PacksswbEmitter<Vec, Vec> for Assembler<'_> {
    fn packsswb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Packsswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PacksswbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn packsswb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Packsswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PacksswbEmitter<Vec, Mem> for Assembler<'_> {
    fn packsswb(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Packsswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PacksswbEmitter<Xmm, Mem> for Assembler<'_> {
    fn packsswb(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Packsswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PackusdwEmitter<Vec, Vec> for Assembler<'_> {
    fn packusdw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Packusdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PackusdwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn packusdw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Packusdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PackusdwEmitter<Vec, Mem> for Assembler<'_> {
    fn packusdw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Packusdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PackusdwEmitter<Xmm, Mem> for Assembler<'_> {
    fn packusdw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Packusdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PackuswbEmitter<Mm, Mm> for Assembler<'_> {
    fn packuswb(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Packuswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PackuswbEmitter<Mm, Mem> for Assembler<'_> {
    fn packuswb(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Packuswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PackuswbEmitter<Vec, Vec> for Assembler<'_> {
    fn packuswb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Packuswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PackuswbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn packuswb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Packuswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PackuswbEmitter<Vec, Mem> for Assembler<'_> {
    fn packuswb(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Packuswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PackuswbEmitter<Xmm, Mem> for Assembler<'_> {
    fn packuswb(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Packuswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PabsbEmitter<Mm, Mm> for Assembler<'_> {
    fn pabsb(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pabsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PabsbEmitter<Mm, Mem> for Assembler<'_> {
    fn pabsb(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pabsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PabsbEmitter<Vec, Vec> for Assembler<'_> {
    fn pabsb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pabsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PabsbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pabsb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pabsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PabsbEmitter<Vec, Mem> for Assembler<'_> {
    fn pabsb(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pabsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PabsbEmitter<Xmm, Mem> for Assembler<'_> {
    fn pabsb(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pabsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PabsdEmitter<Mm, Mm> for Assembler<'_> {
    fn pabsd(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pabsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PabsdEmitter<Mm, Mem> for Assembler<'_> {
    fn pabsd(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pabsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PabsdEmitter<Vec, Vec> for Assembler<'_> {
    fn pabsd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pabsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PabsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pabsd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pabsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PabsdEmitter<Vec, Mem> for Assembler<'_> {
    fn pabsd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pabsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PabsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn pabsd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pabsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PabswEmitter<Mm, Mm> for Assembler<'_> {
    fn pabsw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pabsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PabswEmitter<Mm, Mem> for Assembler<'_> {
    fn pabsw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pabsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PabswEmitter<Vec, Vec> for Assembler<'_> {
    fn pabsw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pabsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PabswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pabsw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pabsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PabswEmitter<Vec, Mem> for Assembler<'_> {
    fn pabsw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pabsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PabswEmitter<Xmm, Mem> for Assembler<'_> {
    fn pabsw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pabsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PaddbEmitter<Mm, Mm> for Assembler<'_> {
    fn paddb(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Paddb as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PaddbEmitter<Mm, Mem> for Assembler<'_> {
    fn paddb(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Paddb as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PaddbEmitter<Vec, Vec> for Assembler<'_> {
    fn paddb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Paddb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PaddbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn paddb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Paddb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PaddbEmitter<Vec, Mem> for Assembler<'_> {
    fn paddb(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Paddb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PaddbEmitter<Xmm, Mem> for Assembler<'_> {
    fn paddb(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Paddb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PadddEmitter<Mm, Mm> for Assembler<'_> {
    fn paddd(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Paddd as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PadddEmitter<Mm, Mem> for Assembler<'_> {
    fn paddd(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Paddd as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PadddEmitter<Vec, Vec> for Assembler<'_> {
    fn paddd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Paddd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PadddEmitter<Xmm, Xmm> for Assembler<'_> {
    fn paddd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Paddd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PadddEmitter<Vec, Mem> for Assembler<'_> {
    fn paddd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Paddd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PadddEmitter<Xmm, Mem> for Assembler<'_> {
    fn paddd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Paddd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PaddqEmitter<Mm, Mm> for Assembler<'_> {
    fn paddq(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Paddq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PaddqEmitter<Mm, Mem> for Assembler<'_> {
    fn paddq(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Paddq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PaddqEmitter<Vec, Vec> for Assembler<'_> {
    fn paddq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Paddq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PaddqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn paddq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Paddq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PaddqEmitter<Vec, Mem> for Assembler<'_> {
    fn paddq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Paddq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PaddqEmitter<Xmm, Mem> for Assembler<'_> {
    fn paddq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Paddq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PaddsbEmitter<Mm, Mm> for Assembler<'_> {
    fn paddsb(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Paddsb as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PaddsbEmitter<Mm, Mem> for Assembler<'_> {
    fn paddsb(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Paddsb as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PaddsbEmitter<Vec, Vec> for Assembler<'_> {
    fn paddsb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Paddsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PaddsbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn paddsb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Paddsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PaddsbEmitter<Vec, Mem> for Assembler<'_> {
    fn paddsb(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Paddsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PaddsbEmitter<Xmm, Mem> for Assembler<'_> {
    fn paddsb(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Paddsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PaddswEmitter<Mm, Mm> for Assembler<'_> {
    fn paddsw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Paddsw as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PaddswEmitter<Mm, Mem> for Assembler<'_> {
    fn paddsw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Paddsw as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PaddswEmitter<Vec, Vec> for Assembler<'_> {
    fn paddsw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Paddsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PaddswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn paddsw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Paddsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PaddswEmitter<Vec, Mem> for Assembler<'_> {
    fn paddsw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Paddsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PaddswEmitter<Xmm, Mem> for Assembler<'_> {
    fn paddsw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Paddsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PaddusbEmitter<Mm, Mm> for Assembler<'_> {
    fn paddusb(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Paddusb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PaddusbEmitter<Mm, Mem> for Assembler<'_> {
    fn paddusb(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Paddusb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PaddusbEmitter<Vec, Vec> for Assembler<'_> {
    fn paddusb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Paddusb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PaddusbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn paddusb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Paddusb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PaddusbEmitter<Vec, Mem> for Assembler<'_> {
    fn paddusb(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Paddusb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PaddusbEmitter<Xmm, Mem> for Assembler<'_> {
    fn paddusb(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Paddusb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PadduswEmitter<Mm, Mm> for Assembler<'_> {
    fn paddusw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Paddusw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PadduswEmitter<Mm, Mem> for Assembler<'_> {
    fn paddusw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Paddusw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PadduswEmitter<Vec, Vec> for Assembler<'_> {
    fn paddusw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Paddusw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PadduswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn paddusw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Paddusw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PadduswEmitter<Vec, Mem> for Assembler<'_> {
    fn paddusw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Paddusw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PadduswEmitter<Xmm, Mem> for Assembler<'_> {
    fn paddusw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Paddusw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PaddwEmitter<Mm, Mm> for Assembler<'_> {
    fn paddw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Paddw as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PaddwEmitter<Mm, Mem> for Assembler<'_> {
    fn paddw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Paddw as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PaddwEmitter<Vec, Vec> for Assembler<'_> {
    fn paddw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Paddw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PaddwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn paddw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Paddw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PaddwEmitter<Vec, Mem> for Assembler<'_> {
    fn paddw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Paddw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PaddwEmitter<Xmm, Mem> for Assembler<'_> {
    fn paddw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Paddw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl<U2: Into<Imm>> PalignrEmitter<Mm, Mm, U2> for Assembler<'_> {
    fn palignr(&mut self, op0: Mm, op1: Mm, op2: U2) {
        self.emit_n(
            InstId::Palignr as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSSE3
    }
}

impl<U2: Into<Imm>> PalignrEmitter<Mm, Mem, U2> for Assembler<'_> {
    fn palignr(&mut self, op0: Mm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Palignr as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSSE3
    }
}

impl PalignrEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn palignr(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Palignr as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSSE3
    }
}

impl<U2: Into<Imm>> PalignrEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn palignr(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Palignr as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSSE3
    }
}

impl PalignrEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn palignr(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Palignr as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSSE3
    }
}

impl<U2: Into<Imm>> PalignrEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn palignr(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Palignr as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSSE3
    }
}

impl PandEmitter<Mm, Mm> for Assembler<'_> {
    fn pand(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pand as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PandEmitter<Mm, Mem> for Assembler<'_> {
    fn pand(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pand as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PandEmitter<Vec, Vec> for Assembler<'_> {
    fn pand(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pand as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PandEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pand(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pand as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PandEmitter<Vec, Mem> for Assembler<'_> {
    fn pand(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pand as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PandEmitter<Xmm, Mem> for Assembler<'_> {
    fn pand(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pand as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PandnEmitter<Mm, Mm> for Assembler<'_> {
    fn pandn(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pandn as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PandnEmitter<Mm, Mem> for Assembler<'_> {
    fn pandn(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pandn as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PandnEmitter<Vec, Vec> for Assembler<'_> {
    fn pandn(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pandn as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PandnEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pandn(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pandn as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PandnEmitter<Vec, Mem> for Assembler<'_> {
    fn pandn(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pandn as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PandnEmitter<Xmm, Mem> for Assembler<'_> {
    fn pandn(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pandn as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PavgbEmitter<Mm, Mm> for Assembler<'_> {
    fn pavgb(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pavgb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl PavgbEmitter<Mm, Mem> for Assembler<'_> {
    fn pavgb(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pavgb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl PavgbEmitter<Vec, Vec> for Assembler<'_> {
    fn pavgb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pavgb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PavgbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pavgb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pavgb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PavgbEmitter<Vec, Mem> for Assembler<'_> {
    fn pavgb(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pavgb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PavgbEmitter<Xmm, Mem> for Assembler<'_> {
    fn pavgb(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pavgb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PavgwEmitter<Mm, Mm> for Assembler<'_> {
    fn pavgw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pavgw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl PavgwEmitter<Mm, Mem> for Assembler<'_> {
    fn pavgw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pavgw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl PavgwEmitter<Vec, Vec> for Assembler<'_> {
    fn pavgw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pavgw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PavgwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pavgw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pavgw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PavgwEmitter<Vec, Mem> for Assembler<'_> {
    fn pavgw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pavgw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PavgwEmitter<Xmm, Mem> for Assembler<'_> {
    fn pavgw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pavgw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PblendvbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn pblendvb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Pblendvb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl PblendvbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn pblendvb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Pblendvb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl PblendvbEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn pblendvb(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Pblendvb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl PblendvbEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn pblendvb(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Pblendvb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1 [EXPLICIT]
    }
}

impl PblendwEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn pblendw(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Pblendw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PblendwEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn pblendw(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Pblendw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PblendwEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pblendw(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Pblendw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PblendwEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn pblendw(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Pblendw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PclmulqdqEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn pclmulqdq(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Pclmulqdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // PCLMULQDQ.
    }
}

impl<U2: Into<Imm>> PclmulqdqEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn pclmulqdq(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Pclmulqdq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // PCLMULQDQ.
    }
}

impl PclmulqdqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pclmulqdq(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Pclmulqdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // PCLMULQDQ.
    }
}

impl<U2: Into<Imm>> PclmulqdqEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn pclmulqdq(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Pclmulqdq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // PCLMULQDQ.
    }
}

impl PcmpestriEmitter<Vec, Vec, Imm, Gp, Gp, Gp> for Assembler<'_> {
    fn pcmpestri(&mut self, op0: Vec, op1: Vec, op2: Imm, op3: Gp, op4: Gp, op5: Gp) {
        self.emit_n(
            InstId::Pcmpestri as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                op4.as_operand(),
                op5.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl<U2: Into<Imm>> PcmpestriEmitter<Xmm, Xmm, U2, Gpd, Gpd, Gpd> for Assembler<'_> {
    fn pcmpestri(&mut self, op0: Xmm, op1: Xmm, op2: U2, op3: Gpd, op4: Gpd, op5: Gpd) {
        self.emit_n(
            InstId::Pcmpestri as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
                op3.as_operand(),
                op4.as_operand(),
                op5.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl PcmpestriEmitter<Vec, Mem, Imm, Gp, Gp, Gp> for Assembler<'_> {
    fn pcmpestri(&mut self, op0: Vec, op1: Mem, op2: Imm, op3: Gp, op4: Gp, op5: Gp) {
        self.emit_n(
            InstId::Pcmpestri as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                op4.as_operand(),
                op5.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl<U2: Into<Imm>> PcmpestriEmitter<Xmm, Mem, U2, Gpd, Gpd, Gpd> for Assembler<'_> {
    fn pcmpestri(&mut self, op0: Xmm, op1: Mem, op2: U2, op3: Gpd, op4: Gpd, op5: Gpd) {
        self.emit_n(
            InstId::Pcmpestri as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
                op3.as_operand(),
                op4.as_operand(),
                op5.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl PcmpestrmEmitter<Vec, Vec, Imm, Vec, Gp, Gp> for Assembler<'_> {
    fn pcmpestrm(&mut self, op0: Vec, op1: Vec, op2: Imm, op3: Vec, op4: Gp, op5: Gp) {
        self.emit_n(
            InstId::Pcmpestrm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                op4.as_operand(),
                op5.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl<U2: Into<Imm>> PcmpestrmEmitter<Xmm, Xmm, U2, Xmm, Gpd, Gpd> for Assembler<'_> {
    fn pcmpestrm(&mut self, op0: Xmm, op1: Xmm, op2: U2, op3: Xmm, op4: Gpd, op5: Gpd) {
        self.emit_n(
            InstId::Pcmpestrm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
                op3.as_operand(),
                op4.as_operand(),
                op5.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl PcmpestrmEmitter<Vec, Mem, Imm, Vec, Gp, Gp> for Assembler<'_> {
    fn pcmpestrm(&mut self, op0: Vec, op1: Mem, op2: Imm, op3: Vec, op4: Gp, op5: Gp) {
        self.emit_n(
            InstId::Pcmpestrm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                op4.as_operand(),
                op5.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl<U2: Into<Imm>> PcmpestrmEmitter<Xmm, Mem, U2, Xmm, Gpd, Gpd> for Assembler<'_> {
    fn pcmpestrm(&mut self, op0: Xmm, op1: Mem, op2: U2, op3: Xmm, op4: Gpd, op5: Gpd) {
        self.emit_n(
            InstId::Pcmpestrm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
                op3.as_operand(),
                op4.as_operand(),
                op5.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl PcmpeqbEmitter<Mm, Mm> for Assembler<'_> {
    fn pcmpeqb(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pcmpeqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PcmpeqbEmitter<Mm, Mem> for Assembler<'_> {
    fn pcmpeqb(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pcmpeqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PcmpeqbEmitter<Vec, Vec> for Assembler<'_> {
    fn pcmpeqb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pcmpeqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pcmpeqb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pcmpeqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqbEmitter<Vec, Mem> for Assembler<'_> {
    fn pcmpeqb(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pcmpeqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqbEmitter<Xmm, Mem> for Assembler<'_> {
    fn pcmpeqb(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pcmpeqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqdEmitter<Mm, Mm> for Assembler<'_> {
    fn pcmpeqd(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pcmpeqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PcmpeqdEmitter<Mm, Mem> for Assembler<'_> {
    fn pcmpeqd(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pcmpeqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PcmpeqdEmitter<Vec, Vec> for Assembler<'_> {
    fn pcmpeqd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pcmpeqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pcmpeqd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pcmpeqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqdEmitter<Vec, Mem> for Assembler<'_> {
    fn pcmpeqd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pcmpeqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqdEmitter<Xmm, Mem> for Assembler<'_> {
    fn pcmpeqd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pcmpeqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqqEmitter<Vec, Vec> for Assembler<'_> {
    fn pcmpeqq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pcmpeqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PcmpeqqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pcmpeqq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pcmpeqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PcmpeqqEmitter<Vec, Mem> for Assembler<'_> {
    fn pcmpeqq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pcmpeqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PcmpeqqEmitter<Xmm, Mem> for Assembler<'_> {
    fn pcmpeqq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pcmpeqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PcmpeqwEmitter<Mm, Mm> for Assembler<'_> {
    fn pcmpeqw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pcmpeqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PcmpeqwEmitter<Mm, Mem> for Assembler<'_> {
    fn pcmpeqw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pcmpeqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PcmpeqwEmitter<Vec, Vec> for Assembler<'_> {
    fn pcmpeqw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pcmpeqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pcmpeqw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pcmpeqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqwEmitter<Vec, Mem> for Assembler<'_> {
    fn pcmpeqw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pcmpeqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpeqwEmitter<Xmm, Mem> for Assembler<'_> {
    fn pcmpeqw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pcmpeqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtbEmitter<Mm, Mm> for Assembler<'_> {
    fn pcmpgtb(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pcmpgtb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PcmpgtbEmitter<Mm, Mem> for Assembler<'_> {
    fn pcmpgtb(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pcmpgtb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PcmpgtbEmitter<Vec, Vec> for Assembler<'_> {
    fn pcmpgtb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pcmpgtb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pcmpgtb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pcmpgtb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtbEmitter<Vec, Mem> for Assembler<'_> {
    fn pcmpgtb(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pcmpgtb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtbEmitter<Xmm, Mem> for Assembler<'_> {
    fn pcmpgtb(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pcmpgtb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtdEmitter<Mm, Mm> for Assembler<'_> {
    fn pcmpgtd(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pcmpgtd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PcmpgtdEmitter<Mm, Mem> for Assembler<'_> {
    fn pcmpgtd(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pcmpgtd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PcmpgtdEmitter<Vec, Vec> for Assembler<'_> {
    fn pcmpgtd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pcmpgtd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pcmpgtd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pcmpgtd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtdEmitter<Vec, Mem> for Assembler<'_> {
    fn pcmpgtd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pcmpgtd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtdEmitter<Xmm, Mem> for Assembler<'_> {
    fn pcmpgtd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pcmpgtd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtqEmitter<Vec, Vec> for Assembler<'_> {
    fn pcmpgtq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pcmpgtq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_2.
    }
}

impl PcmpgtqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pcmpgtq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pcmpgtq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_2.
    }
}

impl PcmpgtqEmitter<Vec, Mem> for Assembler<'_> {
    fn pcmpgtq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pcmpgtq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_2.
    }
}

impl PcmpgtqEmitter<Xmm, Mem> for Assembler<'_> {
    fn pcmpgtq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pcmpgtq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_2.
    }
}

impl PcmpgtwEmitter<Mm, Mm> for Assembler<'_> {
    fn pcmpgtw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pcmpgtw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PcmpgtwEmitter<Mm, Mem> for Assembler<'_> {
    fn pcmpgtw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pcmpgtw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PcmpgtwEmitter<Vec, Vec> for Assembler<'_> {
    fn pcmpgtw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pcmpgtw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pcmpgtw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pcmpgtw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtwEmitter<Vec, Mem> for Assembler<'_> {
    fn pcmpgtw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pcmpgtw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpgtwEmitter<Xmm, Mem> for Assembler<'_> {
    fn pcmpgtw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pcmpgtw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PcmpistriEmitter<Vec, Vec, Imm, Gp> for Assembler<'_> {
    fn pcmpistri(&mut self, op0: Vec, op1: Vec, op2: Imm, op3: Gp) {
        self.emit_n(
            InstId::Pcmpistri as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl<U2: Into<Imm>> PcmpistriEmitter<Xmm, Xmm, U2, Gpd> for Assembler<'_> {
    fn pcmpistri(&mut self, op0: Xmm, op1: Xmm, op2: U2, op3: Gpd) {
        self.emit_n(
            InstId::Pcmpistri as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
                op3.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl PcmpistriEmitter<Vec, Mem, Imm, Gp> for Assembler<'_> {
    fn pcmpistri(&mut self, op0: Vec, op1: Mem, op2: Imm, op3: Gp) {
        self.emit_n(
            InstId::Pcmpistri as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl<U2: Into<Imm>> PcmpistriEmitter<Xmm, Mem, U2, Gpd> for Assembler<'_> {
    fn pcmpistri(&mut self, op0: Xmm, op1: Mem, op2: U2, op3: Gpd) {
        self.emit_n(
            InstId::Pcmpistri as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
                op3.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl PcmpistrmEmitter<Vec, Vec, Imm, Vec> for Assembler<'_> {
    fn pcmpistrm(&mut self, op0: Vec, op1: Vec, op2: Imm, op3: Vec) {
        self.emit_n(
            InstId::Pcmpistrm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl<U2: Into<Imm>> PcmpistrmEmitter<Xmm, Xmm, U2, Xmm> for Assembler<'_> {
    fn pcmpistrm(&mut self, op0: Xmm, op1: Xmm, op2: U2, op3: Xmm) {
        self.emit_n(
            InstId::Pcmpistrm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
                op3.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl PcmpistrmEmitter<Vec, Mem, Imm, Vec> for Assembler<'_> {
    fn pcmpistrm(&mut self, op0: Vec, op1: Mem, op2: Imm, op3: Vec) {
        self.emit_n(
            InstId::Pcmpistrm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl<U2: Into<Imm>> PcmpistrmEmitter<Xmm, Mem, U2, Xmm> for Assembler<'_> {
    fn pcmpistrm(&mut self, op0: Xmm, op1: Mem, op2: U2, op3: Xmm) {
        self.emit_n(
            InstId::Pcmpistrm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
                op3.as_operand(),
            ],
        ); // SSE4_2 [EXPLICIT]
    }
}

impl PextrbEmitter<Gp, Vec, Imm> for Assembler<'_> {
    fn pextrb(&mut self, op0: Gp, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Pextrb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PextrbEmitter<Gpd, Xmm, U2> for Assembler<'_> {
    fn pextrb(&mut self, op0: Gpd, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Pextrb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PextrbEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn pextrb(&mut self, op0: Mem, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Pextrb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PextrbEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn pextrb(&mut self, op0: Mem, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Pextrb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PextrdEmitter<Gp, Vec, Imm> for Assembler<'_> {
    fn pextrd(&mut self, op0: Gp, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Pextrd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PextrdEmitter<Gpd, Xmm, U2> for Assembler<'_> {
    fn pextrd(&mut self, op0: Gpd, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Pextrd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PextrdEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn pextrd(&mut self, op0: Mem, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Pextrd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PextrdEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn pextrd(&mut self, op0: Mem, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Pextrd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PextrqEmitter<Gp, Vec, Imm> for Assembler<'_> {
    fn pextrq(&mut self, op0: Gp, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Pextrq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PextrqEmitter<Gpq, Xmm, U2> for Assembler<'_> {
    fn pextrq(&mut self, op0: Gpq, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Pextrq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PextrqEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn pextrq(&mut self, op0: Mem, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Pextrq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PextrqEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn pextrq(&mut self, op0: Mem, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Pextrq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PextrwEmitter<Gp, Mm, Imm> for Assembler<'_> {
    fn pextrw(&mut self, op0: Gp, op1: Mm, op2: Imm) {
        self.emit_n(
            InstId::Pextrw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE
    }
}

impl<U2: Into<Imm>> PextrwEmitter<Gpd, Mm, U2> for Assembler<'_> {
    fn pextrw(&mut self, op0: Gpd, op1: Mm, op2: U2) {
        self.emit_n(
            InstId::Pextrw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE
    }
}

impl PextrwEmitter<Gp, Vec, Imm> for Assembler<'_> {
    fn pextrw(&mut self, op0: Gp, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Pextrw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> PextrwEmitter<Gpd, Xmm, U2> for Assembler<'_> {
    fn pextrw(&mut self, op0: Gpd, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Pextrw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE2
    }
}

impl PextrwEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn pextrw(&mut self, op0: Mem, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Pextrw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PextrwEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn pextrw(&mut self, op0: Mem, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Pextrw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PhadddEmitter<Mm, Mm> for Assembler<'_> {
    fn phaddd(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Phaddd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhadddEmitter<Mm, Mem> for Assembler<'_> {
    fn phaddd(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Phaddd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhadddEmitter<Vec, Vec> for Assembler<'_> {
    fn phaddd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Phaddd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhadddEmitter<Xmm, Xmm> for Assembler<'_> {
    fn phaddd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Phaddd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhadddEmitter<Vec, Mem> for Assembler<'_> {
    fn phaddd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Phaddd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhadddEmitter<Xmm, Mem> for Assembler<'_> {
    fn phaddd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Phaddd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhaddswEmitter<Mm, Mm> for Assembler<'_> {
    fn phaddsw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Phaddsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PhaddswEmitter<Mm, Mem> for Assembler<'_> {
    fn phaddsw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Phaddsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PhaddswEmitter<Vec, Vec> for Assembler<'_> {
    fn phaddsw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Phaddsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PhaddswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn phaddsw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Phaddsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PhaddswEmitter<Vec, Mem> for Assembler<'_> {
    fn phaddsw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Phaddsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PhaddswEmitter<Xmm, Mem> for Assembler<'_> {
    fn phaddsw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Phaddsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PhaddwEmitter<Mm, Mm> for Assembler<'_> {
    fn phaddw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Phaddw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhaddwEmitter<Mm, Mem> for Assembler<'_> {
    fn phaddw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Phaddw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhaddwEmitter<Vec, Vec> for Assembler<'_> {
    fn phaddw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Phaddw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhaddwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn phaddw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Phaddw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhaddwEmitter<Vec, Mem> for Assembler<'_> {
    fn phaddw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Phaddw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhaddwEmitter<Xmm, Mem> for Assembler<'_> {
    fn phaddw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Phaddw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhminposuwEmitter<Vec, Vec> for Assembler<'_> {
    fn phminposuw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Phminposuw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PhminposuwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn phminposuw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Phminposuw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PhminposuwEmitter<Vec, Mem> for Assembler<'_> {
    fn phminposuw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Phminposuw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PhminposuwEmitter<Xmm, Mem> for Assembler<'_> {
    fn phminposuw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Phminposuw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PhsubdEmitter<Mm, Mm> for Assembler<'_> {
    fn phsubd(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Phsubd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhsubdEmitter<Mm, Mem> for Assembler<'_> {
    fn phsubd(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Phsubd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhsubdEmitter<Vec, Vec> for Assembler<'_> {
    fn phsubd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Phsubd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhsubdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn phsubd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Phsubd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhsubdEmitter<Vec, Mem> for Assembler<'_> {
    fn phsubd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Phsubd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhsubdEmitter<Xmm, Mem> for Assembler<'_> {
    fn phsubd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Phsubd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhsubswEmitter<Mm, Mm> for Assembler<'_> {
    fn phsubsw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Phsubsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PhsubswEmitter<Mm, Mem> for Assembler<'_> {
    fn phsubsw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Phsubsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PhsubswEmitter<Vec, Vec> for Assembler<'_> {
    fn phsubsw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Phsubsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PhsubswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn phsubsw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Phsubsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PhsubswEmitter<Vec, Mem> for Assembler<'_> {
    fn phsubsw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Phsubsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PhsubswEmitter<Xmm, Mem> for Assembler<'_> {
    fn phsubsw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Phsubsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PhsubwEmitter<Mm, Mm> for Assembler<'_> {
    fn phsubw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Phsubw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhsubwEmitter<Mm, Mem> for Assembler<'_> {
    fn phsubw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Phsubw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhsubwEmitter<Vec, Vec> for Assembler<'_> {
    fn phsubw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Phsubw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhsubwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn phsubw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Phsubw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhsubwEmitter<Vec, Mem> for Assembler<'_> {
    fn phsubw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Phsubw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PhsubwEmitter<Xmm, Mem> for Assembler<'_> {
    fn phsubw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Phsubw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PinsrbEmitter<Vec, Gp, Imm> for Assembler<'_> {
    fn pinsrb(&mut self, op0: Vec, op1: Gp, op2: Imm) {
        self.emit_n(
            InstId::Pinsrb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PinsrbEmitter<Xmm, Gpd, U2> for Assembler<'_> {
    fn pinsrb(&mut self, op0: Xmm, op1: Gpd, op2: U2) {
        self.emit_n(
            InstId::Pinsrb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PinsrbEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pinsrb(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Pinsrb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PinsrbEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn pinsrb(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Pinsrb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PinsrdEmitter<Vec, Gp, Imm> for Assembler<'_> {
    fn pinsrd(&mut self, op0: Vec, op1: Gp, op2: Imm) {
        self.emit_n(
            InstId::Pinsrd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PinsrdEmitter<Xmm, Gpd, U2> for Assembler<'_> {
    fn pinsrd(&mut self, op0: Xmm, op1: Gpd, op2: U2) {
        self.emit_n(
            InstId::Pinsrd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PinsrdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pinsrd(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Pinsrd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PinsrdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn pinsrd(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Pinsrd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PinsrqEmitter<Vec, Gp, Imm> for Assembler<'_> {
    fn pinsrq(&mut self, op0: Vec, op1: Gp, op2: Imm) {
        self.emit_n(
            InstId::Pinsrq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PinsrqEmitter<Xmm, Gpq, U2> for Assembler<'_> {
    fn pinsrq(&mut self, op0: Xmm, op1: Gpq, op2: U2) {
        self.emit_n(
            InstId::Pinsrq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PinsrqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pinsrq(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Pinsrq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> PinsrqEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn pinsrq(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Pinsrq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl PinsrwEmitter<Mm, Gp, Imm> for Assembler<'_> {
    fn pinsrw(&mut self, op0: Mm, op1: Gp, op2: Imm) {
        self.emit_n(
            InstId::Pinsrw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE
    }
}

impl<U2: Into<Imm>> PinsrwEmitter<Mm, Gpd, U2> for Assembler<'_> {
    fn pinsrw(&mut self, op0: Mm, op1: Gpd, op2: U2) {
        self.emit_n(
            InstId::Pinsrw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE
    }
}

impl<U2: Into<Imm>> PinsrwEmitter<Mm, Mem, U2> for Assembler<'_> {
    fn pinsrw(&mut self, op0: Mm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Pinsrw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE
    }
}

impl PinsrwEmitter<Vec, Gp, Imm> for Assembler<'_> {
    fn pinsrw(&mut self, op0: Vec, op1: Gp, op2: Imm) {
        self.emit_n(
            InstId::Pinsrw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> PinsrwEmitter<Xmm, Gpd, U2> for Assembler<'_> {
    fn pinsrw(&mut self, op0: Xmm, op1: Gpd, op2: U2) {
        self.emit_n(
            InstId::Pinsrw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE2
    }
}

impl PinsrwEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pinsrw(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Pinsrw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> PinsrwEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn pinsrw(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Pinsrw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE2
    }
}

impl PmaddubswEmitter<Mm, Mm> for Assembler<'_> {
    fn pmaddubsw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pmaddubsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PmaddubswEmitter<Mm, Mem> for Assembler<'_> {
    fn pmaddubsw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pmaddubsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PmaddubswEmitter<Vec, Vec> for Assembler<'_> {
    fn pmaddubsw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pmaddubsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PmaddubswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmaddubsw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pmaddubsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PmaddubswEmitter<Vec, Mem> for Assembler<'_> {
    fn pmaddubsw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pmaddubsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PmaddubswEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmaddubsw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pmaddubsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PmaddwdEmitter<Mm, Mm> for Assembler<'_> {
    fn pmaddwd(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pmaddwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PmaddwdEmitter<Mm, Mem> for Assembler<'_> {
    fn pmaddwd(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pmaddwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PmaddwdEmitter<Vec, Vec> for Assembler<'_> {
    fn pmaddwd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pmaddwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PmaddwdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmaddwd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pmaddwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PmaddwdEmitter<Vec, Mem> for Assembler<'_> {
    fn pmaddwd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pmaddwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PmaddwdEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmaddwd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pmaddwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PmaxsbEmitter<Vec, Vec> for Assembler<'_> {
    fn pmaxsb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pmaxsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmaxsbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmaxsb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pmaxsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmaxsbEmitter<Vec, Mem> for Assembler<'_> {
    fn pmaxsb(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pmaxsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmaxsbEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmaxsb(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pmaxsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmaxsdEmitter<Vec, Vec> for Assembler<'_> {
    fn pmaxsd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pmaxsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmaxsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmaxsd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pmaxsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmaxsdEmitter<Vec, Mem> for Assembler<'_> {
    fn pmaxsd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pmaxsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmaxsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmaxsd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pmaxsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmaxswEmitter<Mm, Mm> for Assembler<'_> {
    fn pmaxsw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pmaxsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl PmaxswEmitter<Mm, Mem> for Assembler<'_> {
    fn pmaxsw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pmaxsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl PmaxswEmitter<Vec, Vec> for Assembler<'_> {
    fn pmaxsw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pmaxsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PmaxswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmaxsw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pmaxsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PmaxswEmitter<Vec, Mem> for Assembler<'_> {
    fn pmaxsw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pmaxsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PmaxswEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmaxsw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pmaxsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PmaxubEmitter<Mm, Mm> for Assembler<'_> {
    fn pmaxub(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pmaxub as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl PmaxubEmitter<Mm, Mem> for Assembler<'_> {
    fn pmaxub(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pmaxub as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl PmaxubEmitter<Vec, Vec> for Assembler<'_> {
    fn pmaxub(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pmaxub as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PmaxubEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmaxub(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pmaxub as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PmaxubEmitter<Vec, Mem> for Assembler<'_> {
    fn pmaxub(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pmaxub as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PmaxubEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmaxub(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pmaxub as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PmaxudEmitter<Vec, Vec> for Assembler<'_> {
    fn pmaxud(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pmaxud as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmaxudEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmaxud(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pmaxud as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmaxudEmitter<Vec, Mem> for Assembler<'_> {
    fn pmaxud(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pmaxud as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmaxudEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmaxud(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pmaxud as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmaxuwEmitter<Vec, Vec> for Assembler<'_> {
    fn pmaxuw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pmaxuw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmaxuwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmaxuw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pmaxuw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmaxuwEmitter<Vec, Mem> for Assembler<'_> {
    fn pmaxuw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pmaxuw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmaxuwEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmaxuw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pmaxuw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PminsbEmitter<Vec, Vec> for Assembler<'_> {
    fn pminsb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pminsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PminsbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pminsb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pminsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PminsbEmitter<Vec, Mem> for Assembler<'_> {
    fn pminsb(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pminsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PminsbEmitter<Xmm, Mem> for Assembler<'_> {
    fn pminsb(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pminsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PminsdEmitter<Vec, Vec> for Assembler<'_> {
    fn pminsd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pminsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PminsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pminsd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pminsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PminsdEmitter<Vec, Mem> for Assembler<'_> {
    fn pminsd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pminsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PminsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn pminsd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pminsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PminswEmitter<Mm, Mm> for Assembler<'_> {
    fn pminsw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pminsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl PminswEmitter<Mm, Mem> for Assembler<'_> {
    fn pminsw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pminsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl PminswEmitter<Vec, Vec> for Assembler<'_> {
    fn pminsw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pminsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PminswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pminsw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pminsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PminswEmitter<Vec, Mem> for Assembler<'_> {
    fn pminsw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pminsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PminswEmitter<Xmm, Mem> for Assembler<'_> {
    fn pminsw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pminsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PminubEmitter<Mm, Mm> for Assembler<'_> {
    fn pminub(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pminub as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl PminubEmitter<Mm, Mem> for Assembler<'_> {
    fn pminub(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pminub as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl PminubEmitter<Vec, Vec> for Assembler<'_> {
    fn pminub(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pminub as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PminubEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pminub(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pminub as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PminubEmitter<Vec, Mem> for Assembler<'_> {
    fn pminub(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pminub as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PminubEmitter<Xmm, Mem> for Assembler<'_> {
    fn pminub(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pminub as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PminudEmitter<Vec, Vec> for Assembler<'_> {
    fn pminud(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pminud as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PminudEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pminud(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pminud as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PminudEmitter<Vec, Mem> for Assembler<'_> {
    fn pminud(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pminud as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PminudEmitter<Xmm, Mem> for Assembler<'_> {
    fn pminud(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pminud as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PminuwEmitter<Vec, Vec> for Assembler<'_> {
    fn pminuw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pminuw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PminuwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pminuw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pminuw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PminuwEmitter<Vec, Mem> for Assembler<'_> {
    fn pminuw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pminuw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PminuwEmitter<Xmm, Mem> for Assembler<'_> {
    fn pminuw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pminuw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmovmskbEmitter<Gp, Mm> for Assembler<'_> {
    fn pmovmskb(&mut self, op0: Gp, op1: Mm) {
        self.emit_n(
            InstId::Pmovmskb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl PmovmskbEmitter<Gpd, Mm> for Assembler<'_> {
    fn pmovmskb(&mut self, op0: Gpd, op1: Mm) {
        self.emit_n(
            InstId::Pmovmskb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl PmovmskbEmitter<Gp, Vec> for Assembler<'_> {
    fn pmovmskb(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Pmovmskb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PmovmskbEmitter<Gpd, Xmm> for Assembler<'_> {
    fn pmovmskb(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Pmovmskb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PmovsxbdEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovsxbd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pmovsxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovsxbd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pmovsxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbdEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovsxbd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pmovsxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbdEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovsxbd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pmovsxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbqEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovsxbq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pmovsxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovsxbq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pmovsxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbqEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovsxbq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pmovsxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbqEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovsxbq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pmovsxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbwEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovsxbw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pmovsxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovsxbw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pmovsxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbwEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovsxbw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pmovsxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxbwEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovsxbw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pmovsxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxdqEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovsxdq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pmovsxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxdqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovsxdq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pmovsxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxdqEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovsxdq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pmovsxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxdqEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovsxdq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pmovsxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxwdEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovsxwd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pmovsxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxwdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovsxwd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pmovsxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxwdEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovsxwd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pmovsxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxwdEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovsxwd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pmovsxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxwqEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovsxwq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pmovsxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxwqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovsxwq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pmovsxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxwqEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovsxwq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pmovsxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovsxwqEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovsxwq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pmovsxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbdEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovzxbd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pmovzxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovzxbd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pmovzxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbdEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovzxbd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pmovzxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbdEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovzxbd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pmovzxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbqEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovzxbq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pmovzxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovzxbq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pmovzxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbqEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovzxbq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pmovzxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbqEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovzxbq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pmovzxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbwEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovzxbw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pmovzxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovzxbw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pmovzxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbwEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovzxbw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pmovzxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxbwEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovzxbw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pmovzxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxdqEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovzxdq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pmovzxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxdqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovzxdq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pmovzxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxdqEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovzxdq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pmovzxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxdqEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovzxdq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pmovzxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxwdEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovzxwd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pmovzxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxwdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovzxwd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pmovzxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxwdEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovzxwd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pmovzxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxwdEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovzxwd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pmovzxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxwqEmitter<Vec, Vec> for Assembler<'_> {
    fn pmovzxwq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pmovzxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxwqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmovzxwq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pmovzxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxwqEmitter<Vec, Mem> for Assembler<'_> {
    fn pmovzxwq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pmovzxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmovzxwqEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmovzxwq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pmovzxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1
    }
}

impl PmuldqEmitter<Vec, Vec> for Assembler<'_> {
    fn pmuldq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pmuldq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmuldqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmuldq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pmuldq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmuldqEmitter<Vec, Mem> for Assembler<'_> {
    fn pmuldq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pmuldq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmuldqEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmuldq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pmuldq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmulhrswEmitter<Mm, Mm> for Assembler<'_> {
    fn pmulhrsw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pmulhrsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PmulhrswEmitter<Mm, Mem> for Assembler<'_> {
    fn pmulhrsw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pmulhrsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PmulhrswEmitter<Vec, Vec> for Assembler<'_> {
    fn pmulhrsw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pmulhrsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PmulhrswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmulhrsw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pmulhrsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PmulhrswEmitter<Vec, Mem> for Assembler<'_> {
    fn pmulhrsw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pmulhrsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PmulhrswEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmulhrsw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pmulhrsw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSSE3
    }
}

impl PmulhwEmitter<Mm, Mm> for Assembler<'_> {
    fn pmulhw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pmulhw as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PmulhwEmitter<Mm, Mem> for Assembler<'_> {
    fn pmulhw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pmulhw as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PmulhwEmitter<Vec, Vec> for Assembler<'_> {
    fn pmulhw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pmulhw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PmulhwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmulhw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pmulhw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PmulhwEmitter<Vec, Mem> for Assembler<'_> {
    fn pmulhw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pmulhw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PmulhwEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmulhw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pmulhw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PmulhuwEmitter<Mm, Mm> for Assembler<'_> {
    fn pmulhuw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pmulhuw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl PmulhuwEmitter<Mm, Mem> for Assembler<'_> {
    fn pmulhuw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pmulhuw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl PmulhuwEmitter<Vec, Vec> for Assembler<'_> {
    fn pmulhuw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pmulhuw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PmulhuwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmulhuw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pmulhuw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PmulhuwEmitter<Vec, Mem> for Assembler<'_> {
    fn pmulhuw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pmulhuw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PmulhuwEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmulhuw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pmulhuw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PmulldEmitter<Vec, Vec> for Assembler<'_> {
    fn pmulld(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pmulld as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmulldEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmulld(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pmulld as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmulldEmitter<Vec, Mem> for Assembler<'_> {
    fn pmulld(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pmulld as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmulldEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmulld(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pmulld as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PmullwEmitter<Mm, Mm> for Assembler<'_> {
    fn pmullw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pmullw as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PmullwEmitter<Mm, Mem> for Assembler<'_> {
    fn pmullw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pmullw as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PmullwEmitter<Vec, Vec> for Assembler<'_> {
    fn pmullw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pmullw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PmullwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmullw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pmullw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PmullwEmitter<Vec, Mem> for Assembler<'_> {
    fn pmullw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pmullw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PmullwEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmullw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pmullw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PmuludqEmitter<Mm, Mm> for Assembler<'_> {
    fn pmuludq(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pmuludq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PmuludqEmitter<Mm, Mem> for Assembler<'_> {
    fn pmuludq(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pmuludq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PmuludqEmitter<Vec, Vec> for Assembler<'_> {
    fn pmuludq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pmuludq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PmuludqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pmuludq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pmuludq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PmuludqEmitter<Vec, Mem> for Assembler<'_> {
    fn pmuludq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pmuludq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PmuludqEmitter<Xmm, Mem> for Assembler<'_> {
    fn pmuludq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Pmuludq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PorEmitter<Mm, Mm> for Assembler<'_> {
    fn por(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Por as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PorEmitter<Mm, Mem> for Assembler<'_> {
    fn por(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Por as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PorEmitter<Vec, Vec> for Assembler<'_> {
    fn por(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Por as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PorEmitter<Xmm, Xmm> for Assembler<'_> {
    fn por(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Por as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PorEmitter<Vec, Mem> for Assembler<'_> {
    fn por(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Por as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PorEmitter<Xmm, Mem> for Assembler<'_> {
    fn por(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Por as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsadbwEmitter<Mm, Mm> for Assembler<'_> {
    fn psadbw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Psadbw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl PsadbwEmitter<Mm, Mem> for Assembler<'_> {
    fn psadbw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Psadbw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl PsadbwEmitter<Vec, Vec> for Assembler<'_> {
    fn psadbw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Psadbw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl PsadbwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psadbw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Psadbw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl PsadbwEmitter<Vec, Mem> for Assembler<'_> {
    fn psadbw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Psadbw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl PsadbwEmitter<Xmm, Mem> for Assembler<'_> {
    fn psadbw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Psadbw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl PslldEmitter<Mm, Mm> for Assembler<'_> {
    fn pslld(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pslld as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PslldEmitter<Mm, Mem> for Assembler<'_> {
    fn pslld(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pslld as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl<U1: Into<Imm>> PslldEmitter<Mm, U1> for Assembler<'_> {
    fn pslld(&mut self, op0: Mm, op1: U1) {
        self.emit_n(
            InstId::Pslld as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // MMX
    }
}

impl PslldEmitter<Vec, Vec> for Assembler<'_> {
    fn pslld(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pslld as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PslldEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pslld(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pslld as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PslldEmitter<Vec, Mem> for Assembler<'_> {
    fn pslld(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pslld as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PslldEmitter<Xmm, Mem> for Assembler<'_> {
    fn pslld(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pslld as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PslldEmitter<Vec, Imm> for Assembler<'_> {
    fn pslld(&mut self, op0: Vec, op1: Imm) {
        self.emit_n(InstId::Pslld as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl<U1: Into<Imm>> PslldEmitter<Xmm, U1> for Assembler<'_> {
    fn pslld(&mut self, op0: Xmm, op1: U1) {
        self.emit_n(
            InstId::Pslld as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // SSE2
    }
}

impl PslldqEmitter<Vec, Imm> for Assembler<'_> {
    fn pslldq(&mut self, op0: Vec, op1: Imm) {
        self.emit_n(InstId::Pslldq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl<U1: Into<Imm>> PslldqEmitter<Xmm, U1> for Assembler<'_> {
    fn pslldq(&mut self, op0: Xmm, op1: U1) {
        self.emit_n(
            InstId::Pslldq as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // SSE2
    }
}

impl PsllqEmitter<Mm, Mm> for Assembler<'_> {
    fn psllq(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Psllq as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PsllqEmitter<Mm, Mem> for Assembler<'_> {
    fn psllq(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Psllq as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl<U1: Into<Imm>> PsllqEmitter<Mm, U1> for Assembler<'_> {
    fn psllq(&mut self, op0: Mm, op1: U1) {
        self.emit_n(
            InstId::Psllq as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // MMX
    }
}

impl PsllqEmitter<Vec, Vec> for Assembler<'_> {
    fn psllq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Psllq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsllqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psllq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Psllq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsllqEmitter<Vec, Mem> for Assembler<'_> {
    fn psllq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Psllq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsllqEmitter<Xmm, Mem> for Assembler<'_> {
    fn psllq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Psllq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsllqEmitter<Vec, Imm> for Assembler<'_> {
    fn psllq(&mut self, op0: Vec, op1: Imm) {
        self.emit_n(InstId::Psllq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl<U1: Into<Imm>> PsllqEmitter<Xmm, U1> for Assembler<'_> {
    fn psllq(&mut self, op0: Xmm, op1: U1) {
        self.emit_n(
            InstId::Psllq as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // SSE2
    }
}

impl PsllwEmitter<Mm, Mm> for Assembler<'_> {
    fn psllw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Psllw as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PsllwEmitter<Mm, Mem> for Assembler<'_> {
    fn psllw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Psllw as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl<U1: Into<Imm>> PsllwEmitter<Mm, U1> for Assembler<'_> {
    fn psllw(&mut self, op0: Mm, op1: U1) {
        self.emit_n(
            InstId::Psllw as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // MMX
    }
}

impl PsllwEmitter<Vec, Vec> for Assembler<'_> {
    fn psllw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Psllw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsllwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psllw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Psllw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsllwEmitter<Vec, Mem> for Assembler<'_> {
    fn psllw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Psllw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsllwEmitter<Xmm, Mem> for Assembler<'_> {
    fn psllw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Psllw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsllwEmitter<Vec, Imm> for Assembler<'_> {
    fn psllw(&mut self, op0: Vec, op1: Imm) {
        self.emit_n(InstId::Psllw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl<U1: Into<Imm>> PsllwEmitter<Xmm, U1> for Assembler<'_> {
    fn psllw(&mut self, op0: Xmm, op1: U1) {
        self.emit_n(
            InstId::Psllw as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // SSE2
    }
}

impl PsradEmitter<Mm, Mm> for Assembler<'_> {
    fn psrad(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Psrad as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PsradEmitter<Mm, Mem> for Assembler<'_> {
    fn psrad(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Psrad as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl<U1: Into<Imm>> PsradEmitter<Mm, U1> for Assembler<'_> {
    fn psrad(&mut self, op0: Mm, op1: U1) {
        self.emit_n(
            InstId::Psrad as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // MMX
    }
}

impl PsradEmitter<Vec, Vec> for Assembler<'_> {
    fn psrad(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Psrad as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsradEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psrad(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Psrad as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsradEmitter<Vec, Mem> for Assembler<'_> {
    fn psrad(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Psrad as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsradEmitter<Xmm, Mem> for Assembler<'_> {
    fn psrad(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Psrad as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsradEmitter<Vec, Imm> for Assembler<'_> {
    fn psrad(&mut self, op0: Vec, op1: Imm) {
        self.emit_n(InstId::Psrad as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl<U1: Into<Imm>> PsradEmitter<Xmm, U1> for Assembler<'_> {
    fn psrad(&mut self, op0: Xmm, op1: U1) {
        self.emit_n(
            InstId::Psrad as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // SSE2
    }
}

impl PsrawEmitter<Mm, Mm> for Assembler<'_> {
    fn psraw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Psraw as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PsrawEmitter<Mm, Mem> for Assembler<'_> {
    fn psraw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Psraw as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl<U1: Into<Imm>> PsrawEmitter<Mm, U1> for Assembler<'_> {
    fn psraw(&mut self, op0: Mm, op1: U1) {
        self.emit_n(
            InstId::Psraw as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // MMX
    }
}

impl PsrawEmitter<Vec, Vec> for Assembler<'_> {
    fn psraw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Psraw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsrawEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psraw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Psraw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsrawEmitter<Vec, Mem> for Assembler<'_> {
    fn psraw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Psraw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsrawEmitter<Xmm, Mem> for Assembler<'_> {
    fn psraw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Psraw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsrawEmitter<Vec, Imm> for Assembler<'_> {
    fn psraw(&mut self, op0: Vec, op1: Imm) {
        self.emit_n(InstId::Psraw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl<U1: Into<Imm>> PsrawEmitter<Xmm, U1> for Assembler<'_> {
    fn psraw(&mut self, op0: Xmm, op1: U1) {
        self.emit_n(
            InstId::Psraw as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // SSE2
    }
}

impl PshufbEmitter<Mm, Mm> for Assembler<'_> {
    fn pshufb(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pshufb as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PshufbEmitter<Mm, Mem> for Assembler<'_> {
    fn pshufb(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pshufb as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PshufbEmitter<Vec, Vec> for Assembler<'_> {
    fn pshufb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pshufb as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PshufbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pshufb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pshufb as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PshufbEmitter<Vec, Mem> for Assembler<'_> {
    fn pshufb(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pshufb as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PshufbEmitter<Xmm, Mem> for Assembler<'_> {
    fn pshufb(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pshufb as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PshufdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn pshufd(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Pshufd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> PshufdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn pshufd(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Pshufd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE2
    }
}

impl PshufdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pshufd(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Pshufd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> PshufdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn pshufd(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Pshufd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE2
    }
}

impl PshufhwEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn pshufhw(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Pshufhw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> PshufhwEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn pshufhw(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Pshufhw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE2
    }
}

impl PshufhwEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pshufhw(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Pshufhw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> PshufhwEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn pshufhw(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Pshufhw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE2
    }
}

impl PshuflwEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn pshuflw(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Pshuflw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> PshuflwEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn pshuflw(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Pshuflw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE2
    }
}

impl PshuflwEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pshuflw(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Pshuflw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> PshuflwEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn pshuflw(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Pshuflw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> PshufwEmitter<Mm, Mm, U2> for Assembler<'_> {
    fn pshufw(&mut self, op0: Mm, op1: Mm, op2: U2) {
        self.emit_n(
            InstId::Pshufw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE
    }
}

impl<U2: Into<Imm>> PshufwEmitter<Mm, Mem, U2> for Assembler<'_> {
    fn pshufw(&mut self, op0: Mm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Pshufw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE
    }
}

impl PsignbEmitter<Mm, Mm> for Assembler<'_> {
    fn psignb(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Psignb as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PsignbEmitter<Mm, Mem> for Assembler<'_> {
    fn psignb(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Psignb as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PsignbEmitter<Vec, Vec> for Assembler<'_> {
    fn psignb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Psignb as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PsignbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psignb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Psignb as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PsignbEmitter<Vec, Mem> for Assembler<'_> {
    fn psignb(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Psignb as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PsignbEmitter<Xmm, Mem> for Assembler<'_> {
    fn psignb(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Psignb as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PsigndEmitter<Mm, Mm> for Assembler<'_> {
    fn psignd(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Psignd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PsigndEmitter<Mm, Mem> for Assembler<'_> {
    fn psignd(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Psignd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PsigndEmitter<Vec, Vec> for Assembler<'_> {
    fn psignd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Psignd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PsigndEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psignd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Psignd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PsigndEmitter<Vec, Mem> for Assembler<'_> {
    fn psignd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Psignd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PsigndEmitter<Xmm, Mem> for Assembler<'_> {
    fn psignd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Psignd as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PsignwEmitter<Mm, Mm> for Assembler<'_> {
    fn psignw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Psignw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PsignwEmitter<Mm, Mem> for Assembler<'_> {
    fn psignw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Psignw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PsignwEmitter<Vec, Vec> for Assembler<'_> {
    fn psignw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Psignw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PsignwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psignw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Psignw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PsignwEmitter<Vec, Mem> for Assembler<'_> {
    fn psignw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Psignw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PsignwEmitter<Xmm, Mem> for Assembler<'_> {
    fn psignw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Psignw as u32, &[op0.as_operand(), op1.as_operand()]); // SSSE3
    }
}

impl PsrldEmitter<Mm, Mm> for Assembler<'_> {
    fn psrld(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Psrld as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PsrldEmitter<Mm, Mem> for Assembler<'_> {
    fn psrld(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Psrld as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl<U1: Into<Imm>> PsrldEmitter<Mm, U1> for Assembler<'_> {
    fn psrld(&mut self, op0: Mm, op1: U1) {
        self.emit_n(
            InstId::Psrld as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // MMX
    }
}

impl PsrldEmitter<Vec, Vec> for Assembler<'_> {
    fn psrld(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Psrld as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsrldEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psrld(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Psrld as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsrldEmitter<Vec, Mem> for Assembler<'_> {
    fn psrld(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Psrld as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsrldEmitter<Xmm, Mem> for Assembler<'_> {
    fn psrld(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Psrld as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsrldEmitter<Vec, Imm> for Assembler<'_> {
    fn psrld(&mut self, op0: Vec, op1: Imm) {
        self.emit_n(InstId::Psrld as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl<U1: Into<Imm>> PsrldEmitter<Xmm, U1> for Assembler<'_> {
    fn psrld(&mut self, op0: Xmm, op1: U1) {
        self.emit_n(
            InstId::Psrld as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // SSE2
    }
}

impl PsrldqEmitter<Vec, Imm> for Assembler<'_> {
    fn psrldq(&mut self, op0: Vec, op1: Imm) {
        self.emit_n(InstId::Psrldq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl<U1: Into<Imm>> PsrldqEmitter<Xmm, U1> for Assembler<'_> {
    fn psrldq(&mut self, op0: Xmm, op1: U1) {
        self.emit_n(
            InstId::Psrldq as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // SSE2
    }
}

impl PsrlqEmitter<Mm, Mm> for Assembler<'_> {
    fn psrlq(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Psrlq as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PsrlqEmitter<Mm, Mem> for Assembler<'_> {
    fn psrlq(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Psrlq as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl<U1: Into<Imm>> PsrlqEmitter<Mm, U1> for Assembler<'_> {
    fn psrlq(&mut self, op0: Mm, op1: U1) {
        self.emit_n(
            InstId::Psrlq as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // MMX
    }
}

impl PsrlqEmitter<Vec, Vec> for Assembler<'_> {
    fn psrlq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Psrlq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsrlqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psrlq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Psrlq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsrlqEmitter<Vec, Mem> for Assembler<'_> {
    fn psrlq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Psrlq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsrlqEmitter<Xmm, Mem> for Assembler<'_> {
    fn psrlq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Psrlq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsrlqEmitter<Vec, Imm> for Assembler<'_> {
    fn psrlq(&mut self, op0: Vec, op1: Imm) {
        self.emit_n(InstId::Psrlq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl<U1: Into<Imm>> PsrlqEmitter<Xmm, U1> for Assembler<'_> {
    fn psrlq(&mut self, op0: Xmm, op1: U1) {
        self.emit_n(
            InstId::Psrlq as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // SSE2
    }
}

impl PsrlwEmitter<Mm, Mm> for Assembler<'_> {
    fn psrlw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Psrlw as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PsrlwEmitter<Mm, Mem> for Assembler<'_> {
    fn psrlw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Psrlw as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl<U1: Into<Imm>> PsrlwEmitter<Mm, U1> for Assembler<'_> {
    fn psrlw(&mut self, op0: Mm, op1: U1) {
        self.emit_n(
            InstId::Psrlw as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // MMX
    }
}

impl PsrlwEmitter<Vec, Vec> for Assembler<'_> {
    fn psrlw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Psrlw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsrlwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psrlw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Psrlw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsrlwEmitter<Vec, Mem> for Assembler<'_> {
    fn psrlw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Psrlw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsrlwEmitter<Xmm, Mem> for Assembler<'_> {
    fn psrlw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Psrlw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsrlwEmitter<Vec, Imm> for Assembler<'_> {
    fn psrlw(&mut self, op0: Vec, op1: Imm) {
        self.emit_n(InstId::Psrlw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl<U1: Into<Imm>> PsrlwEmitter<Xmm, U1> for Assembler<'_> {
    fn psrlw(&mut self, op0: Xmm, op1: U1) {
        self.emit_n(
            InstId::Psrlw as u32,
            &[op0.as_operand(), Into::<Imm>::into(op1).as_operand()],
        ); // SSE2
    }
}

impl PsubbEmitter<Mm, Mm> for Assembler<'_> {
    fn psubb(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Psubb as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PsubbEmitter<Mm, Mem> for Assembler<'_> {
    fn psubb(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Psubb as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PsubbEmitter<Vec, Vec> for Assembler<'_> {
    fn psubb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Psubb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psubb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Psubb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubbEmitter<Vec, Mem> for Assembler<'_> {
    fn psubb(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Psubb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubbEmitter<Xmm, Mem> for Assembler<'_> {
    fn psubb(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Psubb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubdEmitter<Mm, Mm> for Assembler<'_> {
    fn psubd(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Psubd as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PsubdEmitter<Mm, Mem> for Assembler<'_> {
    fn psubd(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Psubd as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PsubdEmitter<Vec, Vec> for Assembler<'_> {
    fn psubd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Psubd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psubd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Psubd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubdEmitter<Vec, Mem> for Assembler<'_> {
    fn psubd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Psubd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubdEmitter<Xmm, Mem> for Assembler<'_> {
    fn psubd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Psubd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubqEmitter<Mm, Mm> for Assembler<'_> {
    fn psubq(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Psubq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubqEmitter<Mm, Mem> for Assembler<'_> {
    fn psubq(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Psubq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubqEmitter<Vec, Vec> for Assembler<'_> {
    fn psubq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Psubq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psubq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Psubq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubqEmitter<Vec, Mem> for Assembler<'_> {
    fn psubq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Psubq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubqEmitter<Xmm, Mem> for Assembler<'_> {
    fn psubq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Psubq as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubsbEmitter<Mm, Mm> for Assembler<'_> {
    fn psubsb(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Psubsb as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PsubsbEmitter<Mm, Mem> for Assembler<'_> {
    fn psubsb(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Psubsb as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PsubsbEmitter<Vec, Vec> for Assembler<'_> {
    fn psubsb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Psubsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubsbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psubsb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Psubsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubsbEmitter<Vec, Mem> for Assembler<'_> {
    fn psubsb(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Psubsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubsbEmitter<Xmm, Mem> for Assembler<'_> {
    fn psubsb(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Psubsb as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubswEmitter<Mm, Mm> for Assembler<'_> {
    fn psubsw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Psubsw as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PsubswEmitter<Mm, Mem> for Assembler<'_> {
    fn psubsw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Psubsw as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PsubswEmitter<Vec, Vec> for Assembler<'_> {
    fn psubsw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Psubsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psubsw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Psubsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubswEmitter<Vec, Mem> for Assembler<'_> {
    fn psubsw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Psubsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubswEmitter<Xmm, Mem> for Assembler<'_> {
    fn psubsw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Psubsw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubusbEmitter<Mm, Mm> for Assembler<'_> {
    fn psubusb(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Psubusb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PsubusbEmitter<Mm, Mem> for Assembler<'_> {
    fn psubusb(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Psubusb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PsubusbEmitter<Vec, Vec> for Assembler<'_> {
    fn psubusb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Psubusb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PsubusbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psubusb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Psubusb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PsubusbEmitter<Vec, Mem> for Assembler<'_> {
    fn psubusb(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Psubusb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PsubusbEmitter<Xmm, Mem> for Assembler<'_> {
    fn psubusb(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Psubusb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PsubuswEmitter<Mm, Mm> for Assembler<'_> {
    fn psubusw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Psubusw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PsubuswEmitter<Mm, Mem> for Assembler<'_> {
    fn psubusw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Psubusw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PsubuswEmitter<Vec, Vec> for Assembler<'_> {
    fn psubusw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Psubusw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PsubuswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psubusw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Psubusw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PsubuswEmitter<Vec, Mem> for Assembler<'_> {
    fn psubusw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Psubusw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PsubuswEmitter<Xmm, Mem> for Assembler<'_> {
    fn psubusw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Psubusw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PsubwEmitter<Mm, Mm> for Assembler<'_> {
    fn psubw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Psubw as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PsubwEmitter<Mm, Mem> for Assembler<'_> {
    fn psubw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Psubw as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PsubwEmitter<Vec, Vec> for Assembler<'_> {
    fn psubw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Psubw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn psubw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Psubw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubwEmitter<Vec, Mem> for Assembler<'_> {
    fn psubw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Psubw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PsubwEmitter<Xmm, Mem> for Assembler<'_> {
    fn psubw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Psubw as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PtestEmitter<Vec, Vec> for Assembler<'_> {
    fn ptest(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Ptest as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PtestEmitter<Xmm, Xmm> for Assembler<'_> {
    fn ptest(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Ptest as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PtestEmitter<Vec, Mem> for Assembler<'_> {
    fn ptest(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Ptest as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PtestEmitter<Xmm, Mem> for Assembler<'_> {
    fn ptest(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Ptest as u32, &[op0.as_operand(), op1.as_operand()]); // SSE4_1
    }
}

impl PunpckhbwEmitter<Mm, Mm> for Assembler<'_> {
    fn punpckhbw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Punpckhbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PunpckhbwEmitter<Mm, Mem> for Assembler<'_> {
    fn punpckhbw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Punpckhbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PunpckhbwEmitter<Vec, Vec> for Assembler<'_> {
    fn punpckhbw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Punpckhbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpckhbwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn punpckhbw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Punpckhbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpckhbwEmitter<Vec, Mem> for Assembler<'_> {
    fn punpckhbw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Punpckhbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpckhbwEmitter<Xmm, Mem> for Assembler<'_> {
    fn punpckhbw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Punpckhbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpckhdqEmitter<Mm, Mm> for Assembler<'_> {
    fn punpckhdq(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Punpckhdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PunpckhdqEmitter<Mm, Mem> for Assembler<'_> {
    fn punpckhdq(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Punpckhdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PunpckhdqEmitter<Vec, Vec> for Assembler<'_> {
    fn punpckhdq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Punpckhdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpckhdqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn punpckhdq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Punpckhdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpckhdqEmitter<Vec, Mem> for Assembler<'_> {
    fn punpckhdq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Punpckhdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpckhdqEmitter<Xmm, Mem> for Assembler<'_> {
    fn punpckhdq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Punpckhdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpckhqdqEmitter<Vec, Vec> for Assembler<'_> {
    fn punpckhqdq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Punpckhqdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpckhqdqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn punpckhqdq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Punpckhqdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpckhqdqEmitter<Vec, Mem> for Assembler<'_> {
    fn punpckhqdq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Punpckhqdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpckhqdqEmitter<Xmm, Mem> for Assembler<'_> {
    fn punpckhqdq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Punpckhqdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpckhwdEmitter<Mm, Mm> for Assembler<'_> {
    fn punpckhwd(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Punpckhwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PunpckhwdEmitter<Mm, Mem> for Assembler<'_> {
    fn punpckhwd(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Punpckhwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PunpckhwdEmitter<Vec, Vec> for Assembler<'_> {
    fn punpckhwd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Punpckhwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpckhwdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn punpckhwd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Punpckhwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpckhwdEmitter<Vec, Mem> for Assembler<'_> {
    fn punpckhwd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Punpckhwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpckhwdEmitter<Xmm, Mem> for Assembler<'_> {
    fn punpckhwd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Punpckhwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpcklbwEmitter<Mm, Mm> for Assembler<'_> {
    fn punpcklbw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Punpcklbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PunpcklbwEmitter<Mm, Mem> for Assembler<'_> {
    fn punpcklbw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Punpcklbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PunpcklbwEmitter<Vec, Vec> for Assembler<'_> {
    fn punpcklbw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Punpcklbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpcklbwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn punpcklbw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Punpcklbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpcklbwEmitter<Vec, Mem> for Assembler<'_> {
    fn punpcklbw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Punpcklbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpcklbwEmitter<Xmm, Mem> for Assembler<'_> {
    fn punpcklbw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Punpcklbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpckldqEmitter<Mm, Mm> for Assembler<'_> {
    fn punpckldq(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Punpckldq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PunpckldqEmitter<Mm, Mem> for Assembler<'_> {
    fn punpckldq(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Punpckldq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PunpckldqEmitter<Vec, Vec> for Assembler<'_> {
    fn punpckldq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Punpckldq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpckldqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn punpckldq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Punpckldq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpckldqEmitter<Vec, Mem> for Assembler<'_> {
    fn punpckldq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Punpckldq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpckldqEmitter<Xmm, Mem> for Assembler<'_> {
    fn punpckldq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Punpckldq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpcklqdqEmitter<Vec, Vec> for Assembler<'_> {
    fn punpcklqdq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Punpcklqdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpcklqdqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn punpcklqdq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Punpcklqdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpcklqdqEmitter<Vec, Mem> for Assembler<'_> {
    fn punpcklqdq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Punpcklqdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpcklqdqEmitter<Xmm, Mem> for Assembler<'_> {
    fn punpcklqdq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Punpcklqdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpcklwdEmitter<Mm, Mm> for Assembler<'_> {
    fn punpcklwd(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Punpcklwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PunpcklwdEmitter<Mm, Mem> for Assembler<'_> {
    fn punpcklwd(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Punpcklwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // MMX
    }
}

impl PunpcklwdEmitter<Vec, Vec> for Assembler<'_> {
    fn punpcklwd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Punpcklwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpcklwdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn punpcklwd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Punpcklwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpcklwdEmitter<Vec, Mem> for Assembler<'_> {
    fn punpcklwd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Punpcklwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PunpcklwdEmitter<Xmm, Mem> for Assembler<'_> {
    fn punpcklwd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Punpcklwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl PxorEmitter<Mm, Mm> for Assembler<'_> {
    fn pxor(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pxor as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PxorEmitter<Mm, Mem> for Assembler<'_> {
    fn pxor(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pxor as u32, &[op0.as_operand(), op1.as_operand()]); // MMX
    }
}

impl PxorEmitter<Vec, Vec> for Assembler<'_> {
    fn pxor(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Pxor as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PxorEmitter<Xmm, Xmm> for Assembler<'_> {
    fn pxor(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Pxor as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PxorEmitter<Vec, Mem> for Assembler<'_> {
    fn pxor(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Pxor as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl PxorEmitter<Xmm, Mem> for Assembler<'_> {
    fn pxor(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Pxor as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl RcppsEmitter<Vec, Vec> for Assembler<'_> {
    fn rcpps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Rcpps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl RcppsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn rcpps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Rcpps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl RcppsEmitter<Vec, Mem> for Assembler<'_> {
    fn rcpps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Rcpps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl RcppsEmitter<Xmm, Mem> for Assembler<'_> {
    fn rcpps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Rcpps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl RcpssEmitter<Vec, Vec> for Assembler<'_> {
    fn rcpss(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Rcpss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl RcpssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn rcpss(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Rcpss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl RcpssEmitter<Vec, Mem> for Assembler<'_> {
    fn rcpss(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Rcpss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl RcpssEmitter<Xmm, Mem> for Assembler<'_> {
    fn rcpss(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Rcpss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl RoundpdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn roundpd(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Roundpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> RoundpdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn roundpd(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Roundpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl RoundpdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn roundpd(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Roundpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> RoundpdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn roundpd(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Roundpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl RoundpsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn roundps(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Roundps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> RoundpsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn roundps(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Roundps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl RoundpsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn roundps(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Roundps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> RoundpsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn roundps(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Roundps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl RoundsdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn roundsd(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Roundsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> RoundsdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn roundsd(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Roundsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl RoundsdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn roundsd(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Roundsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> RoundsdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn roundsd(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Roundsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl RoundssEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn roundss(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Roundss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> RoundssEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn roundss(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Roundss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl RoundssEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn roundss(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Roundss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1
    }
}

impl<U2: Into<Imm>> RoundssEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn roundss(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Roundss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE4_1
    }
}

impl RsqrtpsEmitter<Vec, Vec> for Assembler<'_> {
    fn rsqrtps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Rsqrtps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl RsqrtpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn rsqrtps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Rsqrtps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl RsqrtpsEmitter<Vec, Mem> for Assembler<'_> {
    fn rsqrtps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Rsqrtps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl RsqrtpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn rsqrtps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Rsqrtps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl RsqrtssEmitter<Vec, Vec> for Assembler<'_> {
    fn rsqrtss(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Rsqrtss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl RsqrtssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn rsqrtss(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Rsqrtss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl RsqrtssEmitter<Vec, Mem> for Assembler<'_> {
    fn rsqrtss(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Rsqrtss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl RsqrtssEmitter<Xmm, Mem> for Assembler<'_> {
    fn rsqrtss(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Rsqrtss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl ShufpdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn shufpd(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Shufpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> ShufpdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn shufpd(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Shufpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE2
    }
}

impl ShufpdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn shufpd(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Shufpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE2
    }
}

impl<U2: Into<Imm>> ShufpdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn shufpd(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Shufpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE2
    }
}

impl ShufpsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn shufps(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Shufps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE
    }
}

impl<U2: Into<Imm>> ShufpsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn shufps(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Shufps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE
    }
}

impl ShufpsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn shufps(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Shufps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE
    }
}

impl<U2: Into<Imm>> ShufpsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn shufps(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Shufps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SSE
    }
}

impl SqrtpdEmitter<Vec, Vec> for Assembler<'_> {
    fn sqrtpd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Sqrtpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl SqrtpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn sqrtpd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Sqrtpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl SqrtpdEmitter<Vec, Mem> for Assembler<'_> {
    fn sqrtpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Sqrtpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl SqrtpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn sqrtpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Sqrtpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl SqrtpsEmitter<Vec, Vec> for Assembler<'_> {
    fn sqrtps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Sqrtps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl SqrtpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn sqrtps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Sqrtps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl SqrtpsEmitter<Vec, Mem> for Assembler<'_> {
    fn sqrtps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Sqrtps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl SqrtpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn sqrtps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Sqrtps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl SqrtsdEmitter<Vec, Vec> for Assembler<'_> {
    fn sqrtsd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Sqrtsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl SqrtsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn sqrtsd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Sqrtsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl SqrtsdEmitter<Vec, Mem> for Assembler<'_> {
    fn sqrtsd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Sqrtsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl SqrtsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn sqrtsd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Sqrtsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl SqrtssEmitter<Vec, Vec> for Assembler<'_> {
    fn sqrtss(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Sqrtss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl SqrtssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn sqrtss(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Sqrtss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl SqrtssEmitter<Vec, Mem> for Assembler<'_> {
    fn sqrtss(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Sqrtss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl SqrtssEmitter<Xmm, Mem> for Assembler<'_> {
    fn sqrtss(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Sqrtss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl SubpdEmitter<Vec, Vec> for Assembler<'_> {
    fn subpd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Subpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl SubpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn subpd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Subpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl SubpdEmitter<Vec, Mem> for Assembler<'_> {
    fn subpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Subpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl SubpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn subpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Subpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl SubpsEmitter<Vec, Vec> for Assembler<'_> {
    fn subps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Subps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl SubpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn subps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Subps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl SubpsEmitter<Vec, Mem> for Assembler<'_> {
    fn subps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Subps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl SubpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn subps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Subps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl SubsdEmitter<Vec, Vec> for Assembler<'_> {
    fn subsd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Subsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl SubsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn subsd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Subsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl SubsdEmitter<Vec, Mem> for Assembler<'_> {
    fn subsd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Subsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl SubsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn subsd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Subsd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl SubssEmitter<Vec, Vec> for Assembler<'_> {
    fn subss(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Subss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl SubssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn subss(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Subss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl SubssEmitter<Vec, Mem> for Assembler<'_> {
    fn subss(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Subss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl SubssEmitter<Xmm, Mem> for Assembler<'_> {
    fn subss(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Subss as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl UcomisdEmitter<Vec, Vec> for Assembler<'_> {
    fn ucomisd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Ucomisd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl UcomisdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn ucomisd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Ucomisd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl UcomisdEmitter<Vec, Mem> for Assembler<'_> {
    fn ucomisd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Ucomisd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl UcomisdEmitter<Xmm, Mem> for Assembler<'_> {
    fn ucomisd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Ucomisd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl UcomissEmitter<Vec, Vec> for Assembler<'_> {
    fn ucomiss(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Ucomiss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl UcomissEmitter<Xmm, Xmm> for Assembler<'_> {
    fn ucomiss(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Ucomiss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl UcomissEmitter<Vec, Mem> for Assembler<'_> {
    fn ucomiss(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Ucomiss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl UcomissEmitter<Xmm, Mem> for Assembler<'_> {
    fn ucomiss(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Ucomiss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl UnpckhpdEmitter<Vec, Vec> for Assembler<'_> {
    fn unpckhpd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Unpckhpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl UnpckhpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn unpckhpd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Unpckhpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl UnpckhpdEmitter<Vec, Mem> for Assembler<'_> {
    fn unpckhpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Unpckhpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl UnpckhpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn unpckhpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Unpckhpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl UnpckhpsEmitter<Vec, Vec> for Assembler<'_> {
    fn unpckhps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Unpckhps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl UnpckhpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn unpckhps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Unpckhps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl UnpckhpsEmitter<Vec, Mem> for Assembler<'_> {
    fn unpckhps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Unpckhps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl UnpckhpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn unpckhps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Unpckhps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl UnpcklpdEmitter<Vec, Vec> for Assembler<'_> {
    fn unpcklpd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Unpcklpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl UnpcklpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn unpcklpd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Unpcklpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl UnpcklpdEmitter<Vec, Mem> for Assembler<'_> {
    fn unpcklpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Unpcklpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl UnpcklpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn unpcklpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Unpcklpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2
    }
}

impl UnpcklpsEmitter<Vec, Vec> for Assembler<'_> {
    fn unpcklps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Unpcklps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl UnpcklpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn unpcklps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Unpcklps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl UnpcklpsEmitter<Vec, Mem> for Assembler<'_> {
    fn unpcklps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Unpcklps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl UnpcklpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn unpcklps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Unpcklps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE
    }
}

impl XorpdEmitter<Vec, Vec> for Assembler<'_> {
    fn xorpd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Xorpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl XorpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn xorpd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Xorpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl XorpdEmitter<Vec, Mem> for Assembler<'_> {
    fn xorpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Xorpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl XorpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn xorpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Xorpd as u32, &[op0.as_operand(), op1.as_operand()]); // SSE2
    }
}

impl XorpsEmitter<Vec, Vec> for Assembler<'_> {
    fn xorps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Xorps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl XorpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn xorps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Xorps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl XorpsEmitter<Vec, Mem> for Assembler<'_> {
    fn xorps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Xorps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl XorpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn xorps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Xorps as u32, &[op0.as_operand(), op1.as_operand()]); // SSE
    }
}

impl PavgusbEmitter<Mm, Mm> for Assembler<'_> {
    fn pavgusb(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pavgusb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // 3DNOW
    }
}

impl PavgusbEmitter<Mm, Mem> for Assembler<'_> {
    fn pavgusb(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pavgusb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // 3DNOW
    }
}

impl Pf2idEmitter<Mm, Mm> for Assembler<'_> {
    fn pf2id(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pf2id as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl Pf2idEmitter<Mm, Mem> for Assembler<'_> {
    fn pf2id(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pf2id as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl Pf2iwEmitter<Mm, Mm> for Assembler<'_> {
    fn pf2iw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pf2iw as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl Pf2iwEmitter<Mm, Mem> for Assembler<'_> {
    fn pf2iw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pf2iw as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl PfaccEmitter<Mm, Mm> for Assembler<'_> {
    fn pfacc(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pfacc as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl PfaccEmitter<Mm, Mem> for Assembler<'_> {
    fn pfacc(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pfacc as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl PfaddEmitter<Mm, Mm> for Assembler<'_> {
    fn pfadd(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pfadd as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl PfaddEmitter<Mm, Mem> for Assembler<'_> {
    fn pfadd(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pfadd as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl PfcmpeqEmitter<Mm, Mm> for Assembler<'_> {
    fn pfcmpeq(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pfcmpeq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // 3DNOW
    }
}

impl PfcmpeqEmitter<Mm, Mem> for Assembler<'_> {
    fn pfcmpeq(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pfcmpeq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // 3DNOW
    }
}

impl PfcmpgeEmitter<Mm, Mm> for Assembler<'_> {
    fn pfcmpge(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pfcmpge as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // 3DNOW
    }
}

impl PfcmpgeEmitter<Mm, Mem> for Assembler<'_> {
    fn pfcmpge(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pfcmpge as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // 3DNOW
    }
}

impl PfcmpgtEmitter<Mm, Mm> for Assembler<'_> {
    fn pfcmpgt(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pfcmpgt as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // 3DNOW
    }
}

impl PfcmpgtEmitter<Mm, Mem> for Assembler<'_> {
    fn pfcmpgt(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pfcmpgt as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // 3DNOW
    }
}

impl PfmaxEmitter<Mm, Mm> for Assembler<'_> {
    fn pfmax(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pfmax as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl PfmaxEmitter<Mm, Mem> for Assembler<'_> {
    fn pfmax(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pfmax as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl PfminEmitter<Mm, Mm> for Assembler<'_> {
    fn pfmin(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pfmin as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl PfminEmitter<Mm, Mem> for Assembler<'_> {
    fn pfmin(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pfmin as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl PfmulEmitter<Mm, Mm> for Assembler<'_> {
    fn pfmul(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pfmul as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl PfmulEmitter<Mm, Mem> for Assembler<'_> {
    fn pfmul(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pfmul as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl PfnaccEmitter<Mm, Mm> for Assembler<'_> {
    fn pfnacc(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pfnacc as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl PfnaccEmitter<Mm, Mem> for Assembler<'_> {
    fn pfnacc(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pfnacc as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl PfpnaccEmitter<Mm, Mm> for Assembler<'_> {
    fn pfpnacc(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pfpnacc as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // 3DNOW
    }
}

impl PfpnaccEmitter<Mm, Mem> for Assembler<'_> {
    fn pfpnacc(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pfpnacc as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // 3DNOW
    }
}

impl PfrcpEmitter<Mm, Mm> for Assembler<'_> {
    fn pfrcp(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pfrcp as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl PfrcpEmitter<Mm, Mem> for Assembler<'_> {
    fn pfrcp(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pfrcp as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl Pfrcpit1Emitter<Mm, Mm> for Assembler<'_> {
    fn pfrcpit1(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pfrcpit1 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // 3DNOW
    }
}

impl Pfrcpit1Emitter<Mm, Mem> for Assembler<'_> {
    fn pfrcpit1(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pfrcpit1 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // 3DNOW
    }
}

impl Pfrcpit2Emitter<Mm, Mm> for Assembler<'_> {
    fn pfrcpit2(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pfrcpit2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // 3DNOW
    }
}

impl Pfrcpit2Emitter<Mm, Mem> for Assembler<'_> {
    fn pfrcpit2(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pfrcpit2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // 3DNOW
    }
}

impl PfrcpvEmitter<Mm, Mm> for Assembler<'_> {
    fn pfrcpv(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pfrcpv as u32, &[op0.as_operand(), op1.as_operand()]); // GEODE
    }
}

impl PfrcpvEmitter<Mm, Mem> for Assembler<'_> {
    fn pfrcpv(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pfrcpv as u32, &[op0.as_operand(), op1.as_operand()]); // GEODE
    }
}

impl Pfrsqit1Emitter<Mm, Mm> for Assembler<'_> {
    fn pfrsqit1(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pfrsqit1 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // 3DNOW
    }
}

impl Pfrsqit1Emitter<Mm, Mem> for Assembler<'_> {
    fn pfrsqit1(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pfrsqit1 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // 3DNOW
    }
}

impl PfrsqrtEmitter<Mm, Mm> for Assembler<'_> {
    fn pfrsqrt(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pfrsqrt as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // 3DNOW
    }
}

impl PfrsqrtEmitter<Mm, Mem> for Assembler<'_> {
    fn pfrsqrt(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pfrsqrt as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // 3DNOW
    }
}

impl PfrsqrtvEmitter<Mm, Mm> for Assembler<'_> {
    fn pfrsqrtv(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pfrsqrtv as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // GEODE
    }
}

impl PfrsqrtvEmitter<Mm, Mem> for Assembler<'_> {
    fn pfrsqrtv(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pfrsqrtv as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // GEODE
    }
}

impl PfsubEmitter<Mm, Mm> for Assembler<'_> {
    fn pfsub(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pfsub as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl PfsubEmitter<Mm, Mem> for Assembler<'_> {
    fn pfsub(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pfsub as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl PfsubrEmitter<Mm, Mm> for Assembler<'_> {
    fn pfsubr(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pfsubr as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl PfsubrEmitter<Mm, Mem> for Assembler<'_> {
    fn pfsubr(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pfsubr as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl Pi2fdEmitter<Mm, Mm> for Assembler<'_> {
    fn pi2fd(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pi2fd as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl Pi2fdEmitter<Mm, Mem> for Assembler<'_> {
    fn pi2fd(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pi2fd as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl Pi2fwEmitter<Mm, Mm> for Assembler<'_> {
    fn pi2fw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pi2fw as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl Pi2fwEmitter<Mm, Mem> for Assembler<'_> {
    fn pi2fw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pi2fw as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl PmulhrwEmitter<Mm, Mm> for Assembler<'_> {
    fn pmulhrw(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Pmulhrw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // 3DNOW
    }
}

impl PmulhrwEmitter<Mm, Mem> for Assembler<'_> {
    fn pmulhrw(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(
            InstId::Pmulhrw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // 3DNOW
    }
}

impl PswapdEmitter<Mm, Mm> for Assembler<'_> {
    fn pswapd(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(InstId::Pswapd as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl PswapdEmitter<Mm, Mem> for Assembler<'_> {
    fn pswapd(&mut self, op0: Mm, op1: Mem) {
        self.emit_n(InstId::Pswapd as u32, &[op0.as_operand(), op1.as_operand()]); // 3DNOW
    }
}

impl EmmsEmitter for Assembler<'_> {
    fn emms(&mut self) {
        self.emit_n(InstId::Emms as u32, &[]); // MMX
    }
}

impl FemmsEmitter for Assembler<'_> {
    fn femms(&mut self) {
        self.emit_n(InstId::Femms as u32, &[]); // 3DNOW
    }
}

impl AesdecEmitter<Vec, Vec> for Assembler<'_> {
    fn aesdec(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Aesdec as u32, &[op0.as_operand(), op1.as_operand()]); // AESNI
    }
}

impl AesdecEmitter<Xmm, Xmm> for Assembler<'_> {
    fn aesdec(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Aesdec as u32, &[op0.as_operand(), op1.as_operand()]); // AESNI
    }
}

impl AesdecEmitter<Vec, Mem> for Assembler<'_> {
    fn aesdec(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Aesdec as u32, &[op0.as_operand(), op1.as_operand()]); // AESNI
    }
}

impl AesdecEmitter<Xmm, Mem> for Assembler<'_> {
    fn aesdec(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Aesdec as u32, &[op0.as_operand(), op1.as_operand()]); // AESNI
    }
}

impl AesdeclastEmitter<Vec, Vec> for Assembler<'_> {
    fn aesdeclast(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Aesdeclast as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AESNI
    }
}

impl AesdeclastEmitter<Xmm, Xmm> for Assembler<'_> {
    fn aesdeclast(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Aesdeclast as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AESNI
    }
}

impl AesdeclastEmitter<Vec, Mem> for Assembler<'_> {
    fn aesdeclast(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Aesdeclast as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AESNI
    }
}

impl AesdeclastEmitter<Xmm, Mem> for Assembler<'_> {
    fn aesdeclast(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Aesdeclast as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AESNI
    }
}

impl AesencEmitter<Vec, Vec> for Assembler<'_> {
    fn aesenc(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Aesenc as u32, &[op0.as_operand(), op1.as_operand()]); // AESNI
    }
}

impl AesencEmitter<Xmm, Xmm> for Assembler<'_> {
    fn aesenc(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Aesenc as u32, &[op0.as_operand(), op1.as_operand()]); // AESNI
    }
}

impl AesencEmitter<Vec, Mem> for Assembler<'_> {
    fn aesenc(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Aesenc as u32, &[op0.as_operand(), op1.as_operand()]); // AESNI
    }
}

impl AesencEmitter<Xmm, Mem> for Assembler<'_> {
    fn aesenc(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Aesenc as u32, &[op0.as_operand(), op1.as_operand()]); // AESNI
    }
}

impl AesenclastEmitter<Vec, Vec> for Assembler<'_> {
    fn aesenclast(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Aesenclast as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AESNI
    }
}

impl AesenclastEmitter<Xmm, Xmm> for Assembler<'_> {
    fn aesenclast(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Aesenclast as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AESNI
    }
}

impl AesenclastEmitter<Vec, Mem> for Assembler<'_> {
    fn aesenclast(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Aesenclast as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AESNI
    }
}

impl AesenclastEmitter<Xmm, Mem> for Assembler<'_> {
    fn aesenclast(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Aesenclast as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AESNI
    }
}

impl AesimcEmitter<Vec, Vec> for Assembler<'_> {
    fn aesimc(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Aesimc as u32, &[op0.as_operand(), op1.as_operand()]); // AESNI
    }
}

impl AesimcEmitter<Xmm, Xmm> for Assembler<'_> {
    fn aesimc(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Aesimc as u32, &[op0.as_operand(), op1.as_operand()]); // AESNI
    }
}

impl AesimcEmitter<Vec, Mem> for Assembler<'_> {
    fn aesimc(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Aesimc as u32, &[op0.as_operand(), op1.as_operand()]); // AESNI
    }
}

impl AesimcEmitter<Xmm, Mem> for Assembler<'_> {
    fn aesimc(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Aesimc as u32, &[op0.as_operand(), op1.as_operand()]); // AESNI
    }
}

impl AeskeygenassistEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn aeskeygenassist(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Aeskeygenassist as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AESNI
    }
}

impl<U2: Into<Imm>> AeskeygenassistEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn aeskeygenassist(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Aeskeygenassist as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AESNI
    }
}

impl AeskeygenassistEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn aeskeygenassist(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Aeskeygenassist as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AESNI
    }
}

impl<U2: Into<Imm>> AeskeygenassistEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn aeskeygenassist(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Aeskeygenassist as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AESNI
    }
}

impl Sha1msg1Emitter<Vec, Vec> for Assembler<'_> {
    fn sha1msg1(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Sha1msg1 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA
    }
}

impl Sha1msg1Emitter<Xmm, Xmm> for Assembler<'_> {
    fn sha1msg1(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Sha1msg1 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA
    }
}

impl Sha1msg1Emitter<Vec, Mem> for Assembler<'_> {
    fn sha1msg1(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Sha1msg1 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA
    }
}

impl Sha1msg1Emitter<Xmm, Mem> for Assembler<'_> {
    fn sha1msg1(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Sha1msg1 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA
    }
}

impl Sha1msg2Emitter<Vec, Vec> for Assembler<'_> {
    fn sha1msg2(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Sha1msg2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA
    }
}

impl Sha1msg2Emitter<Xmm, Xmm> for Assembler<'_> {
    fn sha1msg2(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Sha1msg2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA
    }
}

impl Sha1msg2Emitter<Vec, Mem> for Assembler<'_> {
    fn sha1msg2(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Sha1msg2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA
    }
}

impl Sha1msg2Emitter<Xmm, Mem> for Assembler<'_> {
    fn sha1msg2(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Sha1msg2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA
    }
}

impl Sha1nexteEmitter<Vec, Vec> for Assembler<'_> {
    fn sha1nexte(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Sha1nexte as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA
    }
}

impl Sha1nexteEmitter<Xmm, Xmm> for Assembler<'_> {
    fn sha1nexte(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Sha1nexte as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA
    }
}

impl Sha1nexteEmitter<Vec, Mem> for Assembler<'_> {
    fn sha1nexte(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Sha1nexte as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA
    }
}

impl Sha1nexteEmitter<Xmm, Mem> for Assembler<'_> {
    fn sha1nexte(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Sha1nexte as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA
    }
}

impl Sha1rnds4Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn sha1rnds4(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Sha1rnds4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SHA
    }
}

impl<U2: Into<Imm>> Sha1rnds4Emitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn sha1rnds4(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Sha1rnds4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SHA
    }
}

impl Sha1rnds4Emitter<Vec, Mem, Imm> for Assembler<'_> {
    fn sha1rnds4(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Sha1rnds4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SHA
    }
}

impl<U2: Into<Imm>> Sha1rnds4Emitter<Xmm, Mem, U2> for Assembler<'_> {
    fn sha1rnds4(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Sha1rnds4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // SHA
    }
}

impl Sha256msg1Emitter<Vec, Vec> for Assembler<'_> {
    fn sha256msg1(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Sha256msg1 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA
    }
}

impl Sha256msg1Emitter<Xmm, Xmm> for Assembler<'_> {
    fn sha256msg1(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Sha256msg1 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA
    }
}

impl Sha256msg1Emitter<Vec, Mem> for Assembler<'_> {
    fn sha256msg1(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Sha256msg1 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA
    }
}

impl Sha256msg1Emitter<Xmm, Mem> for Assembler<'_> {
    fn sha256msg1(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Sha256msg1 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA
    }
}

impl Sha256msg2Emitter<Vec, Vec> for Assembler<'_> {
    fn sha256msg2(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Sha256msg2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA
    }
}

impl Sha256msg2Emitter<Xmm, Xmm> for Assembler<'_> {
    fn sha256msg2(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Sha256msg2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA
    }
}

impl Sha256msg2Emitter<Vec, Mem> for Assembler<'_> {
    fn sha256msg2(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Sha256msg2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA
    }
}

impl Sha256msg2Emitter<Xmm, Mem> for Assembler<'_> {
    fn sha256msg2(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Sha256msg2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA
    }
}

impl Sha256rnds2Emitter<Vec, Vec, Vec> for Assembler<'_> {
    fn sha256rnds2(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Sha256rnds2 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SHA [EXPLICIT]
    }
}

impl Sha256rnds2Emitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn sha256rnds2(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Sha256rnds2 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SHA [EXPLICIT]
    }
}

impl Sha256rnds2Emitter<Vec, Mem, Vec> for Assembler<'_> {
    fn sha256rnds2(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Sha256rnds2 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SHA [EXPLICIT]
    }
}

impl Sha256rnds2Emitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn sha256rnds2(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Sha256rnds2 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SHA [EXPLICIT]
    }
}

impl Gf2p8affineinvqbEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn gf2p8affineinvqb(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Gf2p8affineinvqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // GFNI
    }
}

impl<U2: Into<Imm>> Gf2p8affineinvqbEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn gf2p8affineinvqb(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Gf2p8affineinvqb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // GFNI
    }
}

impl Gf2p8affineinvqbEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn gf2p8affineinvqb(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Gf2p8affineinvqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // GFNI
    }
}

impl<U2: Into<Imm>> Gf2p8affineinvqbEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn gf2p8affineinvqb(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Gf2p8affineinvqb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // GFNI
    }
}

impl Gf2p8affineqbEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn gf2p8affineqb(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Gf2p8affineqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // GFNI
    }
}

impl<U2: Into<Imm>> Gf2p8affineqbEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn gf2p8affineqb(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Gf2p8affineqb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // GFNI
    }
}

impl Gf2p8affineqbEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn gf2p8affineqb(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Gf2p8affineqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // GFNI
    }
}

impl<U2: Into<Imm>> Gf2p8affineqbEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn gf2p8affineqb(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Gf2p8affineqb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // GFNI
    }
}

impl Gf2p8mulbEmitter<Vec, Vec> for Assembler<'_> {
    fn gf2p8mulb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Gf2p8mulb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // GFNI
    }
}

impl Gf2p8mulbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn gf2p8mulb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Gf2p8mulb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // GFNI
    }
}

impl Gf2p8mulbEmitter<Vec, Mem> for Assembler<'_> {
    fn gf2p8mulb(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Gf2p8mulb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // GFNI
    }
}

impl Gf2p8mulbEmitter<Xmm, Mem> for Assembler<'_> {
    fn gf2p8mulb(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Gf2p8mulb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // GFNI
    }
}

impl KaddbEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kaddb(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kaddb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ
    }
}

impl KadddEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kaddd(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kaddd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW
    }
}

impl KaddqEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kaddq(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kaddq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW
    }
}

impl KaddwEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kaddw(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kaddw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ
    }
}

impl KandbEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kandb(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kandb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ
    }
}

impl KanddEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kandd(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kandd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW
    }
}

impl KandnbEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kandnb(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kandnb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ
    }
}

impl KandndEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kandnd(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kandnd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW
    }
}

impl KandnqEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kandnq(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kandnq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW
    }
}

impl KandnwEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kandnw(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kandnw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F
    }
}

impl KandqEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kandq(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kandq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW
    }
}

impl KandwEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kandw(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kandw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F
    }
}

impl KmovbEmitter<KReg, KReg> for Assembler<'_> {
    fn kmovb(&mut self, op0: KReg, op1: KReg) {
        self.emit_n(InstId::Kmovb as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_DQ
    }
}

impl KmovbEmitter<KReg, Mem> for Assembler<'_> {
    fn kmovb(&mut self, op0: KReg, op1: Mem) {
        self.emit_n(InstId::Kmovb as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_DQ
    }
}

impl KmovbEmitter<KReg, Gp> for Assembler<'_> {
    fn kmovb(&mut self, op0: KReg, op1: Gp) {
        self.emit_n(InstId::Kmovb as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_DQ
    }
}

impl KmovbEmitter<KReg, Gpd> for Assembler<'_> {
    fn kmovb(&mut self, op0: KReg, op1: Gpd) {
        self.emit_n(InstId::Kmovb as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_DQ
    }
}

impl KmovbEmitter<Mem, KReg> for Assembler<'_> {
    fn kmovb(&mut self, op0: Mem, op1: KReg) {
        self.emit_n(InstId::Kmovb as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_DQ
    }
}

impl KmovbEmitter<Gp, KReg> for Assembler<'_> {
    fn kmovb(&mut self, op0: Gp, op1: KReg) {
        self.emit_n(InstId::Kmovb as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_DQ
    }
}

impl KmovbEmitter<Gpd, KReg> for Assembler<'_> {
    fn kmovb(&mut self, op0: Gpd, op1: KReg) {
        self.emit_n(InstId::Kmovb as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_DQ
    }
}

impl KmovdEmitter<KReg, KReg> for Assembler<'_> {
    fn kmovd(&mut self, op0: KReg, op1: KReg) {
        self.emit_n(InstId::Kmovd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_BW
    }
}

impl KmovdEmitter<KReg, Mem> for Assembler<'_> {
    fn kmovd(&mut self, op0: KReg, op1: Mem) {
        self.emit_n(InstId::Kmovd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_BW
    }
}

impl KmovdEmitter<KReg, Gp> for Assembler<'_> {
    fn kmovd(&mut self, op0: KReg, op1: Gp) {
        self.emit_n(InstId::Kmovd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_BW
    }
}

impl KmovdEmitter<KReg, Gpd> for Assembler<'_> {
    fn kmovd(&mut self, op0: KReg, op1: Gpd) {
        self.emit_n(InstId::Kmovd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_BW
    }
}

impl KmovdEmitter<Mem, KReg> for Assembler<'_> {
    fn kmovd(&mut self, op0: Mem, op1: KReg) {
        self.emit_n(InstId::Kmovd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_BW
    }
}

impl KmovdEmitter<Gp, KReg> for Assembler<'_> {
    fn kmovd(&mut self, op0: Gp, op1: KReg) {
        self.emit_n(InstId::Kmovd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_BW
    }
}

impl KmovdEmitter<Gpd, KReg> for Assembler<'_> {
    fn kmovd(&mut self, op0: Gpd, op1: KReg) {
        self.emit_n(InstId::Kmovd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_BW
    }
}

impl KmovqEmitter<KReg, KReg> for Assembler<'_> {
    fn kmovq(&mut self, op0: KReg, op1: KReg) {
        self.emit_n(InstId::Kmovq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_BW
    }
}

impl KmovqEmitter<KReg, Mem> for Assembler<'_> {
    fn kmovq(&mut self, op0: KReg, op1: Mem) {
        self.emit_n(InstId::Kmovq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_BW
    }
}

impl KmovqEmitter<KReg, Gp> for Assembler<'_> {
    fn kmovq(&mut self, op0: KReg, op1: Gp) {
        self.emit_n(InstId::Kmovq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_BW
    }
}

impl KmovqEmitter<KReg, Gpq> for Assembler<'_> {
    fn kmovq(&mut self, op0: KReg, op1: Gpq) {
        self.emit_n(InstId::Kmovq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_BW
    }
}

impl KmovqEmitter<Mem, KReg> for Assembler<'_> {
    fn kmovq(&mut self, op0: Mem, op1: KReg) {
        self.emit_n(InstId::Kmovq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_BW
    }
}

impl KmovqEmitter<Gp, KReg> for Assembler<'_> {
    fn kmovq(&mut self, op0: Gp, op1: KReg) {
        self.emit_n(InstId::Kmovq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_BW
    }
}

impl KmovqEmitter<Gpq, KReg> for Assembler<'_> {
    fn kmovq(&mut self, op0: Gpq, op1: KReg) {
        self.emit_n(InstId::Kmovq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_BW
    }
}

impl KmovwEmitter<KReg, KReg> for Assembler<'_> {
    fn kmovw(&mut self, op0: KReg, op1: KReg) {
        self.emit_n(InstId::Kmovw as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_F
    }
}

impl KmovwEmitter<KReg, Mem> for Assembler<'_> {
    fn kmovw(&mut self, op0: KReg, op1: Mem) {
        self.emit_n(InstId::Kmovw as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_F
    }
}

impl KmovwEmitter<KReg, Gp> for Assembler<'_> {
    fn kmovw(&mut self, op0: KReg, op1: Gp) {
        self.emit_n(InstId::Kmovw as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_F
    }
}

impl KmovwEmitter<KReg, Gpd> for Assembler<'_> {
    fn kmovw(&mut self, op0: KReg, op1: Gpd) {
        self.emit_n(InstId::Kmovw as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_F
    }
}

impl KmovwEmitter<Mem, KReg> for Assembler<'_> {
    fn kmovw(&mut self, op0: Mem, op1: KReg) {
        self.emit_n(InstId::Kmovw as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_F
    }
}

impl KmovwEmitter<Gp, KReg> for Assembler<'_> {
    fn kmovw(&mut self, op0: Gp, op1: KReg) {
        self.emit_n(InstId::Kmovw as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_F
    }
}

impl KmovwEmitter<Gpd, KReg> for Assembler<'_> {
    fn kmovw(&mut self, op0: Gpd, op1: KReg) {
        self.emit_n(InstId::Kmovw as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_F
    }
}

impl KnotbEmitter<KReg, KReg> for Assembler<'_> {
    fn knotb(&mut self, op0: KReg, op1: KReg) {
        self.emit_n(InstId::Knotb as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_DQ
    }
}

impl KnotdEmitter<KReg, KReg> for Assembler<'_> {
    fn knotd(&mut self, op0: KReg, op1: KReg) {
        self.emit_n(InstId::Knotd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_BW
    }
}

impl KnotqEmitter<KReg, KReg> for Assembler<'_> {
    fn knotq(&mut self, op0: KReg, op1: KReg) {
        self.emit_n(InstId::Knotq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_BW
    }
}

impl KnotwEmitter<KReg, KReg> for Assembler<'_> {
    fn knotw(&mut self, op0: KReg, op1: KReg) {
        self.emit_n(InstId::Knotw as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_F
    }
}

impl KorbEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn korb(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Korb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ
    }
}

impl KordEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kord(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kord as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW
    }
}

impl KorqEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn korq(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Korq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW
    }
}

impl KortestbEmitter<KReg, KReg> for Assembler<'_> {
    fn kortestb(&mut self, op0: KReg, op1: KReg) {
        self.emit_n(
            InstId::Kortestb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ
    }
}

impl KortestdEmitter<KReg, KReg> for Assembler<'_> {
    fn kortestd(&mut self, op0: KReg, op1: KReg) {
        self.emit_n(
            InstId::Kortestd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW
    }
}

impl KortestqEmitter<KReg, KReg> for Assembler<'_> {
    fn kortestq(&mut self, op0: KReg, op1: KReg) {
        self.emit_n(
            InstId::Kortestq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW
    }
}

impl KortestwEmitter<KReg, KReg> for Assembler<'_> {
    fn kortestw(&mut self, op0: KReg, op1: KReg) {
        self.emit_n(
            InstId::Kortestw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F
    }
}

impl KorwEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn korw(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Korw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F
    }
}

impl<U2: Into<Imm>> KshiftlbEmitter<KReg, KReg, U2> for Assembler<'_> {
    fn kshiftlb(&mut self, op0: KReg, op1: KReg, op2: U2) {
        self.emit_n(
            InstId::Kshiftlb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ
    }
}

impl<U2: Into<Imm>> KshiftldEmitter<KReg, KReg, U2> for Assembler<'_> {
    fn kshiftld(&mut self, op0: KReg, op1: KReg, op2: U2) {
        self.emit_n(
            InstId::Kshiftld as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_BW
    }
}

impl<U2: Into<Imm>> KshiftlqEmitter<KReg, KReg, U2> for Assembler<'_> {
    fn kshiftlq(&mut self, op0: KReg, op1: KReg, op2: U2) {
        self.emit_n(
            InstId::Kshiftlq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_BW
    }
}

impl<U2: Into<Imm>> KshiftlwEmitter<KReg, KReg, U2> for Assembler<'_> {
    fn kshiftlw(&mut self, op0: KReg, op1: KReg, op2: U2) {
        self.emit_n(
            InstId::Kshiftlw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F
    }
}

impl<U2: Into<Imm>> KshiftrbEmitter<KReg, KReg, U2> for Assembler<'_> {
    fn kshiftrb(&mut self, op0: KReg, op1: KReg, op2: U2) {
        self.emit_n(
            InstId::Kshiftrb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ
    }
}

impl<U2: Into<Imm>> KshiftrdEmitter<KReg, KReg, U2> for Assembler<'_> {
    fn kshiftrd(&mut self, op0: KReg, op1: KReg, op2: U2) {
        self.emit_n(
            InstId::Kshiftrd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_BW
    }
}

impl<U2: Into<Imm>> KshiftrqEmitter<KReg, KReg, U2> for Assembler<'_> {
    fn kshiftrq(&mut self, op0: KReg, op1: KReg, op2: U2) {
        self.emit_n(
            InstId::Kshiftrq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_BW
    }
}

impl<U2: Into<Imm>> KshiftrwEmitter<KReg, KReg, U2> for Assembler<'_> {
    fn kshiftrw(&mut self, op0: KReg, op1: KReg, op2: U2) {
        self.emit_n(
            InstId::Kshiftrw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F
    }
}

impl KtestbEmitter<KReg, KReg> for Assembler<'_> {
    fn ktestb(&mut self, op0: KReg, op1: KReg) {
        self.emit_n(InstId::Ktestb as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_DQ
    }
}

impl KtestdEmitter<KReg, KReg> for Assembler<'_> {
    fn ktestd(&mut self, op0: KReg, op1: KReg) {
        self.emit_n(InstId::Ktestd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_BW
    }
}

impl KtestqEmitter<KReg, KReg> for Assembler<'_> {
    fn ktestq(&mut self, op0: KReg, op1: KReg) {
        self.emit_n(InstId::Ktestq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_BW
    }
}

impl KtestwEmitter<KReg, KReg> for Assembler<'_> {
    fn ktestw(&mut self, op0: KReg, op1: KReg) {
        self.emit_n(InstId::Ktestw as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_DQ
    }
}

impl KunpckbwEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kunpckbw(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kunpckbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F
    }
}

impl KunpckdqEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kunpckdq(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kunpckdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW
    }
}

impl KunpckwdEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kunpckwd(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kunpckwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW
    }
}

impl KxnorbEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kxnorb(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kxnorb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ
    }
}

impl KxnordEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kxnord(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kxnord as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW
    }
}

impl KxnorqEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kxnorq(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kxnorq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW
    }
}

impl KxnorwEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kxnorw(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kxnorw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F
    }
}

impl KxorbEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kxorb(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kxorb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ
    }
}

impl KxordEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kxord(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kxord as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW
    }
}

impl KxorqEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kxorq(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kxorq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW
    }
}

impl KxorwEmitter<KReg, KReg, KReg> for Assembler<'_> {
    fn kxorw(&mut self, op0: KReg, op1: KReg, op2: KReg) {
        self.emit_n(
            InstId::Kxorw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F
    }
}

impl VaddpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaddpd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vaddpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VaddpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaddpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vaddpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VaddpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vaddpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vaddpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VaddpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vaddpd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vaddpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VaddpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaddpd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vaddpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VaddpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaddpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vaddpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VaddpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vaddpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vaddpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VaddpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vaddpd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vaddpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VaddpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaddps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vaddps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VaddpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaddps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vaddps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VaddpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vaddps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vaddps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VaddpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vaddps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vaddps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VaddpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaddps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vaddps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VaddpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaddps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vaddps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VaddpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vaddps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vaddps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VaddpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vaddps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vaddps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VaddsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaddsd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vaddsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VaddsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaddsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vaddsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VaddsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaddsd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vaddsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VaddsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaddsd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vaddsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VaddssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaddss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vaddss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VaddssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaddss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vaddss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VaddssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaddss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vaddss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VaddssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaddss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vaddss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VaddsubpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaddsubpd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vaddsubpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaddsubpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vaddsubpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vaddsubpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vaddsubpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaddsubpd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vaddsubpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaddsubpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vaddsubpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vaddsubpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vaddsubpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaddsubps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vaddsubps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaddsubps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vaddsubps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vaddsubps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vaddsubps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaddsubps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vaddsubps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaddsubps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vaddsubps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VaddsubpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vaddsubps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vaddsubps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VaesdecEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaesdec(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vaesdec as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdecEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaesdec(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vaesdec as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdecEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vaesdec(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vaesdec as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdecEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vaesdec(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vaesdec as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdecEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaesdec(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vaesdec as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdecEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaesdec(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vaesdec as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdecEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vaesdec(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vaesdec as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdecEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vaesdec(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vaesdec as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdeclastEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaesdeclast(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vaesdeclast as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdeclastEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaesdeclast(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vaesdeclast as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdeclastEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vaesdeclast(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vaesdeclast as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdeclastEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vaesdeclast(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vaesdeclast as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdeclastEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaesdeclast(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vaesdeclast as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdeclastEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaesdeclast(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vaesdeclast as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdeclastEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vaesdeclast(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vaesdeclast as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesdeclastEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vaesdeclast(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vaesdeclast as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesencEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaesenc(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vaesenc as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesencEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaesenc(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vaesenc as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesencEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vaesenc(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vaesenc as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesencEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vaesenc(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vaesenc as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesencEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaesenc(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vaesenc as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesencEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaesenc(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vaesenc as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesencEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vaesenc(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vaesenc as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesencEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vaesenc(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vaesenc as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesenclastEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaesenclast(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vaesenclast as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesenclastEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaesenclast(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vaesenclast as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesenclastEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vaesenclast(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vaesenclast as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesenclastEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vaesenclast(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vaesenclast as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesenclastEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaesenclast(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vaesenclast as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesenclastEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaesenclast(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vaesenclast as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesenclastEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vaesenclast(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vaesenclast as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesenclastEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vaesenclast(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vaesenclast as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI VAES
    }
}

impl VaesimcEmitter<Vec, Vec> for Assembler<'_> {
    fn vaesimc(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vaesimc as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+AESNI
    }
}

impl VaesimcEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vaesimc(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vaesimc as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+AESNI
    }
}

impl VaesimcEmitter<Vec, Mem> for Assembler<'_> {
    fn vaesimc(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vaesimc as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+AESNI
    }
}

impl VaesimcEmitter<Xmm, Mem> for Assembler<'_> {
    fn vaesimc(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vaesimc as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+AESNI
    }
}

impl VaeskeygenassistEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vaeskeygenassist(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vaeskeygenassist as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI
    }
}

impl<U2: Into<Imm>> VaeskeygenassistEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vaeskeygenassist(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vaeskeygenassist as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+AESNI
    }
}

impl VaeskeygenassistEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vaeskeygenassist(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vaeskeygenassist as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+AESNI
    }
}

impl<U2: Into<Imm>> VaeskeygenassistEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vaeskeygenassist(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vaeskeygenassist as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+AESNI
    }
}

impl ValigndEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn valignd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Valignd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> ValigndEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn valignd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Valignd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> ValigndEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn valignd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Valignd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> ValigndEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn valignd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Valignd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl ValigndEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn valignd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Valignd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> ValigndEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn valignd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Valignd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> ValigndEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn valignd(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Valignd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> ValigndEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn valignd(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Valignd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl ValignqEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn valignq(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Valignq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> ValignqEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn valignq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Valignq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> ValignqEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn valignq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Valignq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> ValignqEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn valignq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Valignq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl ValignqEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn valignq(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Valignq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> ValignqEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn valignq(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Valignq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> ValignqEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn valignq(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Valignq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> ValignqEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn valignq(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Valignq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VandnpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vandnpd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vandnpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandnpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vandnpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vandnpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandnpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vandnpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vandnpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandnpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vandnpd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vandnpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandnpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vandnpd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vandnpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandnpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vandnpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vandnpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandnpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vandnpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vandnpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandnpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vandnpd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vandnpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandnpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vandnps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vandnps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandnpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vandnps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vandnps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandnpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vandnps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vandnps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandnpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vandnps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vandnps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandnpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vandnps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vandnps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandnpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vandnps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vandnps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandnpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vandnps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vandnps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandnpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vandnps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vandnps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vandpd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vandpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vandpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vandpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vandpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vandpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vandpd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vandpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vandpd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vandpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vandpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vandpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vandpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vandpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vandpd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vandpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VandpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vandps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vandps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vandps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vandps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vandps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vandps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vandps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vandps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vandps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vandps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vandps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vandps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vandps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vandps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VandpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vandps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vandps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VblendmpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vblendmpd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vblendmpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VblendmpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vblendmpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vblendmpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VblendmpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vblendmpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vblendmpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VblendmpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vblendmpd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vblendmpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VblendmpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vblendmpd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vblendmpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VblendmpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vblendmpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vblendmpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VblendmpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vblendmpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vblendmpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VblendmpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vblendmpd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vblendmpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VblendmpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vblendmps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vblendmps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VblendmpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vblendmps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vblendmps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VblendmpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vblendmps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vblendmps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VblendmpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vblendmps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vblendmps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VblendmpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vblendmps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vblendmps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VblendmpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vblendmps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vblendmps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VblendmpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vblendmps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vblendmps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VblendmpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vblendmps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vblendmps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VblendpdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vblendpd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vblendpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VblendpdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vblendpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vblendpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VblendpdEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vblendpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vblendpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl VblendpdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vblendpd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vblendpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VblendpdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vblendpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vblendpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VblendpdEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vblendpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vblendpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl VblendpsEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vblendps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vblendps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VblendpsEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vblendps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vblendps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VblendpsEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vblendps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vblendps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl VblendpsEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vblendps(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vblendps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VblendpsEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vblendps(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vblendps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VblendpsEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vblendps(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vblendps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vblendvpd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vblendvpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vblendvpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vblendvpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpdEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vblendvpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Ymm) {
        self.emit_n(
            InstId::Vblendvpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vblendvpd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vblendvpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vblendvpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vblendvpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpdEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vblendvpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: Ymm) {
        self.emit_n(
            InstId::Vblendvpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpsEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vblendvps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vblendvps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpsEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vblendvps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vblendvps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpsEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vblendvps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Ymm) {
        self.emit_n(
            InstId::Vblendvps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpsEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vblendvps(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vblendvps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpsEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vblendvps(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vblendvps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl VblendvpsEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vblendvps(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: Ymm) {
        self.emit_n(
            InstId::Vblendvps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl Vbroadcastf128Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcastf128(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastf128 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl Vbroadcastf128Emitter<Ymm, Mem> for Assembler<'_> {
    fn vbroadcastf128(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastf128 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl Vbroadcastf32x2Emitter<Vec, Vec> for Assembler<'_> {
    fn vbroadcastf32x2(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vbroadcastf32x2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf32x2Emitter<Ymm, Xmm> for Assembler<'_> {
    fn vbroadcastf32x2(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vbroadcastf32x2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf32x2Emitter<Zmm, Xmm> for Assembler<'_> {
    fn vbroadcastf32x2(&mut self, op0: Zmm, op1: Xmm) {
        self.emit_n(
            InstId::Vbroadcastf32x2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf32x2Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcastf32x2(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastf32x2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf32x2Emitter<Ymm, Mem> for Assembler<'_> {
    fn vbroadcastf32x2(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastf32x2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf32x2Emitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcastf32x2(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastf32x2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf32x4Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcastf32x4(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastf32x4 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vbroadcastf32x4Emitter<Ymm, Mem> for Assembler<'_> {
    fn vbroadcastf32x4(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastf32x4 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vbroadcastf32x4Emitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcastf32x4(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastf32x4 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vbroadcastf32x8Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcastf32x8(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastf32x8 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf32x8Emitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcastf32x8(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastf32x8 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf64x2Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcastf64x2(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastf64x2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf64x2Emitter<Ymm, Mem> for Assembler<'_> {
    fn vbroadcastf64x2(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastf64x2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf64x2Emitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcastf64x2(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastf64x2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcastf64x4Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcastf64x4(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastf64x4 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vbroadcastf64x4Emitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcastf64x4(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastf64x4 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vbroadcasti128Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcasti128(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcasti128 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2
    }
}

impl Vbroadcasti128Emitter<Ymm, Mem> for Assembler<'_> {
    fn vbroadcasti128(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcasti128 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2
    }
}

impl Vbroadcasti32x2Emitter<Vec, Vec> for Assembler<'_> {
    fn vbroadcasti32x2(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vbroadcasti32x2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti32x2Emitter<Xmm, Xmm> for Assembler<'_> {
    fn vbroadcasti32x2(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vbroadcasti32x2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti32x2Emitter<Ymm, Xmm> for Assembler<'_> {
    fn vbroadcasti32x2(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vbroadcasti32x2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti32x2Emitter<Zmm, Xmm> for Assembler<'_> {
    fn vbroadcasti32x2(&mut self, op0: Zmm, op1: Xmm) {
        self.emit_n(
            InstId::Vbroadcasti32x2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti32x2Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcasti32x2(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcasti32x2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti32x2Emitter<Xmm, Mem> for Assembler<'_> {
    fn vbroadcasti32x2(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcasti32x2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti32x2Emitter<Ymm, Mem> for Assembler<'_> {
    fn vbroadcasti32x2(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcasti32x2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti32x2Emitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcasti32x2(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcasti32x2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti32x4Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcasti32x4(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcasti32x4 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vbroadcasti32x4Emitter<Ymm, Mem> for Assembler<'_> {
    fn vbroadcasti32x4(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcasti32x4 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vbroadcasti32x4Emitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcasti32x4(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcasti32x4 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vbroadcasti32x8Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcasti32x8(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcasti32x8 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti32x8Emitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcasti32x8(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcasti32x8 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti64x2Emitter<Vec, Vec> for Assembler<'_> {
    fn vbroadcasti64x2(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vbroadcasti64x2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti64x2Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcasti64x2(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcasti64x2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti64x2Emitter<Ymm, Mem> for Assembler<'_> {
    fn vbroadcasti64x2(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcasti64x2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti64x2Emitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcasti64x2(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcasti64x2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl Vbroadcasti64x4Emitter<Vec, Vec> for Assembler<'_> {
    fn vbroadcasti64x4(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vbroadcasti64x4 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vbroadcasti64x4Emitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcasti64x4(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcasti64x4 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vbroadcasti64x4Emitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcasti64x4(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcasti64x4 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VbroadcastsdEmitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcastsd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastsd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VbroadcastsdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vbroadcastsd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastsd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VbroadcastsdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcastsd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastsd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VbroadcastsdEmitter<Vec, Vec> for Assembler<'_> {
    fn vbroadcastsd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vbroadcastsd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VbroadcastsdEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vbroadcastsd(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vbroadcastsd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VbroadcastsdEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vbroadcastsd(&mut self, op0: Zmm, op1: Xmm) {
        self.emit_n(
            InstId::Vbroadcastsd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VbroadcastssEmitter<Vec, Mem> for Assembler<'_> {
    fn vbroadcastss(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VbroadcastssEmitter<Xmm, Mem> for Assembler<'_> {
    fn vbroadcastss(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VbroadcastssEmitter<Ymm, Mem> for Assembler<'_> {
    fn vbroadcastss(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VbroadcastssEmitter<Zmm, Mem> for Assembler<'_> {
    fn vbroadcastss(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vbroadcastss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VbroadcastssEmitter<Vec, Vec> for Assembler<'_> {
    fn vbroadcastss(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vbroadcastss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VbroadcastssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vbroadcastss(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vbroadcastss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VbroadcastssEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vbroadcastss(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vbroadcastss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VbroadcastssEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vbroadcastss(&mut self, op0: Zmm, op1: Xmm) {
        self.emit_n(
            InstId::Vbroadcastss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VcmppdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vcmppd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmppdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vcmppd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmppdEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vcmppd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl VcmppdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vcmppd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmppdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vcmppd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmppdEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vcmppd(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl VcmppdEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vcmppd(&mut self, op0: KReg, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VcmppdEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vcmppd(&mut self, op0: KReg, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VcmppdEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vcmppd(&mut self, op0: KReg, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VcmppdEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vcmppd(&mut self, op0: KReg, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VcmppdEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vcmppd(&mut self, op0: KReg, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VcmppdEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vcmppd(&mut self, op0: KReg, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VcmppdEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vcmppd(&mut self, op0: KReg, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VcmppdEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vcmppd(&mut self, op0: KReg, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vcmppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VcmppsEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vcmpps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmppsEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vcmpps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmppsEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vcmpps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl VcmppsEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vcmpps(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmppsEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vcmpps(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmppsEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vcmpps(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl VcmppsEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vcmpps(&mut self, op0: KReg, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VcmppsEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vcmpps(&mut self, op0: KReg, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VcmppsEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vcmpps(&mut self, op0: KReg, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VcmppsEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vcmpps(&mut self, op0: KReg, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VcmppsEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vcmpps(&mut self, op0: KReg, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VcmppsEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vcmpps(&mut self, op0: KReg, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VcmppsEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vcmpps(&mut self, op0: KReg, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VcmppsEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vcmpps(&mut self, op0: KReg, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vcmpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VcmpsdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vcmpsd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vcmpsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmpsdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vcmpsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vcmpsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl VcmpsdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vcmpsd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vcmpsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmpsdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vcmpsd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vcmpsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl VcmpsdEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vcmpsd(&mut self, op0: KReg, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vcmpsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VcmpsdEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vcmpsd(&mut self, op0: KReg, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vcmpsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VcmpsdEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vcmpsd(&mut self, op0: KReg, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vcmpsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VcmpsdEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vcmpsd(&mut self, op0: KReg, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vcmpsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VcmpssEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vcmpss(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vcmpss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmpssEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vcmpss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vcmpss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl VcmpssEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vcmpss(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vcmpss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VcmpssEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vcmpss(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vcmpss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl VcmpssEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vcmpss(&mut self, op0: KReg, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vcmpss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VcmpssEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vcmpss(&mut self, op0: KReg, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vcmpss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VcmpssEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vcmpss(&mut self, op0: KReg, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vcmpss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VcmpssEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vcmpss(&mut self, op0: KReg, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vcmpss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VcomisdEmitter<Vec, Vec> for Assembler<'_> {
    fn vcomisd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcomisd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VcomisdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcomisd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcomisd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VcomisdEmitter<Vec, Mem> for Assembler<'_> {
    fn vcomisd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcomisd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VcomisdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcomisd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcomisd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VcomissEmitter<Vec, Vec> for Assembler<'_> {
    fn vcomiss(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcomiss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VcomissEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcomiss(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcomiss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VcomissEmitter<Vec, Mem> for Assembler<'_> {
    fn vcomiss(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcomiss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VcomissEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcomiss(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcomiss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VcompresspdEmitter<Vec, Vec> for Assembler<'_> {
    fn vcompresspd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcompresspd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcompresspd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcompresspd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcompresspd(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vcompresspd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcompresspd(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcompresspd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspdEmitter<Mem, Vec> for Assembler<'_> {
    fn vcompresspd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vcompresspd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vcompresspd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vcompresspd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vcompresspd(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vcompresspd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspdEmitter<Mem, Zmm> for Assembler<'_> {
    fn vcompresspd(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vcompresspd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspsEmitter<Vec, Vec> for Assembler<'_> {
    fn vcompressps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcompressps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcompressps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcompressps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspsEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcompressps(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vcompressps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspsEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcompressps(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcompressps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspsEmitter<Mem, Vec> for Assembler<'_> {
    fn vcompressps(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vcompressps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspsEmitter<Mem, Xmm> for Assembler<'_> {
    fn vcompressps(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vcompressps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspsEmitter<Mem, Ymm> for Assembler<'_> {
    fn vcompressps(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vcompressps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VcompresspsEmitter<Mem, Zmm> for Assembler<'_> {
    fn vcompressps(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vcompressps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vcvtdq2pdEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtdq2pd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtdq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2pdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtdq2pd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtdq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2pdEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtdq2pd(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtdq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2pdEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvtdq2pd(&mut self, op0: Zmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtdq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2pdEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtdq2pd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtdq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtdq2pd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtdq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2pdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtdq2pd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtdq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2pdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtdq2pd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtdq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2psEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtdq2ps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtdq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2psEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtdq2ps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtdq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2psEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtdq2ps(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtdq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2psEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtdq2ps(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtdq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2psEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtdq2ps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtdq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtdq2ps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtdq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtdq2ps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtdq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtdq2psEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtdq2ps(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtdq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtne2ps2bf16Emitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vcvtne2ps2bf16(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vcvtne2ps2bf16 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtne2ps2bf16Emitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vcvtne2ps2bf16(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vcvtne2ps2bf16 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtne2ps2bf16Emitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vcvtne2ps2bf16(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vcvtne2ps2bf16 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtne2ps2bf16Emitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vcvtne2ps2bf16(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vcvtne2ps2bf16 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtne2ps2bf16Emitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtne2ps2bf16(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vcvtne2ps2bf16 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtne2ps2bf16Emitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtne2ps2bf16(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vcvtne2ps2bf16 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtne2ps2bf16Emitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vcvtne2ps2bf16(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vcvtne2ps2bf16 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtne2ps2bf16Emitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vcvtne2ps2bf16(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vcvtne2ps2bf16 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtneps2bf16Emitter<Vec, Vec> for Assembler<'_> {
    fn vcvtneps2bf16(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtneps2bf16 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtneps2bf16Emitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtneps2bf16(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtneps2bf16 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtneps2bf16Emitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtneps2bf16(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtneps2bf16 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtneps2bf16Emitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvtneps2bf16(&mut self, op0: Ymm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtneps2bf16 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtneps2bf16Emitter<Vec, Mem> for Assembler<'_> {
    fn vcvtneps2bf16(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtneps2bf16 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtneps2bf16Emitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtneps2bf16(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtneps2bf16 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtneps2bf16Emitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtneps2bf16(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtneps2bf16 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vcvtpd2dqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtpd2dq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2dqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtpd2dq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2dqEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtpd2dq(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2dqEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvtpd2dq(&mut self, op0: Ymm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2dqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtpd2dq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2dqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtpd2dq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2dqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtpd2dq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2psEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtpd2ps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtpd2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2psEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtpd2ps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtpd2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2psEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtpd2ps(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtpd2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2psEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvtpd2ps(&mut self, op0: Ymm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtpd2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2psEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtpd2ps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtpd2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtpd2ps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtpd2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtpd2ps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtpd2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvtpd2qqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtpd2qq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtpd2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2qqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtpd2qq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtpd2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2qqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtpd2qq(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtpd2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2qqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtpd2qq(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtpd2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2qqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtpd2qq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtpd2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2qqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtpd2qq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtpd2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2qqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtpd2qq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtpd2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2qqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtpd2qq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtpd2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2udqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtpd2udq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtpd2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvtpd2udqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtpd2udq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtpd2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvtpd2udqEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtpd2udq(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtpd2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvtpd2udqEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvtpd2udq(&mut self, op0: Ymm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtpd2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvtpd2udqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtpd2udq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtpd2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvtpd2udqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtpd2udq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtpd2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvtpd2udqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtpd2udq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtpd2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvtpd2uqqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtpd2uqq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtpd2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2uqqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtpd2uqq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtpd2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2uqqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtpd2uqq(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtpd2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2uqqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtpd2uqq(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtpd2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2uqqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtpd2uqq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtpd2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2uqqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtpd2uqq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtpd2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2uqqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtpd2uqq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtpd2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtpd2uqqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtpd2uqq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtpd2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtph2psEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtph2ps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtph2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // F16C AVX512_F{kz}
    }
}

impl Vcvtph2psEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtph2ps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtph2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // F16C AVX512_F{kz}
    }
}

impl Vcvtph2psEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtph2ps(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtph2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // F16C AVX512_F{kz}
    }
}

impl Vcvtph2psEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvtph2ps(&mut self, op0: Zmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtph2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // F16C AVX512_F{kz}
    }
}

impl Vcvtph2psEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtph2ps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // F16C AVX512_F{kz}
    }
}

impl Vcvtph2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtph2ps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // F16C AVX512_F{kz}
    }
}

impl Vcvtph2psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtph2ps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // F16C AVX512_F{kz}
    }
}

impl Vcvtph2psEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtph2ps(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // F16C AVX512_F{kz}
    }
}

impl Vcvtps2dqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtps2dq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2dqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtps2dq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2dqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtps2dq(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2dqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtps2dq(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2dqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtps2dq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2dqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtps2dq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2dqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtps2dq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2dqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtps2dq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2pdEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtps2pd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtps2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2pdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtps2pd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtps2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2pdEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtps2pd(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtps2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2pdEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvtps2pd(&mut self, op0: Zmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtps2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2pdEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtps2pd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtps2pd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2pdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtps2pd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2pdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtps2pd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvtps2phEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vcvtps2ph(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vcvtps2ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // F16C AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vcvtps2phEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vcvtps2ph(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vcvtps2ph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // F16C AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vcvtps2phEmitter<Xmm, Ymm, U2> for Assembler<'_> {
    fn vcvtps2ph(&mut self, op0: Xmm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vcvtps2ph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // F16C AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vcvtps2phEmitter<Ymm, Zmm, U2> for Assembler<'_> {
    fn vcvtps2ph(&mut self, op0: Ymm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vcvtps2ph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // F16C AVX512_F{kz}
    }
}

impl Vcvtps2phEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vcvtps2ph(&mut self, op0: Mem, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vcvtps2ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // F16C AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vcvtps2phEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn vcvtps2ph(&mut self, op0: Mem, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vcvtps2ph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // F16C AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vcvtps2phEmitter<Mem, Ymm, U2> for Assembler<'_> {
    fn vcvtps2ph(&mut self, op0: Mem, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vcvtps2ph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // F16C AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vcvtps2phEmitter<Mem, Zmm, U2> for Assembler<'_> {
    fn vcvtps2ph(&mut self, op0: Mem, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vcvtps2ph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // F16C AVX512_F{kz}
    }
}

impl Vcvtps2qqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtps2qq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtps2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2qqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtps2qq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtps2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2qqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtps2qq(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtps2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2qqEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvtps2qq(&mut self, op0: Zmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtps2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2qqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtps2qq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2qqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtps2qq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2qqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtps2qq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2qqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtps2qq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2udqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtps2udq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtps2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtps2udqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtps2udq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtps2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtps2udqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtps2udq(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtps2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtps2udqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtps2udq(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtps2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtps2udqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtps2udq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtps2udqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtps2udq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtps2udqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtps2udq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtps2udqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtps2udq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtps2uqqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtps2uqq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtps2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2uqqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtps2uqq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtps2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2uqqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtps2uqq(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtps2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2uqqEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvtps2uqq(&mut self, op0: Zmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtps2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2uqqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtps2uqq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2uqqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtps2uqq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2uqqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtps2uqq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtps2uqqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtps2uqq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvtqq2pdEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtqq2pd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtqq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2pdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtqq2pd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtqq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2pdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtqq2pd(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtqq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2pdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtqq2pd(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtqq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2pdEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtqq2pd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtqq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtqq2pd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtqq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2pdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtqq2pd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtqq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2pdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtqq2pd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtqq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2psEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtqq2ps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtqq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2psEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtqq2ps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtqq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2psEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtqq2ps(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtqq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2psEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvtqq2ps(&mut self, op0: Ymm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtqq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2psEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtqq2ps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtqq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtqq2ps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtqq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtqq2psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtqq2ps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtqq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtsd2siEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvtsd2si(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Vcvtsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsd2siEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvtsd2si(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsd2siEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvtsd2si(&mut self, op0: Gpq, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsd2siEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvtsd2si(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Vcvtsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsd2siEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvtsd2si(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Vcvtsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsd2siEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvtsd2si(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Vcvtsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsd2ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vcvtsd2ss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vcvtsd2ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl Vcvtsd2ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vcvtsd2ss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vcvtsd2ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl Vcvtsd2ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtsd2ss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vcvtsd2ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl Vcvtsd2ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtsd2ss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vcvtsd2ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl Vcvtsd2usiEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvtsd2usi(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Vcvtsd2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtsd2usiEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvtsd2usi(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtsd2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtsd2usiEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvtsd2usi(&mut self, op0: Gpq, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtsd2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtsd2usiEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvtsd2usi(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Vcvtsd2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtsd2usiEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvtsd2usi(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Vcvtsd2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtsd2usiEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvtsd2usi(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Vcvtsd2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtsi2sdEmitter<Vec, Vec, Gp> for Assembler<'_> {
    fn vcvtsi2sd(&mut self, op0: Vec, op1: Vec, op2: Gp) {
        self.emit_n(
            InstId::Vcvtsi2sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsi2sdEmitter<Xmm, Xmm, Gpd> for Assembler<'_> {
    fn vcvtsi2sd(&mut self, op0: Xmm, op1: Xmm, op2: Gpd) {
        self.emit_n(
            InstId::Vcvtsi2sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsi2sdEmitter<Xmm, Xmm, Gpq> for Assembler<'_> {
    fn vcvtsi2sd(&mut self, op0: Xmm, op1: Xmm, op2: Gpq) {
        self.emit_n(
            InstId::Vcvtsi2sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsi2sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtsi2sd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vcvtsi2sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsi2sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtsi2sd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vcvtsi2sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsi2ssEmitter<Vec, Vec, Gp> for Assembler<'_> {
    fn vcvtsi2ss(&mut self, op0: Vec, op1: Vec, op2: Gp) {
        self.emit_n(
            InstId::Vcvtsi2ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsi2ssEmitter<Xmm, Xmm, Gpd> for Assembler<'_> {
    fn vcvtsi2ss(&mut self, op0: Xmm, op1: Xmm, op2: Gpd) {
        self.emit_n(
            InstId::Vcvtsi2ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsi2ssEmitter<Xmm, Xmm, Gpq> for Assembler<'_> {
    fn vcvtsi2ss(&mut self, op0: Xmm, op1: Xmm, op2: Gpq) {
        self.emit_n(
            InstId::Vcvtsi2ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsi2ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtsi2ss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vcvtsi2ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtsi2ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtsi2ss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vcvtsi2ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtss2sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vcvtss2sd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vcvtss2sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl Vcvtss2sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vcvtss2sd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vcvtss2sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl Vcvtss2sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtss2sd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vcvtss2sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl Vcvtss2sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtss2sd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vcvtss2sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl Vcvtss2siEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvtss2si(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Vcvtss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtss2siEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvtss2si(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtss2siEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvtss2si(&mut self, op0: Gpq, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtss2siEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvtss2si(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Vcvtss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtss2siEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvtss2si(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Vcvtss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtss2siEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvtss2si(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Vcvtss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{er}
    }
}

impl Vcvtss2usiEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvtss2usi(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Vcvtss2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtss2usiEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvtss2usi(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtss2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtss2usiEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvtss2usi(&mut self, op0: Gpq, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtss2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtss2usiEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvtss2usi(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Vcvtss2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtss2usiEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvtss2usi(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Vcvtss2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtss2usiEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvtss2usi(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Vcvtss2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvttpd2dqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttpd2dq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvttpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvttpd2dqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttpd2dq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvttpd2dqEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvttpd2dq(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvttpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvttpd2dqEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvttpd2dq(&mut self, op0: Ymm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvttpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvttpd2dqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttpd2dq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvttpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvttpd2dqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttpd2dq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvttpd2dqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttpd2dq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttpd2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl Vcvttpd2qqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttpd2qq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvttpd2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2qqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttpd2qq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttpd2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2qqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvttpd2qq(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvttpd2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2qqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvttpd2qq(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvttpd2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2qqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttpd2qq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvttpd2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2qqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttpd2qq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttpd2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2qqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttpd2qq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttpd2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2qqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttpd2qq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttpd2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2udqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttpd2udq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvttpd2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2udqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttpd2udq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttpd2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2udqEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvttpd2udq(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvttpd2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2udqEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvttpd2udq(&mut self, op0: Ymm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvttpd2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2udqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttpd2udq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvttpd2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2udqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttpd2udq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttpd2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2udqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttpd2udq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttpd2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vcvttpd2uqqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttpd2uqq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvttpd2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvttpd2uqqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttpd2uqq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttpd2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvttpd2uqqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvttpd2uqq(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvttpd2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvttpd2uqqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvttpd2uqq(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvttpd2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvttpd2uqqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttpd2uqq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvttpd2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvttpd2uqqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttpd2uqq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttpd2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvttpd2uqqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttpd2uqq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttpd2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvttpd2uqqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttpd2uqq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttpd2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvttps2dqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttps2dq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvttps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvttps2dqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttps2dq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvttps2dqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvttps2dq(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvttps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvttps2dqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvttps2dq(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvttps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvttps2dqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttps2dq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvttps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvttps2dqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttps2dq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvttps2dqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttps2dq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvttps2dqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttps2dq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttps2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vcvttps2qqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttps2qq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvttps2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2qqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttps2qq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttps2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2qqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvttps2qq(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttps2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2qqEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvttps2qq(&mut self, op0: Zmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvttps2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2qqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttps2qq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvttps2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2qqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttps2qq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttps2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2qqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttps2qq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttps2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2qqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttps2qq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttps2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2udqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttps2udq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvttps2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvttps2udqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttps2udq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttps2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvttps2udqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvttps2udq(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvttps2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvttps2udqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvttps2udq(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvttps2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvttps2udqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttps2udq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvttps2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvttps2udqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttps2udq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttps2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvttps2udqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttps2udq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttps2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvttps2udqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttps2udq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttps2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvttps2uqqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttps2uqq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvttps2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2uqqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttps2uqq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttps2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2uqqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvttps2uqq(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttps2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2uqqEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvttps2uqq(&mut self, op0: Zmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvttps2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2uqqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttps2uqq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvttps2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2uqqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttps2uqq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttps2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2uqqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttps2uqq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttps2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttps2uqqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttps2uqq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttps2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl Vcvttsd2siEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvttsd2si(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Vcvttsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttsd2siEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvttsd2si(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttsd2siEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvttsd2si(&mut self, op0: Gpq, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttsd2siEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvttsd2si(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Vcvttsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttsd2siEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvttsd2si(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Vcvttsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttsd2siEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvttsd2si(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Vcvttsd2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttsd2usiEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvttsd2usi(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Vcvttsd2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttsd2usiEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvttsd2usi(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttsd2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttsd2usiEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvttsd2usi(&mut self, op0: Gpq, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttsd2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttsd2usiEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvttsd2usi(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Vcvttsd2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttsd2usiEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvttsd2usi(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Vcvttsd2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttsd2usiEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvttsd2usi(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Vcvttsd2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttss2siEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvttss2si(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Vcvttss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttss2siEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvttss2si(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttss2siEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvttss2si(&mut self, op0: Gpq, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttss2siEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvttss2si(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Vcvttss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttss2siEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvttss2si(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Vcvttss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttss2siEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvttss2si(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Vcvttss2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl Vcvttss2usiEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvttss2usi(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Vcvttss2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttss2usiEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvttss2usi(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttss2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttss2usiEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvttss2usi(&mut self, op0: Gpq, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttss2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttss2usiEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvttss2usi(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Vcvttss2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttss2usiEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvttss2usi(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Vcvttss2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvttss2usiEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvttss2usi(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Vcvttss2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{sae}
    }
}

impl Vcvtudq2pdEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtudq2pd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtudq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2pdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtudq2pd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtudq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2pdEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtudq2pd(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtudq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2pdEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvtudq2pd(&mut self, op0: Zmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtudq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2pdEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtudq2pd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtudq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtudq2pd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtudq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2pdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtudq2pd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtudq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2pdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtudq2pd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtudq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2psEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtudq2ps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtudq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2psEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtudq2ps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtudq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2psEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtudq2ps(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtudq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2psEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtudq2ps(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtudq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2psEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtudq2ps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtudq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtudq2ps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtudq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtudq2ps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtudq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtudq2psEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtudq2ps(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtudq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vcvtuqq2pdEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtuqq2pd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtuqq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2pdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtuqq2pd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtuqq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2pdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtuqq2pd(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtuqq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2pdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtuqq2pd(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtuqq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2pdEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtuqq2pd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtuqq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtuqq2pd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtuqq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2pdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtuqq2pd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtuqq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2pdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtuqq2pd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtuqq2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2psEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtuqq2ps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtuqq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2psEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtuqq2ps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtuqq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2psEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtuqq2ps(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtuqq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2psEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvtuqq2ps(&mut self, op0: Ymm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtuqq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2psEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtuqq2ps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtuqq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtuqq2ps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtuqq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtuqq2psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtuqq2ps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtuqq2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl Vcvtusi2sdEmitter<Vec, Vec, Gp> for Assembler<'_> {
    fn vcvtusi2sd(&mut self, op0: Vec, op1: Vec, op2: Gp) {
        self.emit_n(
            InstId::Vcvtusi2sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtusi2sdEmitter<Xmm, Xmm, Gpd> for Assembler<'_> {
    fn vcvtusi2sd(&mut self, op0: Xmm, op1: Xmm, op2: Gpd) {
        self.emit_n(
            InstId::Vcvtusi2sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtusi2sdEmitter<Xmm, Xmm, Gpq> for Assembler<'_> {
    fn vcvtusi2sd(&mut self, op0: Xmm, op1: Xmm, op2: Gpq) {
        self.emit_n(
            InstId::Vcvtusi2sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtusi2sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtusi2sd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vcvtusi2sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtusi2sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtusi2sd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vcvtusi2sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtusi2ssEmitter<Vec, Vec, Gp> for Assembler<'_> {
    fn vcvtusi2ss(&mut self, op0: Vec, op1: Vec, op2: Gp) {
        self.emit_n(
            InstId::Vcvtusi2ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtusi2ssEmitter<Xmm, Xmm, Gpd> for Assembler<'_> {
    fn vcvtusi2ss(&mut self, op0: Xmm, op1: Xmm, op2: Gpd) {
        self.emit_n(
            InstId::Vcvtusi2ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtusi2ssEmitter<Xmm, Xmm, Gpq> for Assembler<'_> {
    fn vcvtusi2ss(&mut self, op0: Xmm, op1: Xmm, op2: Gpq) {
        self.emit_n(
            InstId::Vcvtusi2ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtusi2ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtusi2ss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vcvtusi2ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{er}
    }
}

impl Vcvtusi2ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtusi2ss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vcvtusi2ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{er}
    }
}

impl VdbpsadbwEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vdbpsadbw(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vdbpsadbw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VdbpsadbwEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vdbpsadbw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vdbpsadbw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VdbpsadbwEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vdbpsadbw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vdbpsadbw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VdbpsadbwEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vdbpsadbw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vdbpsadbw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl VdbpsadbwEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vdbpsadbw(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vdbpsadbw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VdbpsadbwEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vdbpsadbw(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vdbpsadbw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VdbpsadbwEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vdbpsadbw(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vdbpsadbw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VdbpsadbwEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vdbpsadbw(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vdbpsadbw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl VdivpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vdivpd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vdivpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VdivpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vdivpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vdivpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VdivpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vdivpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vdivpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VdivpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vdivpd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vdivpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VdivpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vdivpd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vdivpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VdivpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vdivpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vdivpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VdivpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vdivpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vdivpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VdivpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vdivpd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vdivpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VdivpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vdivps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vdivps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VdivpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vdivps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vdivps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VdivpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vdivps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vdivps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VdivpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vdivps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vdivps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VdivpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vdivps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vdivps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VdivpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vdivps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vdivps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VdivpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vdivps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vdivps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VdivpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vdivps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vdivps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VdivsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vdivsd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vdivsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VdivsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vdivsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vdivsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VdivsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vdivsd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vdivsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VdivsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vdivsd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vdivsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VdivssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vdivss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vdivss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VdivssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vdivss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vdivss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VdivssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vdivss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vdivss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VdivssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vdivss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vdivss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl Vdpbf16psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vdpbf16ps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vdpbf16ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vdpbf16psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vdpbf16ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vdpbf16ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vdpbf16psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vdpbf16ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vdpbf16ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vdpbf16psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vdpbf16ps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vdpbf16ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vdpbf16psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vdpbf16ps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vdpbf16ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vdpbf16psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vdpbf16ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vdpbf16ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vdpbf16psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vdpbf16ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vdpbf16ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl Vdpbf16psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vdpbf16ps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vdpbf16ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BF16{kz|b32}
    }
}

impl VdppdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vdppd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vdppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VdppdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vdppd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vdppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl VdppdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vdppd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vdppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VdppdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vdppd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vdppd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl VdppsEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vdpps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vdpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VdppsEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vdpps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vdpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VdppsEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vdpps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vdpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl VdppsEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vdpps(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vdpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VdppsEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vdpps(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vdpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VdppsEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vdpps(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vdpps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl VexpandpdEmitter<Vec, Vec> for Assembler<'_> {
    fn vexpandpd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vexpandpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vexpandpd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vexpandpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vexpandpd(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vexpandpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vexpandpd(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vexpandpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpdEmitter<Vec, Mem> for Assembler<'_> {
    fn vexpandpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vexpandpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vexpandpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vexpandpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vexpandpd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vexpandpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vexpandpd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vexpandpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpsEmitter<Vec, Vec> for Assembler<'_> {
    fn vexpandps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vexpandps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vexpandps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vexpandps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpsEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vexpandps(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vexpandps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpsEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vexpandps(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vexpandps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpsEmitter<Vec, Mem> for Assembler<'_> {
    fn vexpandps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vexpandps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vexpandps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vexpandps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vexpandps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vexpandps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VexpandpsEmitter<Zmm, Mem> for Assembler<'_> {
    fn vexpandps(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vexpandps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vextractf128Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vextractf128(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextractf128 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl<U2: Into<Imm>> Vextractf128Emitter<Xmm, Ymm, U2> for Assembler<'_> {
    fn vextractf128(&mut self, op0: Xmm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vextractf128 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX
    }
}

impl Vextractf128Emitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextractf128(&mut self, op0: Mem, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextractf128 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl<U2: Into<Imm>> Vextractf128Emitter<Mem, Ymm, U2> for Assembler<'_> {
    fn vextractf128(&mut self, op0: Mem, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vextractf128 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX
    }
}

impl Vextractf32x4Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vextractf32x4(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextractf32x4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextractf32x4Emitter<Xmm, Ymm, U2> for Assembler<'_> {
    fn vextractf32x4(&mut self, op0: Xmm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vextractf32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextractf32x4Emitter<Xmm, Zmm, U2> for Assembler<'_> {
    fn vextractf32x4(&mut self, op0: Xmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vextractf32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vextractf32x4Emitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextractf32x4(&mut self, op0: Mem, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextractf32x4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextractf32x4Emitter<Mem, Ymm, U2> for Assembler<'_> {
    fn vextractf32x4(&mut self, op0: Mem, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vextractf32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextractf32x4Emitter<Mem, Zmm, U2> for Assembler<'_> {
    fn vextractf32x4(&mut self, op0: Mem, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vextractf32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vextractf32x8Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vextractf32x8(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextractf32x8 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextractf32x8Emitter<Ymm, Zmm, U2> for Assembler<'_> {
    fn vextractf32x8(&mut self, op0: Ymm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vextractf32x8 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vextractf32x8Emitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextractf32x8(&mut self, op0: Mem, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextractf32x8 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextractf32x8Emitter<Mem, Zmm, U2> for Assembler<'_> {
    fn vextractf32x8(&mut self, op0: Mem, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vextractf32x8 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vextractf64x2Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vextractf64x2(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextractf64x2 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextractf64x2Emitter<Xmm, Ymm, U2> for Assembler<'_> {
    fn vextractf64x2(&mut self, op0: Xmm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vextractf64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextractf64x2Emitter<Xmm, Zmm, U2> for Assembler<'_> {
    fn vextractf64x2(&mut self, op0: Xmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vextractf64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vextractf64x2Emitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextractf64x2(&mut self, op0: Mem, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextractf64x2 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextractf64x2Emitter<Mem, Ymm, U2> for Assembler<'_> {
    fn vextractf64x2(&mut self, op0: Mem, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vextractf64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextractf64x2Emitter<Mem, Zmm, U2> for Assembler<'_> {
    fn vextractf64x2(&mut self, op0: Mem, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vextractf64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vextractf64x4Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vextractf64x4(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextractf64x4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextractf64x4Emitter<Ymm, Zmm, U2> for Assembler<'_> {
    fn vextractf64x4(&mut self, op0: Ymm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vextractf64x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vextractf64x4Emitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextractf64x4(&mut self, op0: Mem, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextractf64x4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextractf64x4Emitter<Mem, Zmm, U2> for Assembler<'_> {
    fn vextractf64x4(&mut self, op0: Mem, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vextractf64x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vextracti128Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vextracti128(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextracti128 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl<U2: Into<Imm>> Vextracti128Emitter<Xmm, Ymm, U2> for Assembler<'_> {
    fn vextracti128(&mut self, op0: Xmm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vextracti128 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX2
    }
}

impl Vextracti128Emitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextracti128(&mut self, op0: Mem, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextracti128 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl<U2: Into<Imm>> Vextracti128Emitter<Mem, Ymm, U2> for Assembler<'_> {
    fn vextracti128(&mut self, op0: Mem, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vextracti128 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX2
    }
}

impl Vextracti32x4Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vextracti32x4(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextracti32x4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextracti32x4Emitter<Xmm, Ymm, U2> for Assembler<'_> {
    fn vextracti32x4(&mut self, op0: Xmm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vextracti32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextracti32x4Emitter<Xmm, Zmm, U2> for Assembler<'_> {
    fn vextracti32x4(&mut self, op0: Xmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vextracti32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vextracti32x4Emitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextracti32x4(&mut self, op0: Mem, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextracti32x4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextracti32x4Emitter<Mem, Ymm, U2> for Assembler<'_> {
    fn vextracti32x4(&mut self, op0: Mem, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vextracti32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextracti32x4Emitter<Mem, Zmm, U2> for Assembler<'_> {
    fn vextracti32x4(&mut self, op0: Mem, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vextracti32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vextracti32x8Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vextracti32x8(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextracti32x8 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextracti32x8Emitter<Ymm, Zmm, U2> for Assembler<'_> {
    fn vextracti32x8(&mut self, op0: Ymm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vextracti32x8 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vextracti32x8Emitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextracti32x8(&mut self, op0: Mem, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextracti32x8 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextracti32x8Emitter<Mem, Zmm, U2> for Assembler<'_> {
    fn vextracti32x8(&mut self, op0: Mem, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vextracti32x8 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vextracti64x2Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vextracti64x2(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextracti64x2 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextracti64x2Emitter<Xmm, Ymm, U2> for Assembler<'_> {
    fn vextracti64x2(&mut self, op0: Xmm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vextracti64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextracti64x2Emitter<Xmm, Zmm, U2> for Assembler<'_> {
    fn vextracti64x2(&mut self, op0: Xmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vextracti64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vextracti64x2Emitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextracti64x2(&mut self, op0: Mem, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextracti64x2 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextracti64x2Emitter<Mem, Ymm, U2> for Assembler<'_> {
    fn vextracti64x2(&mut self, op0: Mem, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vextracti64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U2: Into<Imm>> Vextracti64x2Emitter<Mem, Zmm, U2> for Assembler<'_> {
    fn vextracti64x2(&mut self, op0: Mem, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vextracti64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vextracti64x4Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vextracti64x4(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextracti64x4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextracti64x4Emitter<Ymm, Zmm, U2> for Assembler<'_> {
    fn vextracti64x4(&mut self, op0: Ymm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vextracti64x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vextracti64x4Emitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextracti64x4(&mut self, op0: Mem, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextracti64x4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl<U2: Into<Imm>> Vextracti64x4Emitter<Mem, Zmm, U2> for Assembler<'_> {
    fn vextracti64x4(&mut self, op0: Mem, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vextracti64x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl VextractpsEmitter<Gp, Vec, Imm> for Assembler<'_> {
    fn vextractps(&mut self, op0: Gp, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextractps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl<U2: Into<Imm>> VextractpsEmitter<Gpd, Xmm, U2> for Assembler<'_> {
    fn vextractps(&mut self, op0: Gpd, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vextractps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_F
    }
}

impl VextractpsEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vextractps(&mut self, op0: Mem, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vextractps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl<U2: Into<Imm>> VextractpsEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn vextractps(&mut self, op0: Mem, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vextractps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_F
    }
}

impl VfixupimmpdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vfixupimmpd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vfixupimmpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VfixupimmpdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vfixupimmpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vfixupimmpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VfixupimmpdEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vfixupimmpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vfixupimmpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VfixupimmpdEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vfixupimmpd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vfixupimmpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VfixupimmpdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vfixupimmpd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vfixupimmpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VfixupimmpdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vfixupimmpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vfixupimmpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VfixupimmpdEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vfixupimmpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vfixupimmpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VfixupimmpdEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vfixupimmpd(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vfixupimmpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VfixupimmpsEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vfixupimmps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vfixupimmps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VfixupimmpsEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vfixupimmps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vfixupimmps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VfixupimmpsEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vfixupimmps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vfixupimmps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VfixupimmpsEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vfixupimmps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vfixupimmps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VfixupimmpsEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vfixupimmps(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vfixupimmps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VfixupimmpsEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vfixupimmps(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vfixupimmps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VfixupimmpsEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vfixupimmps(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vfixupimmps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VfixupimmpsEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vfixupimmps(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vfixupimmps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VfixupimmsdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vfixupimmsd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vfixupimmsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VfixupimmsdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vfixupimmsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vfixupimmsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VfixupimmsdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vfixupimmsd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vfixupimmsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VfixupimmsdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vfixupimmsd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vfixupimmsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VfixupimmssEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vfixupimmss(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vfixupimmss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VfixupimmssEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vfixupimmss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vfixupimmss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VfixupimmssEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vfixupimmss(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vfixupimmss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VfixupimmssEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vfixupimmss(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vfixupimmss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl Vfmadd132pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd132pd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd132pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd132pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd132pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmadd132pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd132pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmadd132pd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd132pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd132pd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd132pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd132pd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd132pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmadd132pd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd132pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmadd132pd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd132psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd132ps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd132psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd132ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd132psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmadd132ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd132psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmadd132ps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd132psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd132ps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd132psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd132ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd132psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmadd132ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd132psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmadd132ps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd132sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd132sd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmadd132sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd132sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd132sd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmadd132sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd132sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd132sd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd132sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd132sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd132sd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd132sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd132ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd132ss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmadd132ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd132ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd132ss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmadd132ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd132ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd132ss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd132ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd132ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd132ss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd132ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd213pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd213pd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd213pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd213pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd213pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmadd213pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd213pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmadd213pd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd213pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd213pd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd213pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd213pd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd213pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmadd213pd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd213pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmadd213pd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd213psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd213ps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd213psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd213ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd213psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmadd213ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd213psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmadd213ps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd213psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd213ps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd213psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd213ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd213psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmadd213ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd213psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmadd213ps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd213sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd213sd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmadd213sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd213sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd213sd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmadd213sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd213sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd213sd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd213sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd213sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd213sd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd213sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd213ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd213ss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmadd213ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd213ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd213ss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmadd213ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd213ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd213ss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd213ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd213ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd213ss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd213ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd231pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd231pd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd231pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd231pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd231pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmadd231pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd231pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmadd231pd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd231pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd231pd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd231pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd231pd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd231pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmadd231pd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd231pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmadd231pd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmadd231psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd231ps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd231psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd231ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd231psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmadd231ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd231psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmadd231ps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd231psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd231ps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd231psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd231ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd231psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmadd231ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd231psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmadd231ps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmadd231sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd231sd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmadd231sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd231sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd231sd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmadd231sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd231sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd231sd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd231sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd231sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd231sd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd231sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd231ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd231ss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmadd231ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd231ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd231ss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmadd231ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd231ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd231ss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd231ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmadd231ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd231ss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd231ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmaddsub132pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsub132pd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmaddsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub132pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsub132pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmaddsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub132pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsub132pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmaddsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub132pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmaddsub132pd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmaddsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub132pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsub132pd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub132pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsub132pd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub132pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsub132pd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub132pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmaddsub132pd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub132psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsub132ps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmaddsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub132psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsub132ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmaddsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub132psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsub132ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmaddsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub132psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmaddsub132ps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmaddsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub132psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsub132ps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub132psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsub132ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub132psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsub132ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub132psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmaddsub132ps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub213pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsub213pd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmaddsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub213pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsub213pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmaddsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub213pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsub213pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmaddsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub213pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmaddsub213pd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmaddsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub213pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsub213pd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub213pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsub213pd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub213pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsub213pd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub213pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmaddsub213pd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub213psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsub213ps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmaddsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub213psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsub213ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmaddsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub213psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsub213ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmaddsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub213psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmaddsub213ps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmaddsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub213psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsub213ps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub213psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsub213ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub213psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsub213ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub213psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmaddsub213ps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub231pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsub231pd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmaddsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub231pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsub231pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmaddsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub231pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsub231pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmaddsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub231pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmaddsub231pd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmaddsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub231pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsub231pd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub231pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsub231pd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub231pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsub231pd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub231pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmaddsub231pd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmaddsub231psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsub231ps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmaddsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub231psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsub231ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmaddsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub231psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsub231ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmaddsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub231psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmaddsub231ps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmaddsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub231psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsub231ps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub231psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsub231ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub231psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsub231ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmaddsub231psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmaddsub231ps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub132pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub132pd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub132pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub132pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub132pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsub132pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub132pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsub132pd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub132pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub132pd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub132pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub132pd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub132pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsub132pd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub132pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsub132pd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub132psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub132ps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub132psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub132ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub132psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsub132ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub132psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsub132ps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub132psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub132ps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub132psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub132ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub132psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsub132ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub132psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsub132ps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub132sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub132sd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsub132sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub132sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub132sd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsub132sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub132sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub132sd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub132sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub132sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub132sd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub132sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub132ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub132ss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsub132ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub132ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub132ss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsub132ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub132ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub132ss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub132ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub132ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub132ss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub132ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub213pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub213pd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub213pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub213pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub213pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsub213pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub213pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsub213pd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub213pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub213pd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub213pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub213pd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub213pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsub213pd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub213pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsub213pd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub213psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub213ps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub213psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub213ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub213psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsub213ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub213psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsub213ps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub213psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub213ps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub213psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub213ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub213psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsub213ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub213psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsub213ps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub213sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub213sd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsub213sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub213sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub213sd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsub213sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub213sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub213sd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub213sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub213sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub213sd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub213sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub213ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub213ss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsub213ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub213ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub213ss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsub213ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub213ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub213ss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub213ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub213ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub213ss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub213ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub231pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub231pd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub231pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub231pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub231pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsub231pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub231pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsub231pd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub231pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub231pd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub231pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub231pd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub231pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsub231pd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub231pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsub231pd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsub231psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub231ps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub231psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub231ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub231psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsub231ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub231psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsub231ps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub231psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub231ps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub231psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub231ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub231psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsub231ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub231psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsub231ps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsub231sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub231sd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsub231sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub231sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub231sd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsub231sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub231sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub231sd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub231sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub231sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub231sd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub231sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub231ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub231ss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsub231ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub231ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub231ss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsub231ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub231ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub231ss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub231ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsub231ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub231ss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub231ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfmsubadd132pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubadd132pd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsubadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd132pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubadd132pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsubadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd132pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubadd132pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmsubadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd132pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsubadd132pd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmsubadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd132pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubadd132pd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd132pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubadd132pd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd132pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubadd132pd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd132pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsubadd132pd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd132psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubadd132ps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsubadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd132psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubadd132ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsubadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd132psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubadd132ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmsubadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd132psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsubadd132ps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmsubadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd132psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubadd132ps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd132psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubadd132ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd132psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubadd132ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd132psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsubadd132ps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd213pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubadd213pd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsubadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd213pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubadd213pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsubadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd213pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubadd213pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmsubadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd213pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsubadd213pd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmsubadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd213pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubadd213pd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd213pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubadd213pd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd213pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubadd213pd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd213pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsubadd213pd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd213psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubadd213ps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsubadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd213psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubadd213ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsubadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd213psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubadd213ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmsubadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd213psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsubadd213ps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmsubadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd213psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubadd213ps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd213psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubadd213ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd213psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubadd213ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd213psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsubadd213ps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd231pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubadd231pd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsubadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd231pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubadd231pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsubadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd231pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubadd231pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmsubadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd231pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsubadd231pd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmsubadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd231pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubadd231pd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd231pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubadd231pd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd231pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubadd231pd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd231pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsubadd231pd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfmsubadd231psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubadd231ps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsubadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd231psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubadd231ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsubadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd231psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubadd231ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmsubadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd231psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsubadd231ps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmsubadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd231psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubadd231ps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd231psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubadd231ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd231psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubadd231ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfmsubadd231psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsubadd231ps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd132pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd132pd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd132pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd132pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd132pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmadd132pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfnmadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd132pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmadd132pd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfnmadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd132pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd132pd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd132pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd132pd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd132pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmadd132pd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd132pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmadd132pd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd132psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd132ps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd132psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd132ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd132psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmadd132ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfnmadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd132psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmadd132ps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfnmadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd132psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd132ps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd132psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd132ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd132psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmadd132ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd132psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmadd132ps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd132sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd132sd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmadd132sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd132sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd132sd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd132sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd132sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd132sd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd132sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd132sd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd132ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd132ss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmadd132ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd132ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd132ss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd132ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd132ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd132ss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd132ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd132ss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd213pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd213pd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd213pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd213pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd213pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmadd213pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfnmadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd213pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmadd213pd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfnmadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd213pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd213pd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd213pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd213pd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd213pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmadd213pd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd213pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmadd213pd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd213psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd213ps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd213psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd213ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd213psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmadd213ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfnmadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd213psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmadd213ps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfnmadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd213psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd213ps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd213psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd213ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd213psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmadd213ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd213psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmadd213ps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd213sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd213sd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmadd213sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd213sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd213sd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd213sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd213sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd213sd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd213sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd213sd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd213ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd213ss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmadd213ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd213ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd213ss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd213ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd213ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd213ss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd213ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd213ss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd231pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd231pd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd231pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd231pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd231pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmadd231pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfnmadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd231pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmadd231pd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfnmadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd231pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd231pd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd231pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd231pd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd231pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmadd231pd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd231pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmadd231pd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmadd231psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd231ps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd231psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd231ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd231psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmadd231ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfnmadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd231psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmadd231ps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfnmadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd231psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd231ps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd231psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd231ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd231psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmadd231ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd231psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmadd231ps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmadd231sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd231sd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmadd231sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd231sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd231sd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd231sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd231sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd231sd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd231sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd231sd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd231ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd231ss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmadd231ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd231ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd231ss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd231ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd231ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd231ss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmadd231ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd231ss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub132pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub132pd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub132pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub132pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub132pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsub132pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfnmsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub132pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmsub132pd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfnmsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub132pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub132pd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub132pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub132pd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub132pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsub132pd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub132pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmsub132pd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub132psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub132ps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub132psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub132ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub132psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsub132ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfnmsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub132psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmsub132ps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfnmsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub132psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub132ps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub132psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub132ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub132psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsub132ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub132psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmsub132ps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub132sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub132sd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmsub132sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub132sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub132sd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub132sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub132sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub132sd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub132sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub132sd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub132ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub132ss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmsub132ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub132ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub132ss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub132ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub132ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub132ss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub132ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub132ss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub213pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub213pd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub213pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub213pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub213pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsub213pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfnmsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub213pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmsub213pd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfnmsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub213pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub213pd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub213pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub213pd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub213pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsub213pd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub213pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmsub213pd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub213psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub213ps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub213psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub213ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub213psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsub213ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfnmsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub213psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmsub213ps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfnmsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub213psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub213ps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub213psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub213ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub213psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsub213ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub213psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmsub213ps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub213sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub213sd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmsub213sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub213sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub213sd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub213sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub213sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub213sd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub213sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub213sd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub213ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub213ss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmsub213ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub213ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub213ss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub213ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub213ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub213ss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub213ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub213ss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub231pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub231pd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub231pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub231pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub231pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsub231pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfnmsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub231pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmsub231pd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfnmsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub231pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub231pd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub231pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub231pd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub231pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsub231pd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub231pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmsub231pd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b64}
    }
}

impl Vfnmsub231psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub231ps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub231psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub231ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub231psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsub231ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfnmsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub231psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmsub231ps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfnmsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub231psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub231ps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub231psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub231ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub231psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsub231ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub231psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmsub231ps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|b32}
    }
}

impl Vfnmsub231sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub231sd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmsub231sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub231sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub231sd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub231sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub231sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub231sd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub231sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub231sd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub231ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub231ss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmsub231ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub231ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub231ss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub231ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub231ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub231ss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl Vfnmsub231ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub231ss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // FMA AVX512_F{kz|er}
    }
}

impl VfpclasspdEmitter<KReg, Vec, Imm> for Assembler<'_> {
    fn vfpclasspd(&mut self, op0: KReg, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vfpclasspd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{k|b64}
    }
}

impl<U2: Into<Imm>> VfpclasspdEmitter<KReg, Xmm, U2> for Assembler<'_> {
    fn vfpclasspd(&mut self, op0: KReg, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vfpclasspd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{k|b64}
    }
}

impl<U2: Into<Imm>> VfpclasspdEmitter<KReg, Ymm, U2> for Assembler<'_> {
    fn vfpclasspd(&mut self, op0: KReg, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vfpclasspd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{k|b64}
    }
}

impl<U2: Into<Imm>> VfpclasspdEmitter<KReg, Zmm, U2> for Assembler<'_> {
    fn vfpclasspd(&mut self, op0: KReg, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vfpclasspd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{k|b64}
    }
}

impl<U2: Into<Imm>> VfpclasspdEmitter<KReg, Mem, U2> for Assembler<'_> {
    fn vfpclasspd(&mut self, op0: KReg, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vfpclasspd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{k|b64}
    }
}

impl VfpclasspsEmitter<KReg, Vec, Imm> for Assembler<'_> {
    fn vfpclassps(&mut self, op0: KReg, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vfpclassps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{k|b32}
    }
}

impl<U2: Into<Imm>> VfpclasspsEmitter<KReg, Xmm, U2> for Assembler<'_> {
    fn vfpclassps(&mut self, op0: KReg, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vfpclassps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{k|b32}
    }
}

impl<U2: Into<Imm>> VfpclasspsEmitter<KReg, Ymm, U2> for Assembler<'_> {
    fn vfpclassps(&mut self, op0: KReg, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vfpclassps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{k|b32}
    }
}

impl<U2: Into<Imm>> VfpclasspsEmitter<KReg, Zmm, U2> for Assembler<'_> {
    fn vfpclassps(&mut self, op0: KReg, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vfpclassps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{k|b32}
    }
}

impl<U2: Into<Imm>> VfpclasspsEmitter<KReg, Mem, U2> for Assembler<'_> {
    fn vfpclassps(&mut self, op0: KReg, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vfpclassps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{k|b32}
    }
}

impl VfpclasssdEmitter<KReg, Vec, Imm> for Assembler<'_> {
    fn vfpclasssd(&mut self, op0: KReg, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vfpclasssd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{k}
    }
}

impl<U2: Into<Imm>> VfpclasssdEmitter<KReg, Xmm, U2> for Assembler<'_> {
    fn vfpclasssd(&mut self, op0: KReg, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vfpclasssd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{k}
    }
}

impl<U2: Into<Imm>> VfpclasssdEmitter<KReg, Mem, U2> for Assembler<'_> {
    fn vfpclasssd(&mut self, op0: KReg, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vfpclasssd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{k}
    }
}

impl VfpclassssEmitter<KReg, Vec, Imm> for Assembler<'_> {
    fn vfpclassss(&mut self, op0: KReg, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vfpclassss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{k}
    }
}

impl<U2: Into<Imm>> VfpclassssEmitter<KReg, Xmm, U2> for Assembler<'_> {
    fn vfpclassss(&mut self, op0: KReg, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vfpclassss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{k}
    }
}

impl<U2: Into<Imm>> VfpclassssEmitter<KReg, Mem, U2> for Assembler<'_> {
    fn vfpclassss(&mut self, op0: KReg, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vfpclassss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{k}
    }
}

impl VgatherdpdEmitter<Vec, Mem> for Assembler<'_> {
    fn vgatherdpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vgatherdpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherdpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vgatherdpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vgatherdpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherdpdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vgatherdpd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vgatherdpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherdpdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vgatherdpd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vgatherdpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl Vgatherdpd3Emitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vgatherdpd_3(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Vgatherdpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl Vgatherdpd3Emitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vgatherdpd_3(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Vgatherdpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl Vgatherdpd3Emitter<Ymm, Mem, Ymm> for Assembler<'_> {
    fn vgatherdpd_3(&mut self, op0: Ymm, op1: Mem, op2: Ymm) {
        self.emit_n(
            InstId::Vgatherdpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl VgatherdpsEmitter<Vec, Mem> for Assembler<'_> {
    fn vgatherdps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vgatherdps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherdpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vgatherdps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vgatherdps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherdpsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vgatherdps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vgatherdps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherdpsEmitter<Zmm, Mem> for Assembler<'_> {
    fn vgatherdps(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vgatherdps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl Vgatherdps3Emitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vgatherdps_3(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Vgatherdps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl Vgatherdps3Emitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vgatherdps_3(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Vgatherdps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl Vgatherdps3Emitter<Ymm, Mem, Ymm> for Assembler<'_> {
    fn vgatherdps_3(&mut self, op0: Ymm, op1: Mem, op2: Ymm) {
        self.emit_n(
            InstId::Vgatherdps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl VgatherqpdEmitter<Vec, Mem> for Assembler<'_> {
    fn vgatherqpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vgatherqpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherqpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vgatherqpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vgatherqpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherqpdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vgatherqpd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vgatherqpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherqpdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vgatherqpd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vgatherqpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl Vgatherqpd3Emitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vgatherqpd_3(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Vgatherqpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl Vgatherqpd3Emitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vgatherqpd_3(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Vgatherqpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl Vgatherqpd3Emitter<Ymm, Mem, Ymm> for Assembler<'_> {
    fn vgatherqpd_3(&mut self, op0: Ymm, op1: Mem, op2: Ymm) {
        self.emit_n(
            InstId::Vgatherqpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl VgatherqpsEmitter<Vec, Mem> for Assembler<'_> {
    fn vgatherqps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vgatherqps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherqpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vgatherqps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vgatherqps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VgatherqpsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vgatherqps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vgatherqps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl Vgatherqps3Emitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vgatherqps_3(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Vgatherqps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl Vgatherqps3Emitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vgatherqps_3(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Vgatherqps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl VgetexppdEmitter<Vec, Vec> for Assembler<'_> {
    fn vgetexppd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vgetexppd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VgetexppdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vgetexppd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vgetexppd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VgetexppdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vgetexppd(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vgetexppd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VgetexppdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vgetexppd(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vgetexppd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VgetexppdEmitter<Vec, Mem> for Assembler<'_> {
    fn vgetexppd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vgetexppd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VgetexppdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vgetexppd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vgetexppd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VgetexppdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vgetexppd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vgetexppd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VgetexppdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vgetexppd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vgetexppd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VgetexppsEmitter<Vec, Vec> for Assembler<'_> {
    fn vgetexpps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vgetexpps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VgetexppsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vgetexpps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vgetexpps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VgetexppsEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vgetexpps(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vgetexpps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VgetexppsEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vgetexpps(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vgetexpps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VgetexppsEmitter<Vec, Mem> for Assembler<'_> {
    fn vgetexpps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vgetexpps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VgetexppsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vgetexpps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vgetexpps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VgetexppsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vgetexpps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vgetexpps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VgetexppsEmitter<Zmm, Mem> for Assembler<'_> {
    fn vgetexpps(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vgetexpps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VgetexpsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vgetexpsd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vgetexpsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetexpsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vgetexpsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vgetexpsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetexpsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vgetexpsd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vgetexpsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetexpsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vgetexpsd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vgetexpsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetexpssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vgetexpss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vgetexpss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetexpssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vgetexpss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vgetexpss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetexpssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vgetexpss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vgetexpss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetexpssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vgetexpss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vgetexpss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetmantpdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vgetmantpd(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vgetmantpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VgetmantpdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vgetmantpd(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vgetmantpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VgetmantpdEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vgetmantpd(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vgetmantpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VgetmantpdEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vgetmantpd(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vgetmantpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VgetmantpdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vgetmantpd(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vgetmantpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VgetmantpdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vgetmantpd(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vgetmantpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VgetmantpdEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vgetmantpd(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vgetmantpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VgetmantpdEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vgetmantpd(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vgetmantpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VgetmantpsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vgetmantps(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vgetmantps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VgetmantpsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vgetmantps(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vgetmantps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VgetmantpsEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vgetmantps(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vgetmantps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VgetmantpsEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vgetmantps(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vgetmantps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VgetmantpsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vgetmantps(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vgetmantps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VgetmantpsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vgetmantps(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vgetmantps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VgetmantpsEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vgetmantps(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vgetmantps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VgetmantpsEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vgetmantps(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vgetmantps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VgetmantsdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vgetmantsd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vgetmantsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VgetmantsdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vgetmantsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vgetmantsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetmantsdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vgetmantsd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vgetmantsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VgetmantsdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vgetmantsd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vgetmantsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetmantssEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vgetmantss(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vgetmantss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VgetmantssEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vgetmantss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vgetmantss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VgetmantssEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vgetmantss(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vgetmantss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VgetmantssEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vgetmantss(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vgetmantss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl Vgf2p8affineinvqbEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vgf2p8affineinvqb(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vgf2p8affineinvqb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineinvqbEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vgf2p8affineinvqb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineinvqb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineinvqbEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vgf2p8affineinvqb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineinvqb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineinvqbEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vgf2p8affineinvqb(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineinvqb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8affineinvqbEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vgf2p8affineinvqb(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vgf2p8affineinvqb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineinvqbEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vgf2p8affineinvqb(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineinvqb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineinvqbEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vgf2p8affineinvqb(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineinvqb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineinvqbEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vgf2p8affineinvqb(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineinvqb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8affineqbEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vgf2p8affineqb(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vgf2p8affineqb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineqbEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vgf2p8affineqb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineqb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineqbEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vgf2p8affineqb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineqb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineqbEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vgf2p8affineqb(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineqb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8affineqbEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vgf2p8affineqb(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vgf2p8affineqb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineqbEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vgf2p8affineqb(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineqb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineqbEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vgf2p8affineqb(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineqb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl<U3: Into<Imm>> Vgf2p8affineqbEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vgf2p8affineqb(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vgf2p8affineqb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8mulbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vgf2p8mulb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vgf2p8mulb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8mulbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vgf2p8mulb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vgf2p8mulb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8mulbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vgf2p8mulb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vgf2p8mulb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8mulbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vgf2p8mulb(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vgf2p8mulb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8mulbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vgf2p8mulb(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vgf2p8mulb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8mulbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vgf2p8mulb(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vgf2p8mulb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8mulbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vgf2p8mulb(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vgf2p8mulb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl Vgf2p8mulbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vgf2p8mulb(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vgf2p8mulb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_VL{kz} GFNI
    }
}

impl VhaddpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vhaddpd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vhaddpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhaddpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vhaddpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vhaddpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhaddpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vhaddpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vhaddpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhaddpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vhaddpd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vhaddpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhaddpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vhaddpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vhaddpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhaddpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vhaddpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vhaddpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhaddpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vhaddps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vhaddps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhaddpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vhaddps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vhaddps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhaddpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vhaddps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vhaddps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhaddpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vhaddps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vhaddps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhaddpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vhaddps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vhaddps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhaddpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vhaddps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vhaddps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhsubpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vhsubpd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vhsubpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhsubpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vhsubpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vhsubpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhsubpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vhsubpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vhsubpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhsubpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vhsubpd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vhsubpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhsubpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vhsubpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vhsubpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhsubpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vhsubpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vhsubpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhsubpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vhsubps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vhsubps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhsubpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vhsubps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vhsubps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhsubpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vhsubps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vhsubps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhsubpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vhsubps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vhsubps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhsubpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vhsubps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vhsubps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VhsubpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vhsubps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vhsubps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl Vinsertf128Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinsertf128(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vinsertf128 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> Vinsertf128Emitter<Ymm, Ymm, Xmm, U3> for Assembler<'_> {
    fn vinsertf128(&mut self, op0: Ymm, op1: Ymm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vinsertf128 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl Vinsertf128Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinsertf128(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vinsertf128 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> Vinsertf128Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vinsertf128(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vinsertf128 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl Vinsertf32x4Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinsertf32x4(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vinsertf32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf32x4Emitter<Ymm, Ymm, Xmm, U3> for Assembler<'_> {
    fn vinsertf32x4(&mut self, op0: Ymm, op1: Ymm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vinsertf32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf32x4Emitter<Zmm, Zmm, Xmm, U3> for Assembler<'_> {
    fn vinsertf32x4(&mut self, op0: Zmm, op1: Zmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vinsertf32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vinsertf32x4Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinsertf32x4(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vinsertf32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf32x4Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vinsertf32x4(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vinsertf32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf32x4Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vinsertf32x4(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vinsertf32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vinsertf32x8Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinsertf32x8(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vinsertf32x8 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf32x8Emitter<Zmm, Zmm, Ymm, U3> for Assembler<'_> {
    fn vinsertf32x8(&mut self, op0: Zmm, op1: Zmm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vinsertf32x8 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vinsertf32x8Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinsertf32x8(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vinsertf32x8 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf32x8Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vinsertf32x8(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vinsertf32x8 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vinsertf64x2Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinsertf64x2(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vinsertf64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf64x2Emitter<Ymm, Ymm, Xmm, U3> for Assembler<'_> {
    fn vinsertf64x2(&mut self, op0: Ymm, op1: Ymm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vinsertf64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf64x2Emitter<Zmm, Zmm, Xmm, U3> for Assembler<'_> {
    fn vinsertf64x2(&mut self, op0: Zmm, op1: Zmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vinsertf64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vinsertf64x2Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinsertf64x2(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vinsertf64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf64x2Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vinsertf64x2(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vinsertf64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf64x2Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vinsertf64x2(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vinsertf64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vinsertf64x4Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinsertf64x4(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vinsertf64x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf64x4Emitter<Zmm, Zmm, Ymm, U3> for Assembler<'_> {
    fn vinsertf64x4(&mut self, op0: Zmm, op1: Zmm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vinsertf64x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vinsertf64x4Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinsertf64x4(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vinsertf64x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinsertf64x4Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vinsertf64x4(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vinsertf64x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vinserti128Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinserti128(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vinserti128 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX2
    }
}

impl<U3: Into<Imm>> Vinserti128Emitter<Ymm, Ymm, Xmm, U3> for Assembler<'_> {
    fn vinserti128(&mut self, op0: Ymm, op1: Ymm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vinserti128 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX2
    }
}

impl Vinserti128Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinserti128(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vinserti128 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX2
    }
}

impl<U3: Into<Imm>> Vinserti128Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vinserti128(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vinserti128 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX2
    }
}

impl Vinserti32x4Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinserti32x4(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vinserti32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinserti32x4Emitter<Ymm, Ymm, Xmm, U3> for Assembler<'_> {
    fn vinserti32x4(&mut self, op0: Ymm, op1: Ymm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vinserti32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinserti32x4Emitter<Zmm, Zmm, Xmm, U3> for Assembler<'_> {
    fn vinserti32x4(&mut self, op0: Zmm, op1: Zmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vinserti32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vinserti32x4Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinserti32x4(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vinserti32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinserti32x4Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vinserti32x4(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vinserti32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinserti32x4Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vinserti32x4(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vinserti32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vinserti32x8Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinserti32x8(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vinserti32x8 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinserti32x8Emitter<Zmm, Zmm, Ymm, U3> for Assembler<'_> {
    fn vinserti32x8(&mut self, op0: Zmm, op1: Zmm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vinserti32x8 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vinserti32x8Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinserti32x8(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vinserti32x8 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinserti32x8Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vinserti32x8(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vinserti32x8 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vinserti64x2Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinserti64x2(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vinserti64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinserti64x2Emitter<Ymm, Ymm, Xmm, U3> for Assembler<'_> {
    fn vinserti64x2(&mut self, op0: Ymm, op1: Ymm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vinserti64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinserti64x2Emitter<Zmm, Zmm, Xmm, U3> for Assembler<'_> {
    fn vinserti64x2(&mut self, op0: Zmm, op1: Zmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vinserti64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vinserti64x2Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinserti64x2(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vinserti64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinserti64x2Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vinserti64x2(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vinserti64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> Vinserti64x2Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vinserti64x2(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vinserti64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl Vinserti64x4Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinserti64x4(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vinserti64x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinserti64x4Emitter<Zmm, Zmm, Ymm, U3> for Assembler<'_> {
    fn vinserti64x4(&mut self, op0: Zmm, op1: Zmm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vinserti64x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl Vinserti64x4Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinserti64x4(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vinserti64x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl<U3: Into<Imm>> Vinserti64x4Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vinserti64x4(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vinserti64x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz}
    }
}

impl VinsertpsEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vinsertps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vinsertps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX AVX512_F
    }
}

impl<U3: Into<Imm>> VinsertpsEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vinsertps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vinsertps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_F
    }
}

impl VinsertpsEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vinsertps(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vinsertps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX AVX512_F
    }
}

impl<U3: Into<Imm>> VinsertpsEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vinsertps(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vinsertps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_F
    }
}

impl VlddquEmitter<Vec, Mem> for Assembler<'_> {
    fn vlddqu(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Vlddqu as u32, &[op0.as_operand(), op1.as_operand()]); // AVX
    }
}

impl VlddquEmitter<Xmm, Mem> for Assembler<'_> {
    fn vlddqu(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Vlddqu as u32, &[op0.as_operand(), op1.as_operand()]); // AVX
    }
}

impl VlddquEmitter<Ymm, Mem> for Assembler<'_> {
    fn vlddqu(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(InstId::Vlddqu as u32, &[op0.as_operand(), op1.as_operand()]); // AVX
    }
}

impl VldmxcsrEmitter<Mem> for Assembler<'_> {
    fn vldmxcsr(&mut self, op0: Mem) {
        self.emit_n(InstId::Vldmxcsr as u32, &[op0.as_operand()]); // AVX
    }
}

impl VmaskmovdquEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmaskmovdqu(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vmaskmovdqu as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX [EXPLICIT]
    }
}

impl VmaskmovdquEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmaskmovdqu(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vmaskmovdqu as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX [EXPLICIT]
    }
}

impl VmaskmovpdEmitter<Mem, Vec, Vec> for Assembler<'_> {
    fn vmaskmovpd(&mut self, op0: Mem, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vmaskmovpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpdEmitter<Mem, Xmm, Xmm> for Assembler<'_> {
    fn vmaskmovpd(&mut self, op0: Mem, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vmaskmovpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpdEmitter<Mem, Ymm, Ymm> for Assembler<'_> {
    fn vmaskmovpd(&mut self, op0: Mem, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vmaskmovpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmaskmovpd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vmaskmovpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmaskmovpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vmaskmovpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vmaskmovpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vmaskmovpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpsEmitter<Mem, Vec, Vec> for Assembler<'_> {
    fn vmaskmovps(&mut self, op0: Mem, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vmaskmovps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpsEmitter<Mem, Xmm, Xmm> for Assembler<'_> {
    fn vmaskmovps(&mut self, op0: Mem, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vmaskmovps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpsEmitter<Mem, Ymm, Ymm> for Assembler<'_> {
    fn vmaskmovps(&mut self, op0: Mem, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vmaskmovps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmaskmovps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vmaskmovps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmaskmovps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vmaskmovps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VmaskmovpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vmaskmovps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vmaskmovps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VmaxpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmaxpd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vmaxpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmaxpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmaxpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vmaxpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmaxpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vmaxpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vmaxpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmaxpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vmaxpd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vmaxpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmaxpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmaxpd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vmaxpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmaxpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmaxpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vmaxpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmaxpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vmaxpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vmaxpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmaxpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vmaxpd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vmaxpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmaxpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmaxps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vmaxps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmaxpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmaxps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vmaxps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmaxpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vmaxps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vmaxps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmaxpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vmaxps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vmaxps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmaxpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmaxps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vmaxps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmaxpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmaxps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vmaxps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmaxpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vmaxps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vmaxps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmaxpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vmaxps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vmaxps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmaxsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmaxsd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vmaxsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VmaxsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmaxsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vmaxsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VmaxsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmaxsd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vmaxsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VmaxsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmaxsd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vmaxsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VmaxssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmaxss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vmaxss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VmaxssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmaxss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vmaxss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VmaxssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmaxss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vmaxss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VmaxssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmaxss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vmaxss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VminpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vminpd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vminpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VminpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vminpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vminpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VminpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vminpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vminpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VminpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vminpd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vminpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VminpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vminpd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vminpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VminpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vminpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vminpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VminpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vminpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vminpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VminpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vminpd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vminpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VminpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vminps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vminps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VminpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vminps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vminps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VminpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vminps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vminps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VminpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vminps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vminps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VminpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vminps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vminps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VminpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vminps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vminps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VminpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vminps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vminps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VminpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vminps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vminps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VminsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vminsd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vminsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VminsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vminsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vminsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VminsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vminsd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vminsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VminsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vminsd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vminsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VminssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vminss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vminss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VminssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vminss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vminss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VminssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vminss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vminss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VminssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vminss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vminss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|sae}
    }
}

impl VmovapdEmitter<Vec, Vec> for Assembler<'_> {
    fn vmovapd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovapd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovapd(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovapd(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovapd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovapd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vmovapd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vmovapd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovapd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovapd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vmovapd(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapdEmitter<Mem, Zmm> for Assembler<'_> {
    fn vmovapd(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vmovapd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Vec, Vec> for Assembler<'_> {
    fn vmovaps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovaps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovaps(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovaps(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovaps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovaps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vmovaps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Zmm, Mem> for Assembler<'_> {
    fn vmovaps(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovaps(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovaps(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Mem, Ymm> for Assembler<'_> {
    fn vmovaps(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovapsEmitter<Mem, Zmm> for Assembler<'_> {
    fn vmovaps(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vmovaps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovdEmitter<Gp, Vec> for Assembler<'_> {
    fn vmovd(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(InstId::Vmovd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovdEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vmovd(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(InstId::Vmovd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovdEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(InstId::Vmovd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(InstId::Vmovd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovdEmitter<Vec, Gp> for Assembler<'_> {
    fn vmovd(&mut self, op0: Vec, op1: Gp) {
        self.emit_n(InstId::Vmovd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovdEmitter<Xmm, Gpd> for Assembler<'_> {
    fn vmovd(&mut self, op0: Xmm, op1: Gpd) {
        self.emit_n(InstId::Vmovd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovdEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Vmovd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Vmovd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovddupEmitter<Vec, Vec> for Assembler<'_> {
    fn vmovddup(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vmovddup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovddupEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovddup(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vmovddup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovddupEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovddup(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vmovddup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovddupEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovddup(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vmovddup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovddupEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovddup(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vmovddup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovddupEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovddup(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovddup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovddupEmitter<Ymm, Mem> for Assembler<'_> {
    fn vmovddup(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vmovddup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovddupEmitter<Zmm, Mem> for Assembler<'_> {
    fn vmovddup(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovddup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovdqaEmitter<Vec, Vec> for Assembler<'_> {
    fn vmovdqa(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vmovdqa as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovdqaEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovdqa(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vmovdqa as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovdqaEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovdqa(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vmovdqa as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovdqaEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovdqa(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqa as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovdqaEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovdqa(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqa as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovdqaEmitter<Ymm, Mem> for Assembler<'_> {
    fn vmovdqa(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqa as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovdqaEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovdqa(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vmovdqa as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovdqaEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovdqa(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vmovdqa as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovdqaEmitter<Mem, Ymm> for Assembler<'_> {
    fn vmovdqa(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vmovdqa as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl Vmovdqa32Emitter<Vec, Vec> for Assembler<'_> {
    fn vmovdqa32(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovdqa32(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovdqa32(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovdqa32(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Vec, Mem> for Assembler<'_> {
    fn vmovdqa32(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Xmm, Mem> for Assembler<'_> {
    fn vmovdqa32(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Ymm, Mem> for Assembler<'_> {
    fn vmovdqa32(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Zmm, Mem> for Assembler<'_> {
    fn vmovdqa32(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Mem, Vec> for Assembler<'_> {
    fn vmovdqa32(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Mem, Xmm> for Assembler<'_> {
    fn vmovdqa32(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Mem, Ymm> for Assembler<'_> {
    fn vmovdqa32(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa32Emitter<Mem, Zmm> for Assembler<'_> {
    fn vmovdqa32(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vmovdqa32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Vec, Vec> for Assembler<'_> {
    fn vmovdqa64(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovdqa64(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovdqa64(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovdqa64(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Vec, Mem> for Assembler<'_> {
    fn vmovdqa64(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Xmm, Mem> for Assembler<'_> {
    fn vmovdqa64(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Ymm, Mem> for Assembler<'_> {
    fn vmovdqa64(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Zmm, Mem> for Assembler<'_> {
    fn vmovdqa64(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Mem, Vec> for Assembler<'_> {
    fn vmovdqa64(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Mem, Xmm> for Assembler<'_> {
    fn vmovdqa64(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Mem, Ymm> for Assembler<'_> {
    fn vmovdqa64(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqa64Emitter<Mem, Zmm> for Assembler<'_> {
    fn vmovdqa64(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vmovdqa64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VmovdquEmitter<Vec, Vec> for Assembler<'_> {
    fn vmovdqu(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vmovdqu as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovdquEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovdqu(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vmovdqu as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovdquEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovdqu(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vmovdqu as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovdquEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovdqu(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqu as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovdquEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovdqu(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqu as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovdquEmitter<Ymm, Mem> for Assembler<'_> {
    fn vmovdqu(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqu as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovdquEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovdqu(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vmovdqu as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovdquEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovdqu(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vmovdqu as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovdquEmitter<Mem, Ymm> for Assembler<'_> {
    fn vmovdqu(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vmovdqu as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl Vmovdqu16Emitter<Vec, Vec> for Assembler<'_> {
    fn vmovdqu16(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovdqu16(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovdqu16(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovdqu16(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Vec, Mem> for Assembler<'_> {
    fn vmovdqu16(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Xmm, Mem> for Assembler<'_> {
    fn vmovdqu16(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Ymm, Mem> for Assembler<'_> {
    fn vmovdqu16(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Zmm, Mem> for Assembler<'_> {
    fn vmovdqu16(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Mem, Vec> for Assembler<'_> {
    fn vmovdqu16(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Mem, Xmm> for Assembler<'_> {
    fn vmovdqu16(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Mem, Ymm> for Assembler<'_> {
    fn vmovdqu16(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu16Emitter<Mem, Zmm> for Assembler<'_> {
    fn vmovdqu16(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vmovdqu16 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu32Emitter<Vec, Vec> for Assembler<'_> {
    fn vmovdqu32(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovdqu32(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovdqu32(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovdqu32(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Vec, Mem> for Assembler<'_> {
    fn vmovdqu32(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Xmm, Mem> for Assembler<'_> {
    fn vmovdqu32(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Ymm, Mem> for Assembler<'_> {
    fn vmovdqu32(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Zmm, Mem> for Assembler<'_> {
    fn vmovdqu32(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Mem, Vec> for Assembler<'_> {
    fn vmovdqu32(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Mem, Xmm> for Assembler<'_> {
    fn vmovdqu32(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Mem, Ymm> for Assembler<'_> {
    fn vmovdqu32(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu32Emitter<Mem, Zmm> for Assembler<'_> {
    fn vmovdqu32(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vmovdqu32 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Vec, Vec> for Assembler<'_> {
    fn vmovdqu64(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovdqu64(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovdqu64(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovdqu64(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Vec, Mem> for Assembler<'_> {
    fn vmovdqu64(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Xmm, Mem> for Assembler<'_> {
    fn vmovdqu64(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Ymm, Mem> for Assembler<'_> {
    fn vmovdqu64(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Zmm, Mem> for Assembler<'_> {
    fn vmovdqu64(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Mem, Vec> for Assembler<'_> {
    fn vmovdqu64(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Mem, Xmm> for Assembler<'_> {
    fn vmovdqu64(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Mem, Ymm> for Assembler<'_> {
    fn vmovdqu64(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu64Emitter<Mem, Zmm> for Assembler<'_> {
    fn vmovdqu64(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vmovdqu64 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vmovdqu8Emitter<Vec, Vec> for Assembler<'_> {
    fn vmovdqu8(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovdqu8(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovdqu8(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovdqu8(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Vec, Mem> for Assembler<'_> {
    fn vmovdqu8(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Xmm, Mem> for Assembler<'_> {
    fn vmovdqu8(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Ymm, Mem> for Assembler<'_> {
    fn vmovdqu8(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Zmm, Mem> for Assembler<'_> {
    fn vmovdqu8(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Mem, Vec> for Assembler<'_> {
    fn vmovdqu8(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Mem, Xmm> for Assembler<'_> {
    fn vmovdqu8(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Mem, Ymm> for Assembler<'_> {
    fn vmovdqu8(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vmovdqu8Emitter<Mem, Zmm> for Assembler<'_> {
    fn vmovdqu8(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vmovdqu8 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VmovhlpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmovhlps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vmovhlps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovhlpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmovhlps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vmovhlps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovhpdEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovhpd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vmovhpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovhpdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovhpd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vmovhpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F
    }
}

impl Vmovhpd3Emitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmovhpd_3(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vmovhpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl Vmovhpd3Emitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmovhpd_3(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vmovhpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovhpsEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovhps(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vmovhps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovhpsEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovhps(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vmovhps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F
    }
}

impl Vmovhps3Emitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmovhps_3(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vmovhps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl Vmovhps3Emitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmovhps_3(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vmovhps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovlhpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmovlhps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vmovlhps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovlhpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmovlhps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vmovlhps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovlpdEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovlpd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vmovlpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovlpdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovlpd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vmovlpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F
    }
}

impl Vmovlpd3Emitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmovlpd_3(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vmovlpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl Vmovlpd3Emitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmovlpd_3(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vmovlpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovlpsEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovlps(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vmovlps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovlpsEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovlps(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vmovlps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F
    }
}

impl Vmovlps3Emitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmovlps_3(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vmovlps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl Vmovlps3Emitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmovlps_3(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vmovlps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovmskpdEmitter<Gp, Vec> for Assembler<'_> {
    fn vmovmskpd(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Vmovmskpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovmskpdEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vmovmskpd(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Vmovmskpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovmskpdEmitter<Gpd, Ymm> for Assembler<'_> {
    fn vmovmskpd(&mut self, op0: Gpd, op1: Ymm) {
        self.emit_n(
            InstId::Vmovmskpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovmskpsEmitter<Gp, Vec> for Assembler<'_> {
    fn vmovmskps(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Vmovmskps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovmskpsEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vmovmskps(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Vmovmskps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovmskpsEmitter<Gpd, Ymm> for Assembler<'_> {
    fn vmovmskps(&mut self, op0: Gpd, op1: Ymm) {
        self.emit_n(
            InstId::Vmovmskps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VmovntdqEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovntdq(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vmovntdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F
    }
}

impl VmovntdqEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovntdq(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vmovntdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F
    }
}

impl VmovntdqEmitter<Mem, Ymm> for Assembler<'_> {
    fn vmovntdq(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vmovntdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F
    }
}

impl VmovntdqEmitter<Mem, Zmm> for Assembler<'_> {
    fn vmovntdq(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vmovntdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F
    }
}

impl VmovntdqaEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovntdqa(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vmovntdqa as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F
    }
}

impl VmovntdqaEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovntdqa(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovntdqa as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F
    }
}

impl VmovntdqaEmitter<Ymm, Mem> for Assembler<'_> {
    fn vmovntdqa(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vmovntdqa as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F
    }
}

impl VmovntdqaEmitter<Zmm, Mem> for Assembler<'_> {
    fn vmovntdqa(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovntdqa as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F
    }
}

impl VmovntpdEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovntpd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vmovntpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovntpdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovntpd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vmovntpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovntpdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vmovntpd(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vmovntpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovntpdEmitter<Mem, Zmm> for Assembler<'_> {
    fn vmovntpd(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vmovntpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovntpsEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovntps(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vmovntps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovntpsEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovntps(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vmovntps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovntpsEmitter<Mem, Ymm> for Assembler<'_> {
    fn vmovntps(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vmovntps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovntpsEmitter<Mem, Zmm> for Assembler<'_> {
    fn vmovntps(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vmovntps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F
    }
}

impl VmovqEmitter<Gp, Vec> for Assembler<'_> {
    fn vmovq(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(InstId::Vmovq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovqEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vmovq(&mut self, op0: Gpq, op1: Xmm) {
        self.emit_n(InstId::Vmovq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovqEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovq(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(InstId::Vmovq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovqEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovq(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(InstId::Vmovq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovqEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Vmovq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Vmovq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovqEmitter<Vec, Gp> for Assembler<'_> {
    fn vmovq(&mut self, op0: Vec, op1: Gp) {
        self.emit_n(InstId::Vmovq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovqEmitter<Xmm, Gpq> for Assembler<'_> {
    fn vmovq(&mut self, op0: Xmm, op1: Gpq) {
        self.emit_n(InstId::Vmovq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovqEmitter<Vec, Vec> for Assembler<'_> {
    fn vmovq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Vmovq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Vmovq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovsdEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovsd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(InstId::Vmovsd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovsdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovsd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(InstId::Vmovsd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovsdEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovsd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Vmovsd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F{kz}
    }
}

impl VmovsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovsd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Vmovsd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F{kz}
    }
}

impl Vmovsd3Emitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmovsd_3(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vmovsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl Vmovsd3Emitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmovsd_3(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vmovsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovshdupEmitter<Vec, Vec> for Assembler<'_> {
    fn vmovshdup(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vmovshdup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovshdupEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovshdup(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vmovshdup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovshdupEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovshdup(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vmovshdup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovshdupEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovshdup(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vmovshdup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovshdupEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovshdup(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vmovshdup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovshdupEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovshdup(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovshdup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovshdupEmitter<Ymm, Mem> for Assembler<'_> {
    fn vmovshdup(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vmovshdup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovshdupEmitter<Zmm, Mem> for Assembler<'_> {
    fn vmovshdup(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovshdup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovsldupEmitter<Vec, Vec> for Assembler<'_> {
    fn vmovsldup(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vmovsldup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovsldupEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovsldup(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vmovsldup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovsldupEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovsldup(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vmovsldup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovsldupEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovsldup(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vmovsldup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovsldupEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovsldup(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vmovsldup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovsldupEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovsldup(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovsldup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovsldupEmitter<Ymm, Mem> for Assembler<'_> {
    fn vmovsldup(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vmovsldup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovsldupEmitter<Zmm, Mem> for Assembler<'_> {
    fn vmovsldup(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovsldup as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovssEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovss(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(InstId::Vmovss as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovssEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovss(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(InstId::Vmovss as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F
    }
}

impl VmovssEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovss(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Vmovss as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F{kz}
    }
}

impl VmovssEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovss(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Vmovss as u32, &[op0.as_operand(), op1.as_operand()]); // AVX AVX512_F{kz}
    }
}

impl Vmovss3Emitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmovss_3(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vmovss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl Vmovss3Emitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmovss_3(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vmovss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Vec, Vec> for Assembler<'_> {
    fn vmovupd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovupd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovupd(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovupd(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovupd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovupd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vmovupd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vmovupd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovupd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovupd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vmovupd(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupdEmitter<Mem, Zmm> for Assembler<'_> {
    fn vmovupd(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vmovupd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Vec, Vec> for Assembler<'_> {
    fn vmovups(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vmovups(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vmovups(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vmovups(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovups(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovups(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vmovups(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Zmm, Mem> for Assembler<'_> {
    fn vmovups(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovups(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovups(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Mem, Ymm> for Assembler<'_> {
    fn vmovups(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmovupsEmitter<Mem, Zmm> for Assembler<'_> {
    fn vmovups(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vmovups as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VmpsadbwEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vmpsadbw(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vmpsadbw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX+
    }
}

impl<U3: Into<Imm>> VmpsadbwEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vmpsadbw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vmpsadbw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX+
    }
}

impl<U3: Into<Imm>> VmpsadbwEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vmpsadbw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vmpsadbw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX+
    }
}

impl VmpsadbwEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vmpsadbw(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vmpsadbw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX+
    }
}

impl<U3: Into<Imm>> VmpsadbwEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vmpsadbw(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vmpsadbw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX+
    }
}

impl<U3: Into<Imm>> VmpsadbwEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vmpsadbw(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vmpsadbw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX+
    }
}

impl VmulpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmulpd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vmulpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmulpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmulpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vmulpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmulpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vmulpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vmulpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmulpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vmulpd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vmulpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmulpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmulpd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vmulpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmulpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmulpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vmulpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmulpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vmulpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vmulpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmulpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vmulpd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vmulpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VmulpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmulps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vmulps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmulpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmulps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vmulps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmulpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vmulps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vmulps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmulpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vmulps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vmulps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmulpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmulps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vmulps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmulpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmulps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vmulps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmulpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vmulps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vmulps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmulpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vmulps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vmulps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VmulsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmulsd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vmulsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VmulsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmulsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vmulsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VmulsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmulsd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vmulsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VmulsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmulsd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vmulsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VmulssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmulss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vmulss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VmulssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmulss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vmulss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VmulssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmulss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vmulss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VmulssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmulss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vmulss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VorpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vorpd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vorpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VorpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vorpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vorpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VorpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vorpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vorpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VorpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vorpd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vorpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VorpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vorpd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vorpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VorpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vorpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vorpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VorpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vorpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vorpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VorpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vorpd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vorpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VorpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vorps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vorps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VorpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vorps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vorps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VorpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vorps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vorps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VorpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vorps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vorps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VorpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vorps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vorps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VorpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vorps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vorps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VorpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vorps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vorps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VorpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vorps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vorps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl Vp2intersectdEmitter<KReg, KReg, Vec, Vec> for Assembler<'_> {
    fn vp2intersectd(&mut self, op0: KReg, op1: KReg, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vp2intersectd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectdEmitter<KReg, KReg, Xmm, Xmm> for Assembler<'_> {
    fn vp2intersectd(&mut self, op0: KReg, op1: KReg, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vp2intersectd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectdEmitter<KReg, KReg, Ymm, Ymm> for Assembler<'_> {
    fn vp2intersectd(&mut self, op0: KReg, op1: KReg, op2: Ymm, op3: Ymm) {
        self.emit_n(
            InstId::Vp2intersectd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectdEmitter<KReg, KReg, Zmm, Zmm> for Assembler<'_> {
    fn vp2intersectd(&mut self, op0: KReg, op1: KReg, op2: Zmm, op3: Zmm) {
        self.emit_n(
            InstId::Vp2intersectd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectdEmitter<KReg, KReg, Vec, Mem> for Assembler<'_> {
    fn vp2intersectd(&mut self, op0: KReg, op1: KReg, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vp2intersectd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectdEmitter<KReg, KReg, Xmm, Mem> for Assembler<'_> {
    fn vp2intersectd(&mut self, op0: KReg, op1: KReg, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vp2intersectd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectdEmitter<KReg, KReg, Ymm, Mem> for Assembler<'_> {
    fn vp2intersectd(&mut self, op0: KReg, op1: KReg, op2: Ymm, op3: Mem) {
        self.emit_n(
            InstId::Vp2intersectd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectdEmitter<KReg, KReg, Zmm, Mem> for Assembler<'_> {
    fn vp2intersectd(&mut self, op0: KReg, op1: KReg, op2: Zmm, op3: Mem) {
        self.emit_n(
            InstId::Vp2intersectd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectqEmitter<KReg, KReg, Vec, Vec> for Assembler<'_> {
    fn vp2intersectq(&mut self, op0: KReg, op1: KReg, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vp2intersectq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectqEmitter<KReg, KReg, Xmm, Xmm> for Assembler<'_> {
    fn vp2intersectq(&mut self, op0: KReg, op1: KReg, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vp2intersectq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectqEmitter<KReg, KReg, Ymm, Ymm> for Assembler<'_> {
    fn vp2intersectq(&mut self, op0: KReg, op1: KReg, op2: Ymm, op3: Ymm) {
        self.emit_n(
            InstId::Vp2intersectq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectqEmitter<KReg, KReg, Zmm, Zmm> for Assembler<'_> {
    fn vp2intersectq(&mut self, op0: KReg, op1: KReg, op2: Zmm, op3: Zmm) {
        self.emit_n(
            InstId::Vp2intersectq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectqEmitter<KReg, KReg, Vec, Mem> for Assembler<'_> {
    fn vp2intersectq(&mut self, op0: KReg, op1: KReg, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vp2intersectq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectqEmitter<KReg, KReg, Xmm, Mem> for Assembler<'_> {
    fn vp2intersectq(&mut self, op0: KReg, op1: KReg, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vp2intersectq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectqEmitter<KReg, KReg, Ymm, Mem> for Assembler<'_> {
    fn vp2intersectq(&mut self, op0: KReg, op1: KReg, op2: Ymm, op3: Mem) {
        self.emit_n(
            InstId::Vp2intersectq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl Vp2intersectqEmitter<KReg, KReg, Zmm, Mem> for Assembler<'_> {
    fn vp2intersectq(&mut self, op0: KReg, op1: KReg, op2: Zmm, op3: Mem) {
        self.emit_n(
            InstId::Vp2intersectq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VP2INTERSECT{kz}
    }
}

impl VpabsbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpabsb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Vpabsb as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabsbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpabsb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Vpabsb as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabsbEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpabsb(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(InstId::Vpabsb as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabsbEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpabsb(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(InstId::Vpabsb as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabsbEmitter<Vec, Mem> for Assembler<'_> {
    fn vpabsb(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Vpabsb as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabsbEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpabsb(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Vpabsb as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabsbEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpabsb(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(InstId::Vpabsb as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabsbEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpabsb(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(InstId::Vpabsb as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabsdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpabsd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Vpabsd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_F{kz}
    }
}

impl VpabsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpabsd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Vpabsd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_F{kz}
    }
}

impl VpabsdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpabsd(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(InstId::Vpabsd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_F{kz}
    }
}

impl VpabsdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpabsd(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(InstId::Vpabsd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_F{kz}
    }
}

impl VpabsdEmitter<Vec, Mem> for Assembler<'_> {
    fn vpabsd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Vpabsd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_F{kz}
    }
}

impl VpabsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpabsd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Vpabsd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_F{kz}
    }
}

impl VpabsdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpabsd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(InstId::Vpabsd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_F{kz}
    }
}

impl VpabsdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpabsd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(InstId::Vpabsd as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_F{kz}
    }
}

impl VpabsqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpabsq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Vpabsq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_F{kz}
    }
}

impl VpabsqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpabsq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Vpabsq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_F{kz}
    }
}

impl VpabsqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpabsq(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(InstId::Vpabsq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_F{kz}
    }
}

impl VpabsqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpabsq(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(InstId::Vpabsq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_F{kz}
    }
}

impl VpabsqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpabsq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Vpabsq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_F{kz}
    }
}

impl VpabsqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpabsq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Vpabsq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_F{kz}
    }
}

impl VpabsqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpabsq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(InstId::Vpabsq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_F{kz}
    }
}

impl VpabsqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpabsq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(InstId::Vpabsq as u32, &[op0.as_operand(), op1.as_operand()]); // AVX512_F{kz}
    }
}

impl VpabswEmitter<Vec, Vec> for Assembler<'_> {
    fn vpabsw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Vpabsw as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpabsw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Vpabsw as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabswEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpabsw(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(InstId::Vpabsw as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabswEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpabsw(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(InstId::Vpabsw as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabswEmitter<Vec, Mem> for Assembler<'_> {
    fn vpabsw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Vpabsw as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabswEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpabsw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Vpabsw as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabswEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpabsw(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(InstId::Vpabsw as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpabswEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpabsw(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(InstId::Vpabsw as u32, &[op0.as_operand(), op1.as_operand()]); // AVX+ AVX512_BW{kz}
    }
}

impl VpackssdwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpackssdw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpackssdw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackssdwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpackssdw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpackssdw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackssdwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpackssdw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpackssdw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackssdwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpackssdw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpackssdw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackssdwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpackssdw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpackssdw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackssdwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpackssdw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpackssdw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackssdwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpackssdw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpackssdw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackssdwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpackssdw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpackssdw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpacksswbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpacksswb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpacksswb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpacksswbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpacksswb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpacksswb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpacksswbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpacksswb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpacksswb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpacksswbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpacksswb(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpacksswb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpacksswbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpacksswb(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpacksswb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpacksswbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpacksswb(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpacksswb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpacksswbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpacksswb(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpacksswb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpacksswbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpacksswb(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpacksswb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpackusdwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpackusdw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpackusdw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackusdwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpackusdw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpackusdw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackusdwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpackusdw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpackusdw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackusdwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpackusdw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpackusdw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackusdwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpackusdw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpackusdw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackusdwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpackusdw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpackusdw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackusdwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpackusdw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpackusdw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackusdwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpackusdw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpackusdw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz|b32}
    }
}

impl VpackuswbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpackuswb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpackuswb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpackuswbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpackuswb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpackuswb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpackuswbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpackuswb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpackuswb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpackuswbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpackuswb(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpackuswb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpackuswbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpackuswb(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpackuswb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpackuswbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpackuswb(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpackuswb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpackuswbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpackuswb(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpackuswb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpackuswbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpackuswb(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpackuswb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpaddb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpaddb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpaddb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpaddb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpaddb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpaddb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpaddb(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpaddb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpaddb(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpaddb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpaddb(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpaddb(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpaddb(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpadddEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpaddd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpaddd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpadddEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpaddd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpaddd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpadddEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpaddd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpaddd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpadddEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpaddd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpaddd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpadddEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpaddd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpaddd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpadddEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpaddd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpadddEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpaddd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpadddEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpaddd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpaddqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpaddq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpaddq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpaddqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpaddq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpaddq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpaddqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpaddq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpaddq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpaddqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpaddq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpaddq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpaddqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpaddq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpaddq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpaddqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpaddq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpaddqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpaddq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpaddqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpaddq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpaddsbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpaddsb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpaddsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddsbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpaddsb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpaddsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddsbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpaddsb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpaddsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddsbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpaddsb(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpaddsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddsbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpaddsb(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpaddsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddsbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpaddsb(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddsbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpaddsb(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddsbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpaddsb(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddswEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpaddsw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpaddsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddswEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpaddsw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpaddsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddswEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpaddsw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpaddsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddswEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpaddsw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpaddsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddswEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpaddsw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpaddsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddswEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpaddsw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddswEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpaddsw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddswEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpaddsw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddusbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpaddusb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpaddusb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddusbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpaddusb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpaddusb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddusbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpaddusb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpaddusb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddusbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpaddusb(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpaddusb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddusbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpaddusb(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpaddusb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddusbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpaddusb(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddusb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddusbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpaddusb(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddusb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddusbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpaddusb(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddusb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpadduswEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpaddusw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpaddusw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpadduswEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpaddusw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpaddusw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpadduswEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpaddusw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpaddusw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpadduswEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpaddusw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpaddusw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpadduswEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpaddusw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpaddusw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpadduswEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpaddusw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddusw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpadduswEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpaddusw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddusw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpadduswEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpaddusw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddusw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpaddw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpaddw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpaddw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpaddw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpaddw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpaddw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpaddw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpaddw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpaddw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpaddw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpaddw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpaddw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpaddwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpaddw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpaddw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpalignrEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpalignr(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpalignr as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VpalignrEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpalignr(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpalignr as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VpalignrEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpalignr(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vpalignr as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VpalignrEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpalignr(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vpalignr as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpalignrEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpalignr(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpalignr as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VpalignrEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpalignr(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpalignr as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VpalignrEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpalignr(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpalignr as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VpalignrEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vpalignr(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpalignr as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpandEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpand(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpand as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpandEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpand(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpand as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpandEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpand(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpand as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpandEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpand(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpand as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpandEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpand(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpand as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpandEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpand(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpand as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpanddEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpandd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpandd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpanddEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpandd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpandd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpanddEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpandd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpandd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpanddEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpandd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpandd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpanddEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpandd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpandd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpanddEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpandd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpandd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpanddEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpandd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpandd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpanddEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpandd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpandd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpandnEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpandn(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpandn as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpandnEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpandn(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpandn as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpandnEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpandn(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpandn as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpandnEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpandn(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpandn as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpandnEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpandn(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpandn as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpandnEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpandn(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpandn as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpandndEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpandnd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpandnd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpandndEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpandnd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpandnd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpandndEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpandnd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpandnd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpandndEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpandnd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpandnd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpandndEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpandnd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpandnd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpandndEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpandnd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpandnd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpandndEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpandnd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpandnd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpandndEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpandnd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpandnd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpandnqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpandnq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpandnq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandnqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpandnq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpandnq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandnqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpandnq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpandnq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandnqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpandnq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpandnq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandnqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpandnq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpandnq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandnqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpandnq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpandnq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandnqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpandnq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpandnq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandnqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpandnq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpandnq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpandq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpandq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpandq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpandq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpandq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpandq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpandq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpandq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpandq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpandq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpandq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpandq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpandq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpandq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpandqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpandq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpandq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpavgbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpavgb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpavgb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpavgb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpavgb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpavgb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpavgb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpavgb(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpavgb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpavgb(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpavgb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpavgb(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpavgb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpavgb(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpavgb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpavgb(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpavgb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpavgw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpavgw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpavgw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpavgw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpavgw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpavgw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpavgw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpavgw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpavgw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpavgw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpavgw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpavgw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpavgw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpavgw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpavgwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpavgw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpavgw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpblenddEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpblendd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpblendd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX2
    }
}

impl<U3: Into<Imm>> VpblenddEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpblendd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpblendd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX2
    }
}

impl<U3: Into<Imm>> VpblenddEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpblendd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vpblendd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX2
    }
}

impl VpblenddEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpblendd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpblendd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX2
    }
}

impl<U3: Into<Imm>> VpblenddEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpblendd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpblendd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX2
    }
}

impl<U3: Into<Imm>> VpblenddEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpblendd(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpblendd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX2
    }
}

impl VpblendmbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpblendmb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpblendmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpblendmb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpblendmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpblendmb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpblendmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpblendmb(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpblendmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpblendmb(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpblendmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpblendmb(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpblendmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpblendmb(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpblendmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpblendmb(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpblendmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpblendmd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpblendmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpblendmdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpblendmd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpblendmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpblendmdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpblendmd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpblendmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpblendmdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpblendmd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpblendmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpblendmdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpblendmd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpblendmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpblendmdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpblendmd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpblendmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpblendmdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpblendmd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpblendmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpblendmdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpblendmd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpblendmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpblendmqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpblendmq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpblendmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpblendmqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpblendmq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpblendmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpblendmqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpblendmq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpblendmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpblendmqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpblendmq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpblendmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpblendmqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpblendmq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpblendmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpblendmqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpblendmq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpblendmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpblendmqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpblendmq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpblendmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpblendmqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpblendmq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpblendmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpblendmwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpblendmw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpblendmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpblendmw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpblendmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpblendmw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpblendmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpblendmw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpblendmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpblendmw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpblendmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpblendmw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpblendmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpblendmw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpblendmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendmwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpblendmw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpblendmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpblendvbEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpblendvb(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vpblendvb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX+
    }
}

impl VpblendvbEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpblendvb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vpblendvb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX+
    }
}

impl VpblendvbEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpblendvb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Ymm) {
        self.emit_n(
            InstId::Vpblendvb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX+
    }
}

impl VpblendvbEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpblendvb(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vpblendvb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX+
    }
}

impl VpblendvbEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpblendvb(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vpblendvb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX+
    }
}

impl VpblendvbEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vpblendvb(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: Ymm) {
        self.emit_n(
            InstId::Vpblendvb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX+
    }
}

impl VpblendwEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpblendw(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpblendw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX+
    }
}

impl<U3: Into<Imm>> VpblendwEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpblendw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpblendw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX+
    }
}

impl<U3: Into<Imm>> VpblendwEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpblendw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vpblendw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX+
    }
}

impl VpblendwEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpblendw(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpblendw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX+
    }
}

impl<U3: Into<Imm>> VpblendwEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpblendw(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpblendw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX+
    }
}

impl<U3: Into<Imm>> VpblendwEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpblendw(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpblendw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX+
    }
}

impl VpbroadcastbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpbroadcastb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpbroadcastb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpbroadcastb(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vpbroadcastb(&mut self, op0: Zmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Vec, Mem> for Assembler<'_> {
    fn vpbroadcastb(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpbroadcastb(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpbroadcastb(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpbroadcastb(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Vec, Gp> for Assembler<'_> {
    fn vpbroadcastb(&mut self, op0: Vec, op1: Gp) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Xmm, Gpd> for Assembler<'_> {
    fn vpbroadcastb(&mut self, op0: Xmm, op1: Gpd) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Ymm, Gpd> for Assembler<'_> {
    fn vpbroadcastb(&mut self, op0: Ymm, op1: Gpd) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpbroadcastbEmitter<Zmm, Gpd> for Assembler<'_> {
    fn vpbroadcastb(&mut self, op0: Zmm, op1: Gpd) {
        self.emit_n(
            InstId::Vpbroadcastb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpbroadcastdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpbroadcastd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpbroadcastd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpbroadcastd(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vpbroadcastd(&mut self, op0: Zmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Vec, Mem> for Assembler<'_> {
    fn vpbroadcastd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpbroadcastd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpbroadcastd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpbroadcastd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Vec, Gp> for Assembler<'_> {
    fn vpbroadcastd(&mut self, op0: Vec, op1: Gp) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Xmm, Gpd> for Assembler<'_> {
    fn vpbroadcastd(&mut self, op0: Xmm, op1: Gpd) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Ymm, Gpd> for Assembler<'_> {
    fn vpbroadcastd(&mut self, op0: Ymm, op1: Gpd) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpbroadcastdEmitter<Zmm, Gpd> for Assembler<'_> {
    fn vpbroadcastd(&mut self, op0: Zmm, op1: Gpd) {
        self.emit_n(
            InstId::Vpbroadcastd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vpbroadcastmb2qEmitter<Vec, KReg> for Assembler<'_> {
    fn vpbroadcastmb2q(&mut self, op0: Vec, op1: KReg) {
        self.emit_n(
            InstId::Vpbroadcastmb2q as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD
    }
}

impl Vpbroadcastmb2qEmitter<Xmm, KReg> for Assembler<'_> {
    fn vpbroadcastmb2q(&mut self, op0: Xmm, op1: KReg) {
        self.emit_n(
            InstId::Vpbroadcastmb2q as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD
    }
}

impl Vpbroadcastmb2qEmitter<Ymm, KReg> for Assembler<'_> {
    fn vpbroadcastmb2q(&mut self, op0: Ymm, op1: KReg) {
        self.emit_n(
            InstId::Vpbroadcastmb2q as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD
    }
}

impl Vpbroadcastmb2qEmitter<Zmm, KReg> for Assembler<'_> {
    fn vpbroadcastmb2q(&mut self, op0: Zmm, op1: KReg) {
        self.emit_n(
            InstId::Vpbroadcastmb2q as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD
    }
}

impl Vpbroadcastmw2dEmitter<Vec, KReg> for Assembler<'_> {
    fn vpbroadcastmw2d(&mut self, op0: Vec, op1: KReg) {
        self.emit_n(
            InstId::Vpbroadcastmw2d as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD
    }
}

impl Vpbroadcastmw2dEmitter<Xmm, KReg> for Assembler<'_> {
    fn vpbroadcastmw2d(&mut self, op0: Xmm, op1: KReg) {
        self.emit_n(
            InstId::Vpbroadcastmw2d as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD
    }
}

impl Vpbroadcastmw2dEmitter<Ymm, KReg> for Assembler<'_> {
    fn vpbroadcastmw2d(&mut self, op0: Ymm, op1: KReg) {
        self.emit_n(
            InstId::Vpbroadcastmw2d as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD
    }
}

impl Vpbroadcastmw2dEmitter<Zmm, KReg> for Assembler<'_> {
    fn vpbroadcastmw2d(&mut self, op0: Zmm, op1: KReg) {
        self.emit_n(
            InstId::Vpbroadcastmw2d as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD
    }
}

impl VpbroadcastqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpbroadcastq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpbroadcastq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpbroadcastq(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vpbroadcastq(&mut self, op0: Zmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpbroadcastq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpbroadcastq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpbroadcastq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpbroadcastq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Vec, Gp> for Assembler<'_> {
    fn vpbroadcastq(&mut self, op0: Vec, op1: Gp) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Xmm, Gpq> for Assembler<'_> {
    fn vpbroadcastq(&mut self, op0: Xmm, op1: Gpq) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Ymm, Gpq> for Assembler<'_> {
    fn vpbroadcastq(&mut self, op0: Ymm, op1: Gpq) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpbroadcastqEmitter<Zmm, Gpq> for Assembler<'_> {
    fn vpbroadcastq(&mut self, op0: Zmm, op1: Gpq) {
        self.emit_n(
            InstId::Vpbroadcastq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpbroadcastwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpbroadcastw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpbroadcastw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpbroadcastw(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vpbroadcastw(&mut self, op0: Zmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Vec, Mem> for Assembler<'_> {
    fn vpbroadcastw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpbroadcastw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpbroadcastw(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpbroadcastw(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX2 AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Vec, Gp> for Assembler<'_> {
    fn vpbroadcastw(&mut self, op0: Vec, op1: Gp) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Xmm, Gpd> for Assembler<'_> {
    fn vpbroadcastw(&mut self, op0: Xmm, op1: Gpd) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Ymm, Gpd> for Assembler<'_> {
    fn vpbroadcastw(&mut self, op0: Ymm, op1: Gpd) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpbroadcastwEmitter<Zmm, Gpd> for Assembler<'_> {
    fn vpbroadcastw(&mut self, op0: Zmm, op1: Gpd) {
        self.emit_n(
            InstId::Vpbroadcastw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpclmulqdqEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpclmulqdq(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpclmulqdq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX VPCLMULQDQ AVX512_F
    }
}

impl<U3: Into<Imm>> VpclmulqdqEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpclmulqdq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpclmulqdq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX VPCLMULQDQ AVX512_F
    }
}

impl<U3: Into<Imm>> VpclmulqdqEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpclmulqdq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vpclmulqdq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX VPCLMULQDQ AVX512_F
    }
}

impl<U3: Into<Imm>> VpclmulqdqEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpclmulqdq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vpclmulqdq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX VPCLMULQDQ AVX512_F
    }
}

impl VpclmulqdqEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpclmulqdq(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpclmulqdq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX VPCLMULQDQ AVX512_F
    }
}

impl<U3: Into<Imm>> VpclmulqdqEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpclmulqdq(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpclmulqdq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX VPCLMULQDQ AVX512_F
    }
}

impl<U3: Into<Imm>> VpclmulqdqEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpclmulqdq(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpclmulqdq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX VPCLMULQDQ AVX512_F
    }
}

impl<U3: Into<Imm>> VpclmulqdqEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vpclmulqdq(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpclmulqdq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX VPCLMULQDQ AVX512_F
    }
}

impl VpcmpbEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpb(&mut self, op0: KReg, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpcmpb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpbEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcmpb(&mut self, op0: KReg, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpbEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpcmpb(&mut self, op0: KReg, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpbEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpcmpb(&mut self, op0: KReg, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl VpcmpbEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpb(&mut self, op0: KReg, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpcmpb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpbEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcmpb(&mut self, op0: KReg, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpbEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vpcmpb(&mut self, op0: KReg, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpbEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vpcmpb(&mut self, op0: KReg, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl VpcmpdEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpd(&mut self, op0: KReg, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpcmpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpdEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcmpd(&mut self, op0: KReg, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpdEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpcmpd(&mut self, op0: KReg, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpdEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpcmpd(&mut self, op0: KReg, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpdEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpd(&mut self, op0: KReg, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpcmpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpdEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcmpd(&mut self, op0: KReg, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpdEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vpcmpd(&mut self, op0: KReg, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpdEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vpcmpd(&mut self, op0: KReg, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpeqbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpcmpeqb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpeqb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpeqb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpcmpeqb(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpcmpeqb(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpcmpeqb(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqbEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vpcmpeqb(&mut self, op0: KReg, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqbEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpeqb(&mut self, op0: KReg, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqbEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpeqb(&mut self, op0: KReg, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqbEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vpcmpeqb(&mut self, op0: KReg, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqbEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vpcmpeqb(&mut self, op0: KReg, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqbEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vpcmpeqb(&mut self, op0: KReg, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqbEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vpcmpeqb(&mut self, op0: KReg, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqbEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vpcmpeqb(&mut self, op0: KReg, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpcmpeqd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpeqd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpeqd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpcmpeqd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpcmpeqd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpcmpeqd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqdEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vpcmpeqd(&mut self, op0: KReg, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpeqdEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpeqd(&mut self, op0: KReg, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpeqdEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpeqd(&mut self, op0: KReg, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpeqdEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vpcmpeqd(&mut self, op0: KReg, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpeqdEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vpcmpeqd(&mut self, op0: KReg, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpeqdEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vpcmpeqd(&mut self, op0: KReg, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpeqdEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vpcmpeqd(&mut self, op0: KReg, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpeqdEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vpcmpeqd(&mut self, op0: KReg, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpeqqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpcmpeqq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpeqq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpeqq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpcmpeqq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpcmpeqq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpcmpeqq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqqEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vpcmpeqq(&mut self, op0: KReg, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpeqqEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpeqq(&mut self, op0: KReg, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpeqqEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpeqq(&mut self, op0: KReg, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpeqqEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vpcmpeqq(&mut self, op0: KReg, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpeqqEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vpcmpeqq(&mut self, op0: KReg, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpeqqEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vpcmpeqq(&mut self, op0: KReg, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpeqqEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vpcmpeqq(&mut self, op0: KReg, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpeqqEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vpcmpeqq(&mut self, op0: KReg, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpeqwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpcmpeqw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpeqw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpeqw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpcmpeqw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpcmpeqw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpcmpeqw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpeqwEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vpcmpeqw(&mut self, op0: KReg, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqwEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpeqw(&mut self, op0: KReg, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqwEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpeqw(&mut self, op0: KReg, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqwEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vpcmpeqw(&mut self, op0: KReg, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqwEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vpcmpeqw(&mut self, op0: KReg, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqwEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vpcmpeqw(&mut self, op0: KReg, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqwEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vpcmpeqw(&mut self, op0: KReg, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpeqwEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vpcmpeqw(&mut self, op0: KReg, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpeqw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpestriEmitter<Vec, Vec, Imm, Gp, Gp, Gp> for Assembler<'_> {
    fn vpcmpestri(&mut self, op0: Vec, op1: Vec, op2: Imm, op3: Gp, op4: Gp, op5: Gp) {
        self.emit_n(
            InstId::Vpcmpestri as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                op4.as_operand(),
                op5.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl<U2: Into<Imm>> VpcmpestriEmitter<Xmm, Xmm, U2, Gpd, Gpd, Gpd> for Assembler<'_> {
    fn vpcmpestri(&mut self, op0: Xmm, op1: Xmm, op2: U2, op3: Gpd, op4: Gpd, op5: Gpd) {
        self.emit_n(
            InstId::Vpcmpestri as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
                op3.as_operand(),
                op4.as_operand(),
                op5.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl VpcmpestriEmitter<Vec, Mem, Imm, Gp, Gp, Gp> for Assembler<'_> {
    fn vpcmpestri(&mut self, op0: Vec, op1: Mem, op2: Imm, op3: Gp, op4: Gp, op5: Gp) {
        self.emit_n(
            InstId::Vpcmpestri as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                op4.as_operand(),
                op5.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl<U2: Into<Imm>> VpcmpestriEmitter<Xmm, Mem, U2, Gpd, Gpd, Gpd> for Assembler<'_> {
    fn vpcmpestri(&mut self, op0: Xmm, op1: Mem, op2: U2, op3: Gpd, op4: Gpd, op5: Gpd) {
        self.emit_n(
            InstId::Vpcmpestri as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
                op3.as_operand(),
                op4.as_operand(),
                op5.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl VpcmpestrmEmitter<Vec, Vec, Imm, Vec, Gp, Gp> for Assembler<'_> {
    fn vpcmpestrm(&mut self, op0: Vec, op1: Vec, op2: Imm, op3: Vec, op4: Gp, op5: Gp) {
        self.emit_n(
            InstId::Vpcmpestrm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                op4.as_operand(),
                op5.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl<U2: Into<Imm>> VpcmpestrmEmitter<Xmm, Xmm, U2, Xmm, Gpd, Gpd> for Assembler<'_> {
    fn vpcmpestrm(&mut self, op0: Xmm, op1: Xmm, op2: U2, op3: Xmm, op4: Gpd, op5: Gpd) {
        self.emit_n(
            InstId::Vpcmpestrm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
                op3.as_operand(),
                op4.as_operand(),
                op5.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl VpcmpestrmEmitter<Vec, Mem, Imm, Vec, Gp, Gp> for Assembler<'_> {
    fn vpcmpestrm(&mut self, op0: Vec, op1: Mem, op2: Imm, op3: Vec, op4: Gp, op5: Gp) {
        self.emit_n(
            InstId::Vpcmpestrm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                op4.as_operand(),
                op5.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl<U2: Into<Imm>> VpcmpestrmEmitter<Xmm, Mem, U2, Xmm, Gpd, Gpd> for Assembler<'_> {
    fn vpcmpestrm(&mut self, op0: Xmm, op1: Mem, op2: U2, op3: Xmm, op4: Gpd, op5: Gpd) {
        self.emit_n(
            InstId::Vpcmpestrm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
                op3.as_operand(),
                op4.as_operand(),
                op5.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl VpcmpgtbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpcmpgtb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpgtb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpgtb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpcmpgtb(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpcmpgtb(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpcmpgtb(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtbEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vpcmpgtb(&mut self, op0: KReg, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtbEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpgtb(&mut self, op0: KReg, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtbEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpgtb(&mut self, op0: KReg, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtbEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vpcmpgtb(&mut self, op0: KReg, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtbEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vpcmpgtb(&mut self, op0: KReg, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtbEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vpcmpgtb(&mut self, op0: KReg, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtbEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vpcmpgtb(&mut self, op0: KReg, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtbEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vpcmpgtb(&mut self, op0: KReg, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpcmpgtd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpgtd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpgtd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpcmpgtd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpcmpgtd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpcmpgtd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtdEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vpcmpgtd(&mut self, op0: KReg, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpgtdEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpgtd(&mut self, op0: KReg, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpgtdEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpgtd(&mut self, op0: KReg, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpgtdEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vpcmpgtd(&mut self, op0: KReg, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpgtdEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vpcmpgtd(&mut self, op0: KReg, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpgtdEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vpcmpgtd(&mut self, op0: KReg, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpgtdEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vpcmpgtd(&mut self, op0: KReg, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpgtdEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vpcmpgtd(&mut self, op0: KReg, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpgtqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpcmpgtq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpgtq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpgtq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpcmpgtq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpcmpgtq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpcmpgtq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtqEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vpcmpgtq(&mut self, op0: KReg, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpgtqEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpgtq(&mut self, op0: KReg, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpgtqEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpgtq(&mut self, op0: KReg, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpgtqEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vpcmpgtq(&mut self, op0: KReg, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpgtqEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vpcmpgtq(&mut self, op0: KReg, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpgtqEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vpcmpgtq(&mut self, op0: KReg, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpgtqEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vpcmpgtq(&mut self, op0: KReg, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpgtqEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vpcmpgtq(&mut self, op0: KReg, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpgtwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpcmpgtw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpgtw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpgtw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpcmpgtw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpcmpgtw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpcmpgtw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpcmpgtwEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vpcmpgtw(&mut self, op0: KReg, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtwEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vpcmpgtw(&mut self, op0: KReg, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtwEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vpcmpgtw(&mut self, op0: KReg, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtwEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vpcmpgtw(&mut self, op0: KReg, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtwEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vpcmpgtw(&mut self, op0: KReg, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtwEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vpcmpgtw(&mut self, op0: KReg, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtwEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vpcmpgtw(&mut self, op0: KReg, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpgtwEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vpcmpgtw(&mut self, op0: KReg, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpcmpgtw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpcmpistriEmitter<Vec, Vec, Imm, Gp> for Assembler<'_> {
    fn vpcmpistri(&mut self, op0: Vec, op1: Vec, op2: Imm, op3: Gp) {
        self.emit_n(
            InstId::Vpcmpistri as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl<U2: Into<Imm>> VpcmpistriEmitter<Xmm, Xmm, U2, Gpd> for Assembler<'_> {
    fn vpcmpistri(&mut self, op0: Xmm, op1: Xmm, op2: U2, op3: Gpd) {
        self.emit_n(
            InstId::Vpcmpistri as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
                op3.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl VpcmpistriEmitter<Vec, Mem, Imm, Gp> for Assembler<'_> {
    fn vpcmpistri(&mut self, op0: Vec, op1: Mem, op2: Imm, op3: Gp) {
        self.emit_n(
            InstId::Vpcmpistri as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl<U2: Into<Imm>> VpcmpistriEmitter<Xmm, Mem, U2, Gpd> for Assembler<'_> {
    fn vpcmpistri(&mut self, op0: Xmm, op1: Mem, op2: U2, op3: Gpd) {
        self.emit_n(
            InstId::Vpcmpistri as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
                op3.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl VpcmpistrmEmitter<Vec, Vec, Imm, Vec> for Assembler<'_> {
    fn vpcmpistrm(&mut self, op0: Vec, op1: Vec, op2: Imm, op3: Vec) {
        self.emit_n(
            InstId::Vpcmpistrm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl<U2: Into<Imm>> VpcmpistrmEmitter<Xmm, Xmm, U2, Xmm> for Assembler<'_> {
    fn vpcmpistrm(&mut self, op0: Xmm, op1: Xmm, op2: U2, op3: Xmm) {
        self.emit_n(
            InstId::Vpcmpistrm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
                op3.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl VpcmpistrmEmitter<Vec, Mem, Imm, Vec> for Assembler<'_> {
    fn vpcmpistrm(&mut self, op0: Vec, op1: Mem, op2: Imm, op3: Vec) {
        self.emit_n(
            InstId::Vpcmpistrm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl<U2: Into<Imm>> VpcmpistrmEmitter<Xmm, Mem, U2, Xmm> for Assembler<'_> {
    fn vpcmpistrm(&mut self, op0: Xmm, op1: Mem, op2: U2, op3: Xmm) {
        self.emit_n(
            InstId::Vpcmpistrm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
                op3.as_operand(),
            ],
        ); // AVX [EXPLICIT]
    }
}

impl VpcmpqEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpq(&mut self, op0: KReg, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpcmpq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpqEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcmpq(&mut self, op0: KReg, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpqEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpcmpq(&mut self, op0: KReg, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpqEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpcmpq(&mut self, op0: KReg, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpqEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpq(&mut self, op0: KReg, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpcmpq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpqEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcmpq(&mut self, op0: KReg, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpqEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vpcmpq(&mut self, op0: KReg, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpqEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vpcmpq(&mut self, op0: KReg, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpubEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpub(&mut self, op0: KReg, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpcmpub as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpubEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcmpub(&mut self, op0: KReg, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpub as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpubEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpcmpub(&mut self, op0: KReg, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpub as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpubEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpcmpub(&mut self, op0: KReg, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpub as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl VpcmpubEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpub(&mut self, op0: KReg, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpcmpub as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpubEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcmpub(&mut self, op0: KReg, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpub as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpubEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vpcmpub(&mut self, op0: KReg, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpub as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl<U3: Into<Imm>> VpcmpubEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vpcmpub(&mut self, op0: KReg, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpub as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k}
    }
}

impl VpcmpudEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpud(&mut self, op0: KReg, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpcmpud as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpudEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcmpud(&mut self, op0: KReg, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpud as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpudEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpcmpud(&mut self, op0: KReg, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpud as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpudEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpcmpud(&mut self, op0: KReg, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpud as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpudEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpud(&mut self, op0: KReg, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpcmpud as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpudEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcmpud(&mut self, op0: KReg, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpud as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpudEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vpcmpud(&mut self, op0: KReg, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpud as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl<U3: Into<Imm>> VpcmpudEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vpcmpud(&mut self, op0: KReg, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpud as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b32}
    }
}

impl VpcmpuqEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpuq(&mut self, op0: KReg, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpcmpuq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuqEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcmpuq(&mut self, op0: KReg, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpuq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuqEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpcmpuq(&mut self, op0: KReg, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpuq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuqEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpcmpuq(&mut self, op0: KReg, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpuq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpuqEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpuq(&mut self, op0: KReg, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpcmpuq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuqEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcmpuq(&mut self, op0: KReg, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpuq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuqEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vpcmpuq(&mut self, op0: KReg, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpuq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuqEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vpcmpuq(&mut self, op0: KReg, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpuq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{k|b64}
    }
}

impl VpcmpuwEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpuw(&mut self, op0: KReg, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpcmpuw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuwEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcmpuw(&mut self, op0: KReg, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpuw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuwEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpcmpuw(&mut self, op0: KReg, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpuw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuwEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpcmpuw(&mut self, op0: KReg, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpuw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl VpcmpuwEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpuw(&mut self, op0: KReg, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpcmpuw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuwEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcmpuw(&mut self, op0: KReg, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpuw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuwEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vpcmpuw(&mut self, op0: KReg, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpuw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpuwEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vpcmpuw(&mut self, op0: KReg, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpuw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl VpcmpwEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpw(&mut self, op0: KReg, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpcmpw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpwEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcmpw(&mut self, op0: KReg, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpwEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpcmpw(&mut self, op0: KReg, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpwEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpcmpw(&mut self, op0: KReg, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vpcmpw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl VpcmpwEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpw(&mut self, op0: KReg, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpcmpw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpwEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcmpw(&mut self, op0: KReg, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpwEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vpcmpw(&mut self, op0: KReg, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl<U3: Into<Imm>> VpcmpwEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vpcmpw(&mut self, op0: KReg, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcmpw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_BW{k|b64}
    }
}

impl VpcompressbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpcompressb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpcompressb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompressbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpcompressb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpcompressb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompressbEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpcompressb(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vpcompressb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompressbEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpcompressb(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpcompressb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompressbEmitter<Mem, Vec> for Assembler<'_> {
    fn vpcompressb(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpcompressb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompressbEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpcompressb(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpcompressb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompressbEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpcompressb(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpcompressb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompressbEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpcompressb(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpcompressb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompressdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpcompressd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpcompressd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpcompressd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpcompressd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpcompressd(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vpcompressd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpcompressd(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpcompressd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressdEmitter<Mem, Vec> for Assembler<'_> {
    fn vpcompressd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpcompressd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpcompressd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpcompressd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpcompressd(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpcompressd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressdEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpcompressd(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpcompressd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpcompressq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpcompressq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpcompressq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpcompressq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpcompressq(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vpcompressq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpcompressq(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpcompressq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressqEmitter<Mem, Vec> for Assembler<'_> {
    fn vpcompressq(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpcompressq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressqEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpcompressq(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpcompressq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressqEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpcompressq(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpcompressq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompressqEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpcompressq(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpcompressq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpcompresswEmitter<Vec, Vec> for Assembler<'_> {
    fn vpcompressw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpcompressw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompresswEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpcompressw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpcompressw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompresswEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpcompressw(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vpcompressw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompresswEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpcompressw(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpcompressw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompresswEmitter<Mem, Vec> for Assembler<'_> {
    fn vpcompressw(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpcompressw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompresswEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpcompressw(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpcompressw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompresswEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpcompressw(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpcompressw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpcompresswEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpcompressw(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpcompressw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpconflictdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpconflictd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpconflictd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpconflictd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpconflictd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpconflictd(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vpconflictd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpconflictd(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpconflictd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictdEmitter<Vec, Mem> for Assembler<'_> {
    fn vpconflictd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpconflictd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpconflictd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpconflictd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpconflictd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpconflictd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpconflictd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpconflictd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpconflictq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpconflictq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpconflictq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpconflictq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpconflictq(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vpconflictq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpconflictq(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpconflictq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpconflictq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpconflictq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpconflictq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpconflictq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpconflictq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpconflictq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpconflictqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpconflictq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpconflictq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VpdpbusdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpbusd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpdpbusd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpbusd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpdpbusd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpbusd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpdpbusd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpdpbusd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpdpbusd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpbusd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbusd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpbusd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbusd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpbusd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbusd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpdpbusd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbusd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpbusds(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpdpbusds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpbusds(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpdpbusds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpbusds(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpdpbusds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpdpbusds(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpdpbusds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpbusds(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbusds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpbusds(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbusds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpbusds(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbusds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpbusdsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpdpbusds(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbusds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpwssd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpdpwssd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpwssd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpdpwssd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpwssd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpdpwssd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpdpwssd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpdpwssd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpwssd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwssd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpwssd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwssd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpwssd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwssd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpdpwssd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwssd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpwssds(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpdpwssds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpwssds(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpdpwssds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpwssds(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpdpwssds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpdpwssds(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpdpwssds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpwssds(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwssds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpwssds(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwssds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpwssds(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwssds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl VpdpwssdsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpdpwssds(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwssds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX_VNNI AVX512_VNNI{kz|b32}
    }
}

impl Vperm2f128Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vperm2f128(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vperm2f128 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> Vperm2f128Emitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vperm2f128(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vperm2f128 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl Vperm2f128Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vperm2f128(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vperm2f128 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> Vperm2f128Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vperm2f128(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vperm2f128 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl Vperm2i128Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vperm2i128(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vperm2i128 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX2
    }
}

impl<U3: Into<Imm>> Vperm2i128Emitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vperm2i128(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vperm2i128 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX2
    }
}

impl Vperm2i128Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vperm2i128(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vperm2i128 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX2
    }
}

impl<U3: Into<Imm>> Vperm2i128Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vperm2i128(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vperm2i128 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX2
    }
}

impl VpermbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpermb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl VpermbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpermb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl VpermbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpermb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl VpermbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermb(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpermb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl VpermbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermb(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpermb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl VpermbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermb(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl VpermbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermb(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpermb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl VpermbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermb(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl VpermdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpermd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpermd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpermd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpermd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpermd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl Vpermi2bEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermi2b(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpermi2b as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermi2bEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermi2b(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpermi2b as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermi2bEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermi2b(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpermi2b as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermi2bEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermi2b(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpermi2b as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermi2bEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermi2b(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2b as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermi2bEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermi2b(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2b as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermi2bEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermi2b(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2b as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermi2bEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermi2b(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2b as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermi2dEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermi2d(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpermi2d as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2dEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermi2d(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpermi2d as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2dEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermi2d(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpermi2d as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2dEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermi2d(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpermi2d as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2dEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermi2d(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2d as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2dEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermi2d(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2d as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2dEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermi2d(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2d as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2dEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermi2d(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2d as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermi2pd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpermi2pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermi2pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpermi2pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermi2pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpermi2pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermi2pd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpermi2pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermi2pd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermi2pd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermi2pd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermi2pd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermi2ps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpermi2ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermi2ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpermi2ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermi2ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpermi2ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermi2ps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpermi2ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermi2ps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermi2ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermi2ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermi2ps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermi2qEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermi2q(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpermi2q as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2qEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermi2q(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpermi2q as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2qEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermi2q(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpermi2q as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2qEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermi2q(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpermi2q as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2qEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermi2q(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2q as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2qEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermi2q(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2q as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2qEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermi2q(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2q as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2qEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermi2q(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2q as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermi2wEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermi2w(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpermi2w as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermi2wEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermi2w(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpermi2w as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermi2wEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermi2w(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpermi2w as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermi2wEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermi2w(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpermi2w as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermi2wEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermi2w(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2w as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermi2wEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermi2w(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2w as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermi2wEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermi2w(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2w as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermi2wEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermi2w(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermi2w as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpermilpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermilpd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermilpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermilpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermilpd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermilpd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermilpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermilpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermilpd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpermilpd(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpermilpd(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpdEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpermilpd(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpdEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpermilpd(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpermilpd(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpermilpd(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpdEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpermilpd(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpdEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpermilpd(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpermilpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermilps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermilps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermilps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermilps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermilps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermilps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermilps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermilps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpermilps(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpermilps(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpsEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpermilps(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpsEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpermilps(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermilpsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpermilps(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpermilps(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpsEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpermilps(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermilpsEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpermilps(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpermilps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpermpdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpermpd(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl<U2: Into<Imm>> VpermpdEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpermpd(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX2
    }
}

impl<U2: Into<Imm>> VpermpdEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpermpd(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX2
    }
}

impl VpermpdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpermpd(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl<U2: Into<Imm>> VpermpdEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpermpd(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX2
    }
}

impl<U2: Into<Imm>> VpermpdEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpermpd(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX2
    }
}

impl VpermpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermpd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermpd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermpd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermpd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpermps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpermps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpermps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpermps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpermps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpermqEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpermq(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermqEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpermq(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermqEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpermq(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpermqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpermq(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermqEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpermq(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpermqEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpermq(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpermqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpermqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2bEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermt2b(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpermt2b as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermt2bEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermt2b(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpermt2b as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermt2bEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermt2b(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpermt2b as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermt2bEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermt2b(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpermt2b as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermt2bEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermt2b(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2b as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermt2bEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermt2b(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2b as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermt2bEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermt2b(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2b as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermt2bEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermt2b(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2b as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz}
    }
}

impl Vpermt2dEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermt2d(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpermt2d as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2dEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermt2d(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpermt2d as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2dEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermt2d(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpermt2d as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2dEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermt2d(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpermt2d as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2dEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermt2d(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2d as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2dEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermt2d(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2d as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2dEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermt2d(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2d as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2dEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermt2d(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2d as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2pdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermt2pd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpermt2pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2pdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermt2pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpermt2pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2pdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermt2pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpermt2pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2pdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermt2pd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpermt2pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2pdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermt2pd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2pdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermt2pd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2pdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermt2pd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2pdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermt2pd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2pd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2psEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermt2ps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpermt2ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2psEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermt2ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpermt2ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2psEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermt2ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpermt2ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2psEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermt2ps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpermt2ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2psEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermt2ps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2psEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermt2ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2psEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermt2ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2psEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermt2ps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vpermt2qEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermt2q(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpermt2q as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2qEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermt2q(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpermt2q as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2qEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermt2q(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpermt2q as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2qEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermt2q(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpermt2q as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2qEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermt2q(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2q as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2qEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermt2q(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2q as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2qEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermt2q(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2q as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2qEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermt2q(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2q as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vpermt2wEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermt2w(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpermt2w as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermt2wEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermt2w(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpermt2w as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermt2wEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermt2w(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpermt2w as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermt2wEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermt2w(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpermt2w as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermt2wEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermt2w(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2w as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermt2wEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermt2w(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2w as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermt2wEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermt2w(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2w as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpermt2wEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermt2w(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermt2w as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpermwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpermw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpermw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpermwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpermw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpermw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpermwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpermw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpermw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpermwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpermw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpermw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpermwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpermw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpermw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpermwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpermw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpermwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpermw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpermw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpermwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpermw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpermw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpexpandbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpexpandb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpexpandb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpexpandb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpexpandb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandbEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpexpandb(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vpexpandb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandbEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpexpandb(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpexpandb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandbEmitter<Vec, Mem> for Assembler<'_> {
    fn vpexpandb(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpexpandb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandbEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpexpandb(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpexpandb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandbEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpexpandb(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpexpandb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandbEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpexpandb(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpexpandb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpanddEmitter<Vec, Vec> for Assembler<'_> {
    fn vpexpandd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpexpandd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpanddEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpexpandd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpexpandd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpanddEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpexpandd(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vpexpandd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpanddEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpexpandd(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpexpandd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpanddEmitter<Vec, Mem> for Assembler<'_> {
    fn vpexpandd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpexpandd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpanddEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpexpandd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpexpandd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpanddEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpexpandd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpexpandd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpanddEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpexpandd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpexpandd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpandqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpexpandq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpexpandq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpandqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpexpandq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpexpandq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpandqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpexpandq(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vpexpandq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpandqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpexpandq(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpexpandq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpandqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpexpandq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpexpandq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpandqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpexpandq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpexpandq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpandqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpexpandq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpexpandq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpandqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpexpandq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpexpandq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpexpandwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpexpandw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpexpandw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpexpandw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpexpandw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandwEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpexpandw(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vpexpandw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandwEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpexpandw(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpexpandw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandwEmitter<Vec, Mem> for Assembler<'_> {
    fn vpexpandw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpexpandw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpexpandw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpexpandw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandwEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpexpandw(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpexpandw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpexpandwEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpexpandw(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpexpandw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpextrbEmitter<Gp, Vec, Imm> for Assembler<'_> {
    fn vpextrb(&mut self, op0: Gp, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpextrb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_BW
    }
}

impl<U2: Into<Imm>> VpextrbEmitter<Gpd, Xmm, U2> for Assembler<'_> {
    fn vpextrb(&mut self, op0: Gpd, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpextrb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_BW
    }
}

impl VpextrbEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vpextrb(&mut self, op0: Mem, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpextrb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_BW
    }
}

impl<U2: Into<Imm>> VpextrbEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn vpextrb(&mut self, op0: Mem, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpextrb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_BW
    }
}

impl VpextrdEmitter<Gp, Vec, Imm> for Assembler<'_> {
    fn vpextrd(&mut self, op0: Gp, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpextrd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ
    }
}

impl<U2: Into<Imm>> VpextrdEmitter<Gpd, Xmm, U2> for Assembler<'_> {
    fn vpextrd(&mut self, op0: Gpd, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpextrd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_DQ
    }
}

impl VpextrdEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vpextrd(&mut self, op0: Mem, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpextrd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ
    }
}

impl<U2: Into<Imm>> VpextrdEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn vpextrd(&mut self, op0: Mem, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpextrd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_DQ
    }
}

impl VpextrqEmitter<Gp, Vec, Imm> for Assembler<'_> {
    fn vpextrq(&mut self, op0: Gp, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpextrq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ
    }
}

impl<U2: Into<Imm>> VpextrqEmitter<Gpq, Xmm, U2> for Assembler<'_> {
    fn vpextrq(&mut self, op0: Gpq, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpextrq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_DQ
    }
}

impl VpextrqEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vpextrq(&mut self, op0: Mem, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpextrq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ
    }
}

impl<U2: Into<Imm>> VpextrqEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn vpextrq(&mut self, op0: Mem, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpextrq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_DQ
    }
}

impl VpextrwEmitter<Gp, Vec, Imm> for Assembler<'_> {
    fn vpextrw(&mut self, op0: Gp, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpextrw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_BW
    }
}

impl<U2: Into<Imm>> VpextrwEmitter<Gpd, Xmm, U2> for Assembler<'_> {
    fn vpextrw(&mut self, op0: Gpd, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpextrw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_BW
    }
}

impl VpextrwEmitter<Mem, Vec, Imm> for Assembler<'_> {
    fn vpextrw(&mut self, op0: Mem, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpextrw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_BW
    }
}

impl<U2: Into<Imm>> VpextrwEmitter<Mem, Xmm, U2> for Assembler<'_> {
    fn vpextrw(&mut self, op0: Mem, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpextrw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_BW
    }
}

impl VpgatherddEmitter<Vec, Mem> for Assembler<'_> {
    fn vpgatherdd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpgatherdd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherddEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpgatherdd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpgatherdd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherddEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpgatherdd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpgatherdd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherddEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpgatherdd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpgatherdd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl Vpgatherdd3Emitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpgatherdd_3(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Vpgatherdd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl Vpgatherdd3Emitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpgatherdd_3(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Vpgatherdd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl Vpgatherdd3Emitter<Ymm, Mem, Ymm> for Assembler<'_> {
    fn vpgatherdd_3(&mut self, op0: Ymm, op1: Mem, op2: Ymm) {
        self.emit_n(
            InstId::Vpgatherdd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl VpgatherdqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpgatherdq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpgatherdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherdqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpgatherdq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpgatherdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherdqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpgatherdq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpgatherdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherdqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpgatherdq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpgatherdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl Vpgatherdq3Emitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpgatherdq_3(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Vpgatherdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl Vpgatherdq3Emitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpgatherdq_3(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Vpgatherdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl Vpgatherdq3Emitter<Ymm, Mem, Ymm> for Assembler<'_> {
    fn vpgatherdq_3(&mut self, op0: Ymm, op1: Mem, op2: Ymm) {
        self.emit_n(
            InstId::Vpgatherdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl VpgatherqdEmitter<Vec, Mem> for Assembler<'_> {
    fn vpgatherqd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpgatherqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherqdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpgatherqd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpgatherqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherqdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpgatherqd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpgatherqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl Vpgatherqd3Emitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpgatherqd_3(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Vpgatherqd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl Vpgatherqd3Emitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpgatherqd_3(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Vpgatherqd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl VpgatherqqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpgatherqq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpgatherqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherqqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpgatherqq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpgatherqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherqqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpgatherqq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpgatherqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpgatherqqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpgatherqq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpgatherqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl Vpgatherqq3Emitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpgatherqq_3(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Vpgatherqq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl Vpgatherqq3Emitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpgatherqq_3(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Vpgatherqq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl Vpgatherqq3Emitter<Ymm, Mem, Ymm> for Assembler<'_> {
    fn vpgatherqq_3(&mut self, op0: Ymm, op1: Mem, op2: Ymm) {
        self.emit_n(
            InstId::Vpgatherqq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl VphadddEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vphaddd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vphaddd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphadddEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vphaddd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vphaddd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphadddEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vphaddd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vphaddd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphadddEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vphaddd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vphaddd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphadddEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vphaddd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vphaddd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphadddEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vphaddd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vphaddd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphaddswEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vphaddsw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vphaddsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphaddswEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vphaddsw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vphaddsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphaddswEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vphaddsw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vphaddsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphaddswEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vphaddsw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vphaddsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphaddswEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vphaddsw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vphaddsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphaddswEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vphaddsw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vphaddsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphaddwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vphaddw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vphaddw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphaddwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vphaddw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vphaddw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphaddwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vphaddw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vphaddw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphaddwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vphaddw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vphaddw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphaddwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vphaddw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vphaddw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphaddwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vphaddw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vphaddw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphminposuwEmitter<Vec, Vec> for Assembler<'_> {
    fn vphminposuw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vphminposuw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VphminposuwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphminposuw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vphminposuw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VphminposuwEmitter<Vec, Mem> for Assembler<'_> {
    fn vphminposuw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vphminposuw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VphminposuwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphminposuw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vphminposuw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VphsubdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vphsubd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vphsubd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphsubdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vphsubd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vphsubd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphsubdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vphsubd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vphsubd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphsubdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vphsubd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vphsubd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphsubdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vphsubd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vphsubd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphsubdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vphsubd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vphsubd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphsubswEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vphsubsw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vphsubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphsubswEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vphsubsw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vphsubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphsubswEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vphsubsw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vphsubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphsubswEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vphsubsw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vphsubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphsubswEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vphsubsw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vphsubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphsubswEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vphsubsw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vphsubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphsubwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vphsubw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vphsubw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphsubwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vphsubw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vphsubw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphsubwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vphsubw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vphsubw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphsubwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vphsubw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vphsubw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphsubwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vphsubw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vphsubw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VphsubwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vphsubw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vphsubw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpinsrbEmitter<Vec, Vec, Gp, Imm> for Assembler<'_> {
    fn vpinsrb(&mut self, op0: Vec, op1: Vec, op2: Gp, op3: Imm) {
        self.emit_n(
            InstId::Vpinsrb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VpinsrbEmitter<Xmm, Xmm, Gpd, U3> for Assembler<'_> {
    fn vpinsrb(&mut self, op0: Xmm, op1: Xmm, op2: Gpd, op3: U3) {
        self.emit_n(
            InstId::Vpinsrb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpinsrbEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpinsrb(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpinsrb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VpinsrbEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpinsrb(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpinsrb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpinsrdEmitter<Vec, Vec, Gp, Imm> for Assembler<'_> {
    fn vpinsrd(&mut self, op0: Vec, op1: Vec, op2: Gp, op3: Imm) {
        self.emit_n(
            InstId::Vpinsrd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> VpinsrdEmitter<Xmm, Xmm, Gpd, U3> for Assembler<'_> {
    fn vpinsrd(&mut self, op0: Xmm, op1: Xmm, op2: Gpd, op3: U3) {
        self.emit_n(
            InstId::Vpinsrd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_DQ{kz}
    }
}

impl VpinsrdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpinsrd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpinsrd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> VpinsrdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpinsrd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpinsrd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_DQ{kz}
    }
}

impl VpinsrqEmitter<Vec, Vec, Gp, Imm> for Assembler<'_> {
    fn vpinsrq(&mut self, op0: Vec, op1: Vec, op2: Gp, op3: Imm) {
        self.emit_n(
            InstId::Vpinsrq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> VpinsrqEmitter<Xmm, Xmm, Gpq, U3> for Assembler<'_> {
    fn vpinsrq(&mut self, op0: Xmm, op1: Xmm, op2: Gpq, op3: U3) {
        self.emit_n(
            InstId::Vpinsrq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_DQ{kz}
    }
}

impl VpinsrqEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpinsrq(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpinsrq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> VpinsrqEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpinsrq(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpinsrq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_DQ{kz}
    }
}

impl VpinsrwEmitter<Vec, Vec, Gp, Imm> for Assembler<'_> {
    fn vpinsrw(&mut self, op0: Vec, op1: Vec, op2: Gp, op3: Imm) {
        self.emit_n(
            InstId::Vpinsrw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VpinsrwEmitter<Xmm, Xmm, Gpd, U3> for Assembler<'_> {
    fn vpinsrw(&mut self, op0: Xmm, op1: Xmm, op2: Gpd, op3: U3) {
        self.emit_n(
            InstId::Vpinsrw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpinsrwEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpinsrw(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpinsrw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX AVX512_BW{kz}
    }
}

impl<U3: Into<Imm>> VpinsrwEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpinsrw(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpinsrw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_BW{kz}
    }
}

impl VplzcntdEmitter<Vec, Vec> for Assembler<'_> {
    fn vplzcntd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vplzcntd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VplzcntdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vplzcntd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vplzcntd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VplzcntdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vplzcntd(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vplzcntd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VplzcntdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vplzcntd(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vplzcntd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VplzcntdEmitter<Vec, Mem> for Assembler<'_> {
    fn vplzcntd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vplzcntd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VplzcntdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vplzcntd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vplzcntd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VplzcntdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vplzcntd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vplzcntd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VplzcntdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vplzcntd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vplzcntd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b32}
    }
}

impl VplzcntqEmitter<Vec, Vec> for Assembler<'_> {
    fn vplzcntq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vplzcntq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b64}
    }
}

impl VplzcntqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vplzcntq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vplzcntq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b64}
    }
}

impl VplzcntqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vplzcntq(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vplzcntq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b64}
    }
}

impl VplzcntqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vplzcntq(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vplzcntq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b64}
    }
}

impl VplzcntqEmitter<Vec, Mem> for Assembler<'_> {
    fn vplzcntq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vplzcntq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b64}
    }
}

impl VplzcntqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vplzcntq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vplzcntq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b64}
    }
}

impl VplzcntqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vplzcntq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vplzcntq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b64}
    }
}

impl VplzcntqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vplzcntq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vplzcntq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_CD{kz|b64}
    }
}

impl Vpmadd52huqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmadd52huq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmadd52huq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52huqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmadd52huq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmadd52huq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52huqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmadd52huq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmadd52huq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52huqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmadd52huq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpmadd52huq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52huqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmadd52huq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmadd52huq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52huqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmadd52huq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmadd52huq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52huqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmadd52huq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmadd52huq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52huqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmadd52huq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmadd52huq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52luqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmadd52luq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmadd52luq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52luqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmadd52luq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmadd52luq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52luqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmadd52luq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmadd52luq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52luqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmadd52luq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpmadd52luq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52luqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmadd52luq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmadd52luq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52luqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmadd52luq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmadd52luq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52luqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmadd52luq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmadd52luq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl Vpmadd52luqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmadd52luq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmadd52luq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_IFMA{kz|b64}
    }
}

impl VpmaddubswEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmaddubsw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmaddubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddubswEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmaddubsw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmaddubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddubswEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmaddubsw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmaddubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddubswEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmaddubsw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpmaddubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddubswEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaddubsw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmaddubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddubswEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaddubsw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaddubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddubswEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaddubsw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaddubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddubswEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmaddubsw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaddubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddwdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmaddwd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmaddwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddwdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmaddwd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmaddwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddwdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmaddwd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmaddwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddwdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmaddwd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpmaddwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddwdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaddwd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmaddwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddwdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaddwd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaddwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddwdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaddwd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaddwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaddwdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmaddwd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaddwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaskmovdEmitter<Mem, Vec, Vec> for Assembler<'_> {
    fn vpmaskmovd(&mut self, op0: Mem, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmaskmovd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovdEmitter<Mem, Xmm, Xmm> for Assembler<'_> {
    fn vpmaskmovd(&mut self, op0: Mem, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmaskmovd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovdEmitter<Mem, Ymm, Ymm> for Assembler<'_> {
    fn vpmaskmovd(&mut self, op0: Mem, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmaskmovd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaskmovd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmaskmovd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaskmovd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaskmovd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaskmovd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaskmovd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovqEmitter<Mem, Vec, Vec> for Assembler<'_> {
    fn vpmaskmovq(&mut self, op0: Mem, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmaskmovq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovqEmitter<Mem, Xmm, Xmm> for Assembler<'_> {
    fn vpmaskmovq(&mut self, op0: Mem, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmaskmovq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovqEmitter<Mem, Ymm, Ymm> for Assembler<'_> {
    fn vpmaskmovq(&mut self, op0: Mem, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmaskmovq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaskmovq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmaskmovq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaskmovq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaskmovq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl VpmaskmovqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaskmovq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaskmovq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2
    }
}

impl VpmaxsbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmaxsb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmaxsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxsbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmaxsb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmaxsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxsbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmaxsb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmaxsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxsbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmaxsb(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpmaxsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxsbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaxsb(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxsbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaxsb(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxsbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaxsb(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxsbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmaxsb(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmaxsd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmaxsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmaxsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmaxsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxsdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmaxsd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmaxsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxsdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmaxsd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpmaxsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaxsd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaxsd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxsdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaxsd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxsdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmaxsd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxsqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmaxsq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmaxsq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxsqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmaxsq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmaxsq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxsqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmaxsq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmaxsq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxsqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmaxsq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpmaxsq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxsqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaxsq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxsq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxsqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaxsq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxsq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxsqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaxsq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxsq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxsqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmaxsq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxsq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxswEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmaxsw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmaxsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxswEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmaxsw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmaxsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxswEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmaxsw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmaxsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxswEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmaxsw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpmaxsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxswEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaxsw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxswEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaxsw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxswEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaxsw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxswEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmaxsw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxubEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmaxub(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmaxub as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxubEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmaxub(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmaxub as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxubEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmaxub(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmaxub as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxubEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmaxub(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpmaxub as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxubEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaxub(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxub as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxubEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaxub(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxub as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxubEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaxub(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxub as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxubEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmaxub(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxub as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxudEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmaxud(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmaxud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxudEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmaxud(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmaxud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxudEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmaxud(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmaxud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxudEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmaxud(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpmaxud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxudEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaxud(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxudEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaxud(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxudEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaxud(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxudEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmaxud(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmaxuqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmaxuq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmaxuq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxuqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmaxuq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmaxuq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxuqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmaxuq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmaxuq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxuqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmaxuq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpmaxuq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxuqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaxuq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxuq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxuqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaxuq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxuq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxuqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaxuq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxuq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxuqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmaxuq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxuq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpmaxuwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmaxuw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmaxuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxuwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmaxuw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmaxuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxuwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmaxuw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmaxuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxuwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmaxuw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpmaxuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxuwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmaxuw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxuwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmaxuw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxuwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmaxuw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmaxuwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmaxuw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmaxuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminsbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpminsb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpminsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminsbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpminsb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpminsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminsbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpminsb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpminsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminsbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpminsb(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpminsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminsbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpminsb(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpminsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminsbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpminsb(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpminsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminsbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpminsb(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpminsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminsbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpminsb(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpminsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpminsd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpminsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpminsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpminsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminsdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpminsd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpminsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminsdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpminsd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpminsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpminsd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpminsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpminsd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpminsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminsdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpminsd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpminsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminsdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpminsd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpminsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminsqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpminsq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpminsq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminsqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpminsq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpminsq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminsqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpminsq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpminsq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminsqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpminsq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpminsq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminsqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpminsq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpminsq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminsqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpminsq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpminsq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminsqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpminsq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpminsq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminsqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpminsq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpminsq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminswEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpminsw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpminsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminswEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpminsw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpminsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminswEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpminsw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpminsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminswEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpminsw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpminsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminswEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpminsw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpminsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminswEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpminsw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpminsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminswEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpminsw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpminsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminswEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpminsw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpminsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminubEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpminub(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpminub as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminubEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpminub(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpminub as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminubEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpminub(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpminub as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminubEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpminub(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpminub as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminubEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpminub(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpminub as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminubEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpminub(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpminub as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminubEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpminub(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpminub as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminubEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpminub(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpminub as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminudEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpminud(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpminud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminudEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpminud(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpminud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminudEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpminud(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpminud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminudEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpminud(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpminud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminudEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpminud(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpminud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminudEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpminud(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpminud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminudEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpminud(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpminud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminudEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpminud(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpminud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpminuqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpminuq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpminuq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminuqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpminuq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpminuq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminuqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpminuq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpminuq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminuqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpminuq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpminuq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminuqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpminuq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpminuq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminuqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpminuq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpminuq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminuqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpminuq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpminuq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminuqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpminuq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpminuq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpminuwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpminuw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpminuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminuwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpminuw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpminuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminuwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpminuw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpminuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminuwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpminuw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpminuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminuwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpminuw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpminuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminuwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpminuw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpminuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminuwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpminuw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpminuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpminuwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpminuw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpminuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl Vpmovb2mEmitter<KReg, Vec> for Assembler<'_> {
    fn vpmovb2m(&mut self, op0: KReg, op1: Vec) {
        self.emit_n(
            InstId::Vpmovb2m as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovb2mEmitter<KReg, Xmm> for Assembler<'_> {
    fn vpmovb2m(&mut self, op0: KReg, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovb2m as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovb2mEmitter<KReg, Ymm> for Assembler<'_> {
    fn vpmovb2m(&mut self, op0: KReg, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovb2m as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovb2mEmitter<KReg, Zmm> for Assembler<'_> {
    fn vpmovb2m(&mut self, op0: KReg, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovb2m as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovd2mEmitter<KReg, Vec> for Assembler<'_> {
    fn vpmovd2m(&mut self, op0: KReg, op1: Vec) {
        self.emit_n(
            InstId::Vpmovd2m as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovd2mEmitter<KReg, Xmm> for Assembler<'_> {
    fn vpmovd2m(&mut self, op0: KReg, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovd2m as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovd2mEmitter<KReg, Ymm> for Assembler<'_> {
    fn vpmovd2m(&mut self, op0: KReg, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovd2m as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovd2mEmitter<KReg, Zmm> for Assembler<'_> {
    fn vpmovd2m(&mut self, op0: KReg, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovd2m as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ
    }
}

impl VpmovdbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovdb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovdb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdbEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovdb(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdbEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vpmovdb(&mut self, op0: Xmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdbEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovdb(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpmovdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdbEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovdb(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdbEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovdb(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdbEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovdb(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovdw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovdw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdwEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovdw(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdwEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vpmovdw(&mut self, op0: Ymm, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdwEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovdw(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpmovdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdwEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovdw(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdwEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovdw(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovdwEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovdw(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vpmovm2bEmitter<Vec, KReg> for Assembler<'_> {
    fn vpmovm2b(&mut self, op0: Vec, op1: KReg) {
        self.emit_n(
            InstId::Vpmovm2b as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovm2bEmitter<Xmm, KReg> for Assembler<'_> {
    fn vpmovm2b(&mut self, op0: Xmm, op1: KReg) {
        self.emit_n(
            InstId::Vpmovm2b as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovm2bEmitter<Ymm, KReg> for Assembler<'_> {
    fn vpmovm2b(&mut self, op0: Ymm, op1: KReg) {
        self.emit_n(
            InstId::Vpmovm2b as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovm2bEmitter<Zmm, KReg> for Assembler<'_> {
    fn vpmovm2b(&mut self, op0: Zmm, op1: KReg) {
        self.emit_n(
            InstId::Vpmovm2b as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovm2dEmitter<Vec, KReg> for Assembler<'_> {
    fn vpmovm2d(&mut self, op0: Vec, op1: KReg) {
        self.emit_n(
            InstId::Vpmovm2d as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovm2dEmitter<Xmm, KReg> for Assembler<'_> {
    fn vpmovm2d(&mut self, op0: Xmm, op1: KReg) {
        self.emit_n(
            InstId::Vpmovm2d as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovm2dEmitter<Ymm, KReg> for Assembler<'_> {
    fn vpmovm2d(&mut self, op0: Ymm, op1: KReg) {
        self.emit_n(
            InstId::Vpmovm2d as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovm2dEmitter<Zmm, KReg> for Assembler<'_> {
    fn vpmovm2d(&mut self, op0: Zmm, op1: KReg) {
        self.emit_n(
            InstId::Vpmovm2d as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovm2qEmitter<Vec, KReg> for Assembler<'_> {
    fn vpmovm2q(&mut self, op0: Vec, op1: KReg) {
        self.emit_n(
            InstId::Vpmovm2q as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovm2qEmitter<Xmm, KReg> for Assembler<'_> {
    fn vpmovm2q(&mut self, op0: Xmm, op1: KReg) {
        self.emit_n(
            InstId::Vpmovm2q as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovm2qEmitter<Ymm, KReg> for Assembler<'_> {
    fn vpmovm2q(&mut self, op0: Ymm, op1: KReg) {
        self.emit_n(
            InstId::Vpmovm2q as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovm2qEmitter<Zmm, KReg> for Assembler<'_> {
    fn vpmovm2q(&mut self, op0: Zmm, op1: KReg) {
        self.emit_n(
            InstId::Vpmovm2q as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovm2wEmitter<Vec, KReg> for Assembler<'_> {
    fn vpmovm2w(&mut self, op0: Vec, op1: KReg) {
        self.emit_n(
            InstId::Vpmovm2w as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovm2wEmitter<Xmm, KReg> for Assembler<'_> {
    fn vpmovm2w(&mut self, op0: Xmm, op1: KReg) {
        self.emit_n(
            InstId::Vpmovm2w as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovm2wEmitter<Ymm, KReg> for Assembler<'_> {
    fn vpmovm2w(&mut self, op0: Ymm, op1: KReg) {
        self.emit_n(
            InstId::Vpmovm2w as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovm2wEmitter<Zmm, KReg> for Assembler<'_> {
    fn vpmovm2w(&mut self, op0: Zmm, op1: KReg) {
        self.emit_n(
            InstId::Vpmovm2w as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW
    }
}

impl VpmovmskbEmitter<Gp, Vec> for Assembler<'_> {
    fn vpmovmskb(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Vpmovmskb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+
    }
}

impl VpmovmskbEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vpmovmskb(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovmskb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+
    }
}

impl VpmovmskbEmitter<Gpd, Ymm> for Assembler<'_> {
    fn vpmovmskb(&mut self, op0: Gpd, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovmskb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+
    }
}

impl Vpmovq2mEmitter<KReg, Vec> for Assembler<'_> {
    fn vpmovq2m(&mut self, op0: KReg, op1: Vec) {
        self.emit_n(
            InstId::Vpmovq2m as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovq2mEmitter<KReg, Xmm> for Assembler<'_> {
    fn vpmovq2m(&mut self, op0: KReg, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovq2m as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovq2mEmitter<KReg, Ymm> for Assembler<'_> {
    fn vpmovq2m(&mut self, op0: KReg, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovq2m as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ
    }
}

impl Vpmovq2mEmitter<KReg, Zmm> for Assembler<'_> {
    fn vpmovq2m(&mut self, op0: KReg, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovq2m as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_DQ
    }
}

impl VpmovqbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovqb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovqb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqbEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovqb(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqbEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vpmovqb(&mut self, op0: Xmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqbEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovqb(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpmovqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqbEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovqb(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqbEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovqb(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqbEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovqb(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovqd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovqd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqdEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovqd(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqdEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vpmovqd(&mut self, op0: Ymm, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqdEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovqd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpmovqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovqd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovqd(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqdEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovqd(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovqw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovqw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqwEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovqw(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqwEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vpmovqw(&mut self, op0: Xmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqwEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovqw(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpmovqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqwEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovqw(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqwEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovqw(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovqwEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovqw(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsdb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovsdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsdb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdbEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovsdb(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovsdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdbEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vpmovsdb(&mut self, op0: Xmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovsdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdbEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovsdb(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpmovsdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdbEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovsdb(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdbEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovsdb(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovsdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdbEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovsdb(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovsdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsdw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovsdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsdw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdwEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovsdw(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovsdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdwEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vpmovsdw(&mut self, op0: Ymm, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovsdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdwEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovsdw(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpmovsdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdwEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovsdw(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdwEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovsdw(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovsdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsdwEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovsdw(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovsdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsqb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovsqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsqb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqbEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovsqb(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovsqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqbEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vpmovsqb(&mut self, op0: Xmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovsqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqbEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovsqb(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpmovsqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqbEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovsqb(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqbEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovsqb(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovsqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqbEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovsqb(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovsqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsqd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovsqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsqd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqdEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovsqd(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovsqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqdEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vpmovsqd(&mut self, op0: Ymm, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovsqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqdEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovsqd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpmovsqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovsqd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovsqd(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovsqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqdEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovsqd(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovsqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsqw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovsqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsqw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqwEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovsqw(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovsqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqwEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vpmovsqw(&mut self, op0: Xmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovsqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqwEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovsqw(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpmovsqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqwEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovsqw(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqwEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovsqw(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovsqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovsqwEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovsqw(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovsqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovswbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovswb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovswbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovswb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovswbEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovswb(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovswbEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vpmovswb(&mut self, op0: Ymm, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovswbEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovswb(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpmovswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovswbEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovswb(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovswbEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovswb(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovswbEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovswb(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovsxbdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsxbd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovsxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsxbd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbdEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovsxbd(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbdEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vpmovsxbd(&mut self, op0: Zmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbdEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovsxbd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovsxbd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovsxbd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovsxbd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsxbq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovsxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsxbq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovsxbq(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbqEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vpmovsxbq(&mut self, op0: Zmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovsxbq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovsxbq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovsxbq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovsxbq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxbwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsxbw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovsxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovsxbwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsxbw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovsxbwEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovsxbw(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovsxbwEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vpmovsxbw(&mut self, op0: Zmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovsxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovsxbwEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovsxbw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovsxbwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovsxbw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovsxbwEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovsxbw(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovsxbwEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovsxbw(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovsxdqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsxdq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovsxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxdqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsxdq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxdqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovsxdq(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxdqEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vpmovsxdq(&mut self, op0: Zmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovsxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxdqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovsxdq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxdqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovsxdq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxdqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovsxdq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxdqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovsxdq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsxwd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovsxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsxwd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwdEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovsxwd(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwdEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vpmovsxwd(&mut self, op0: Zmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovsxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwdEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovsxwd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovsxwd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovsxwd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovsxwd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovsxwq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovsxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovsxwq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovsxwq(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwqEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vpmovsxwq(&mut self, op0: Zmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovsxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovsxwq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovsxwq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovsxwq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovsxwqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovsxwq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovsxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovusdbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovusdb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovusdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovusdb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovusdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdbEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovusdb(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovusdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdbEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vpmovusdb(&mut self, op0: Xmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovusdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdbEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovusdb(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpmovusdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdbEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovusdb(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovusdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdbEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovusdb(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovusdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdbEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovusdb(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovusdb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovusdw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovusdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovusdw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovusdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdwEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovusdw(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovusdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdwEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vpmovusdw(&mut self, op0: Ymm, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovusdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdwEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovusdw(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpmovusdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdwEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovusdw(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovusdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdwEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovusdw(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovusdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusdwEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovusdw(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovusdw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovusqb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovusqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovusqb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovusqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqbEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovusqb(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovusqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqbEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vpmovusqb(&mut self, op0: Xmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovusqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqbEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovusqb(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpmovusqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqbEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovusqb(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovusqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqbEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovusqb(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovusqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqbEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovusqb(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovusqb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovusqd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovusqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovusqd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovusqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqdEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovusqd(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovusqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqdEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vpmovusqd(&mut self, op0: Ymm, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovusqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqdEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovusqd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpmovusqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovusqd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovusqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovusqd(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovusqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqdEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovusqd(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovusqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovusqw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovusqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovusqw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovusqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqwEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovusqw(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovusqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqwEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vpmovusqw(&mut self, op0: Xmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovusqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqwEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovusqw(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpmovusqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqwEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovusqw(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovusqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqwEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovusqw(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovusqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovusqwEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovusqw(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovusqw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpmovuswbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovuswb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovuswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovuswbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovuswb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovuswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovuswbEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovuswb(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovuswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovuswbEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vpmovuswb(&mut self, op0: Ymm, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovuswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovuswbEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovuswb(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpmovuswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovuswbEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovuswb(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovuswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovuswbEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovuswb(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovuswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovuswbEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovuswb(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovuswb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl Vpmovw2mEmitter<KReg, Vec> for Assembler<'_> {
    fn vpmovw2m(&mut self, op0: KReg, op1: Vec) {
        self.emit_n(
            InstId::Vpmovw2m as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovw2mEmitter<KReg, Xmm> for Assembler<'_> {
    fn vpmovw2m(&mut self, op0: KReg, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovw2m as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovw2mEmitter<KReg, Ymm> for Assembler<'_> {
    fn vpmovw2m(&mut self, op0: KReg, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovw2m as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW
    }
}

impl Vpmovw2mEmitter<KReg, Zmm> for Assembler<'_> {
    fn vpmovw2m(&mut self, op0: KReg, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovw2m as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW
    }
}

impl VpmovwbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovwb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovwb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovwbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovwb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovwb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovwbEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vpmovwb(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovwb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovwbEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vpmovwb(&mut self, op0: Ymm, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovwb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovwbEmitter<Mem, Vec> for Assembler<'_> {
    fn vpmovwb(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpmovwb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovwbEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpmovwb(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovwb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovwbEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpmovwb(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovwb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovwbEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpmovwb(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpmovwb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpmovzxbdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovzxbd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovzxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovzxbd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovzxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbdEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovzxbd(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovzxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbdEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vpmovzxbd(&mut self, op0: Zmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovzxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbdEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovzxbd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovzxbd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovzxbd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovzxbd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovzxbq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovzxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovzxbq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovzxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovzxbq(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovzxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbqEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vpmovzxbq(&mut self, op0: Zmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovzxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovzxbq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovzxbq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovzxbq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovzxbq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxbwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovzxbw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovzxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovzxbwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovzxbw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovzxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovzxbwEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovzxbw(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovzxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovzxbwEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vpmovzxbw(&mut self, op0: Zmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovzxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovzxbwEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovzxbw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovzxbwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovzxbw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovzxbwEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovzxbw(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovzxbwEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovzxbw(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmovzxdqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovzxdq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovzxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxdqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovzxdq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovzxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxdqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovzxdq(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovzxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxdqEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vpmovzxdq(&mut self, op0: Zmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovzxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxdqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovzxdq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxdqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovzxdq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxdqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovzxdq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxdqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovzxdq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovzxwd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovzxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovzxwd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovzxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwdEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovzxwd(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovzxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwdEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vpmovzxwd(&mut self, op0: Zmm, op1: Ymm) {
        self.emit_n(
            InstId::Vpmovzxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwdEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovzxwd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovzxwd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovzxwd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovzxwd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpmovzxwq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpmovzxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpmovzxwq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovzxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vpmovzxwq(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovzxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwqEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vpmovzxwq(&mut self, op0: Zmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpmovzxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpmovzxwq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpmovzxwq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpmovzxwq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmovzxwqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpmovzxwq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpmovzxwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpmuldqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmuldq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmuldq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpmuldqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmuldq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmuldq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpmuldqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmuldq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmuldq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpmuldqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmuldq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpmuldq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpmuldqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmuldq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmuldq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpmuldqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmuldq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmuldq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpmuldqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmuldq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmuldq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpmuldqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmuldq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmuldq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpmulhrswEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmulhrsw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmulhrsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhrswEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmulhrsw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmulhrsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhrswEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmulhrsw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmulhrsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhrswEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmulhrsw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpmulhrsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhrswEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmulhrsw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmulhrsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhrswEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmulhrsw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmulhrsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhrswEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmulhrsw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmulhrsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhrswEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmulhrsw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmulhrsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhuwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmulhuw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmulhuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhuwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmulhuw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmulhuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhuwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmulhuw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmulhuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhuwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmulhuw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpmulhuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhuwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmulhuw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmulhuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhuwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmulhuw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmulhuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhuwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmulhuw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmulhuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhuwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmulhuw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmulhuw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmulhw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmulhw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmulhw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmulhw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmulhw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmulhw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmulhw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpmulhw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmulhw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmulhw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmulhw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmulhw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmulhw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmulhw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulhwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmulhw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmulhw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmulldEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmulld(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmulld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmulldEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmulld(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmulld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmulldEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmulld(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmulld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmulldEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmulld(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpmulld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmulldEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmulld(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmulld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmulldEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmulld(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmulld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmulldEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmulld(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmulld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmulldEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmulld(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmulld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpmullqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmullq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmullq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VpmullqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmullq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmullq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VpmullqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmullq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmullq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VpmullqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmullq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpmullq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VpmullqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmullq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmullq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VpmullqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmullq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmullq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VpmullqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmullq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmullq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VpmullqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmullq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmullq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VpmullwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmullw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmullw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmullwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmullw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmullw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmullwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmullw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmullw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmullwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmullw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpmullw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmullwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmullw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmullw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmullwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmullw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmullw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmullwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmullw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmullw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmullwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmullw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmullw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpmultishiftqbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmultishiftqb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmultishiftqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz|b64}
    }
}

impl VpmultishiftqbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmultishiftqb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmultishiftqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz|b64}
    }
}

impl VpmultishiftqbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmultishiftqb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmultishiftqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz|b64}
    }
}

impl VpmultishiftqbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmultishiftqb(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpmultishiftqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz|b64}
    }
}

impl VpmultishiftqbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmultishiftqb(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmultishiftqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz|b64}
    }
}

impl VpmultishiftqbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmultishiftqb(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmultishiftqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz|b64}
    }
}

impl VpmultishiftqbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmultishiftqb(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmultishiftqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz|b64}
    }
}

impl VpmultishiftqbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmultishiftqb(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmultishiftqb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI{kz|b64}
    }
}

impl VpmuludqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpmuludq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpmuludq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpmuludqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmuludq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpmuludq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpmuludqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpmuludq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpmuludq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpmuludqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpmuludq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpmuludq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpmuludqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpmuludq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpmuludq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpmuludqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpmuludq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmuludq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpmuludqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpmuludq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpmuludq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpmuludqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpmuludq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpmuludq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpopcntbEmitter<Vec, Vec> for Assembler<'_> {
    fn vpopcntb(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpopcntb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntbEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpopcntb(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpopcntb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntbEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpopcntb(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vpopcntb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntbEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpopcntb(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpopcntb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntbEmitter<Vec, Mem> for Assembler<'_> {
    fn vpopcntb(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpopcntb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntbEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpopcntb(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpopcntb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntbEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpopcntb(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpopcntb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntbEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpopcntb(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpopcntb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntdEmitter<Vec, Vec> for Assembler<'_> {
    fn vpopcntd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpopcntd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b32}
    }
}

impl VpopcntdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpopcntd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpopcntd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b32}
    }
}

impl VpopcntdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpopcntd(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vpopcntd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b32}
    }
}

impl VpopcntdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpopcntd(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpopcntd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b32}
    }
}

impl VpopcntdEmitter<Vec, Mem> for Assembler<'_> {
    fn vpopcntd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpopcntd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b32}
    }
}

impl VpopcntdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpopcntd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpopcntd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b32}
    }
}

impl VpopcntdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpopcntd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpopcntd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b32}
    }
}

impl VpopcntdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpopcntd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpopcntd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b32}
    }
}

impl VpopcntqEmitter<Vec, Vec> for Assembler<'_> {
    fn vpopcntq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpopcntq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b64}
    }
}

impl VpopcntqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpopcntq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpopcntq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b64}
    }
}

impl VpopcntqEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpopcntq(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vpopcntq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b64}
    }
}

impl VpopcntqEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpopcntq(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpopcntq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b64}
    }
}

impl VpopcntqEmitter<Vec, Mem> for Assembler<'_> {
    fn vpopcntq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpopcntq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b64}
    }
}

impl VpopcntqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpopcntq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpopcntq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b64}
    }
}

impl VpopcntqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpopcntq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpopcntq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b64}
    }
}

impl VpopcntqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpopcntq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpopcntq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_VPOPCNTDQ{kz|b64}
    }
}

impl VpopcntwEmitter<Vec, Vec> for Assembler<'_> {
    fn vpopcntw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vpopcntw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vpopcntw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vpopcntw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntwEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vpopcntw(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vpopcntw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntwEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vpopcntw(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vpopcntw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntwEmitter<Vec, Mem> for Assembler<'_> {
    fn vpopcntw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vpopcntw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vpopcntw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vpopcntw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntwEmitter<Ymm, Mem> for Assembler<'_> {
    fn vpopcntw(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vpopcntw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VpopcntwEmitter<Zmm, Mem> for Assembler<'_> {
    fn vpopcntw(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vpopcntw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_BITALG{kz|b32}
    }
}

impl VporEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpor(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpor as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VporEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpor(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpor as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VporEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpor(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpor as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VporEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpor(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpor as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VporEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpor(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpor as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VporEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpor(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpor as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpordEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpord(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpord as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpordEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpord(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpord as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpordEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpord(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpord as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpordEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpord(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpord as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpordEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpord(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpord as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpordEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpord(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpord as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpordEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpord(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpord as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpordEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpord(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpord as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VporqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vporq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vporq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VporqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vporq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vporq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VporqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vporq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vporq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VporqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vporq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vporq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VporqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vporq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vporq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VporqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vporq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vporq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VporqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vporq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vporq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VporqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vporq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vporq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VproldEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vprold(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vprold as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VproldEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vprold(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vprold as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VproldEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vprold(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vprold as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VproldEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vprold(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vprold as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VproldEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vprold(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vprold as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VproldEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vprold(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vprold as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VproldEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vprold(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vprold as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VproldEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vprold(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vprold as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VprolqEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vprolq(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vprolq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprolqEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vprolq(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vprolq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprolqEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vprolq(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vprolq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprolqEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vprolq(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vprolq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VprolqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vprolq(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vprolq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprolqEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vprolq(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vprolq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprolqEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vprolq(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vprolq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprolqEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vprolq(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vprolq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VprolvdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vprolvd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vprolvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprolvdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vprolvd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vprolvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprolvdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vprolvd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vprolvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprolvdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vprolvd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vprolvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprolvdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vprolvd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vprolvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprolvdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vprolvd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vprolvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprolvdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vprolvd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vprolvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprolvdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vprolvd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vprolvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprolvqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vprolvq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vprolvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprolvqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vprolvq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vprolvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprolvqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vprolvq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vprolvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprolvqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vprolvq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vprolvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprolvqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vprolvq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vprolvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprolvqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vprolvq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vprolvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprolvqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vprolvq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vprolvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprolvqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vprolvq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vprolvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprordEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vprord(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vprord as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VprordEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vprord(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vprord as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VprordEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vprord(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vprord as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VprordEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vprord(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vprord as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VprordEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vprord(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vprord as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VprordEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vprord(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vprord as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VprordEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vprord(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vprord as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VprordEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vprord(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vprord as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VprorqEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vprorq(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vprorq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprorqEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vprorq(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vprorq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprorqEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vprorq(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vprorq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprorqEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vprorq(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vprorq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VprorqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vprorq(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vprorq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprorqEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vprorq(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vprorq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprorqEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vprorq(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vprorq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VprorqEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vprorq(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vprorq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VprorvdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vprorvd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vprorvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprorvdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vprorvd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vprorvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprorvdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vprorvd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vprorvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprorvdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vprorvd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vprorvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprorvdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vprorvd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vprorvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprorvdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vprorvd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vprorvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprorvdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vprorvd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vprorvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprorvdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vprorvd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vprorvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VprorvqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vprorvq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vprorvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprorvqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vprorvq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vprorvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprorvqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vprorvq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vprorvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprorvqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vprorvq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vprorvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprorvqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vprorvq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vprorvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprorvqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vprorvq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vprorvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprorvqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vprorvq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vprorvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VprorvqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vprorvq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vprorvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsadbwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsadbw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsadbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW
    }
}

impl VpsadbwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsadbw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsadbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW
    }
}

impl VpsadbwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsadbw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpsadbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW
    }
}

impl VpsadbwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsadbw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpsadbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW
    }
}

impl VpsadbwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsadbw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsadbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW
    }
}

impl VpsadbwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsadbw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsadbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW
    }
}

impl VpsadbwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsadbw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsadbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW
    }
}

impl VpsadbwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsadbw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsadbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW
    }
}

impl VpscatterddEmitter<Mem, Vec> for Assembler<'_> {
    fn vpscatterdd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpscatterdd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterddEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpscatterdd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpscatterdd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterddEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpscatterdd(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpscatterdd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterddEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpscatterdd(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpscatterdd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterdqEmitter<Mem, Vec> for Assembler<'_> {
    fn vpscatterdq(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpscatterdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterdqEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpscatterdq(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpscatterdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterdqEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpscatterdq(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpscatterdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterdqEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpscatterdq(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpscatterdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterqdEmitter<Mem, Vec> for Assembler<'_> {
    fn vpscatterqd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpscatterqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterqdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpscatterqd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpscatterqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterqdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpscatterqd(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpscatterqd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterqqEmitter<Mem, Vec> for Assembler<'_> {
    fn vpscatterqq(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vpscatterqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterqqEmitter<Mem, Xmm> for Assembler<'_> {
    fn vpscatterqq(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vpscatterqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterqqEmitter<Mem, Ymm> for Assembler<'_> {
    fn vpscatterqq(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vpscatterqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpscatterqqEmitter<Mem, Zmm> for Assembler<'_> {
    fn vpscatterqq(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vpscatterqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VpshlddEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpshldd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpshldd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshlddEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpshldd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpshldd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshlddEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpshldd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vpshldd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshlddEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpshldd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vpshldd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshlddEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpshldd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpshldd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshlddEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpshldd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpshldd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshlddEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpshldd(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpshldd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshlddEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vpshldd(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpshldd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldqEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpshldq(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpshldq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldqEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpshldq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpshldq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldqEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpshldq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vpshldq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldqEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpshldq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vpshldq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldqEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpshldq(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpshldq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldqEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpshldq(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpshldq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldqEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpshldq(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpshldq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldqEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vpshldq(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpshldq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshldvd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpshldvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshldvd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpshldvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpshldvd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpshldvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpshldvd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpshldvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshldvd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpshldvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshldvd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshldvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpshldvd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpshldvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpshldvd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshldvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshldvq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpshldvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshldvq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpshldvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpshldvq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpshldvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpshldvq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpshldvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshldvq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpshldvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshldvq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshldvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpshldvq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpshldvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpshldvq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshldvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshldvw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpshldvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshldvw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpshldvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpshldvw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpshldvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpshldvw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpshldvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshldvw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpshldvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshldvw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshldvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpshldvw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpshldvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldvwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpshldvw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshldvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldwEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpshldw(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpshldw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldwEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpshldw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpshldw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldwEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpshldw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vpshldw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldwEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpshldw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vpshldw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshldwEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpshldw(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpshldw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldwEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpshldw(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpshldw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldwEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpshldw(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpshldw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshldwEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vpshldw(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpshldw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrddEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpshrdd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpshrdd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrddEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpshrdd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpshrdd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrddEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpshrdd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vpshrdd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrddEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpshrdd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vpshrdd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrddEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpshrdd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpshrdd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrddEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpshrdd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpshrdd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrddEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpshrdd(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpshrdd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrddEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vpshrdd(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpshrdd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdqEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpshrdq(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpshrdq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdqEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpshrdq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpshrdq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdqEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpshrdq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vpshrdq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdqEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpshrdq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vpshrdq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdqEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpshrdq(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpshrdq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdqEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpshrdq(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpshrdq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdqEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpshrdq(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpshrdq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdqEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vpshrdq(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpshrdq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshrdvd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpshrdvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshrdvd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpshrdvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpshrdvd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpshrdvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpshrdvd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpshrdvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshrdvd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpshrdvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshrdvd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshrdvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpshrdvd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpshrdvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpshrdvd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshrdvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshrdvq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpshrdvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshrdvq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpshrdvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpshrdvq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpshrdvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpshrdvq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpshrdvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshrdvq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpshrdvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshrdvq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshrdvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpshrdvq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpshrdvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpshrdvq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshrdvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshrdvw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpshrdvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshrdvw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpshrdvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpshrdvw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpshrdvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpshrdvw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpshrdvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshrdvw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpshrdvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshrdvw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshrdvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpshrdvw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpshrdvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdvwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpshrdvw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshrdvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdwEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpshrdw(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpshrdw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdwEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpshrdw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpshrdw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdwEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpshrdw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vpshrdw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdwEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpshrdw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vpshrdw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshrdwEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpshrdw(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpshrdw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdwEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpshrdw(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpshrdw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdwEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpshrdw(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpshrdw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl<U3: Into<Imm>> VpshrdwEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vpshrdw(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpshrdw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_VBMI2{kz}
    }
}

impl VpshufbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshufb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpshufb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshufbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshufb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpshufb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshufbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpshufb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpshufb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshufbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpshufb(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpshufb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshufbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshufb(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpshufb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshufbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshufb(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshufb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshufbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpshufb(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpshufb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshufbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpshufb(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshufb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshufbitqmbEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vpshufbitqmb(&mut self, op0: KReg, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpshufbitqmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BITALG{k}
    }
}

impl VpshufbitqmbEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vpshufbitqmb(&mut self, op0: KReg, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpshufbitqmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BITALG{k}
    }
}

impl VpshufbitqmbEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vpshufbitqmb(&mut self, op0: KReg, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpshufbitqmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BITALG{k}
    }
}

impl VpshufbitqmbEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vpshufbitqmb(&mut self, op0: KReg, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpshufbitqmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BITALG{k}
    }
}

impl VpshufbitqmbEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vpshufbitqmb(&mut self, op0: KReg, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpshufbitqmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BITALG{k}
    }
}

impl VpshufbitqmbEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vpshufbitqmb(&mut self, op0: KReg, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshufbitqmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BITALG{k}
    }
}

impl VpshufbitqmbEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vpshufbitqmb(&mut self, op0: KReg, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpshufbitqmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BITALG{k}
    }
}

impl VpshufbitqmbEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vpshufbitqmb(&mut self, op0: KReg, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshufbitqmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BITALG{k}
    }
}

impl VpshufdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpshufd(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpshufd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpshufdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpshufd(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpshufd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpshufdEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpshufd(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vpshufd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpshufdEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpshufd(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vpshufd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpshufdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpshufd(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpshufd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpshufdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpshufd(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpshufd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpshufdEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpshufd(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpshufd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpshufdEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpshufd(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpshufd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpshufhwEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpshufhw(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpshufhw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshufhwEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpshufhw(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpshufhw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshufhwEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpshufhw(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vpshufhw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshufhwEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpshufhw(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vpshufhw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshufhwEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpshufhw(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpshufhw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshufhwEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpshufhw(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpshufhw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshufhwEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpshufhw(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpshufhw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshufhwEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpshufhw(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpshufhw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshuflwEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpshuflw(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpshuflw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshuflwEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpshuflw(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpshuflw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshuflwEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpshuflw(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vpshuflw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshuflwEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpshuflw(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vpshuflw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpshuflwEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpshuflw(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpshuflw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshuflwEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpshuflw(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpshuflw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshuflwEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpshuflw(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpshuflw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpshuflwEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpshuflw(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpshuflw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsignbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsignb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsignb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpsignbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsignb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsignb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpsignbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsignb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpsignb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpsignbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsignb(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsignb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpsignbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsignb(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsignb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpsignbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsignb(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsignb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpsigndEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsignd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsignd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpsigndEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsignd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsignd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpsigndEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsignd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpsignd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpsigndEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsignd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsignd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpsigndEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsignd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsignd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpsigndEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsignd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsignd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpsignwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsignw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsignw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpsignwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsignw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsignw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpsignwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsignw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpsignw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpsignwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsignw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsignw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpsignwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsignw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsignw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpsignwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsignw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsignw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpslldEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpslld(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpslldEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpslld(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpslldEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpslld(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpslldEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpslld(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpslldEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpslld(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpslldEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpslld(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpslldEmitter<Ymm, Ymm, Xmm> for Assembler<'_> {
    fn vpslld(&mut self, op0: Ymm, op1: Ymm, op2: Xmm) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpslldEmitter<Zmm, Zmm, Xmm> for Assembler<'_> {
    fn vpslld(&mut self, op0: Zmm, op1: Zmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpslldEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpslld(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpslldEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpslld(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpslldEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpslld(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpslldEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpslld(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpslldEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpslld(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpslldEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpslld(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpslldEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpslld(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpslldEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpslld(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpslld as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VpslldqEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpslldq(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpslldq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW
    }
}

impl<U2: Into<Imm>> VpslldqEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpslldq(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpslldq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW
    }
}

impl<U2: Into<Imm>> VpslldqEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpslldq(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vpslldq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW
    }
}

impl<U2: Into<Imm>> VpslldqEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpslldq(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vpslldq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW
    }
}

impl VpslldqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpslldq(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpslldq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW
    }
}

impl<U2: Into<Imm>> VpslldqEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpslldq(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpslldq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_BW
    }
}

impl<U2: Into<Imm>> VpslldqEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpslldq(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpslldq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_BW
    }
}

impl<U2: Into<Imm>> VpslldqEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpslldq(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpslldq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_BW
    }
}

impl VpsllqEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpsllq(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsllqEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpsllq(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsllqEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpsllq(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsllqEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpsllq(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpsllqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsllq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsllqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsllq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsllqEmitter<Ymm, Ymm, Xmm> for Assembler<'_> {
    fn vpsllq(&mut self, op0: Ymm, op1: Ymm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsllqEmitter<Zmm, Zmm, Xmm> for Assembler<'_> {
    fn vpsllq(&mut self, op0: Zmm, op1: Zmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsllqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsllq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsllqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsllq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsllqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsllq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsllqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsllq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsllqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpsllq(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsllqEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpsllq(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsllqEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpsllq(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsllqEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpsllq(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsllq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsllvdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsllvd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsllvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsllvdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsllvd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsllvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsllvdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsllvd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpsllvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsllvdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsllvd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpsllvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsllvdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsllvd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsllvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsllvdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsllvd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsllvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsllvdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsllvd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsllvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsllvdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsllvd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsllvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsllvqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsllvq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsllvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsllvqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsllvq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsllvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsllvqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsllvq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpsllvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsllvqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsllvq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpsllvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsllvqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsllvq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsllvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsllvqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsllvq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsllvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsllvqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsllvq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsllvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsllvqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsllvq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsllvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsllvwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsllvw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsllvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsllvwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsllvw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsllvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsllvwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsllvw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpsllvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsllvwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsllvw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpsllvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsllvwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsllvw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsllvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsllvwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsllvw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsllvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsllvwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsllvw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsllvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsllvwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsllvw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsllvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsllwEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpsllw(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsllwEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpsllw(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsllwEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpsllw(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsllwEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpsllw(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsllwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsllw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsllwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsllw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsllwEmitter<Ymm, Ymm, Xmm> for Assembler<'_> {
    fn vpsllw(&mut self, op0: Ymm, op1: Ymm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsllwEmitter<Zmm, Zmm, Xmm> for Assembler<'_> {
    fn vpsllw(&mut self, op0: Zmm, op1: Zmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsllwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsllw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsllwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsllw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsllwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsllw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsllwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsllw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsllwEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpsllw(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsllwEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpsllw(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsllwEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpsllw(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsllwEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpsllw(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsllw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl VpsradEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpsrad(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsradEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpsrad(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsradEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpsrad(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsradEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpsrad(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpsradEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsrad(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsradEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsrad(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsradEmitter<Ymm, Ymm, Xmm> for Assembler<'_> {
    fn vpsrad(&mut self, op0: Ymm, op1: Ymm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsradEmitter<Zmm, Zmm, Xmm> for Assembler<'_> {
    fn vpsrad(&mut self, op0: Zmm, op1: Zmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsradEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsrad(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsradEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsrad(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsradEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsrad(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsradEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsrad(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsradEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpsrad(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsradEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpsrad(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsradEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpsrad(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsradEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpsrad(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsrad as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VpsraqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsraq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpsraqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsraq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpsraqEmitter<Ymm, Ymm, Xmm> for Assembler<'_> {
    fn vpsraq(&mut self, op0: Ymm, op1: Ymm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpsraqEmitter<Zmm, Zmm, Xmm> for Assembler<'_> {
    fn vpsraq(&mut self, op0: Zmm, op1: Zmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpsraqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsraq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpsraqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsraq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpsraqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsraq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpsraqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsraq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VpsraqEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpsraq(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsraqEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpsraq(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsraqEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpsraq(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsraqEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpsraq(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsraqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpsraq(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsraqEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpsraq(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsraqEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpsraq(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsraqEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpsraq(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsraq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsravdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsravd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsravd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsravdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsravd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsravd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsravdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsravd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpsravd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsravdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsravd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpsravd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsravdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsravd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsravd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsravdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsravd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsravd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsravdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsravd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsravd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsravdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsravd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsravd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsravqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsravq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsravq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsravqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsravq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsravq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsravqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsravq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpsravq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsravqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsravq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpsravq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsravqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsravq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsravq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsravqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsravq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsravq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsravqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsravq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsravq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsravqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsravq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsravq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsravwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsravw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsravw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsravwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsravw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsravw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsravwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsravw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpsravw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsravwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsravw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpsravw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsravwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsravw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsravw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsravwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsravw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsravw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsravwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsravw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsravw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsravwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsravw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsravw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsrawEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpsraw(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrawEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpsraw(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrawEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpsraw(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrawEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpsraw(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrawEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsraw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrawEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsraw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrawEmitter<Ymm, Ymm, Xmm> for Assembler<'_> {
    fn vpsraw(&mut self, op0: Ymm, op1: Ymm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrawEmitter<Zmm, Zmm, Xmm> for Assembler<'_> {
    fn vpsraw(&mut self, op0: Zmm, op1: Zmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrawEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsraw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrawEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsraw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrawEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsraw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrawEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsraw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrawEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpsraw(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrawEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpsraw(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrawEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpsraw(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrawEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpsraw(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsraw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl VpsrldEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpsrld(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsrldEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpsrld(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsrldEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpsrld(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsrldEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpsrld(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpsrldEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsrld(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsrldEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsrld(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsrldEmitter<Ymm, Ymm, Xmm> for Assembler<'_> {
    fn vpsrld(&mut self, op0: Ymm, op1: Ymm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsrldEmitter<Zmm, Zmm, Xmm> for Assembler<'_> {
    fn vpsrld(&mut self, op0: Zmm, op1: Zmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsrldEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsrld(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsrldEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsrld(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsrldEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsrld(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsrldEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsrld(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz}
    }
}

impl VpsrldEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpsrld(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsrldEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpsrld(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsrldEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpsrld(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VpsrldEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpsrld(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsrld as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VpsrldqEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpsrldq(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpsrldq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW
    }
}

impl<U2: Into<Imm>> VpsrldqEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpsrldq(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpsrldq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW
    }
}

impl<U2: Into<Imm>> VpsrldqEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpsrldq(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vpsrldq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW
    }
}

impl<U2: Into<Imm>> VpsrldqEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpsrldq(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vpsrldq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW
    }
}

impl VpsrldqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpsrldq(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpsrldq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW
    }
}

impl<U2: Into<Imm>> VpsrldqEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpsrldq(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsrldq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_BW
    }
}

impl<U2: Into<Imm>> VpsrldqEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpsrldq(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsrldq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_BW
    }
}

impl<U2: Into<Imm>> VpsrldqEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpsrldq(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsrldq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_BW
    }
}

impl VpsrlqEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpsrlq(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsrlqEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpsrlq(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsrlqEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpsrlq(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsrlqEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpsrlq(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VpsrlqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsrlq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VpsrlqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsrlq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VpsrlqEmitter<Ymm, Ymm, Xmm> for Assembler<'_> {
    fn vpsrlq(&mut self, op0: Ymm, op1: Ymm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VpsrlqEmitter<Zmm, Zmm, Xmm> for Assembler<'_> {
    fn vpsrlq(&mut self, op0: Zmm, op1: Zmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VpsrlqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsrlq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VpsrlqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsrlq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VpsrlqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsrlq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VpsrlqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsrlq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz}
    }
}

impl VpsrlqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpsrlq(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsrlqEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpsrlq(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsrlqEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpsrlq(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VpsrlqEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpsrlq(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsrlq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VpsrlvdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsrlvd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsrlvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsrlvdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsrlvd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsrlvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsrlvdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsrlvd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpsrlvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsrlvdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsrlvd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpsrlvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsrlvdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsrlvd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsrlvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsrlvdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsrlvd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsrlvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsrlvdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsrlvd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsrlvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsrlvdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsrlvd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsrlvd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b32}
    }
}

impl VpsrlvqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsrlvq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsrlvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsrlvqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsrlvq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsrlvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsrlvqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsrlvq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpsrlvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsrlvqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsrlvq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpsrlvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsrlvqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsrlvq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsrlvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsrlvqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsrlvq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsrlvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsrlvqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsrlvq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsrlvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsrlvqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsrlvq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsrlvq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX2 AVX512_F{kz|b64}
    }
}

impl VpsrlvwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsrlvw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsrlvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsrlvwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsrlvw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsrlvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsrlvwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsrlvw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpsrlvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsrlvwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsrlvw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpsrlvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsrlvwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsrlvw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsrlvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsrlvwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsrlvw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsrlvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsrlvwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsrlvw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsrlvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsrlvwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsrlvw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsrlvw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl VpsrlwEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpsrlw(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrlwEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vpsrlw(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrlwEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vpsrlw(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrlwEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vpsrlw(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrlwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsrlw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrlwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsrlw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrlwEmitter<Ymm, Ymm, Xmm> for Assembler<'_> {
    fn vpsrlw(&mut self, op0: Ymm, op1: Ymm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrlwEmitter<Zmm, Zmm, Xmm> for Assembler<'_> {
    fn vpsrlw(&mut self, op0: Zmm, op1: Zmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrlwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsrlw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrlwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsrlw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrlwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsrlw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrlwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsrlw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsrlwEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpsrlw(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrlwEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vpsrlw(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrlwEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vpsrlw(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl<U2: Into<Imm>> VpsrlwEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vpsrlw(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vpsrlw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_BW{kz}
    }
}

impl VpsubbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsubb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsubb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsubb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsubb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsubb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpsubb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsubb(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpsubb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsubb(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsubb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsubb(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsubb(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsubb(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsubd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsubd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpsubdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsubd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsubd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpsubdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsubd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpsubd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpsubdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsubd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpsubd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpsubdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsubd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsubd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpsubdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsubd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpsubdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsubd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpsubdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsubd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpsubqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsubq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsubq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpsubqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsubq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsubq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpsubqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsubq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpsubq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpsubqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsubq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpsubq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpsubqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsubq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsubq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpsubqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsubq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpsubqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsubq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpsubqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsubq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpsubsbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsubsb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsubsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubsbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsubsb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsubsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubsbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsubsb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpsubsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubsbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsubsb(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpsubsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubsbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsubsb(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsubsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubsbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsubsb(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubsbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsubsb(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubsbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsubsb(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubsb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubswEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsubsw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubswEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsubsw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubswEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsubsw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpsubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubswEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsubsw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpsubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubswEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsubsw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubswEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsubsw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubswEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsubsw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubswEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsubsw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubsw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubusbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsubusb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsubusb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubusbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsubusb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsubusb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubusbEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsubusb(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpsubusb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubusbEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsubusb(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpsubusb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubusbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsubusb(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsubusb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubusbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsubusb(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubusb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubusbEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsubusb(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubusb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubusbEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsubusb(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubusb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubuswEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsubusw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsubusw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubuswEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsubusw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsubusw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubuswEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsubusw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpsubusw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubuswEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsubusw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpsubusw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubuswEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsubusw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsubusw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubuswEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsubusw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubusw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubuswEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsubusw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubusw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubuswEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsubusw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubusw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpsubwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpsubw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpsubw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpsubwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpsubw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpsubw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpsubwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpsubw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpsubw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpsubwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpsubw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpsubw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpsubwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpsubw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpsubw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpsubwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpsubw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpsubwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpsubw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpsubwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpsubw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpsubw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_BW{kz}
    }
}

impl VpternlogdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpternlogd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpternlogd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VpternlogdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpternlogd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpternlogd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VpternlogdEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpternlogd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vpternlogd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VpternlogdEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpternlogd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vpternlogd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VpternlogdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpternlogd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpternlogd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VpternlogdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpternlogd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpternlogd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VpternlogdEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpternlogd(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpternlogd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VpternlogdEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vpternlogd(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpternlogd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VpternlogqEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpternlogq(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpternlogq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VpternlogqEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpternlogq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpternlogq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VpternlogqEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vpternlogq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vpternlogq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VpternlogqEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vpternlogq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vpternlogq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VpternlogqEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpternlogq(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpternlogq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VpternlogqEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpternlogq(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpternlogq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VpternlogqEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vpternlogq(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpternlogq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VpternlogqEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vpternlogq(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpternlogq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VptestEmitter<Vec, Vec> for Assembler<'_> {
    fn vptest(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Vptest as u32, &[op0.as_operand(), op1.as_operand()]); // AVX
    }
}

impl VptestEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vptest(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Vptest as u32, &[op0.as_operand(), op1.as_operand()]); // AVX
    }
}

impl VptestEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vptest(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(InstId::Vptest as u32, &[op0.as_operand(), op1.as_operand()]); // AVX
    }
}

impl VptestEmitter<Vec, Mem> for Assembler<'_> {
    fn vptest(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Vptest as u32, &[op0.as_operand(), op1.as_operand()]); // AVX
    }
}

impl VptestEmitter<Xmm, Mem> for Assembler<'_> {
    fn vptest(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Vptest as u32, &[op0.as_operand(), op1.as_operand()]); // AVX
    }
}

impl VptestEmitter<Ymm, Mem> for Assembler<'_> {
    fn vptest(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(InstId::Vptest as u32, &[op0.as_operand(), op1.as_operand()]); // AVX
    }
}

impl VptestmbEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vptestmb(&mut self, op0: KReg, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vptestmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmbEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vptestmb(&mut self, op0: KReg, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vptestmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmbEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vptestmb(&mut self, op0: KReg, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vptestmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmbEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vptestmb(&mut self, op0: KReg, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vptestmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmbEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vptestmb(&mut self, op0: KReg, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vptestmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmbEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vptestmb(&mut self, op0: KReg, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vptestmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmbEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vptestmb(&mut self, op0: KReg, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vptestmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmbEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vptestmb(&mut self, op0: KReg, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vptestmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmdEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vptestmd(&mut self, op0: KReg, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vptestmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestmdEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vptestmd(&mut self, op0: KReg, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vptestmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestmdEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vptestmd(&mut self, op0: KReg, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vptestmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestmdEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vptestmd(&mut self, op0: KReg, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vptestmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestmdEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vptestmd(&mut self, op0: KReg, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vptestmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestmdEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vptestmd(&mut self, op0: KReg, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vptestmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestmdEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vptestmd(&mut self, op0: KReg, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vptestmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestmdEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vptestmd(&mut self, op0: KReg, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vptestmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestmqEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vptestmq(&mut self, op0: KReg, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vptestmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestmqEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vptestmq(&mut self, op0: KReg, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vptestmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestmqEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vptestmq(&mut self, op0: KReg, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vptestmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestmqEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vptestmq(&mut self, op0: KReg, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vptestmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestmqEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vptestmq(&mut self, op0: KReg, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vptestmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestmqEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vptestmq(&mut self, op0: KReg, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vptestmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestmqEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vptestmq(&mut self, op0: KReg, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vptestmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestmqEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vptestmq(&mut self, op0: KReg, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vptestmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestmwEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vptestmw(&mut self, op0: KReg, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vptestmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmwEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vptestmw(&mut self, op0: KReg, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vptestmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmwEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vptestmw(&mut self, op0: KReg, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vptestmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmwEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vptestmw(&mut self, op0: KReg, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vptestmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmwEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vptestmw(&mut self, op0: KReg, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vptestmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmwEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vptestmw(&mut self, op0: KReg, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vptestmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmwEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vptestmw(&mut self, op0: KReg, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vptestmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestmwEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vptestmw(&mut self, op0: KReg, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vptestmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmbEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vptestnmb(&mut self, op0: KReg, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vptestnmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmbEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vptestnmb(&mut self, op0: KReg, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vptestnmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmbEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vptestnmb(&mut self, op0: KReg, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vptestnmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmbEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vptestnmb(&mut self, op0: KReg, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vptestnmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmbEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vptestnmb(&mut self, op0: KReg, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vptestnmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmbEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vptestnmb(&mut self, op0: KReg, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vptestnmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmbEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vptestnmb(&mut self, op0: KReg, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vptestnmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmbEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vptestnmb(&mut self, op0: KReg, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vptestnmb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmdEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vptestnmd(&mut self, op0: KReg, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vptestnmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestnmdEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vptestnmd(&mut self, op0: KReg, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vptestnmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestnmdEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vptestnmd(&mut self, op0: KReg, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vptestnmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestnmdEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vptestnmd(&mut self, op0: KReg, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vptestnmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestnmdEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vptestnmd(&mut self, op0: KReg, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vptestnmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestnmdEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vptestnmd(&mut self, op0: KReg, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vptestnmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestnmdEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vptestnmd(&mut self, op0: KReg, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vptestnmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestnmdEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vptestnmd(&mut self, op0: KReg, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vptestnmd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b32}
    }
}

impl VptestnmqEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vptestnmq(&mut self, op0: KReg, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vptestnmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestnmqEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vptestnmq(&mut self, op0: KReg, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vptestnmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestnmqEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vptestnmq(&mut self, op0: KReg, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vptestnmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestnmqEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vptestnmq(&mut self, op0: KReg, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vptestnmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestnmqEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vptestnmq(&mut self, op0: KReg, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vptestnmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestnmqEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vptestnmq(&mut self, op0: KReg, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vptestnmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestnmqEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vptestnmq(&mut self, op0: KReg, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vptestnmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestnmqEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vptestnmq(&mut self, op0: KReg, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vptestnmq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{k|b64}
    }
}

impl VptestnmwEmitter<KReg, Vec, Vec> for Assembler<'_> {
    fn vptestnmw(&mut self, op0: KReg, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vptestnmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmwEmitter<KReg, Xmm, Xmm> for Assembler<'_> {
    fn vptestnmw(&mut self, op0: KReg, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vptestnmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmwEmitter<KReg, Ymm, Ymm> for Assembler<'_> {
    fn vptestnmw(&mut self, op0: KReg, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vptestnmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmwEmitter<KReg, Zmm, Zmm> for Assembler<'_> {
    fn vptestnmw(&mut self, op0: KReg, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vptestnmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmwEmitter<KReg, Vec, Mem> for Assembler<'_> {
    fn vptestnmw(&mut self, op0: KReg, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vptestnmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmwEmitter<KReg, Xmm, Mem> for Assembler<'_> {
    fn vptestnmw(&mut self, op0: KReg, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vptestnmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmwEmitter<KReg, Ymm, Mem> for Assembler<'_> {
    fn vptestnmw(&mut self, op0: KReg, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vptestnmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VptestnmwEmitter<KReg, Zmm, Mem> for Assembler<'_> {
    fn vptestnmw(&mut self, op0: KReg, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vptestnmw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_BW{k}
    }
}

impl VpunpckhbwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpunpckhbw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpunpckhbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhbwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpunpckhbw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpunpckhbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhbwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpunpckhbw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpunpckhbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhbwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpunpckhbw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpunpckhbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhbwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpunpckhbw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpunpckhbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhbwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpunpckhbw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpckhbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhbwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpunpckhbw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpckhbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhbwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpunpckhbw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpckhbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhdqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpunpckhdq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpunpckhdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckhdqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpunpckhdq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpunpckhdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckhdqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpunpckhdq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpunpckhdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckhdqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpunpckhdq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpunpckhdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckhdqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpunpckhdq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpunpckhdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckhdqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpunpckhdq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpckhdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckhdqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpunpckhdq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpckhdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckhdqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpunpckhdq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpckhdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckhqdqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpunpckhqdq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpunpckhqdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpckhqdqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpunpckhqdq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpunpckhqdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpckhqdqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpunpckhqdq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpunpckhqdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpckhqdqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpunpckhqdq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpunpckhqdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpckhqdqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpunpckhqdq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpunpckhqdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpckhqdqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpunpckhqdq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpckhqdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpckhqdqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpunpckhqdq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpckhqdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpckhqdqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpunpckhqdq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpckhqdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpckhwdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpunpckhwd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpunpckhwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhwdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpunpckhwd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpunpckhwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhwdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpunpckhwd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpunpckhwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhwdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpunpckhwd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpunpckhwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhwdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpunpckhwd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpunpckhwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhwdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpunpckhwd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpckhwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhwdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpunpckhwd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpckhwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckhwdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpunpckhwd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpckhwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklbwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpunpcklbw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpunpcklbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklbwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpunpcklbw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpunpcklbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklbwEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpunpcklbw(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpunpcklbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklbwEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpunpcklbw(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpunpcklbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklbwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpunpcklbw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpunpcklbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklbwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpunpcklbw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpcklbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklbwEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpunpcklbw(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpcklbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklbwEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpunpcklbw(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpcklbw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpckldqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpunpckldq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpunpckldq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckldqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpunpckldq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpunpckldq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckldqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpunpckldq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpunpckldq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckldqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpunpckldq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpunpckldq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckldqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpunpckldq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpunpckldq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckldqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpunpckldq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpckldq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckldqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpunpckldq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpckldq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpckldqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpunpckldq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpckldq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b32}
    }
}

impl VpunpcklqdqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpunpcklqdq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpunpcklqdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpcklqdqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpunpcklqdq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpunpcklqdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpcklqdqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpunpcklqdq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpunpcklqdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpcklqdqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpunpcklqdq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpunpcklqdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpcklqdqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpunpcklqdq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpunpcklqdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpcklqdqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpunpcklqdq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpcklqdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpcklqdqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpunpcklqdq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpcklqdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpcklqdqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpunpcklqdq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpcklqdq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_F{kz|b64}
    }
}

impl VpunpcklwdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpunpcklwd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpunpcklwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklwdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpunpcklwd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpunpcklwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklwdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpunpcklwd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpunpcklwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklwdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpunpcklwd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpunpcklwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklwdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpunpcklwd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpunpcklwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklwdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpunpcklwd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpcklwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklwdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpunpcklwd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpcklwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpunpcklwdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpunpcklwd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpunpcklwd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+ AVX512_BW{kz}
    }
}

impl VpxorEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpxor(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpxor as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpxorEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpxor(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpxor as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpxorEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpxor(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpxor as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpxorEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpxor(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpxor as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpxorEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpxor(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpxor as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpxorEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpxor(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpxor as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX+
    }
}

impl VpxordEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpxord(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpxord as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpxordEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpxord(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpxord as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpxordEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpxord(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpxord as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpxordEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpxord(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpxord as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpxordEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpxord(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpxord as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpxordEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpxord(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpxord as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpxordEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpxord(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpxord as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpxordEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpxord(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpxord as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VpxorqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpxorq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpxorq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpxorqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpxorq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpxorq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpxorqEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpxorq(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpxorq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpxorqEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vpxorq(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vpxorq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpxorqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpxorq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpxorq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpxorqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpxorq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpxorq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpxorqEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpxorq(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpxorq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VpxorqEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vpxorq(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vpxorq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VrangepdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vrangepd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vrangepd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U3: Into<Imm>> VrangepdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vrangepd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vrangepd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U3: Into<Imm>> VrangepdEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vrangepd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vrangepd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U3: Into<Imm>> VrangepdEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vrangepd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vrangepd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VrangepdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vrangepd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vrangepd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U3: Into<Imm>> VrangepdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vrangepd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vrangepd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U3: Into<Imm>> VrangepdEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vrangepd(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vrangepd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U3: Into<Imm>> VrangepdEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vrangepd(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vrangepd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VrangepsEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vrangeps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vrangeps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U3: Into<Imm>> VrangepsEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vrangeps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vrangeps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U3: Into<Imm>> VrangepsEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vrangeps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vrangeps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U3: Into<Imm>> VrangepsEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vrangeps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vrangeps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl VrangepsEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vrangeps(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vrangeps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U3: Into<Imm>> VrangepsEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vrangeps(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vrangeps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U3: Into<Imm>> VrangepsEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vrangeps(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vrangeps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U3: Into<Imm>> VrangepsEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vrangeps(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vrangeps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl VrangesdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vrangesd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vrangesd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_DQ{kz|sae}
    }
}

impl<U3: Into<Imm>> VrangesdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vrangesd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vrangesd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz|sae}
    }
}

impl VrangesdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vrangesd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vrangesd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_DQ{kz|sae}
    }
}

impl<U3: Into<Imm>> VrangesdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vrangesd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vrangesd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz|sae}
    }
}

impl VrangessEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vrangess(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vrangess as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_DQ{kz|sae}
    }
}

impl<U3: Into<Imm>> VrangessEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vrangess(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vrangess as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz|sae}
    }
}

impl VrangessEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vrangess(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vrangess as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_DQ{kz|sae}
    }
}

impl<U3: Into<Imm>> VrangessEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vrangess(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vrangess as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz|sae}
    }
}

impl Vrcp14pdEmitter<Vec, Vec> for Assembler<'_> {
    fn vrcp14pd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vrcp14pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrcp14pdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vrcp14pd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vrcp14pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrcp14pdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vrcp14pd(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vrcp14pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrcp14pdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vrcp14pd(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vrcp14pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrcp14pdEmitter<Vec, Mem> for Assembler<'_> {
    fn vrcp14pd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vrcp14pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrcp14pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vrcp14pd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vrcp14pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrcp14pdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vrcp14pd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vrcp14pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrcp14pdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vrcp14pd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vrcp14pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrcp14psEmitter<Vec, Vec> for Assembler<'_> {
    fn vrcp14ps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vrcp14ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrcp14psEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vrcp14ps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vrcp14ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrcp14psEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vrcp14ps(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vrcp14ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrcp14psEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vrcp14ps(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vrcp14ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrcp14psEmitter<Vec, Mem> for Assembler<'_> {
    fn vrcp14ps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vrcp14ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrcp14psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vrcp14ps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vrcp14ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrcp14psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vrcp14ps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vrcp14ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrcp14psEmitter<Zmm, Mem> for Assembler<'_> {
    fn vrcp14ps(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vrcp14ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrcp14sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vrcp14sd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vrcp14sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrcp14sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vrcp14sd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vrcp14sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrcp14sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vrcp14sd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vrcp14sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrcp14sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vrcp14sd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vrcp14sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrcp14ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vrcp14ss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vrcp14ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrcp14ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vrcp14ss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vrcp14ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrcp14ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vrcp14ss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vrcp14ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrcp14ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vrcp14ss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vrcp14ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VrcppsEmitter<Vec, Vec> for Assembler<'_> {
    fn vrcpps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Vrcpps as u32, &[op0.as_operand(), op1.as_operand()]); // AVX
    }
}

impl VrcppsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vrcpps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Vrcpps as u32, &[op0.as_operand(), op1.as_operand()]); // AVX
    }
}

impl VrcppsEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vrcpps(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(InstId::Vrcpps as u32, &[op0.as_operand(), op1.as_operand()]); // AVX
    }
}

impl VrcppsEmitter<Vec, Mem> for Assembler<'_> {
    fn vrcpps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Vrcpps as u32, &[op0.as_operand(), op1.as_operand()]); // AVX
    }
}

impl VrcppsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vrcpps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Vrcpps as u32, &[op0.as_operand(), op1.as_operand()]); // AVX
    }
}

impl VrcppsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vrcpps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(InstId::Vrcpps as u32, &[op0.as_operand(), op1.as_operand()]); // AVX
    }
}

impl VrcpssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vrcpss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vrcpss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VrcpssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vrcpss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vrcpss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VrcpssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vrcpss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vrcpss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VrcpssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vrcpss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vrcpss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VreducepdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vreducepd(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vreducepd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U2: Into<Imm>> VreducepdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vreducepd(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vreducepd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U2: Into<Imm>> VreducepdEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vreducepd(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vreducepd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U2: Into<Imm>> VreducepdEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vreducepd(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vreducepd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VreducepdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vreducepd(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vreducepd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U2: Into<Imm>> VreducepdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vreducepd(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vreducepd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U2: Into<Imm>> VreducepdEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vreducepd(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vreducepd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl<U2: Into<Imm>> VreducepdEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vreducepd(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vreducepd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b64}
    }
}

impl VreducepsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vreduceps(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vreduceps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U2: Into<Imm>> VreducepsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vreduceps(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vreduceps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U2: Into<Imm>> VreducepsEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vreduceps(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vreduceps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U2: Into<Imm>> VreducepsEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vreduceps(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vreduceps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl VreducepsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vreduceps(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vreduceps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U2: Into<Imm>> VreducepsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vreduceps(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vreduceps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U2: Into<Imm>> VreducepsEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vreduceps(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vreduceps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl<U2: Into<Imm>> VreducepsEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vreduceps(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vreduceps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_DQ{kz|b32}
    }
}

impl VreducesdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vreducesd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vreducesd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> VreducesdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vreducesd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vreducesd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl VreducesdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vreducesd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vreducesd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> VreducesdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vreducesd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vreducesd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl VreducessEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vreducess(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vreducess as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> VreducessEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vreducess(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vreducess as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl VreducessEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vreducess(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vreducess as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl<U3: Into<Imm>> VreducessEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vreducess(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vreducess as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_DQ{kz}
    }
}

impl VrndscalepdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vrndscalepd(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vrndscalepd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VrndscalepdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vrndscalepd(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vrndscalepd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VrndscalepdEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vrndscalepd(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vrndscalepd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VrndscalepdEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vrndscalepd(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vrndscalepd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VrndscalepdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vrndscalepd(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vrndscalepd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VrndscalepdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vrndscalepd(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vrndscalepd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VrndscalepdEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vrndscalepd(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vrndscalepd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U2: Into<Imm>> VrndscalepdEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vrndscalepd(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vrndscalepd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VrndscalepsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vrndscaleps(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vrndscaleps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VrndscalepsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vrndscaleps(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vrndscaleps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VrndscalepsEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vrndscaleps(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vrndscaleps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VrndscalepsEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vrndscaleps(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vrndscaleps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VrndscalepsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vrndscaleps(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vrndscaleps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VrndscalepsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vrndscaleps(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vrndscaleps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VrndscalepsEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vrndscaleps(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vrndscaleps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U2: Into<Imm>> VrndscalepsEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vrndscaleps(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vrndscaleps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl VrndscalesdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vrndscalesd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vrndscalesd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VrndscalesdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vrndscalesd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vrndscalesd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VrndscalesdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vrndscalesd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vrndscalesd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VrndscalesdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vrndscalesd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vrndscalesd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VrndscalessEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vrndscaless(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vrndscaless as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VrndscalessEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vrndscaless(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vrndscaless as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VrndscalessEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vrndscaless(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vrndscaless as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl<U3: Into<Imm>> VrndscalessEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vrndscaless(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vrndscaless as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|sae}
    }
}

impl VroundpdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vroundpd(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vroundpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl<U2: Into<Imm>> VroundpdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vroundpd(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vroundpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX
    }
}

impl<U2: Into<Imm>> VroundpdEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vroundpd(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vroundpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX
    }
}

impl VroundpdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vroundpd(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vroundpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl<U2: Into<Imm>> VroundpdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vroundpd(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vroundpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX
    }
}

impl<U2: Into<Imm>> VroundpdEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vroundpd(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vroundpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX
    }
}

impl VroundpsEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vroundps(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vroundps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl<U2: Into<Imm>> VroundpsEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vroundps(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vroundps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX
    }
}

impl<U2: Into<Imm>> VroundpsEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vroundps(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vroundps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX
    }
}

impl VroundpsEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vroundps(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vroundps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl<U2: Into<Imm>> VroundpsEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vroundps(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vroundps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX
    }
}

impl<U2: Into<Imm>> VroundpsEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vroundps(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vroundps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // AVX
    }
}

impl VroundsdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vroundsd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vroundsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VroundsdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vroundsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vroundsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl VroundsdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vroundsd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vroundsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VroundsdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vroundsd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vroundsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl VroundssEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vroundss(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vroundss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VroundssEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vroundss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vroundss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl VroundssEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vroundss(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vroundss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX
    }
}

impl<U3: Into<Imm>> VroundssEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vroundss(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vroundss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX
    }
}

impl Vrsqrt14pdEmitter<Vec, Vec> for Assembler<'_> {
    fn vrsqrt14pd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vrsqrt14pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrsqrt14pdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vrsqrt14pd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vrsqrt14pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrsqrt14pdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vrsqrt14pd(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vrsqrt14pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrsqrt14pdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vrsqrt14pd(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vrsqrt14pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrsqrt14pdEmitter<Vec, Mem> for Assembler<'_> {
    fn vrsqrt14pd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vrsqrt14pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrsqrt14pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vrsqrt14pd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vrsqrt14pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrsqrt14pdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vrsqrt14pd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vrsqrt14pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrsqrt14pdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vrsqrt14pd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vrsqrt14pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl Vrsqrt14psEmitter<Vec, Vec> for Assembler<'_> {
    fn vrsqrt14ps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vrsqrt14ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrsqrt14psEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vrsqrt14ps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vrsqrt14ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrsqrt14psEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vrsqrt14ps(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vrsqrt14ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrsqrt14psEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vrsqrt14ps(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vrsqrt14ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrsqrt14psEmitter<Vec, Mem> for Assembler<'_> {
    fn vrsqrt14ps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vrsqrt14ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrsqrt14psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vrsqrt14ps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vrsqrt14ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrsqrt14psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vrsqrt14ps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vrsqrt14ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrsqrt14psEmitter<Zmm, Mem> for Assembler<'_> {
    fn vrsqrt14ps(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vrsqrt14ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl Vrsqrt14sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vrsqrt14sd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vrsqrt14sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrsqrt14sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vrsqrt14sd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vrsqrt14sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrsqrt14sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vrsqrt14sd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vrsqrt14sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrsqrt14sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vrsqrt14sd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vrsqrt14sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrsqrt14ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vrsqrt14ss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vrsqrt14ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrsqrt14ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vrsqrt14ss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vrsqrt14ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrsqrt14ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vrsqrt14ss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vrsqrt14ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl Vrsqrt14ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vrsqrt14ss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vrsqrt14ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz}
    }
}

impl VrsqrtpsEmitter<Vec, Vec> for Assembler<'_> {
    fn vrsqrtps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vrsqrtps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VrsqrtpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vrsqrtps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vrsqrtps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VrsqrtpsEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vrsqrtps(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vrsqrtps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VrsqrtpsEmitter<Vec, Mem> for Assembler<'_> {
    fn vrsqrtps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vrsqrtps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VrsqrtpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vrsqrtps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vrsqrtps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VrsqrtpsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vrsqrtps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vrsqrtps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VrsqrtssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vrsqrtss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vrsqrtss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VrsqrtssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vrsqrtss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vrsqrtss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VrsqrtssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vrsqrtss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vrsqrtss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VrsqrtssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vrsqrtss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vrsqrtss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX
    }
}

impl VscalefpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vscalefpd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vscalefpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VscalefpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vscalefpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vscalefpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VscalefpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vscalefpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vscalefpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VscalefpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vscalefpd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vscalefpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VscalefpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vscalefpd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vscalefpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VscalefpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vscalefpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vscalefpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VscalefpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vscalefpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vscalefpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VscalefpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vscalefpd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vscalefpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b64}
    }
}

impl VscalefpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vscalefps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vscalefps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VscalefpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vscalefps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vscalefps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VscalefpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vscalefps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vscalefps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VscalefpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vscalefps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vscalefps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VscalefpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vscalefps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vscalefps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VscalefpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vscalefps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vscalefps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VscalefpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vscalefps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vscalefps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VscalefpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vscalefps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vscalefps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|b32}
    }
}

impl VscalefsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vscalefsd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vscalefsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|er}
    }
}

impl VscalefsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vscalefsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vscalefsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|er}
    }
}

impl VscalefsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vscalefsd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vscalefsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|er}
    }
}

impl VscalefsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vscalefsd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vscalefsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|er}
    }
}

impl VscalefssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vscalefss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vscalefss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|er}
    }
}

impl VscalefssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vscalefss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vscalefss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|er}
    }
}

impl VscalefssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vscalefss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vscalefss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|er}
    }
}

impl VscalefssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vscalefss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vscalefss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX512_F{kz|er}
    }
}

impl VscatterdpdEmitter<Mem, Vec> for Assembler<'_> {
    fn vscatterdpd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vscatterdpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterdpdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vscatterdpd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vscatterdpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterdpdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vscatterdpd(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vscatterdpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterdpdEmitter<Mem, Zmm> for Assembler<'_> {
    fn vscatterdpd(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vscatterdpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterdpsEmitter<Mem, Vec> for Assembler<'_> {
    fn vscatterdps(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vscatterdps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterdpsEmitter<Mem, Xmm> for Assembler<'_> {
    fn vscatterdps(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vscatterdps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterdpsEmitter<Mem, Ymm> for Assembler<'_> {
    fn vscatterdps(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vscatterdps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterdpsEmitter<Mem, Zmm> for Assembler<'_> {
    fn vscatterdps(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vscatterdps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterqpdEmitter<Mem, Vec> for Assembler<'_> {
    fn vscatterqpd(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vscatterqpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterqpdEmitter<Mem, Xmm> for Assembler<'_> {
    fn vscatterqpd(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vscatterqpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterqpdEmitter<Mem, Ymm> for Assembler<'_> {
    fn vscatterqpd(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vscatterqpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterqpdEmitter<Mem, Zmm> for Assembler<'_> {
    fn vscatterqpd(&mut self, op0: Mem, op1: Zmm) {
        self.emit_n(
            InstId::Vscatterqpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterqpsEmitter<Mem, Vec> for Assembler<'_> {
    fn vscatterqps(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(
            InstId::Vscatterqps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterqpsEmitter<Mem, Xmm> for Assembler<'_> {
    fn vscatterqps(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(
            InstId::Vscatterqps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl VscatterqpsEmitter<Mem, Ymm> for Assembler<'_> {
    fn vscatterqps(&mut self, op0: Mem, op1: Ymm) {
        self.emit_n(
            InstId::Vscatterqps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX512_F{k}
    }
}

impl Vshuff32x4Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vshuff32x4(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vshuff32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> Vshuff32x4Emitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vshuff32x4(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vshuff32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> Vshuff32x4Emitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vshuff32x4(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vshuff32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl Vshuff32x4Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vshuff32x4(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vshuff32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> Vshuff32x4Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vshuff32x4(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vshuff32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> Vshuff32x4Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vshuff32x4(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vshuff32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl Vshuff64x2Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vshuff64x2(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vshuff64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> Vshuff64x2Emitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vshuff64x2(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vshuff64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> Vshuff64x2Emitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vshuff64x2(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vshuff64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl Vshuff64x2Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vshuff64x2(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vshuff64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> Vshuff64x2Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vshuff64x2(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vshuff64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> Vshuff64x2Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vshuff64x2(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vshuff64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl Vshufi32x4Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vshufi32x4(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vshufi32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> Vshufi32x4Emitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vshufi32x4(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vshufi32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> Vshufi32x4Emitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vshufi32x4(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vshufi32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl Vshufi32x4Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vshufi32x4(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vshufi32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> Vshufi32x4Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vshufi32x4(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vshufi32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> Vshufi32x4Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vshufi32x4(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vshufi32x4 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b32}
    }
}

impl Vshufi64x2Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vshufi64x2(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vshufi64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> Vshufi64x2Emitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vshufi64x2(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vshufi64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> Vshufi64x2Emitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vshufi64x2(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vshufi64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl Vshufi64x2Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vshufi64x2(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vshufi64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> Vshufi64x2Emitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vshufi64x2(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vshufi64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> Vshufi64x2Emitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vshufi64x2(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vshufi64x2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX512_F{kz|b64}
    }
}

impl VshufpdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vshufpd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vshufpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VshufpdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vshufpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vshufpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VshufpdEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vshufpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vshufpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VshufpdEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vshufpd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vshufpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VshufpdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vshufpd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vshufpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VshufpdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vshufpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vshufpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VshufpdEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vshufpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vshufpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl<U3: Into<Imm>> VshufpdEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vshufpd(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vshufpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VshufpsEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vshufps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vshufps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VshufpsEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vshufps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vshufps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VshufpsEmitter<Ymm, Ymm, Ymm, U3> for Assembler<'_> {
    fn vshufps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vshufps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VshufpsEmitter<Zmm, Zmm, Zmm, U3> for Assembler<'_> {
    fn vshufps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vshufps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VshufpsEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vshufps(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vshufps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VshufpsEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vshufps(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vshufps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VshufpsEmitter<Ymm, Ymm, Mem, U3> for Assembler<'_> {
    fn vshufps(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vshufps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl<U3: Into<Imm>> VshufpsEmitter<Zmm, Zmm, Mem, U3> for Assembler<'_> {
    fn vshufps(&mut self, op0: Zmm, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vshufps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsqrtpdEmitter<Vec, Vec> for Assembler<'_> {
    fn vsqrtpd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vsqrtpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsqrtpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vsqrtpd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vsqrtpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsqrtpdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vsqrtpd(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vsqrtpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsqrtpdEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vsqrtpd(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vsqrtpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsqrtpdEmitter<Vec, Mem> for Assembler<'_> {
    fn vsqrtpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vsqrtpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsqrtpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vsqrtpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vsqrtpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsqrtpdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vsqrtpd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vsqrtpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsqrtpdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vsqrtpd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vsqrtpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsqrtpsEmitter<Vec, Vec> for Assembler<'_> {
    fn vsqrtps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vsqrtps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsqrtpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vsqrtps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vsqrtps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsqrtpsEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vsqrtps(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vsqrtps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsqrtpsEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vsqrtps(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vsqrtps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsqrtpsEmitter<Vec, Mem> for Assembler<'_> {
    fn vsqrtps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vsqrtps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsqrtpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vsqrtps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vsqrtps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsqrtpsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vsqrtps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vsqrtps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsqrtpsEmitter<Zmm, Mem> for Assembler<'_> {
    fn vsqrtps(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vsqrtps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsqrtsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsqrtsd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vsqrtsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsqrtsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsqrtsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vsqrtsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsqrtsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsqrtsd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vsqrtsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsqrtsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsqrtsd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vsqrtsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsqrtssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsqrtss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vsqrtss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsqrtssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsqrtss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vsqrtss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsqrtssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsqrtss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vsqrtss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsqrtssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsqrtss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vsqrtss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VstmxcsrEmitter<Mem> for Assembler<'_> {
    fn vstmxcsr(&mut self, op0: Mem) {
        self.emit_n(InstId::Vstmxcsr as u32, &[op0.as_operand()]); // AVX
    }
}

impl VsubpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsubpd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vsubpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsubpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsubpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vsubpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsubpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vsubpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vsubpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsubpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vsubpd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vsubpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsubpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsubpd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vsubpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsubpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsubpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vsubpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsubpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vsubpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vsubpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsubpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vsubpd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vsubpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VsubpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsubps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vsubps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsubpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsubps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vsubps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsubpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vsubps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vsubps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsubpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vsubps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vsubps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsubpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsubps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vsubps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsubpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsubps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vsubps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsubpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vsubps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vsubps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsubpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vsubps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vsubps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VsubsdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsubsd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vsubsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsubsdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsubsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vsubsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsubsdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsubsd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vsubsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsubsdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsubsd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vsubsd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsubssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsubss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vsubss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsubssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsubss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vsubss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsubssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsubss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vsubss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VsubssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsubss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vsubss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|er}
    }
}

impl VtestpdEmitter<Vec, Vec> for Assembler<'_> {
    fn vtestpd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vtestpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VtestpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vtestpd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vtestpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VtestpdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vtestpd(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vtestpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VtestpdEmitter<Vec, Mem> for Assembler<'_> {
    fn vtestpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vtestpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VtestpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vtestpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vtestpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VtestpdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vtestpd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vtestpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VtestpsEmitter<Vec, Vec> for Assembler<'_> {
    fn vtestps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vtestps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VtestpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vtestps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vtestps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VtestpsEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vtestps(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vtestps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VtestpsEmitter<Vec, Mem> for Assembler<'_> {
    fn vtestps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vtestps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VtestpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vtestps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vtestps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VtestpsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vtestps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vtestps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX
    }
}

impl VucomisdEmitter<Vec, Vec> for Assembler<'_> {
    fn vucomisd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vucomisd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VucomisdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vucomisd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vucomisd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VucomisdEmitter<Vec, Mem> for Assembler<'_> {
    fn vucomisd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vucomisd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VucomisdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vucomisd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vucomisd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VucomissEmitter<Vec, Vec> for Assembler<'_> {
    fn vucomiss(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vucomiss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VucomissEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vucomiss(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vucomiss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VucomissEmitter<Vec, Mem> for Assembler<'_> {
    fn vucomiss(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vucomiss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VucomissEmitter<Xmm, Mem> for Assembler<'_> {
    fn vucomiss(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vucomiss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX AVX512_F{sae}
    }
}

impl VunpckhpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vunpckhpd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vunpckhpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpckhpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vunpckhpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vunpckhpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpckhpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vunpckhpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vunpckhpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpckhpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vunpckhpd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vunpckhpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpckhpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vunpckhpd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vunpckhpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpckhpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vunpckhpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vunpckhpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpckhpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vunpckhpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vunpckhpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpckhpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vunpckhpd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vunpckhpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpckhpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vunpckhps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vunpckhps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpckhpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vunpckhps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vunpckhps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpckhpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vunpckhps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vunpckhps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpckhpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vunpckhps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vunpckhps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpckhpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vunpckhps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vunpckhps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpckhpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vunpckhps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vunpckhps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpckhpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vunpckhps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vunpckhps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpckhpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vunpckhps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vunpckhps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpcklpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vunpcklpd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vunpcklpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpcklpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vunpcklpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vunpcklpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpcklpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vunpcklpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vunpcklpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpcklpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vunpcklpd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vunpcklpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpcklpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vunpcklpd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vunpcklpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpcklpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vunpcklpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vunpcklpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpcklpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vunpcklpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vunpcklpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpcklpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vunpcklpd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vunpcklpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b64}
    }
}

impl VunpcklpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vunpcklps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vunpcklps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpcklpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vunpcklps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vunpcklps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpcklpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vunpcklps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vunpcklps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpcklpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vunpcklps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vunpcklps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpcklpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vunpcklps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vunpcklps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpcklpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vunpcklps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vunpcklps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpcklpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vunpcklps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vunpcklps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VunpcklpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vunpcklps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vunpcklps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_F{kz|b32}
    }
}

impl VxorpdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vxorpd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vxorpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VxorpdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vxorpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vxorpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VxorpdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vxorpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vxorpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VxorpdEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vxorpd(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vxorpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VxorpdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vxorpd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vxorpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VxorpdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vxorpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vxorpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VxorpdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vxorpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vxorpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VxorpdEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vxorpd(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vxorpd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b64}
    }
}

impl VxorpsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vxorps(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vxorps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VxorpsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vxorps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vxorps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VxorpsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vxorps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vxorps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VxorpsEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vxorps(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vxorps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VxorpsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vxorps(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vxorps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VxorpsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vxorps(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vxorps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VxorpsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vxorps(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vxorps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VxorpsEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vxorps(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vxorps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX AVX512_DQ{kz|b32}
    }
}

impl VzeroallEmitter for Assembler<'_> {
    fn vzeroall(&mut self) {
        self.emit_n(InstId::Vzeroall as u32, &[]); // AVX
    }
}

impl VzeroupperEmitter for Assembler<'_> {
    fn vzeroupper(&mut self) {
        self.emit_n(InstId::Vzeroupper as u32, &[]); // AVX
    }
}

impl VfmaddpdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddpd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vfmaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vfmaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpdEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Ymm) {
        self.emit_n(
            InstId::Vfmaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmaddpd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vfmaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmaddpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vfmaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpdEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfmaddpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: Ymm) {
        self.emit_n(
            InstId::Vfmaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpdEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddpd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vfmaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpdEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vfmaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpdEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Mem) {
        self.emit_n(
            InstId::Vfmaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpsEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vfmaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpsEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vfmaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpsEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Ymm) {
        self.emit_n(
            InstId::Vfmaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpsEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmaddps(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vfmaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpsEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmaddps(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vfmaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpsEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfmaddps(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: Ymm) {
        self.emit_n(
            InstId::Vfmaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpsEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vfmaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpsEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vfmaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddpsEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Mem) {
        self.emit_n(
            InstId::Vfmaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vfmaddsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vfmaddsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmaddsd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vfmaddsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmaddsd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vfmaddsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsdEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vfmaddsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsdEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vfmaddsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddssEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddss(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vfmaddss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddssEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vfmaddss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddssEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmaddss(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vfmaddss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddssEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmaddss(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vfmaddss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddssEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddss(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vfmaddss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddssEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vfmaddss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsubpd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vfmaddsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsubpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vfmaddsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpdEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsubpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Ymm) {
        self.emit_n(
            InstId::Vfmaddsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmaddsubpd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vfmaddsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmaddsubpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vfmaddsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpdEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfmaddsubpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: Ymm) {
        self.emit_n(
            InstId::Vfmaddsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpdEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsubpd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vfmaddsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpdEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsubpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vfmaddsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpdEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsubpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Mem) {
        self.emit_n(
            InstId::Vfmaddsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpsEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsubps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vfmaddsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpsEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsubps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vfmaddsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpsEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsubps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Ymm) {
        self.emit_n(
            InstId::Vfmaddsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpsEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmaddsubps(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vfmaddsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpsEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmaddsubps(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vfmaddsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpsEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfmaddsubps(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: Ymm) {
        self.emit_n(
            InstId::Vfmaddsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpsEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsubps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vfmaddsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpsEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsubps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vfmaddsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmaddsubpsEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsubps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Mem) {
        self.emit_n(
            InstId::Vfmaddsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubaddpd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vfmsubaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubaddpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vfmsubaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpdEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubaddpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Ymm) {
        self.emit_n(
            InstId::Vfmsubaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmsubaddpd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vfmsubaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmsubaddpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vfmsubaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpdEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfmsubaddpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: Ymm) {
        self.emit_n(
            InstId::Vfmsubaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpdEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubaddpd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vfmsubaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpdEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubaddpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vfmsubaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpdEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubaddpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Mem) {
        self.emit_n(
            InstId::Vfmsubaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpsEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubaddps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vfmsubaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpsEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubaddps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vfmsubaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpsEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubaddps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Ymm) {
        self.emit_n(
            InstId::Vfmsubaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpsEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmsubaddps(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vfmsubaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpsEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmsubaddps(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vfmsubaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpsEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfmsubaddps(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: Ymm) {
        self.emit_n(
            InstId::Vfmsubaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpsEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubaddps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vfmsubaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpsEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubaddps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vfmsubaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubaddpsEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubaddps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Mem) {
        self.emit_n(
            InstId::Vfmsubaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubpd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vfmsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vfmsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpdEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Ymm) {
        self.emit_n(
            InstId::Vfmsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmsubpd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vfmsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmsubpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vfmsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpdEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfmsubpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: Ymm) {
        self.emit_n(
            InstId::Vfmsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpdEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubpd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vfmsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpdEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vfmsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpdEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Mem) {
        self.emit_n(
            InstId::Vfmsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpsEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vfmsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpsEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vfmsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpsEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Ymm) {
        self.emit_n(
            InstId::Vfmsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpsEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmsubps(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vfmsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpsEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmsubps(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vfmsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpsEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfmsubps(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: Ymm) {
        self.emit_n(
            InstId::Vfmsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpsEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vfmsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpsEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vfmsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubpsEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Mem) {
        self.emit_n(
            InstId::Vfmsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubsdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubsd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vfmsubsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubsdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vfmsubsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubsdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmsubsd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vfmsubsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubsdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmsubsd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vfmsubsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubsdEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubsd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vfmsubsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubsdEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vfmsubsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubssEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubss(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vfmsubss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubssEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vfmsubss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubssEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfmsubss(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vfmsubss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubssEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfmsubss(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vfmsubss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubssEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubss(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vfmsubss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfmsubssEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vfmsubss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmaddpd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vfnmaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmaddpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vfnmaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpdEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmaddpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Ymm) {
        self.emit_n(
            InstId::Vfnmaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfnmaddpd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vfnmaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfnmaddpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vfnmaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpdEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfnmaddpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: Ymm) {
        self.emit_n(
            InstId::Vfnmaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpdEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmaddpd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vfnmaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpdEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmaddpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vfnmaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpdEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmaddpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Mem) {
        self.emit_n(
            InstId::Vfnmaddpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpsEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmaddps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vfnmaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpsEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmaddps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vfnmaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpsEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmaddps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Ymm) {
        self.emit_n(
            InstId::Vfnmaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpsEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfnmaddps(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vfnmaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpsEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfnmaddps(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vfnmaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpsEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfnmaddps(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: Ymm) {
        self.emit_n(
            InstId::Vfnmaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpsEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmaddps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vfnmaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpsEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmaddps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vfnmaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddpsEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmaddps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Mem) {
        self.emit_n(
            InstId::Vfnmaddps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddsdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmaddsd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vfnmaddsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddsdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmaddsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vfnmaddsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddsdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfnmaddsd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vfnmaddsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddsdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfnmaddsd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vfnmaddsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddsdEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmaddsd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vfnmaddsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddsdEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmaddsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vfnmaddsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddssEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmaddss(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vfnmaddss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddssEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmaddss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vfnmaddss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddssEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfnmaddss(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vfnmaddss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddssEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfnmaddss(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vfnmaddss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddssEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmaddss(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vfnmaddss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmaddssEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmaddss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vfnmaddss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsubpd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vfnmsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsubpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vfnmsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpdEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsubpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Ymm) {
        self.emit_n(
            InstId::Vfnmsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfnmsubpd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vfnmsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfnmsubpd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vfnmsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpdEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfnmsubpd(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: Ymm) {
        self.emit_n(
            InstId::Vfnmsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpdEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsubpd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vfnmsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpdEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsubpd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vfnmsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpdEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsubpd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Mem) {
        self.emit_n(
            InstId::Vfnmsubpd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpsEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsubps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vfnmsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpsEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsubps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vfnmsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpsEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsubps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Ymm) {
        self.emit_n(
            InstId::Vfnmsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpsEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfnmsubps(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vfnmsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpsEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfnmsubps(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vfnmsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpsEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vfnmsubps(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: Ymm) {
        self.emit_n(
            InstId::Vfnmsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpsEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsubps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vfnmsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpsEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsubps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vfnmsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubpsEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsubps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Mem) {
        self.emit_n(
            InstId::Vfnmsubps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubsdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsubsd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vfnmsubsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubsdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsubsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vfnmsubsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubsdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfnmsubsd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vfnmsubsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubsdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfnmsubsd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vfnmsubsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubsdEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsubsd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vfnmsubsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubsdEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsubsd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vfnmsubsd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubssEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsubss(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vfnmsubss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubssEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsubss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vfnmsubss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubssEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vfnmsubss(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vfnmsubss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubssEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vfnmsubss(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vfnmsubss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubssEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsubss(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vfnmsubss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfnmsubssEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsubss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vfnmsubss as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // FMA4
    }
}

impl VfrczpdEmitter<Vec, Vec> for Assembler<'_> {
    fn vfrczpd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vfrczpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VfrczpdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vfrczpd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vfrczpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VfrczpdEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vfrczpd(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vfrczpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VfrczpdEmitter<Vec, Mem> for Assembler<'_> {
    fn vfrczpd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vfrczpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VfrczpdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vfrczpd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vfrczpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VfrczpdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vfrczpd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vfrczpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VfrczpsEmitter<Vec, Vec> for Assembler<'_> {
    fn vfrczps(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vfrczps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VfrczpsEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vfrczps(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vfrczps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VfrczpsEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vfrczps(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vfrczps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VfrczpsEmitter<Vec, Mem> for Assembler<'_> {
    fn vfrczps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vfrczps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VfrczpsEmitter<Xmm, Mem> for Assembler<'_> {
    fn vfrczps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vfrczps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VfrczpsEmitter<Ymm, Mem> for Assembler<'_> {
    fn vfrczps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vfrczps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VfrczsdEmitter<Vec, Vec> for Assembler<'_> {
    fn vfrczsd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vfrczsd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VfrczsdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vfrczsd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vfrczsd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VfrczsdEmitter<Vec, Mem> for Assembler<'_> {
    fn vfrczsd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vfrczsd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VfrczsdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vfrczsd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vfrczsd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VfrczssEmitter<Vec, Vec> for Assembler<'_> {
    fn vfrczss(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vfrczss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VfrczssEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vfrczss(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vfrczss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VfrczssEmitter<Vec, Mem> for Assembler<'_> {
    fn vfrczss(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vfrczss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VfrczssEmitter<Xmm, Mem> for Assembler<'_> {
    fn vfrczss(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vfrczss as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VpcmovEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpcmov(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vpcmov as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpcmovEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpcmov(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vpcmov as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpcmovEmitter<Ymm, Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpcmov(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Ymm) {
        self.emit_n(
            InstId::Vpcmov as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpcmovEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpcmov(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vpcmov as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpcmovEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpcmov(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vpcmov as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpcmovEmitter<Ymm, Ymm, Mem, Ymm> for Assembler<'_> {
    fn vpcmov(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: Ymm) {
        self.emit_n(
            InstId::Vpcmov as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpcmovEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vpcmov(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vpcmov as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpcmovEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpcmov(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vpcmov as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpcmovEmitter<Ymm, Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpcmov(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Mem) {
        self.emit_n(
            InstId::Vpcmov as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpcombEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcomb(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpcomb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcombEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcomb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpcomb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcombEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcomb(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpcomb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcombEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcomb(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcomb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomdEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcomd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpcomd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomdEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcomd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpcomd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomdEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcomd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpcomd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomdEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcomd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcomd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomqEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcomq(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpcomq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomqEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcomq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpcomq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomqEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcomq(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpcomq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomqEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcomq(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcomq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomwEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcomw(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpcomw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomwEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcomw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpcomw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomwEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcomw(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpcomw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomwEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcomw(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcomw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomubEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcomub(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpcomub as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomubEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcomub(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpcomub as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomubEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcomub(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpcomub as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomubEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcomub(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcomub as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomudEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcomud(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpcomud as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomudEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcomud(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpcomud as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomudEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcomud(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpcomud as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomudEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcomud(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcomud as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomuqEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcomuq(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpcomuq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomuqEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcomuq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpcomuq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomuqEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcomuq(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpcomuq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomuqEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcomuq(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcomuq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomuwEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpcomuw(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vpcomuw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomuwEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vpcomuw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vpcomuw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // XOP
    }
}

impl VpcomuwEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpcomuw(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vpcomuw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl<U3: Into<Imm>> VpcomuwEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vpcomuw(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vpcomuw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        ); // XOP
    }
}

impl Vpermil2pdEmitter<Vec, Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpermil2pd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec, op4: Imm) {
        self.emit_n(
            InstId::Vpermil2pd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                op4.as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2pdEmitter<Xmm, Xmm, Xmm, Xmm, U4> for Assembler<'_> {
    fn vpermil2pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm, op4: U4) {
        self.emit_n(
            InstId::Vpermil2pd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                Into::<Imm>::into(op4).as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2pdEmitter<Ymm, Ymm, Ymm, Ymm, U4> for Assembler<'_> {
    fn vpermil2pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Ymm, op4: U4) {
        self.emit_n(
            InstId::Vpermil2pd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                Into::<Imm>::into(op4).as_operand(),
            ],
        ); // XOP
    }
}

impl Vpermil2pdEmitter<Vec, Vec, Mem, Vec, Imm> for Assembler<'_> {
    fn vpermil2pd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec, op4: Imm) {
        self.emit_n(
            InstId::Vpermil2pd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                op4.as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2pdEmitter<Xmm, Xmm, Mem, Xmm, U4> for Assembler<'_> {
    fn vpermil2pd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm, op4: U4) {
        self.emit_n(
            InstId::Vpermil2pd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                Into::<Imm>::into(op4).as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2pdEmitter<Ymm, Ymm, Mem, Ymm, U4> for Assembler<'_> {
    fn vpermil2pd(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: Ymm, op4: U4) {
        self.emit_n(
            InstId::Vpermil2pd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                Into::<Imm>::into(op4).as_operand(),
            ],
        ); // XOP
    }
}

impl Vpermil2pdEmitter<Vec, Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpermil2pd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem, op4: Imm) {
        self.emit_n(
            InstId::Vpermil2pd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                op4.as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2pdEmitter<Xmm, Xmm, Xmm, Mem, U4> for Assembler<'_> {
    fn vpermil2pd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem, op4: U4) {
        self.emit_n(
            InstId::Vpermil2pd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                Into::<Imm>::into(op4).as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2pdEmitter<Ymm, Ymm, Ymm, Mem, U4> for Assembler<'_> {
    fn vpermil2pd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Mem, op4: U4) {
        self.emit_n(
            InstId::Vpermil2pd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                Into::<Imm>::into(op4).as_operand(),
            ],
        ); // XOP
    }
}

impl Vpermil2psEmitter<Vec, Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vpermil2ps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec, op4: Imm) {
        self.emit_n(
            InstId::Vpermil2ps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                op4.as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2psEmitter<Xmm, Xmm, Xmm, Xmm, U4> for Assembler<'_> {
    fn vpermil2ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm, op4: U4) {
        self.emit_n(
            InstId::Vpermil2ps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                Into::<Imm>::into(op4).as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2psEmitter<Ymm, Ymm, Ymm, Ymm, U4> for Assembler<'_> {
    fn vpermil2ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Ymm, op4: U4) {
        self.emit_n(
            InstId::Vpermil2ps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                Into::<Imm>::into(op4).as_operand(),
            ],
        ); // XOP
    }
}

impl Vpermil2psEmitter<Vec, Vec, Mem, Vec, Imm> for Assembler<'_> {
    fn vpermil2ps(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec, op4: Imm) {
        self.emit_n(
            InstId::Vpermil2ps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                op4.as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2psEmitter<Xmm, Xmm, Mem, Xmm, U4> for Assembler<'_> {
    fn vpermil2ps(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm, op4: U4) {
        self.emit_n(
            InstId::Vpermil2ps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                Into::<Imm>::into(op4).as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2psEmitter<Ymm, Ymm, Mem, Ymm, U4> for Assembler<'_> {
    fn vpermil2ps(&mut self, op0: Ymm, op1: Ymm, op2: Mem, op3: Ymm, op4: U4) {
        self.emit_n(
            InstId::Vpermil2ps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                Into::<Imm>::into(op4).as_operand(),
            ],
        ); // XOP
    }
}

impl Vpermil2psEmitter<Vec, Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vpermil2ps(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem, op4: Imm) {
        self.emit_n(
            InstId::Vpermil2ps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                op4.as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2psEmitter<Xmm, Xmm, Xmm, Mem, U4> for Assembler<'_> {
    fn vpermil2ps(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem, op4: U4) {
        self.emit_n(
            InstId::Vpermil2ps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                Into::<Imm>::into(op4).as_operand(),
            ],
        ); // XOP
    }
}

impl<U4: Into<Imm>> Vpermil2psEmitter<Ymm, Ymm, Ymm, Mem, U4> for Assembler<'_> {
    fn vpermil2ps(&mut self, op0: Ymm, op1: Ymm, op2: Ymm, op3: Mem, op4: U4) {
        self.emit_n(
            InstId::Vpermil2ps as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
                Into::<Imm>::into(op4).as_operand(),
            ],
        ); // XOP
    }
}

impl VphaddbdEmitter<Vec, Vec> for Assembler<'_> {
    fn vphaddbd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vphaddbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddbdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphaddbd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vphaddbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddbdEmitter<Vec, Mem> for Assembler<'_> {
    fn vphaddbd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vphaddbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddbdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphaddbd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vphaddbd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddbqEmitter<Vec, Vec> for Assembler<'_> {
    fn vphaddbq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vphaddbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddbqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphaddbq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vphaddbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddbqEmitter<Vec, Mem> for Assembler<'_> {
    fn vphaddbq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vphaddbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddbqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphaddbq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vphaddbq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddbwEmitter<Vec, Vec> for Assembler<'_> {
    fn vphaddbw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vphaddbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddbwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphaddbw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vphaddbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddbwEmitter<Vec, Mem> for Assembler<'_> {
    fn vphaddbw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vphaddbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddbwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphaddbw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vphaddbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphadddqEmitter<Vec, Vec> for Assembler<'_> {
    fn vphadddq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vphadddq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphadddqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphadddq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vphadddq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphadddqEmitter<Vec, Mem> for Assembler<'_> {
    fn vphadddq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vphadddq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphadddqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphadddq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vphadddq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddwdEmitter<Vec, Vec> for Assembler<'_> {
    fn vphaddwd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vphaddwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddwdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphaddwd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vphaddwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddwdEmitter<Vec, Mem> for Assembler<'_> {
    fn vphaddwd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vphaddwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddwdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphaddwd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vphaddwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddwqEmitter<Vec, Vec> for Assembler<'_> {
    fn vphaddwq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vphaddwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddwqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphaddwq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vphaddwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddwqEmitter<Vec, Mem> for Assembler<'_> {
    fn vphaddwq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vphaddwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddwqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphaddwq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vphaddwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddubdEmitter<Vec, Vec> for Assembler<'_> {
    fn vphaddubd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vphaddubd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddubdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphaddubd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vphaddubd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddubdEmitter<Vec, Mem> for Assembler<'_> {
    fn vphaddubd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vphaddubd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddubdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphaddubd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vphaddubd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddubqEmitter<Vec, Vec> for Assembler<'_> {
    fn vphaddubq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vphaddubq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddubqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphaddubq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vphaddubq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddubqEmitter<Vec, Mem> for Assembler<'_> {
    fn vphaddubq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vphaddubq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddubqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphaddubq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vphaddubq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddubwEmitter<Vec, Vec> for Assembler<'_> {
    fn vphaddubw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vphaddubw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddubwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphaddubw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vphaddubw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddubwEmitter<Vec, Mem> for Assembler<'_> {
    fn vphaddubw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vphaddubw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddubwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphaddubw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vphaddubw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddudqEmitter<Vec, Vec> for Assembler<'_> {
    fn vphaddudq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vphaddudq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddudqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphaddudq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vphaddudq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddudqEmitter<Vec, Mem> for Assembler<'_> {
    fn vphaddudq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vphaddudq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphaddudqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphaddudq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vphaddudq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphadduwdEmitter<Vec, Vec> for Assembler<'_> {
    fn vphadduwd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vphadduwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphadduwdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphadduwd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vphadduwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphadduwdEmitter<Vec, Mem> for Assembler<'_> {
    fn vphadduwd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vphadduwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphadduwdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphadduwd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vphadduwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphadduwqEmitter<Vec, Vec> for Assembler<'_> {
    fn vphadduwq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vphadduwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphadduwqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphadduwq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vphadduwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphadduwqEmitter<Vec, Mem> for Assembler<'_> {
    fn vphadduwq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vphadduwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphadduwqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphadduwq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vphadduwq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphsubbwEmitter<Vec, Vec> for Assembler<'_> {
    fn vphsubbw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vphsubbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphsubbwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphsubbw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vphsubbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphsubbwEmitter<Vec, Mem> for Assembler<'_> {
    fn vphsubbw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vphsubbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphsubbwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphsubbw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vphsubbw as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphsubdqEmitter<Vec, Vec> for Assembler<'_> {
    fn vphsubdq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vphsubdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphsubdqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphsubdq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vphsubdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphsubdqEmitter<Vec, Mem> for Assembler<'_> {
    fn vphsubdq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vphsubdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphsubdqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphsubdq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vphsubdq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphsubwdEmitter<Vec, Vec> for Assembler<'_> {
    fn vphsubwd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vphsubwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphsubwdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vphsubwd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vphsubwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphsubwdEmitter<Vec, Mem> for Assembler<'_> {
    fn vphsubwd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vphsubwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VphsubwdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vphsubwd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vphsubwd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // XOP
    }
}

impl VpmacsddEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmacsdd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vpmacsdd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsddEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmacsdd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vpmacsdd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsddEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmacsdd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vpmacsdd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsddEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmacsdd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vpmacsdd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsdqhEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmacsdqh(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vpmacsdqh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsdqhEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmacsdqh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vpmacsdqh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsdqhEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmacsdqh(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vpmacsdqh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsdqhEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmacsdqh(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vpmacsdqh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsdqlEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmacsdql(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vpmacsdql as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsdqlEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmacsdql(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vpmacsdql as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsdqlEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmacsdql(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vpmacsdql as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsdqlEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmacsdql(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vpmacsdql as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacswdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmacswd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vpmacswd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacswdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmacswd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vpmacswd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacswdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmacswd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vpmacswd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacswdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmacswd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vpmacswd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacswwEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmacsww(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vpmacsww as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacswwEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmacsww(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vpmacsww as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacswwEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmacsww(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vpmacsww as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacswwEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmacsww(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vpmacsww as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssddEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmacssdd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vpmacssdd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssddEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmacssdd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vpmacssdd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssddEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmacssdd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vpmacssdd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssddEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmacssdd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vpmacssdd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssdqhEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmacssdqh(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vpmacssdqh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssdqhEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmacssdqh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vpmacssdqh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssdqhEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmacssdqh(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vpmacssdqh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssdqhEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmacssdqh(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vpmacssdqh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssdqlEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmacssdql(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vpmacssdql as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssdqlEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmacssdql(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vpmacssdql as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssdqlEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmacssdql(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vpmacssdql as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacssdqlEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmacssdql(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vpmacssdql as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsswdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmacsswd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vpmacsswd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsswdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmacsswd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vpmacsswd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsswdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmacsswd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vpmacsswd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsswdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmacsswd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vpmacsswd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsswwEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmacssww(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vpmacssww as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsswwEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmacssww(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vpmacssww as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsswwEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmacssww(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vpmacssww as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmacsswwEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmacssww(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vpmacssww as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmadcsswdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmadcsswd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vpmadcsswd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmadcsswdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmadcsswd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vpmadcsswd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmadcsswdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmadcsswd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vpmadcsswd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmadcsswdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmadcsswd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vpmadcsswd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmadcswdEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpmadcswd(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vpmadcswd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmadcswdEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpmadcswd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vpmadcswd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmadcswdEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpmadcswd(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vpmadcswd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VpmadcswdEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpmadcswd(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vpmadcswd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VppermEmitter<Vec, Vec, Vec, Vec> for Assembler<'_> {
    fn vpperm(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Vec) {
        self.emit_n(
            InstId::Vpperm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VppermEmitter<Xmm, Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpperm(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Xmm) {
        self.emit_n(
            InstId::Vpperm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VppermEmitter<Vec, Vec, Mem, Vec> for Assembler<'_> {
    fn vpperm(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Vec) {
        self.emit_n(
            InstId::Vpperm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VppermEmitter<Xmm, Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpperm(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: Xmm) {
        self.emit_n(
            InstId::Vpperm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VppermEmitter<Vec, Vec, Vec, Mem> for Assembler<'_> {
    fn vpperm(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Mem) {
        self.emit_n(
            InstId::Vpperm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VppermEmitter<Xmm, Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpperm(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: Mem) {
        self.emit_n(
            InstId::Vpperm as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        ); // XOP
    }
}

impl VprotbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vprotb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vprotb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vprotb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vprotb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotbEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vprotb(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Vprotb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotbEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vprotb(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Vprotb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vprotb(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vprotb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vprotb(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vprotb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotbEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vprotb(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vprotb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl<U2: Into<Imm>> VprotbEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vprotb(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vprotb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // XOP
    }
}

impl VprotbEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vprotb(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vprotb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl<U2: Into<Imm>> VprotbEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vprotb(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vprotb as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // XOP
    }
}

impl VprotdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vprotd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vprotd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vprotd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vprotd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotdEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vprotd(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Vprotd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotdEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vprotd(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Vprotd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vprotd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vprotd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vprotd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vprotd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotdEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vprotd(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vprotd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl<U2: Into<Imm>> VprotdEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vprotd(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vprotd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // XOP
    }
}

impl VprotdEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vprotd(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vprotd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl<U2: Into<Imm>> VprotdEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vprotd(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vprotd as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // XOP
    }
}

impl VprotqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vprotq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vprotq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vprotq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vprotq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotqEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vprotq(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Vprotq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotqEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vprotq(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Vprotq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vprotq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vprotq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vprotq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vprotq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotqEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vprotq(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vprotq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl<U2: Into<Imm>> VprotqEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vprotq(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vprotq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // XOP
    }
}

impl VprotqEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vprotq(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vprotq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl<U2: Into<Imm>> VprotqEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vprotq(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vprotq as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // XOP
    }
}

impl VprotwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vprotw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vprotw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vprotw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vprotw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotwEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vprotw(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Vprotw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotwEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vprotw(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Vprotw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vprotw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vprotw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vprotw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vprotw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VprotwEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vprotw(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vprotw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl<U2: Into<Imm>> VprotwEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vprotw(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vprotw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // XOP
    }
}

impl VprotwEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vprotw(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vprotw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl<U2: Into<Imm>> VprotwEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vprotw(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vprotw as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        ); // XOP
    }
}

impl VpshabEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshab(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpshab as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshabEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshab(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpshab as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshabEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpshab(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Vpshab as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshabEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpshab(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Vpshab as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshabEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshab(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpshab as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshabEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshab(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshab as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshadEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshad(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpshad as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshadEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshad(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpshad as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshadEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpshad(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Vpshad as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshadEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpshad(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Vpshad as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshadEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshad(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpshad as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshadEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshad(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshad as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshaqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshaq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpshaq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshaqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshaq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpshaq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshaqEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpshaq(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Vpshaq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshaqEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpshaq(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Vpshaq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshaqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshaq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpshaq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshaqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshaq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshaq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshawEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshaw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpshaw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshawEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshaw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpshaw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshawEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpshaw(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Vpshaw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshawEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpshaw(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Vpshaw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshawEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshaw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpshaw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshawEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshaw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshaw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshlbEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshlb(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpshlb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshlbEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshlb(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpshlb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshlbEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpshlb(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Vpshlb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshlbEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpshlb(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Vpshlb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshlbEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshlb(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpshlb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshlbEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshlb(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshlb as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshldEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshld(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpshld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshldEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshld(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpshld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshldEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpshld(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Vpshld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshldEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpshld(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Vpshld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshldEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshld(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpshld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshldEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshld(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshld as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshlqEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshlq(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpshlq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshlqEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshlq(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpshlq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshlqEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpshlq(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Vpshlq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshlqEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpshlq(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Vpshlq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshlqEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshlq(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpshlq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshlqEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshlq(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshlq as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshlwEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpshlw(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpshlw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshlwEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpshlw(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpshlw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshlwEmitter<Vec, Mem, Vec> for Assembler<'_> {
    fn vpshlw(&mut self, op0: Vec, op1: Mem, op2: Vec) {
        self.emit_n(
            InstId::Vpshlw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshlwEmitter<Xmm, Mem, Xmm> for Assembler<'_> {
    fn vpshlw(&mut self, op0: Xmm, op1: Mem, op2: Xmm) {
        self.emit_n(
            InstId::Vpshlw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshlwEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpshlw(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpshlw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl VpshlwEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpshlw(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpshlw as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // XOP
    }
}

impl Vbcstnebf162psEmitter<Vec, Mem> for Assembler<'_> {
    fn vbcstnebf162ps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vbcstnebf162ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vbcstnebf162psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vbcstnebf162ps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vbcstnebf162ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vbcstnebf162psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vbcstnebf162ps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vbcstnebf162ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vbcstnesh2psEmitter<Vec, Mem> for Assembler<'_> {
    fn vbcstnesh2ps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vbcstnesh2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vbcstnesh2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vbcstnesh2ps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vbcstnesh2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vbcstnesh2psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vbcstnesh2ps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vbcstnesh2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtneebf162psEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtneebf162ps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtneebf162ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtneebf162psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtneebf162ps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtneebf162ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtneebf162psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtneebf162ps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtneebf162ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtneeph2psEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtneeph2ps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtneeph2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtneeph2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtneeph2ps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtneeph2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtneeph2psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtneeph2ps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtneeph2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtneobf162psEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtneobf162ps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtneobf162ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtneobf162psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtneobf162ps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtneobf162ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtneobf162psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtneobf162ps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtneobf162ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtneoph2psEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtneoph2ps(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtneoph2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtneoph2psEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtneoph2ps(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtneoph2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtneoph2psEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtneoph2ps(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtneoph2ps as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VpdpbssdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpbssd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpdpbssd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbssdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpbssd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpdpbssd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbssdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpbssd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpdpbssd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbssdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpbssd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbssd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbssdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpbssd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbssd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbssdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpbssd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbssd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbssdsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpbssds(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpdpbssds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbssdsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpbssds(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpdpbssds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbssdsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpbssds(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpdpbssds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbssdsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpbssds(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbssds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbssdsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpbssds(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbssds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbssdsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpbssds(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbssds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbsudEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpbsud(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpdpbsud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbsudEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpbsud(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpdpbsud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbsudEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpbsud(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpdpbsud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbsudEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpbsud(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbsud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbsudEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpbsud(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbsud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbsudEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpbsud(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbsud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbsudsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpbsuds(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpdpbsuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbsudsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpbsuds(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpdpbsuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbsudsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpbsuds(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpdpbsuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbsudsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpbsuds(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbsuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbsudsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpbsuds(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbsuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbsudsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpbsuds(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbsuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbuudEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpbuud(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpdpbuud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbuudEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpbuud(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpdpbuud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbuudEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpbuud(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpdpbuud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbuudEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpbuud(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbuud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbuudEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpbuud(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbuud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbuudEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpbuud(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbuud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbuudsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpbuuds(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpdpbuuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbuudsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpbuuds(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpdpbuuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbuudsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpbuuds(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpdpbuuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbuudsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpbuuds(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbuuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbuudsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpbuuds(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbuuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpbuudsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpbuuds(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpbuuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwsudEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpwsud(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpdpwsud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwsudEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpwsud(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpdpwsud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwsudEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpwsud(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpdpwsud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwsudEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpwsud(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwsud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwsudEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpwsud(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwsud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwsudEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpwsud(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwsud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwsudsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpwsuds(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpdpwsuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwsudsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpwsuds(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpdpwsuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwsudsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpwsuds(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpdpwsuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwsudsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpwsuds(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwsuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwsudsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpwsuds(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwsuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwsudsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpwsuds(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwsuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwusdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpwusd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpdpwusd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwusdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpwusd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpdpwusd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwusdEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpwusd(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpdpwusd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwusdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpwusd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwusd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwusdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpwusd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwusd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwusdEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpwusd(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwusd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwusdsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpwusds(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpdpwusds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwusdsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpwusds(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpdpwusds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwusdsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpwusds(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpdpwusds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwusdsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpwusds(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwusds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwusdsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpwusds(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwusds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwusdsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpwusds(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwusds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwuudEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpwuud(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpdpwuud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwuudEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpwuud(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpdpwuud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwuudEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpwuud(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpdpwuud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwuudEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpwuud(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwuud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwuudEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpwuud(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwuud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwuudEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpwuud(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwuud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwuudsEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vpdpwuuds(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vpdpwuuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwuudsEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vpdpwuuds(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vpdpwuuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwuudsEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vpdpwuuds(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vpdpwuuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwuudsEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vpdpwuuds(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwuuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwuudsEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vpdpwuuds(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwuuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VpdpwuudsEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vpdpwuuds(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vpdpwuuds as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsha512msg1Emitter<Vec, Vec> for Assembler<'_> {
    fn vsha512msg1(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vsha512msg1 as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vsha512msg1Emitter<Ymm, Xmm> for Assembler<'_> {
    fn vsha512msg1(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vsha512msg1 as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vsha512msg2Emitter<Vec, Vec> for Assembler<'_> {
    fn vsha512msg2(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vsha512msg2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vsha512msg2Emitter<Ymm, Ymm> for Assembler<'_> {
    fn vsha512msg2(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vsha512msg2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vsha512rnds2Emitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsha512rnds2(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vsha512rnds2 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsha512rnds2Emitter<Ymm, Ymm, Xmm> for Assembler<'_> {
    fn vsha512rnds2(&mut self, op0: Ymm, op1: Ymm, op2: Xmm) {
        self.emit_n(
            InstId::Vsha512rnds2 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsm3msg1Emitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsm3msg1(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vsm3msg1 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsm3msg1Emitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsm3msg1(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vsm3msg1 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsm3msg1Emitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsm3msg1(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vsm3msg1 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsm3msg1Emitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsm3msg1(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vsm3msg1 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsm3msg2Emitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsm3msg2(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vsm3msg2 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsm3msg2Emitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsm3msg2(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vsm3msg2 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsm3msg2Emitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsm3msg2(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vsm3msg2 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsm3msg2Emitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsm3msg2(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vsm3msg2 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsm3rnds2Emitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vsm3rnds2(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vsm3rnds2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> Vsm3rnds2Emitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vsm3rnds2(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vsm3rnds2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        );
    }
}

impl Vsm3rnds2Emitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vsm3rnds2(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vsm3rnds2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> Vsm3rnds2Emitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vsm3rnds2(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vsm3rnds2 as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        );
    }
}

impl Vsm4key4Emitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsm4key4(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vsm4key4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsm4key4Emitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsm4key4(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vsm4key4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsm4key4Emitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vsm4key4(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vsm4key4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsm4key4Emitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsm4key4(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vsm4key4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsm4key4Emitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsm4key4(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vsm4key4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsm4key4Emitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vsm4key4(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vsm4key4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsm4rnds4Emitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsm4rnds4(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vsm4rnds4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsm4rnds4Emitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsm4rnds4(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vsm4rnds4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsm4rnds4Emitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vsm4rnds4(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vsm4rnds4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsm4rnds4Emitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsm4rnds4(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vsm4rnds4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsm4rnds4Emitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsm4rnds4(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vsm4rnds4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vsm4rnds4Emitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vsm4rnds4(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vsm4rnds4 as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VaddphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaddph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vaddph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VaddphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaddph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vaddph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VaddphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vaddph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vaddph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VaddphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vaddph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vaddph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VaddphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaddph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vaddph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VaddphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaddph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vaddph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VaddphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vaddph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vaddph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VaddphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vaddph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vaddph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VaddshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vaddsh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vaddsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VaddshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vaddsh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vaddsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VaddshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vaddsh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vaddsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VaddshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vaddsh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vaddsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VcmpphEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vcmpph(&mut self, op0: KReg, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vcmpph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VcmpphEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vcmpph(&mut self, op0: KReg, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vcmpph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VcmpphEmitter<KReg, Ymm, Ymm, U3> for Assembler<'_> {
    fn vcmpph(&mut self, op0: KReg, op1: Ymm, op2: Ymm, op3: U3) {
        self.emit_n(
            InstId::Vcmpph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VcmpphEmitter<KReg, Zmm, Zmm, U3> for Assembler<'_> {
    fn vcmpph(&mut self, op0: KReg, op1: Zmm, op2: Zmm, op3: U3) {
        self.emit_n(
            InstId::Vcmpph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        );
    }
}

impl VcmpphEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vcmpph(&mut self, op0: KReg, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vcmpph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VcmpphEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vcmpph(&mut self, op0: KReg, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vcmpph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VcmpphEmitter<KReg, Ymm, Mem, U3> for Assembler<'_> {
    fn vcmpph(&mut self, op0: KReg, op1: Ymm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vcmpph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VcmpphEmitter<KReg, Zmm, Mem, U3> for Assembler<'_> {
    fn vcmpph(&mut self, op0: KReg, op1: Zmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vcmpph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        );
    }
}

impl VcmpshEmitter<KReg, Vec, Vec, Imm> for Assembler<'_> {
    fn vcmpsh(&mut self, op0: KReg, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vcmpsh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VcmpshEmitter<KReg, Xmm, Xmm, U3> for Assembler<'_> {
    fn vcmpsh(&mut self, op0: KReg, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vcmpsh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        );
    }
}

impl VcmpshEmitter<KReg, Vec, Mem, Imm> for Assembler<'_> {
    fn vcmpsh(&mut self, op0: KReg, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vcmpsh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VcmpshEmitter<KReg, Xmm, Mem, U3> for Assembler<'_> {
    fn vcmpsh(&mut self, op0: KReg, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vcmpsh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        );
    }
}

impl VcomishEmitter<Vec, Vec> for Assembler<'_> {
    fn vcomish(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcomish as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VcomishEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcomish(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcomish as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VcomishEmitter<Vec, Mem> for Assembler<'_> {
    fn vcomish(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcomish as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VcomishEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcomish(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcomish as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtdq2phEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtdq2ph(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtdq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtdq2phEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtdq2ph(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtdq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtdq2phEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtdq2ph(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtdq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtdq2phEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvtdq2ph(&mut self, op0: Ymm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtdq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtdq2phEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtdq2ph(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtdq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtdq2phEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtdq2ph(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtdq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtdq2phEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtdq2ph(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtdq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtpd2phEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtpd2ph(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtpd2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtpd2phEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtpd2ph(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtpd2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtpd2phEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtpd2ph(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtpd2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtpd2phEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vcvtpd2ph(&mut self, op0: Xmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtpd2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtpd2phEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtpd2ph(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtpd2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtpd2phEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtpd2ph(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtpd2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2dqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtph2dq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtph2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2dqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtph2dq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtph2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2dqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtph2dq(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtph2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2dqEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvtph2dq(&mut self, op0: Zmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtph2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2dqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtph2dq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2dqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtph2dq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2dqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtph2dq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2dqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtph2dq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2pdEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtph2pd(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtph2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2pdEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtph2pd(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtph2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2pdEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtph2pd(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtph2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2pdEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vcvtph2pd(&mut self, op0: Zmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtph2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2pdEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtph2pd(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2pdEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtph2pd(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2pdEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtph2pd(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2pdEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtph2pd(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2pd as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2psxEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtph2psx(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtph2psx as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2psxEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtph2psx(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtph2psx as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2psxEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtph2psx(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtph2psx as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2psxEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvtph2psx(&mut self, op0: Zmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtph2psx as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2psxEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtph2psx(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2psx as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2psxEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtph2psx(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2psx as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2psxEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtph2psx(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2psx as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2psxEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtph2psx(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2psx as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2qqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtph2qq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtph2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2qqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtph2qq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtph2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2qqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtph2qq(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtph2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2qqEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vcvtph2qq(&mut self, op0: Zmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtph2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2qqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtph2qq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2qqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtph2qq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2qqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtph2qq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2qqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtph2qq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2udqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtph2udq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtph2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2udqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtph2udq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtph2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2udqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtph2udq(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtph2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2udqEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvtph2udq(&mut self, op0: Zmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtph2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2udqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtph2udq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2udqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtph2udq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2udqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtph2udq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2udqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtph2udq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2uqqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtph2uqq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtph2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2uqqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtph2uqq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtph2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2uqqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvtph2uqq(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtph2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2uqqEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vcvtph2uqq(&mut self, op0: Zmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtph2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2uqqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtph2uqq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2uqqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtph2uqq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2uqqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtph2uqq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2uqqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtph2uqq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2uwEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtph2uw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtph2uw as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2uwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtph2uw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtph2uw as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2uwEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtph2uw(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtph2uw as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2uwEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtph2uw(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtph2uw as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2uwEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtph2uw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2uw as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2uwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtph2uw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2uw as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2uwEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtph2uw(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2uw as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2uwEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtph2uw(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2uw as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2wEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtph2w(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtph2w as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2wEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtph2w(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtph2w as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2wEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtph2w(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtph2w as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2wEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtph2w(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtph2w as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2wEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtph2w(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2w as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2wEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtph2w(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2w as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2wEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtph2w(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2w as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtph2wEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtph2w(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtph2w as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtps2phxEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtps2phx(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtps2phx as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtps2phxEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtps2phx(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtps2phx as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtps2phxEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtps2phx(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtps2phx as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtps2phxEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvtps2phx(&mut self, op0: Ymm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtps2phx as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtps2phxEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtps2phx(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2phx as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtps2phxEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtps2phx(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2phx as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtps2phxEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtps2phx(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtps2phx as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtqq2phEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtqq2ph(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtqq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtqq2phEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtqq2ph(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtqq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtqq2phEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtqq2ph(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtqq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtqq2phEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vcvtqq2ph(&mut self, op0: Xmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtqq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtqq2phEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtqq2ph(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtqq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtqq2phEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtqq2ph(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtqq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtsd2shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vcvtsd2sh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vcvtsd2sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtsd2shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vcvtsd2sh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vcvtsd2sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtsd2shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtsd2sh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vcvtsd2sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtsd2shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtsd2sh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vcvtsd2sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtsh2sdEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vcvtsh2sd(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vcvtsh2sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtsh2sdEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vcvtsh2sd(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vcvtsh2sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtsh2sdEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtsh2sd(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vcvtsh2sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtsh2sdEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtsh2sd(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vcvtsh2sd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtsh2siEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvtsh2si(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Vcvtsh2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtsh2siEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvtsh2si(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtsh2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtsh2siEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvtsh2si(&mut self, op0: Gpq, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtsh2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtsh2siEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvtsh2si(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Vcvtsh2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtsh2siEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvtsh2si(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Vcvtsh2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtsh2siEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvtsh2si(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Vcvtsh2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtsh2ssEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vcvtsh2ss(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vcvtsh2ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtsh2ssEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vcvtsh2ss(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vcvtsh2ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtsh2ssEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtsh2ss(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vcvtsh2ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtsh2ssEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtsh2ss(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vcvtsh2ss as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtsh2usiEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvtsh2usi(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Vcvtsh2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtsh2usiEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvtsh2usi(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtsh2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtsh2usiEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvtsh2usi(&mut self, op0: Gpq, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtsh2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtsh2usiEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvtsh2usi(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Vcvtsh2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtsh2usiEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvtsh2usi(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Vcvtsh2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtsh2usiEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvtsh2usi(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Vcvtsh2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtsi2shEmitter<Vec, Vec, Gp> for Assembler<'_> {
    fn vcvtsi2sh(&mut self, op0: Vec, op1: Vec, op2: Gp) {
        self.emit_n(
            InstId::Vcvtsi2sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtsi2shEmitter<Xmm, Xmm, Gpd> for Assembler<'_> {
    fn vcvtsi2sh(&mut self, op0: Xmm, op1: Xmm, op2: Gpd) {
        self.emit_n(
            InstId::Vcvtsi2sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtsi2shEmitter<Xmm, Xmm, Gpq> for Assembler<'_> {
    fn vcvtsi2sh(&mut self, op0: Xmm, op1: Xmm, op2: Gpq) {
        self.emit_n(
            InstId::Vcvtsi2sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtsi2shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtsi2sh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vcvtsi2sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtsi2shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtsi2sh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vcvtsi2sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtss2shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vcvtss2sh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vcvtss2sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtss2shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vcvtss2sh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vcvtss2sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtss2shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtss2sh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vcvtss2sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtss2shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtss2sh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vcvtss2sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvttph2dqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttph2dq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvttph2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2dqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttph2dq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttph2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2dqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvttph2dq(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttph2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2dqEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvttph2dq(&mut self, op0: Zmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvttph2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2dqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttph2dq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2dqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttph2dq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2dqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttph2dq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2dqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttph2dq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2dq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2qqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttph2qq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvttph2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2qqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttph2qq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttph2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2qqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvttph2qq(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttph2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2qqEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vcvttph2qq(&mut self, op0: Zmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttph2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2qqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttph2qq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2qqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttph2qq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2qqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttph2qq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2qqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttph2qq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2qq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2udqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttph2udq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvttph2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2udqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttph2udq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttph2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2udqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvttph2udq(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttph2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2udqEmitter<Zmm, Ymm> for Assembler<'_> {
    fn vcvttph2udq(&mut self, op0: Zmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvttph2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2udqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttph2udq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2udqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttph2udq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2udqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttph2udq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2udqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttph2udq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2udq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2uqqEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttph2uqq(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvttph2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2uqqEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttph2uqq(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttph2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2uqqEmitter<Ymm, Xmm> for Assembler<'_> {
    fn vcvttph2uqq(&mut self, op0: Ymm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttph2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2uqqEmitter<Zmm, Xmm> for Assembler<'_> {
    fn vcvttph2uqq(&mut self, op0: Zmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttph2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2uqqEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttph2uqq(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2uqqEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttph2uqq(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2uqqEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttph2uqq(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2uqqEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttph2uqq(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2uqq as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2uwEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttph2uw(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvttph2uw as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2uwEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttph2uw(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttph2uw as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2uwEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvttph2uw(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvttph2uw as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2uwEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvttph2uw(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvttph2uw as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2uwEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttph2uw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2uw as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2uwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttph2uw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2uw as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2uwEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttph2uw(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2uw as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2uwEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttph2uw(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2uw as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2wEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvttph2w(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvttph2w as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2wEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvttph2w(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttph2w as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2wEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvttph2w(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvttph2w as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2wEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvttph2w(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvttph2w as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2wEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvttph2w(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2w as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2wEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvttph2w(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2w as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2wEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvttph2w(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2w as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttph2wEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvttph2w(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvttph2w as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttsh2siEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvttsh2si(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Vcvttsh2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttsh2siEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvttsh2si(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttsh2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttsh2siEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvttsh2si(&mut self, op0: Gpq, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttsh2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttsh2siEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvttsh2si(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Vcvttsh2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttsh2siEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvttsh2si(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Vcvttsh2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttsh2siEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvttsh2si(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Vcvttsh2si as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttsh2usiEmitter<Gp, Vec> for Assembler<'_> {
    fn vcvttsh2usi(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(
            InstId::Vcvttsh2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttsh2usiEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vcvttsh2usi(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttsh2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttsh2usiEmitter<Gpq, Xmm> for Assembler<'_> {
    fn vcvttsh2usi(&mut self, op0: Gpq, op1: Xmm) {
        self.emit_n(
            InstId::Vcvttsh2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttsh2usiEmitter<Gp, Mem> for Assembler<'_> {
    fn vcvttsh2usi(&mut self, op0: Gp, op1: Mem) {
        self.emit_n(
            InstId::Vcvttsh2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttsh2usiEmitter<Gpd, Mem> for Assembler<'_> {
    fn vcvttsh2usi(&mut self, op0: Gpd, op1: Mem) {
        self.emit_n(
            InstId::Vcvttsh2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvttsh2usiEmitter<Gpq, Mem> for Assembler<'_> {
    fn vcvttsh2usi(&mut self, op0: Gpq, op1: Mem) {
        self.emit_n(
            InstId::Vcvttsh2usi as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtudq2phEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtudq2ph(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtudq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtudq2phEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtudq2ph(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtudq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtudq2phEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtudq2ph(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtudq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtudq2phEmitter<Ymm, Zmm> for Assembler<'_> {
    fn vcvtudq2ph(&mut self, op0: Ymm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtudq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtudq2phEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtudq2ph(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtudq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtudq2phEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtudq2ph(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtudq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtudq2phEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtudq2ph(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtudq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtuqq2phEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtuqq2ph(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtuqq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtuqq2phEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtuqq2ph(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtuqq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtuqq2phEmitter<Xmm, Ymm> for Assembler<'_> {
    fn vcvtuqq2ph(&mut self, op0: Xmm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtuqq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtuqq2phEmitter<Xmm, Zmm> for Assembler<'_> {
    fn vcvtuqq2ph(&mut self, op0: Xmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtuqq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtuqq2phEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtuqq2ph(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtuqq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtuqq2phEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtuqq2ph(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtuqq2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtusi2shEmitter<Vec, Vec, Gp> for Assembler<'_> {
    fn vcvtusi2sh(&mut self, op0: Vec, op1: Vec, op2: Gp) {
        self.emit_n(
            InstId::Vcvtusi2sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtusi2shEmitter<Xmm, Xmm, Gpd> for Assembler<'_> {
    fn vcvtusi2sh(&mut self, op0: Xmm, op1: Xmm, op2: Gpd) {
        self.emit_n(
            InstId::Vcvtusi2sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtusi2shEmitter<Xmm, Xmm, Gpq> for Assembler<'_> {
    fn vcvtusi2sh(&mut self, op0: Xmm, op1: Xmm, op2: Gpq) {
        self.emit_n(
            InstId::Vcvtusi2sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtusi2shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vcvtusi2sh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vcvtusi2sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtusi2shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vcvtusi2sh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vcvtusi2sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vcvtuw2phEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtuw2ph(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtuw2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtuw2phEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtuw2ph(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtuw2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtuw2phEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtuw2ph(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtuw2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtuw2phEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtuw2ph(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtuw2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtuw2phEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtuw2ph(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtuw2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtuw2phEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtuw2ph(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtuw2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtuw2phEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtuw2ph(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtuw2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtuw2phEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtuw2ph(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtuw2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtw2phEmitter<Vec, Vec> for Assembler<'_> {
    fn vcvtw2ph(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vcvtw2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtw2phEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vcvtw2ph(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vcvtw2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtw2phEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vcvtw2ph(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vcvtw2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtw2phEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vcvtw2ph(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vcvtw2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtw2phEmitter<Vec, Mem> for Assembler<'_> {
    fn vcvtw2ph(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vcvtw2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtw2phEmitter<Xmm, Mem> for Assembler<'_> {
    fn vcvtw2ph(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtw2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtw2phEmitter<Ymm, Mem> for Assembler<'_> {
    fn vcvtw2ph(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtw2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Vcvtw2phEmitter<Zmm, Mem> for Assembler<'_> {
    fn vcvtw2ph(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vcvtw2ph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VdivphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vdivph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vdivph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VdivphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vdivph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vdivph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VdivphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vdivph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vdivph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VdivphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vdivph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vdivph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VdivphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vdivph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vdivph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VdivphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vdivph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vdivph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VdivphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vdivph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vdivph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VdivphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vdivph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vdivph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VdivshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vdivsh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vdivsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VdivshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vdivsh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vdivsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VdivshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vdivsh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vdivsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VdivshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vdivsh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vdivsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmaddcphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfcmaddcph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfcmaddcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmaddcphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfcmaddcph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfcmaddcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmaddcphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfcmaddcph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfcmaddcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmaddcphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfcmaddcph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfcmaddcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmaddcphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfcmaddcph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfcmaddcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmaddcphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfcmaddcph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfcmaddcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmaddcphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfcmaddcph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfcmaddcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmaddcphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfcmaddcph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfcmaddcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmaddcshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfcmaddcsh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfcmaddcsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmaddcshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfcmaddcsh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfcmaddcsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmaddcshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfcmaddcsh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfcmaddcsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmaddcshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfcmaddcsh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfcmaddcsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmulcphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfcmulcph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfcmulcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmulcphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfcmulcph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfcmulcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmulcphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfcmulcph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfcmulcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmulcphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfcmulcph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfcmulcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmulcphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfcmulcph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfcmulcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmulcphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfcmulcph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfcmulcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmulcphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfcmulcph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfcmulcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmulcphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfcmulcph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfcmulcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmulcshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfcmulcsh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfcmulcsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmulcshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfcmulcsh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfcmulcsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmulcshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfcmulcsh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfcmulcsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfcmulcshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfcmulcsh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfcmulcsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd132phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd132ph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd132phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd132ph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd132phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmadd132ph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd132phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmadd132ph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd132phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd132ph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd132phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd132ph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd132phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmadd132ph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd132phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmadd132ph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd132shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd132sh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmadd132sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd132shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd132sh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmadd132sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd132shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd132sh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd132sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd132shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd132sh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd132sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd213phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd213ph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd213phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd213ph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd213phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmadd213ph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd213phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmadd213ph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd213phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd213ph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd213phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd213ph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd213phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmadd213ph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd213phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmadd213ph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd213shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd213sh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmadd213sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd213shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd213sh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmadd213sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd213shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd213sh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd213sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd213shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd213sh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd213sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd231phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd231ph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd231phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd231ph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd231phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmadd231ph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd231phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmadd231ph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd231phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd231ph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd231phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd231ph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd231phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmadd231ph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd231phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmadd231ph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd231shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmadd231sh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmadd231sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd231shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmadd231sh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmadd231sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd231shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmadd231sh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd231sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmadd231shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmadd231sh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmadd231sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmaddcphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddcph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmaddcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmaddcphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddcph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmaddcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmaddcphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddcph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmaddcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmaddcphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmaddcph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmaddcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmaddcphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddcph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmaddcphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddcph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmaddcphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddcph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmaddcphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmaddcph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmaddcshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddcsh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmaddcsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmaddcshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddcsh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmaddcsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmaddcshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddcsh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddcsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmaddcshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddcsh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddcsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub132phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsub132ph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmaddsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub132phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsub132ph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmaddsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub132phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsub132ph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmaddsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub132phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmaddsub132ph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmaddsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub132phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsub132ph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub132phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsub132ph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub132phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsub132ph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub132phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmaddsub132ph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub213phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsub213ph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmaddsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub213phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsub213ph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmaddsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub213phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsub213ph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmaddsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub213phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmaddsub213ph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmaddsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub213phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsub213ph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub213phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsub213ph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub213phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsub213ph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub213phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmaddsub213ph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub231phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmaddsub231ph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmaddsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub231phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmaddsub231ph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmaddsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub231phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmaddsub231ph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmaddsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub231phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmaddsub231ph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmaddsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub231phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmaddsub231ph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub231phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmaddsub231ph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub231phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmaddsub231ph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmaddsub231phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmaddsub231ph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmaddsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub132phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub132ph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub132phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub132ph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub132phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsub132ph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub132phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsub132ph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub132phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub132ph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub132phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub132ph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub132phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsub132ph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub132phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsub132ph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub132shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub132sh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsub132sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub132shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub132sh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsub132sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub132shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub132sh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub132sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub132shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub132sh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub132sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub213phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub213ph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub213phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub213ph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub213phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsub213ph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub213phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsub213ph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub213phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub213ph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub213phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub213ph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub213phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsub213ph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub213phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsub213ph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub213shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub213sh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsub213sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub213shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub213sh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsub213sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub213shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub213sh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub213sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub213shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub213sh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub213sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub231phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub231ph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub231phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub231ph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub231phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsub231ph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub231phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsub231ph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub231phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub231ph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub231phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub231ph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub231phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsub231ph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub231phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsub231ph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub231shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsub231sh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsub231sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub231shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsub231sh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsub231sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub231shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsub231sh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub231sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsub231shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsub231sh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsub231sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd132phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubadd132ph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsubadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd132phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubadd132ph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsubadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd132phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubadd132ph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmsubadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd132phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsubadd132ph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmsubadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd132phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubadd132ph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd132phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubadd132ph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd132phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubadd132ph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd132phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsubadd132ph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd213phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubadd213ph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsubadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd213phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubadd213ph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsubadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd213phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubadd213ph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmsubadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd213phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsubadd213ph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmsubadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd213phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubadd213ph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd213phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubadd213ph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd213phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubadd213ph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd213phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsubadd213ph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd231phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmsubadd231ph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmsubadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd231phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmsubadd231ph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmsubadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd231phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmsubadd231ph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmsubadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd231phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmsubadd231ph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmsubadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd231phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmsubadd231ph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd231phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmsubadd231ph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd231phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmsubadd231ph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfmsubadd231phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmsubadd231ph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmsubadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmulcphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmulcph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmulcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmulcphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmulcph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmulcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmulcphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfmulcph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfmulcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmulcphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfmulcph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfmulcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmulcphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmulcph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmulcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmulcphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmulcph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmulcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmulcphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfmulcph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfmulcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmulcphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfmulcph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmulcph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmulcshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfmulcsh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfmulcsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmulcshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfmulcsh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfmulcsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmulcshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfmulcsh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfmulcsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfmulcshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfmulcsh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfmulcsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd132phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd132ph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd132phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd132ph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd132phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmadd132ph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfnmadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd132phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmadd132ph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfnmadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd132phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd132ph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd132phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd132ph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd132phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmadd132ph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd132phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmadd132ph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd132shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd132sh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmadd132sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd132shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd132sh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd132sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd132shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd132sh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd132shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd132sh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd132sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd213phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd213ph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd213phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd213ph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd213phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmadd213ph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfnmadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd213phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmadd213ph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfnmadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd213phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd213ph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd213phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd213ph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd213phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmadd213ph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd213phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmadd213ph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd213shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd213sh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmadd213sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd213shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd213sh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd213sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd213shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd213sh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd213shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd213sh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd213sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd231phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd231ph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd231phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd231ph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd231phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmadd231ph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfnmadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd231phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmadd231ph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfnmadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd231phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd231ph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd231phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd231ph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd231phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmadd231ph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd231phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmadd231ph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd231shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmadd231sh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmadd231sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd231shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmadd231sh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmadd231sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd231shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmadd231sh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmadd231shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmadd231sh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmadd231sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub132phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub132ph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub132phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub132ph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub132phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsub132ph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfnmsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub132phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmsub132ph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfnmsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub132phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub132ph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub132phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub132ph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub132phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsub132ph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub132phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmsub132ph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub132shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub132sh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmsub132sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub132shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub132sh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub132sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub132shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub132sh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub132shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub132sh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub132sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub213phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub213ph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub213phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub213ph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub213phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsub213ph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfnmsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub213phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmsub213ph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfnmsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub213phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub213ph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub213phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub213ph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub213phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsub213ph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub213phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmsub213ph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub213shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub213sh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmsub213sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub213shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub213sh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub213sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub213shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub213sh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub213shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub213sh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub213sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub231phEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub231ph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub231phEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub231ph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub231phEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vfnmsub231ph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vfnmsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub231phEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vfnmsub231ph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vfnmsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub231phEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub231ph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub231phEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub231ph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub231phEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vfnmsub231ph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub231phEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vfnmsub231ph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231ph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub231shEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vfnmsub231sh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vfnmsub231sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub231shEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vfnmsub231sh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vfnmsub231sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub231shEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vfnmsub231sh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vfnmsub231shEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vfnmsub231sh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vfnmsub231sh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VfpclassphEmitter<KReg, Vec, Imm> for Assembler<'_> {
    fn vfpclassph(&mut self, op0: KReg, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vfpclassph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl<U2: Into<Imm>> VfpclassphEmitter<KReg, Xmm, U2> for Assembler<'_> {
    fn vfpclassph(&mut self, op0: KReg, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vfpclassph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VfpclassphEmitter<KReg, Ymm, U2> for Assembler<'_> {
    fn vfpclassph(&mut self, op0: KReg, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vfpclassph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VfpclassphEmitter<KReg, Zmm, U2> for Assembler<'_> {
    fn vfpclassph(&mut self, op0: KReg, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vfpclassph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VfpclassphEmitter<KReg, Mem, U2> for Assembler<'_> {
    fn vfpclassph(&mut self, op0: KReg, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vfpclassph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl VfpclassshEmitter<KReg, Vec, Imm> for Assembler<'_> {
    fn vfpclasssh(&mut self, op0: KReg, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vfpclasssh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl<U2: Into<Imm>> VfpclassshEmitter<KReg, Xmm, U2> for Assembler<'_> {
    fn vfpclasssh(&mut self, op0: KReg, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vfpclasssh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VfpclassshEmitter<KReg, Mem, U2> for Assembler<'_> {
    fn vfpclasssh(&mut self, op0: KReg, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vfpclasssh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl VgetexpphEmitter<Vec, Vec> for Assembler<'_> {
    fn vgetexpph(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vgetexpph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VgetexpphEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vgetexpph(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vgetexpph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VgetexpphEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vgetexpph(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vgetexpph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VgetexpphEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vgetexpph(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vgetexpph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VgetexpphEmitter<Vec, Mem> for Assembler<'_> {
    fn vgetexpph(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vgetexpph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VgetexpphEmitter<Xmm, Mem> for Assembler<'_> {
    fn vgetexpph(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vgetexpph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VgetexpphEmitter<Ymm, Mem> for Assembler<'_> {
    fn vgetexpph(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vgetexpph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VgetexpphEmitter<Zmm, Mem> for Assembler<'_> {
    fn vgetexpph(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vgetexpph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VgetexpshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vgetexpsh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vgetexpsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VgetexpshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vgetexpsh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vgetexpsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VgetexpshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vgetexpsh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vgetexpsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VgetexpshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vgetexpsh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vgetexpsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VgetmantphEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vgetmantph(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vgetmantph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl<U2: Into<Imm>> VgetmantphEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vgetmantph(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vgetmantph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VgetmantphEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vgetmantph(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vgetmantph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VgetmantphEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vgetmantph(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vgetmantph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl VgetmantphEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vgetmantph(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vgetmantph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl<U2: Into<Imm>> VgetmantphEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vgetmantph(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vgetmantph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VgetmantphEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vgetmantph(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vgetmantph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VgetmantphEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vgetmantph(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vgetmantph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl VgetmantshEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vgetmantsh(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vgetmantsh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VgetmantshEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vgetmantsh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vgetmantsh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        );
    }
}

impl VgetmantshEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vgetmantsh(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vgetmantsh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VgetmantshEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vgetmantsh(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vgetmantsh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        );
    }
}

impl VmaxphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmaxph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vmaxph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmaxphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmaxph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vmaxph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmaxphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vmaxph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vmaxph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmaxphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vmaxph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vmaxph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmaxphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmaxph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vmaxph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmaxphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmaxph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vmaxph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmaxphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vmaxph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vmaxph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmaxphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vmaxph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vmaxph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmaxshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmaxsh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vmaxsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmaxshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmaxsh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vmaxsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmaxshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmaxsh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vmaxsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmaxshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmaxsh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vmaxsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VminphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vminph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vminph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VminphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vminph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vminph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VminphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vminph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vminph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VminphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vminph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vminph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VminphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vminph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vminph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VminphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vminph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vminph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VminphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vminph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vminph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VminphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vminph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vminph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VminshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vminsh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vminsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VminshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vminsh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vminsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VminshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vminsh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vminsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VminshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vminsh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vminsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmovshEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovsh(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(InstId::Vmovsh as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl VmovshEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovsh(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(InstId::Vmovsh as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl VmovshEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovsh(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Vmovsh as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl VmovshEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovsh(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Vmovsh as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl Vmovsh3Emitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmovsh_3(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vmovsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Vmovsh3Emitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmovsh_3(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vmovsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmovwEmitter<Gp, Vec> for Assembler<'_> {
    fn vmovw(&mut self, op0: Gp, op1: Vec) {
        self.emit_n(InstId::Vmovw as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl VmovwEmitter<Gpd, Xmm> for Assembler<'_> {
    fn vmovw(&mut self, op0: Gpd, op1: Xmm) {
        self.emit_n(InstId::Vmovw as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl VmovwEmitter<Mem, Vec> for Assembler<'_> {
    fn vmovw(&mut self, op0: Mem, op1: Vec) {
        self.emit_n(InstId::Vmovw as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl VmovwEmitter<Mem, Xmm> for Assembler<'_> {
    fn vmovw(&mut self, op0: Mem, op1: Xmm) {
        self.emit_n(InstId::Vmovw as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl VmovwEmitter<Vec, Gp> for Assembler<'_> {
    fn vmovw(&mut self, op0: Vec, op1: Gp) {
        self.emit_n(InstId::Vmovw as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl VmovwEmitter<Xmm, Gpd> for Assembler<'_> {
    fn vmovw(&mut self, op0: Xmm, op1: Gpd) {
        self.emit_n(InstId::Vmovw as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl VmovwEmitter<Vec, Mem> for Assembler<'_> {
    fn vmovw(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Vmovw as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl VmovwEmitter<Xmm, Mem> for Assembler<'_> {
    fn vmovw(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Vmovw as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl VmulphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmulph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vmulph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmulphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmulph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vmulph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmulphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vmulph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vmulph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmulphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vmulph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vmulph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmulphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmulph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vmulph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmulphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmulph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vmulph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmulphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vmulph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vmulph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmulphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vmulph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vmulph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmulshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vmulsh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vmulsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmulshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vmulsh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vmulsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmulshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vmulsh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vmulsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VmulshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vmulsh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vmulsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VrcpphEmitter<Vec, Vec> for Assembler<'_> {
    fn vrcpph(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(InstId::Vrcpph as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl VrcpphEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vrcpph(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(InstId::Vrcpph as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl VrcpphEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vrcpph(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(InstId::Vrcpph as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl VrcpphEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vrcpph(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(InstId::Vrcpph as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl VrcpphEmitter<Vec, Mem> for Assembler<'_> {
    fn vrcpph(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(InstId::Vrcpph as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl VrcpphEmitter<Xmm, Mem> for Assembler<'_> {
    fn vrcpph(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(InstId::Vrcpph as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl VrcpphEmitter<Ymm, Mem> for Assembler<'_> {
    fn vrcpph(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(InstId::Vrcpph as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl VrcpphEmitter<Zmm, Mem> for Assembler<'_> {
    fn vrcpph(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(InstId::Vrcpph as u32, &[op0.as_operand(), op1.as_operand()]);
    }
}

impl VrcpshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vrcpsh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vrcpsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VrcpshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vrcpsh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vrcpsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VrcpshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vrcpsh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vrcpsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VrcpshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vrcpsh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vrcpsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VreducephEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vreduceph(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vreduceph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl<U2: Into<Imm>> VreducephEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vreduceph(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vreduceph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VreducephEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vreduceph(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vreduceph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VreducephEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vreduceph(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vreduceph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl VreducephEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vreduceph(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vreduceph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl<U2: Into<Imm>> VreducephEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vreduceph(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vreduceph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VreducephEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vreduceph(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vreduceph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VreducephEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vreduceph(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vreduceph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl VreduceshEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vreducesh(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vreducesh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VreduceshEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vreducesh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vreducesh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        );
    }
}

impl VreduceshEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vreducesh(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vreducesh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VreduceshEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vreducesh(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vreducesh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        );
    }
}

impl VrndscalephEmitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vrndscaleph(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vrndscaleph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl<U2: Into<Imm>> VrndscalephEmitter<Xmm, Xmm, U2> for Assembler<'_> {
    fn vrndscaleph(&mut self, op0: Xmm, op1: Xmm, op2: U2) {
        self.emit_n(
            InstId::Vrndscaleph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VrndscalephEmitter<Ymm, Ymm, U2> for Assembler<'_> {
    fn vrndscaleph(&mut self, op0: Ymm, op1: Ymm, op2: U2) {
        self.emit_n(
            InstId::Vrndscaleph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VrndscalephEmitter<Zmm, Zmm, U2> for Assembler<'_> {
    fn vrndscaleph(&mut self, op0: Zmm, op1: Zmm, op2: U2) {
        self.emit_n(
            InstId::Vrndscaleph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl VrndscalephEmitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vrndscaleph(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vrndscaleph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl<U2: Into<Imm>> VrndscalephEmitter<Xmm, Mem, U2> for Assembler<'_> {
    fn vrndscaleph(&mut self, op0: Xmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vrndscaleph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VrndscalephEmitter<Ymm, Mem, U2> for Assembler<'_> {
    fn vrndscaleph(&mut self, op0: Ymm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vrndscaleph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl<U2: Into<Imm>> VrndscalephEmitter<Zmm, Mem, U2> for Assembler<'_> {
    fn vrndscaleph(&mut self, op0: Zmm, op1: Mem, op2: U2) {
        self.emit_n(
            InstId::Vrndscaleph as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                Into::<Imm>::into(op2).as_operand(),
            ],
        );
    }
}

impl VrndscaleshEmitter<Vec, Vec, Vec, Imm> for Assembler<'_> {
    fn vrndscalesh(&mut self, op0: Vec, op1: Vec, op2: Vec, op3: Imm) {
        self.emit_n(
            InstId::Vrndscalesh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VrndscaleshEmitter<Xmm, Xmm, Xmm, U3> for Assembler<'_> {
    fn vrndscalesh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm, op3: U3) {
        self.emit_n(
            InstId::Vrndscalesh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        );
    }
}

impl VrndscaleshEmitter<Vec, Vec, Mem, Imm> for Assembler<'_> {
    fn vrndscalesh(&mut self, op0: Vec, op1: Vec, op2: Mem, op3: Imm) {
        self.emit_n(
            InstId::Vrndscalesh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                op3.as_operand(),
            ],
        );
    }
}

impl<U3: Into<Imm>> VrndscaleshEmitter<Xmm, Xmm, Mem, U3> for Assembler<'_> {
    fn vrndscalesh(&mut self, op0: Xmm, op1: Xmm, op2: Mem, op3: U3) {
        self.emit_n(
            InstId::Vrndscalesh as u32,
            &[
                op0.as_operand(),
                op1.as_operand(),
                op2.as_operand(),
                Into::<Imm>::into(op3).as_operand(),
            ],
        );
    }
}

impl VrsqrtphEmitter<Vec, Vec> for Assembler<'_> {
    fn vrsqrtph(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vrsqrtph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VrsqrtphEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vrsqrtph(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vrsqrtph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VrsqrtphEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vrsqrtph(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vrsqrtph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VrsqrtphEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vrsqrtph(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vrsqrtph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VrsqrtphEmitter<Vec, Mem> for Assembler<'_> {
    fn vrsqrtph(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vrsqrtph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VrsqrtphEmitter<Xmm, Mem> for Assembler<'_> {
    fn vrsqrtph(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vrsqrtph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VrsqrtphEmitter<Ymm, Mem> for Assembler<'_> {
    fn vrsqrtph(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vrsqrtph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VrsqrtphEmitter<Zmm, Mem> for Assembler<'_> {
    fn vrsqrtph(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vrsqrtph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VrsqrtshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vrsqrtsh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vrsqrtsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VrsqrtshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vrsqrtsh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vrsqrtsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VrsqrtshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vrsqrtsh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vrsqrtsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VrsqrtshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vrsqrtsh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vrsqrtsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VscalefphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vscalefph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vscalefph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VscalefphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vscalefph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vscalefph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VscalefphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vscalefph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vscalefph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VscalefphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vscalefph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vscalefph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VscalefphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vscalefph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vscalefph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VscalefphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vscalefph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vscalefph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VscalefphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vscalefph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vscalefph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VscalefphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vscalefph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vscalefph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VscalefshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vscalefsh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vscalefsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VscalefshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vscalefsh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vscalefsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VscalefshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vscalefsh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vscalefsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VscalefshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vscalefsh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vscalefsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VsqrtphEmitter<Vec, Vec> for Assembler<'_> {
    fn vsqrtph(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vsqrtph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VsqrtphEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vsqrtph(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vsqrtph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VsqrtphEmitter<Ymm, Ymm> for Assembler<'_> {
    fn vsqrtph(&mut self, op0: Ymm, op1: Ymm) {
        self.emit_n(
            InstId::Vsqrtph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VsqrtphEmitter<Zmm, Zmm> for Assembler<'_> {
    fn vsqrtph(&mut self, op0: Zmm, op1: Zmm) {
        self.emit_n(
            InstId::Vsqrtph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VsqrtphEmitter<Vec, Mem> for Assembler<'_> {
    fn vsqrtph(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vsqrtph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VsqrtphEmitter<Xmm, Mem> for Assembler<'_> {
    fn vsqrtph(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vsqrtph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VsqrtphEmitter<Ymm, Mem> for Assembler<'_> {
    fn vsqrtph(&mut self, op0: Ymm, op1: Mem) {
        self.emit_n(
            InstId::Vsqrtph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VsqrtphEmitter<Zmm, Mem> for Assembler<'_> {
    fn vsqrtph(&mut self, op0: Zmm, op1: Mem) {
        self.emit_n(
            InstId::Vsqrtph as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VsqrtshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsqrtsh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vsqrtsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VsqrtshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsqrtsh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vsqrtsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VsqrtshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsqrtsh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vsqrtsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VsqrtshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsqrtsh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vsqrtsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VsubphEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsubph(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vsubph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VsubphEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsubph(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vsubph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VsubphEmitter<Ymm, Ymm, Ymm> for Assembler<'_> {
    fn vsubph(&mut self, op0: Ymm, op1: Ymm, op2: Ymm) {
        self.emit_n(
            InstId::Vsubph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VsubphEmitter<Zmm, Zmm, Zmm> for Assembler<'_> {
    fn vsubph(&mut self, op0: Zmm, op1: Zmm, op2: Zmm) {
        self.emit_n(
            InstId::Vsubph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VsubphEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsubph(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vsubph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VsubphEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsubph(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vsubph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VsubphEmitter<Ymm, Ymm, Mem> for Assembler<'_> {
    fn vsubph(&mut self, op0: Ymm, op1: Ymm, op2: Mem) {
        self.emit_n(
            InstId::Vsubph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VsubphEmitter<Zmm, Zmm, Mem> for Assembler<'_> {
    fn vsubph(&mut self, op0: Zmm, op1: Zmm, op2: Mem) {
        self.emit_n(
            InstId::Vsubph as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VsubshEmitter<Vec, Vec, Vec> for Assembler<'_> {
    fn vsubsh(&mut self, op0: Vec, op1: Vec, op2: Vec) {
        self.emit_n(
            InstId::Vsubsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VsubshEmitter<Xmm, Xmm, Xmm> for Assembler<'_> {
    fn vsubsh(&mut self, op0: Xmm, op1: Xmm, op2: Xmm) {
        self.emit_n(
            InstId::Vsubsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VsubshEmitter<Vec, Vec, Mem> for Assembler<'_> {
    fn vsubsh(&mut self, op0: Vec, op1: Vec, op2: Mem) {
        self.emit_n(
            InstId::Vsubsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VsubshEmitter<Xmm, Xmm, Mem> for Assembler<'_> {
    fn vsubsh(&mut self, op0: Xmm, op1: Xmm, op2: Mem) {
        self.emit_n(
            InstId::Vsubsh as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl VucomishEmitter<Vec, Vec> for Assembler<'_> {
    fn vucomish(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vucomish as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VucomishEmitter<Xmm, Xmm> for Assembler<'_> {
    fn vucomish(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Vucomish as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VucomishEmitter<Vec, Mem> for Assembler<'_> {
    fn vucomish(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Vucomish as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl VucomishEmitter<Xmm, Mem> for Assembler<'_> {
    fn vucomish(&mut self, op0: Xmm, op1: Mem) {
        self.emit_n(
            InstId::Vucomish as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl LdtilecfgEmitter<Mem> for Assembler<'_> {
    fn ldtilecfg(&mut self, op0: Mem) {
        self.emit_n(InstId::Ldtilecfg as u32, &[op0.as_operand()]);
    }
}

impl SttilecfgEmitter<Mem> for Assembler<'_> {
    fn sttilecfg(&mut self, op0: Mem) {
        self.emit_n(InstId::Sttilecfg as u32, &[op0.as_operand()]);
    }
}

impl TileloaddEmitter<Tmm, Mem> for Assembler<'_> {
    fn tileloadd(&mut self, op0: Tmm, op1: Mem) {
        self.emit_n(
            InstId::Tileloadd as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl Tileloaddt1Emitter<Tmm, Mem> for Assembler<'_> {
    fn tileloaddt1(&mut self, op0: Tmm, op1: Mem) {
        self.emit_n(
            InstId::Tileloaddt1 as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl TilereleaseEmitter for Assembler<'_> {
    fn tilerelease(&mut self) {
        self.emit_n(InstId::Tilerelease as u32, &[]);
    }
}

impl TilestoredEmitter<Mem, Tmm> for Assembler<'_> {
    fn tilestored(&mut self, op0: Mem, op1: Tmm) {
        self.emit_n(
            InstId::Tilestored as u32,
            &[op0.as_operand(), op1.as_operand()],
        );
    }
}

impl TilezeroEmitter<Tmm> for Assembler<'_> {
    fn tilezero(&mut self, op0: Tmm) {
        self.emit_n(InstId::Tilezero as u32, &[op0.as_operand()]);
    }
}

impl Tdpbf16psEmitter<Tmm, Tmm, Tmm> for Assembler<'_> {
    fn tdpbf16ps(&mut self, op0: Tmm, op1: Tmm, op2: Tmm) {
        self.emit_n(
            InstId::Tdpbf16ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Tcmmimfp16psEmitter<Tmm, Tmm, Tmm> for Assembler<'_> {
    fn tcmmimfp16ps(&mut self, op0: Tmm, op1: Tmm, op2: Tmm) {
        self.emit_n(
            InstId::Tcmmimfp16ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Tcmmrlfp16psEmitter<Tmm, Tmm, Tmm> for Assembler<'_> {
    fn tcmmrlfp16ps(&mut self, op0: Tmm, op1: Tmm, op2: Tmm) {
        self.emit_n(
            InstId::Tcmmrlfp16ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Tdpfp16psEmitter<Tmm, Tmm, Tmm> for Assembler<'_> {
    fn tdpfp16ps(&mut self, op0: Tmm, op1: Tmm, op2: Tmm) {
        self.emit_n(
            InstId::Tdpfp16ps as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl TdpbssdEmitter<Tmm, Tmm, Tmm> for Assembler<'_> {
    fn tdpbssd(&mut self, op0: Tmm, op1: Tmm, op2: Tmm) {
        self.emit_n(
            InstId::Tdpbssd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl TdpbsudEmitter<Tmm, Tmm, Tmm> for Assembler<'_> {
    fn tdpbsud(&mut self, op0: Tmm, op1: Tmm, op2: Tmm) {
        self.emit_n(
            InstId::Tdpbsud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl TdpbusdEmitter<Tmm, Tmm, Tmm> for Assembler<'_> {
    fn tdpbusd(&mut self, op0: Tmm, op1: Tmm, op2: Tmm) {
        self.emit_n(
            InstId::Tdpbusd as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl TdpbuudEmitter<Tmm, Tmm, Tmm> for Assembler<'_> {
    fn tdpbuud(&mut self, op0: Tmm, op1: Tmm, op2: Tmm) {
        self.emit_n(
            InstId::Tdpbuud as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        );
    }
}

impl Cbw0Emitter for Assembler<'_> {
    fn cbw_0(&mut self) {
        self.emit_n(InstId::Cbw as u32, &[]); // ANY [IMPLICIT] AX <- Sign Extend AL
    }
}

impl Cdq0Emitter for Assembler<'_> {
    fn cdq_0(&mut self) {
        self.emit_n(InstId::Cdq as u32, &[]); // ANY [IMPLICIT] EDX:EAX <- Sign Extend EAX
    }
}

impl Cdqe0Emitter for Assembler<'_> {
    fn cdqe_0(&mut self) {
        self.emit_n(InstId::Cdqe as u32, &[]); // X64 [IMPLICIT] RAX <- Sign Extend EAX
    }
}

impl Cmpxchg2Emitter<Gp, Gp> for Assembler<'_> {
    fn cmpxchg_2(&mut self, op0: Gp, op1: Gp) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // I486 [IMPLICIT]
    }
}

impl Cmpxchg2Emitter<GpbLo, GpbLo> for Assembler<'_> {
    fn cmpxchg_2(&mut self, op0: GpbLo, op1: GpbLo) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // I486 [IMPLICIT]
    }
}

impl Cmpxchg2Emitter<GpbHi, GpbLo> for Assembler<'_> {
    fn cmpxchg_2(&mut self, op0: GpbHi, op1: GpbLo) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // I486 [IMPLICIT]
    }
}

impl Cmpxchg2Emitter<Gpw, Gpw> for Assembler<'_> {
    fn cmpxchg_2(&mut self, op0: Gpw, op1: Gpw) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // I486 [IMPLICIT]
    }
}

impl Cmpxchg2Emitter<Gpd, Gpd> for Assembler<'_> {
    fn cmpxchg_2(&mut self, op0: Gpd, op1: Gpd) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // I486 [IMPLICIT]
    }
}

impl Cmpxchg2Emitter<Gpq, Gpq> for Assembler<'_> {
    fn cmpxchg_2(&mut self, op0: Gpq, op1: Gpq) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // I486 [IMPLICIT]
    }
}

impl Cmpxchg2Emitter<Mem, Gp> for Assembler<'_> {
    fn cmpxchg_2(&mut self, op0: Mem, op1: Gp) {
        self.emit_n(
            InstId::Cmpxchg as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // I486 [IMPLICIT]
    }
}

impl Cmpxchg16b1Emitter<Mem> for Assembler<'_> {
    fn cmpxchg16b_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Cmpxchg16b as u32, &[op0.as_operand()]); // CMPXCHG8B [IMPLICIT] m == RDX:RAX ? m <- RCX:RBX
    }
}

impl Cmpxchg8b1Emitter<Mem> for Assembler<'_> {
    fn cmpxchg8b_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Cmpxchg8b as u32, &[op0.as_operand()]); // CMPXCHG16B[IMPLICIT] m == EDX:EAX ? m <- ECX:EBX
    }
}

impl Cqo0Emitter for Assembler<'_> {
    fn cqo_0(&mut self) {
        self.emit_n(InstId::Cqo as u32, &[]); // X64 [IMPLICIT] RDX:RAX <- Sign Extend RAX
    }
}

impl Cwd0Emitter for Assembler<'_> {
    fn cwd_0(&mut self) {
        self.emit_n(InstId::Cwd as u32, &[]); // ANY [IMPLICIT] DX:AX <- Sign Extend AX
    }
}

impl Cwde0Emitter for Assembler<'_> {
    fn cwde_0(&mut self) {
        self.emit_n(InstId::Cwde as u32, &[]); // ANY [IMPLICIT] EAX <- Sign Extend AX
    }
}

impl Div1Emitter<Gp> for Assembler<'_> {
    fn div_1(&mut self, op0: Gp) {
        self.emit_n(InstId::Div as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Div1Emitter<GpbLo> for Assembler<'_> {
    fn div_1(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Div as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Div1Emitter<GpbHi> for Assembler<'_> {
    fn div_1(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Div as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Div1Emitter<Gpw> for Assembler<'_> {
    fn div_1(&mut self, op0: Gpw) {
        self.emit_n(InstId::Div as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Div1Emitter<Gpd> for Assembler<'_> {
    fn div_1(&mut self, op0: Gpd) {
        self.emit_n(InstId::Div as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Div1Emitter<Gpq> for Assembler<'_> {
    fn div_1(&mut self, op0: Gpq) {
        self.emit_n(InstId::Div as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Div1Emitter<Mem> for Assembler<'_> {
    fn div_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Div as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / m8} {xDX[Rem]:xAX[Quot] <- DX:AX / m16|m32|m64}
    }
}

impl Idiv1Emitter<Gp> for Assembler<'_> {
    fn idiv_1(&mut self, op0: Gp) {
        self.emit_n(InstId::Idiv as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Idiv1Emitter<GpbLo> for Assembler<'_> {
    fn idiv_1(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Idiv as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Idiv1Emitter<GpbHi> for Assembler<'_> {
    fn idiv_1(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Idiv as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Idiv1Emitter<Gpw> for Assembler<'_> {
    fn idiv_1(&mut self, op0: Gpw) {
        self.emit_n(InstId::Idiv as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Idiv1Emitter<Gpd> for Assembler<'_> {
    fn idiv_1(&mut self, op0: Gpd) {
        self.emit_n(InstId::Idiv as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Idiv1Emitter<Gpq> for Assembler<'_> {
    fn idiv_1(&mut self, op0: Gpq) {
        self.emit_n(InstId::Idiv as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / r8} {xDX[Rem]:xAX[Quot] <- DX:AX / r16|r32|r64}
    }
}

impl Idiv1Emitter<Mem> for Assembler<'_> {
    fn idiv_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Idiv as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AH[Rem]: AL[Quot] <- AX / m8} {xDX[Rem]:xAX[Quot] <- DX:AX / m16|m32|m64}
    }
}

impl Imul1Emitter<Gp> for Assembler<'_> {
    fn imul_1(&mut self, op0: Gp) {
        self.emit_n(InstId::Imul as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xAX:xDX <- xAX * r16|r32|r64}
    }
}

impl Imul1Emitter<GpbLo> for Assembler<'_> {
    fn imul_1(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Imul as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xAX:xDX <- xAX * r16|r32|r64}
    }
}

impl Imul1Emitter<GpbHi> for Assembler<'_> {
    fn imul_1(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Imul as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xAX:xDX <- xAX * r16|r32|r64}
    }
}

impl Imul1Emitter<Gpw> for Assembler<'_> {
    fn imul_1(&mut self, op0: Gpw) {
        self.emit_n(InstId::Imul as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xAX:xDX <- xAX * r16|r32|r64}
    }
}

impl Imul1Emitter<Gpd> for Assembler<'_> {
    fn imul_1(&mut self, op0: Gpd) {
        self.emit_n(InstId::Imul as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xAX:xDX <- xAX * r16|r32|r64}
    }
}

impl Imul1Emitter<Gpq> for Assembler<'_> {
    fn imul_1(&mut self, op0: Gpq) {
        self.emit_n(InstId::Imul as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xAX:xDX <- xAX * r16|r32|r64}
    }
}

impl Imul1Emitter<Mem> for Assembler<'_> {
    fn imul_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Imul as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AX <- AL * m8} {xAX:xDX <- xAX * m16|m32|m64}
    }
}

impl IretEmitter for Assembler<'_> {
    fn iret(&mut self) {
        self.emit_n(InstId::Iret as u32, &[]); // ANY [IMPLICIT]
    }
}

impl IretdEmitter for Assembler<'_> {
    fn iretd(&mut self) {
        self.emit_n(InstId::Iretd as u32, &[]); // ANY [IMPLICIT]
    }
}

impl IretqEmitter for Assembler<'_> {
    fn iretq(&mut self) {
        self.emit_n(InstId::Iretq as u32, &[]); // X64 [IMPLICIT]
    }
}

impl Jecxz1Emitter<Label> for Assembler<'_> {
    fn jecxz_1(&mut self, op0: Label) {
        self.emit_n(InstId::Jecxz as u32, &[op0.as_operand()]); // ANY [IMPLICIT] Short jump if CX/ECX/RCX is zero.
    }
}

impl<U0: Into<Imm>> Jecxz1Emitter<U0> for Assembler<'_> {
    fn jecxz_1(&mut self, op0: U0) {
        self.emit_n(InstId::Jecxz as u32, &[Into::<Imm>::into(op0).as_operand()]); // ANY [IMPLICIT] Short jump if CX/ECX/RCX is zero.
    }
}

impl Loop1Emitter<Label> for Assembler<'_> {
    fn loop_1(&mut self, op0: Label) {
        self.emit_n(InstId::Loop as u32, &[op0.as_operand()]); // ANY [IMPLICIT] Decrement xCX; short jump if xCX != 0.
    }
}

impl<U0: Into<Imm>> Loop1Emitter<U0> for Assembler<'_> {
    fn loop_1(&mut self, op0: U0) {
        self.emit_n(InstId::Loop as u32, &[Into::<Imm>::into(op0).as_operand()]); // ANY [IMPLICIT] Decrement xCX; short jump if xCX != 0.
    }
}

impl Loope1Emitter<Label> for Assembler<'_> {
    fn loope_1(&mut self, op0: Label) {
        self.emit_n(InstId::Loope as u32, &[op0.as_operand()]); // ANY [IMPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 1.
    }
}

impl<U0: Into<Imm>> Loope1Emitter<U0> for Assembler<'_> {
    fn loope_1(&mut self, op0: U0) {
        self.emit_n(InstId::Loope as u32, &[Into::<Imm>::into(op0).as_operand()]); // ANY [IMPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 1.
    }
}

impl Loopne1Emitter<Label> for Assembler<'_> {
    fn loopne_1(&mut self, op0: Label) {
        self.emit_n(InstId::Loopne as u32, &[op0.as_operand()]); // ANY [IMPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 0.
    }
}

impl<U0: Into<Imm>> Loopne1Emitter<U0> for Assembler<'_> {
    fn loopne_1(&mut self, op0: U0) {
        self.emit_n(
            InstId::Loopne as u32,
            &[Into::<Imm>::into(op0).as_operand()],
        ); // ANY [IMPLICIT] Decrement xCX; short jump if xCX != 0 && ZF == 0.
    }
}

impl Mul1Emitter<Gp> for Assembler<'_> {
    fn mul_1(&mut self, op0: Gp) {
        self.emit_n(InstId::Mul as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xDX:xAX <- xAX * r16|r32|r64}
    }
}

impl Mul1Emitter<GpbLo> for Assembler<'_> {
    fn mul_1(&mut self, op0: GpbLo) {
        self.emit_n(InstId::Mul as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xDX:xAX <- xAX * r16|r32|r64}
    }
}

impl Mul1Emitter<GpbHi> for Assembler<'_> {
    fn mul_1(&mut self, op0: GpbHi) {
        self.emit_n(InstId::Mul as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xDX:xAX <- xAX * r16|r32|r64}
    }
}

impl Mul1Emitter<Gpw> for Assembler<'_> {
    fn mul_1(&mut self, op0: Gpw) {
        self.emit_n(InstId::Mul as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xDX:xAX <- xAX * r16|r32|r64}
    }
}

impl Mul1Emitter<Gpd> for Assembler<'_> {
    fn mul_1(&mut self, op0: Gpd) {
        self.emit_n(InstId::Mul as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xDX:xAX <- xAX * r16|r32|r64}
    }
}

impl Mul1Emitter<Gpq> for Assembler<'_> {
    fn mul_1(&mut self, op0: Gpq) {
        self.emit_n(InstId::Mul as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AX <- AL * r8} {xDX:xAX <- xAX * r16|r32|r64}
    }
}

impl Mul1Emitter<Mem> for Assembler<'_> {
    fn mul_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Mul as u32, &[op0.as_operand()]); // ANY [IMPLICIT] {AX <- AL * m8} {xDX:xAX <- xAX * m16|m32|m64}
    }
}

impl RetEmitter for Assembler<'_> {
    fn ret(&mut self) {
        self.emit_n(InstId::Ret as u32, &[]);
    }
}

impl<U0: Into<Imm>> Ret1Emitter<U0> for Assembler<'_> {
    fn ret_1(&mut self, op0: U0) {
        self.emit_n(InstId::Ret as u32, &[Into::<Imm>::into(op0).as_operand()]);
    }
}

impl RetfEmitter for Assembler<'_> {
    fn retf(&mut self) {
        self.emit_n(InstId::Retf as u32, &[]);
    }
}

impl<U0: Into<Imm>> Retf1Emitter<U0> for Assembler<'_> {
    fn retf_1(&mut self, op0: U0) {
        self.emit_n(InstId::Retf as u32, &[Into::<Imm>::into(op0).as_operand()]);
    }
}

impl XlatbEmitter for Assembler<'_> {
    fn xlatb(&mut self) {
        self.emit_n(InstId::Xlatb as u32, &[]); // ANY [IMPLICIT]
    }
}

impl Aaa0Emitter for Assembler<'_> {
    fn aaa_0(&mut self) {
        self.emit_n(InstId::Aaa as u32, &[]); // X86 [IMPLICIT]
    }
}

impl<U0: Into<Imm>> Aad1Emitter<U0> for Assembler<'_> {
    fn aad_1(&mut self, op0: U0) {
        self.emit_n(InstId::Aad as u32, &[Into::<Imm>::into(op0).as_operand()]); // X86 [IMPLICIT]
    }
}

impl<U0: Into<Imm>> Aam1Emitter<U0> for Assembler<'_> {
    fn aam_1(&mut self, op0: U0) {
        self.emit_n(InstId::Aam as u32, &[Into::<Imm>::into(op0).as_operand()]); // X86 [IMPLICIT]
    }
}

impl Aas0Emitter for Assembler<'_> {
    fn aas_0(&mut self) {
        self.emit_n(InstId::Aas as u32, &[]); // X86 [IMPLICIT]
    }
}

impl Daa0Emitter for Assembler<'_> {
    fn daa_0(&mut self) {
        self.emit_n(InstId::Daa as u32, &[]); // X86 [IMPLICIT]
    }
}

impl Das0Emitter for Assembler<'_> {
    fn das_0(&mut self) {
        self.emit_n(InstId::Das as u32, &[]); // X86 [IMPLICIT]
    }
}

impl Lahf0Emitter for Assembler<'_> {
    fn lahf_0(&mut self) {
        self.emit_n(InstId::Lahf as u32, &[]); // LAHFSAHF [IMPLICIT] AH <- EFL
    }
}

impl Sahf0Emitter for Assembler<'_> {
    fn sahf_0(&mut self) {
        self.emit_n(InstId::Sahf as u32, &[]); // LAHFSAHF [IMPLICIT] EFL <- AH
    }
}

impl Cpuid0Emitter for Assembler<'_> {
    fn cpuid_0(&mut self) {
        self.emit_n(InstId::Cpuid as u32, &[]); // I486 [IMPLICIT] EAX:EBX:ECX:EDX <- CPUID[EAX:ECX]
    }
}

impl Clzero0Emitter for Assembler<'_> {
    fn clzero_0(&mut self) {
        self.emit_n(InstId::Clzero as u32, &[]); // CLZERO [IMPLICIT]
    }
}

impl Rdpru0Emitter for Assembler<'_> {
    fn rdpru_0(&mut self) {
        self.emit_n(InstId::Rdpru as u32, &[]); // RDPRU [IMPLICIT] EDX:EAX <- PRU[ECX]
    }
}

impl Rdpkru0Emitter for Assembler<'_> {
    fn rdpkru_0(&mut self) {
        self.emit_n(InstId::Rdpkru as u32, &[]); // RDPKRU [IMPLICIT] EDX:EAX <- PKRU[ECX]
    }
}

impl Rdtsc0Emitter for Assembler<'_> {
    fn rdtsc_0(&mut self) {
        self.emit_n(InstId::Rdtsc as u32, &[]); // RDTSC [IMPLICIT] EDX:EAX <- CNT
    }
}

impl Rdtscp0Emitter for Assembler<'_> {
    fn rdtscp_0(&mut self) {
        self.emit_n(InstId::Rdtscp as u32, &[]); // RDTSCP [IMPLICIT] EDX:EAX:EXC <- CNT
    }
}

impl Mulx3Emitter<Gp, Gp, Gp> for Assembler<'_> {
    fn mulx_3(&mut self, op0: Gp, op1: Gp, op2: Gp) {
        self.emit_n(
            InstId::Mulx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2 [IMPLICIT]
    }
}

impl Mulx3Emitter<Gpd, Gpd, Gpd> for Assembler<'_> {
    fn mulx_3(&mut self, op0: Gpd, op1: Gpd, op2: Gpd) {
        self.emit_n(
            InstId::Mulx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2 [IMPLICIT]
    }
}

impl Mulx3Emitter<Gpq, Gpq, Gpq> for Assembler<'_> {
    fn mulx_3(&mut self, op0: Gpq, op1: Gpq, op2: Gpq) {
        self.emit_n(
            InstId::Mulx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2 [IMPLICIT]
    }
}

impl Mulx3Emitter<Gp, Gp, Mem> for Assembler<'_> {
    fn mulx_3(&mut self, op0: Gp, op1: Gp, op2: Mem) {
        self.emit_n(
            InstId::Mulx as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // BMI2 [IMPLICIT]
    }
}

impl Xgetbv0Emitter for Assembler<'_> {
    fn xgetbv_0(&mut self) {
        self.emit_n(InstId::Xgetbv as u32, &[]); // XSAVE [IMPLICIT] EDX:EAX <- XCR[ECX]
    }
}

impl Xrstor1Emitter<Mem> for Assembler<'_> {
    fn xrstor_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Xrstor as u32, &[op0.as_operand()]); // XSAVE [IMPLICIT]
    }
}

impl Xrstor641Emitter<Mem> for Assembler<'_> {
    fn xrstor64_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Xrstor64 as u32, &[op0.as_operand()]); // XSAVE+X64 [IMPLICIT]
    }
}

impl Xrstors1Emitter<Mem> for Assembler<'_> {
    fn xrstors_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Xrstors as u32, &[op0.as_operand()]); // XSAVE [IMPLICIT]
    }
}

impl Xrstors641Emitter<Mem> for Assembler<'_> {
    fn xrstors64_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Xrstors64 as u32, &[op0.as_operand()]); // XSAVE+X64 [IMPLICIT]
    }
}

impl Xsave1Emitter<Mem> for Assembler<'_> {
    fn xsave_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Xsave as u32, &[op0.as_operand()]); // XSAVE [IMPLICIT]
    }
}

impl Xsave641Emitter<Mem> for Assembler<'_> {
    fn xsave64_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Xsave64 as u32, &[op0.as_operand()]); // XSAVE+X64 [IMPLICIT]
    }
}

impl Xsavec1Emitter<Mem> for Assembler<'_> {
    fn xsavec_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Xsavec as u32, &[op0.as_operand()]); // XSAVE [IMPLICIT]
    }
}

impl Xsavec641Emitter<Mem> for Assembler<'_> {
    fn xsavec64_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Xsavec64 as u32, &[op0.as_operand()]); // XSAVE+X64 [IMPLICIT]
    }
}

impl Xsaveopt1Emitter<Mem> for Assembler<'_> {
    fn xsaveopt_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Xsaveopt as u32, &[op0.as_operand()]); // XSAVE [IMPLICIT]
    }
}

impl Xsaveopt641Emitter<Mem> for Assembler<'_> {
    fn xsaveopt64_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Xsaveopt64 as u32, &[op0.as_operand()]); // XSAVE+X64 [IMPLICIT]
    }
}

impl Xsaves1Emitter<Mem> for Assembler<'_> {
    fn xsaves_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Xsaves as u32, &[op0.as_operand()]); // XSAVE [IMPLICIT]
    }
}

impl Xsaves641Emitter<Mem> for Assembler<'_> {
    fn xsaves64_1(&mut self, op0: Mem) {
        self.emit_n(InstId::Xsaves64 as u32, &[op0.as_operand()]); // XSAVE+X64 [IMPLICIT]
    }
}

impl SyscallEmitter for Assembler<'_> {
    fn syscall(&mut self) {
        self.emit_n(InstId::Syscall as u32, &[]); // X64 [IMPLICIT]
    }
}

impl SysenterEmitter for Assembler<'_> {
    fn sysenter(&mut self) {
        self.emit_n(InstId::Sysenter as u32, &[]); // X64 [IMPLICIT]
    }
}

impl Hreset1Emitter<Imm> for Assembler<'_> {
    fn hreset_1(&mut self, op0: Imm) {
        self.emit_n(InstId::Hreset as u32, &[op0.as_operand()]); // HRESET [IMPLICIT]
    }
}

impl SeamcallEmitter for Assembler<'_> {
    fn seamcall(&mut self) {
        self.emit_n(InstId::Seamcall as u32, &[]);
    }
}

impl SeamopsEmitter for Assembler<'_> {
    fn seamops(&mut self) {
        self.emit_n(InstId::Seamops as u32, &[]);
    }
}

impl SeamretEmitter for Assembler<'_> {
    fn seamret(&mut self) {
        self.emit_n(InstId::Seamret as u32, &[]);
    }
}

impl TdcallEmitter for Assembler<'_> {
    fn tdcall(&mut self) {
        self.emit_n(InstId::Tdcall as u32, &[]);
    }
}

impl PconfigEmitter for Assembler<'_> {
    fn pconfig(&mut self) {
        self.emit_n(InstId::Pconfig as u32, &[]); // PCONFIG [IMPLICIT]
    }
}

impl Rdmsr0Emitter for Assembler<'_> {
    fn rdmsr_0(&mut self) {
        self.emit_n(InstId::Rdmsr as u32, &[]); // ANY [IMPLICIT]
    }
}

impl Rdpmc0Emitter for Assembler<'_> {
    fn rdpmc_0(&mut self) {
        self.emit_n(InstId::Rdpmc as u32, &[]); // ANY [IMPLICIT]
    }
}

impl SysexitEmitter for Assembler<'_> {
    fn sysexit(&mut self) {
        self.emit_n(InstId::Sysexit as u32, &[]); // X64 [IMPLICIT]
    }
}

impl SysexitqEmitter for Assembler<'_> {
    fn sysexitq(&mut self) {
        self.emit_n(InstId::Sysexitq as u32, &[]); // X64 [IMPLICIT]
    }
}

impl SysretEmitter for Assembler<'_> {
    fn sysret(&mut self) {
        self.emit_n(InstId::Sysret as u32, &[]); // X64 [IMPLICIT]
    }
}

impl SysretqEmitter for Assembler<'_> {
    fn sysretq(&mut self) {
        self.emit_n(InstId::Sysretq as u32, &[]); // X64 [IMPLICIT]
    }
}

impl Wrmsr0Emitter for Assembler<'_> {
    fn wrmsr_0(&mut self) {
        self.emit_n(InstId::Wrmsr as u32, &[]); // ANY [IMPLICIT]
    }
}

impl Xsetbv0Emitter for Assembler<'_> {
    fn xsetbv_0(&mut self) {
        self.emit_n(InstId::Xsetbv as u32, &[]); // XSAVE [IMPLICIT] XCR[ECX] <- EDX:EAX
    }
}

impl Monitor0Emitter for Assembler<'_> {
    fn monitor_0(&mut self) {
        self.emit_n(InstId::Monitor as u32, &[]);
    }
}

impl Monitorx0Emitter for Assembler<'_> {
    fn monitorx_0(&mut self) {
        self.emit_n(InstId::Monitorx as u32, &[]);
    }
}

impl Mwait0Emitter for Assembler<'_> {
    fn mwait_0(&mut self) {
        self.emit_n(InstId::Mwait as u32, &[]);
    }
}

impl Mwaitx0Emitter for Assembler<'_> {
    fn mwaitx_0(&mut self) {
        self.emit_n(InstId::Mwaitx as u32, &[]);
    }
}

impl Tpause1Emitter<Gp> for Assembler<'_> {
    fn tpause_1(&mut self, op0: Gp) {
        self.emit_n(InstId::Tpause as u32, &[op0.as_operand()]);
    }
}

impl Tpause1Emitter<Gpd> for Assembler<'_> {
    fn tpause_1(&mut self, op0: Gpd) {
        self.emit_n(InstId::Tpause as u32, &[op0.as_operand()]);
    }
}

impl Umwait1Emitter<Gp> for Assembler<'_> {
    fn umwait_1(&mut self, op0: Gp) {
        self.emit_n(InstId::Umwait as u32, &[op0.as_operand()]);
    }
}

impl Umwait1Emitter<Gpd> for Assembler<'_> {
    fn umwait_1(&mut self, op0: Gpd) {
        self.emit_n(InstId::Umwait as u32, &[op0.as_operand()]);
    }
}

impl Blendvpd2Emitter<Vec, Vec> for Assembler<'_> {
    fn blendvpd_2(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Blendvpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Blendvpd2Emitter<Xmm, Xmm> for Assembler<'_> {
    fn blendvpd_2(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Blendvpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Blendvpd2Emitter<Vec, Mem> for Assembler<'_> {
    fn blendvpd_2(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Blendvpd as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Blendvps2Emitter<Vec, Vec> for Assembler<'_> {
    fn blendvps_2(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Blendvps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Blendvps2Emitter<Xmm, Xmm> for Assembler<'_> {
    fn blendvps_2(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Blendvps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Blendvps2Emitter<Vec, Mem> for Assembler<'_> {
    fn blendvps_2(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Blendvps as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Pblendvb2Emitter<Vec, Vec> for Assembler<'_> {
    fn pblendvb_2(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Pblendvb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Pblendvb2Emitter<Xmm, Xmm> for Assembler<'_> {
    fn pblendvb_2(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Pblendvb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Pblendvb2Emitter<Vec, Mem> for Assembler<'_> {
    fn pblendvb_2(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Pblendvb as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Maskmovq2Emitter<Mm, Mm> for Assembler<'_> {
    fn maskmovq_2(&mut self, op0: Mm, op1: Mm) {
        self.emit_n(
            InstId::Maskmovq as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE [IMPLICIT]
    }
}

impl Maskmovdqu2Emitter<Vec, Vec> for Assembler<'_> {
    fn maskmovdqu_2(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Maskmovdqu as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SSE2 [IMPLICIT]
    }
}

impl Pcmpestri3Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn pcmpestri_3(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Pcmpestri as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Pcmpestri3Emitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pcmpestri_3(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Pcmpestri as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Pcmpestrm3Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn pcmpestrm_3(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Pcmpestrm as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Pcmpestrm3Emitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pcmpestrm_3(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Pcmpestrm as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Pcmpistri3Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn pcmpistri_3(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Pcmpistri as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Pcmpistri3Emitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pcmpistri_3(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Pcmpistri as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Pcmpistrm3Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn pcmpistrm_3(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Pcmpistrm as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Pcmpistrm3Emitter<Vec, Mem, Imm> for Assembler<'_> {
    fn pcmpistrm_3(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Pcmpistrm as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // SSE4_1 [IMPLICIT]
    }
}

impl Sha256rnds22Emitter<Vec, Vec> for Assembler<'_> {
    fn sha256rnds2_2(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Sha256rnds2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA [IMPLICIT]
    }
}

impl Sha256rnds22Emitter<Xmm, Xmm> for Assembler<'_> {
    fn sha256rnds2_2(&mut self, op0: Xmm, op1: Xmm) {
        self.emit_n(
            InstId::Sha256rnds2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA [IMPLICIT]
    }
}

impl Sha256rnds22Emitter<Vec, Mem> for Assembler<'_> {
    fn sha256rnds2_2(&mut self, op0: Vec, op1: Mem) {
        self.emit_n(
            InstId::Sha256rnds2 as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // SHA [IMPLICIT]
    }
}

impl Vmaskmovdqu2Emitter<Vec, Vec> for Assembler<'_> {
    fn vmaskmovdqu_2(&mut self, op0: Vec, op1: Vec) {
        self.emit_n(
            InstId::Vmaskmovdqu as u32,
            &[op0.as_operand(), op1.as_operand()],
        ); // AVX [IMPLICIT]
    }
}

impl Vpcmpestri3Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpestri_3(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpcmpestri as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX [IMPLICIT]
    }
}

impl Vpcmpestri3Emitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpestri_3(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpcmpestri as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX [IMPLICIT]
    }
}

impl Vpcmpestrm3Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpestrm_3(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpcmpestrm as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX [IMPLICIT]
    }
}

impl Vpcmpestrm3Emitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpestrm_3(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpcmpestrm as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX [IMPLICIT]
    }
}

impl Vpcmpistri3Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpistri_3(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpcmpistri as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX [IMPLICIT]
    }
}

impl Vpcmpistri3Emitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpistri_3(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpcmpistri as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX [IMPLICIT]
    }
}

impl Vpcmpistrm3Emitter<Vec, Vec, Imm> for Assembler<'_> {
    fn vpcmpistrm_3(&mut self, op0: Vec, op1: Vec, op2: Imm) {
        self.emit_n(
            InstId::Vpcmpistrm as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX [IMPLICIT]
    }
}

impl Vpcmpistrm3Emitter<Vec, Mem, Imm> for Assembler<'_> {
    fn vpcmpistrm_3(&mut self, op0: Vec, op1: Mem, op2: Imm) {
        self.emit_n(
            InstId::Vpcmpistrm as u32,
            &[op0.as_operand(), op1.as_operand(), op2.as_operand()],
        ); // AVX [IMPLICIT]
    }
}
